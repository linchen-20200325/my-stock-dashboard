# HANDOFF — `my-stock-dashboard` 專案交接

━━━━━━━━━━━━━━━━━━━━━━
【給下一個 AI 的說明】
━━━━━━━━━━━━━━━━━━━━━━

你在接手一個進行中的專案。這份交接本是唯一真相源。
請先做三件事：

1. 讀完這份交接本全文
2. 讀「常設規矩」那一節（在最後）
3. 讀進度總表，知道自己現在在哪

【你的責任】
從你接手的那一刻起，這份交接本是你的。
你每完成一件事、每次派工、每次 commit、每次停下回報，
都要同步更新它。

不更新 = 下一個 AI 又要重問 = 我浪費時間。

【更新觸發點】
① 派工出去時 → 寫「已派工 X，等回報」
② 收到回報時 → 「派工中」改「稽核中」
③ commit 前 → 該項移到「已完成」
④ 每則回覆停點前 → 檢查一輪
⑤ 收到我新指令時 → 先讀交接本，再動作

【每則回覆結尾加一行】
📓 交接本：已更新 <哪一節> ／ 無變動

【不准什麼都問我】
只有三類可問：動錢／動凍結檔／開新頁。
其餘自決，寫進交接本。

━━━━━━━━━━━━━━━━━━━━━━

（客戶 2026-09-26 頒布；本節永遠在第一頁，不准刪、不准改寫、不准往下移）

---

> **建立日期 2026-09-24**（本版為**全文重寫**；舊版只交接了「融資餘額燈」一條支線，
> 沒寫專案是什麼、文件該讀哪些、v2 UI 主線做到哪、環境有什麼坑）。
>
> **本檔的讀者是「一個完全沒有任何上下文的 AI」。**
> 因此：
> - 每個宣稱都附 **檔案路徑 + 符號名**，讓你能自己查證；依 `CLAUDE.md` §8.2.A.0 規則 1，**本檔不寫行號**。
> - 會漂移的量測值一律標「（量測日 2026-09-24）」或改寫成「請現場執行 `<指令>`」（§8.2.A.0 規則 4）。
> - **實測事實**與**未經第二組驗的判斷**分開標示，後者逐條標 ⚠️（§-2 規則 6）。
> - 專有名詞第一次出現時用一句話解釋。
>
> ⛔ **不得把本檔任何全稱句（「只有…」「全部…」「沒有其他…」）當成既定前提。**
> 本檔多數盤點是**單組**做的（見 §8 誠實揭露表），接手請自行複驗。

---

## ★ 進度總表／未完成項目（唯一清單，每輪開工先讀）

> 📌 **清點日 2026-09-26，基準 `origin/main` `84f58ea`**（PR #698 merge；前兩版基準 `07f8800` → `f14c5fd`）。開發線 `claude/stock-dashboard-handoff-g9dtm0` `7f87f41`（＝ PR #675 head）。
> 📌 **2026-09-26 更新**：`origin/main` 已前進到 **`bd6ceec`**（PR #699 merge ＝ **S5-H `hold.vix` 已完**）；下方各處「已對 `origin/main` `84f58ea` 核對」仍指當時基準，未重核。
> 📌 **2026-09-26 再更新**：`origin/main` 已前進到 **`3f4493c`**（PR #700 merge ＝ **S5-W `why.qa` 已完**）；**S5-F** 稽核通過、~~**PR #701 待合併**（等 CI）~~（→ 下行：已 merge）。
> 📌 **2026-09-26 三更新**：`origin/main` 已前進到 **`2810064`**（完整 `281006489e60909b03aec07e7005cea52063d79a`；PR #701 merge ＝ **S5-F `find.screen_result` 已完**；CI「Fast checks」「Slow tests」check run 皆 success —— 編修組經 GitHub API 實測）。~~**Q1** 獨立 QA 通過、PR 準備中（未開）~~（→ 下行：已 merge #702）；**Q2** 已派工；**Q5** 獨立 QA 回報 1 項阻擋、發回實作組修正中。
> 📌 **2026-09-26 四更新**：`origin/main` 已前進到 **`0769730`**（完整 `07697303bdbdd49d3c0fe50d53a2364cb4c32c96`；PR #702 merge ＝ **Q1 `NOT_BOUND_NOW` 已完**；PR head `2705861`；CI「Fast checks」「Slow tests」check run 皆 success —— 編修組經 GitHub API 實測）。**Q5** 修正完成 → 搬主線解衝突中 → 待 QA 複驗；**Q2** 實作中；**B9 ⑥～⑧** 已派工、實作中；**C6** 分支查證已完（刪分支另登 **C6-del**，卡住待客戶）。
> 📌 **2026-09-26 五更新**：`origin/main` 已前進到 **`76bec1b`**（完整 `76bec1b699faca3af3780461ef7351c4b0fa03e3`；PR #703 merge ＝ **Q5 查一檔各卡靜默失敗 已完**；PR head `185392e`；CI「Fast checks」「Slow tests」check run 皆 success —— 編修組經 GitHub API 實測）。**Q2** 實作完成（**S4-T 併入 Q2**）→ 稽核中（獨立 QA）；**B9 ⑥⑦** 實作完成 → 稽核中（獨立 QA）；**B9 ⑧** 跳過 → 卡住；**Q3** 已派工、實作中；新登記 **B9 ⑩**、**Q2-r1**、**Q2-r2**（待辦）；每 5 項自檢本次執行（見「已完成（近期）」表下）。
> 📌 **2026-09-26 六更新**：`origin/main` 已前進到 **`45d5385`**（完整 `45d538517ef59c1bdf47b93bf87e707307322502`；PR #704 merge ＝ **Q2（含 S4-T）已完**；PR head `0c7fa54`；CI「Fast checks」「Slow tests」check run 皆 success —— 編修組經 GitHub API 實測）。**B9 ⑥⑦** 獨立 QA 抓到 1 項阻擋（趨勢因子 where）→ 已修 → **複驗中**；**Q3** 實作完成 → **稽核中（獨立 QA）**；新登記 **B9 ⑪⑫⑬**、**Q2-r3**、**Q3-r1～r5**（待辦）；**Q2-r1** 補 QA 實測證據（矛盾已確認，維持待辦）；**Q2-r2** 經 QA 查證改「低優先，僅登記」；每 5 項自檢計數器：距下次 4 項（見「已完成（近期）」表下）。
> 📌 客戶 2026-09-26【交接表 · 維護時機】所稱「**HANDOFF 第一節**」＝**本節**（進度總表）；檔首【給下一個 AI 的說明】是客戶頒布的導讀，不是登記本 —— ⚠️ 總管判讀。本節末尾固定附「**給下一個 AI**」段。
> ⭐ **規則（客戶 2026-09-26）**：每一輪**先讀本節**；發現過期 → **先更新本節再動工**；項目結案 → 移到文末「已完成（近期）」並附 merge hash。
> 📌 **2026-09-26 清點輪（客戶令）**：下方 (a)(b)(c) 三張主表改為**全量清點、四欄**（含已完列），**主表即現行真相源**；B3／B6 子表保留為**明細**，文末「已完成（近期）」照舊保留作流水。
> 📌 **2026-09-26 客戶令 —— 解除清點輪暫停**（原文節錄，總管轉述）：「**清點已完，直接從清單挑下一項做…全部待辦做完 or 只剩卡住的，才停一次回報**」⇒ 清點令（「⛔ 不修 code、⛔ 不開新工作，只清點」）的**暫停已解除**；S5-H／S5-W／S5-F 三軌已派工恢復（見 (d)），其後順序見文末「給下一個 AI」第 1～2 點。
> ⛔ **全 repo 只有這一份現行待辦清單**；本檔其他段落（§3.2、§6.4、§6.5 等）的待辦／待裁表**保留為歷史紀錄**，一律以本節為準。
> 「誰決定」＝客戶 or 總管；標 ⚠️ 者為**總管判讀**，客戶一句話可推翻。
> ⚠️ **「誰決定」欄寫於客戶 2026-09-26「只有三類可問：動錢／動凍結檔／開新頁；其餘自決」新令之前**：標「客戶」的列，凡不屬這三類者，依新令改為**內部自決並寫進本檔** —— 本輪**未逐列改寫**；新令與 K1、`CLAUDE.md` §-1.5.D v3 §03-2 的字面衝突 **⚠️ 待確認**（見文末「常設規矩」§2）。
> ⚠️ 本清單由**單組**彙整（§-2 規則 6），⛔ 不得當成「repo 已無其他待辦」的全稱前提（未納入範圍見本節「清點盲點」）。

**狀態定義**
- **已完**：已 merge 進 `origin/main`（附 hash）；或已定案「不做」並留理由（標「定案不做」）。
- **進行中**：有未提交的工作區改動（~~本輪全部依客戶清點令**暫停**，未 QA、未提交~~ → 2026-09-26 客戶令已解除暫停，見上；現況未 QA、未提交）；(d) 的「**派工中**」列亦計入此欄（2026-09-26 補：(d) 的「**稽核中**」列同計入此欄）。
- **待辦**：已登記、尚未動工；**不卡在**客戶的內容決定（規格／文案／新源／token／二擇一）—— 可能仍需客戶排程或特許動 L1／L3，或依 §-1 碰到該檔才做。
- **卡住**：卡在客戶的內容決定，或卡在實機／線上環境（沙箱驗不到）。
- **⚠️單組**：該列結論（狀態判定、原因、key↔常數對映或分類）只經一組、未經第二組獨立驗；無此標者＝有 merge hash 或已有兩組獨立複查。

**計數**（清點日 2026-09-26；編修組清點，**2026-09-26 二組複核更正**：B7a 待辦→已完、新增 DOC2 待辦；其餘列數與狀態二組逐列重算一致；commit 前依更新觸發點 ③ 將 (d)「清點交接本」稽核中→已完）；⚠️ 單組 —— 現行數字為 2026-09-26 ~~三更新~~ ~~四更新~~ ~~五更新~~ **六更新**逐列重數（方法見表下）

| 類別 | 已完 | 進行中 | 待辦 | 卡住 | 合計 |
|---|---|---|---|---|---|
| (a) 五頁卡化 | 11 | 0 | 0 | 1 | 12 |
| (b) 燈號「變化方向」（16 盞） | 7（真方向 5＋定案不做 2） | 0 | 0 | 9 | 16 |
| (c) 其他 | ~~7~~ ~~8~~ ~~9~~ ~~10~~ ~~11~~ ~~13~~ ~~14~~ **15**（含定案不做 2；+S5-H；+S5-W；+S5-F；+Q1；+C6 查證；+Q5；+Q2） | ~~3~~ ~~4~~ ~~3~~ ~~3~~ ~~5~~ ~~4~~ **3**（Q3／B9 ⑥⑦） | ~~10~~ ~~8~~ ~~13~~ ~~17~~ ~~13~~ ~~13~~ **22**（含 Q1-r2～r5、Q2-r1～r3、Q3-r1～r5、Q5-r1、B9 ⑩～⑬） | ~~12~~ ~~13~~ ~~15~~ ~~16~~ **18**（含 Q1-r1、Q5-r2、C6-del、B9 ⑧⑨） | ~~32~~ ~~33~~ ~~34~~ ~~39~~ ~~46~~ ~~47~~ ~~49~~ **58** |
| (d) 派工追蹤 | ~~1~~ ~~2~~ ~~3~~ ~~4~~ ~~5~~ ~~6~~ **7**（+S5-H；+S5-W；+S5-F；+Q1；+Q5；+Q2） | ~~0~~ ~~3~~ ~~4~~ ~~3~~ ~~3~~ ~~3~~ ~~3~~ **2**（B9 ⑥⑦ 複驗中；Q3 稽核中） | 0 | 0 | ~~1~~ ~~4~~ ~~6~~ ~~7~~ ~~8~~ **9** |
| **合計** | ~~25~~ ~~27~~ ~~29~~ ~~31~~ ~~33~~ ~~36~~ ~~38~~ **40** | ~~**3**~~ ~~6~~ ~~8~~ ~~6~~ ~~6~~ ~~8~~ ~~7~~ **5** | ~~10~~ ~~8~~ ~~13~~ ~~17~~ ~~13~~ ~~13~~ **22** | ~~22~~ ~~23~~ ~~25~~ ~~26~~ **28** | ~~61~~ ~~62~~ ~~65~~ ~~68~~ ~~73~~ ~~81~~ ~~83~~ ~~86~~ **95** |

📌 **2026-09-26 更新**（S5-H 已完 #699 `bd6ceec`；派工 Q1／Q5；新登記 B9 ⑤）：編修組依上列異動重算（(a)(b) 無異動），單組。
📌 **2026-09-26 再更新**（S5-W 已完 #700 `3f4493c`；S5-F PR #701 待合併；Q1／Q5 稽核中；新登記 B9 ⑥⑦⑧⑨〔S5-F QA 非阻擋殘留〕＋ S4-T〔停利卡 why 句〕共 5 列待辦）：編修組依上列異動重算（(a)(b) 無異動），單組。
📌 **2026-09-26 三更新**（S5-F 已完 #701 `2810064`；Q1 稽核通過、PR 準備中；Q2 已派工；Q5 發回修正中；新登記 Q1-r1～r5、Q5-r1、Q5-r2 共 7 列）：**逐列重數，未用加減推** —— 方法：以腳本逐列讀 (a)(b)(c)(d) 四張主表每一列的「狀態」欄，去掉 `~~…~~` 刪除線內的舊字後取第一個狀態詞（已完／進行中／待辦／卡住；(d) 的「派工中」「稽核中」歸進行中），指向列 B9 ①②排除；(a) 12、(b) 16、(c) 46、(d) 7 列，合計 81。⚠️ 單組（編修組一組重數，未經第二組驗；狀態歸類沿用各列現有文字）。
📌 **2026-09-26 四更新**（Q1 已完 #702 `0769730`；C6 查證已完；新登記 **C6-del**〔卡住〕；Q5 修正完成 → 搬主線解衝突中；B9 ⑥～⑧ 已派工〔(c) 3 列待辦→進行中、(d) 新增 1 列〕）：**逐列重數，未用加減推** —— 沿用上次腳本方法（逐列讀四張主表「狀態」欄，去掉 `~~…~~` 後取第一個狀態詞，「派工中」「稽核中」歸進行中，B9 ①② 指向列排除；同一腳本對上一版重跑得 81，與上次一致）；(a) 12、(b) 16、(c) 47、(d) 8 列，合計 **83**＝已完 36／進行中 8／待辦 13／卡住 26。⚠️ 單組（編修組一組重數，未經第二組驗；狀態歸類沿用各列現有文字）。
📌 **2026-09-26 五更新**（Q5 已完 #703 `76bec1b`；Q2 實作完成 → 稽核中〔**S4-T 併入 Q2**〕；B9 ⑥⑦ 稽核中、⑧ 跳過 → 卡住、⑨ 依總管改卡住；Q3 已派工〔(c) 待辦→進行中、(d) 新增 1 列〕；新登記 **B9 ⑩**、**Q2-r1**、**Q2-r2**〔待辦〕）：**逐列重數，未用加減推** —— 沿用上次腳本方法（逐列讀四張主表「狀態」欄，去掉 `~~…~~` 後取第一個狀態詞，「派工中」「稽核中」歸進行中；同一腳本對上一版〔`79e52af`〕重跑得 83，與上次一致）；**本次新增一條指向列：S4-T**（→ 併入 Q2，比照 B9 ① → 併入 S5-F 不計；⚠️ 編修組判讀 —— 若改計入，(c) 50、進行中 8、合計 87）；(a) 12、(b) 16、(c) 49、(d) 9 列，合計 **86**＝已完 38／進行中 7／待辦 13／卡住 28。⚠️ 單組（編修組一組重數，未經第二組驗；狀態歸類沿用各列現有文字）。
📌 **2026-09-26 六更新**（Q2〔含 S4-T〕已完 #704 `45d5385`〔(c)(d) 各 1 列進行中→已完〕；B9 ⑥⑦ QA 1 阻擋已修 → 複驗中〔仍進行中〕；Q3 實作完成 → 稽核中〔仍進行中〕；新登記 **B9 ⑪⑫⑬**、**Q2-r3**、**Q3-r1～r5** 共 9 列〔待辦〕；Q2-r2 改「低優先，僅登記」〔仍計待辦〕）：**逐列重數，未用加減推** —— 同一腳本（逐列讀四張主表「狀態」欄，去掉 `~~…~~` 後取第一個狀態詞，「派工中」「稽核中」歸進行中；B9 ①②、S4-T 三條指向列排除；同一腳本對上一版〔`6380f12`〕重跑得 86＝38／7／13／28，與上次一致）；(a) 12、(b) 16、(c) 58、(d) 9 列，合計 **95**＝已完 40／進行中 5／待辦 22／卡住 28。S4-T 仍為指向列（→ 併入 Q2，Q2 已完）；⚠️ 若改計入，(c) 59、已完 41、合計 96（編修組判讀）。⚠️ 單組（編修組一組重數，未經第二組驗；狀態歸類沿用各列現有文字）。

計數規則：(c) 的 B9 ①（→ 併入 S5-F）與 B9 ②（→ S4 結案）~~是~~、以及 2026-09-26 五更新起的 **S4-T**（→ 併入 Q2）是**指向列、不計**；其餘每列計 1。⚠️ (d) 的派工列與 (c) c-1 的同名列追的是**同一份工作**（(d) 追派工進度、(c) 追項目本身），依 (d) 慣例各計 1 ⇒ ~~「進行中 6」**不是 6 件獨立工作**，實為 3 件~~ → ~~「進行中 8」**不是 8 件獨立工作**，實為 4 件（S5-W／S5-F／Q1／Q5）~~ →~~「進行中 6」**不是 6 件獨立工作**，實為 3 件（S5-F／Q1／Q5）；「已完」中 S5-H、S5-W 同樣在 (c)(d) 各計 1。~~ ~~→（2026-09-26 三更新）「進行中 6」**不是 6 件獨立工作**，實為 3 件（Q1／Q2／Q5）；「已完」中 S5-H、S5-W、S5-F 同樣在 (c)(d) 各計 1。~~ → ~~（2026-09-26 四更新）「進行中 8」**不是 8 件獨立工作**，實為 3 件（Q2／Q5／B9 ⑥～⑧ —— 後者在 (c) 佔 3 列、(d) 佔 1 列，屬同一份派工）；「已完」中 S5-H、S5-W、S5-F、Q1 同樣在 (c)(d) 各計 1。~~ ~~→（2026-09-26 五更新）「進行中 7」**不是 7 件獨立工作**，實為 3 件（Q2〔含 S4-T〕／Q3／B9 ⑥⑦ —— Q2、Q3 各在 (c)(d) 佔 1 列，B9 ⑥⑦ 在 (c) 佔 2 列、(d) 佔 1 列）；「已完」中 S5-H、S5-W、S5-F、Q1、Q5 同樣在 (c)(d) 各計 1。~~ →（2026-09-26 六更新）「進行中 5」**不是 5 件獨立工作**，實為 2 件（Q3〔(c)(d) 各佔 1 列〕／B9 ⑥⑦〔(c) 佔 2 列、(d) 佔 1 列〕）；「已完」中 S5-H、S5-W、S5-F、Q1、Q5、Q2 同樣在 (c)(d) 各計 1。

**(d) 派工追蹤**（依檔首【給下一個 AI 的說明】更新觸發點 ①②③：派工 →「已派工 X，等回報」；收到回報 →「稽核中」；commit 前 → 移到「已完成」）

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| 清點交接本（本輪） | **已完**（~~稽核中~~；編修組交回 → 第二組獨立複驗交回、更正已寫入本檔〔各處標「2026-09-26 二組複核更正」〕→ 總管複驗接受 → commit） | 見本 commit（docs(handoff): 全面清點…；未走 PR，本行無法自引 hash） | 無。本檔＋`docs/v2/wip_patches/` 同一 commit 入庫。總管 |
| 已派工 S5-H hold.vix 恢復（從 msd-h／patch 續做）~~，等回報~~ | **已完**（~~派工中~~ → 回報 → 獨立 QA 通過 → merge；2026-09-26；已移「已完成（近期）」） | #699 `bd6ceec` | 無。細節見 (c) c-1 S5-H 列；非阻擋殘留（文案）→ **B9 ⑤**。~~起點：工作區 `/home/user/msd-h` 或 `docs/v2/wip_patches/S5-H_84f58ea.patch`（在 `84f58ea` 上 `git apply`）。回報後 → 稽核中 → 獨立 QA → merge → 移「已完成」附 hash。~~ 授權：批 5（見 c-1 表下 📌）＋本節檔首解除暫停令。總管 |
| 已派工 S5-W why.qa 恢復~~，等回報~~ | **已完**（~~稽核中：獨立 QA 抓到 blocker〔「`STOP`＋無 `parts` 顯示成紅」〕，修正中~~ → blocker 已修 → 第二輪獨立 QA 通過 → merge；2026-09-26；已移「已完成（近期）」） | #700 `3f4493c` | 無。細節見 (c) c-1 S5-W 列。~~修掉 blocker → 重跑獨立 QA → merge → 移「已完成」附 hash。起點：`/home/user/msd-w` 或 `docs/v2/wip_patches/S5-W_84f58ea.patch`（修正中的改動位置以執行 AI 回報為準，本編修組未查證）。~~ 總管 |
| 已派工 S5-F find.screen_result 恢復~~，等回報~~ | **已完**（~~稽核中 → PR #701 待合併~~；~~派工中~~ → ~~獨立 QA 中~~ → 稽核通過 → PR #701 → CI 綠 → merge；2026-09-26；已移「已完成（近期）」） | #701 `2810064` | 無。細節見 (c) c-1 S5-F 列。QA 非阻擋殘留 **B9 ⑥⑦⑧⑨**（(c) c-2，待辦；收尾前提「PR #701 merge 後」已成立）。~~CI 綠 → merge → **立刻**移「已完成」附 hash。~~ ~~起點：`/home/user/msd-f` 或 `docs/v2/wip_patches/S5-F_84f58ea.patch`（含 L1 `yield_pe_fetcher.py`，在批 5 授權射程內；**尚無測試**〔派工前狀態；現況以 QA 回報為準〕）。~~ 總管 |
| 已派工 Q5 查一檔各卡~~，等回報~~ | **已完**（~~**進行中（修正完成 → 搬主線解衝突中 → 待 QA 複驗）**~~；~~派工中~~ → 稽核中 → 2026-09-26 獨立 QA 回報 **1 項阻擋**〔B1〕→ ~~發回實作組修正中~~ → 實作組修好 B1 ＋ 3 個測試缺口 → 搬主線解衝突 → **原 QA 組複驗通過、無阻擋**〔單組 QA〕→ PR #703 → CI 綠 → merge；2026-09-26；已移「已完成（近期）」） | #703 `76bec1b`（完整 `76bec1b699faca3af3780461ef7351c4b0fa03e3`；PR head `185392e`） | ⭐ **2026-09-26 五更新**：無。細節見 (c) c-1 Q5 列；「2 處註解言過其實」經原 QA 組複驗**已修正且據實**（總管轉述）⇒ 原 ⚠️ 待確認**解除**。已知限制照登：估值卡現價、ETF 溢價兩條路徑在 production 紅路徑**少觸發**（⛔ 不得對客戶宣稱已修）。非阻擋殘留 **Q5-r1**（待辦）／**Q5-r2**（卡住）。總管 ｜~~📌 **2026-09-26 四更新**：工作區 `/home/user/msd-q5`（基底 `origin/main` `2810064`），正在解 `src/services/dividend_station_service.py` 與 #699 的衝突 → 送**原 QA 組**複驗 → PR → CI 綠 → merge → 移「已完成」附 hash。「2 處註解言過其實」本次回報未提及是否已修 ⇒ ⚠️ 待確認（QA 複驗時一併看）。~~ ~~修掉 B1 ＋ 3 個測試缺口 ＋ 2 處註解言過其實 → 重跑獨立 QA → PR → CI 綠 → merge → 移「已完成」附 hash。~~ ~~細節見 (c) c-1 Q5 列；非阻擋新待辦 **Q5-r1／Q5-r2**（c-2）。~~ 授權：批 5（見 c-1 表下 2026-09-26 總管判讀）。總管 |
| 已派工 Q1 NOT_BOUND token 吞錯~~，等回報~~ | **已完**（~~稽核中 → 稽核通過、PR 準備中~~；~~派工中~~ → 稽核中 → 2026-09-26 獨立 QA **通過、無阻擋**〔單組 QA〕→ PR #702 → CI 綠 → merge；2026-09-26；已移「已完成（近期）」） | #702 `0769730`（完整 `07697303bdbdd49d3c0fe50d53a2364cb4c32c96`；PR head `2705861`） | 無。細節見 (c) c-1 Q1 列。QA 非阻擋殘留 **Q1-r1～r5**（c-2）。~~開 PR → CI 綠 → merge → **立刻**移「已完成」附 hash。~~（持股頁 `NOT_BOUND_NOW`：token／sheet-id 讀取錯誤被吞成「未綁定」）。總管 |
| 已派工 Q2 `hold.alloc_split`／`hold.deep.core_satellite` 全部失敗仍落空態（2026-09-26 起**含 S4-T**）~~，等回報~~ | **已完**（~~**稽核中（獨立 QA）**~~（~~**派工中（實作中）**~~；2026-09-26 派工 → 2026-09-26 實作完成、回報 → 稽核中；~~未 merge~~）→ 獨立 QA **通過、無阻擋**〔單組 QA〕→ PR #704 → CI 綠 → merge；2026-09-26；已移「已完成（近期）」） | #704 `45d5385`（完整 `45d538517ef59c1bdf47b93bf87e707307322502`；PR head `0c7fa54`） | ⭐ **2026-09-26 六更新**：無。細節見 (c) c-1 Q2 列（**S4-T 隨本 PR 結案**）。獨立 QA 通過、無阻擋（單組；p04 窮舉 50,481 張卡對 baseline 只有預期 8 張不同 —— 總管轉述，編修組未重跑）。非阻擋殘留 **Q2-r1**（QA 實測確認矛盾，待辦）／**Q2-r2**（低優先，僅登記）／**Q2-r3**（新登記，待辦）（c-2）。總管 ｜⭐ **2026-09-26 五更新（總管轉述，編修組未讀 diff 內容）**：實作只動 **L5 `src/ui/views/page_hold.py`**（L3／L1 未動）＋測試；**S4-T 併入 Q2**（總管裁定）：分配卡 ⑤⑥ 與停利卡共用新常數 `NO_PRICE_WHY`（把 why 截在「，」、去掉「可以重跑一次」）；紅態不再渲染「⚠️ 這個比例只涵蓋一部分」。編修組實測工作區 `/home/user/msd-q2`（HEAD `623e326`）未提交改動：`src/ui/views/page_hold.py`（含 `NO_PRICE_WHY` 定義）、`tests/test_p04_hold_v2_cards.py`、`tests/test_v2_silent_fail_b4.py`，未追蹤 `tests/test_v2_silent_fail_b8_alloc.py`、`tests/test_v2_silent_fail_b7_binding.py`（＝#702 版）。~~下一步：獨立 QA → PR → CI 綠 → merge → 移「已完成」附 hash。~~ 新登記非阻擋待辦 **Q2-r1／Q2-r2**（c-2）。｜~~回報 → 稽核中 → 獨立 QA → PR → CI 綠 → merge → 移「已完成」附 hash。~~ 工作區 `/home/user/msd-q2`（基底＝`origin/main` `3f4493c` ＋ Q1 本地 commit；編修組實測 HEAD `623e326`「local: Q1 base (not pushed)」，其父＝`3f4493c`）。📌 **2026-09-26 四更新（編修組實測）**：`623e326` 的 4 個 Q1 原始碼檔與 #702 merge 版**逐 blob 相同**，工作區未追蹤的 `tests/test_v2_silent_fail_b7_binding.py` 亦與 #702 版相同 ⇒ 搬到 `origin/main` `0769730` 時 Q1 部分不應再產生內容差異（⚠️ 衝突與否以實際 rebase 為準）。授權：批 5（見 c-1 表下 2026-09-26 總管判讀）。總管 |
| 已派工 B9 ~~⑥～⑧~~ **⑥⑦** S5-F QA 非阻擋殘留收尾（`page_find.py` 紅卡 where 句／「這輪失敗」用語~~／`RuntimeError()` 事實列~~）~~，等回報~~ | **稽核中（複驗中）**（~~**稽核中（獨立 QA）**~~（~~**派工中（實作中）**~~；2026-09-26 派工 → 2026-09-26 ⑥⑦ 實作完成、回報 → 稽核中；**⑧ 跳過 → 卡住**，見 c-2；未 merge）→ 2026-09-26 獨立 QA 抓到 **1 項阻擋**（趨勢因子 where）→ 已修 → **複驗中**；未 merge） | —（待合併） | ⭐ **2026-09-26 六更新（總管轉述；因子→where 對映經編修組 grep 工作區確認）**：獨立 QA（單組）抓到 **1 項阻擋** —— 五更新的「⑥ 趨勢因子 where → `SNAPSHOT_WAIT_WHERE`（要等排程補抓）」在實際可達情境**幾乎都錯**：趨勢卡出現時多是舊季 parquet 損毀／schema 漂移，排程只抓本季＋去年同季、不會重寫舊季 ⇒ **已修**：改指 `SCREEN_NO_PARTIAL_WHERE`（編修組實測 `/home/user/msd-b9` 的 `src/ui/views/page_find.py`：`trend`／`rs_leader`／PE 因子皆對映 `SCREEN_NO_PARTIAL_WHERE`）；另補 M7 行為測試、修正缺貨來源註解 → **複驗中**。下一步：複驗通過 → PR → CI 綠 → merge → 移「已完成」附 hash。QA 另登 **B9 ⑪⑫⑬**（c-2，待辦）、並核實 **B9 ⑩**。總管 ｜⭐ **2026-09-26 五更新（總管轉述，編修組未讀 diff 內容）**：⑥ ~~趨勢因子 where → `SNAPSHOT_WAIT_WHERE`；~~（→ 六更新：QA 阻擋，已改 `SCREEN_NO_PARTIAL_WHERE`）估值／RS 因子 where 只刪到剩「本站不以殘缺資料湊出名單」；⑦ 刪「這輪」。⑧ 未做（「跨季趨勢為空表」只存在 L3 log，上畫面＝新文案，需客戶定措辭）。~~下一步：獨立 QA → PR → CI 綠 → merge → 移「已完成」附 hash。~~ 實作組讀碼另發現 **B9 ⑩**（c-2，待辦）。｜~~回報 → 稽核中 → 獨立 QA → PR → CI 綠 → merge → 移「已完成」附 hash。~~ 工作區 `/home/user/msd-b9`（基底 `origin/main` `2810064`；編修組實測當下未提交改動：`src/ui/views/page_find.py`、`tests/test_v2_silent_fail_b5_find.py`）。**B9 ⑨** 不在本次派工（~~仍待辦~~ → 2026-09-26 五更新：維持規格洞 ⇒ **卡住**）。細節見 (c) c-2 B9 ⑥～⑧ 列。總管 |
| 已派工 Q3 `hold.deep.stress`／`hold.deep.var`／`hold.deep.dividend_cash` 持股深度分析三卡「算不出來」~~，等回報~~ | **稽核中（獨立 QA）**（~~**派工中（實作中）**~~（2026-09-26）→ 2026-09-26 實作完成、回報 → 稽核中；未 merge） | —（待合併） | ⭐ **2026-09-26 六更新（總管轉述；檔名為編修組 `git status` 實測，未讀 diff 內容）**：實作完成 —— L1 `src/data/etf/etf_fetch.py`（失敗時回的空 df 掛 `attrs` 旗標＋`fetch_etf_price(failed=)`）、L3 `src/services/portfolio_deep_service.py`（`get_portfolio_var(strict=)` 等加性參數）、L5 `src/ui/views/page_hold.py`；測試改 `tests/test_p04_hold_v2_cards.py`、`tests/test_p04_hold_view.py`、`tests/test_v2_silent_fail_b3.py`、`tests/test_v2_silent_fail_b7_binding.py`，新檔 `tests/test_v2_silent_fail_b9_deep.py`（未追蹤）→ **稽核中（獨立 QA）**。下一步：獨立 QA → 搬到 `origin/main` `45d5385` → PR → CI 綠 → merge → 移「已完成」附 hash。基底對齊（編修組實測）：`b547202` 內的 Q2 wip 4 檔與 #704 merge 版**逐 blob 相同**；Q3 改動檔與 #703（Q5）的 6 檔**檔名不重疊**（⚠️ 僅檔案層級比對，衝突與否以實際 rebase 為準）。實作組回報的跳過路徑 → **Q3-r1～r5**（c-2，待辦，⚠️ 單組）。｜~~回報 → 稽核中 → 獨立 QA → PR → CI 綠 → merge → 移「已完成」附 hash。~~ 工作區 `/home/user/msd-q3`（基底＝`origin/main` `0769730` ＋ Q2 wip 本地 commit；編修組實測 HEAD `b547202`「local: Q2 wip base (not pushed)」，其父＝`0769730`；~~當下**無**未提交改動~~〔五更新當下〕）。~~⚠️ 基底不含 #703（Q5）；Q5 改的 6 檔與 Q3 預期改動檔是否重疊，編修組未查證。~~（→ 六更新：已查，檔名不重疊，見上）細節見 (c) c-1 Q3 列。授權：批 5（見 c-1 表下 2026-09-26 總管判讀）。總管 |

**~~A. 本輪進行中~~**（**2026-09-26 清點輪：本區併入 (c) c-1 的 S5-F／S5-W／S5-H 三列**；原文保留）

~~（**無**）—— 原 A1（「▸ 詳細」a11y）、A2（方向列補 4 盞）已由 #697 `f14c5fd` 結案，見文末「已完成（近期）」與下方 **B3 子表**。~~ → 原 A1＝(a) a9、原 A2＝(b) 表；現有 **3 項進行中**（~~暫停~~ → 2026-09-26 已派工恢復，見 (d)），見 (c) c-1。

**下一步候選（總管可自主）**：（**無**）—— 2026-09-26 逐項檢查：B 區全部卡在客戶的規格／文案／新源／授權；C 區是總管已判「不做」或「僅登記、§-1 不主動動工」；D 區要實機或線上環境，沙箱做不到。B12 §9 ①② 的「由誰裁」屬總管職權、可先判歸屬，但即使裁了也因客戶「不動卡面」拍板而無可動工項。C5 的幾項小修（註解名不符、未用 import、`pr-check.yml` 失效觸發）技術上不需客戶輸入，但**無 bug 觸發**，依 §-1 只在任務碰到該檔時順手收尾，⛔ 不單獨開工。⚠️ 單組判讀。
📌 **2026-09-26 派工補註**：上段「下一步候選：（無）」與下句「恢復與否等客戶」**寫於客戶解除暫停令之前**，已過期 —— 現行排程見文末「給下一個 AI」第 1～2 點（原文保留）。
📌 **2026-09-26 清點輪補註**：三項進行中（S5-F／W／H）已依客戶清點令暫停，~~**恢復與否等客戶**~~（→ 2026-09-26 客戶令已解除暫停，見本節檔首）；「待辦」中屬總管可自主的只有查證類（~~**B7a** 現場量測 main 的融資檔、~~ ~~**C6** 比對殘留分支內容~~），⛔ 仍依 §-1 需有觸發（客戶指派）才做。⚠️ 單組判讀。（**2026-09-26 二組複核更正**：B7a 已由二組現場量測結案，見 c-4。）（📌 **2026-09-26 四更新**：C6 查證亦已完成〔兩組獨立稽核、結論一致〕，見 c-5；刪分支另登 **C6-del**〔卡住，客戶〕。）

### (a) 五頁卡化

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| a1 🚦 今天 `today`（整頁 33 張卡） | 已完 | #678 `2292644`（v2 卡面樣板上線）→ #680 `2b53ba0`（葉1 ① 三張）→ #681 `bcd46c7`（整頁） | 卡化本身無。殘留：**B4**（本表末列，卡住）、**B5** K1（(c) c-3，卡住）；燈卡方向列見 (b)。⚠️單組彙整 |
| a2 🔍 找標的 `find`（4 張卡） | 已完 | #687 `83e0f47` | 殘留：**S5-F**（~~進行中；暫停 → 2026-09-26 已派工恢復 → 稽核中 → 稽核通過、PR #701 待合併~~ → **已完** #701 `2810064`，2026-09-26）、**B9 ⑥～⑩**（S5-F QA 殘留；前提「PR #701 merge 後」已成立；~~待辦~~ → ~~2026-09-26 **⑥～⑧ 進行中**〔已派工、實作中〕、**⑨ 待辦**~~ → 2026-09-26 五更新：~~**⑥⑦ 稽核中**~~、**⑧ 卡住**〔需客戶定措辭〕、**⑨ 卡住**〔規格洞〕、**⑩ 待辦**〔新登記〕 → 2026-09-26 六更新：**⑥⑦ 複驗中**〔QA 1 阻擋已修〕、**⑩ 待辦**〔QA 已核實〕）、**B9 ⑪⑫⑬**（待辦，2026-09-26 六更新新登記）、**B6**（`find.screen_result`／`find.sector_flow`）。⚠️單組彙整 |
| a3 🔬 查一檔 `inspect`（14 張卡；批次表與下鑽格照舊） | 已完 | #691 `f38dbd4` | 殘留：**Q5** 各卡靜默失敗（~~待辦~~ → ~~2026-09-26 進行中，已派工 → 稽核中 → QA 回報 1 項阻擋 → 發回修正中 → 修正完成〔實作組自述〕→ 搬主線解衝突中 → 待 QA 複驗~~ → **已完** #703 `76bec1b`，2026-09-26；原 QA 組複驗通過；估值卡現價、ETF 溢價兩條路徑 production 紅路徑少觸發 ⇒ ⛔ 不得對客戶宣稱已修）、**Q5-r1**（待辦）／**Q5-r2**（卡住）、**B6**、**C5b** 未用 `UI_EMPTY` import。⚠️單組彙整 |
| a4 💼 我的持股 `hold`（23 張卡） | 已完 | #688 `66e4fe0` | 殘留：**S5-H**（~~進行中；暫停 → 2026-09-26 已派工恢復~~ → **已完** #699 `bd6ceec`，2026-09-26）、~~**Q1～Q4**（待辦）~~ →**Q1**（~~進行中，已派工 → 稽核中 → 2026-09-26 稽核通過、PR 準備中~~ → **已完** #702 `0769730`，2026-09-26）、~~**Q2～Q4**（待辦）~~ → **Q2**（~~2026-09-26 進行中，已派工、實作中~~ → ~~2026-09-26 **稽核中**，含 S4-T~~ → **已完** #704 `45d5385`，2026-09-26，含 S4-T）、~~**Q3～Q4**（待辦）~~ → **Q3**（~~2026-09-26 進行中，已派工、實作中~~ → 2026-09-26 **稽核中**）、**Q4**（待辦）、**Q1-r1**（卡住）／**Q1-r2～r5**（待辦）、**Q2-r1／Q2-r2**（待辦，2026-09-26 新登記；六更新：r1 QA 實測確認矛盾、r2 改低優先·僅登記）、**Q2-r3**、**Q3-r1～r5**（待辦，2026-09-26 六更新新登記）、**S4-T**（停利卡 why 句，~~待辦~~ → ~~2026-09-26 **併入 Q2，稽核中**~~ → 併入 Q2，**已完** #704 `45d5385`）、**B6**、**B9 ③**。⚠️單組彙整 |
| a5 📖 憑什麼 `why`（教學／資料體檢／工程診斷／AI 問答各卡；3 張表照舊） | 已完 | #691 `f38dbd4` | 殘留：**S5-W**（~~進行中；暫停 → 2026-09-26 已派工恢復 → 稽核中，QA blocker 修正中~~ → **已完** #700 `3f4493c`，2026-09-26）、**B6**（`why.qa`／`why.source.none`）。⚠️單組彙整 |
| a6 今天頁「風險」格接上 danger（客戶裁示「風險 ← danger」；零新增取數） | 已完 | #692 `10f385c` | 無（「動能」格維持未接線，見 B4） |
| a7 今天頁 16 張燈卡「▸ 詳細」折疊（客戶核准線框）＋改樣式開關修 iPhone 點不動 | 已完 | #685 `9fe19fd`、#686 `8873c9e` | 原生 `<details>` 在 iPhone 點不動的**真因未確認**（沙箱無 WebKit）；實機驗證併 **D1** |
| a8 卡片文字含空行破版（五頁） | 已完 | #693 `ac96000`（hover 提示空行）、#695 `07f8800`（5 頁卡面） | 原型路徑 `render.py`／`markup.py` 的同類殘留見 **C5b** |
| a9 五頁「▸ 詳細」a11y（`aria-labelledby`／`aria-controls`／2px 焦點框；⛔ 刻意不用 `aria-expanded`）＝原 A1 | 已完 | #697 `f14c5fd` | 真 iPhone／VoiceOver 未驗 → **D1**（客戶） |
| a10 標記 #11「▨ 無資料」（規格變更，⛔ 非 bug fix） | 已完 | #689 `3e576ef` | 射程只登記 `(hold.portfolio_count, COUNT_EMPTY_NOW)`；擴大射程卡在 **B6**（客戶） |
| a11 測試守衛（`src/ui_v2/` 在 main 的單元守衛；近期修正的測試缺口，只改測試） | 已完 | #679 `5725fff`、#696 `6ec1cb9` | 無 |
| **B4** 今天頁 6 塊未接線（§6.4 舊寫「7 塊」，「風險」格已由 #692 接上） | 卡住 | — | **交易日**：無台股假日曆（也需源）；**Sheet 綁定**：`bound_empty`／`failed` 無文案、live 需 sheet 名；**動能**：客戶裁示維持不接（無公式）；**今天該做的事**：無推導規格；**AI 摘要**：無 prompt／規格，須點擊才跑；**外資淨買賣**（＝(b) `foreign_net`）：單位未定 ⇒ **客戶**給文案／規格（動能除外）。📌 2026-09-26 於 `origin/main` 核對到這 6 塊（`src/ui/tabs/tab_today.py` 的 `_staged_card` 交易日／動能／今天該做的事／AI 摘要 4 處 ＋ `statusbar.sheet` `UI_UNWIRED` ＋ 燈 `foreign_net`；「風險」的 `_staged_card` 已由 `page_today.py::build_summary_tiles` 覆蓋）⚠️單組 |

### (b) 燈號「變化方向」（16 盞）

SSOT `shared/lamp_direction_thresholds.py`（`LAMP_DIRECTION_WINDOWS`／`LAMP_DIRECTION_FLAT_BAND`），**已對 `origin/main` `84f58ea` 逐盞核對**：真方向 5 盞、`mode="none"` 11 盞（其中 `foreign_net` 決策端未接線，卡上不出此列）。
共用機制（不計列）：首批 4 盞 #683 `17ede3f`；方向計算失敗改顯示「計算失敗」、不再整列消失 #684 `66f57f9`；16 盞全數登錄＋每盞錯誤隔離 #693 `ac96000`；`ndc_signal` 真方向 #697 `f14c5fd`。
類別：**①沒歷史 ②需新源 ③判斷未定 ④其他**。逐盞原因明細見下方 **B3 子表**（保留）。

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| `margin` 融資餘額（籌碼） | 已完 · 真方向（%，帶 1.0%，近 20 交易日）⚠️單組 | #683 `17ede3f` | 無。燈號本身「恆紅、無鑑別力」屬 **B8**；歷史檔乾淨與否見 **B7a**（2026-09-26 二組複核：`origin/main` 那份已乾淨，B7a 已完） |
| `bias_240` 年線乖離（中期） | 已完 · 真方向（百分點，帶 1.0，近 20 交易日）⚠️單組 | #683 `17ede3f` | 無 |
| `ism_pmi` 台灣 PMI（中期） | 已完 · 真方向（點，帶 0.5，較上月、兩月須相鄰）⚠️單組 | #683 `17ede3f` | 無 |
| `vix` VIX（短線） | 已完 · 真方向（點，帶 0.5，近 20 交易日）⚠️單組 | #693 `ac96000` | 帶寬量測源為 GitHub `datasets/finance-vix`（CBOE 鏡像，僅校準）→ **B9 ④** 待客戶確認；log 理由不精確 → **C4** |
| `ndc_signal` 國發會景氣燈號（長期） | 已完 · 真方向（帶 0＝官方整數解析度，較上月） | #697 `f14c5fd` | 未對線上真資料跑 → **D2**（客戶部署後看）；StockFeel／MacroMicro 分支只有單點 ⇒ 命中該分支時「無資料」屬預期 |
| `adl` 騰落比（短線） | 已完 · **定案不做**（無資料 ④雜訊；＝原 **C1**） | #693 `ac96000` | `ad_ratio` 單日估計、幾乎無自相關（lag-1 −0.028、lag-20 0.016）⇒ 20 日差會誤導；獨立 QA 否決真方向。總管（已記錄供客戶知悉） |
| `jingqi` 旌旗（籌碼） | 已完 · **定案不做**（無資料 ④雜訊；＝原 **C1**，2026-09-26 由 #697 量測後併入） | #697 `f14c5fd` | 5 日均 lag-20 0.025 ⇒ 同 `adl`；獨立複查一致。總管 |
| `m1b_m2_gap` M1B-M2（長期） | 卡住 · 無資料 ④資料損壞 ⚠️單組 | #683 `17ede3f`（登錄為 none） | `finmind_m1m2.parquet` 負值、程式刻意不讀 ⇒ 先解 **B7b**。客戶 |
| `health` 總經健康評分（長期） | 卡住 · 無資料 ④歷史不同源／非交易日 | #693 登錄、#697 複查維持 | 需與燈值同源、按交易日落地的歷史（屬新增資料落地，§-1.2 資料層凍結）。⚠️客戶（編修組判讀） |
| `tw_export` 台灣出口（中期） | 卡住 · 無資料 ①／② | #693 登錄、#697 複查維持 | 可提供歷史的源；日後接須限定同一條 fallback、同一次抓取的相鄰兩月。客戶 |
| `us_core_cpi` 美國核心 CPI（中期） | 卡住 · 無資料 ② ⚠️單組 | #693 `ac96000` | 只有前值、無日期 ⇒ 帶日期的源。客戶 |
| `us10y` 美債 10 年（中期）＝原 **B1** | 卡住 · 無資料 ② ⚠️單組 | #693 `ac96000` | 燈只用單點 FRED；`^TNX` 是不同源 ⇒ 客戶指定可接受的新源。客戶 |
| `dxy` 美元指數（中期）＝原 **B2** | 卡住 · 無資料 ② ⚠️單組 | #693 `ac96000` | 本地無歷史可量平盤帶；備援 DX-Y.NYB→DX=F→UUP 尺度不同 ⇒ 同上。客戶 |
| `fut_net` 外資期貨淨部位（短線） | 卡住 · 無資料 ① ⚠️單組 | #693 `ac96000` | L1 `build_leading_fast` 每次只即時抓近 ~14 天、無落地歷史 ⇒ 不會自己累積；需新增落地歷史或拉長抓取窗（動 L1，§-1.2）。⚠️客戶（更正見 B3 子表） |
| `foreign_net` 外資淨買賣（籌碼） | 卡住 · **未接線**（決策端 `wired=False`，卡上不出方向列）⚠️單組 | #693 `ac96000`（登錄） | 燈本身接線＝ **B4**「外資淨買賣」（客戶給單位／規格）；FinMind 法人單位查證（§4.1）⚠️總管 |
| `news_systemic` 新聞系統性風險 | 卡住 · 無資料 ③ ⚠️單組 | #693 `ac96000` | 無數值序列、判準未定義（亦列於 `OUT_OF_REACH_LIGHT_KEYS`）⇒ 判準規格。客戶 |

### (c) 其他待辦

**c-1 靜默失敗**（「抓取失敗」與「真的是 0」分開；背景見 §6.4「4 張卡靜默失敗」）

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| S1 批 1：`find.screen_result` 存活池讀取失敗 → 紅「選股中止」（`SCREEN_ABORTED_NOW`）；`hold.take_profit` 列帶 `_detail.error` → 紅「停利判不出來」（`TP_FAILED_NOW`） | 已完（「修好」判定來自單組 QA）⚠️單組 | #690 `2a56483` | 無；其餘歧義路徑見 S5-F、S4 |
| S2 批 2：`hold.switch`（客戶特許動 L3：`get_switch_in_candidates(strict=)` 選股池失敗／空池拋出 → `SWITCH_FAILED_NOW` 紅） | 已完 ⚠️單組 | #693 `ac96000` | 否決 regime 缺資料的殘留見 C2 |
| S3 批 3：`hold.deep.dividend_cash`（客戶特許只動 L1 `fetch_etf_dividends`：失敗不入快取、帶 `attrs['fetch_failed']`、每檔 180s 失敗冷卻；任一檔失敗 → `CASH_FAILED_NOW` 紅） | 已完 ⚠️單組 | #694 `d48709b` | yfinance 回空不拋例外見 C3；卡面出處寫 L3 見 B9 ③ |
| S4 批 4：`hold.take_profit` 持有個股無現價 → 既有 `TP_FAILED_NOW` 紅（只改 L5）＝原 **B9 ②** | 已完 ⚠️單組 | #698 `84f58ea` | 無 |
| S5-F 批 5：`find.screen_result` 其餘歧義路徑（＝ **B9 ①** ＋ B6 子表 find ②③：唯一勾選因子自身輸入失敗、季快照缺） | **已完**（2026-09-26；~~**進行中**（暫停，2026-09-26 已派工恢復 → **稽核中**：獨立 QA 中 → **稽核通過、PR #701 待合併**（等 CI）；未 merge）~~；獨立 QA 一組通過 → PR #701 → CI 綠 → merge）⚠️單組（一組 QA） | #701 `2810064`（完整 `281006489e60909b03aec07e7005cea52063d79a`） | ⭐ **結果（2026-09-26）**：PR #701 merge ＝ `2810064`；CI「Fast checks」「Slow tests」check run 皆 success（編修組經 GitHub API 實測）。實際 merge 改 **8 檔**（編修組 `git diff --stat 3f4493c 2810064` 實測）：L1 `src/data/stock/yield_pe_fetcher.py`、L3 `src/services/fundamental_screener_service.py`、L3 `src/services/valuation_service.py`、L5 `src/ui/views/page_find.py`、測試 `tests/test_p02_find_v2_cards.py`／`tests/test_p02_find_view.py`／`tests/test_v2_silent_fail_b1.py`（⚠️ 下文「未改」為進行中時的描述，merge 時**已改**）、新測試 `tests/test_v2_silent_fail_b5_find.py`。QA 非阻擋殘留 4 項＝ **B9 ⑥⑦⑧⑨**（c-2，待辦；收尾前提「PR #701 merge 後」已成立）。~~📌 **2026-09-26 現況**：獨立 QA 通過、PR #701 已開等 CI → merge 後**立刻**本節寫一行含 hash。QA 非阻擋殘留 4 項已登記 **B9 ⑥⑦⑧⑨**（c-2）。~~ ｜以下為進行中時的描述（保留作歷史，改動內容以 PR #701 為準）：工作區 `/home/user/msd-f`（detached `84f58ea`）改 4 檔：L1 `src/data/stock/yield_pe_fetcher.py::fetch_pe_name_maps(failed_markets=)`、L3 `src/services/fundamental_screener_service.py`（`get_fundamental_survivors(strict=)`／`build_trend_map(strict=)`）、L3 `src/services/valuation_service.py::get_pe_name_maps(failed_markets=)`、L5 `src/ui/views/page_find.py`；**尚無測試**；既有 `tests/test_v2_silent_fail_b1.py::test_b_other_aux_failures_still_leave_a_valid_zero` 鎖住灰色結果、**未改**。~~⚠️ 動到 L1（§-1.2 資料層凍結）—— 本組**未查到**本批的 L1 特許紀錄 ⇒ 恢復前須確認。~~ → **2026-09-26 二組複核更正**：L1 改動**在批 5 授權射程內**（授權原文見本表下方 📌）；現況＝**依清點令暫停**，不是「缺授權」。持久副本：`docs/v2/wip_patches/S5-F_84f58ea.patch`。~~恢復與否：**客戶**（解除清點令暫停）~~ → **2026-09-26 客戶令已解除暫停（原文見上）；已派工，恢復中** |
| S5-W 批 5：`why.qa` Gemini 安全封鎖／格式錯被吞成「成功＋空」 | **已完**（2026-09-26；~~進行中（暫停，2026-09-26 已派工恢復 → 稽核中：獨立 QA 抓到 blocker〔「`STOP`＋無 `parts` 顯示成紅」〕，修正中；未 merge）~~；獨立 QA 兩輪，第 1 輪 blocker 已修、第 2 輪通過）⚠️單組（每輪一組 QA） | #700 `3f4493c` | ⭐ **結果**：L3 `src/services/ai_qa_service.py::run_agent(fail_on_blocked_reply=)`（預設 False ⇒ 其他 caller 行為不變），**只有** L5 `src/ui/views/page_why.py` opt in；`blockReason`／非 `STOP` 的 `finishReason`／缺 `finishReason`／格式錯 → 既有紅態 `QA_FAILED_NOW`；**明確 `STOP` ＋ 無文字 → 灰**（第 1 輪 QA blocker「`STOP` 無 `parts` 顯示成紅」即此路徑，已修）；錯誤文字只列 key／型別、內容已遮蔽。實際 merge 改 3 檔（`ai_qa_service.py`、`page_why.py`、新測試 `tests/test_v2_silent_fail_b5_why.py`）。｜（以下為進行中時的描述，保留作歷史）工作區 `/home/user/msd-w`（detached `84f58ea`）：L3 `src/services/ai_qa_service.py`（新增 `_reply_failure`、`run_agent(fail_on_blocked_reply=)`，預設 False）、L5 `src/ui/views/page_why.py`、新測試 `tests/test_v2_silent_fail_b5_why.py`（未追蹤）。持久副本：`docs/v2/wip_patches/S5-W_84f58ea.patch`。~~恢復與否：**客戶**（解除清點令暫停）~~ → **2026-09-26 客戶令已解除暫停（原文見上）；已派工，恢復中** |
| S5-H 批 5：`hold.vix` 抓不到被畫成灰「有效的結果」（`VIX_EMPTY_NOW`） | **已完**（2026-09-26；~~進行中（暫停，2026-09-26 已派工恢復；未 QA、未提交）~~；獨立 QA 一組通過） | #699 `bd6ceec` | ⭐ **結果**：L3 `src/services/dividend_station_service.py::fetch_vix(*, strict=False)`（`strict=True` 例外上拋；預設路徑〔v1 ETF 戰情室／每日推播／v2 戰情表〕輸出與 log 不變）＋ L5 `src/ui/views/page_hold.py::load_vix` 用 strict、`None`／NaN → 既有紅態 `VIX_FAILED_NOW`；版面不變。**獨立 QA 通過**：17 個情境、其他持股卡 5 萬張輸出逐位元組相同、19 個突變全數被測試抓到。實際 merge 另改 `tests/test_p04_hold_view.py`（patch 未列）。**非阻擋殘留**：例外分支註記「…是那一支 L3 自己載不進來」對 runtime bug 不精確 → 登記 **B9 ⑤**（文案＝客戶決定）。｜（以下為進行中時的描述，保留作歷史）工作區 `/home/user/msd-h`（detached `84f58ea`）：L3 `src/services/dividend_station_service.py::fetch_vix(strict=)`、L5 `src/ui/views/page_hold.py`、改 `tests/test_p04_hold_v2_cards.py`、新測試 `tests/test_v2_silent_fail_b5_vix.py`（未追蹤）。持久副本：`docs/v2/wip_patches/S5-H_84f58ea.patch`。~~恢復與否：**客戶**（解除清點令暫停）~~ → **2026-09-26 客戶令已解除暫停（原文見上）；已派工，恢復中** |
| Q1 持股頁 `NOT_BOUND_NOW`（含 `hold.binding`）：token／sheet-id 讀取錯誤被吞成「未綁定」 | **已完**（2026-09-26；~~**進行中（稽核通過、PR 準備中）**~~（~~待辦（佇列）~~ → ~~已派工~~ → ~~稽核中~~；2026-09-26 獨立 QA **通過、無阻擋**，見 (d)）→ PR #702 → CI 綠 → merge）⚠️單組（一組 QA） | #702 `0769730`（完整 `07697303bdbdd49d3c0fe50d53a2364cb4c32c96`；PR head `2705861`） | ⭐ **結果（2026-09-26）**：PR #702 merge ＝ `0769730`；CI「Fast checks」「Slow tests」check run 皆 success（編修組經 GitHub API 實測）。實際 merge 改 **5 檔**（編修組 `git diff --stat 2810064 0769730` 實測）：L1 `src/data/portfolio/gsheet_portfolio.py`（新增 `_oauth_import_error()`）、L3 `src/services/portfolio_binding_service.py`（`BindingState` 加 `read_error`，預設空字串）、L3 `src/services/holdings_service.py`（`read_error` 非空時拋出）、L5 `src/ui/views/page_hold.py`（`load_binding` 讀取失敗走既有紅態）、新測試 `tests/test_v2_silent_fail_b7_binding.py`（符號與「不新增使用者可見文案」出自 commit `2705861` 訊息，編修組未逐行讀 diff）。QA 非阻擋殘留 5 項＝ **Q1-r1～r5**（c-2）。~~📌 **2026-09-26**：已套上新 main、準備開 PR（尚未開）→ CI 綠 → merge → **立刻**本節寫一行含 hash。QA 非阻擋殘留 5 項已登記 **Q1-r1～r5**（c-2）。改動檔以執行 AI 回報為準（編修組未查證）。~~ ~~客戶排批；須動 L1／L3 者須逐卡特許（§-1.2）。~~ → 2026-09-26 批 5 授權涵蓋、排在 S5 之後依序做（見本表下 📌 總管判讀）。`hold.binding` 另經客戶排除 |
| Q2 `hold.alloc_split`（`SPLIT_NO_VALUE_NOW`）／`hold.deep.core_satellite`「算不出比例」：全部失敗時仍落空態 | **已完**（2026-09-26；~~**進行中（稽核中：獨立 QA）**~~（~~**進行中（派工中、實作中）**~~；~~待辦（佇列）~~；2026-09-26 已派工 → 2026-09-26 實作完成、回報 → 稽核中，見 (d)）→ 獨立 QA 一組**通過、無阻擋** → PR #704 → CI 綠 → merge）⚠️單組（一組 QA；key↔常數對映） | #704 `45d5385`（完整 `45d538517ef59c1bdf47b93bf87e707307322502`；PR head `0c7fa54`） | ⭐ **結果（2026-09-26 六更新）**：PR #704 merge ＝ `45d5385`；CI「Fast checks」「Slow tests」check run 皆 success（編修組經 GitHub API 實測）。實際 merge 改 **4 檔**（編修組 `git diff --stat 76bec1b 45d5385` 實測）：L5 `src/ui/views/page_hold.py`（含 `NO_PRICE_WHY` 定義，編修組 `git grep` 實測）、`tests/test_p04_hold_v2_cards.py`、`tests/test_v2_silent_fail_b4.py`、新測試 `tests/test_v2_silent_fail_b8_alloc.py` —— 與「只動 L5＋測試」相符。獨立 QA 通過、無阻擋（單組；p04 窮舉 50,481 張卡對 baseline 只有預期 8 張不同 —— 總管轉述，編修組未重跑）。**S4-T 隨本 PR 結案**。非阻擋殘留 **Q2-r1**（QA 實測確認矛盾，待辦）／**Q2-r2**（低優先，僅登記）／**Q2-r3**（新登記，待辦）（c-2）。｜以下為進行中時的描述（保留作歷史）：⭐ **2026-09-26 五更新（總管轉述）**：實作只動 **L5 `src/ui/views/page_hold.py`**（L3／L1 未動）＋測試；**併入 S4-T**（總管裁定）：分配卡 ⑤⑥ 與停利卡共用新常數 `NO_PRICE_WHY`（why 截在「，」、去掉「可以重跑一次」）；紅態不再渲染「⚠️ 這個比例只涵蓋一部分」。~~下一步：獨立 QA → PR → CI 綠 → merge → 本節寫一行含 hash。~~非阻擋新待辦 **Q2-r1／Q2-r2**（c-2）。~~回報 → 獨立 QA → PR → merge。~~ 工作區 `/home/user/msd-q2`（基底＝`origin/main` `3f4493c` ＋ Q1 本地 commit `623e326`）。授權同 Q1（批 5） |
| Q3 `hold.deep.stress`（`STRESS_EMPTY_NOW`）／`hold.deep.var`（`VAR_EMPTY_NOW`）／`hold.deep.dividend_cash`（`CASH_EMPTY_NOW`）「算不出來」 | **進行中（稽核中：獨立 QA）**（~~**進行中（派工中、實作中）**~~（~~待辦（佇列）~~；2026-09-26 已派工，等回報，見 (d)）→ 2026-09-26 實作完成、回報 → 稽核中）⚠️單組（key↔常數對映） | —（待合併） | ⭐ **2026-09-26 六更新（總管轉述；檔名編修組 `git status` 實測）**：實作完成 —— L1 `src/data/etf/etf_fetch.py`（失敗時回的空 df 掛 `attrs` 旗標＋`fetch_etf_price(failed=)`）、L3 `src/services/portfolio_deep_service.py`（`get_portfolio_var(strict=)` 等加性參數）、L5 `src/ui/views/page_hold.py` ＋ 測試（檔名見 (d)）→ **稽核中（獨立 QA）**。下一步：獨立 QA → 搬到 `origin/main` `45d5385`（基底內 Q2 wip 與 #704 逐 blob 相同，編修組實測）→ PR → CI 綠 → merge → 本節寫一行含 hash。實作組回報的跳過路徑登記 **Q3-r1～r5**（c-2，待辦，⚠️ 單組）。｜工作區 `/home/user/msd-q3`（基底＝`origin/main` `0769730` ＋ Q2 wip 本地 commit `b547202`）。~~回報 → 稽核中 → 獨立 QA → PR → merge。~~ ~~同 Q1~~ 授權同 Q1（批 5） |
| Q4 持股頁 `EMPTY_SHEET_NOW`：Sheet 列「張數／均價」空白被 L1 `src/data/portfolio/gsheet_portfolio.py::parse_portfolio_records` **靜默丟掉** ⇒「空」可能是整張被丟光 | 待辦（佇列） | — | ~~修在 L1 ⇒ 客戶排批＋特許~~ → 修在 L1；2026-09-26 批 5 授權涵蓋、排在 S5 之後依序做（見本表下 📌 總管判讀） |
| Q5 查一檔（inspect）各卡：抓取失敗時欄位為 `None`，與「真的沒有」同一條路徑；批次卡＝輸入空或格式錯 | **已完**（2026-09-26；~~**進行中（修正完成 → 搬主線解衝突中 → 待 QA 複驗）**~~（~~稽核中 → 發回修正中~~；~~待辦（佇列）~~ → ~~已派工~~ → 稽核中；2026-09-26 獨立 QA 回報 **1 項阻擋**，見 (d)）→ B1 ＋ 3 測試缺口已修 → **原 QA 組複驗通過、無阻擋** → PR #703 → CI 綠 → merge）⚠️單組（一組 QA，兩輪） | #703 `76bec1b`（完整 `76bec1b699faca3af3780461ef7351c4b0fa03e3`；PR head `185392e`） | ⭐ **結果（2026-09-26 五更新）**：PR #703 merge ＝ `76bec1b`；CI「Fast checks」「Slow tests」check run 皆 success（編修組經 GitHub API 實測）。實際 merge 改 **6 檔**（編修組 `git diff --stat 0769730 76bec1b` 實測）：L1 `src/data/core/data_loader.py`、L1 `src/data/core/financial_statements_fetcher.py`、L3 `src/services/dividend_station_service.py`、L3 `src/services/stock_grp_service.py`、L5 `src/ui/views/page_inspect.py`、新測試 `tests/test_v2_silent_fail_b6_inspect.py`。原 QA 組初驗 1 阻擋（B1）＋ 3 測試缺口 → 已修 → 複驗**通過、無阻擋**（單組，總管轉述）；四更新時標 ⚠️ 的「2 處註解言過其實」經複驗**已修正且據實** ⇒ **解除 ⚠️**。**已知限制照登**：估值卡現價、ETF 溢價兩條路徑在 production 紅路徑**少觸發** ⇒ ⛔ 不得對客戶宣稱已修（詳下方 QA 回報段 ①②）。非阻擋殘留 **Q5-r1**（待辦）／**Q5-r2**（卡住）。｜以下為進行中時的描述（保留作歷史，⛔ 不是現行狀態）：📌 **2026-09-26 四更新（總管轉述，編修組未查證內容）**：實作組回報已修好阻擋 **B1** 與 **3 個測試缺口**（測試 105 條；三條新測試各自在守衛被拔掉時轉紅）—— ⚠️ **實作組自述、尚未經 QA 複驗**；「2 處註解言過其實」本次回報未提及 ⇒ ⚠️ 待確認。現於 `/home/user/msd-q5`（基底 `origin/main` `2810064`）解 `src/services/dividend_station_service.py` 與 #699 的衝突，之後送**原 QA 組**複驗 → PR → merge。編修組實測該工作區已 stage 的改動檔：`src/data/core/data_loader.py`、`src/data/core/financial_statements_fetcher.py`、`src/services/dividend_station_service.py`、`src/services/stock_grp_service.py`、`src/ui/views/page_inspect.py`、新檔 `tests/test_v2_silent_fail_b6_inspect.py` —— 與 #702 的 5 檔**檔名不重疊**（⚠️ 僅檔案層級比對）。｜📌 **2026-09-26 QA 回報（總管轉述，編修組未查證）**：**阻擋 B1**＝ETF 紅卡只有單腿（配息／溢價／同儕）失敗時，where 句仍寫「這一支 L3 對 ETF 是 fail-loud 的（拿不到日線就直接拋）」，與同卡 why「回報失敗」矛盾；修法只刪該前綴。另有 **3 個測試缺口**、**2 處註解言過其實** ⇒ 全數**發回實作組修正中**，修完重跑獨立 QA。⛔ **兩條「修了但 production 幾乎不會觸發」的路徑，不得對客戶宣稱已修**：① 估值卡價格（`yf.download` 斷網回空表、FinMind 段吞錯 ⇒ 仍落灰「無股價」並快取 1 小時）；② ETF 溢價（下層全吞網路錯）。非阻擋新待辦 **Q5-r1／Q5-r2**（c-2）。授權同 Q1（批 5） |
| S4-T `hold.take_profit`（#698）紅卡 why 句「…可以重跑一次」**不實**：L1 `src/data/core/data_loader.py::StockDataLoader._get_combined_data_cached`（`@st.cache_data(ttl=TTL_1HOUR)`）會把確定性負結果「查無資料」快取 1 小時 ⇒ 1 小時內重跑結果相同（同 `CLAUDE.md` §-2 記載的「可以重跑一次」錯誤指引事故型） | → 併入 **Q2**（~~稽核中~~ → **已完** #704 `45d5385`，2026-09-26）（~~待辦（佇列）~~；2026-09-26 總管裁定併入 Q2）⚠️單組 | ~~—（隨 Q2 合併）~~ #704 `45d5385`（隨 Q2） | 📌 **2026-09-26 六更新**：隨 Q2 由 #704 `45d5385` merge 結案（`NO_PRICE_WHY` 在 `origin/main` `src/ui/views/page_hold.py` 存在，編修組 `git grep` 實測）；本列仍為指向列、不計。｜⭐ **2026-09-26 五更新**：併入 Q2 —— 停利卡與分配卡 ⑤⑥ 共用新常數 `NO_PRICE_WHY`（why 截在「，」、去掉「可以重跑一次」），見 Q2 列。⚠️ 計數：本列比照 B9 ①（→ 併入 S5-F）改為**指向列、不計**（編修組判讀，見計數規則）。~~修法：**只刪句**（同 S5-F 作法，⛔ 不新增文案）；排在持股頁 `hold` 軌（與 Q2～Q4 同軌，見本表下 📌）。~~ 總管 |
| C2 `hold.switch` 嚴格模式：空頭／警戒下所有存活者都未評分時漏紅（否決 regime 缺資料回 `[]`） | 已完 · **定案不做** ⚠️單組 | — | 不猜就分不出來。總管 |
| C3 配息：yfinance `.dividends` 空但未拋例外 ⇒ 仍視為「沒配息」 | 已完 · **定案不做** ⚠️單組 | — | 分不出來。總管 |

📌 **2026-09-26 總管判讀（Q1～Q5）**：客戶 2026-09-26 批次授權「**本批放行『失敗被當成沒結果』的所有卡，不用每張問我**」（原文見本表下「S5 批次的授權」）涵蓋 Q1～Q5 所需的 L1／L3 改動（含 Q4 的 L1 `gsheet_portfolio.py::parse_portfolio_records`）⇒ **不需逐卡特許**；排在 S5-H／W／F 之後依序做：**Q1 → Q2 → Q3 → Q4**（持股頁 `hold`，依序、一次一張）→ **Q5**（查一檔 `inspect`）；每張走「實作 → 獨立 QA（§-2 規則 4）→ merge → 本節寫一行含 hash」。⚠️ 此為**總管判讀**（承接下方二組判讀），⛔ 非客戶逐字明示；客戶一句話可推翻。
📌 **2026-09-26 派工更新**：**Q1**、**Q5** 已派工（與 S5-W／S5-F 並行，見 (d)）；剩 **Q2 → Q3 → Q4** 依序。
📌 **2026-09-26 再更新**：S5-W 已完 #700 `3f4493c`；S5-F PR #701 待合併；Q1／Q5 稽核中；持股頁軌新增 **S4-T**（停利卡 why 句刪減）⇒ 剩 **Q2 → Q3 → Q4 ＋ S4-T**。
📌 **2026-09-26 三更新**：**S5-F 已完 #701 `2810064`**（S5 三軌至此全數 merge：#699／#700／#701）；**Q1** 稽核通過、PR 準備中；**Q2** 已派工（`/home/user/msd-q2`）；**Q5** QA 回報 1 項阻擋、發回修正中 ⇒ 持股頁軌剩 **Q3 → Q4 ＋ S4-T**。
📌 **2026-09-26 四更新**：**Q1 已完 #702 `0769730`**；**Q5** 修正完成（實作組自述）→ 搬主線解衝突中 → 待 QA 複驗；**Q2** 實作中；**B9 ⑥～⑧** 已派工、實作中（`/home/user/msd-b9`）。⭐ **Q1 殘留排序（總管判讀，⚠️單組）**：**Q1-r2**（`secrets.toml` 格式錯被吞灰）、**Q1-r3**（import 失敗建議重新登入＝不實指引 ⇒ **只刪句**）、**Q1-r5**（`repr` 未清洗）排在持股頁軌 **Q4 ＋ S4-T 之後**；**Q1-r4**（既有來源誤歸）排在 r2／r3／r5 之後；**Q1-r1** 維持卡住（新字樣＝客戶）⇒ 持股頁軌：**Q3 → Q4 ＋ S4-T → Q1-r2／r3／r5 → Q1-r4**。
📌 **2026-09-26 五更新**：**Q5 已完 #703 `76bec1b`**（原 QA 組複驗通過；估值卡現價、ETF 溢價兩條路徑 production 紅路徑少觸發，⛔ 不得對客戶宣稱已修）；**Q2** 實作完成、**S4-T 併入 Q2**（總管裁定：共用新常數 `NO_PRICE_WHY`）→ 稽核中；**Q3** 已派工（`/home/user/msd-q3`）、實作中；**B9 ⑥⑦** 稽核中、**⑧** 跳過 → 卡住。⇒ 持股頁軌（總管排程）：**Q2（QA → PR → merge）＋ Q3（實作中）→ Q4 → Q1-r2／r3／r5 → Q1-r4**；~~S4-T~~ 已不再單列（併入 Q2）。新登記 **Q2-r1／Q2-r2**（c-2，待辦）排在 **B9 ⑩ 之後**（見「給下一個 AI」第 1 點）。
📌 **2026-09-26 六更新**：**Q2（含 S4-T）已完 #704 `45d5385`**（獨立 QA 一組通過、無阻擋）；**Q3** 實作完成 → 稽核中（獨立 QA）；**B9 ⑥⑦** QA 1 阻擋已修 → 複驗中。⇒ 持股頁軌（總管排程）：**Q3（QA → PR → merge）→ Q4 → Q1-r2／r3／r5 → Q1-r4 → Q2-r1** → Q5-r1 → B9 ⑩⑫ → 小項 C4／C5b／DOC1／DOC2（~~Q2-r1／Q2-r2 排在 B9 ⑩ 之後~~ → 六更新：Q2-r1 提前到 Q1-r4 之後、Q5-r1 之前；**Q2-r2** 改低優先、僅登記，不在排程）；**Q2-r3**、**Q3-r1～r5**、**B9 ⑪⑬** 位置未定（總管未排；見「給下一個 AI」第 1 點）。

📌 其餘 11 個工作區（`/home/user/msd-{badge,base,cards2,dirfail,find,fold,hold,lamp,mainfix,silent,toggle}`）2026-09-26 核對：**皆無未提交改動、HEAD 皆已是 `origin/main` 的祖先** ⇒ 過期，無待救工作（二組 2026-09-26 逐一重跑 `git status`／`merge-base --is-ancestor`，一致）。~~⚠️ **S5-F／W／H 的改動只存在本容器的本地工作區**（未 commit、未 push），換容器即消失。~~

📌 **2026-09-26 二組複核更正 —— 持久副本**：S5-F／W／H 三軌的未提交改動已存成 `docs/v2/wip_patches/S5-F_84f58ea.patch`／`S5-W_84f58ea.patch`／`S5-H_84f58ea.patch`（含未追蹤的新測試檔）。二組實測：三份 patch 在 `84f58ea` 上 `git apply --check` 皆可乾淨套用，套用後 11 個檔與三個工作區**逐位元組相同**。⇒ **工作區（`/home/user/msd-{f,w,h}`）是一次性的、換容器即消失；patch 才是持久副本** —— ⚠️ 前提是 patch 檔**隨本檔一起 commit**（本輪清點時仍為未追蹤檔）。恢復方式：從 `84f58ea` 開新分支 → `git apply docs/v2/wip_patches/S5-X_84f58ea.patch`。
📌 **2026-09-26 更新**：S5-H #699 `bd6ceec`、S5-W #700 `3f4493c`、S5-F #701 `2810064` **三軌皆已 merge** ⇒ 三份 patch 與工作區 `/home/user/msd-{f,w,h}` 均已是**歷史**，⛔ 不要再套用（現行以 `origin/main` 為準）。

📌 **2026-09-26 二組複核更正 —— S5 批次的授權**（客戶原文由總管轉述；二組無從由 repo 查證原文，只核對到 S5-H patch 內註解「批次 5（客戶授權）」與之相符）：
- 停利卡那一令（→ S4／#698）的射程是「只動停利卡相關 L3／資料層」（#698 實際只改 L5）；
- 接著客戶 2026-09-26 下令：「【授權：逐卡修失敗源頭，連跑】停利已完。本批放行「失敗被當成沒結果」的所有卡，不用每張問我。」⇒ **S5-F／W／H（含 S5-F 動 L1 `yield_pe_fetcher.py`）都在這批授權射程內**；
- 之後客戶下清點令（「⛔ 不修 code、⛔ 不開新工作，只清點」）⇒ 三軌**暫停**。恢復只需客戶解除暫停，**不需另外的 L1／L3 特許**。（→ **2026-09-26 客戶已解除暫停**，原文見本節檔首；三軌已派工，見 (d)。）
- ⚠️ 二組判讀（未經第三組驗，客戶／總管一句話可推翻）：Q1～Q5 字面上同屬「失敗被當成沒結果」的卡 ⇒ **疑同在批 5 授權射程內**；若是，Q1～Q5「須逐卡特許」一語已過期，現況同為「因清點令暫停」。

**c-2 標記矛盾與 QA 殘留**

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| B6 標記矛盾：#7「缺漏·可重跑」vs 文字「重按也一樣」／「有效的結果」—— hold／inspect／why 其餘卡**合一條**（客戶：不另開單）。已修靜默失敗卡（`find.screen_result`／`hold.take_profit`／`hold.switch`／`hold.deep.dividend_cash`）仍未登記進 `V2_VALID_EMPTY_SPEC`（`origin/main` `84f58ea` 上仍只登記 `hold.portfolio_count`）。📌 2026-09-26 客戶核准的逐卡審查已完成（**兩組獨立、結論一致**）：沒有任何新的 `(卡, now)` 對符合 #11 登記條件，上列四張卡**不得登記**；該輪零程式變更。逐卡理由見下方 **B6 子表** | ~~未修~~ → 審查完成、**矛盾未解** ⇒ **卡住** | — | 與持股頁同一根因；改 builder／文案需授權；現有常數拆不開任何一對 ⇒ 登記＝把 #11 擴散到真缺漏。~~客戶授權後一次修~~ → **客戶二擇一**：(1) 逐卡修上游讓失敗轉紅（需逐卡授權動 L1／L3）(2) 新文案拆「真零 vs 歧義」（需文案／規格）（見 B6 子表下「解法」）。⚠️單組判讀：批 4（#698）與批 5（S5-F／W／H）逐卡修的正是 B6 子表列出的歧義路徑 ⇒ **實質上在走解法 (1)**，但客戶是否正式選 (1)、修完後是否登記 #11，**未見明示**。📌 2026-09-26 二組複核補註：客戶「【授權：逐卡修失敗源頭，連跑】」（原文見 c-1 表下）字面上即解法 (1) ⇒ **「選 (1) 還是 (2)」已由該令回答**（⚠️ 二組判讀）；仍未明示的只剩「修完後是否登記 #11」。📌 2026-09-26：`hold.vix` 亦已由 #699 `bd6ceec` 修成失敗轉紅（S5-H）；#699 未動 `src/ui/views/page_today.py` ⇒ `V2_VALID_EMPTY_SPEC` 不變、`hold.vix` 未登記 #11。📌 2026-09-26：`why.qa` 亦已由 #700 `3f4493c` 修成失敗轉紅（S5-W）；#700 只改 3 檔、未動 `page_today.py` ⇒ `V2_VALID_EMPTY_SPEC` 不變。📌 2026-09-26：`find.screen_result` 的 B6 子表 ②③ 路徑亦已由 #701 `2810064` 修成失敗轉紅（S5-F）；#701 改 8 檔、未動 `page_today.py`（編修組 `git diff --stat` 實測）⇒ `V2_VALID_EMPTY_SPEC` 不變；① 一個因子都沒勾仍未處理（→ B9 ⑨）。📌 2026-09-26：持股頁 `NOT_BOUND_NOW` 的 token／sheet-id 讀取錯誤路徑亦已由 #702 `0769730` 改走既有紅態（Q1）；#702 改 5 檔、未動 `page_today.py`（編修組 `git diff --stat` 實測）⇒ `V2_VALID_EMPTY_SPEC` 不變、`NOT_BOUND_NOW` 未登記 #11；全域狀態列仍顯示「未綁定」→ Q1-r1。📌 2026-09-26：查一檔（inspect）各卡的抓取失敗路徑亦已由 #703 `76bec1b` 修成失敗轉紅（Q5）；#703 改 6 檔、未動 `page_today.py`（編修組 `git diff --stat 0769730 76bec1b` 實測）⇒ `V2_VALID_EMPTY_SPEC` 不變；估值卡現價、ETF 溢價兩條路徑 production 紅路徑少觸發、另三條分不出「失敗 vs 無資料」→ Q5-r2。📌 2026-09-26：`hold.alloc_split`（`SPLIT_NO_VALUE_NOW`）／`hold.deep.core_satellite` 的「全部失敗仍落空態」路徑亦已由 #704 `45d5385` 改走紅態（Q2）；#704 改 4 檔、未動 `page_today.py`（編修組 `git diff --stat 76bec1b 45d5385` 實測）⇒ `V2_VALID_EMPTY_SPEC` 不變、`SPLIT_NO_VALUE_NOW` 未登記 #11 |
| B9 ① `find` 唯一勾選因子自身輸入失敗 → 灰色「有效的結果」 | → 併入 **S5-F**（~~進行中；2026-09-26 稽核通過、PR #701 待合併~~ → **已完**，2026-09-26） | #701 `2810064` | 見 S5-F |
| B9 ~~② `hold.take_profit` 在 `current_price` 為 `None` 時~~ | → **S4** 結案 | #698 `84f58ea` | 無 |
| B9 ③ 配息卡 `why` 寫「L3…拋出例外」但錯在 L1（既有文案限制）；2026-09-26 核對 `origin/main` `src/ui/views/page_hold.py::SRC_DIV_CASH` 仍寫「L3 配息現金流」 | 卡住 ⚠️單組 | — | 客戶文案 |
| B9 ④ `vix` 平盤帶用 GitHub `datasets/finance-vix`（CBOE 鏡像，僅校準）量測 —— 請客戶確認源可接受 | 卡住 | — | 客戶確認 |
| B9 ⑤ `hold.vix` 例外分支註記「這不是取數失敗（L3 取不到時回的是「沒有值」而不是例外），是那一支 L3 自己載不進來」（`src/ui/views/page_hold.py::build_vix_card` 的 `UI_FAILED` 分支，#699 前既有文案）—— S5-H 改 `fetch_vix(strict=True)` 後，L3 本體的 runtime 例外（程式層錯誤；取數失敗依 `fetch_yf_close` 契約回空、不拋）也會走這一支 ⇒ 「L3 自己載不進來」只貼合 import 失敗，對 runtime bug **不精確**（S5-H 獨立 QA 非阻擋發現，2026-09-26 登記） | 卡住 ⚠️單組（一組 QA） | — | 文案＝客戶決定（同 B9 ③）。客戶 |
| B9 ⑥ S5-F（PR #701）因子失敗紅卡的 where 句，對 PE／RS／趨勢因子仍指向「FinMind 額度／MOPS–Goodinfo 鏈」⇒ **誤導**（S5-F 獨立 QA 非阻擋發現，2026-09-26 登記）~~；趨勢因子較貼切的是既有 `SNAPSHOT_WAIT_WHERE`~~（→ 2026-09-26 六更新：B9 獨立 QA 判此改法為**阻擋**、已改指 `SCREEN_NO_PARTIAL_WHERE`，見右欄） | **進行中（稽核中：複驗中）**（~~**進行中（稽核中：獨立 QA）**~~（~~**進行中（派工中、實作中）**~~；~~待辦~~ → 2026-09-26 已派工，工作區 `/home/user/msd-b9`，見 (d) → 2026-09-26 實作完成 → 稽核中；📌 收尾前提「PR #701 merge 後」**已成立**：#701 `2810064`）→ 2026-09-26 獨立 QA 抓到 **1 項阻擋** → 已修 → 複驗中）⚠️單組（一組 QA） | —（待合併） | ⭐ **2026-09-26 六更新（總管轉述；對映經編修組 grep 工作區確認）**：獨立 QA 抓到 **1 項阻擋** —— 趨勢因子 where 指「要等排程補抓」（`SNAPSHOT_WAIT_WHERE`）在實際可達情境**幾乎都錯**：趨勢卡出現時多是舊季 parquet 損毀／schema 漂移，排程只抓本季＋去年同季、不會重寫舊季 ⇒ **已修**：改指 `SCREEN_NO_PARTIAL_WHERE`（「本站不以殘缺資料湊出名單」）；另補 M7 行為測試、修正缺貨來源註解（⑥⑦ 同一份工作）→ **複驗中**。下一步：複驗通過 → PR → merge。估值／RS／趨勢三因子紅卡 where 現只剩原則句 → **B9 ⑪**。｜⭐ **2026-09-26 五更新（總管轉述）**：~~趨勢因子 where → `SNAPSHOT_WAIT_WHERE`；~~（→ 六更新：QA 阻擋，已改）估值／RS 因子 where **只刪到剩**「本站不以殘缺資料湊出名單」（只刪句）。~~下一步：獨立 QA → PR → merge。~~~~依因子改指既有 where 常數（趨勢 → `SNAPSHOT_WAIT_WHERE`）；PR #701 merge 後在 `src/ui/views/page_find.py` 收尾。~~ 總管 |
| B9 ⑦ S5-F 紅卡用語「這輪失敗」暗示一次性；但季快照缺是**持續性**的，重跑不會好（S5-F 獨立 QA 非阻擋發現，2026-09-26 登記） | **進行中（稽核中：複驗中）**（~~**進行中（稽核中：獨立 QA）**~~（~~**進行中（派工中、實作中）**~~；~~待辦~~ → 2026-09-26 已派工，工作區 `/home/user/msd-b9`，見 (d) → 2026-09-26 實作完成 → 稽核中；📌 收尾前提「PR #701 merge 後」**已成立**：#701 `2810064`）→ 2026-09-26 同輪 QA 阻擋〔在 ⑥〕已修 → 複驗中）⚠️單組（一組 QA） | —（待合併） | ⭐ **2026-09-26 六更新**：⑥⑦ 同一份工作；QA 阻擋在 ⑥（見上列），修正後**一起複驗中**。下一步：複驗通過 → PR → merge。｜⭐ **2026-09-26 五更新（總管轉述）**：刪「這輪」。~~下一步：獨立 QA → PR → merge。~~~~用語措辭。~~ 📌 另記（**不在**本次 B9 派工範圍，待評估）：`src/ui/views/page_hold.py` 也有一份「…這輪失敗」（編修組 `git grep` `origin/main` 見於 VIX 卡相關字串「需要的數字沒抓到 —— 通常是上游來源這輪失敗」）。總管 |
| B9 ⑧ S5-F 紅卡事實列顯示「`RuntimeError()`」—— 對使用者無資訊量；可改沿用 L3 log 既有用語「跨季趨勢為空表」（S5-F 獨立 QA 非阻擋發現，2026-09-26 登記） | **卡住**（~~**進行中（派工中、實作中）**~~；~~待辦~~ → 2026-09-26 已派工，工作區 `/home/user/msd-b9`，見 (d) → 2026-09-26 實作組**跳過** → 卡住）⚠️單組（一組 QA） | — | ⭐ **2026-09-26 五更新（總管轉述）**：「跨季趨勢為空表」**只存在 L3 log**，搬上畫面＝**新使用者文案**（K1）⇒ 需**客戶定措辭**。~~措辭決定（沿用 L3 log 用語與否）。總管~~ 客戶 |
| B9 ⑨ S5-F 規格缺口（S5-F 獨立 QA 非阻擋發現，2026-09-26 登記）：紅卡未提示「取消勾選這個因子」；未點名是**哪個市場**失敗；① 一個因子都沒勾（＝B6 子表 find ①）未處理 | **卡住**（規格洞；~~待辦~~ → 2026-09-26 五更新：總管「維持規格洞（卡住）」；📌 2026-09-26 收尾前提「PR #701 merge 後」**已成立**：#701 `2810064`）⚠️單組（一組 QA） | — | 規格補齊（新提示字樣＝新使用者文案，K1）。~~總管。~~ 📌 2026-09-26：**不在** B9 ⑥～⑧ 的派工內；~~總管排程（見「給下一個 AI」第 1 點）未列入 ⇒ 位置未定（⚠️ 編修組照實轉述，未另排）~~ → 2026-09-26 五更新：總管定為**規格洞、卡住**（⚠️ 本列先前狀態欄寫「待辦」，本次依總管說法改卡住）。是否修＝總管；規格／字樣＝客戶（⚠️ 後半為編修組依「卡住」定義推論，總管未明示） |
| B9 ⑩ S5-F 紅卡的「**MOPS／Goodinfo 備援鏈**」一句，對**缺貨因子**、**排名例外**、**存活池讀取例外**三種卡也**不成立**（B9 ⑥⑦ 實作組讀碼發現，2026-09-26 登記）：季財報只走 FinMind、月營收備援為 TWSE／TPEx OpenAPI、資料體檢不監控 MOPS／Goodinfo | 待辦 ⚠️單組（~~實作組一組讀碼，**待 QA 核實**~~ → 2026-09-26 六更新：實作組讀碼 ＋ B9 獨立 QA 一組**核實**） | — | ⭐ **2026-09-26 六更新（B9 獨立 QA 核實，總管轉述；編修組只 `git grep` 確認 `fetch_margin_balance` 位於 `src/data/daily/daily_data_fetchers.py`，其餘未重驗）**：季財報**只走 FinMind**、月營收備援為 **TWSE／TPEx OpenAPI**、資料體檢 `@monitored` 9 支**無**選股網 fetcher、Goodinfo 只在 `fetch_margin_balance` 備援鏈 ⇒「看 MOPS／Goodinfo 備援鏈」對**缺貨、排名例外、存活池例外**三卡皆**不成立**；另 `src/ui/views/page_inspect.py` 有 **3 處同類指路未深驗**。排程：與 **B9 ⑫** 同批（總管排程「B9 ⑩⑫」）。｜~~先由獨立 QA 核實讀碼結論；成立則~~比照 B9 ⑥ 作法（只刪句或改指既有 where 常數，⛔ 不新增文案）在 `src/ui/views/page_find.py` 收尾。總管 |
| B9 ⑪ 估值／RS／趨勢三因子紅卡的 where（B9 ⑥ 修正後）只剩原則句「本站不以殘缺資料湊出名單」（`SCREEN_NO_PARTIAL_WHERE`）、**沒有可操作指路**（B9 ⑥⑦ 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | — | QA 指出既有句「放寬條件（少勾幾個因子）後再按「🎯 開始選股」」可作出口（該句存在於 `origin/main` `src/ui/views/page_find.py`，編修組 `git grep` 實測）；要不要採用屬**呈現判斷**。誰決定＝總管（若視為**新文案組合**則＝客戶，K1）。位置未定（總管排程未列入） |
| B9 ⑫ 快照**整個缺**時，存活池讀取例外卡的 where 仍是 FinMind／MOPS 句，本該指既有 `SNAPSHOT_WAIT_WHERE`（B9 ⑥⑦ 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | — | 改指既有 where 常數（⛔ 不新增文案），在 `src/ui/views/page_find.py` 收尾；排程：與 **B9 ⑩** 同批（總管排程「B9 ⑩⑫」）。總管 |
| B9 ⑬「FinMind 額度**每日 00:00** 重置」（`src/ui/views/page_find.py` 的 `V2_SHORT_ROWS` 摘錄與 `build_screen_result_card` 內 where 句）與 repo 其他處說法**矛盾**：`src/ui/tabs/tab_stock_picker.py::render_tab_stock_picker`、`src/data/core/financial_statements_fetcher.py::_fetch_financial_statements_cached` 註解寫「FinMind 限額為**每小時制**」；`src/data/core/data_loader.py::StockDataLoader.__init__` 印「匿名模式（**每小時 600 次**）」；`shared/macro_buckets.py::leading_table_empty_state_html` 寫「額度用罄(**每日 600 次**)」（B9 ⑥⑦ 獨立 QA 非阻擋發現，2026-09-26 登記；QA 原報以行號標示，本檔依 §8.2.A.0 規則 1 改寫為符號，由編修組 `git grep` `origin/main` `45d5385` 定位） | 待辦 ⚠️單組（一組 QA） | — | FinMind 官方額度規則**未對外查證** ⇒ 先查證哪一說對，再更正錯的幾處（事實更正，⛔ 不新增文案）。位置未定（總管排程未列入）。總管 |
| Q1-r1 讀取失敗（token／sheet-id）時，**全域狀態列**仍顯示「未綁定」（Q1 獨立 QA 非阻擋發現，2026-09-26 登記） | 卡住 ⚠️單組（一組 QA） | —（待合併） | 要改需新標籤字樣 ⇒ K1 新文案 ⇒ **規格洞**，卡住待客戶給字樣。是否修＝總管；字樣＝客戶（K1）—— ⚠️ 總管指示「誰決定＝總管」與本列「待客戶」並陳，編修組依 B9 ⑤ 前例歸卡住 → 📌 **2026-09-26 定案（總管）**：**是否修＝總管；字樣＝客戶（K1 新文案）** ⇒ 維持卡住（卡在字樣）；上句「並陳」已由此定案取代（原文保留） |
| Q1-r2 `secrets.toml` 格式錯誤被 `_safe_secret`／`_get_active_sheet_id` 吞成灰（「未綁定」）（Q1 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | —（待合併） | 📌 2026-09-26 排序（總管判讀，⚠️單組）：持股頁軌 **Q4 ＋ S4-T 之後**，與 r3／r5 同批。總管 |
| Q1-r3 `oauth_state` import 失敗時，持股數卡建議「重新登入」＝**不實指引**（Q1 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | —（待合併） | 修法：**只刪句**（⛔ 不新增文案）。📌 2026-09-26 排序（總管判讀，⚠️單組）：持股頁軌 **Q4 ＋ S4-T 之後**，與 r2／r5 同批。總管 |
| Q1-r4 station 家族把持股錯誤歸到**錯的來源**（既有問題，非 Q1 造成；Q1 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | —（待合併） | 📌 2026-09-26 排序（總管判讀，⚠️單組）：排在 **Q1-r2／r3／r5 之後**。總管 |
| Q1-r5 例外 `repr` 未經清洗直接上頁面（**潛在**，QA 未實測到外洩；Q1 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | —（待合併） | 📌 2026-09-26 排序（總管判讀，⚠️單組）：持股頁軌 **Q4 ＋ S4-T 之後**，與 r2／r3 同批。總管 |
| Q2-r1 ⑦ AI 總結仍吃 L3 digest 的配置比例；Q2 讓分配卡轉紅時，同頁 ⑦ 可能仍顯示比例 ⇒ **同頁可能不一致**（Q2 實作回報，2026-09-26 登記） | 待辦 ⚠️單組（Q2 獨立 QA 一組實測） | — | ⭐ **2026-09-26 六更新（Q2 獨立 QA 實測，總管轉述）**：**矛盾已確認** —— 整批失敗時 prompt 仍寫「實際配置：核心 64% / 衛星 36%」；無現價時 prompt 把原因寫成「部分持股缺金額」。⇒ 維持待辦；要修得動 L3 `src/services/dividend_station_service.py::build_station_digest`／`build_summary_prompt`（符號存在，編修組 `git grep` `origin/main` 實測），在批 5 授權射程內（⚠️ 單組判讀）。排程：**Q1-r4 之後、Q5-r1 之前**（總管）。｜~~先由 Q2 獨立 QA 判定是否構成矛盾；構成才修。~~ 總管 |
| Q2-r2 VIX 卡（#699）why 仍含「可以重跑一次」；~~在快取下是否不實**未查**~~（→ 2026-09-26 六更新：Q2 獨立 QA 已查，見右欄）（同 S4-T／Q5-r1 事故型之疑；2026-09-26 登記） | 待辦（**低優先，僅登記**；2026-09-26 六更新）⚠️單組（一組 QA 查證） | — | ⭐ **2026-09-26 六更新（Q2 獨立 QA 查證，總管轉述；符號經編修組 `git grep` `origin/main` 確認存在）**：L1 `src/data/macro/macro_core.py::_fetch_yf_close_base` **不快取失敗**、proxy `src/data/proxy/proxy_helper.py::_URL_CACHE` 只存 200 回應 300 秒 ⇒「可以重跑一次」這句**大致成立**（QA 用語）；唯一洞是 Yahoo 回 200 但收盤價**全 null** 時，空序列被快取 1 小時 ⇒ 改為**低優先、僅登記**（不在排程；碰到該檔再收尾，§-1）。｜~~先查 `hold.vix` 取數路徑的快取行為；不實則比照 S4-T 只刪句（⛔ 不新增文案）。~~ 總管 |
| Q2-r3 已下市逾 360 天或興櫃個股：現價**永久** `None` → 紅卡 why 寫「需要的數字沒抓到 —— 通常是上游來源這輪失敗」（`NO_PRICE_WHY`，源自 `shared/station_specs.py` `MISS_TEXT[MISS_NO_INPUT]`，編修組 `git grep` 實測）**不準**（Q2 獨立 QA 非阻擋發現，2026-09-26 登記；與停利卡批 4〔#698〕既有裁示一致） | 待辦 ⚠️單組（一組 QA） | — | ⚠️ 需判斷是否為**規格洞**（另寫「這檔已無報價」類說明＝新文案，K1 ⇒ 客戶）。位置未定（總管排程未列入）。總管 |
| Q3-r1 yfinance 回空但**未拋例外**時，仍分不出「失敗 vs 無資料」（Q3 實作組回報的跳過路徑，2026-09-26 登記；⚠️ 編修組觀察：與 C3 同型） | 待辦 ⚠️單組（實作組一組回報，未經 QA） | — | 待 Q3 獨立 QA 看過；處置與位置未定（總管排程未列入）。總管 |
| Q3-r2 Beta 失敗路徑未處理 —— 要改 L2 `src/compute/etf/etf_calc.py::calc_portfolio_stress_test`（Q3 實作組回報的跳過路徑，2026-09-26 登記） | 待辦 ⚠️單組（實作組一組回報，未經 QA） | — | 需改計算層（L2）；批 5 授權是否涵蓋 L2 **待判讀**（同 Q5-r2）。位置未定（總管排程未列入）。總管 |
| Q3-r3 `src/services/portfolio_deep_service.py::_resolve_yf_ticker` 吞例外（Q3 實作組回報的跳過路徑，2026-09-26 登記） | 待辦 ⚠️單組（實作組一組回報，未經 QA） | — | 位置未定（總管排程未列入）。總管 |
| Q3-r4 配息 `REASON_DIVIDEND_CONTRACT_DRIFT`（`src/services/portfolio_deep_service.py`）顯示成**灰卡**、且 where 叫人補張數（不實指引）；production **目前走不到**（Q3 實作組回報的跳過路徑，2026-09-26 登記） | 待辦 ⚠️單組（實作組一組回報，未經 QA） | — | production 走不到 ⇒ 目前無 bug 觸發（§-1）。位置未定（總管排程未列入）。總管 |
| Q3-r5 L1 取價失敗仍快取 1 小時 ⇒ Yahoo 恢復後 VaR 卡最長紅 1 小時；屬 `CLAUDE.md` §1.A 第 3 條 (a)「失敗不入快取」的**既有問題**（非 Q3 造成；Q3 實作組回報，2026-09-26 登記） | 待辦 ⚠️單組（實作組一組回報，未經 QA） | — | 修在 L1（具體函式編修組未查證）。位置未定（總管排程未列入）。總管 |
| Q5-r1 估值卡灰態「若是暫時抓不到，按…重跑一次」在快取期內（價格 1 小時、配息 30 分）**不實**（Q5 獨立 QA 非阻擋發現，2026-09-26 登記） | 待辦 ⚠️單組（一組 QA） | —（待合併） | 修法：**只刪句**（同 S4-T／S5-F 作法，⛔ 不新增文案）。總管 |
| Q5-r2 三條**無法不猜就分辨**「失敗 vs 無資料」的路徑 —— ETF 溢價（需改 L2）、配息鏈與籌碼（需改 L1）—— ＋ 估值價格「查無資料」路徑（Q5 獨立 QA 非阻擋發現，2026-09-26 登記） | 卡住 ⚠️單組（一組 QA） | —（待合併） | 需改計算層／資料層讓上游分辨「失敗」與「無資料」。誰決定＝總管：批 5 授權是否涵蓋 L2 改動**待判讀**（⚠️單組）；若判不涵蓋 ⇒ 須客戶授權。⚠️ 依總管指示歸「卡住」—— 與本節「卡住」定義（卡在客戶內容決定／實機）字面不完全相符，並陳 |

**c-3 規格／文件債**

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| B5 K1：燈卡「門檻出處」「命中來源」露出內部識別字（來源 §6.4；相關台帳 `docs/v2/prototype/FIELD_LABELS_TODO.md`） | 卡住 ⚠️單組（唯讀稽核） | — | 客戶的 **16 盞白話對照表**未到；⛔ AI 不得自擬（＝捏造，§1）。客戶定序「四頁卡化 ＞ 今天頁未接線 ＞ K1」—— 四頁卡化已於 #691 完成。客戶 |
| B12 原型卡面內部程式符號（B 層 52／48／45 處，`docs/v2/prototype/FIELD_LABELS_TODO.md`）：客戶 2026-09-22 拍板「**不修、只登記**」；甲類 7 項規格已有中文標籤但依拍板不動卡面；乙類待補規格；丙類與 §7 家族等該檔 **§9 兩個裁示點**（① 線框 `name` 算不算「規格已給標籤」② 卡面是否同享「引述」例外）（與 B5 同源不同射程：B5 管燈卡兩欄，本條管原型全卡面） | 卡住 ⚠️單組 | — | 客戶補規格／放行卡面後再動；§9 ①② 該檔自陳「不判定由誰裁」（歸屬未定）。⚠️ 量測釘在 `e8d3a75`（2026-09-22），其後 #687～#691 卡化已改卡面，數字可能已漂移（未重測）。客戶 |
| DOC1 文件過期敘述（僅登記）：① `docs/v2/spec/UI_PAGE_FIND.md`「逐項處置」第 3 點 2026-09-26 補註，把 `find.screen_result` 不能登記的理由之一寫成「存活池讀取失敗」—— **自 #690 起過期**（結論「不得登記」仍成立，見 B6 子表下「順帶登記」）；② 本檔 §3.2「各頁進度」四頁「仍待卡化」自 #691 起過期（該段已加註指向本節）；③ `ARCHITECTURE.md` 目錄樹列不存在的 `README.md`；④ `CLAUDE.md` §8.2.A.5「183 道 `assert`」量測值漂移（2026-09-24 實測 194） | 待辦（僅登記）⚠️單組 | — | 碰到該檔時順手更正（§-1，⛔ 不單獨開工）。總管 |
| DOC2 找標的頁卡名 key 不一致：規格／原型 `find.screen_summary` vs 程式 `find.screen_result`（**2026-09-26 二組複核補登**：#687 commit `8f3f869` 訊息自陳「未處理（另案）」；已登記於開發線 `docs/v2/spec/DATA_MAP_GAPS.md` `ED-d`，`origin/main` 的 `src/ui_v2/page_find.py` 檔頭也註明兩名並存） | 待辦（僅登記）⚠️單組（二組補登） | — | 哪個名字對＝技術命名（v3 §03-1 內部自決）；無 bug 觸發，碰到該檔時收尾（§-1，⛔ 不單獨開工）。總管 |

**c-4 資料污染與融資支線**

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| B7a `finmind_margin.parquet` 單位混用（元／張數逐日交錯；來源 §6.4） | ~~待辦（查證）⚠️單組~~ → **已完**（**2026-09-26 二組複核更正**：`origin/main` 那份已乾淨，二組現場量測） | `a59ff63`（2026-09-24 cron 全檔重建，非 PR）＋ #682 `55331c4`（恢復融資走勢圖） | **二組實測（`origin/main` `84f58ea`，blob `22e4cf8`）**：4,912 列、2006-09-29～2026-09-24、日期單調且唯一；`source` 只剩一種（`…:MarginPurchaseMoney:TodayBalance`）、`fetched_at` 單一值（整檔同一次重抓）；**4,912／4,912 列落在 sanity `[500, 10000]` 億**、0 列 < 1e8；最大單日變動 12.9%（2025-04 崩跌，合理）。逐版回溯：`269a80d`（同日較早一次 cron）仍 2,975 列不合格 ⇒ **`a59ff63` 是轉乾淨的那一版**。**開發線那份（blob `2049223`）仍髒**（4,957 列、2,975 列不合格＝§6.4 量測對象），但它＝ merge-base `5808c03` 的版本、開發線自分岔後沒改過 ⇒ 日後合併時會取 main 的乾淨版。連帶：§6.5 (A)／(B) 對融資**已失效**。⚠️ 單一個第二組實測、未經第三組驗。總管 |
| B7b `finmind_m1m2.parquet` 負值（main 與開發線兩份都壞、壞法不同；來源 §6.4） | 卡住 ⚠️單組 | — | ~~FinMind token（本容器無）＋資料層凍結；刪資料可能**不可逆** ⇒ 客戶選 §6.5 (A) 給 token 重抓／(B) 刪列。~~ → **2026-09-26 二組複核更正**：§6.5 (A)／(B) 是**融資檔**的選項，**套不到 m1m2**。二組實測 `origin/main` 那份：238 列**全部**來自 `CBC:PXWeb:EF19M01+EF21M01`、`fetched_at` 單一值（＝ 2026-09-24 cron **整檔重抓**），仍有 **87 列** M1B 或 M2 為負（最早 2006-10-01、最晚 2026-07-01）⇒ **壞在現行 cron 的解析，不是歷史積欠**：`origin/main` `scripts/export_stock_db.py::_money_supply_sanity_gate` docstring 已自陳根因＝`scripts/update_macro_history.py::fetch_finmind_m1m2` 的 CBC PXWeb 解析「把流量當存量」（抓到月變動額而非餘額）。⇒ **FinMind token 無關**（該段不走 FinMind，檔名是歷史遺留）；重抓只會重現同樣的壞值；刪列也不是解（下次 cron 又寫回）。**要解＝改 cron 解析碼（L1 腳本，§-1.2 資料層凍結）**；資料可由 CBC 重建 ⇒ 屬「能重建」側（§-1.5.F 判定 3(5)），**不是不可逆**。⚠️ 單一個第二組實測＋讀 docstring，**未實跑 CBC**。連帶：(b) `m1b_m2_gap`。客戶（特許動 L1 腳本） |
| B7c M1B-M2 退到 `^TWII` 代理層未標明（`is_proxy_tier` 在 L1 `fetch_m1b_m2_block` 重新打包時被丟；來源 §6.4） | 卡住 ⚠️單組（程式閱讀、未實跑） | — | 客戶 2026-09-25 裁示「另開工單」、未排；修在 L1 ⇒ 須客戶排入＋特許 |
| B8 融資「趨勢燈」後續方向 (a′)(b)(c)（來源 §6.5） | 卡住 ⚠️單組 | — | 相對分位實測無鑑別力；客戶決定要不要做、走哪條（前一位總管推薦 (c) 先修 B7，未經第二組驗）。📌 2026-09-26 二組複核補註：(c) 的**融資部分**在 `origin/main` 已由 `a59ff63` 達成（見 B7a）；(a′)(b) 若要做，應以 main 那份乾淨檔重跑（§6.3 的預覽數字是在舊檔上算的，未在乾淨檔重現）。客戶 |

> ~~B7a 原列文字（編修組，保留）：📌 **疑已在 main 解決**：`origin/main` `a59ff63`（2026-09-24 cron）重寫該檔；#682 `55331c4`（commit `8a43f95`）與 `tests/test_macro_v2_tab.py` 的 B-4 段註解自陳「歷史檔重建為乾淨資料」並恢復融資走勢圖；§6.4 的混用量測對象是**開發線**那份（blob 與 main 不同：main `22e4cf8`／開發線 `2049223`）。本組**未重測**（本容器 python 無 pandas），⛔ 不得據此宣稱已解。下一步：現場量測 main 那份；若乾淨，§6.5 (A)／(B) 對融資部分即失效。總管（查證）~~
> ↑ **2026-09-26 二組複核更正**：原列「本組**未重測**（本容器 python 無 pandas）」不精確 —— 系統 `python3` 確實無 pandas／pytest，但本 session 的 scratchpad 內有 venv（pandas 3.0.6／pytest 9.1.1／streamlit 1.59.2），二組已用它現場量測（結果見 B7a 列）。⚠️ scratchpad 非持久，新容器須自建 venv。

**c-5 分支／流程**

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| B10 分支政策矛盾、PR #675（draft、base `v2`）（來源 §4.2～4.3） | 卡住 | — | 2026-09-26 實測：PR #675 仍 `open`／`draft`／base `v2`／head `7f87f41`（本開發線）；GitHub 上 open PR **只有 #675**；開發線 vs `origin/main` ahead 102／behind 84，`origin/v2` vs `origin/main` ahead 14／behind 84。分支政策為客戶頒布 ⇒ 依 §4.4 格式附推薦請示；⛔ 不得自行 merge／轉 ready／改 base。客戶 |
| B11 `STRATEGY_INTAKE.md` §4 七項待裁（三套方法論納入與否等） | 卡住 | — | 客戶未回。客戶 |
| C6 §4.5 其他分支是否已合併、可否刪（§8 #14；2026-09-26 完整性複查補登） | **已完**（查證；2026-09-26；~~待辦（查證）⚠️單組~~；兩組獨立稽核〔patch 等價角度＋檔案內容角度〕、結論一致 —— 各自為單組結論，交叉一致） | —（查證，無程式改動） | ⭐ **結論（2026-09-26）**：① **`data`：⛔ 勿刪** —— 活的排程線：`origin/main` `.github/workflows/export_db.yml` 每日 UTC 21:00（`cron: "0 21 * * *"`）以 orphan 分支 `_pub` ＋ `git push -f origin _pub:data` 發布 `stock.db`（永遠 1 個 commit）；下游 `2026_strategy_0719` 讀取；`scripts/verify_data_branch.py` 讀 `origin/data:stock.db`（tip 每日被 force-push 覆寫，編修組本次 `git ls-remote` 為 `cb118b5`）。② **`feat/v2-card-template`（`c59a67a`，PR #677 closed 未 merge）：可刪** —— 其 `src/ui_v2/` 七檔已由 `dbab9b1`（PR #678）等價帶入 main（編修組複驗：限定這七檔的 patch-id 相同、blob 逐檔相同；`dbab9b1` 整個 commit 另含其他檔，為超集，故整 commit 的 patch-id 不同）。③ **`wireframe/stock-ia-5pages`（`2744e8b`）：可刪** —— 已被 main 的 `8c0c8d6` 起的修訂版取代。④ **`claude/my-stock-dashboard-ui-rewrite-r40gjc`（`a30cde7`，只含 `docs/SESSION_CHECKPOINT_2026-09-08.md`）：有條件可刪** —— 同內容（blob `6e985c9`）以 `docs/v2/audit/CHECKPOINT.md` 存在於 `origin/v2` 與本開發線（commit `671b0c6`）；**前提是 `v2` 保留**。⑤ 本機 10 條過期 remote-tracking ref（`origin/claude/*`，tip 皆已在 main 祖先內），**prune 未執行**；部分有同名本地分支被 `/home/user/msd-*` worktree 占用。（總管抽查屬實：`export_db.yml` cron 與 orphan＋force-push 兩處、`origin/v2` 有 `docs/v2/audit/CHECKPOINT.md`；編修組另抽查 ②④ 的 patch-id／blob 與 `8c0c8d6` 在 main 祖先內。）刪分支＝不可逆 ⇒ 另登 **C6-del**（下列，卡住）。｜以下為查證前的描述（保留作歷史）：~~📌 2026-09-26 `git ls-remote`：§4.5 列的 `feat/v2-card-live`／`feat/v2-fullpage-cards`／`feat/v2-verdict-cards`／`test/ui-v2-guards` **已不在 remote**；尚存 4 條皆「非 `origin/main` 祖先、各多 1 個 commit」：`claude/my-stock-dashboard-ui-rewrite-r40gjc`（2026-09-08 session 中止點文件）、`data`（2026-09-25 `stock.db` `[skip ci]` —— ⚠️ 疑為排程資料線，⛔ 勿刪）、`feat/v2-card-template`（2026-09-24）、`wireframe/stock-ia-5pages`（2026-09-05）。「多 1 commit」≠ 未併（可能已由別的 commit 帶入），**內容未比對**；本機另有已刪分支的過期 remote-tracking ref（未 prune）。查證：總管；刪：客戶（不可逆）~~ |
| C6-del 刪 `feat/v2-card-template`、`wireframe/stock-ia-5pages`、（`v2` 保留前提下）`claude/my-stock-dashboard-ui-rewrite-r40gjc`；**⛔ `data` 勿刪**（C6 查證結果，2026-09-26 登記） | 卡住（不可逆，待客戶） | — | 刪 remote 分支＝**不可逆** ⇒ 請示客戶（附推薦：刪上列三條、保留 `data`）；`ui-rewrite-r40gjc` 須以 `v2` 保留為前提。本機過期 remote-tracking ref 的 prune 不在本列（非刪 remote）。客戶 |

**c-6 僅登記小修**（§-1：無 bug 觸發，碰到該檔才順手收尾，⛔ 不單獨開工）

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| C4 `vix` L2 log 理由「沒有歷史序列」在序列被拒時不精確 | 待辦（僅登記）⚠️單組 | — | 僅 log。總管 |
| C5a 註解寫 `V2_VALID_EMPTY_PAIRS`、實際符號是 `V2_VALID_EMPTY_SPEC`（§6.4） | 已完 ⚠️單組 | #692 `10f385c` | 無（2026-09-26 `origin/main` 的 `src/`、`shared/` grep 0 命中） |
| C5b `load_binding` 契約漂移（假設性）；`page_inspect.py` 未用的 `UI_EMPTY` import（既有 pyflakes）；`src/ui_v2/render.py` `grid_html`／`layer_html` 空行外露（僅原型路徑）；`markup.py` title／fold 空行只在頁面層擋；`pr-check.yml` 觸發不存在的 `ui-v2`（§4.5；📌 2026-09-26 核對：只有**開發線**那份有此行，`origin/main` 那份只觸發 `main`） | 待辦（僅登記）⚠️單組 | — | 總管 |

**c-7 待線上／實機驗證**（沙箱驗不到，§-1.5.A-7）

| 項目 | 狀態 | merge hash | 還缺什麼＋誰決定 |
|---|---|---|---|
| D1 「▸ 詳細」a11y（#697）在真 iPhone／VoiceOver 的朗讀與切換 | 卡住（沙箱無 WebKit／VoiceOver） | #697 `f14c5fd` | 程式＋5 頁 72 組截圖已驗（滑鼠／觸控零像素差）；**實機未驗** ⇒ 客戶用手機試用時順帶確認。客戶 |
| D2 `ndc_signal` 方向列（#697）對線上真資料 | 卡住（沙箱連不到 FinMind／data.gov.tw） | #697 `f14c5fd` | 單元測試已驗；**未對真資料跑** ⇒ 部署後看今天頁該卡是否出「較上月」。客戶 |

**清點盲點**（⚠️ 本輪**未**納入，⛔ 不得把上表讀成全量）
- `docs/v2/stage1/INDICATOR_SSOT.md` 的 `N-1`～`N-14` 計算層缺陷修復進度 —— 本輪未查。
- `STATE.md`／`ARCHITECTURE.md`／`SPEC.md` 是否已依 `PROCESS.md §4` 同步 #687～#698 —— 本輪未查。
- 上表計數、分類與「待辦／卡住」的劃分為**單組**清點（§-2 規則 6），待第二組複驗。（📌 2026-09-26 二組已逐列重算計數、核對全部 merge hash 與 16 盞 SSOT，更正見各處「二組複核更正」；**分類判讀**〔待辦 vs 卡住、五類歸屬〕二組只抽查、未逐列重判。）
- 🆕（2026-09-26 二組補登）開發線 `docs/v2/spec/DATA_MAP_GAPS.md`（364 行）的**規格洞登記簿**（今天頁 ①~⑲／ⓐ~ⓕ、找標的 `ED-*`／`EE-GAP-*`、憑什麼 `WHY-G-*`／`FA-*`、查一檔 `INS-G-*` 等）—— 本表**未逐條併入**；只挑出有 commit 自陳「另案」的一條（DOC2）。
- 🆕（2026-09-26 二組補登）四頁的 `unwired`（未接線）卡 —— 例：#688 `57b5993` 訊息自陳持股頁「7 塊未實作照現況」（`origin/main` `src/ui/views/page_hold.py` 有 `REBALANCE_UNWIRED_NOW`／`GRAPE_UNWIRED_NOW`／`PICK_SHEET_UNWIRED_NOW`／`WATCHLIST_UNWIRED_NOW` 等）。本表只列了**今天頁**的（B4）；其餘四頁的未接線卡**未盤點**，是否算待辦（多數依規格刻意不接）亦未判定。

以下子表為**明細**（保留）；狀態以上方 (a)(b)(c)(d) 主表為準。

**B3 子表：方向列「無資料」逐盞**（2026-09-26；16 盞中 **11 盞無資料**；已有真方向 5 盞：`margin`／`bias_240`／`ism_pmi`／`vix`／`ndc_signal`，出處 `shared/lamp_direction_thresholds.py`）
類別：**①沒歷史 ②需新源 ③判斷未定 ④其他**。

| 燈 key | 類別 | 為什麼無資料 | 要解需要什麼 | 誰決定 |
|---|---|---|---|---|
| `health`（總經健康評分） | ④ | 唯一歷史是前進式驗證凍結檔 `data_cache/macro_forward_test/signals.parquet`：`date` 是 **cron 執行日**不是交易日（區間缺 4 個交易日、含週六列、09-22 過期重複列）；燈值 dict 不帶 `health_partial`、輸入不同（cron `fetch_macro_bundle` vs 頁面 session）；且 `src/ui/views/page_today.py::OUT_OF_REACH_LIGHT_KEYS` 本就列著 `health` | ⚠️ 需與燈值同源、按交易日落地的歷史（屬新增資料落地，§-1.2 資料層凍結）—— 編修組判讀 | ⚠️ 客戶 |
| `jingqi` | ④（雜訊） | 5 日均的 lag-20 自相關 **0.025**（`twii_ohlcv.parquet` 4,886 列）⇒ 20 日差是雜訊，同 `adl` | — | 總管：**不做**（同 C1） |
| `tw_export`（台灣出口） | ①／② | L1 只回最新一點；本機無出口 YoY 歷史、外部源 403；5 路 fallback 口徑不一；MOF 月資料回溯修正 ±5% | 可提供歷史的源；日後接須限定同一條 fallback、同一次抓取的相鄰兩月 | 客戶 |
| `m1b_m2_gap`（M1B-M2） | ④（資料損壞） | `finmind_m1m2.parquet` 負值，程式刻意不讀 | 先解 B7 | 客戶（同 B7） |
| `fut_net` | ① | ~~僅存 14 天~~ → L1 `src/data/macro/leading_indicators.py::build_leading_fast` 每次只**即時**抓近 ~14 天（TAIFEX 逐日、有 timeout 預算），`data_cache/` 無落地歷史 ⇒ **不會自己累積** | ~~等累積~~ → 需新增落地歷史或拉長抓取窗（動 L1，§-1.2） | ~~⚠️ 總管~~ → ⚠️ 客戶 |
| `us_core_cpi` | ② | 只有前值、無日期 | 帶日期的源 | 客戶 |
| `news_systemic` | ③ | 無數值序列、判準未定義（亦列於 `OUT_OF_REACH_LIGHT_KEYS`） | 判準規格 | 客戶 |
| `foreign_net` | ④（燈未接線） | 決策端未接線（燈永遠不亮，卡上不出此列）；FinMind 法人單位未確認（§4.1） | 先查證單位 | ⚠️ 總管 |
| `adl` | ④（雜訊） | 見 C1 | — | 總管：**不做**（C1） |
| `us10y`／`dxy` | ② | 見 B1／B2 | 見 B1／B2 | 客戶 |

⚠️ `fut_net` 列的更正（「等累積」→ 不會累積、誰決定改客戶）為**編修組 2026-09-26 程式閱讀**（`build_leading_fast` 的 14 天抓取窗＋`git ls-tree origin/main data_cache/` 無對應檔），**未經第二組驗**；原文保留加刪除線。類別歸屬中 `health`／`jingqi`／`tw_export` 依總管 2026-09-26 指示，其餘為編修組歸類。

**B6 子表：標記矛盾逐卡審查（2026-09-26，客戶核准；兩組獨立審查、結論一致）**
**結論**：**無新增 #11 登記** —— `src/ui/views/page_today.py::V2_VALID_EMPTY_SPEC` 維持只有 `("hold.portfolio_count", "src.ui.views.page_hold", "COUNT_EMPTY_NOW")`；**本輪零程式變更**。登記門檻（該符號註解明文）：同一對 `(key, now)` ⛔ 不得再蓋著任何一條「真缺漏／上游失敗被吞成空／還沒載入／未評估／未綁定」路徑。

| 卡（`key`／`now` 常數） | 為什麼不能登記（這個「空」還蓋著哪條歧義路徑） | 相關 |
|---|---|---|
| `find.screen_result`／`SCREEN_EMPTY_NOW` | ① 表單允許一個因子都不勾 → L3 回空＋「請至少勾選」；② 去年季快照缺 → 存活 0 檔＋「季快照未就緒」，**不報錯**；③ 唯一勾選因子自身掃描失敗 → 靜默為空 | ③＝B9 ①；2026-09-26 S5-F 已修 ②③：#701 `2810064`（失敗 → 紅；① 仍未處理 → B9 ⑨；#11 仍未登記） |
| `find.sector_flow`／`FLOW_EMPTY_NOW` | 快取尚未產出 ≠ 真的沒有資金流 | — |
| 持股頁 `EMPTY_SHEET_NOW` | Sheet 列若「張數／均價」空白，被 `parse_portfolio_records` **靜默丟掉** ⇒「空」可能是整張被丟光 | — |
| 持股頁 `NOT_BOUND_NOW`（含 `hold.binding`） | token／sheet-id 讀取錯誤被吞成「未綁定」；`hold.binding` 另經客戶排除 | ＝Q1（~~2026-09-26 稽核通過、PR 準備中；—（PR 準備中）~~ → 2026-09-26 **已完** #702 `0769730`：讀取失敗 → 既有紅態；#11 仍未登記；全域狀態列 → Q1-r1） |
| `hold.take_profit`／`TP_EMPTY_NOW` | 抓價失敗靜默 → `current_price` 為 `None`、該列無錯誤 → 被跳過；「沒有一檔達門檻」可能是價格沒抓到 | ＝B9 ② |
| `hold.switch`／`SWITCH_EMPTY_NOW` | 否決 regime 嚴格模式缺資料時回 `[]`；未判定燈號被排除在換出候選外 | 同 C2 |
| `hold.deep.dividend_cash`／`CASH_NO_PAYOUT_NOW` | yfinance 回空但**不拋例外** ⇒ 分不出「沒配息」與「沒抓到」 | 同 C3 |
| 拆分／壓力／VaR／現金 空態（`hold.alloc_split` `SPLIT_NO_VALUE_NOW`／`hold.deep.stress` `STRESS_EMPTY_NOW`／`hold.deep.var` `VAR_EMPTY_NOW`／`hold.deep.dividend_cash` `CASH_EMPTY_NOW`） | 空＝輸入缺漏，不是有效的零 | 拆分＝Q2（~~2026-09-26 已派工、實作中~~ → ~~2026-09-26 稽核中，—（待合併）~~ → 2026-09-26 **已完** #704 `45d5385`：全部失敗 → 紅；#11 仍未登記）；壓力／VaR／現金＝Q3（~~待辦~~ → ~~2026-09-26 已派工、實作中，—（待合併）~~ → 2026-09-26 稽核中，—（待合併）） |
| `hold.macro_stage` `MACRO_EMPTY_NOW`／`hold.position_cap` `CAP_EMPTY_NOW` | 未評估 ≠ 有效的空 | — |
| `hold.vix`／`VIX_EMPTY_NOW` | 抓取未命中 | 2026-09-26 S5-H 已修：#699 `bd6ceec`（抓不到 → 紅 `VIX_FAILED_NOW`；#11 仍未登記） |
| 查一檔（inspect）各卡 | 抓取失敗時欄位為 `None`，與「真的沒有」同一條路徑；批次卡＝輸入空或格式錯 | ＝Q5（~~2026-09-26 進行中：QA 回報 1 項阻擋、發回修正中 → 修正完成〔實作組自述〕→ 搬主線解衝突中 → 待 QA 複驗；—（待合併）~~ → 2026-09-26 **已完** #703 `76bec1b`：抓取失敗 → 紅；#11 仍未登記；估值卡現價、ETF 溢價兩條路徑 production 少觸發，另見 Q5-r2） |
| `why.qa`／`QA_EMPTY_NOW` | Gemini 安全封鎖／回覆格式錯被吞成「成功＋空」；且此態屬「缺漏·可重跑」⇒ #11 不適用 | 2026-09-26 S5-W 已修：#700 `3f4493c`（封鎖／格式錯 → 紅 `QA_FAILED_NOW`；明確 `STOP` 無文字仍灰；#11 仍未登記） |
| `why.source.none`／`EMPTY_REGISTRY_NOW` | 未載入（缺漏·可重跑）⇒ #11 不適用 | — |
| 今天頁 | 客戶排除；其空態屬未評估／未知 | — |

**解法（二選一，客戶決定；⛔ 均未動工）**：現有 `*_NOW` 常數**拆不開任何一對**（同一個常數同時蓋著「真的零」與「歧義」），要解 B6 只剩兩條路 ——
1. **先修上游歧義路徑，讓失敗轉紅**（逐卡動 L1／L3，例：`hold.take_profit` 缺價列、`parse_portfolio_records` 靜默丟列、`why.qa` 吞掉安全封鎖、`find` 不勾因子／季快照缺）⇒ 修完後剩下的「真零」子路徑才可能單獨登記 #11。**需客戶逐卡授權動 L1／L3**（§-1.2 資料層凍結＋§-1）。
2. **新文案**把「真零」與「歧義」分成不同 `now`（規格／文案決定）⇒ **需客戶給文案**。

📌 **順帶登記（不修）**：`docs/v2/spec/UI_PAGE_FIND.md`「逐項處置」第 3 點的 2026-09-26 補註，把 `find.screen_result` 不能登記的理由之一寫成「存活池讀取失敗（`_load_survivors()` 回錯 → L3 回空表）」—— **自 #690 起該理由已過期**；結論「不得登記」仍成立（改由上表 ①②③ 支撐）。**僅登記，規格文件本輪未動。**
⚠️ 本子表的逐卡路徑分析為**兩組獨立審查一致**的結論（總管轉述）；編修組只核對了符號名（`V2_VALID_EMPTY_SPEC` 內容、各 `*_NOW` 常數、卡 key）在 `origin/main` `35d8cc3`（＝`f14c5fd`＋一筆機器人資料 commit）上存在，**未重做路徑分析**。拆分／壓力／VaR／現金、macro／cap、vix 三列的 `key`↔常數對映由編修組依常數名補上，**未經第二組驗**。

**已完成（近期）**

| 項目 | merge |
|---|---|
| 今天頁 16 張燈卡「▸ 詳細」折疊（客戶核准線框）＋改樣式開關修 iPhone 點不動（＝主表 a7；2026-09-26 每 5 項自檢補登：原只在主表、本流水表漏列） | #685 `9fe19fd`、#686 `8873c9e` |
| 找標的頁卡化 | #687 `83e0f47` |
| 我的持股頁卡化 | #688 `66e4fe0` |
| 標記 #11「▨ 無資料」（規格變更） | #689 `3e576ef` |
| 靜默失敗批 1（`find.screen_result`、`hold.take_profit`） | #690 `2a56483` |
| 查一檔、憑什麼兩頁卡化（§3.2「四頁待卡化」至此結案） | #691 `f38dbd4` |
| 今天頁「風險」格接上 danger | #692 `10f385c` |
| `hold.switch` 靜默失敗（L3）＋ 16 盞方向登錄（VIX 真值）＋ hover 空行修正 | #693 `ac96000` |
| 配息卡靜默失敗（L1 `fetch_etf_dividends`，失敗冷卻 180s）＝ 靜默失敗第 2 批 | #694 `d48709b` |
| 5 頁卡片文字空行破版 | #695 `07f8800` |
| 測試缺口補齊（僅測試） | #696 `6ec1cb9` |
| （原 A1）「▸ 詳細」a11y：可及名稱改為「▸ 詳細 ＋ 卡片標題」（`aria-labelledby`）、`aria-controls` 指向內容區、鍵盤焦點框 1px 點線 → 2px 實線並外移（`outline-offset:2px`，不壓「▸」）；⛔ 刻意不用 `aria-expanded`（無 JS 無法隨勾選更新，寫死會對朗讀說謊，§1）；5 頁滑鼠／觸控零像素差異。📌 開關在此之前**本來就能用鍵盤聚焦**（§6.4 舊「鍵盤點不到」已加註更正）。⚠️ 實機未驗 → D1 | #697 `f14c5fd` |
| （原 A2）方向列：`ndc_signal` 改真方向（官方整數 9–45、帶寬 0、「較上月」；L1 `fetch_ndc_block` 只在 FinMind／6099 分支從同一次抓取附帶上月值，純新增欄位、不新增抓取）；`health`／`jingqi`／`tw_export` 經量測＋獨立複查**維持「無資料」**（原因見 B3 子表）。⚠️ 未對真資料實測 → D2 | #697 `f14c5fd` |
| B6 逐卡審查完成（兩組獨立一致）：無新增登記（B6 本身**仍未結案**，矛盾待客戶二擇一，見 B6 子表） | 2026-09-26（零程式變更，無 merge） |
| 衛星停利卡：持有個股抓不到現價 → 紅卡（＝靜默失敗第 4 批，原 B9 ②；只改 L5） | #698 `84f58ea` |
| B7a 融資歷史檔（`origin/main` 那份）已乾淨 —— cron 整檔重建；2026-09-26 二組現場量測確認（開發線那份仍髒，＝merge-base 版） | `a59ff63`（cron，非 PR）＋ #682 `55331c4` |
| 清點交接本（本輪，(d) 派工追蹤）：四欄進度總表＋給下一個 AI＋常設規矩；暫停中工作留 patch（`docs/v2/wip_patches/`）；二組複核更正 | 見本 commit（docs(handoff): 全面清點…；開發線 `claude/stock-dashboard-handoff-g9dtm0`，非 merge） |
| S5-H `hold.vix`（💼 我的持股 VIX 卡）抓不到 → 紅 `VIX_FAILED_NOW`（不再畫成灰色「有效的結果」）＝靜默失敗批 5 第 1 項：L3 `fetch_vix(strict=)`＋L5 `load_vix` 用 strict；獨立 QA 通過（17 情境、其他持股卡 5 萬張逐位元組相同、19 突變全殺）；非阻擋文案殘留 → B9 ⑤ | #699 `bd6ceec` |
| S5-W `why.qa`（📖 憑什麼 AI 問答卡）Gemini 封鎖／格式錯 → 紅 `QA_FAILED_NOW`（不再當成「有效結果」）＝靜默失敗批 5 第 2 項：L3 `run_agent(fail_on_blocked_reply=)`（預設 False）只有 `page_why` opt in；`blockReason`／非 `STOP`／缺 `finishReason`／格式錯 → 紅，明確 `STOP` 無文字 → 灰；錯誤文字只列 key／型別、已遮蔽；獨立 QA 兩輪（第 1 輪 blocker「`STOP` 無 `parts` 顯示成紅」已修） | #700 `3f4493c` |
| S5-F `find.screen_result`（🔍 找標的選股結果卡）唯一勾選因子自身輸入失敗／季快照缺 → 紅卡（不再當成「有效結果」）＝靜默失敗批 5 第 3 項（＝B9 ① ＋ B6 子表 find ②③）：改 L1 `yield_pe_fetcher.py`、L3 `fundamental_screener_service.py`／`valuation_service.py`、L5 `page_find.py` ＋ 4 個測試檔（共 8 檔，編修組 `git diff --stat` 實測）；獨立 QA 一組通過；CI Fast／Slow check run 皆 success（GitHub API 實測）；非阻擋殘留 → B9 ⑥～⑨ | #701 `2810064` |
| Q1 持股頁 `NOT_BOUND_NOW`（💼 我的持股）：token／sheet-id 讀取錯誤不再被吞成灰「未綁定」→ 走既有紅態（不新增使用者可見文案）：L1 `gsheet_portfolio.py`、L3 `portfolio_binding_service.py`／`holdings_service.py`、L5 `page_hold.py` ＋ 新測試 `tests/test_v2_silent_fail_b7_binding.py`（共 5 檔，編修組 `git diff --stat` 實測）；獨立 QA 一組通過；CI Fast／Slow check run 皆 success（GitHub API 實測）；非阻擋殘留 → Q1-r1～r5 | #702 `0769730`（PR head `2705861`） |
| C6 殘留分支查證（兩組獨立稽核、交叉一致）：`data` ⛔ 勿刪（活的排程線）；`feat/v2-card-template`、`wireframe/stock-ia-5pages` 可刪；`claude/my-stock-dashboard-ui-rewrite-r40gjc` 在 `v2` 保留前提下可刪 ⇒ 刪除另登 **C6-del**（卡住，客戶） | —（查證，無程式改動；2026-09-26） |
| Q5 查一檔（🔬 inspect）各卡：抓取失敗不再被當成「沒資料」→ 走紅態：L1 `data_loader.py`／`financial_statements_fetcher.py`、L3 `dividend_station_service.py`／`stock_grp_service.py`、L5 `page_inspect.py` ＋ 新測試 `tests/test_v2_silent_fail_b6_inspect.py`（共 6 檔，編修組 `git diff --stat` 實測）；原 QA 組初驗 1 阻擋（B1）＋ 3 測試缺口 → 已修 → 複驗通過、無阻擋（單組）；「2 處註解言過其實」複驗確認已修正且據實；CI Fast／Slow check run 皆 success（GitHub API 實測）。**已知限制**：估值卡現價、ETF 溢價在 production 紅路徑少觸發（⛔ 不得對客戶宣稱已修）；非阻擋殘留 → Q5-r1（待辦）／Q5-r2（卡住） | #703 `76bec1b`（PR head `185392e`） |
| Q2 `hold.alloc_split`／`hold.deep.core_satellite`（💼 我的持股分配卡）全部失敗不再落空態 → 紅；**併入 S4-T**：停利卡與分配卡 ⑤⑥ 共用新常數 `NO_PRICE_WHY`（why 截在「，」、去掉不實的「可以重跑一次」）；紅態不再渲染「⚠️ 這個比例只涵蓋一部分」。只改 L5 `page_hold.py` ＋ 3 個測試檔（共 4 檔，編修組 `git diff --stat 76bec1b 45d5385` 實測）；獨立 QA 一組通過、無阻擋（p04 窮舉 50,481 張卡對 baseline 只有預期 8 張不同 —— 總管轉述）；CI Fast／Slow check run 皆 success（GitHub API 實測）；非阻擋殘留 → Q2-r1（待辦）／Q2-r2（低優先，僅登記）／Q2-r3（待辦） | #704 `45d5385`（PR head `0c7fa54`） |

📌 2026-09-26 清點輪起，**狀態以上方 (a)(b)(c)(d) 主表為準**（主表已含全部「已完」列與 hash）；本表照客戶規則保留作流水。
📌 **2026-09-26 每 5 項自檢**（常設規矩 §7「拿 git log 對 HANDOFF，補漏」）：`git log --merges --oneline origin/main -15`（#685～#699）逐一比對 hash —— #687～#698 皆在本表；**#699 `bd6ceec` 本次補**；**#685 `9fe19fd`／#686 `8873c9e` 只在主表 a7、本表漏列 ⇒ 已補登**；其餘無缺。⚠️ 編修組機械比對，單組。
📌 **2026-09-26 自檢計數器（四更新）**：自上次自檢以來 merge 了 #699 `bd6ceec`、#700 `3f4493c`、#701 `2810064`、#702 `0769730` 共 **4 項**（總管計）⇒ **距下次自檢 1 項**：**下一個 merge 後**即拿 `git log --merges --oneline origin/main` 對本表與主表，補漏。⚠️ 並陳：上行自檢紀錄的比對範圍已含 #699（「#685～#699」）⇒ 依紀錄為 3 項、距下次 2 項；**取嚴，照總管「下一個 merge 後自檢」執行**（提早自檢無害）。（→ 2026-09-26 五更新：#703 merge 後**已執行**，見下行。）
📌 **2026-09-26 每 5 項自檢（第 2 次，#703 merge 後觸發）**：`git fetch origin main` → `git log --oneline --first-parent origin/main`，範圍 `84f58ea..76bec1b`（起點取 #698 `84f58ea` 之後 ⇒ **含 #699**；上次自檢紀錄範圍「#685～#699」本已含 #699，本次照總管要求重核）。first-parent 共 **5 個 commit、全為 PR merge、期間無其他 commit**（無機器人資料 commit）。逐一比對本檔（主表＋本流水表，各須有一行含 hash 與狀態）：

| merge | 項目 | 主表 | 本流水表 | 結果 |
|---|---|---|---|---|
| #699 `bd6ceec` | S5-H `hold.vix` | (c) c-1 S5-H 列、(d)、a4 | 有 | 無漏 |
| #700 `3f4493c` | S5-W `why.qa` | (c) c-1 S5-W 列、(d)、a5 | 有 | 無漏 |
| #701 `2810064` | S5-F `find.screen_result` | (c) c-1 S5-F 列、(d)、a2 | 有 | 無漏 |
| #702 `0769730` | Q1 `NOT_BOUND_NOW` | (c) c-1 Q1 列、(d)、a4 | 有 | 無漏 |
| #703 `76bec1b` | Q5 查一檔各卡 | (c) c-1 Q5 列、(d)、a3 | 有 | 本次更新即寫入（非補漏） |

⇒ **無漏登**。**兩種算法的出入，本次以實際 git log 為準**：上次自檢紀錄範圍寫「#685～#699」（且 #699 為「本次補」），故依紀錄上次自檢點＝#699 之後、至今 #700～#703＝**4 項**；總管計「#699～#703」＝**5 項** —— 差在 #699 是否算在上次自檢內。實際 `git log --first-parent 84f58ea..76bec1b` 為 5 個 merge、`bd6ceec..76bec1b` 為 4 個；本次**兩個範圍都已逐一核對**（取寬，#699 一併核），故出入不影響結論。附帶：上次自檢用 `git log --merges`，會漏掉 first-parent 上的非 merge commit（例：機器人資料 commit `35d8cc3`、`0c29572`、`f9aea73`、`f9baab2`、`0ba303f`，皆在上次範圍、屬排程資料回寫、非工作項，本檔不逐筆登記）；本次改用 `--first-parent`，範圍內無此類 commit。⚠️ 編修組機械比對，單組。
📌 **2026-09-26 自檢計數器（五更新）**：**歸零** —— 自檢點＝`76bec1b`（#703）；**距下次自檢 5 項**（下一次：自 `76bec1b` 起累計 5 個 merge 後，拿 `git log --oneline --first-parent origin/main` 對本表與主表）。（→ 六更新：已過 1 項，見下行。）
📌 **2026-09-26 自檢計數器（六更新）**：上次自檢點＝`76bec1b`（#703，已含於上次自檢）；其後 merge **1 項**＝#704 `45d5385`（編修組 `git log --oneline --first-parent 76bec1b..origin/main` 實測：只有這 1 個 commit，為 PR merge；已寫入主表 (c) c-1 Q2 列、(d)、a4 與本流水表）⇒ **距下次自檢 4 項**。

### 給下一個 AI

> 固定格式（客戶 2026-09-26【交接表 · 維護時機】「交接段」）；**每次更新本節時同步改這五點**。以下據 2026-09-26 清點實況填寫（**第 1～2 點於 2026-09-26 派工 S5-H／W／F 時更新**；**2026-09-26 S5-H 已完 #699 `bd6ceec`／派工 Q1、Q5 時再更新第 1～5 點**；**2026-09-26 S5-W 已完 #700 `3f4493c`／S5-F PR #701 時再更新第 1～5 點**；**2026-09-26 S5-F 已完 #701 `2810064`／Q1 稽核通過／Q2 派工／Q5 發回修正時三更新第 1～5 點**；**2026-09-26 Q1 已完 #702 `0769730`／C6 查證完成／Q5 搬主線解衝突／B9 ⑥～⑧ 派工時四更新第 1～5 點**；**2026-09-26 Q5 已完 #703 `76bec1b`／Q2（含 S4-T）稽核中／B9 ⑥⑦ 稽核中／Q3 派工時五更新第 1～5 點**；**2026-09-26 Q2（含 S4-T）已完 #704 `45d5385`／B9 ⑥⑦ 複驗中／Q3 稽核中時六更新第 1～5 點**）；⚠️ 工作區內容為編修組讀 diff 所得，**單組、未 QA**。

1. **從哪項接手（一項）**：📌 **2026-09-26 現況（Q2 已完 #704 `45d5385` 後六更新）**：**接手一項＝Q3**（`hold.deep.stress`／`hold.deep.var`／`hold.deep.dividend_cash` 持股深度分析三卡「算不出來」；實作完成、動 L1 `etf_fetch.py`／L3 `portfolio_deep_service.py`／L5 `page_hold.py`，**稽核中：獨立 QA**，`/home/user/msd-q3`）→ **QA → PR → CI 綠 → merge → 立刻本節寫一行含 hash**。**並行中**：**B9 ⑥⑦**（QA 1 阻擋〔趨勢因子 where〕已修 → **複驗中**，`/home/user/msd-b9`），見 (d)。**之後的順序**（總管排程）：**Q4**（`EMPTY_SHEET_NOW`：Sheet 列被 L1 `parse_portfolio_records` 靜默丟掉）→ **Q1-r2／r3／r5** → **Q1-r4** → **Q2-r1** → **Q5-r1**（只刪句）→ **B9 ⑩⑫** → 小項 **C4／C5b／DOC1／DOC2**。**位置未定**（總管未排）：**B9 ⑪⑬**、**Q2-r3**、**Q3-r1～r5**；**Q2-r2** 低優先、僅登記（不在排程）。**卡住（不在排程）**：**B9 ⑧**（客戶定措辭）、**B9 ⑨**（規格洞）、**Q1-r1**（字樣＝客戶）、**Q5-r2**、**C6-del**（刪分支＝不可逆，待客戶）。
   ~~（歷史，Q2 已結案 #704 `45d5385`）📌 **2026-09-26 現況（Q5 已完 #703 `76bec1b` 後五更新）**：**接手一項＝Q2**（`hold.alloc_split`／`hold.deep.core_satellite` 全部失敗仍落空態；**已併入 S4-T**〔停利卡與分配卡 ⑤⑥ 共用新常數 `NO_PRICE_WHY`〕；實作完成、只動 L5 `page_hold.py`，**稽核中：獨立 QA**）→ **QA → PR → CI 綠 → merge → 立刻本節寫一行含 hash**。**並行中**：**B9 ⑥⑦**（稽核中：獨立 QA，`/home/user/msd-b9`）、**Q3**（實作中，`/home/user/msd-q3`），見 (d)。**之後的順序**（總管排程）：**Q4** → **Q1-r2／r3／r5** → **Q1-r4** → **Q5-r1**（只刪句）→ **B9 ⑩**（先 QA 核實讀碼）→ **Q2-r1／Q2-r2** → 小項 **C4／C5b／DOC1／DOC2**。**卡住（不在排程）**：**B9 ⑧**（客戶定措辭）、**B9 ⑨**（規格洞）、**Q1-r1**（字樣＝客戶）、**Q5-r2**、**C6-del**（刪分支＝不可逆，待客戶）。~~
   ~~（歷史，Q5 已結案 #703 `76bec1b`）📌 **2026-09-26 現況（Q1 已完 #702 `0769730` 後四更新）**：**接手一項＝Q5**（查一檔 `inspect` 各卡靜默失敗；實作組回報已修好 QA 阻擋 B1 ＋ 3 個測試缺口〔⚠️ 實作組自述、未經 QA 複驗〕，正在 `/home/user/msd-q5` 解與 #699 的衝突）→ **送原 QA 組複驗 → PR → CI 綠 → merge → 立刻本節寫一行含 hash**。**並行中**：**Q2**（實作中，`/home/user/msd-q2`）、**B9 ⑥～⑧**（實作中，`/home/user/msd-b9`），見 (d)。**之後的順序**（總管排程）：**Q3 → Q4 ＋ S4-T**（持股頁 `hold` 軌）→ **Q1-r2／r3／r5** → **Q1-r4** → **Q5-r1**（只刪句）→ 小項 **C4／C5b／DOC1／DOC2**。**C6** 查證已完（刪分支＝**C6-del**，卡住待客戶）；**Q1-r1** 卡住（字樣＝客戶）。**B9 ⑨** 未列入本排程 ⇒ 位置未定（⚠️ 編修組照總管排程轉述，未另排）。~~
   ~~（歷史，Q1 已結案 #702 `0769730`）📌 **2026-09-26 現況（S5-F 已完 #701 `2810064` 後三更新）**：**接手一項＝Q1**（`NOT_BOUND_NOW`：token／sheet-id 讀取錯誤被吞成「未綁定」；獨立 QA **通過、無阻擋**〔單組 QA〕、已套上新 main）→ **開 PR → CI 綠 → merge → 立刻本節寫一行含 hash**。**並行中**：**Q2**（實作中，工作區 `/home/user/msd-q2`）、**Q5**（QA 阻擋 B1 修正中），見 (d)。**之後的順序**：**Q3 → Q4 ＋ S4-T**（持股頁 `hold` 軌）→ **B9 ⑥～⑨**（`page_find.py` 收尾；前提「PR #701 merge 後」已成立）→ **Q5-r1**（只刪句）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**。Q1-r2～r5 為新登記待辦（c-2），總管指示的順序未列入 ⇒ 位置未定（⚠️ 編修組照總管指示轉述，未另排）。~~
   ~~（歷史，S5-F 已結案 #701 `2810064`）📌 **2026-09-26 現況（S5-W 已完 #700 `3f4493c` 後更新）**：進行中三軌 —— **S5-F**（稽核通過、**PR #701 待合併**，等 CI）、**Q1**（`NOT_BOUND_NOW` token 吞錯，稽核中）、**Q5**（查一檔各卡，稽核中），見 (d)。**接手一項：S5-F**（PR #701 CI 綠 → merge → **立刻**本節寫一行含 hash；其餘兩軌照 (d) 追 QA）。**三軌之後的順序**：持股頁 `hold` 軌 **Q2 → Q3 → Q4 ＋ S4-T**（停利卡 why 句「可以重跑一次」只刪句）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**；S5-F 的 QA 殘留 **B9 ⑥～⑨**（c-2，待辦）於 PR #701 merge 後碰到 `page_find.py` 時收尾（⚠️ 排序為編修組依總管指示轉述，未另排；B9 ⑥～⑨ 的插入位置總管未指定）。~~
   ~~（歷史，S5-W 已結案）📌 **2026-09-26 現況（S5-H 已完 #699 `bd6ceec` 後更新）**：進行中四軌 —— **S5-W**（稽核中：獨立 QA 抓到 blocker，總管轉述「`STOP`＋無 `parts` 顯示成紅」，修正中）、**S5-F**（稽核中：獨立 QA 中）、**Q5**（查一檔各卡，派工中）、**Q1**（`NOT_BOUND_NOW` token 吞錯，派工中），見 (d)。**接手一項：S5-W**（四軌中唯一已知有 blocker 待修的一項；其餘三軌照 (d) 追回報）。**四軌之後的順序**：**Q2 → Q3 → Q4**（持股頁 `hold`，依序、一次一張）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**（Q1～Q5 的授權見 (c) c-1 表下 2026-09-26 總管判讀）。~~
   ~~（歷史，S5-H 已結案）📌 **2026-09-26 現況**：S5-H／S5-W／S5-F 三軌已依客戶解除暫停令（原文見本節檔首）**派工恢復（派工中，等回報）**，見 (d)。**三軌之後的順序**：**Q1 → Q2 → Q3 → Q4**（持股頁 `hold`，依序、一次一張）→ **Q5**（查一檔 `inspect`）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**（Q1～Q5 的授權見 (c) c-1 表下 2026-09-26 總管判讀）。若接手時三軌尚未回報／merge，仍從 **S5-H** 接（原描述保留如下）：**S5-H —— `hold.vix`（💼 我的持股「VIX（市場恐慌指數）」卡）靜默失敗**（(c) c-1）。問題：L3 `fetch_vix()` 吞掉例外、抓不到回 `None`，卡片把它畫成灰卡「這是一個**有效的結果**」（`VIX_EMPTY_NOW`）；應改走既有紅態 `VIX_FAILED_NOW`。~~

2. **那項做到哪、下一步是什麼**（📌 2026-09-26 六改寫為 **Q3**；~~五改寫為 **Q2**~~；~~四改寫為 **Q5**~~；Q2 已結案 #704 `45d5385`、Q5 已結案 #703 `76bec1b`、Q1 已結案 #702 `0769730`、S5-F 已結案 #701 `2810064`、S5-W 已結案 #700 `3f4493c`、S5-H 已結案 #699 `bd6ceec`，其描述保留於本點下方作歷史）：
   - **做到哪（Q3）**：實作完成（總管轉述；檔名經編修組 `git status` 實測，未讀 diff 內容）—— L1 `src/data/etf/etf_fetch.py`（失敗時回的空 df 掛 `attrs` 旗標＋`fetch_etf_price(failed=)`）、L3 `src/services/portfolio_deep_service.py`（`get_portfolio_var(strict=)` 等加性參數）、L5 `src/ui/views/page_hold.py` ＋ 測試（見第 3 點）。現況：**稽核中（獨立 QA）**。工作區 `/home/user/msd-q3`（HEAD `b547202`「local: Q2 wip base (not pushed)」，父＝`0769730`）。實作組回報的跳過路徑 → **Q3-r1～r5**（c-2，待辦，⚠️ 單組）。
   - **下一步（Q3）**：獨立 QA（§-2 規則 4）→ 過了先搬到現行 `origin/main` `45d5385`（編修組實測：基底 `b547202` 內的 Q2 wip 4 檔與 #704 merge 版**逐 blob 相同**；Q3 改動檔與 #703 的 6 檔**檔名不重疊**；⚠️ 衝突與否以實際 rebase 為準）→ PR → CI 綠（fast lane 擋 merge；slow lane 要看 check run／job log）→ merge → **立刻**在本節寫一行（含 hash）→ (d)、c-1、「已完成（近期）」同步移列。
   - **同時在跑**：**B9 ⑥⑦**（`/home/user/msd-b9`，基底 `2810064`；獨立 QA 抓到 1 阻擋〔趨勢因子 where 指 `SNAPSHOT_WAIT_WHERE` 不成立〕→ 已改指 `SCREEN_NO_PARTIAL_WHERE`、補 M7 行為測試、修正缺貨來源註解 → **複驗中**；⑧ 卡住）→ 複驗通過 → PR → merge。B9 改動檔（`page_find.py`、`test_v2_silent_fail_b5_find.py`）與 #702／#703／#704 的改動檔**檔名不重疊**（編修組實測）。每項 merge 完**立刻**在本節寫一行含 hash。
   - **之後（2026-09-26 六更新排程，總管）**：**Q4** → **Q1-r2／r3／r5**（r3 只刪句）→ **Q1-r4** → **Q2-r1**（動 L3 `build_station_digest`／`build_summary_prompt`）→ **Q5-r1**（只刪句）→ **B9 ⑩⑫**（只刪句／改指既有 where 常數）→ 小項 **C4／C5b／DOC1／DOC2**；**B9 ⑪⑬、Q2-r3、Q3-r1～r5 位置未定**；**Q2-r2** 低優先、僅登記。每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。
   - ⭐ **每 5 項自檢**：上次於 #703 後執行（自檢點 `76bec1b`）；其後已 merge #704 共 **1 項** ⇒ **距下次 4 項**；累計滿 5 個 merge 後，以 `git log --oneline --first-parent origin/main` 對本檔（見「已完成（近期）」表下）。
   - 📌 **（歷史）Q2 —— 已結案 #704 `45d5385`（2026-09-26）**；以下五項為其進行中時的描述，保留、⛔ 不是現行待辦：
   - ~~**做到哪（Q2，含 S4-T）**：實作完成（總管轉述）—— 只動 **L5 `src/ui/views/page_hold.py`**（L3／L1 未動）＋測試；**S4-T 併入 Q2**（總管裁定）：分配卡 ⑤⑥ 與停利卡共用新常數 `NO_PRICE_WHY`（把 why 截在「，」、去掉「可以重跑一次」）；紅態不再渲染「⚠️ 這個比例只涵蓋一部分」。現況：**稽核中（獨立 QA）**。工作區 `/home/user/msd-q2`（HEAD `623e326`「local: Q1 base (not pushed)」，父＝`3f4493c`；編修組實測未提交改動見第 3 點）。~~
   - ~~**下一步（Q2）**：獨立 QA（§-2 規則 4）→ 過了先搬到現行 `origin/main` `76bec1b`（工作區基底 `3f4493c`＋Q1 本地 commit，**不含 #701／#703**；Q1 部分與 #702 逐 blob 相同，四更新實測）→ PR → CI 綠（fast lane 擋 merge；slow lane 要看 check run／job log）→ merge → **立刻**在本節寫一行（含 hash）→ (d)、c-1（Q2、S4-T 兩列）、「已完成（近期）」同步移列。QA 時一併判定 **Q2-r1**（⑦ AI 總結吃 L3 digest 比例，同頁可能不一致）是否構成矛盾。~~
   - ~~**同時在跑**：**B9 ⑥⑦**（稽核中：獨立 QA，`/home/user/msd-b9`，基底 `2810064`；⑧ 跳過 → 卡住）、**Q3**（實作中，`/home/user/msd-q3`，基底 `0769730`＋Q2 wip 本地 commit `b547202`）；各自 → 獨立 QA → PR → merge。每項 merge 完**立刻**在本節寫一行含 hash。⚠️ Q3 的基底含 Q2 未合併的 wip ⇒ Q2 若在 QA 被改，Q3 需重新對齊（編修組判讀）。~~（→ 六更新：#704 與 `b547202` 內的 Q2 wip 逐 blob 相同 ⇒ 無需重新對齊 Q2 部分）
   - ~~**之後（2026-09-26 五更新排程，總管）**：**Q4** → **Q1-r2／r3／r5**（r3 只刪句）→ **Q1-r4** → **Q5-r1**（只刪句）→ **B9 ⑩**（先 QA 核實讀碼）→ **Q2-r1／Q2-r2** → 小項 **C4／C5b／DOC1／DOC2**；每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。~~
   - ~~⭐ **每 5 項自檢**：2026-09-26 已於 #703 後執行、計數器**歸零**（自檢點 `76bec1b`）；再累計 5 個 merge 後，以 `git log --oneline --first-parent origin/main` 對本檔（見「已完成（近期）」表下）。~~
   - 📌 **（歷史）Q5 —— 已結案 #703 `76bec1b`（2026-09-26）**；以下五項為其進行中時的描述，保留、⛔ 不是現行待辦：
   - ~~**做到哪（Q5）**：獨立 QA 第 1 輪回報阻擋 **B1**（ETF 紅卡只有單腿失敗時，where 句仍寫「這一支 L3 對 ETF 是 fail-loud 的」，與同卡 why 矛盾）＋ 3 個測試缺口 ＋ 2 處註解言過其實；**實作組回報已修好 B1 與 3 個測試缺口**（測試 105 條；三條新測試各自在守衛被拔掉時轉紅）—— ⚠️ **實作組自述、尚未經 QA 複驗**；「2 處註解」本次回報未提及 ⇒ ⚠️ 待確認。現於 `/home/user/msd-q5`（基底 `origin/main` `2810064`）解 `src/services/dividend_station_service.py` 與 #699 的衝突；編修組實測已 stage 6 檔（見第 3 點）。~~
   - ~~**下一步（Q5）**：解完衝突 →（建議先搬到現行 `origin/main` `0769730`；Q5 已 stage 的檔與 #702 的 5 檔**檔名不重疊**，編修組實測）→ 送**原 QA 組**複驗（含「2 處註解」）→ PR → CI 綠（fast lane 擋 merge；slow lane 要看 check run／job log）→ merge → **立刻**在本節寫一行（含 hash）→ (d)、c-1、「已完成（近期）」同步移列。⛔ 估值卡價格、ETF 溢價兩條「修了但 production 幾乎不會觸發」的路徑**不得對客戶宣稱已修**。~~
   - ~~**同時在跑**：**Q2**（實作中，`/home/user/msd-q2`；其基底內的 Q1 本地 commit `623e326` 與 #702 版逐 blob 相同，編修組實測）、**B9 ⑥～⑧**（實作中，`/home/user/msd-b9`，基底 `2810064`）；各自回報 → 稽核中 → 獨立 QA → PR → merge。每項 merge 完**立刻**在本節寫一行含 hash。~~
   - ~~**之後（2026-09-26 排程，總管）**：**Q3 → Q4 ＋ S4-T**（停利卡 why 句「可以重跑一次」不實 ⇒ 只刪句）→ **Q1-r2／r3／r5**（r3 只刪句）→ **Q1-r4** → **Q5-r1**（只刪句）→ 小項 **C4／C5b／DOC1／DOC2**；每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。~~
   - ~~⭐ **下一個 merge 後先做一次自檢**（`git log --merges` 對本檔，見「已完成（近期）」表下自檢計數器）。~~
   - 📌 **（歷史）Q1 —— 已結案 #702 `0769730`（2026-09-26）**；以下四項為其進行中時的描述，保留、⛔ 不是現行待辦：
   - ~~**做到哪（Q1）**：獨立 QA **通過、無阻擋**（單組 QA）；已套上新 main；**PR 尚未開**（—（PR 準備中））。改動檔以執行 AI 回報為準（編修組未查證）。QA 的 5 項**非阻擋**殘留已登記 **Q1-r1～r5**（(c) c-2；r1 卡住〔K1 新文案〕、r2～r5 待辦）。~~
   - ~~**下一步（Q1）**：開 PR → CI 綠（fast lane 擋 merge；slow lane 要看 check run／job log）→ merge → **立刻**在本節寫一行（含 hash）→ (d)、c-1、「已完成（近期）」同步移列。~~
   - ~~**同時在跑**：**Q2**（派工中，`/home/user/msd-q2`；回報 → 稽核中 → 獨立 QA → PR → merge）、**Q5**（獨立 QA 回報阻擋 B1 ＋ 3 個測試缺口 ＋ 2 處註解言過其實 → 發回修正中 → 重跑獨立 QA → PR → merge；⛔ 估值卡價格、ETF 溢價兩條「修了但 production 幾乎不會觸發」的路徑**不得對客戶宣稱已修**）。每項 merge 完**立刻**在本節寫一行含 hash。~~
   - ~~**之後（2026-09-26 排程）**：**Q3 → Q4 ＋ S4-T**（停利卡 why 句「可以重跑一次」不實 ⇒ 只刪句）→ **B9 ⑥～⑨**（`page_find.py` 收尾，前提已成立）→ **Q5-r1**（只刪句）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**；每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。~~
   - 📌 **（歷史）S5-F —— 已結案 #701 `2810064`（2026-09-26）**；以下四項為其進行中時的描述，保留、⛔ 不是現行待辦：
   - ~~**做到哪（S5-F）**：獨立 QA **通過**、**PR #701 已開，等 CI**；未 merge。QA 的 4 項**非阻擋**殘留已登記 **B9 ⑥～⑨**（(c) c-2，待辦）。~~
   - ~~**下一步（S5-F）**：PR #701 CI 綠 → merge → **立刻**在本節寫一行（含 hash）→ (d)、c-1、「已完成（近期）」同步移列。~~
   - ~~**同時在跑**：**Q1**、**Q5**（稽核中 → 獨立 QA 通過 → PR → merge）。每項 merge 完**立刻**在本節寫一行含 hash。~~
   - ~~**三軌之後（2026-09-26 排程）**：持股頁 `hold` 軌 **Q2 → Q3 → Q4 ＋ S4-T**（停利卡 why 句「可以重跑一次」不實 ⇒ 只刪句，同 S5-F 作法）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**；每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。~~
   - 📌 **（歷史）S5-W —— 已結案 #700 `3f4493c`（2026-09-26）**；以下四項為其進行中時的描述，保留、⛔ 不是現行待辦：
   - ~~**做到哪（S5-W）**：已依解除暫停令派工恢復並回報；**獨立 QA 抓到 blocker**（總管轉述：「`STOP`＋無 `parts` 顯示成紅」），執行 AI **修正中**。派工前的持久副本＝`docs/v2/wip_patches/S5-W_84f58ea.patch`（L3 `src/services/ai_qa_service.py` 新增 `_reply_failure`、`run_agent(fail_on_blocked_reply=)`〔預設 False〕；L5 `src/ui/views/page_why.py`；新測試 `tests/test_v2_silent_fail_b5_why.py`）；修正中的改動位置以執行 AI 回報為準（本編修組未查證）。~~
   - ~~**下一步（S5-W）**：修掉 blocker → 重跑獨立 QA（§-2 規則 4）→ PR → CI 綠 → merge → **立刻**在本節寫一行（含 hash）。~~
   - ~~**同時在跑**：**S5-F**（獨立 QA 中 → 通過後 merge）、**Q5**、**Q1**（派工中 → 回報 → 稽核中 → 獨立 QA → merge）。每項 merge 完**立刻**在本節寫一行含 hash。~~
   - ~~**四軌之後（2026-09-26 排程）**：**Q2 → Q3 → Q4**（持股頁 `hold`，依序）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**；每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。~~
   - 📌 **（歷史）S5-H —— 已結案 #699 `bd6ceec`（2026-09-26）**；以下三項為其進行中時的描述，保留、⛔ 不是現行待辦：
   - **做到哪**：~~改動在工作區 `/home/user/msd-h`（detached `84f58ea`）—— ⚠️ **只存在本容器、未 commit、未 push，換容器即消失**；消失時照下列描述重做：~~ → **2026-09-26 二組複核更正**：**持久副本＝`docs/v2/wip_patches/S5-H_84f58ea.patch`**（二組實測：在 `84f58ea` 上可乾淨套用，套用後與工作區 `/home/user/msd-h` 逐位元組相同；工作區是一次性的，換容器即消失）。恢復：從 `84f58ea` 開分支 → `git apply docs/v2/wip_patches/S5-H_84f58ea.patch`。⚠️ patch 須隨本檔 commit 才持久。改動內容：
     - L3 `src/services/dividend_station_service.py`：`fetch_vix()` → `fetch_vix(*, strict=False)`；本體抽成 `_latest_vix_close()`（不吞例外；上游無可用收盤 → `None`）；`strict=True` 時例外原樣上拋、不印 log；預設路徑（`get_station_rows` → v1 ETF 戰情室分頁／每日推播腳本／v2 戰情表）行為不變（同 #693 `get_switch_in_candidates(strict=)` 作法）；docstring 補「`None` 只有『抓不到』一個意思（休市也拿得到最後收盤）」。
     - L5 `src/ui/views/page_hold.py`：`VixReadout` 新增 `fetch_failed: bool = False`；`load_vix()` 改呼叫 `fetch_vix(strict=True)`，`_num()` 後為 `None` → `fetch_failed=True`；`build_vix_card()` 帶 `reason=MISS_FETCH_FAILED` 升紅 `VIX_FAILED_NOW`，why＝L0 `MISS_TEXT[MISS_NO_INPUT]` 去掉開頭「這盞燈」、where＝原 empty 分支的重試句上提為常數 `VIX_RETRY_WHERE`（一字未改）；`V2_SHORT_ROWS` 的 `("hold.vix", VIX_FAILED_NOW)` 加第二組摘錄；兩段舊 docstring 以刪除線保留並寫更正理由。
     - 測試：`tests/test_p04_hold_v2_cards.py::_enumerate` 加一例 `VixReadout(requested=True, fetch_failed=True)`；新檔 `tests/test_v2_silent_fail_b5_vix.py`（462 行，類別：根因／L3／呼叫點／紅卡／不變／其他持股卡逐位元組相同／突變）。
     - **未做**：測試**未跑**（~~本容器 python 無 pytest、無 pandas~~ → **2026-09-26 二組複核更正**：系統 `python3` 確實無 pandas／pytest，但本 session scratchpad 有 venv 可跑〔pandas 3.0.6／pytest 9.1.1／streamlit 1.59.2〕；scratchpad 非持久，新容器須自建 venv 再跑）；**未 QA**；未 commit。
   - **下一步**：跑測試補齊 → 另一組獨立 QA（§-2 規則 4）→ PR → CI 綠 → merge → **立刻**在本節寫一行（含 hash）。~~⚠️ 本項依客戶 2026-09-26 清點令（「⛔ 不修 code、⛔ 不開新工作，只清點」）**暫停中**，恢復須客戶解除暫停。~~ → **2026-09-26 客戶令已解除暫停；S5-H／S5-W／S5-F 三軌已派工恢復（派工中）**，見 (d)。~~程式註解自稱「批次 5（客戶授權）」，本組未查到該授權原文 ⇒ ⚠️ 待確認。~~ → **2026-09-26 二組複核更正**：授權原文（總管轉述）＝客戶 2026-09-26「【授權：逐卡修失敗源頭，連跑】停利已完。本批放行「失敗被當成沒結果」的所有卡，不用每張問我。」⇒ S5-H 在授權射程內，**不需另外特許**，只差解除暫停（詳見 (c) c-1 表下 📌）。
   - ~~**三軌之後（2026-09-26 排程）**：**Q1 → Q2 → Q3 → Q4**（持股頁 `hold`，依序）→ **Q5**（查一檔 `inspect`）→ 小項 **C4／C5b／DOC1／DOC2／C6（查證）**；每項同樣走「實作 → 獨立 QA → merge → 本節寫一行含 hash」。依客戶令：全部待辦做完或只剩卡住的，才停一次回報。~~（→ 2026-09-26 已改寫，見本點上方「四軌之後」）

3. **哪些檔已動、哪些 ⛔ 不准動**：
   - **已動（三軌皆未 commit；三軌檔案互不重疊）**。~~只在本容器~~ → **2026-09-26 二組複核更正**：持久副本在 `docs/v2/wip_patches/S5-{F,W,H}_84f58ea.patch`（在 `84f58ea` 上 `git apply`；二組實測三份皆可乾淨套用、與工作區逐位元組相同）；下列工作區路徑只在本容器、換容器即消失：
     S5-F `/home/user/msd-f`：`src/data/stock/yield_pe_fetcher.py`（L1 ⚠️）、`src/services/fundamental_screener_service.py`、`src/services/valuation_service.py`、`src/ui/views/page_find.py`（無測試）；
     S5-W `/home/user/msd-w`：`src/services/ai_qa_service.py`、`src/ui/views/page_why.py`、新檔 `tests/test_v2_silent_fail_b5_why.py`；
     S5-H `/home/user/msd-h`：`src/services/dividend_station_service.py`、`src/ui/views/page_hold.py`、`tests/test_p04_hold_v2_cards.py`、新檔 `tests/test_v2_silent_fail_b5_vix.py`。
   - 📌 **2026-09-26 更新**：**S5-H 已 merge**（#699 `bd6ceec`；實際改 5 檔＝上列 4 檔＋`tests/test_p04_hold_view.py`）⇒ 其 patch／工作區已是歷史；**S5-W／S5-F 仍未 merge**（稽核中）；**Q1／Q5** 派工中，改動檔以執行 AI 回報為準（本編修組未查證）。
   - 📌 **2026-09-26 再更新**：**S5-W 已 merge**（#700 `3f4493c`；實改 3 檔＝上列 S5-W 3 檔）⇒ 其 patch／工作區已是歷史；**S5-F** 在 PR #701（未 merge，改動以 PR 為準）；**Q1／Q5** 稽核中，改動檔以執行 AI 回報為準（本編修組未查證）。
   - 📌 **2026-09-26 三更新**：**S5-F 已 merge**（#701 `2810064`；實改 **8 檔**＝上列 S5-F 4 檔 ＋ `tests/test_p02_find_v2_cards.py`、`tests/test_p02_find_view.py`、`tests/test_v2_silent_fail_b1.py`、新檔 `tests/test_v2_silent_fail_b5_find.py`，編修組 `git diff --stat 3f4493c 2810064` 實測）⇒ S5 三軌的 patch／工作區**全部**已是歷史。**Q1**（稽核通過、PR 準備中）／**Q5**（發回修正中）改動檔以執行 AI 回報為準（編修組未查證）；**Q2** 工作區 `/home/user/msd-q2`（HEAD `623e326`「local: Q1 base (not pushed)」，父＝`3f4493c`；編修組實測當下另有未追蹤 `tests/test_v2_silent_fail_b7_binding.py`，屬誰的改動未查證）。
   - 📌 **2026-09-26 四更新**：**Q1 已 merge**（#702 `0769730`；實改 **5 檔**：`src/data/portfolio/gsheet_portfolio.py`（L1）、`src/services/portfolio_binding_service.py`、`src/services/holdings_service.py`、`src/ui/views/page_hold.py`、新檔 `tests/test_v2_silent_fail_b7_binding.py`，編修組 `git diff --stat 2810064 0769730` 實測）。**未提交的工作區（編修組實測當下狀態，只在本容器、換容器即消失）**：**Q5** `/home/user/msd-q5`（detached `2810064`，已 stage）：`src/data/core/data_loader.py`（L1）、`src/data/core/financial_statements_fetcher.py`（L1）、`src/services/dividend_station_service.py`、`src/services/stock_grp_service.py`、`src/ui/views/page_inspect.py`、新檔 `tests/test_v2_silent_fail_b6_inspect.py`；**Q2** `/home/user/msd-q2`（HEAD `623e326`）：改 `src/ui/views/page_hold.py`、`tests/test_p04_hold_v2_cards.py`，未追蹤 `tests/test_v2_silent_fail_b7_binding.py`（＝#702 版，blob 相同）、`tests/test_v2_silent_fail_b8_alloc.py`；**B9 ⑥～⑧** `/home/user/msd-b9`（detached `2810064`）：改 `src/ui/views/page_find.py`、`tests/test_v2_silent_fail_b5_find.py`。檔名重疊（編修組實測）：三個工作區**彼此不重疊**；Q2 的 `page_hold.py` 與 #702 同檔（Q2 基底已含 #702 的內容）。⚠️ 改動內容未 QA、編修組未讀 diff。
   - 📌 **2026-09-26 五更新**：**Q5 已 merge**（#703 `76bec1b`；實改 **6 檔**：`src/data/core/data_loader.py`（L1）、`src/data/core/financial_statements_fetcher.py`（L1）、`src/services/dividend_station_service.py`、`src/services/stock_grp_service.py`、`src/ui/views/page_inspect.py`、新檔 `tests/test_v2_silent_fail_b6_inspect.py`，編修組 `git diff --stat 0769730 76bec1b` 實測）⇒ 工作區 `/home/user/msd-q5`（仍 detached `2810064`＋staged）已是**歷史**，⛔ 不要再用。**未提交的工作區（編修組實測當下狀態，只在本容器、換容器即消失）**：**Q2（含 S4-T）** `/home/user/msd-q2`（HEAD `623e326`）：改 `src/ui/views/page_hold.py`、`tests/test_p04_hold_v2_cards.py`、`tests/test_v2_silent_fail_b4.py`；未追蹤 `tests/test_v2_silent_fail_b8_alloc.py`、`tests/test_v2_silent_fail_b7_binding.py`（＝#702 版）；**B9 ⑥⑦** `/home/user/msd-b9`（detached `2810064`）：改 `src/ui/views/page_find.py`、`tests/test_v2_silent_fail_b5_find.py`；**Q3** `/home/user/msd-q3`（HEAD `b547202`「local: Q2 wip base (not pushed)」＝ `0769730` ＋ Q2 wip：`page_hold.py`、`test_p04_hold_v2_cards.py`、`test_v2_silent_fail_b4.py`、`test_v2_silent_fail_b8_alloc.py`）：當下**無**未提交改動。檔名重疊（編修組實測）：Q2 與 Q3 同動 `page_hold.py`（Q3 基底已含 Q2 wip）；B9 與兩者不重疊。⚠️ 改動內容未 QA、編修組未讀 diff 全文（只 grep 到 `NO_PRICE_WHY` 定義存在於 Q2 的 `page_hold.py`）。
   - 📌 **2026-09-26 六更新**：**Q2（含 S4-T）已 merge**（#704 `45d5385`；實改 **4 檔**：`src/ui/views/page_hold.py`、`tests/test_p04_hold_v2_cards.py`、`tests/test_v2_silent_fail_b4.py`、新檔 `tests/test_v2_silent_fail_b8_alloc.py`，編修組 `git diff --stat 76bec1b 45d5385` 實測）⇒ 工作區 `/home/user/msd-q2` 已是**歷史**，⛔ 不要再用。**未提交的工作區（編修組實測當下狀態，只在本容器、換容器即消失）**：**Q3** `/home/user/msd-q3`（HEAD `b547202`）：改 `src/data/etf/etf_fetch.py`（L1）、`src/services/portfolio_deep_service.py`（L3）、`src/ui/views/page_hold.py`、`tests/test_p04_hold_v2_cards.py`、`tests/test_p04_hold_view.py`、`tests/test_v2_silent_fail_b3.py`、`tests/test_v2_silent_fail_b7_binding.py`；未追蹤 `tests/test_v2_silent_fail_b9_deep.py`；**B9 ⑥⑦** `/home/user/msd-b9`（detached `2810064`）：改 `src/ui/views/page_find.py`、`tests/test_v2_silent_fail_b5_find.py`。檔名重疊（編修組實測）：Q3 與 B9 **不重疊**；Q3 與 #704 同動 `page_hold.py`／`test_p04_hold_v2_cards.py`（Q3 基底 `b547202` 內的 Q2 wip 4 檔與 #704 **逐 blob 相同**）。⚠️ 改動內容未 QA、編修組未讀 diff 全文。
   - **⛔ 不准動**：`docs/v2/prototype/lamp_preview.html`；凍結快照 `data_cache/forward_test/picks.parquet`、`data_cache/macro_forward_test/signals.parquet`；PR #675（⛔ merge／轉 ready／改 base）；remote 分支 **`data`**（活的排程線，⛔ 勿刪，見 C6）；其餘 C6 分支的刪除待客戶（C6-del）；資料層（資料庫／Schema／遷移檔／資料源，`CLAUDE.md` §-1.2）—— 除非客戶逐任務明示授權；（本輪工單）本工作區未追蹤的 `docs/v2/screens_r4/`、`docs/v2/screens_r5/` 不 stage。

4. **待你裁的五類各幾項**（⚠️單組：編修組分類，每列只計一個主類；五類的定義與「只有三類拿去問」見文末「常設規矩」§2）：
   ~~**版面異動 0**｜**不可逆 3**（B7b 刪列、B10 PR #675／分支、C6 刪分支）｜**動 L3 或資料層 12**（S5-F／S5-W／S5-H 恢復、Q1～Q5、B7c、`health`、`fut_net`、`m1b_m2_gap`〔依 B7b〕）｜~~**需新源 5**（`us10y`、`dxy`、`us_core_cpi`、`tw_export`、B9 ④）｜**規格有洞 9**（B4、B5、B6、B9 ③、B12、B8、B11、`news_systemic`、`foreign_net`）。另：D1／D2 屬實機驗證，不在五類。
   → **2026-09-26 二組複核更正**：**版面異動 0**｜**不可逆 2**（B10 PR #675／分支、C6 刪分支；B7b 移出 —— 它的解法是改 cron 解析、資料可由 CBC 重建，見 c-4）｜**動 L3 或資料層 10**（Q1～Q5〔⚠️ 二組判讀：疑已在批 5 授權射程內，見 c-1 表下〕、B7b〔改 L1 cron 解析〕、B7c、`health`、`fut_net`、`m1b_m2_gap`〔依 B7b〕）｜**需新源 5**（同上）｜**規格有洞 9**（同上）｜**另：~~暫停待解除~~ 已派工恢復 3**（S5-F／S5-W／S5-H —— 批 5 已授權，~~只差客戶解除清點令暫停，~~ 2026-09-26 客戶已解除暫停；不是待裁五類；Q1～Q5 見 c-1 表下 2026-09-26 總管判讀）。合計 0+2+10+5+9+3＝29，與原 29 一致。
   → 📌 **2026-09-26 更新**（S5-H 已完、Q1／Q5 已派工、新登記 B9 ⑤）：**版面異動 0**｜**不可逆 2**（同上）｜**動 L3 或資料層 8**（Q2～Q4、B7b、B7c、`health`、`fut_net`、`m1b_m2_gap`）｜**需新源 5**（同上）｜**規格有洞 10**（上列 9 ＋ **B9 ⑤** 文案）｜**另：已派工 4**（S5-F／S5-W／Q1／Q5；不是待裁五類）。合計 0+2+8+5+10+4＝29（原 29 − S5-H 已完 ＋ B9 ⑤）。⚠️ 單組。
   → 📌 **2026-09-26 再更新**（S5-W 已完 #700 `3f4493c`）：**另：已派工 3**（S5-F／Q1／Q5）；五類各數不變 ⇒ 合計 0+2+8+5+10+3＝**28**。新登記的 B9 ⑥～⑨、S4-T 狀態為**待辦**、「誰決定」＝總管（依總管指示）⇒ **不計入待裁五類**。⚠️ 單組。
   → 📌 **2026-09-26 三更新**（S5-F 已完 #701 `2810064`；Q2 已派工；新登記 Q1-r1～r5、Q5-r1、Q5-r2）：**版面異動 0**｜**不可逆 2**（B10 PR #675／分支、C6 刪分支）｜**動 L3 或資料層 8**（Q3、Q4、B7b、B7c、`health`、`fut_net`、`m1b_m2_gap`、**Q5-r2**〔需改 L2／L1 分辨失敗與無資料〕）｜**需新源 5**（`us10y`、`dxy`、`us_core_cpi`、`tw_export`、B9 ④）｜**規格有洞 11**（B4、B5、B6、B9 ③、B9 ⑤、B12、B8、B11、`news_systemic`、`foreign_net`、**Q1-r1**〔K1 新文案〕）｜**另：已派工 3**（Q1／Q2／Q5；不是待裁五類）。合計 0+2+8+5+11+3＝**29**（逐項列名重數）。Q1-r2～r5、Q5-r1 為**待辦**、誰決定＝總管 ⇒ 不計入五類（同 B9 ⑥～⑨ 前例）。⚠️ 單組（五類歸屬為編修組分類）。
   → 📌 **2026-09-26 四更新**（Q1 已完 #702 `0769730`；C6 查證已完 → 新登記 **C6-del**；B9 ⑥～⑧ 已派工）：**版面異動 0**｜**不可逆 2**（B10 PR #675／分支、**C6-del** 刪分支〔承接原「C6 刪分支」〕）｜**動 L3 或資料層 8**（Q3、Q4、B7b、B7c、`health`、`fut_net`、`m1b_m2_gap`、Q5-r2）｜**需新源 5**（`us10y`、`dxy`、`us_core_cpi`、`tw_export`、B9 ④）｜**規格有洞 11**（B4、B5、B6、B9 ③、B9 ⑤、B12、B8、B11、`news_systemic`、`foreign_net`、Q1-r1〔是否修＝總管、字樣＝客戶〕）｜**另：已派工 3**（Q2／Q5／B9 ⑥～⑧；不是待裁五類）。合計 0+2+8+5+11+3＝**29**（逐項列名重數）。B9 ⑨、Q1-r2～r5、Q5-r1、S4-T 為**待辦**、誰決定＝總管 ⇒ 不計入五類。⚠️ 單組（五類歸屬為編修組分類）。
   → 📌 **2026-09-26 五更新**（Q5 已完 #703 `76bec1b`；Q3 已派工；B9 ⑧⑨ 改卡住；新登記 B9 ⑩、Q2-r1、Q2-r2〔待辦〕；S4-T 併入 Q2）：**版面異動 0**｜**不可逆 2**（B10 PR #675／分支、C6-del 刪分支）｜**動 L3 或資料層 7**（Q4、B7b、B7c、`health`、`fut_net`、`m1b_m2_gap`、Q5-r2；Q3 移出 → 已派工）｜**需新源 5**（`us10y`、`dxy`、`us_core_cpi`、`tw_export`、B9 ④）｜**規格有洞 13**（B4、B5、B6、B9 ③、B9 ⑤、**B9 ⑧**〔新文案需客戶定措辭〕、**B9 ⑨**〔規格洞〕、B12、B8、B11、`news_systemic`、`foreign_net`、Q1-r1）｜**另：已派工 3**（Q2〔含 S4-T〕／B9 ⑥⑦／Q3；不是待裁五類）。合計 0+2+7+5+13+3＝**30**（逐項列名重數）。B9 ⑩、Q1-r2～r5、Q2-r1、Q2-r2、Q5-r1 為**待辦**、誰決定＝總管 ⇒ 不計入五類。⚠️ 單組（五類歸屬為編修組分類；B9 ⑧⑨ 歸「規格有洞」為編修組依總管「需客戶定措辭」「規格洞」轉述歸類）。
   → 📌 **2026-09-26 六更新**（Q2〔含 S4-T〕已完 #704 `45d5385`；Q3 稽核中；B9 ⑥⑦ 複驗中；新登記 B9 ⑪⑫⑬、Q2-r3、Q3-r1～r5〔待辦〕；Q2-r2 改低優先）：**版面異動 0**｜**不可逆 2**（B10 PR #675／分支、C6-del 刪分支）｜**動 L3 或資料層 7**（Q4、B7b、B7c、`health`、`fut_net`、`m1b_m2_gap`、Q5-r2）｜**需新源 5**（`us10y`、`dxy`、`us_core_cpi`、`tw_export`、B9 ④）｜**規格有洞 13**（B4、B5、B6、B9 ③、B9 ⑤、B9 ⑧、B9 ⑨、B12、B8、B11、`news_systemic`、`foreign_net`、Q1-r1）｜**另：已派工 2**（B9 ⑥⑦／Q3；Q2 移出 → 已完；不是待裁五類）。合計 0+2+7+5+13+2＝**29**（逐項列名重數）。B9 ⑩～⑬、Q1-r2～r5、Q2-r1～r3、Q3-r1～r5、Q5-r1 為**待辦**、誰決定＝總管 ⇒ 不計入五類（同前例）；⚠️ 其中 **Q2-r3**（疑規格洞）、**B9 ⑪**（若視為新文案組合＝客戶）、**Q3-r2**（需改 L2）若日後改判卡住，會分別進「規格有洞」「規格有洞」「動 L3 或資料層」（編修組判讀）。⚠️ 單組（五類歸屬為編修組分類）。

5. **開場該讀哪些檔**：
   - `HANDOFF.md`（本檔）：檔首【給下一個 AI 的說明】→ 本節（進度總表）→ 文末「常設規矩」。
   - `CLAUDE.md`：§-2（派工／複驗／單組揭露）、§-1.5.H（回覆紀律）、§-1.2（分支政策、資料層凍結）、§1（Fail Loud）、§-1（沒指派不動工）。
   - 接 **Q3** 時（2026-09-26 六更新起；Q2 已結案 #704）：Q3 執行 AI 的回報與工作區 `/home/user/msd-q3` 的 diff（尚無 PR 號）、Q3 獨立 QA 報告（進行中）、`docs/v2/spec/UI_PAGE_HOLD.md`、`docs/v2/spec/DATA_MAP_HOLD.md`、`docs/v2/spec/UI_COMPONENTS.md` §2（徽章 11 種、#11 射程）；`CLAUDE.md` §1.A 第 3 條（只快取成功結果、失敗退避＝Q3-r5 的由來）；前例 `tests/test_v2_silent_fail_b3.py`（配息卡批 3，#694；L1 `fetch_etf_dividends` 失敗不入快取）、`tests/test_v2_silent_fail_b8_alloc.py`（Q2，#704）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。
   - ~~接 **Q2**（含 S4-T）時（2026-09-26 五更新起；Q5 已結案 #703）：Q2 執行 AI 的回報與工作區 `/home/user/msd-q2` 的 diff（尚無 PR 號）、Q2 獨立 QA 報告（進行中）、`docs/v2/spec/UI_PAGE_HOLD.md`、`docs/v2/spec/DATA_MAP_HOLD.md`、`docs/v2/spec/UI_COMPONENTS.md` §2（徽章 11 種、#11 射程）；`CLAUDE.md` §-2（「可以重跑一次」錯誤指引事故＝S4-T／`NO_PRICE_WHY` 的由來）；前例 `tests/test_v2_silent_fail_b4.py`（停利卡批 4，#698）、`tests/test_v2_silent_fail_b7_binding.py`（Q1，#702）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。~~（Q2 已結案 #704 `45d5385`）
   - ~~接 **Q5** 時（2026-09-26 四更新起；Q1 已結案 #702）：Q5 執行 AI 的回報與工作區 `/home/user/msd-q5` 的 staged diff（尚無 PR 號）、Q5 第 1 輪 QA 報告（阻擋 B1、3 個測試缺口、2 處註解）、`docs/v2/spec/UI_PAGE_INSPECT.md`、`docs/v2/spec/DATA_MAP_INSPECT.md`、`docs/v2/spec/UI_COMPONENTS.md` §2（徽章 11 種、#11 射程）；前例 `tests/test_v2_silent_fail_b7_binding.py`（Q1，#702）、`tests/test_v2_silent_fail_b5_vix.py`（S5-H，#699；與 Q5 同動 `dividend_station_service.py`）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。~~（Q5 已結案 #703 `76bec1b`）
   - ~~接 **Q1** 時（2026-09-26 三更新起；S5-F 已結案 #701）：Q1 執行 AI 的回報與 diff（PR 準備中，尚無 PR 號）、`docs/v2/spec/UI_PAGE_HOLD.md`、`docs/v2/spec/DATA_MAP_HOLD.md`、`docs/v2/spec/UI_COMPONENTS.md` §2（徽章 11 種、#11 射程）；前例 `tests/test_v2_silent_fail_b5_find.py`（S5-F，#701）、`tests/test_v2_silent_fail_b5_vix.py`（S5-H，#699）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。~~（Q1 已結案 #702 `0769730`）
   - ~~接 **S5-F** 時（2026-09-26 再更新起；S5-W 已結案 #700）：PR #701 的 diff、`docs/v2/spec/UI_PAGE_FIND.md`、`docs/v2/spec/DATA_MAP_FIND.md`、`docs/v2/spec/UI_COMPONENTS.md` §2；前例 `tests/test_v2_silent_fail_b5_why.py`（S5-W，#700）、`tests/test_v2_silent_fail_b5_vix.py`（S5-H，#699）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。~~（S5-F 已結案 #701 `2810064`）
   - ~~接 **S5-W** 時（2026-09-26 起）：`docs/v2/spec/UI_PAGE_WHY.md`、`docs/v2/spec/DATA_MAP_WHY.md`、`docs/v2/spec/UI_COMPONENTS.md` §2（徽章 11 種、#11 射程）；前例 `tests/test_v2_silent_fail_b5_vix.py`（S5-H，#699）、`tests/test_v2_silent_fail_b2.py`（`strict=` 作法）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。~~（S5-W 已結案 #700 `3f4493c`）
   - ~~接 S5-H 時：`docs/v2/spec/UI_PAGE_HOLD.md`、`docs/v2/spec/DATA_MAP_HOLD.md`、`docs/v2/spec/UI_COMPONENTS.md` §2（徽章 11 種、#11 射程）；前例 `tests/test_v2_silent_fail_b2.py`（`strict=` 作法）、`tests/test_v2_silent_fail_b4.py`（停利卡批 4）。⚠️ `docs/v2/spec/` 只在開發線，`origin/main` 沒有（§4.2 第 3 點）。~~（S5-H 已結案 #699 `bd6ceec`）

---

## 0. 這個專案是什麼

> **這一節解決的疑問**：我接手的是什麼東西？誰在用？跑在哪裡？

**「台股 AI 戰情室」** —— 一位台股散戶（本專案的「**客戶**」本人）的**個人投資決策輔助儀表板**。
**Streamlit 單頁應用**，部署在 **Streamlit Cloud**（合併進 `main` 就自動重新部署）。

它要解的問題：把「**總經現在紅燈還綠燈 → 該不該加碼 → 買哪一檔 → 手上這些該不該換**」
這條決策鏈，從散落各網站的人工查詢，收斂成一個畫面。

**四大資料流**（出處：`DATASTATION.md` 檔頭逐字）：**總經狀態／個股評分／AI 分析／風控**。

| 面向 | 實測現況（量測日 2026-09-24） |
|---|---|
| 進入點 | 根目錄 `app.py`（Streamlit 主腳本） |
| **v1 線上 7 個頂層頁籤**（`app.py` 的 `st.tabs(...)`，逐字） | `🌍 市場環境`／`🔬 選股`／`🏦 ETF`／`🔧 工具箱`／`💼 我的持股戰情室`／`📁 組合管理`／`🧬 AI 問答` |
| 排程回寫 | `.github/workflows/` 共 **16 個** workflow 檔，其中 **12 個含 `schedule:`**（cron 自動跑、回寫 `data_cache/`）。另 1 支是 CI（`pr-check.yml`） |
| 台股資料來源 | 多走 **NAS Squid proxy**（`src/data/proxy/proxy_helper.py`） |
| 使用者持股帳本 | **Google Sheets**（`src/data/portfolio/gsheet_portfolio.py`，OAuth） |
| 測試 | `tests/` 底下 **374 支 `test_*.py`** |

⚠️ **根目錄沒有 `README.md`**（實測 `ls README.md` → No such file）。
但 `ARCHITECTURE.md` 的目錄樹仍把 `README.md` 列在裡面 ⇒ **`ARCHITECTURE.md` 該處已過期**。
⛔ 不要去找一份不存在的 README；**本檔就是入口**。

---

## 1. 接手第一天只讀這三份

> **這一節解決的疑問**：21 份根目錄文件 + 72 份 `docs/` 文件，我到底先讀哪個？

### ① `CLAUDE.md` 的**六節**（⛔ 不要整份 2,198 行讀完）

`CLAUDE.md` 是本專案的**憲法**，位階規則是「**數字越小、位階越高**」（§-2 > §-1.5 > §-1.2 > §-1 > §0~§8）。

| 節 | 一句話 | 為什麼非讀不可 |
|---|---|---|
| **§-2** | **每一件任務都要派 subagent 執行；總管不自己寫實作；總管必須自己複驗 subagent 的關鍵宣稱**；調查／稽核要**多組獨立**派工，不可自己查自己 | 這是本 repo 最常被違反、也最貴的一條 |
| **§-1** | **沒有客戶指派、沒有實際 bug 觸發 → 停手等指令**，不主動找事做 | ⛔ 看到任何「待修清單」都不是動工授權 |
| **§-1.2** | **分支政策** —— 客戶原文「新分支名 `v2`」「**`main` 保留 v1，不合併直到 v2 完成**」「v2 上線時直接合併 `main`」；資料層凍結 | 與實測現況有矛盾，見 **§4** |
| **§-1.5.D v3 §03** | **唯二能問客戶的事**：① UI 版面／欄位增減／分頁動線異動（**必須先出文字線框草稿**）② 商業邏輯衝突與不可逆操作。其餘（欄名、TTL、取數路徑、目錄命名、死碼何時刪）**一律內部拍板，且嚴禁拋選擇題**（要問必須附推薦方案） | 決定你什麼時候可以開口問客戶 |
| **§-1.5.H** | **回覆紀律**：每輪 ≤ 15 行、表格優先、結尾必加 `⏸ 等待你回覆：【明確問題】` 或 `✅ 本輪完成，無需回覆。` | 每一輪都會用到 |
| **§1** | **Fail Loud, Never Fake** —— ⛔ 禁 `fillna(0)`／沉默 `ffill`／dummy 資料／`except: pass`／自己估一個合理值。**錯的數字比沒有數字更危險** | 客戶的紅線 |

### ② **本檔**（`HANDOFF.md`）

### ③ `docs/v2/README.md` ＋ `docs/wireframes/stock_ia_v1.html`

後者是**五頁 IA（資訊架構）的線框 SSOT**（單一真相源）——
`shared/ia_nav.py` 的頁 id 與顯示名**直接沿用該 HTML 的 `PAGES` 資料結構**，⛔ 不另取名。

### 動工前補讀

- `PROCESS.md`（**64 行，很短**）—— 流程治理／PR 規範／Anti-Loop。
- `STATE.md` 的**最上面一段**。
  ⚠️ **`STATE.md` 有 4,594 行，往下全部是歷史紀錄，⛔ 不是待辦清單。**
  ⛔ 不要從裡面挑一條出來當任務做（違 §-1）。

---

## 2. 文件地圖

> **這一節解決的疑問**：哪份文件有用、哪份已過期、我遇到某個問題該翻哪一份？

**根目錄 21 份 `.md`，合計 14,151 行**（量測日 2026-09-24；請現場跑 `wc -l *.md` 複驗）。

### (a) 必讀

| 檔 | 行數 | 內容 | 何時讀 |
|---|---|---|---|
| `CLAUDE.md` | 2,198 | **專案憲法**（資料完整性 + 派工 + 溝通邊界 + 分層架構） | 第一天讀 §1 列的六節 |
| `PROCESS.md` | 64 | 流程治理、PR 規範、卡關救援 | 動工前 |
| `STATE.md` | 4,594 | 版本流水帳 | **只讀最上面一段**；⛔ 其餘是歷史 |
| **本檔** | — | 交接 | — |

### (b) 遇到才查

| 檔 | 行數 | 內容 | 何時讀 |
|---|---|---|---|
| `ARCHITECTURE.md` | 2,332 | 七層分層、模組住哪一層、依賴方向 | **找「某個檔屬於哪一層」時**。⚠️ **部分已過期**（例：目錄樹仍列不存在的 `README.md`） |
| `SPEC.md` | 1,460 | 功能規格、UI 字串、跨模組介面契約 | 改 UI 文案或跨模組介面時 |
| `DEAD_CODE.md` | 131 | **死碼登記簿（全 repo 唯一入口）**，編號 `D-0NN` | **刪任何東西前必查**（`CLAUDE.md` §8.2.A.3 客戶裁示）。⛔ 它**不是**全 repo 死碼的窮舉，「未登記 ≠ 不存在」 |
| `DATASTATION.md` | 545 | 各模組函式簽章與回傳結構（給外部 import 用） | 要從外部 import 本專案模組時 |
| `MACRO_MASTER.md` | 126 | **總經「五桶 16 盞燈」現況盤點** | 動總經燈之前 |
| `MACRO_HEALTH_REWEIGHT_PROPOSAL.md` | 104 | 健康評分權重調整提案 | 動權重時 |
| `STRATEGY_INTAKE.md` | 147 | 客戶一份外部策略提示語的**納入前盤點**，七項待客戶裁示 | 客戶提到「那份策略」時 |
| `HANDOFF_MARGIN_RELATIVE.md` | 112 | 融資餘額燈「相對分位」支線的完整細節 | 見本檔 **§6** |

**名詞：什麼是「五桶 16 盞燈」？**
`shared/macro_buckets.py` 的 `BUCKET_ORDER` 逐字 `["long", "mid", "short", "chips", "news"]` ——
把 16 個總經指標分成五個「桶」，每個指標算一盞紅／黃／綠燈：

| 桶 | 盞數 | 燈 key |
|---|---|---|
| 🌳 `long` 長期／結構・景氣位階 | 3 | `health`、`ndc_signal`、`m1b_m2_gap` |
| 📈 `mid` 中期／景氣循環 3–12 月 | 6 | `ism_pmi`、`us_core_cpi`、`tw_export`、`bias_240`、`us10y`、`dxy` |
| ⚡ `short` 短線急殺／即時 risk-off | 3 | `vix`、`adl`、`fut_net` |
| 🧩 `chips` 籌碼／大戶定位 | 3 | `margin`、`jingqi`、`foreign_net` |
| 📰 `news` 新聞／系統性風險 | 1 | `news_systemic` |

另有 **2 條「參考走勢」不算燈**（`usdtwd` 匯率、`taiex` 加權指數，掛 `REFERENCE_BUCKET`，
不進 x/16 分母）。**桶內用 worst-of 彙總**（紅 > 黃 > 綠），**桶間無權重**。
（出處：`MACRO_MASTER.md` §1，該組已引 `shared/macro_buckets.py` 的 `BUCKET_ORDER` / `aggregate_level` 逐字。）

### (c) ⛔ 不用讀（歷史／已過期／非規格）

| 檔 | 為什麼不用讀 |
|---|---|
| `HANDOFF_2026-08-15.md`（147 行） | 已過期的舊交接；內含 **Windows 本機路徑**，在本容器裡完全不適用 |
| `PLAN_T1_T2.md`（317）／`PLAN_T3.md`（240）／`PLAN_T4.md`（224） | 三份計畫**皆已執行完畢**，只剩歷史價值 |
| `BATCH_A_DELIVERY.md`（170） | 一次性交付紀錄 |
| `MACRO_CALIBRATION.md`（70） | 2026-07-12 自動產生的校準報告。⚠️ **它自己在「樣本不足／限制聲明」段寫明是 `TWII-only mode`** —— 只注入 `^TWII` OHLCV，**外資買賣超／ADL／外資期貨／M1B-M2／jingqi 全部以「中性預設」（0 / 1.0 / None）代入** ⇒ 報告裡的 precision／recall **不是完整系統的表現**。⛔ 不要引用它的數字下結論 |
| `STRATEGY_MANUAL.md`（909） | 檔頭自陳「**系統版本 v10.53.0，更新日期 2026-05-05**」，而**現行 code 已到 v19.x** ⇒ 僅供了解歷史設計背景 |
| `ARCHIVED_FEATURES.md`（92） | 已下架功能的存檔 |
| `MACRO_HEALTH_WEIGHT_PROPOSAL.md`（27） | 機器產物，~~待人工審查~~，非規格（📌 2026-09-26 註：檔內自陳「待人審」已過期 —— 權重已於 2026-07-11 採納，PR #517 `a8035c6`〔v19.102 Phase 3 收官，`STATE.md` 同日段〕；⛔ 非現行待辦） |

### (d) `docs/` 樹（**共 72 份 `.md`**，量測日 2026-09-24）

| 路徑 | 檔數 | 定位 |
|---|---|---|
| `docs/v2/README.md` | 1 | **v2 存檔的導讀頁，先讀它**（它自己說「先讀完這一頁再讀任何一份文件」） |
| `docs/v2/spec/` | 28 | **v2 規格庫** —— 含七份 `UI_PAGE_*`（五頁各一 + `UI_COMPONENTS` + `UI_TOKENS`）、六份 `DATA_MAP_*`（每頁的資料對映）、`DECISIONS_PENDING.md`（~~待客戶裁示~~ → 📌 2026-09-26 註：D-1~D-5 已於 2026-09-23 全數裁示、檔名沿用，見該檔末「裁示落地對照表」；⛔ 非現行待辦）、`DECISION_RULES.md`、`UI_PRINCIPLES.md`（客戶 24 條視覺原則） |
| `docs/v2/audit/` | 18 | ⛔ **稽核軌跡，不是規格，⛔ 不可照著做**。它記的是「誰查了什麼、查到什麼錯」 |
| `docs/v2/wireframe/` | 18 | 五頁線框的**原始碼**（`wf_page_*.js` + `assemble.js` Node 組裝器）。**組裝產物刻意不入庫** |
| `docs/v2/stage1/` | 4 | 含 **`INDICATOR_SSOT.md`** —— ⭐ **裡面的 `N-1`／`N-2`／`N-3` 三條根因，就是「為什麼要做 v2」的答案**（見下） |
| `docs/v2/prototype/` | 5 檔 + `__pycache__` | HTML 視覺原型與產生器，見 **§5** |
| `docs/wireframes/stock_ia_v1.html` | 1 | **五頁 IA 的線框 SSOT**（`shared/ia_nav.py` 的出處） |
| ⚠️ `docs/v2/HANDOFF_UI_2026-09-16.md` | 1 | **已過期**（它自己也標明「未經第二組複驗、不含新結論」） |

**⭐ `N-1`／`N-2`／`N-3` 是什麼**（`docs/v2/stage1/INDICATOR_SSOT.md` 逐字，⭐ v2 存在的理由）：

| 編號 | 缺陷 |
|---|---|
| **N-1** | `calc_fundamental_score` **量綱錯誤** —— `avg_div`（**元/股**）被當成**殖利率 %** 去比對 `YIELD_HIGH/MID/LOW` ⇒ **估值分與真實殖利率顛倒**（實測：真殖 1.80% 判「便宜區 >7%」、真殖 10.00% 判「偏貴 <3%」） |
| **N-2** | `calc_health_score` **缺值直接回 0** ⇒ 什麼都沒抓到的股票顯示「0 分 · 🔴 弱勢危險」。直接違反客戶紅線「缺失值不得填 0」 |
| **N-3** | `calc_sharpe` **回 `0.0` 繞過 ETF 綜合分的 rescale 機制** ⇒ 資料不足的 ETF 少一顆星，剛好跨過 `KEEP_COMPOSITE_MIN=0.65` ⇒ **「留下」翻成「觀察」** |

⚠️ 同一份文件還登記了 `N-4` ~ `N-14`（含兩條**憲法自己過期**的登記：`N-12` 指 `CLAUDE.md §3.3`
宣告「magic number ❌ 0 項」與實況不符、`N-14` 指 §8.2 寫錯 `exit_signals.py` 的所在目錄）。
⚠️ **這些是該組的單組結論，未經第二組獨立驗。**

---

## 3. 🔴 主線：v2 五頁戰情室 UI 重寫

> **這一節解決的疑問**：客戶真正在做的事是什麼？做到哪了？我加入時卡在哪一步？

**這才是專案主線。**（`CLAUDE.md` §-1.2 記載客戶拍板路線 A「**UI 全新重寫**」。）
舊版 7 個頁籤是「功能分類」導向；v2 五頁是「**決策問句**」導向。

### 3.1 五頁是哪五頁

**顯示名的 SSOT 在 `shared/ia_nav.py::PAGE_LABELS`（L0 層，純字串、零 I/O、零 streamlit）**，逐字：

| 頁 id | 顯示名 | 這一頁回答的問題 |
|---|---|---|
| `today` | `🚦 今天` | 今天整體環境如何？ |
| `find` | `🔍 找標的` | 該買哪一檔？ |
| `inspect` | `🔬 查一檔` | 這一檔到底好不好？ |
| `hold` | `💼 我的持股` | 手上這些該不該動？ |
| `why` | `📖 憑什麼` | 上面那些結論的依據是什麼？ |

⛔ **任何地方都不准手抄這些字串** —— `ia_nav.py` 的檔頭自陳，它存在的理由就是
「指路句裡的分頁名改了，指路句不會自己跟著改」這個已發生過的 bug。

### 3.2 各頁進度（量測日 2026-09-24）

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

五頁的實作檔在 **`src/ui/views/page_{today,find,inspect,hold,why}.py`**。

| 頁 | 卡面來源 | 狀態 |
|---|---|---|
| `page_today.py` | **v2 卡面** —— 實測 **20 處** `src.ui_v2` 引用，module 層 `from src.ui_v2 import components / markup / page_today` | **✅ 唯一已整頁卡化**。該檔自陳「手寫 12 列 ＋ SSOT 展開 21 列 ＝ **整頁 33 張卡**」（commit `383c933`，2026-09-24） |
| `page_find.py` | 舊 `_ui_kit` 卡面（**0 處** `src/ui_v2` 引用） | 仍待卡化 |
| `page_inspect.py` | 同上，0 處 | 仍待卡化 |
| `page_hold.py` | 同上，0 處 | 仍待卡化 |
| `page_why.py` | 同上，0 處 | 仍待卡化 |

⚠️ **重要澄清：五頁「都已經是卡片骨架頁」，⛔ 不是長文字牆。**
實測五頁**全部**都在用 `src/ui/views/_ui_kit.py` 的 `grid` / `render_card_isolated` / `section_header`
（每頁都有數處到十數處呼叫）。**差別只在卡面樣式是 v2 版還是舊版**，
⛔ 不要以為 `find`／`inspect`／`hold`／`why` 還沒開始做。

**名詞：什麼是「卡（card）」？** 畫面上一個獨立的資訊方塊，自帶標題、徽章（badge）與內容；
`render_card_isolated` 的「isolated」指**單張卡渲染失敗不會拖垮整頁**。

### 3.3 `src/ui_v2/` 契約層

這是 v2 卡面的**契約層**（定義「卡長什麼樣」的純資料與純字串），實測 **7 支 `.py`**（含 `__init__.py`）：

| 檔 | 職責 | 純度 |
|---|---|---|
| `tokens.py` | 設計 token（顏色／間距／字級） | **純層**（零 streamlit） |
| `components.py` | 元件契約，含 `BADGES`（徽章字典） | **純層** |
| `page_today.py` | 「今天」頁的**版面與狀態契約**，含 `_LAYER_PLAN`（層序） | **純層** |
| `markup.py` | 產 HTML/CSS 字串（`page_css` / `card_html`） | **純層** |
| `render.py` | ⚠️ **唯一 import streamlit 的** | `src/ui/views/page_today.py` 的註解**明文寫「⛔ 不得被 import」** |
| `app_today.py` | **demo 進入點**（`streamlit run src/ui_v2/app_today.py`） | 線上 `app.py` **走不到它** |

⚠️ **關於「`src/ui_v2/` 有沒有 production caller」**：PR #675 的描述（寫於 **2026-09-23**）說
「`src/ui_v2/` 至今仍 0 production caller」。**該敘述已被 2026-09-24 的改動推翻** ——
實測 `src/ui/views/page_today.py`（線上 `app.py` 會 import 的檔）**已經 import 了
`components` / `markup` / `page_today` 三支**。
⇒ **現況：`src/ui_v2/` 的四支純層有 production caller（只有「今天」一頁在用）；
`render.py` 與 `app_today.py` 仍無。**

### 3.4 五頁怎麼掛上去（⭐ 這是最容易搞錯的一段）

**不是頂層頁籤，是側欄 radio。** 實作在 `app.py`：

| 項目 | 實測值（逐字） |
|---|---|
| 元件 | `st.radio` in `st.sidebar` |
| 標題 | `🆕 新版戰情室（試用中）` |
| session key | `_sb_ia_page`（`app.py` 的 `_IA_NAV_KEY`） |
| 第一個選項 | `不使用（用下方原功能）`（`_IA_NAV_OFF`）—— **它不是一個頁 id** |
| 預設 | **`index=0` ⇒ 預設關閉**，畫面完全等同舊版 7 頁籤 |

**選到一頁之後會發生什麼**（`app.py` 的 `_IA_VIEWS` 分派區塊）：
主畫面**整片換成那一頁** → 呼叫 `_render_footer()`（免責聲明照出）→ **`st.stop()`**。
⇒ 舊 7 頁籤、全域置底常駐條、「🔗 我的組合」狀態列、「🧭 總經指南針」**都不渲染**。

**沒選時連 import 都不做**：五頁各有一支 late-import wrapper（`_ia_view_today` 等），
**只有被選到的那一支會被呼叫**。⛔ 刻意**不用 `importlib` 動態字串** ——
那會讓 `tests/test_c3_layering_guard.py` 的 AST 掃描看不見 `L6→L5` 這條依賴，等於把分層依賴藏起來。
另有一道 **Fail Loud 對帳**：`_IA_VIEWS` 的 key 集合與 `PAGE_LABELS` 對不起來就 `raise RuntimeError`
（**刻意用 `raise` 不用 `assert`**，因為 `assert` 在 `python -O` 下會被整條拿掉）。
守衛：`tests/test_ia_v2_sidebar_nav.py`。

⚠️ **歷史教訓（⛔ 不要重蹈）**：2026-09-07（PR #663～#665）曾把五頁掛成**頂層頁籤**，
變成 **12 個頁籤並排** → 手機上舊頁籤被擠出畫面 →
**2026-09-08 客戶拍板撤回，改成側欄 radio**（`app.py` 註解標記 FE-35）。

### 3.5 ⚠️ 新舊 UI 是**並存，不是取代**

- `CLAUDE.md` §-1.2 客戶原文逐字：「**舊版不刪，留在 `main`**」。
- `app.py` 註解逐字：五頁與舊 7 頁籤「**刻意並存，不得合併、不得改名**」，並逐條說明為什麼看似重複的其實是兩件事：
  - `🔬 查一檔`（新）＝「一個代碼進去、一份判決出來」 vs `🔬 選股`（舊）＝「篩一批出來」；
  - `💼 我的持股`（新）＝ IA v2 的**殼**（多數區塊仍標 `unwired`） vs `💼 我的持股戰情室`（舊）＝**線上已在跑的既有實作**；
  - `📖 憑什麼`（新）與舊的「🔎 資料診斷 / 📚 教學 / 🧬 AI 問答」三處功能重疊 —— **三處都保留、都沒動**。

**名詞：什麼是 `unwired`？** ＝「**版面已經畫出來，但還沒接上真實資料**」的區塊。
它會誠實顯示自己沒資料，⛔ 不是畫假數字（這是 `CLAUDE.md` §1 Fail Loud 的要求）。

**舊 UI 的規模**（量測日 2026-09-24）：`src/ui/tabs/` **22 支 `.py`**，
另有 `src/ui/etf/`、`src/ui/pages/`、`src/ui/render/`。

**名詞：`L0`~`L6` 是什麼？** `CLAUDE.md` §8.2 定義的**七層架構**，import 只能由高往低：

| 層 | 職責 | 代表目錄 |
|---|---|---|
| **L0** Infra | 常數／TTL／門檻／設定（被全層 import，⛔ 不得依賴任何 L1+） | `shared/`、`src/config/` |
| **L1** Data | 外部資料抓取／快取／proxy（⛔ 不得 import streamlit，除登記例外） | `src/data/` |
| **L2** Compute | **純函式**運算／評分／策略（⛔ 不得做 I/O） | `src/compute/` |
| **L3** Service | 業務邏輯編排／AI 整合 | `src/services/` |
| **L4** Render | 圖表生成／通用 UI 元件 | `src/ui/render/` |
| **L5** UI | Streamlit Tab / View 級組裝 | `src/ui/tabs/`、`src/ui/views/`、`src/ui/etf/`、`src/ui_v2/` |
| **L6** App | session 路由 + 全域編排 | `app.py` |

**名詞：`EX-PASSTHRU-1` 是什麼？** `CLAUDE.md` §8.2.A.1 登記的一條**分層豁免**：
L5/L6 本來不准直接呼叫 L1 的資料抓取函式，但若該函式**沒有對應的 L3 service**、
caller 端**只是取數**、且該函式**在 L1 內已自帶 `@st.cache_data`**，就豁免。
⛔ 但它**不涵蓋**「UI 檔內自己實作抓取」「UI 層自建 cache」「跨層直取底線開頭的私有符號」。
機械守衛：`tests/test_c3_layering_guard.py`。

---

## 4. 🔴 分支現況

> **這一節解決的疑問**：我該在哪條分支上工作？線上跑的是哪條？為什麼看起來有矛盾？
>
> ⛔ **本節只攤開事實與矛盾，⛔ 不下「應該怎麼做」的建議** ——
> 分支政策是**客戶頒布**的，可能另有本檔不知道的裁示。

### 4.1 三條線各是什麼

| 分支 | HEAD | 內容定位 |
|---|---|---|
| `origin/main` | `bcd46c7`（Merge PR #681） | **Streamlit Cloud 實際部署的線**。⚠️ 名義上是「v1」，但**實測已含 v2 的 code**（見 4.2 第 2 點） |
| `origin/v2` | `8f4a060`（2026-09-16，**純文件 commit**：「編入客戶 2026-09-16 分支政策」） | 客戶 2026-09-16 頒布分支政策時開的線。**vs `main`：ahead 14／behind 39** ⇒ **幾乎沒動過** |
| `claude/stock-dashboard-handoff-g9dtm0` | `10b6e5e` | **實際的開發線**（工作樹乾淨）。**vs `main`：ahead 91／behind 39** |

**PR #675**（量測日 2026-09-24）：`open`／**`draft`**／**base `v2`**／head 是上面那條開發線／
77 commits／90 changed files／`mergeable_state: clean`。

📌 **2026-09-26 二組複核**：上表是 2026-09-24 的量測，**已過期**（現值：`origin/main` `84f58ea`〔#698〕；開發線 `7f87f41` vs main ahead 102／behind 84；`origin/v2` ahead 14／behind 84；PR #675 仍 open／draft／base `v2`／head `7f87f41`，GitHub 上 open PR 只有 #675 —— 二組以 `git rev-list` 與 GitHub API 重跑一致，見文件開頭 c-5 B10）。
⚠️ **上表數字會漂移**，接手請現場重跑：
```
git fetch --all
git rev-list --left-right --count origin/main...HEAD
git rev-list --left-right --count origin/main...origin/v2
```

### 4.2 🔴 三者的矛盾（逐條攤開，⛔ 不下結論）

**以下四點同時為真：**

**1. 客戶 2026-09-16 的原文裁示**（`CLAUDE.md` §-1.2 逐字）：
> 「**新分支名 `v2`，從當前開發線 HEAD 開**」
> 「**`main` 保留 v1，不合併直到 v2 完成**」
> 「**v2 上線時直接合併 `main`**，Streamlit Cloud 自動重部署」

**2. 但實測 `main` 上已經有 v2 的 code。** `git ls-tree -r origin/main` 命中：
- `src/ui_v2/` **7 支 `.py` 全部在 `main`**
- `src/ui/views/page_{today,find,inspect,hold,why}.py` **五頁全部在 `main`**
- `shared/ia_nav.py` **在 `main`**

**它們是怎麼進去的**（`git log origin/main --diff-filter=A` 實測）：

| 進入物 | commit / PR | 日期 |
|---|---|---|
| `shared/ia_nav.py` | `50b6412`（PR #662） | **2026-09-05** |
| `src/ui/views/page_today.py` | `8f81e96`（PR #663） | **2026-09-07** |
| `src/ui/views/page_find.py` | `1e90d9d`（PR #664） | **2026-09-07** |
| `page_inspect.py` / `page_hold.py` / `page_why.py` | `340be68`（PR #665） | **2026-09-07** |
| `src/ui_v2/`（6 支 + `__init__`）與「今天」頁卡面 | `dbab9b1` / `0aaef3e` / `383c933`，經 PR **#678／#679／#680／#681** | **2026-09-23~24** |

⇒ **五頁與 `ia_nav` 是在 2026-09-16 分支政策頒布之前就進 `main` 的；
`src/ui_v2/` 則是在頒布之後（2026-09-24）才進 `main` 的。**

**3. 但實測 `main` 上幾乎沒有 v2 的文件。**
`origin/main` 的 `docs/v2/` **只有 2 個檔**（`spec/S1-6_COMPLIANCE_COPY_GUIDE.md` 與
`wireframe/wf_page_why.js`）—— **`docs/v2/prototype/`、`docs/v2/audit/`、`docs/v2/stage1/`
與 `docs/v2/README.md` 整個不在 `main`**。
根目錄同樣缺：`DEAD_CODE.md`／`MACRO_MASTER.md`／`STRATEGY_INTAKE.md`／
`HANDOFF.md`（本檔）／`HANDOFF_MARGIN_RELATIVE.md` **都不在 `main`**。
（`main` 根目錄只有 16 份 `.md`，開發線有 21 份。）

**4. 而實際開發沒有發生在 `v2` 上**，發生在 `claude/stock-dashboard-handoff-g9dtm0`；
**PR #675 的 base 卻指向 `v2`**（那條幾乎沒動過的線）。

> ⚠️ **本檔只陳述上述四點事實。⛔ 不判斷誰對誰錯、⛔ 不建議改 base、⛔ 不建議 merge。**
> ⚠️ **未查證**：是否有客戶在 2026-09-16 之後另外裁示放寬分支政策。
> 本組只查了 `CLAUDE.md` §-1.2 的文字，**沒有翻對話紀錄**（也無從翻）。

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

### 4.3 接手 AI 在分支上 ⛔ 不准自己決定的事

- ⛔ **不得自行 merge PR #675**、⛔ **不得轉 ready**、⛔ **不得改它的 base**。
- ⛔ **不得自行把開發線併進 `v2` 或 `main`**。
- ⛔ **不得因為「`main` 上已經有 v2 code」就推論「分支政策已作廢」。**

**理由**：`CLAUDE.md` §-1.5.D v3 §03-2 ② 把「**不可逆操作**」列為**必須請示客戶**的兩類之一；
merge 與改 base **直接影響線上部署**（`main` 一動，Streamlit Cloud 就重新部署）。

⚠️ **一個容易誤用的例外**：`CLAUDE.md` §-1.5.A-6 末項記載 `PROCESS.md §4` 有一條
「直接 merge PR」的**常設授權**。⛔ **不要拿它來合理化本節禁止的事** ——
那條授權涵蓋的是「一般 PR 走完流程後 merge 這個動作」，
⛔ **不涵蓋**「改 base」「把 draft 轉 ready」「在分支政策本身有矛盾時逕自選一邊」。
分支政策是客戶頒布的，⇒ 落在請示白名單。

### 4.4 需要時該怎麼問客戶（**格式範例**）

⚠️ `CLAUDE.md` §-1.5.D v3 §03-1 **嚴禁向客戶拋出選擇題**；§03-2 要求**必須附總管推薦方案與具體原因**。
⇒ ⛔ **不要問「要不要改 base？」**（那是純選擇題、且用了技術語言），而要用**業務語言 + 附推薦**，例如：

> 「目前新版 UI 的開發其實都在 `claude/…` 這條線上，你 9/16 開的 `v2` 那條幾乎沒動；
> 而線上實際跑的 `main`，已經有一部分新版程式了。
> **我建議把開發線直接併進 `main`（理由：那才是線上部署的線，`v2` 已經沒有承載開發）。**
> 你要維持原本『v2 完成才併 main』的規劃，還是改成這樣？」

⚠️ **上面那句「我建議」只是格式範例，⛔ 不是本檔的建議。**
接手 AI 必須**自己先查證、自己形成推薦**，再照這個格式問。

### 4.5 其他分支（一行帶過）

`origin/feat/v2-card-live`／`feat/v2-card-template`／`feat/v2-fullpage-cards`／`feat/v2-verdict-cards`／
`test/ui-v2-guards`／`wireframe/stock-ia-5pages`／`origin/data`／`origin/claude/my-stock-dashboard-ui-rewrite-r40gjc`。
~~⚠️ **未查證各自是否已合併、是否可刪。**~~ → 📌 **2026-09-26 已查證**（兩組獨立、交叉一致）：`data` ⛔ 勿刪（活的排程線）；`feat/v2-card-template`、`wireframe/stock-ia-5pages` 可刪；`claude/my-stock-dashboard-ui-rewrite-r40gjc` 在 `v2` 保留前提下可刪；其餘四條已不在 remote。詳見第一節 (c) c-5 **C6**；刪＝不可逆 → **C6-del** 卡住待客戶。

⚠️ **另一個實測到的小矛盾**：`.github/workflows/pr-check.yml` 的觸發分支寫
`branches: [main, v2, ui-v2]`，但 **`origin/ui-v2` 這條分支已不存在**
（`git rev-parse --verify origin/ui-v2` → `fatal: Needed a single revision`）。
⇒ 設定檔裡留著一條指向不存在分支的觸發規則。**⛔ 本檔只登記，不建議動它**（§-1：沒指派不動工）。

---

## 5. 環境與工具的坑

> **這一節解決的疑問**：這個容器裡什麼能用、什麼不能用、跑測試怎麼跑、什麼東西 CI 綠了也不算數？

| 坑 | 實測（量測日 2026-09-24） | 怎麼做 |
|---|---|---|
| **沒有 `gh` CLI** | `which gh` → 不存在 | 用 `mcp__github__*` MCP 工具，或直接打 GitHub REST API |
| **跑測試** | `pytest.ini` 逐字：`testpaths = tests`、`addopts = --strict-markers -m "not slow"` | `pytest` ＝ **fast lane**（預設排除 slow）；`pytest -m slow` ＝ **AppTest lane** |
| **CI 兩條 lane** | `.github/workflows/pr-check.yml`：`fast-checks` 跑 `pytest -v`（**失敗即阻擋 merge**）／`slow-tests` 跑 `pytest -v -m slow --tb=short`，且 **`continue-on-error: true`** | ⚠️ **slow lane 紅了不會擋 merge，也不會在 PR 上顯眼** ⇒ **要自己去看 job log** |
| ⭐ **原型：CI 綠不算數** | `CLAUDE.md` §8.2.A.5 客戶**逐字頒布**：「`docs/v2/prototype/` 的守衛只在手動執行時跑。CI 只跑 `tests/`。**改任何一份原型前，必須手動執行產生器，不能只看 CI 綠。**」<br>⚠️ **機制原因（即使 CI 現在真的會跑產生器，這條仍然成立）**：`tests/ui_v2/test_prototype_generator.py` 把產生器的輸出 **重導到 `tmp_path`** ⇒ 它**永遠不證明「入庫的 `today_v2.html` 與現行 `src/ui_v2/` 同源」**。真實案例：commit `57fc464`（2026-09-24）「重產 `today_v2.html` —— 修 provenance 檔頭對不上造成的 drift 紅燈」 | 改 `docs/v2/prototype/` 任何檔之後，跑：<br>`python docs/v2/prototype/gen_today_v2.py`<br>（就地覆寫 `today_v2.html`，尾端會印自驗摘要） |
| **沙箱驗不到的東西** | Streamlit Cloud 線上行為／NAS webhook／12 支 cron workflow 的實跑；`docs/` 整棵樹 **pytest 從不收集**（`testpaths = tests`） | `CLAUDE.md` §-1.5.A-7 要求「**無法沙箱驗的限制要誠實標明**」 ⇒ 交付時要寫出來，⛔ 不得吞掉 |
| **缺件（本容器）** | **無 `FINMIND_TOKEN` / `FINMIND_API_TOKEN`** 環境變數；**無 `.streamlit/secrets.toml`**；**無 FinMind SDK**（`import FinMind` → `ModuleNotFoundError`） | raw HTTP 仍可用，但需要 token 的路徑一律走不通 ⇒ 見 **§6 卡關點** |
| **Python 套件**（2026-09-26 二組複核補登） | 系統 `python3`（3.11）**無 pandas、無 pytest**（`import` → `ModuleNotFoundError`）。本 session 曾在 scratchpad 建 venv（pandas 3.0.6／pytest 9.1.1／streamlit 1.59.2／pyarrow），**scratchpad 非持久** | ⛔ 不要寫「本容器沒有 pandas／pytest 所以測試跑不了」—— 自建 venv：`python3 -m venv <scratchpad>/venv && <scratchpad>/venv/bin/pip install -r requirements.txt pytest`（⚠️ 建法為二組建議，未實測整份 requirements 能否裝完） |

⚠️ **一個量測值漂移的實例（拿來當「為什麼不寫死數字」的活教材）**：
`CLAUDE.md` §8.2.A.5 記載 `docs/v2/prototype/gen_today_v2.py` 有「**183 道 `assert`**」（量測日 2026-09-22）。
本組實測（2026-09-24）為 **194 道**。
⇒ **⛔ 不要引用憲法裡的這個數字**；需要時**現場跑 `grep -c "assert " docs/v2/prototype/gen_today_v2.py`**。
（憲法該節自己也寫明「本條的效力 ⛔ 不依賴這兩個數字」。）

---

## 6. 支線：融資餘額燈「相對分位」

> **這一節解決的疑問**：上一輪在做什麼？卡在哪？客戶還欠我一個什麼決定？
>
> ⚠️ **這是一條支線，⛔ 不是主線。** 主線是 §3 的 v2 UI 重寫。
> 完整細節見 `HANDOFF_MARGIN_RELATIVE.md`（112 行）。

### 6.1 客戶要解的問題

客戶要的是「**趨勢**」，不是「**水位**」。

**實測**：`shared/macro_buckets.py` 全檔 `pct_change|rolling|quantile|.diff(|.shift(|ewm`
**命中數 ＝ 0**（量測日 2026-09-24）⇒ **28 盞燈（總經 16 ＋ 持股 12）全部只判水位。**

### 6.2 已寫好但沒接線的解法

`shared/relative_thresholds.py`（**448 行**）已寫好、`tests/test_relative_thresholds.py` **33 個測試綠**。
主要符號：`classify_by_pct_rank(series, *, window=756, yellow=0.75, red=0.90, high_bad=True, min_periods=None)`、
`margin_leverage_ratio(margin_yi, market_cap_yi)`、`vol_normalized_bias`、`foreign_futures_share`。

⚠️ **前一版交接說「0 production caller」—— 本組實測發現這句話不精確，在此更正**：

| 符號 | production caller（排除 `tests/`） |
|---|---|
| `classify_by_pct_rank` | **0**（`shared/macro_buckets.py` 只在**註解**裡提到它） |
| `margin_leverage_ratio` | **0**（同上，只在註解） |
| `vol_normalized_bias` | **0** |
| `foreign_futures_share` | **0** |
| **`DEFAULT_BIAS_MA_LEN`** | ⚠️ **有 1 個** —— `src/data/macro/macro_cache_reader.py` 真的 `from shared.relative_thresholds import DEFAULT_BIAS_MA_LEN` 並實際使用 |

⇒ **正確說法：四個分類函式 0 production caller；但整個模組**不是**孤兒（有一個常數被真的用到）。**
⛔ 刪它之前務必看清楚這個差別（並先查 `DEAD_CODE.md`）。

`macro_buckets` 自陳逐字：「絕對門檻已被市值成長淹沒，實測 5,148 億早已穿透兩線 →
**燈號恆紅、鑑別力歸零**。相對化方案見 `shared/relative_thresholds`（**尚未接線，屬行為變更需另案**）。」

### 6.3 🔴 但預覽證明「相對分位」救不了

2026 年 122 個有結論日，**絕對門檻組與相對分位組都是 122/122 恆紅、0 轉態**。

**原因**：融資餘額 2026 **單邊創新高**（3,509.5 → 5,878.7 億），
而**單調上升序列的滾動分位恆等於 1.0**
（實測 2026 pct_rank 中位數 **0.9954**、**40/122 天 ＝ 1.0**、**122/122 天 ≥ 0.90 紅線**）。

⇒ `relative_thresholds.py` docstring 宣稱的「改成相對分位才有鑑別力」，
**在本 repo 實測資料上不成立**。唯一真實增益是 **B 組比 A 組早約一個月轉紅**
（2025-11-28 vs 2025-12-30）＝ **靈敏度差異，不是鑑別力**。

⚠️ 預覽腳本與圖**只存在於前一個 session 的 sandbox，未進 repo，已隨容器消失**。
要重現請依上述參數自行重跑。

### 6.4 🔴 順帶查出的資料污染

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

`data_cache/finmind_margin.parquet`（4,957 列）**單位混用**：

| 列數 | 量級 | 口徑 | sanity `[500, 10000]` 億 |
|---|---|---|---|
| **1,982** | 1e11~1e12 | **元**（`MarginPurchaseMoney`） | 通過 |
| **2,975** | 1e5~1e7 | **張數**（`MarginPurchaseVolume`） | — |

**逐日交錯混在整段歷史。** `source` 欄：4,930 列舊 buggy 路徑、27 列修好的路徑。
⚠️ **任何吃這份 parquet 的分析結論都不可信。**
📌 **2026-09-26 二組複核更正（上文一字未改）**：上表量的是**開發線**那份（blob `2049223`＝merge-base 版）。**`origin/main` 那份已由 cron `a59ff63`（2026-09-24）整檔重建為乾淨資料**：4,912 列、單一 `source`（`MarginPurchaseMoney:TodayBalance`）、4,912／4,912 列通過 sanity —— 見文件開頭 c-4 **B7a**（已完）。

**🔴 但 cron 的 code 已經修好了** —— `scripts/update_macro_history.py::fetch_finmind_margin`
**實測已 import 並呼叫** `shared/margin_schema.py` 的 `extract_margin_money_series` 與
`margin_twd_sanity_mask`；守衛 `tests/test_b3_margin_schema.py`。
修復後寫入的 27 列（`2026-08-06` → `2026-09-11`）**全部乾淨**。
⇒ **髒的是 2026-08-07 之前舊 code 留下的歷史積欠，⛔ 不是現在的程式。**

**另一份：`data_cache/finmind_m1m2.parquet` 損壞**（量測日 2026-09-24，`origin/main` `a59ff63`，**登記，未修**）：

| 項目 | 實測 |
|---|---|
| M1B／M2 出現**負值** | 例：`2026-06-01` `m1b=-125145`、`m2=-11301` |
| `m1b_m2_gap` 範圍 | 約 **-13,976 ～ +7,721**（對照該燈門檻帶：黃 ≤1.00／紅 ≤0.00） |
| 最後一列 | `2026-07-01` |

⚠️ 本分支（`claude/stock-dashboard-handoff-g9dtm0`）的同一檔**與 `main` 不同**（240 列 vs 238 列；
`2026-06-01` 為 `m1b=-119620`、`m2=-4349`；gap 上限約 +36,034）—— **兩邊都壞**，只是壞法不同。
**後果**：v2「🚦 今天」燈卡的「變化方向」列，**M1B-M2 固定顯示「無資料」**，程式刻意不讀此檔
（`shared/lamp_direction_thresholds.py` 檔頭）。**未修**（§-1：無客戶指派；~~且修法同 §6.5 需重抓歷史~~）。
📌 **2026-09-26 二組複核更正**：「修法同 §6.5 需重抓歷史」不成立 —— `origin/main` 那份在 2026-09-24 cron **已整檔重抓**（`fetched_at` 單一值、238 列全來自 `CBC:PXWeb:EF19M01+EF21M01`），仍有 87 列 M1B 或 M2 為負 ⇒ 根因是現行 `scripts/update_macro_history.py::fetch_finmind_m1m2` 的 CBC PXWeb 解析（「把流量當存量」，`origin/main` `scripts/export_stock_db.py::_money_supply_sanity_gate` docstring 自陳），**FinMind token 與此無關**；要解須改該解析碼。見文件開頭 c-4 **B7b**。

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

**待辦：M1B-M2 燈「替代值未標明」**（**待辦，另開工單；客戶 2026-09-25 裁示**；**登記，未動工**）：

| 項目 | 內容 |
|---|---|
| 事實 | 線上 M1B-M2 現值走 `src/data/macro/macro_snapshot.py::fetch_m1b_m2_block` → `src/data/macro/tw_macro.py::fetch_cbc_m1b_m2`（CBC ms1.json → CPX EF15M01 → `^TWII` 代理）→ FRED → IMF |
| 問題 | 退到 `^TWII` 代理層時，卡片「現值」實為**加權指數動能推算**，但 `hit_source` 標籤仍是通用字串、`is_proxy_tier` 在 `fetch_m1b_m2_block` 重新打包時**被丟掉** ⇒ 使用者看不出是替代值（違 `CLAUDE.md` §1「任何填補必須在輸出帶旗標」精神） |
| 補充 | 卡片「缺值」文案（`shared/station_specs.py::MISS_TEXT[MISS_NO_INPUT]`，16 盞共用）經查證在**所有來源皆失敗時屬實**，客戶裁示**維持不改**；`docs/v2/prototype/lamp_preview.html` 中 M1B-M2 顯示的「沒抓到」為**預覽腳本自設狀態，非線上現象** |
| 處置 | 依 §-1：**只登記，未動工** |

⚠️ 上列事實為**程式閱讀，未實跑**；**單組結論，未經第二組驗**（§-2 規則 6），⛔ 不得當既定前提。

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

**~~待辦：~~ 已結案（#697 `f14c5fd`，2026-09-26）：今天頁燈卡「▸ 詳細」~~鍵盤／螢幕朗讀點不到~~ 鍵盤／螢幕朗讀 a11y**（**待補；客戶 2026-09-25 裁示：選 (b) 並接受此代價，登記待補**；~~**登記，未動工**~~ → **2026-09-26 客戶核准動工，#697 結案**）：

| 項目 | 內容 |
|---|---|
| 事實 | 今天頁燈卡「▸ 詳細」已由原生 `<details>` 改為樣式開關（checkbox＋label），~~改後鍵盤／螢幕朗讀點不到~~ |
| 📌 事實更正（2026-09-26；有意識的更正，⛔ 不是漏刪） | **「鍵盤點不到」不成立**：樣式開關自引入的 commit `6300672`（2026-09-25）起，input 就是**視覺隱藏、非 `display:none`**，可 Tab 聚焦、Space 切換，`:focus-visible` 已有 1px 點線焦點框（`src/ui_v2/markup.py::_fold_rules`；以 `git log -S` 核對）。當時真正的缺口在**朗讀與可見度**：① 16 個開關的朗讀名稱都只叫「▸ 詳細」、不帶卡片標題；② 無 `aria-controls`；③ 1px 點線焦點框在深色卡上幾乎看不見（②③ 依 #697 程式註解）。⇒ 被推翻的只有「鍵盤點不到」；朗讀面的擔心方向是對的，已由 #697 補上 |
| 背景 | 原生 `<details>` 在客戶 iPhone 上點不動（**真因未確認**，沙箱無 WebKit 可測），故改用樣式開關 |
| 補法方向 | （**僅登記、未驗**）input 保留可聚焦並加 `aria-expanded`／可見焦點樣式，或改回可存取元件 |
| 實際補法（#697） | `aria-labelledby`＝開關字＋卡標題 ⇒ 朗讀「▸ 詳細 〈卡標題〉」；`aria-controls` 指向內容區；焦點框改 2px 實線、`outline-offset:2px`（不壓「▸」）；⛔ **刻意不用 `aria-expanded`**（無 JS 無法隨勾選更新，寫死的值會對朗讀說謊，§1 —— 與上列「補法方向」不同，屬有意偏離）；5 頁 72 組截圖滑鼠／觸控逐像素一致。⚠️ **未在真 iPhone／VoiceOver 實測**（見文件開頭 D1） |
| 處置 | ~~依 §-1：**只登記，未動工**~~ → 2026-09-26 客戶核准動工，#697 `f14c5fd` 結案 |

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

**待辦：K1 在燈卡上復發 —— 「門檻出處」「命中來源」露出內部識別字**（**客戶 2026-09-25 裁示：K1 由「不修」升級為「需規格補 16 盞對照表」**；**登記，未動工**）：

| 項目 | 內容 |
|---|---|
| 事實 | K1 原登記於 `docs/v2/prototype/gen_today_v2.py::COPY_SPEC_GAPS`（鍵 `"K1"`），並渲染進 `docs/v2/prototype/today_v2.html`：「🔴 卡面上有內部函式名／模組名／enum 名，五頁全中 —— ⛔ 本輪不修，因為修它必須發明規格沒給的中文標籤」。2026-09-25 稽核 `origin/main` `8873c9e` 查出：「🚦 今天」→「指標明細」**16 盞燈卡**的「▸ 詳細」內（#685／#686 起），「**門檻出處**」直出 `shared/macro_buckets.py::BUCKET_DANGER_SPECS` 的 `source`（例 `SSOT:HEALTH_DEFENSE_THRESHOLD(35)+DESIGN(50)`、`SSOT:MACRO_THRESHOLDS.VIX`），「**命中來源**」直出 `src/compute/macro/macro_helpers.py::compute_five_bucket_summary` 經 `_traced` 產生的 `hit_source` 標籤（例 `macro_info.vix.current (Yahoo ^VIX → FRED VIXCLS)`）⇒ **K1 在燈卡上復發** |
| 客戶裁示 | K1 由「不修」升級為「**需規格補 16 盞對照表**」：規格須逐盞提供「門檻出處」與「命中來源」的**使用者白話文案**；⛔ **AI 不得自行發明文案**（＝捏造，違 `CLAUDE.md` §1） |
| 卡在哪 | **規格（16 盞對照表）尚未提供** |
| 優先序 | 客戶定序：**四頁卡化 ＞ 今天頁 7 塊未接線 ＞ K1** |
| 處置 | **本輪未修**；只登記，待規格到位後再動工 |

⚠️ 上列稽核為**單組、唯讀**結論，**未經第二組驗**（§-2 規則 6），⛔ 不得當既定前提。

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

**~~待辦：~~ 規格變更（⛔ 不是 bug fix）：標記矛盾：空白卡寫『這是一個有效的結果』卻顯示 #7『缺漏·可重跑』**（**客戶 2026-09-26 裁示：合併為一輪、另開工單，兩頁共用對照一起修；⛔ 不得併入持股頁卡化那一輪**；~~**登記，未動工**~~ → **2026-09-26 同日以規格變更處理（新增徽章 #11，見本條末「處理結果」表）—— ⚠️ 只處理了一部分，見下**）：

| 項目 | 內容 |
|---|---|
| 受影響卡 | **持股頁（PR #688，分支 `claude/v2-hold-cards` `57b5993`）**：`src/ui/views/page_hold.py` 的 `hold.switch`／`hold.macro_stage`／`hold.take_profit` 空白態（`else:   # UI_EMPTY`，`why` 寫「**這是一個有效的結果**」）。**找股頁**：`src/ui/views/page_find.py` 的 `find.screen_result` 空白態（`SCREEN_EMPTY_NOW`「選股已完成，符合條件的標的是 0 檔」，`why` 寫「這是一個有效的結果」）—— ~~既有「0 筆結果」徽章 #7 vs 文字「有效結果」（⚠️ 待確認：`src/` 內 grep 不到「0 筆結果」字面，該標籤字樣出處未查到）~~ → **更正（2026-09-26）**：「0 筆結果」是**轉述、不是畫面字面**；畫面上的真實文字是 `SCREEN_EMPTY_NOW`「**選股已完成，符合條件的標的是 0 檔**」（`src/ui/views/page_find.py`），徽章 #7 vs `why`「這是一個有效的結果」 |
| 真因 | 上列空白態的建構函式呼叫 `shared/ui_state.py::classify_ui_state()` **未帶 `reason`** ⇒ 判為 `UI_EMPTY`；共用對照 `src/ui/views/page_today.py::V2_STATE_VOCAB` 把 `UI_EMPTY` 翻成 `("empty", None)`；`src/ui_v2/page_today.py::resolve_badge()` 對 `empty` 且無 `MISS_NOT_APPLICABLE` ⇒ 回 **#7「缺漏 · 可重跑」**（`src/ui_v2/components.py`） |
| 徽章缺口 | 現有 10 顆徽章（`src/ui_v2/components.py` `_badge(1..10)`）**沒有一顆**表示「有效的空結果」；#8「不適用 · 重跑無效」語意也不對 |
| 既有揭露 | `page_today.py` 在 `V2_BADGE_AMBIGUOUS` 旁已註明：裸 `UI_EMPTY` 的徽章文字「比 L0 多講了一句話」（#7 宣稱可重跑），要改須先改 v2 契約層 |
| 修法方向 | 在**共用徽章集／`V2_STATE_VOCAB`** 層決定（⛔ 不在單頁另發明徽章）；屬**共用元件異動 ⇒ 動工前須客戶核准** |
| 附記 | 持股頁卡化那一輪的**手機截圖上傳失敗**（檔案過大／過長，伺服器回 400）；客戶表示**不阻擋任何事** |
| 處置 | ~~依 §-1：**只登記，未動工**~~ → 2026-09-26 客戶裁示動工，走**規格變更**（見下表） |

⚠️ 上列事實為**程式閱讀（grep `/home/user/msd-hold` 的 `src/`、`shared/`），未實跑**；**單組結論，未經第二組驗**（§-2 規則 6），⛔ 不得當既定前提。

**處理結果（2026-09-26；性質＝規格變更，⛔ 不是 bug fix；決策者：客戶）**：

| 項目 | 內容 |
|---|---|
| 規格變更 | `docs/v2/spec/UI_COMPONENTS.md §2` 徽章 ~~恰 10 種~~ → **11 種**：新增 **#11「▨ 無資料」**＝有效的空結果；圖示／文字**逐字沿用** L0 `shared/ui_state.py` `UI_STATE_META[UI_EMPTY]`（實作直接讀那一格，⛔ 不抄）。舊表述保留加刪除線，理由兩邊並陳於該段 |
| 射程 | **登記制**：`src/ui/views/page_today.py::V2_VALID_EMPTY_SPEC`，只列「上游成功算完、結果真的是 0／空」且**同一對 `(key, now)` 不會出現在任何缺漏／失敗路徑**的卡；L0 態須恰為 `UI_EMPTY`。其餘一律維持原徽章 |
| ✅ 納入（1） | `hold.portfolio_count` ＋ `COUNT_EMPTY_NOW`：Sheet 讀成功、`list_portfolios()` 長度 0（L3 `STATUS_BOUND_EMPTY`）；讀取失敗走 `None`＋`MISS_FETCH_FAILED`（紅）、未綁走 idle，兩者都不會產出這一態 |
| ⛔ 未納入：本條點名的四張卡 | **`find.screen_result`**：`_load_survivors()` 讀取失敗時 L3 回空表＋「季快照未就緒」，畫面同樣落到 `SCREEN_EMPTY_NOW` ⇒ 同一對 `(key, now)` 蓋著真缺漏。**`hold.switch`**：L3 `get_switch_in_candidates()` 讀選股池失敗時**吞例外回 `[]`**，換出端判不出來的燈也不會變紅 ⇒ 「沒有建議換股」可能來自失敗。**`hold.take_profit`**：L3 只判有損益% 的衛星，**全部缺均價時也回 0 檔**（`why` 自陳「也可能是…判不了」）。**`hold.macro_stage`**：「總經本輪未評估」＝未評估，客戶明文排除 |
| ⛔ 未納入：其他候選 | `hold.deep.dividend_cash`「近一年查不到任何一筆配息」（L3 自陳上游把「真的沒除息」與「抓不到配息」回成同一個空序列）；`hold.position_cap`（未評估）；`hold.deep.stress`／`var`／`dividend_cash` 的「算不出來」、`hold.alloc_split`／`deep.core_satellite`「算不出比例」（缺張數／均價＝真缺漏）；`hold.ai_summary`「AI 回了空白」（異常）；`NOT_BOUND_*`／`EMPTY_SHEET_*`／「這一輪沒有讀 Google Sheet」／`NO_ROWS_NOW`（漂移）／`hold.vix`（稍後再試）／`find.sector_flow`（快取未產生）；今天頁全部 |
| ⚠️ 原矛盾仍在 | 上列四張點名卡**仍畫 #7**，卡面仍寫「這是一個有效的結果」⇒ **矛盾對它們還沒解**。要讓它們畫 #11，須先在 builder／loader 把「真的是 0」與「失敗被吞成空」分成不同的 `now`（或讓 L3 不吞例外）—— **屬改卡面文案／builder／L3，不在本次授權**（客戶授權範圍＝共用對照表＋規格檔＋對應測試），**待客戶另行裁示** |
| 程式 | 分支 `claude/v2-badge-vocab`（基於 `66e4fe0`，未 commit）：`src/ui_v2/components.py`（#11）、`src/ui_v2/page_today.py`（`resolve_badge(valid_empty=)`，非 `empty` 態拒收）、`src/ui/views/page_today.py`（登記表＋三頁共用 `v2_card_badge_n()`）、`page_find.py`／`page_hold.py` 各改一個呼叫點＋import、`shared/ui_state.py`（僅註解）、測試 |

⚠️ 納入／排除判定為**單組程式閱讀＋列舉實跑**的結論，**未經第二組驗**（§-2 規則 6），⛔ 不得當既定前提。

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

**待辦：4 張卡「靜默失敗」—— 讓「抓取失敗」與「真的是 0」分開，才能畫 #11**（**客戶 2026-09-26 裁示：分兩批工單**；~~**登記，未動工**~~ → **2026-09-26 客戶裁示更新：第 1 批已動工，2/3 修好；第 2 批仍未動工**）：

| 項目 | 內容 |
|---|---|
| 背景 | 上條「處理結果」已新增 #11「▨ 無資料」，但點名的卡因**失敗被吞成空**而未納入 `V2_VALID_EMPTY_SPEC`。本條即其後續工單 |
| **第 1 批（3 張）** | `find.screen_result`（`SCREEN_EMPTY_NOW`）、`hold.switch`（`SWITCH_EMPTY_NOW`）、`hold.take_profit`（`TP_EMPTY_NOW`） |
| 第 1 批範圍 | **只改「抓取失敗 vs 真的 0」的判定**；⛔ **不動資料層** |
| 第 1 批證據 | ① `src/ui/views/page_find.py:836-838` 接住存活池例外、回 `None`；② `src/services/fundamental_screener_service.py:493-498` 再吞一次（`survivors_df = None`），之後 `:338-339` 回空表並附誤導說明「**季快照未就緒**」；③ `src/services/dividend_station_service.py:781-785`（`get_switch_in_candidates`）接住例外回 `[]`，且排名表為空時**也**回 `[]`；④ `flag_take_profit`（`dividend_station_service.py:944-952`，`return` 在 `:953`）略過 `_detail.error` 的列；⑤ 卡面文案寫「**這是一個有效的結果**」：`src/ui/views/page_hold.py:1981-1990`（`hold.switch`）、`:2161-2168`（`hold.take_profit`）、`src/ui/views/page_find.py:1393`（`find.screen_result` 的 `why`） |
| **第 2 批（1 張）** | `hold.deep.dividend_cash`（`CASH_NO_PAYOUT_NOW`） |
| **第 1 批結果（2026-09-26，客戶裁示）** | 分支 `claude/v2-silent-fail-b1`，PR 目標 `main`。⚠️ 待確認：本組核對時該分支**只在 `/home/user/msd-silent` 本地、改動未 commit**（`page_find.py`／`page_hold.py`／兩支既有測試＋新檔 `tests/test_v2_silent_fail_b1.py`），`git ls-remote origin` **查無此分支** ⇒ PR 尚未開、行號以該工作區為準 |
| 第 1 批 ✅ `find.screen_result` | **已修**：存活池失敗時卡轉**紅**「**選股中止**」（`SCREEN_ABORTED_NOW`，`page_find.py:610`），卡面點名「**L3 基本面存活池…拋出例外**」（`page_find.py:498`；測試 `test_v2_silent_fail_b1.py:89`） |
| 第 1 批 ✅ `hold.take_profit` | **已修**：held 個股列帶 `_detail.error` 時卡轉**紅**「**停利判不出來**」（`TP_FAILED_NOW`，`page_hold.py:1530`；判定條件 `:2142-2148` 與 L3 `flag_take_profit` 跳過條件 `dividend_station_service.py:946` 逐字對齊） |
| 第 1 批 ⛔ `hold.switch` | **未修**：頁面**沒有任何訊號**分得出「抓取失敗」與「真的空」。須改 **L3** `src/services/dividend_station_service.py:781-785`（`get_switch_in_candidates`：`:781` 接住例外、`:783` 回 `[]`；`:785` 排名表空時**也**回 `[]`，且丟掉 `_note`）—— 行號已於 `msd-silent` 與 `origin/main` 兩邊 grep 核對一致。超出第 1 批「只改頁面」範圍 ⇒ **⏸ 待客戶授權改 L3** |
| QA 殘留 (a)（**僅登記**，既有、不在本批） | `find`：**唯一勾選的因子自己的輸入失敗**時（例：只勾估值且 PE 表失敗；或只勾缺貨／RS／跨季且該掃描失敗），卡顯示 0 檔＋灰色「**有效結果**」。頁面**已有** `aux_errors`（`page_find.py:803`；「不轉紅」設計見 `:220`）⇒ **可在頁面修**。⚠️ 現有測試 `tests/test_v2_silent_fail_b1.py:132` `test_b_other_aux_failures_still_leave_a_valid_zero` **把這個灰色結果鎖住**，修時須一併改。⚠️ 待確認：「唯一因子」情境為 QA 推論，本組只核對到 `aux_errors` 與該測試存在，**未實跑** |
| QA 殘留 (b)（**僅登記**，既有、不在本批） | `hold.take_profit`：held 個股列**沒有** `_detail.error` 但 `current_price` 為 `None` ⇒ `損益%` 為 `None`（`dividend_station_service.py:442-443`，須 `_avg and _cur` 皆有值）⇒ L3 略過，卡仍寫「**這是一個有效的結果**」；`why` 只怪「沒有均價」（`page_hold.py:2188-2192`），沒提現價缺失。⚠️ 待確認：未實跑 |
| 第 2 批證據 | `src/data/etf/etf_fetch.py::fetch_etf_dividends` `:293-296` 接住**所有**例外回空 `Series` ⇒ 「真的沒配息」與「抓不到」同形 |
| 第 2 批閘門 | **需改資料層** ⇒ ⛔ **動工前須先向客戶報告計畫**（資料層凍結，`CLAUDE.md` §-1.2）。**2026-09-26 客戶裁示：不變**，仍須先報告計畫、未動工 |
| 收尾 | 第 1、2 批修好後，對應 `(key, now)` 才可登記進 `src/ui/views/page_today.py::V2_VALID_EMPTY_SPEC`（`:2207`）取得 #11 |
| 僅登記、不排程 ① | `load_binding` 契約漂移落空（`src/ui/views/page_hold.py:1003-1018`）：若 L3 漂移，卡會落進 `UI_EMPTY`，#11 就會宣稱「有效的結果」。**現行 L3 不會產出此態** |
| 僅登記、不排程 ② | 註解寫 `V2_VALID_EMPTY_PAIRS`，實際符號是 `V2_VALID_EMPTY_SPEC`：`src/ui_v2/page_today.py:533`、`src/ui_v2/components.py:212` |
| 處置 | 依 §-1：~~**登記，未動工**~~ → **2026-09-26（客戶裁示）**：第 1 批已動工（2/3 修好，`hold.switch` 待 L3 授權）；QA 殘留 (a)(b) 與「僅登記 ①②」**登記、不動工**；第 2 批未動工 |

⚠️ 行號以 `origin/main` `66e4fe0` ＋ PR #689（`claude/v2-badge-vocab` `78d8744`）為準，已於 `/home/user/msd-badge` 逐一 grep 核對命中；⚠️ 待確認：「僅登記 ①」所述「L3 漂移時會落進 `UI_EMPTY`」與「現行 L3 不會產出」為 QA 推論，本組**只核對到行號與程式片段，未實跑驗證該路徑**。
⚠️ 上列發現來自**單組 QA、程式閱讀、未實跑**；**未經第二組驗**（§-2 規則 6），⛔ 不得當既定前提。
⚠️ **2026-09-26 新增列**（第 1 批結果、QA 殘留 (a)(b)）同樣來自**單組 QA**，**未經第二組驗**；行號已於 `/home/user/msd-silent`（未 commit 工作區）與 `origin/main` grep 核對，`dividend_station_service.py:781-785` 兩邊一致；`page_find.py`／`page_hold.py`／新測試檔行號**僅存在於未 commit 的工作區**，合併後可能漂移。

### 6.5 🔴 卡關點與待客戶裁示

> 📌 **現行待辦以文件開頭『未完成項目』為準**（2026-09-26）；本段保留為歷史紀錄。

**重抓歷史需要 FinMind token，本容器沒有**（見 §5「缺件」）。

📌 **2026-09-26 二組複核更正（下表一字未改）**：下表 (A)／(B) 針對的是**融資檔**的 2,975 列；**`origin/main` 那份已由 cron `a59ff63` 重建乾淨**（見文件開頭 c-4 B7a）⇒ **此二選一對 `main` 已失效**，⛔ 不要再拿去問客戶。它也**套不到 m1m2**（見 c-4 B7b：m1m2 壞在現行 cron 解析，不是缺 token）。

客戶尚未回答的二選一（**歷史紀錄**）：

| 選項 | 內容 | 性質 |
|---|---|---|
| **(A)** | 客戶提供 token → 用已修好的 cron 重抓歷史，真正修好 | 前一位總管的推薦 |
| **(B)** | 清掉不合 sanity 的 2,975 列 → 留 1,982 列乾淨資料 | ⚠️ **那 60% 歷史無法從程式重建**，且 parquet 受 git 追蹤 ⇒ 屬**不可逆刪資料**，落在 v3 §03-2 ② **必須客戶拍板** |

⛔ **沒有第三條路**：張數無法換算成金額（要靠當日價格，等於捏造，**違 §1 Fail Loud**）。

**若客戶要繼續做「趨勢燈」，三個後續方向**（皆未驗證，僅登記）：
(a′) 換分母（`margin_leverage_ratio` + 找總市值資料源 —— ⚠️ **本地無上市總市值資料源**，
`data_cache/` 只有 margin／inst／m1m2／pmi；TWII 是指數點位、經除權息調整、不含股本成長，
**拿來當分母＝捏造**）／(b) 改判**變化率／動能**而非水位分位／(c) **先修 §6.4 的資料污染**
（因為 (a′)(b) 都吃同一份 parquet）。⚠️ 前一位總管推薦 **(c) 優先**，該推薦**未經第二組驗**。

---

## 7. 客戶的工作模式

> **這一節解決的疑問**：客戶怎麼下指令？我該怎麼回？回多長？
>
> 📌 **2026-09-26 起，客戶常設規則彙整於文末「§9 常設規矩」**（較新；兩處不一致時以該節為準）；本表保留為明細。

| 項目 | 規則 |
|---|---|
| **指令形式** | 每輪發一張 **【本輪：…】工單**，含**範圍**、**⛔ 約束**、**回報格式** |
| **邊界紀律** | ⭐ **嚴格照工單邊界做，超出即停手回報。** 工單寫「只動 X」就是只動 X |
| **語言** | 回覆用**繁體中文** |
| **篇幅** | 每輪 ≤ **15 行**、**表格優先**、結尾**必加**（二選一，互斥、不得都不加）：<br>`⏸ 等待你回覆：【明確問題】` 或 `✅ 本輪完成，無需回覆。` |
| **例外** | 客戶**指名要求**的問答／查證／原理說明／計算式 **不受 15 行拘束**，客戶明示「**越詳細越好**」、含**完整數學式**（`CLAUDE.md` §-1.5.H H-2） |
| **揭露義務不因篇幅解除** | ⭐ 「不寫免責」禁的是**罐頭樣板**，⛔ **不是**禁具體揭露。真有未驗的承重宣稱 → **壓成一行塞進 15 行內，⛔ 不是刪掉**（§-1.5.H H-1） |
| **程式碼要求** | **防禦性設計**（API 延遲／空值處理）＋ **效能**（Pandas 向量化，避免逐列迴圈）；寫完跑 `CLAUDE.md` §6 自審清單 |
| **交付報告** | 三段式（v3 §04）：① 功能交付與架構摘要（**清理／刪除了哪些檔要逐檔列出**）② 品管試用與資料審計結果 ③ 成品操作指引 |

---

## 8. 誠實揭露（`CLAUDE.md` §-2 規則 6）

> **這一節解決的疑問**：本檔哪些話可以信、哪些話我必須自己重驗？

| # | 宣稱 | 誰驗的 | 強度 |
|---|---|---|---|
| 1 | §0 專案定位、7 個舊頁籤逐字、workflow 檔數（16／12 排程）、無 `README.md` | **總管親自實測**（讀 `app.py`、`ls .github/workflows/`、`grep -l "schedule:"`） | 🟢 實測事實 |
| 2 | §3.2 各頁卡面（`page_today` 20 處 `ui_v2` 引用／其餘四頁 0 處）、五頁**都**已用 `_ui_kit` | **總管親自實測**（逐檔 `grep -c`） | 🟢 實測事實 |
| 3 | §3.4 側欄 radio 的 key／標題／`index=0`／`st.stop()`／late-import | **總管親自實測**（讀 `app.py` 該區塊全文） | 🟢 實測事實 |
| 4 | **§4 全部分支數字**（三條線 HEAD、ahead/behind、PR #675 狀態、v2 code 進 `main` 的 commit 與日期、`main` 上缺哪些文件） | **總管親自實測**（`git rev-list`／`git ls-tree`／`git log --diff-filter=A`／GitHub API） | 🟢 實測事實 |
| 5 | §5 無 `gh`、`pytest.ini` 內容、兩條 lane 的 `continue-on-error`、無 token／無 secrets／無 FinMind SDK、194 道 assert | **總管親自實測** | 🟢 實測事實 |
| 6 | §6 全部數字（`macro_buckets` 命中 0、448 行、33 測試、parquet 1,982／2,975、cron 已接 schema、2026 分位分布） | **前一位總管親自實測**，本組**複驗了其中可就地驗的部分**（命中數、行數、測試數、cron import） | 🟢 實測事實 |
| 7 | §6.2「`DEFAULT_BIAS_MA_LEN` 有 production caller」＝ **更正前一版交接的「0 production caller」** | **本組實測**（逐符號 `grep`） | 🟢 實測事實（**且推翻了舊敘述**） |
| 8 | §3.3「`src/ui_v2/` 現在有 production caller」＝ **更正 PR #675 描述（寫於 2026-09-23）的「0 production caller」** | **本組實測** | 🟢 實測事實（**且推翻了舊敘述**） |
| 9 | **§2 文件地圖的逐份「定性」**（哪份該讀、哪份過期、哪份是歷史） | **單組盤點**（讀各檔檔頭 + 行數） | ⚠️ **未經第二組驗** —— 定性是**判斷**，不是事實 |
| 10 | **§3.3 `src/ui_v2/` 各檔職責** | **單組**（讀檔頭與 `page_today.py` 的註解轉述） | ⚠️ **未經第二組驗** |
| 11 | **`docs/v2/` 各子目錄檔數**（spec 28／audit 18／wireframe 18／stage1 4／prototype 5+`__pycache__`） | **單組** `ls <dir> \| wc -l` | ⚠️ 數字是實測，但**分類定性**（「audit 是軌跡不是規格」）未經第二組驗 |
| 12 | **「`main` 上有 `src/ui_v2/` 是否另有客戶裁示」** | **完全未查證** | 🔴 **未知** —— §4.2 只陳述矛盾，⛔ 不得推論 |
| 13 | §2 的 `N-1`~`N-3`、§6.5 的 A/B/C 可得性分級 | **原產出組的單組結論**，本檔僅**轉錄** | ⚠️ **未經第二組驗**，⛔ 不得當既定前提 |
| 14 | §4.5「其他分支是否已合併／可刪」 | ~~**未查證**~~ → 2026-09-26 **兩組獨立稽核**（patch 等價／檔案內容兩角度），結論一致；總管與編修組各抽查數項屬實 | ~~🔴 **未知**~~ → 🟢 查證完成（見第一節 c-5 **C6**／**C6-del**） |

### ⛔ 最後一句（最重要）

**⛔ 不得把本檔任何全稱句（「只有…」「全部…」「沒有其他地方…」）當成既定前提去做下一步決定。**
`CLAUDE.md` §-1.5.C 記載了一個真實前例：一句「牴觸只有這四處」的單組全稱句，
後來被獨立稽核組**另查出三處**而遭否證。**請據此打折信任本檔。**

**接手第一件事，請自己重跑一次 §4.1 的三行 `git` 指令** —— 那是本檔最會漂移、
也最影響你第一步該做什麼的一組數字。

---

## 9. 常設規矩

> **本節永遠是本檔最後一節**（檔首【給下一個 AI 的說明】第 2 點「讀『常設規矩』那一節（在最後）」）。彙整日 2026-09-26。
> **只彙整、不發明**：每條附出處（客戶頒布日／`CLAUDE.md` 節號／本檔節號）；出處只有「總管轉述」者照實標明。
> §7「客戶的工作模式」保留為明細；**兩處不一致時以本節為準**（本節較新）。
> ⚠️ 本節由單組彙整（§-2 規則 6），未經第二組驗；標「總管判讀」者客戶一句話可推翻。

### §1 回覆紀律

| 規則 | 內容 | 出處 |
|---|---|---|
| 語言 | **繁體中文** | 本檔 §7 |
| 第一行 | 「**HANDOFF：已更新（+N）**」 | 客戶 2026-09-26【交接表 · 維護時機】第 4 點（下方 §7 逐字） |
| 篇幅 | 每輪 **≤ 15 行**；算渲染後的行（含表格每列、清單每項），不含結尾標記（「怎麼數」為總管定義） | `CLAUDE.md` §-1.5.H 第 1 條、H-3 #1。⚠️ 待確認：若客戶其後另頒新行數，以最新為準（本檔未見更新） |
| 只講三件事 | 做了什麼、結果、卡在哪 | §-1.5.H 第 2 條 |
| 不重述已知 | 不重述已知、不列「我沒查到的」、不寫免責（背景／既有事實不重講） | §-1.5.H 第 3 條 |
| 揭露不因篇幅解除 | 禁的是**罐頭免責**，⛔ 不是具體揭露：承重的未驗宣稱**壓成一行「⚠️ 待確認：…」**，⛔ 不刪、⛔ 不展開 | §-1.5.H H-1 ③⑥；「⚠️ 待確認」一行格式為總管轉述 |
| 表格優先 | 表格優先於文字 | §-1.5.H 第 4 條 |
| 結尾標記 | 需要客戶回覆 → `⏸ 等待你回覆：【明確問題】`（上下各一條 `━` 線）；不需要 → `✅ 本輪完成，無需回覆。`；**互斥、恰一個** | §-1.5.H 第 5、6 條、H-3 #2 |
| 最後一行 | `📓 交接本：已更新 <哪一節> ／ 無變動` | 客戶 2026-09-26【給下一個 AI 的說明】 |
| 首尾順序 | 首行「HANDOFF：已更新（+N）」、末行「📓 交接本：…」**兩條都適用**；⏸／✅ 標記因此落在 📓 行之前 | 總管判讀（2026-09-26）。⚠️ 待確認：首行與 📓 行是否計入 15 行，客戶未指定 |
| 例外 | 客戶**指名要求**的問答／查證／原理說明／計算式不受 15 行拘束（「只講重點、不重述、表格優先」仍適用） | §-1.5.H H-2（總管判讀） |

### §2 什麼可以問客戶

| 規則 | 內容 | 出處 |
|---|---|---|
| **現行（2026-09-26 起）** | **只有三類可問：動錢／動凍結檔／開新頁。其餘自決，寫進交接本。** | 客戶 2026-09-26【給下一個 AI 的說明】 |
| 被取代的舊門檻 | 先前的「停手五類」—— **版面異動／不可逆／動 L3 或資料層／需新源／規格有洞** —— **2026-09-26 起不再是「要問客戶」的門檻**（五類清單出處為總管轉述客戶先前指令，本檔與 `CLAUDE.md` 無逐字原文） | ⚠️ 總管判讀（2026-09-26） |
| 五類的新用途 | 五類改為**登記用**：撞到 → 立刻在第一節寫「卡住＋卡在哪」，並計入「給下一個 AI」第 4 點；**五類照登記，只有三類拿去問，兩令並存** | ⚠️ 總管判讀（2026-09-26，見下方 §7 判讀註） |
| 文案／規格缺口、版面異動 | 2026-09-26 起**內部決定**，決定內容與理由**必須寫進本檔** | ⚠️ 總管判讀（新令「其餘自決，寫進交接本」的推論） |
| 請示格式（仍有效） | 附**總管推薦方案＋具體原因**，⛔ 不拋選擇題；問題須一句話可答 | `CLAUDE.md` §-1.5.D v3 §03-2、§-1.5.H H-3 #3 |
| ⚠️ 待確認（衝突，未裁） | (i) `CLAUDE.md` §-1.5.D v3 §03-2 ① 規定版面／欄位增減／分頁動線異動**須先出線框請示**，與「版面異動內部決定」字面衝突，`CLAUDE.md` 尚未同步；(ii) **K1**（本表 §4：AI 不得自行發明使用者文案）與「文案缺口內部決定」衝突 —— 內部決定的文案是否仍須有規格出處，未定；(iii)「動凍結檔」射程未定義（`CLAUDE.md` §-1.2 的資料庫／Schema／遷移檔／資料源？是否含新增資料源、`data_cache/` 凍結快照？） | 編修組登記 |

### §3 資料與凍結

| 規則 | 內容 | 出處 |
|---|---|---|
| 資料層凍結 | 資料庫、Schema、遷移檔不動；v2 可建自己的 view／元件／頁面，但**不動資料源**；⛔ 例外只限客戶**逐任務明示授權**（前例：靜默失敗批 3 特許只動 L1 `fetch_etf_dividends`） | `CLAUDE.md` §-1.2；本檔 (c) S3 |
| 凍結快照 | ⛔ 不刪不改 `data_cache/forward_test/picks.parquet`（無法重建的前進式驗證樣本）、`data_cache/macro_forward_test/signals.parquet`（唯讀） | `CLAUDE.md` §-1.5.F 判定 3(5)；本檔 B3 子表 `health` 列 |
| Fail Loud | ⛔ `fillna(0)`／沉默 `ffill`／dummy 資料／`except: pass`／自己估一個合理值；錯的數字比沒有數字更危險 | `CLAUDE.md` §1 |
| 不碰的檔 | ⛔ `docs/v2/prototype/lamp_preview.html`：不改、不 stage、回覆中不提 | 總管轉述（客戶令） |

### §4 文案與標記

| 規則 | 內容 | 出處 |
|---|---|---|
| K1 | 使用者看得到的文案，⛔ AI 不得自行發明（＝捏造，違 §1）；規格沒給的中文標籤不自擬（與 §2 新令的衝突見 §2 ⚠️ 待確認） | 客戶 2026-09-25（本檔 §6.4 K1） |
| 徽章 11 種 | `docs/v2/spec/UI_COMPONENTS.md` §2 徽章 11 種；**#11「▨ 無資料」只給「真的跑了、結果真的是 0／空」**，且同一對 `(key, now)` ⛔ 不得蓋著任何缺漏／上游失敗被吞／未載入／未評估／未綁定路徑；登記制，登記處 `src/ui/views/page_today.py::V2_VALID_EMPTY_SPEC` | 客戶 2026-09-26 規格變更（#689）；本檔 B6 子表 |
| 用詞 | ⛔ 用「大致完成」 | 客戶 2026-09-26【交接表 · 維護時機】禁止 |

### §5 派工與驗證

| 規則 | 內容 | 出處 |
|---|---|---|
| 派工 | 總管不寫實作；每件事都派 subagent；調查／稽核至少多組、不同角度，⛔ 自己查自己 | `CLAUDE.md` §-2 規則 1、2、4、5 |
| 複驗 | subagent 回報 ≠ 完成；總管自己查證關鍵宣稱（尤其全稱句） | `CLAUDE.md` §-2 規則 3 |
| 單組揭露 | 未經第二組驗的全稱句：要嘛派一組獨立驗，要嘛明說「沒有第二組驗過」（本檔標 ⚠️單組），且 ⛔ 不得當後續動作的前提、⛔ 不得寫進 commit／PR 描述當事實 | `CLAUDE.md` §-2 規則 6 |
| 動工閘門 | 沒有客戶指派、沒有實際 bug 觸發 → 停手等指令 | `CLAUDE.md` §-1 |

### §6 commit／push／merge

| 規則 | 內容 | 出處 |
|---|---|---|
| 流程 | 實作 → **獨立 QA** → 開 PR → **CI 綠**（fast lane `pytest -v` 擋 merge；slow lane `continue-on-error`，要自己看 job log）→ merge（merge 動作本身為常設授權） | `PROCESS.md` §4「PR 規範」；本檔 §5；`CLAUDE.md` §-1.5.A-6 |
| merge 後 | **立刻**在第一節寫一行（含 hash），⛔ 不累積 | 客戶 2026-09-26【交接表 · 維護時機】第 2 點 |
| 原型 | 改 `docs/v2/prototype/` 任何一份前後都手動跑 `python docs/v2/prototype/gen_today_v2.py`，⛔ 只看 CI 綠 | `CLAUDE.md` §8.2.A.5；本檔 §5 |
| PR #675 | ⛔ merge／轉 ready／改 base（含 rebase）；⛔ 自行把開發線併進 `v2` 或 `main`；分支政策須請示客戶 | 本檔 §4.3 |

### §7 交接本維護

**更新觸發點**（逐字引自檔首【給下一個 AI 的說明】，客戶 2026-09-26）：

① 派工出去時 → 寫「已派工 X，等回報」
② 收到回報時 → 「派工中」改「稽核中」
③ commit 前 → 該項移到「已完成」
④ 每則回覆停點前 → 檢查一輪
⑤ 收到我新指令時 → 先讀交接本，再動作

**【交接表 · 維護時機】**（客戶 2026-09-26 頒布，逐字收錄）：

【交接表 · 維護時機】（長期生效）
■ 唯一登記本：HANDOFF 第一節
■ 四個維護時機（缺一即斷）
1. 開工前 → 先讀第一節，自己挑下一項
2. merge 完 → 立刻寫一行（含 hash），⛔ 不累積
3. 撞五類 → 立刻寫「卡住＋卡在哪」
4. 每輪回報第一行 → 「HANDOFF：已更新（+N）」
■ 每 5 項自檢
拿 git log 對 HANDOFF，補漏，⛔ 不靠記憶
■ 交接段（新 AI 專用，固定格式）
第一節末尾必附「給下一個 AI」：
1. 從哪項接手（一項）
2. 那項做到哪、下一步是什麼
3. 哪些檔已動、哪些⛔ 不准動
4. 待你裁的五類各幾項
5. 開場該讀哪些檔
■ 禁止
⛔ 收工前一次補寫
⛔ 只寫結果不寫 hash
⛔ 用「大致完成」

> ⚠️ **總管判讀（2026-09-26）**：上文「**五類**」＝ **版面異動／不可逆／動 L3 或資料層／需新源／規格有洞** —— 用於在第一節登記「卡住」與「給下一個 AI」第 4 點的計數；**向客戶提問**則依同日另一令，**只限三類（動錢／動凍結檔／開新頁）**。**兩令並存：五類照登記，只有三類拿去問。** 另：每則回覆的**第一行**「HANDOFF：已更新（+N）」與**最後一行**「📓 交接本：…」**兩條都適用**。客戶一句話可推翻。
> 📌「HANDOFF 第一節」＝本檔「★ 進度總表／未完成項目」一節（檔首【給下一個 AI 的說明】是導讀，不是登記本）—— ⚠️ 總管判讀。
