# T1 / T2 架構規劃（§8.1 + §7 對齊文件）

> **本文件禁止附帶 code。** 依 `CLAUDE.md` §8.1：「動工前先提交架構規劃（文字 + 簡單流程圖），
> 這一步禁止寫 code」；§7：「計算式先用**數學式**寫給我確認，再寫程式」。
> 核准後才進 Execute，且一次只改一個模組（§8.5）。
>
> 目標：把使用者流程圖的層次 1「**影響全系統風控係數**」與層次 2「**已持有資產組合分析**」接通。
> 建立日：2026-08-14

---

## §0 先講結論：缺的是「接線」不是「功能」

兩組並行探查（各附 `檔案:行號` 證據）的共同結論：

| 流程圖節點 | 原料 | 接線 |
|---|---|---|
| 總經 → 風控係數 | ✅ 齊 | ⚠️ **只兌現 2/12** |
| 選股引擎（基本面） | ✅ 齊（14 條財報門檻） | ❌ **零 regime** |
| 產業集中度 | ✅ 資料源早就在 | ❌ **從沒用過** |
| ETF↔個股穿透 | ⚠️ 缺 name→代號 對照 | ❌ 無 |
| LINE 推播 | ✅ **已上線 2 條** | ✅ 可直接沿用 |

所以 T1/T2 全是**接線工程**，不新建任何資料源。

---

## §1 T1：接通「總經 → 全系統風控係數」

### 1.1 單一職責（一句話）

> 讓總經 regime 從「印在卡片上的字」變成「真的改變下游門檻的參數」。

### 1.2 現況：那支箭頭斷在哪

`get_macro_regime()`（`src/services/allocation_service.py:297`）產出 canonical 五態
`{bull, neutral, caution, bear, unknown}`，共 **12+ 個消費點**，但：

**真的改變數值的只有 2 處**

| # | 位置 | 影響 |
|---|---|---|
| 1 | `stock_grp_sections/section_batch_fetcher.py:86,251-255` | regime → `config.WEIGHT_TABLES` 六因子權重（實測：趨勢因子 bull 0.30 vs bear 0.15；風險因子 bear 0.25 vs bull 0.05）→ **排名會換人** |
| 2 | `shared/allocation_decision.py:336` | regime → ETF 股/債/現金三桶比例 |

**其餘 10+ 處全是顯示層** —— `macro_stock_link.py:35,54`、`section_warroom.py:97`、
`section_traffic_light.py:53`、`section_cross_ai.py:163`、`section_chips.py:522,578`、
`app.py` 置底條 `:942` 等，都只是把同一個數字重複印在不同卡片上。

**最關鍵的斷點**：`src/services/fundamental_screener_service.py` 全檔 grep `regime` = **0 命中**。
⇒ 總經說「空頭防禦」時，選股網照樣用同一組門檻、同一套百分位，選出**同一批股票**。

### 1.3 提案：兩個接點（各自獨立可回退）

#### T1-a　選股網：空頭時要求「贏過大盤」

**不新增任何門檻常數**，改用既有參數。

現況（`fundamental_screener_service.py:490`）呼叫 RS 篩選時傳 `beat_only=False`，
且 `shared/rs_screen_thresholds.py:48` 的 `RS_RANKABLE_TIERS` **包含 `TIER_LAG`（落後大盤）**
⇒ 相對強度目前**不具否決權**，只是一個百分位分數。

**改法**：`beat_only` 由 regime 決定。

```
beat_only = regime ∈ {bear, caution}
```

| regime | beat_only | 語意 |
|---|---|---|
| `bull` / `neutral` | `False` | 維持現況：落後股仍可入選，靠基本面翻身 |
| `caution` / `bear` | `True` | 空頭只買相對強勢股 —— 弱勢股在空頭跌更兇 |
| `unknown` | **`False` + 畫面標註「總經未評估，未套用空頭濾網」** | §1：不可假裝套用了 |

**為什麼這個改法最好**：
- 零新常數（不製造新的 magic number，§3.3）
- 單一參數、語意清楚、可一行回退
- 沿用專案既有的 regime→參數 範式（`resolve_scoring_regime` 已是同一手法）
- 金融邏輯正確：空頭中弱勢股的下檔風險顯著高於強勢股（動能效應在空頭更強）

**⚠️ 已知副作用**：空頭時入選檔數會變少，可能出現「0 檔符合」。
這是**正確**行為（空頭本來就該少買），但畫面必須誠實說明原因，不可顯示空表了事。

#### T1-b　個股組合：把「總經建議持股水位」接進組合頁

**這一項我要先反對 agent 的建議。**

探查 agent 建議「讓 `check_portfolio_limits()` 的集中度守門隨 regime 收緊」。
**我認為這在金融邏輯上是錯的**，理由：

> 集中度風險（單一標的佔比過高）源自**投組結構**，與市場多空**無關**。
> 一檔股票佔你 30% 部位，在多頭和空頭都同樣是 30% 的個股風險。
> 空頭該做的是**降低總曝險**（現金比例↑），不是改變「單股不超過 10%」這條結構規則。
> 把兩件事混在一起，會讓使用者以為多頭時可以放心重壓單一標的。

**改提**：個股組合頁顯示「**你的持股檔數 vs 總經建議持股水位**」的對照。

現況 `get_allocation()`（`shared/allocation_decision.py:163-165`）已產出
`final_lo / final_hi / final_mid` 建議持股區間，但**個股組合頁完全沒用到**
（`tab_stock_grp.py:477` 的 `check_portfolio_limits(_weights)` 只吃權重）。

```
建議：畫面新增一列（純顯示，不改任何判定）
  「總經建議持股 {final_lo}–{final_hi}%　你目前 {N} 檔（等權假設約 {N × 單檔上限}%）」
```

⚠️ 這裡有個誠實性限制，見 §2.4：**個股組合沒有權重資料**，所以「你目前 X%」
其實算不出來，只能講檔數。硬要換算成百分比就是捏造（§1）。

**所以 T1-b 我建議先只做「並列顯示 + 明確標註無法換算」**，等使用者決定要不要補權重資料
再談真正的水位對帳。這符合 §8.1 step 6「用不到的抽象標『先不做』」。

### 1.4 資料流與依賴方向

```
L3 allocation_service.get_macro_regime()          [既有]
        │
        ├─► L3 fundamental_screener_service       [T1-a：新接線]
        │        └─► beat_only 參數 ─► L2 rs_leader_screener   [既有純函式，不改]
        │
        └─► L5 tab_stock_grp                      [T1-b：新接線，純顯示]
```

**依賴方向檢查**：L3→L3、L3→L2、L5→L3 —— 全部合規，**零跨層上行**，
不需要新增任何 §8.2.A 例外。

### 1.5 失敗降級

| 情境 | 行為 |
|---|---|
| `regime == 'unknown'`（總經未載入） | `beat_only=False`（維持現況），畫面**明確標註**「總經未評估 → 未套用空頭濾網」。§1：不可靜默當作 bull，也不可假裝有套用 |
| `get_macro_regime()` 拋例外 | 往上拋，由 `_render_tab_isolated` 接住並顯示該區塊錯誤（Batch A 已補齊隔離器） |

### 1.6 自評過度設計

- ❌ **不做**「風控係數 k」抽象層（把 regime 映射成一個 0–1 乘數再乘到各處）——
  當前只有 2 個接點，抽象層是 §8.1 step 6 的典型反例。等接點 ≥5 個再談。
- ❌ **不做** regime → 財報硬門檻（負債比、三率三升）的調整 —— 基本面體質不該隨市場情緒浮動。
- ❌ **不做** T1-b 的權重換算 —— 沒資料，硬做就是捏造。

---

## §2 T2：產業集中度

### 2.1 單一職責（一句話）

> 回答一個問題：「我這籃股票，是不是全押在同一個產業？」

### 2.2 §7 第 1 點｜資料來源與單位

| 項目 | 內容 |
|---|---|
| **函式** | `fetch_industry_map_bulk()`（`src/data/stock/market_close_fetcher.py:240`） |
| **主源** | FinMind |
| **備援** | TWSE `t187ap03_L`（上市）／ TPEX `t187ap03_O`（上櫃） |
| **回傳** | `dict[str, str]`：股票代號 → 產業別名稱 |
| **單位** | 無（分類字串，非數值） |
| **既有第二來源** | `src/data/sector_flow/reader.py:28` `ticker_sector.json` |

⚠️ **須先確認的事**：兩個來源的產業分類**是否為同一套分類法**。
若不同（例如一個是 TWSE 33 類、另一個是 GICS 11 類），混用會讓集中度算錯。
**動工第一步就是驗證這一點**，不一致則明定其中一個為 SSOT。

### 2.3 §7 第 2 點｜發布延遲與回溯修正

產業別屬**靜態屬性**，非時間序列：
- 發布延遲：不適用
- 回溯修正：極低頻（公司變更產業別，一年數次）
- PIT 對齊：**不適用**（本功能只看當下持股的當下分類，無歷史回測）

⇒ §2.3 的 lookahead 風險在此項不存在。

### 2.4 §7 第 3 點｜邊界條件（含一個硬限制）

#### 🔴 硬限制：個股組合**沒有權重資料**

`gsheet_portfolio.py:60` 的 `stock_watchlist` schema 是 `['name', 'ticker', 'updated_at']`，
`:56-58` 註解明寫「§1 反捏造：此分頁**只**存 name/ticker/updated_at 三欄，無張數/均價」。

⇒ **算集中度需要權重 w_i，但我們沒有。** 只有兩條路：

| 選項 | 作法 | 誠實性 |
|---|---|---|
| **A（建議）** | 明示「**等權假設**」：每檔權重 = 1/N | ✅ 合憲，但畫面**必須**標註「假設等權持有；實際依你的部位大小會不同」 |
| B | 要求使用者補張數 | 需改 schema，屬 T3 範疇 |

**採 A**，且標註文字不可省 —— 省了就是讓使用者以為那是他的真實集中度（§1）。

#### 其他邊界

| 情境 | 處理 |
|---|---|
| 組合只有 1 檔 | HHI = 1.0（完全集中）。數學正確但無資訊量 → **N < 3 時不顯示**，改印「檔數過少，集中度無參考價值」 |
| 某檔查不到產業別 | **不可** 丟進「其他」桶（會低估集中度）。列為「未分類 N 檔」並在標題標註「本次有 N 檔無產業資料，集中度可能低估」 |
| 全部查不到 | Fail loud：印診斷（FinMind quota？TWSE 無回應？），**不顯示任何數字** |
| 組合為空 | 不渲染本區塊 |
| 同一產業名稱有全形/半形/空白差異 | 正規化後再分組，否則「半導體」與「半導體 」會被當兩類 |

### 2.5 §7 第 4 點｜計算式（**這段最需要你確認**）

設組合有 N 檔股票，分屬 K 個產業。等權假設下每檔權重 1/N。

**產業 i 的權重**（n_i = 產業 i 的檔數）：

$$w_i = \frac{n_i}{N}, \qquad \sum_{i=1}^{K} w_i = 1$$

**指標 1：前 N 大產業佔比**（直觀，建議當主指標）

$$\text{Top1} = \max_i w_i, \qquad \text{Top3} = \sum_{i \in \text{top-3}} w_i$$

**指標 2：HHI（Herfindahl–Hirschman Index）**

$$HHI = \sum_{i=1}^{K} w_i^2$$

值域：$\frac{1}{K} \le HHI \le 1$。完全分散（K 類等分）時 $HHI = 1/K$；全押一類時 $HHI = 1$。

**指標 3：有效產業數**（比 HHI 好解讀，建議一併顯示）

$$N_{\text{eff}} = \frac{1}{HHI}$$

語意：「你的組合實質上等於分散在 $N_{\text{eff}}$ 個產業」。
例：10 檔分 5 產業但 6 檔集中在半導體 → $HHI = 0.6^2 + 4×0.1^2 = 0.40$ → $N_{\text{eff}} = 2.5$，
即「名義 5 個產業，實質只有 2.5 個」。這比「HHI = 0.40」好懂太多。

#### ⚠️ 門檻要你裁示（§3.3：不可腦補常數）

美國 DOJ 的 HHI 標準（<1500 分散 / 1500–2500 中度 / >2500 高度集中，×10000 尺度）
是**衡量產業市場結構**用的，**不是投資組合分散度標準**，直接套用沒有依據。

我的建議：**不用 HHI 絕對門檻，改用「有效產業數」的直觀規則**，因為它可解釋：

| $N_{\text{eff}}$ | 燈號 | 說法 |
|---|---|---|
| ≥ 4 | 🟢 | 實質分散在 4 個以上產業 |
| 2 – 4 | 🟡 | 實質集中在 2–4 個產業，單一產業利空會明顯影響 |
| < 2 | 🔴 | 實質等於押注單一產業 |

這三個數字**仍然是我提的**，需要你確認或改。確認後我會寫進
`shared/concentration_thresholds.py`（新的 L0 SSOT），附 docstring 說明來源是「user 裁示」
而非憑空 —— 這正是 §3.3 要求的。

**或者**你也可以選擇「先只顯示數字與排行，不給燈號」—— 那就完全不需要門檻常數，
等你用一陣子、對自己的組合有感覺後再定。**這個選項我其實更推薦。**

### 2.6 模組切分與資料流

```
L1  market_close_fetcher.fetch_industry_map_bulk()      [既有，不改]
        │  dict[代號 → 產業別]
        ▼
L2  compute/risk/concentration.py                       [新檔，純函式]
        │  · normalize_industry_name()
        │  · compute_industry_concentration(tickers, industry_map)
        │      → {weights, top1, top3, hhi, n_eff, unclassified}
        ▼
L3  services/stock_grp_service.py                       [既有，加一個編排函式]
        ▼
L5  tab_stock_grp / stock_grp_sections/section_*.py     [新 section，純顯示]
```

**依賴方向**：L1→L2→L3→L5，全部下行，合規。
新檔 `src/compute/risk/concentration.py` 是**純函式、零 I/O**，符合 §8.2「L2 不得 import requests」。

### 2.7 失敗降級

| 情境 | 行為 |
|---|---|
| `fetch_industry_map_bulk()` 失敗 | Fail loud：印診斷卡（含 FINMIND_TOKEN 檢查，比照 `section_chips.py:134-144` 既有範式），**不顯示任何集中度數字** |
| 部分代號查無產業 | 顯示集中度，但**標題明確標註**「N 檔無產業資料，數值可能低估」 |

### 2.8 自評過度設計

- ❌ **不做** ETF↔個股穿透曝險 —— 有真實技術障礙：`fetch_etf_holdings`（`etf_fetch.py:687-689`）
  回傳的 key 是**公司名稱**不是股票代號，`portfolio_gates.py:508` 自承「中文股名對不上英文公司名，
  這是『沒比到』不是『不重疊』」。要做得先建 name→代號 對照表 = 獨立一批工作。
- ❌ **不做** 風格因子曝險（價值/成長/動能）—— 當前需求沒提到。
- ❌ **不做** 產業歷史趨勢 —— 沒有歷史持股快照可比。

---

## §3 測試計畫

| 類型 | 內容 |
|---|---|
| 單元 | `compute_industry_concentration` 的 HHI / N_eff 數學正確性（含手算對照） |
| 邊界 | 空組合 / 1 檔 / 全部同產業 / 全部不同產業 / 部分未分類 / 全部未分類 |
| Property-based | 對任意組合恆有 $1/K \le HHI \le 1$ 且 $\sum w_i = 1$（浮點用 `math.isclose`，§4.3 禁止 `==`） |
| 守衛 | `beat_only` 必須由 regime 決定，不得寫死（AST 掃描，比照 `test_s1_zombie_loaded_state.py` 的做法） |
| 分層 | 新檔 `concentration.py` 不得 import requests / streamlit（`test_c3_layering_guard.py` 自動涵蓋） |

### 三個最容易讓這段程式出錯的輸入（§6 要求）

1. **全部 N 檔屬同一產業** → $HHI = 1$、$N_{\text{eff}} = 1$。若程式用 `1/hhi` 而 hhi 有浮點誤差變成 0，會除零。**必須 guard**（§4.4「大數除以小數」）。
2. **產業名稱含尾隨空白 / 全形字元**（TWSE 與 FinMind 極可能不一致）→ 同一產業被拆成兩類 → 集中度**被低估**，方向是危險的那一邊。
3. **N = 0（空組合）** → $\sum w_i$ 為 0，所有除法爆炸。必須在入口擋掉並回明確的「無資料」狀態，而非回 `HHI = 0`（那會顯示成「完美分散」，是最糟的假訊號）。

---

## §4 交付順序（§8.5：一次一個模組）

| 序 | 模組 | 為什麼這個順序 |
|---|---|---|
| 1 | 驗證兩個產業資料源是否同一套分類法 | 這是 T2 的地基，不對就全錯 |
| 2 | L2 `concentration.py` 純函式 + 測試 | 零 I/O，可完整單測 |
| 3 | L3 編排 + L5 顯示 | 接線 |
| 4 | T1-a `beat_only` 接線 + 守衛測試 | 獨立於 T2，可平行 review |
| 5 | T1-b 並列顯示 | 最小，純顯示 |

每完成一個跑 §6 自審，貼 diff + 說明為何不破壞既有行為。

---

## §5 需要你裁示的三件事

1. **T2 的燈號門檻**：用我提的 $N_{\text{eff}}$ ≥4 / 2–4 / <2 三段？還是**先只顯示數字不給燈號**（我更推薦）？
2. **T1-b**：同意「只做並列顯示 + 標註無法換算權重」，還是你想直接補張數資料（那會變成 T3 範疇）？
3. **我否決了 agent 的「集中度守門隨 regime 收緊」** —— 理由見 §1.3。你同意嗎？若你認為該做，請說明你想達成的效果，我再找正確的實作方式。
