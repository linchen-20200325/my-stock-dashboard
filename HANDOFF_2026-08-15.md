# 進度交接 — 2026-08-15

> 本檔為 token 用盡前的狀態快照。下次接手先讀這份，再讀 `STATE.md` v19.193~195 三段。

---

## 🚨 最重要：所有改動**尚未 push**

本機 `D:\01.Github\20260813\股票` 有六個批次的改動，**5856 passed，一行都沒上傳**。

```powershell
cd "D:\01.Github\20260813\股票"
pytest -q                    # 應為 5856 passed
git status --short

# 稽核文件歸位（可選）
mkdir docs\audit-2026-08 -Force
git mv REFACTOR_PROPOSAL.md docs\audit-2026-08\ 2>$null
git mv REFACTOR_PROPOSAL.html docs\audit-2026-08\ 2>$null
git mv ALL_ISSUES.md docs\audit-2026-08\ 2>$null
git mv LIVE_VERIFY_AND_DECLUTTER.md docs\audit-2026-08\ 2>$null

git checkout -b feat/flowchart-alignment-v19.195
git add -A
git commit -m "flowchart alignment: S1 + A-2 + T1/T2/T3/T4 (v19.195, 5856 passed)"
git push -u origin feat/flowchart-alignment-v19.195
```

> `PLAN_T1_T2.md` / `PLAN_T3.md` / `PLAN_T4.md` / `BATCH_A_DELIVERY.md` **留在根目錄**
> —— 它們記錄了四次「查證推翻提案」的過程。

**要在 Streamlit Cloud 看**：Settings → Branch 暫時指向該分支（免先 merge）。
⚠️ 看不到新東西先 **Reboot app** —— 你自己記過「Cloud 從睡眠喚醒不保證跑最新 commit」。

---

## 已完成（六批，全綠未推）

| 批次 | 內容 | 測試 |
|---|---|---|
| Batch A | 16 項零風險修正（AUM 幣別 / 診斷 gate / 隔離器 / TTL SSOT / 誠實性文案） | — |
| S1 | 殭屍已載入態（`?chips=1` 跨 session 還原移除） | +4 |
| A-2 | 死碼清理 + 龍頭 OR→AND + SOP 第②關職責分離 | — |
| T1 | 選股網空頭濾網（regime 接線）+ 總經水位對照 | +26 |
| T2 | 產業集中度（L2 純函式 + L3 + L5） | +83 |
| T3 | 觀察池訊號推播（判定/組字/cron/workflow/文件） | +61 |
| T4 | 紅K錨點 L2 收斂 + 總表三欄 + VCP布林下鑽 | +40 |

**部署後要確認五處**：個股組合的 📡總經水位對照 / 🏭產業集中度 / 總表三新欄（絕對停損·停損RR·距支撐）/ 型態下鑽底下的 VCP+布林 / 總經頁切走再切回不再空殼。

**還要設一個 secret**：`APP_BASE_URL` = `https://my-stock-dashboard-namd8rh9b8sn3qzdjshzfa.streamlit.app/`
（T3 深連結用；CSV 與 LINE 憑證沿用既有 `WEEKLY_WATCH_CSV_URL` / `LINE_*`）

---

## 🔍 大掃除稽核結果（2026-08-15，**尚未修任何一項**）

user 實機發現總經頁判讀前後矛盾，已完成兩路稽核。**以下全部是診斷，未動程式碼。**

### A. 六處判讀矛盾（總經頁）

**跨案共通根因**：紅綠燈卡（`calc_traffic_light` → `arbitrate_regime`）、五桶（`compute_bucket_dangers`）、建議持股（`build_allocation_decision`）是**三套獨立引擎**，只在 health 一個數字上有交集，三者輸出並排在同一畫面卻**沒有任何一致性約束**。①③④⑥ 全是這個結構的症狀。

| # | 問題 | 性質 | 嚴重度 | 關鍵證據 |
|---|---|---|---|---|
| ① | 信心 100% 但新聞未掃描、外資現貨無值 | 顯示/口徑 | 中 | `macro_helpers.py:365-386` 分母只含燈號卡自己 5 個輸入；`CONFIDENCE_SOURCE_COUNT=5`（`signal_thresholds.py:110`）docstring 已失真；`_fk` 判的是 **key 存不存在**（`:232`）非有無值 |
| ② | 頂卡「外資買超 453.5億」vs 明細「外資現貨—」 | **判定（SSOT 破口）** | **高** | 同一個量兩條路：`market_assessment_apply.py:56-66` 照算；`macro_helpers.py:1775-1780` 寫死 `"foreign_net": None`（註「單位待確認」） |
| ③ | 「積極操作」vs「建議持股 20%」 | **判定** | **高** | 健康 62 → `THROTTLE_TIERS` 應 50-70%（`position_throttle.py:25-30`）；被三環第一環壓到 20%（`allocation_service.py:148,199-207`，外資期貨 −85363 ≤ −15000）。**同一筆期貨數在文案端不生效** —— `regime_arbiter.py:176-178` 要求 score<2 且 >30000 口，實際 score 4.5 → 兩套互不知情的門檻 |
| ④ | 「多頭市場」vs 四桶全紅 | **判定（缺規則）** | **高** | 頂卡只吃 3 輸入、五桶吃 16 個 worst-of；`macro_buckets.py:162-167` 自承 health 公式「真正餵進的只有 jingqi 一項，其餘 14 項全不含」。**零連動規則**。且解釋句與同卡徽章自相矛盾（徽章已掛 ❌廣度偏弱、🔴融資極危） |
| ⑤ | 廣度 44.7%：頂卡 ❌ vs 明細 🟡 | 顯示 | 低 | 同一 SSOT（`MARKET_BREADTH_NEUTRAL_PCT=50`），但 `market_strategy.py:116-121` 是**二分法**、`macro_buckets.py:261-266` 是**三分法**。頂卡的 ❌ 實際等價於桶的 🟡 |
| ⑥ | 融資自承鑑別力歸零卻仍決定桶色 | **判定** | **高** | `macro_buckets.py:281-290` 註解明寫「絕對門檻已被市值成長淹沒…燈號恆紅、鑑別力歸零」；仍以 worst-of 計入（`macro_helpers.py:1793-1805`）。`DangerSpec` **無 `advisory_only` 欄位**（全檔 grep 0 命中）。且它超標幅度最大 → 幾乎必然搶走籌碼桶 headline |

**修法風險提醒**：
- ① 若真把新聞/外資納入分母 → conf 常態跌破 `handlers.py:77` 的 70% 擋燈門檻，**燈號會整片消失**
- ③ 統一兩套期貨門檻＝行為變更，須重跑校準
- ④ 真降 regime 會連鎖影響個股/組合/ETF 全站（`allocation_service.py:224-231` 來源鏈）
- ⑥ 加 `advisory_only` 後籌碼桶 🔴→🟡，屬行為變更需核准；低風險替代是接 `shared/relative_thresholds`（該檔已存在**未接線**）

### B. 效能：最值得先修的三項

**放大器**：`app.py:436` 六個主 tab + `:495/518/768/835` sub-tab，**st.tabs eager render → 所有 body 每次 rerun 全跑**。以下每項「無 gate 的外部請求」，代價是**每按一次任何控制項都付一次**。

| 順位 | 問題 | 證據 | 估計 | 風險 |
|---|---|---|---|---|
| 🔴 1 | **gsheet 全檔零快取 × 兩個 eager tab** | `gsheet_portfolio.py` 整檔無 `@st.cache_data`（`:229-232` 自陳「每次重新建立」）；`tab_sector_flow.py:47` 無 gate；`sector_flow_service.py:48-49` N+1 序列；`portfolio_manager.py:132,200` 同樣無 gate | 每次 rerun 多 `4+2N` 趟 Google API ≈ **1.5–4 秒** | 低（加 TTL_1HOUR + 存檔時 `.clear()`） |
| 🔴 2 | **ETF 3-3-3/標準差帶 default `0050` 自動抓** | `etf_tab_smart.py:84` `default='0050'` → 恆非空 → `:504-508`/`:134-137` 自動計算；`app.py:820-826` 無 gate | TTL 過期後任一互動付 yfinance 5y+2y ≈ **3–8 秒** | **最低**（改 `default=''`，空標的分支 `:491-494` 早就寫好） |
| 🟠 3 | **產業集中度 N 檔序列 FinMind** | `tab_stock_grp.py:134` 無 gate；`stock_grp_service.py:97-102` 序列迴圈；預設清單 10 檔 | 冷啟動 **3–15 秒** | A: 改 `fetch_industry_map_bulk` 整表（N→1 趟，需 L3 加快取）中風險／B: ThreadPool 低風險／C: 改 button opt-in 最快 |
| 🟡 4 | `load_macro_state` 每 rerun 讀磁碟約 18 次 | `macro_state_locker.py:252-254` 無快取；`allocation_service.py:255` 在快取簽章比對**之前**呼叫 | 本機 10–30ms；**Cloud overlay fs 可放大到 100–400ms** | 極低（一行裝飾器） |

**其他發現**：
- `tab_stock_grp.py:636-639` 快取 key 含 slider 權重 → **調權重觸發 N 檔全量重抓**（權重只影響最後合議，與抓取無關）
- Q5 cache key 失效 **未找到違例**（`shortage_screener_service.py:197` / `rs_leader_service.py:281` 是做對的示範）
- 冷啟動：`data_loader.py:7` / `etf_fetch.py:31` 的 module-level `import yfinance` 可改 lazy（專案已有 late import 範式）

⚠️ **稽核者誠實揭露**：無法實機 profiling，秒數為「往返次數 × 典型 RTT」推估；總經頁 gate 之後的 section 成本未完整盤點。

### C. 「抓了卻沒用於判斷的資料」

**第三路稽核未執行**（token 用盡）。下次接手可派。

---

## 下一步（user 已裁示順序）

### 1️⃣ 先做「A 部位包」

給 `stock_watchlist` 加 `lots` / `avg_price` 兩欄（ETF 分頁本來就有）。**一處 schema 改動解鎖五項**：

- 個股實際成本與報酬（層次 2）
- 停損停利門檻使用者可設（層次 3）
- 部位型停損警報（層次 4）
- 產業集中度改用**真實權重**（目前是等權假設）
- 總經水位對照能**真的相減**（目前刻意不減）

⚠️ **這不違反 §1**：`gsheet_portfolio.py:332` 那句「絕無張數/均價」擋的是**系統編造價格**，不是禁止使用者輸入真實成本（ETF 分頁一直存 `avg_price`）。差別在「使用者填的真值」vs「系統猜的假值」。

**代價**：改 Sheet schema + UI 輸入 + cron 讀取要改走 Service Account（公開 CSV 會暴露部位）。約兩批。

### 2️⃣ 大掃除修復（上面 A/B 兩節，逐項需 user 裁示範圍）

### 3️⃣ 之後（等實際使用一個月再定）

進場訊號推播 + 決策記錄閉環（層次 3/4 各一格，一起做才成閉環）、ETF↔個股穿透曝險（需先建 name→代號 對照表）、Health Score 五維雷達（難點在正規化映射）。

---

## 流程圖四層完成度

| 層次 | 完成度 | 缺口 |
|---|---|---|
| 1 市場與總經洞察 | ~75% | 「影響全系統風控係數」箭頭只兌現 **3/12**（其餘 9 處只是顯示） |
| 2 核心分析模組 | ~70% | 個股成本報酬 ❌、ETF/個股重疊曝險 ❌（產業集中度 ✅ 這輪做的） |
| 3 資產監控與評分 | ~60% | 進場訊號 ❌、停損門檻可設 ❌、統一 Health Score ❌ |
| 4 LINE 推送閉環 | ~75% | 進場訊號推播 ⚠️、**確認買賣回饋 ❌（唯一零進度的格子）** |

---

## ⚠️ 給下一個接手者的工具警告

**`Glob` 在此環境不可靠** —— 對明明存在的檔案回「找不到」（`data_cache/**`、`src/ui/tabs/stock_sections/*.py` 都中招）。
本輪曾因此誤判「`data_cache/` 不存在、前進式驗證 0 樣本」並寫進 STATE.md，後以 `Read` 實開檔案推翻（實際有兩個 cohort 正常累積）。

**一律用 `Grep` / `Read`，絕不用 Glob 下否定結論。**

同類教訓本輪共四次（`beat_only` 百分位語意、`risk_control` 需 buy_price、`data_cache` 誤判、盈虧比是結構常數）——
共通點都是**憑既有描述或單一工具結果下結論，未實地查證**。
`CLAUDE.md` §7「計算式先用數學式寫給我確認」正是防這個。
