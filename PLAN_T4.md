# T4 架構規劃：單筆 / 多筆診斷對稱化

> **本文件禁止附帶 code**（`CLAUDE.md` §8.1）。核准後才進 Execute，一次一個模組（§8.5）。
> 建立日：2026-08-14　基準：v19.194（5815 passed）
>
> 對應流程圖層次 2「單/多筆標的即時診斷」旁標註的 **「補齊詳細維度」**。

---

## §0 先修正一個我自己講錯的說法

我先前多次說「單筆獨有 **17 項**、多筆獨有 13 項，全部該補齊」。
實地清點 `tab_stock.py:872-955` 的 13 個 section 之後，**這個說法是誤導的**：

> **多筆不是「單筆的殘缺版」，它是不同的視角。**
> 單筆 = 一檔的**深度**；多筆 = N 檔的**比較**。
> 把 20 檔的 K 線圖、20 份心理檢查表堆到同一頁，不叫補齊，叫災難。

所以 T4 的目標**不是**「把 17 項搬過去」，而是：

1. 逐項判斷**這個維度在比較情境下該長什麼樣**（表格欄？下鑽？不適用？）
2. 只做真的有價值的那幾項
3. 抽共用邏輯，消除兩邊各寫一次的重複

---

## §1 單筆 13 個 section 的逐項分類（實地清點）

來源：`tab_stock.py:872-955`。

| # | Section | 多筆現況 | 分類 |
|---|---|---|---|
| 1 | `psy_checklist` 心理檢查表 | 無 | ❌ **不適用** |
| 2 | `when_buy_sell` 什麼時候買賣 | 無 | ⚠️ **部分**（見下） |
| 3 | `dragon_alert` 龍頭預警 | 無 | 🔶 表格徽章 |
| 4 | `health_score` 健康度評分 | ✅ 總表已有健康度欄 | ✅ 已對稱 |
| 5 | `vcp_bollinger` VCP+布林圖 | 無 | 🔽 下鑽 |
| 6 | `chips_20d` 20 日籌碼 | ✅ 總表有籌碼欄 | ✅ 已對稱 |
| 7 | `kline_chart` K 線圖 | 無 | 🔽 下鑽 |
| 8 | `op_recommendation` 即時建議 | ✅ 總表有綜合建議欄 | ✅ 已對稱 |
| 9 | `357_valuation` 三河流圖 | 無 | 🔽 下鑽 |
| 10 | `financial_leading` 財報領先 | ✅ `render_financial_health_section` | ✅ 已對稱 |
| 11 | `revenue_trend` 月營收/毛利 | ✅ `_render_fin_trend_section` | ✅ 已對稱 |
| 12 | `strategy_conclusion` 策略 1 結論 | ✅ 財報趨勢×轉機 | ✅ 已對稱 |
| 13 | `d2_leading` 基本面先行 | ✅ 三階段濾網 | ✅ 已對稱 |

**清點結果：13 項裡 7 項早就對稱了。** 真正的缺口只有 6 項，且性質不同：

- **❌ 不適用（1 項）**：心理檢查表是「單檔進場前的 SOP 勾選」，
  20 檔逐一勾選在比較情境下沒有意義。**不做。**
- **⚠️ 部分（1 項）**：`when_buy_sell` 的**圖**不適合搬，但它算出來的
  **停利/停損價與盈虧比**很適合當表格欄位。
- **🔶 表格徽章（1 項）**：龍頭預警是布林結果，適合當總表的一個徽章欄。
- **🔽 下鑽（3 項）**：K 線圖 / 三河流圖 / VCP+布林 —— 都是「看一檔」的圖，
  適合做成「點選某檔 → 展開」。

---

## §2 已有的前例：照著做，不要另創一套

`CLAUDE.md` §8.2 L5 那列記著（v19.164）：

> `pattern_targets_ui.py`（型態目標價）—— 組合改**批次表 + 下鑽共用批次 df**，無獨立分頁

這正是本案要的模式，而且**已經在你的專案裡跑過一次**。T4 照抄這個範式：

```
批次算一次 df  ──┬─→ 總表加欄（一眼比較）
                 └─→ 下鑽 expander（選一檔看細節，重用同一份 df，零額外抓取）
```

⚠️ **關鍵約束：下鑽必須重用批次 df，不可為了畫圖再抓一次。**
20 檔各抓一次 K 線 = 20 次網路請求，會讓頁面慢到不能用。
`pattern_targets_ui` 的 v19.164 改版就是為了這件事。

---

## §3 提案：分兩批，先做表格欄

### ⚠️ 本節於 2026-08-14 查證後大幅修正（原提案有兩欄是無效的）

**原提案要加「停利/停損價」與「盈虧比」兩欄。查證後推翻。**

`tab_stock.py:648-658` 有 v19.179 B1-b 的修正註記，明寫：

> 舊碼 `(_tp1_p - _cur_p) / max(_cur_p - _sl_p, 0.01)`：
> `_tp1_p` = 現價×(1+5%)、`_sl_p` = 現價×(1−8%) ⇒ **現價完全約掉**

數學上：

$$RR = \frac{1.05P - P}{P - 0.92P} = \frac{0.05P}{0.08P} = 0.625 \quad(\text{對所有 } P \text{ 恆定})$$

⇒ **「盈虧比」是常數，每檔都一樣**；「停利/停損價」只是現價 ±固定 %，
放進比較表等於印 20 列相同的 0.625 與一欄「現價乘 1.05」。
**多筆頁的意義是比較，而這兩欄沒有任何可比性。**

本專案已於 v19.179 發現並處理過此事（該批的標題就叫「盈虧比誠實化」），
我提案時未讀到那段註記。**這是本輪第四次查證推翻提案，但也是第一次在寫 code 前擋下。**

### Batch T4-1（修正版）：加**真的會因股而異**的三欄

判準改為：**這個數字在 20 檔之間會不會不一樣？** 不會的一律不加。

| 欄位 | 來源（`tab_stock.py`） | 為什麼可比 |
|---|---|---|
| 絕對停損線 + 距現價% | `_abs_sl`（大量紅 K 低點，`:820-838`） | **真實的每股錨點**，不是現價的固定倍數 |
| 實際盈虧比 | `_rr2 = (_tp1_p − _cur_p)/(_cur_p − _abs_sl)`（`:845-847`） | 分母是紅 K 低點 ⇒ **因股而異**，v19.179 保留的就是這個 |
| 🏆 龍頭 | `evaluate_leading_gates`（T1 那批已收斂成共用） | 布林徽章，零額外計算 |

**額外候選（待你決定要不要一起加）**：近 20 日壓力/支撐的**距現價 %**
（`_dist_hi` / `_dist_lo`，`:798-799`）—— 也是因股而異，且直接回答
「這 20 檔裡誰離支撐最近」。

⚠️ `_abs_sl` 為 None（近 20 日無大量紅 K）時，該檔三欄留白並標「無紅K錨點」，
**不可**退回固定 −8%（那又變回常數，且 v19.179 已明文指出同名不同義的問題）。

### Batch T4-2：下鑽 expander（三張圖）

總表下方加一個 `st.selectbox` 選一檔 → expander 展開該檔的
K 線圖 / 三河流圖 / VCP+布林。**重用批次 df**。

**為什麼後做**：需要先確認 T4-1 的批次 df 結構夠不夠餵這三張圖。
若不夠（例如河流圖需要季配息序列而批次沒抓），那就是另一個資料問題，
要先解決才談畫圖 —— 這正是我這輪學到的「先驗證再規劃」。

---

## §4 §7 四點對齊

### 4.1 資料來源與單位

**本批零新資料源。** 三個欄位全部來自批次已抓的東西：

| 欄位 | 上游 | 單位 |
|---|---|---|
| `_tp1_p` / `_tp2_p` / `_sl_p` | 停利停損常數 × 現價（`signal_thresholds.STOP_PROFIT_T1_PCT` 等） | **元** |
| 盈虧比 | 由上述三者計算 | **無單位**（比值） |
| 龍頭徽章 | `evaluate_leading_gates(cl2, cx2, capital)` | bool |

⚠️ **待實作前驗證**：`section_batch_fetcher` 的批次結果裡**有沒有** `cl` / `cx` / `capital`
三個欄位？若沒有，龍頭徽章就需要額外抓取 —— 那會改變本批的成本評估。
**這一項是動工第一步要查的。**

### 4.2 發布延遲 / PIT

停利停損是**現價的函數**，無延遲問題。
龍頭徽章吃季報（合約負債 / 資本支出），延遲同財報體檢（季後 ~45 天），
與同頁既有的財報欄位一致，**不引入新的 PIT 風險**。

### 4.3 邊界條件

| 情境 | 處理 |
|---|---|
| 抓不到現價 | 三欄全留白，**不可**填 0（0 會被讀成「停損價 0 元」） |
| ATR 算不出 | `atr_stop_price` 已有 fallback（固定 `STOP_LOSS_PCT`），沿用並標註 |
| 盈虧比分母為 0（現價 = 停損價） | 回 `None` 顯示「—」，**不可** inf（§4.4 大數除以小數） |
| `capital` 缺 | 龍頭徽章留白，不顯示「未達標」（那是「不知道」不是「沒達標」） |
| 空組合 | 總表本來就不渲染 |

### 4.4 計算式

**停利停損價**（沿用單筆既有式，不新增常數）：

$$TP_1 = P \times (1 + \text{STOP\_PROFIT\_T1\_PCT}/100)$$
$$SL = P - k \times ATR \quad\text{（}k = \text{ATR\_STOP\_MULTIPLIER} = 1.5\text{）}$$

ATR 缺時退回 $SL = P \times (1 - \text{STOP\_LOSS\_DEFAULT\_PCT}/100)$。

**盈虧比**：

$$RR = \frac{TP_1 - P}{P - SL}, \qquad P > SL$$

$P \le SL$（現價已跌破停損）時回 `None` 並在欄位顯示「已破損」——
那是**有意義的狀態**，不是計算失敗。

⚠️ 上面三式我都標了來源常數，但 **`STOP_PROFIT_T1_PCT` 等是否真的是這個語意，
動工第一步要實讀確認**（本輪已兩次因為憑描述寫規劃而出錯）。

---

## §5 模組切分與依賴

```
L2  compute/scoring/（既有停利停損計算）      ← 若已有純函式就直接用
        ▼
L5  stock_grp_sections/section_portfolio_summary.py::_render_master_table
        └─ 加三欄（純顯示）
```

**不新增模組。** 若發現停利停損計算目前只存在於 `section_when_buy_sell.py`（L5）
的行內邏輯，才需要下沉成 L2 純函式 —— 那會是本批的第一步，
且是**真正的 SSOT 收斂**（兩邊共用一份判定）。

---

## §6 自評過度設計

- ❌ **不做**心理檢查表的多筆版（性質不適用）
- ❌ **不做**獨立的「多筆詳細分頁」（v19.164 已證明批次表+下鑽較好，
  且該次改版就是為了**取消**獨立分頁）
- ❌ **不做**下鑽（留 T4-2），先確認批次 df 夠不夠餵圖

---

## §7 動工前必須先查證的三件事

本輪我已經因為「憑既有描述寫規劃」錯了三次（T1-a 的 `beat_only`、
T3 的 `risk_control` 簽章、`data_cache` 誤判）。故本規劃書把查證步驟明列為**第一批工作**：

1. `section_batch_fetcher` 的批次結果 df **實際有哪些欄**？（決定三個新欄要不要額外抓）
2. 停利停損 / 盈虧比的計算目前**寫在哪**？是 L5 行內還是已有 L2 純函式？
3. `STOP_PROFIT_T1_PCT` / `ATR_STOP_MULTIPLIER` 的**實際語意與單位**（% 還是小數？）

三者查完才寫第一行 code。若查證結果推翻本規劃書任何一節，**先改規劃書再動工**。

---

## §8 需要你確認

1. **同意「13 項裡 7 項早就對稱、真缺口只有 6 項」的清點嗎？** 若你實際使用時
   覺得某個「已對稱」的項目其實不好用（例如總表的健康度欄不夠看），
   告訴我哪一項，那會改變優先序。
2. **同意先做 T4-1（總表加三欄）、下鑽留 T4-2 嗎？**
3. **心理檢查表確定不做多筆版？** 我判斷它不適用，但那是你的使用習慣，你說了算。
