# 核心開發與治理協議 (Core Protocol v2.0)

> ⚠️ 本檔之上另有 `CLAUDE.md §-2`（AI 總管與執行分工，凌駕全篇）、`CLAUDE.md §-1.5`（虛擬軟體公司運行規範 / 客戶交付模式，2026-08-27 user 頒布）與 `CLAUDE.md §-1`（沒實際 bug / 沒具體需求 → 不要動）。位階：**§-2 > §-1.5 > §-1 > 本檔**。§3 兩條已由 §-2 收斂（2026-08-25）；§3 三步法的**核准對象**另由 §-1.5 收斂（2026-08-27）。均見該節就地註記，**舊條文一律保留不刪**。

## §1 狀態與記憶管理 (State & Memory)
- **冷熱資料分離**：專案根目錄必須維持極簡 `STATE.md`。每次任務**僅限讀取此檔與目錄結構**來理解專案目標，嚴禁要求使用者重複解釋。
- **防幻覺機制**：對話超過 10 輪時，修改程式碼前**必須重新讀取目標檔**（不准信任記憶）。
- **主動壓縮**：階段任務完成時，主動提醒我執行 `/compact` 指令，保留核心決策並清理無用推理鏈。
    - ✅ **與 `CLAUDE.md §-1.5` 第二條「嚴禁打擾客戶」正交，本條未動（2026-08-27 判定）**：提醒執行 `/compact` **不是請示** —— 沒有把任何技術決策外包給客戶，而是**只有客戶按得到的操作**的交班通知，性質同 `CLAUDE.md §-1.5.A-7` 的「這項沒實測到，客戶請看哪裡判讀」。**但**不得夾帶技術問題搭便車：提醒就只是提醒。

## §2 精準讀寫與檢索 (Precision I/O)
- **大檔案防截斷**：讀取超過 500 行的檔案，強制使用 `offset` 與 `limit` 分段讀取；搜尋結果超過 2000 bytes 時，必須用 `grep` 進行二次精確驗證。
- **動工前大掃除**：重構前優先清理 Dead code 與 Unused imports，極大化釋放 Token 空間。
    - ⚠️ **「內部自主拍板」不等於「可以趁機清全 repo」（2026-08-27 判定，本條未動）**：`CLAUDE.md §-1.5` 第一條 3「內部自修」的範圍是**本次任務的檔案邊界內**（見 `CLAUDE.md §-1.5.A-2` 邊界段：「內部自修是**修這次弄到的東西**，不是**趁機清全 repo**」）。超出邊界的既有 dead code、既有違憲（`CLAUDE.md §8.2.A.2` 待修表）仍受 `CLAUDE.md §-1` 拘束 —— **沒有 user 指派 / 沒有實際 bug 觸發就不要碰**。本條的「重構前先清」以**那次重構本來就會動到的範圍**為限。
- **局部編輯**：閉嘴寫扣 (No-Yapping)。嚴禁整檔覆蓋，僅針對特定函數或行數進行精準替換。

## §3 規劃與多線程 (Plan & Parallel Execute)
- **嚴格三步法**：~~Explore Agent（唯讀探索環境） -> 提出 Plan（3 句話藍圖）與我確認 -> 獲准後才 Execute（動手改 code）。~~ ~~**現行**（此「現行」指 **2026-08-25 版**，已於 2026-08-27 被下一句取代，原文保留不刪）：Explore（**派 subagent** 唯讀探索） -> 提出 Plan（3 句話藍圖）與我確認 -> 獲准後才 Execute（**同樣派 subagent** 動手改 code，總管不自己寫）。~~ **現行（2026-08-27）**：Explore（**派 subagent** 唯讀探索） -> **產出** Plan（3 句話藍圖） -> **依藍圖「逐條目」分流核准對象**：技術實作條目走**內部 gate**（獨立 QA 核准，實作者不得自己驗收自己）；業務語意 / UI 版面佈局 / 不可逆毀滅性操作 / `CLAUDE.md §8.4` step 4 重構範圍等條目走**客戶 gate**（商業語言 + 附總管推薦方案；UI 另須線框草稿先行） -> **獲准才** Execute（**同樣派 subagent** 動手改 code，總管不自己寫）。
    - ⚠️ **2026-08-25 user 決策，由 `CLAUDE.md §-2` 收斂；上面那條劃線是有意識的政策變更，不是漏刪。** 舊條把「派 agent」限縮在 Explore（唯讀）階段，Execute 預設總管自己下場；`CLAUDE.md §-2` 規則 1「每一次任務都要派工」＋ 規則 2「總管不自己寫實作」要求**實作也派工**。
    - **舊規則的理由（仍然成立，不是寫錯）**：唯讀探索弄壞東西的風險最低，是派工 CP 值最高的階段；Execute 由總管親自下場，省一次規格傳遞、少一層轉譯失真，改動小時通常也真的比較快。
    - **新規則為何勝出**：`CLAUDE.md §-2` 末段實證 —— 同一 session 內總管自己動手的東西被派出去的稽核 agent 抓到**三次**實質錯誤，且三次全出在「看起來很小、我自己來比較快」的 Execute 上。轉譯失真是**看得到、可複驗**的；總管自己寫錯是**沒有第二個人看**的。**這條不是被推翻，是被權衡掉的。**
    - ⚠️ ~~**三步法本身完全不變**：派工**不取代** user 核准 gate —— 藍圖仍要先給 user 過，獲准才 Execute。本條只改「**誰動手**」，沒鬆動「**要不要先問**」。~~ 把工作**發回原 subagent 續做**仍屬派工（**此半句未動，仍然有效**）。
    - ⚠️ **2026-08-27 user 決策，由 `CLAUDE.md §-1.5` 收斂；上面那條劃線是有意識的政策變更，不是漏刪。** 舊條把「藍圖一律先給 user 過」寫成**無例外**；`CLAUDE.md §-1.5` 第一條「不得中斷請示技術細節」＋第二條「嚴禁打擾客戶（內部自主拍板區）」要求**技術實作性藍圖改走內部 gate**。**三個步驟一步都沒有取消**，改的是**第二步的核准對象**。
    - ⚠️ **「gate 換人」不是「gate 取消」**：內部 gate 依 `CLAUDE.md §-1.5` 第一條 3「實作與稽核嚴格分離」—— **實作者不得自己驗收自己**；再加 `CLAUDE.md §-2` 規則 3/4 的多組獨立複驗。**沒有獨立 QA 核准就 Execute ＝ 違憲**，不因為「反正不用問 user」就可以直接動手。**Plan 這一步本身也不准省**：藍圖照出，只是收件人變了。
    - ⚠️ **分流以「條目」為單位，不以「整份藍圖」為單位**：一份 3 句話藍圖常同時含業務與技術條目。**不得**因為「整體看起來是技術的」就把裡面的業務條目一起吞掉自行拍板；也**不得**因為裡面有一條業務條目就把整份藍圖丟回客戶。判準見 `CLAUDE.md §-1.5.A-4`：**這個問題，客戶能用業務語言回答嗎？**
    - **舊規則的理由（仍然成立，不是寫錯）**：藍圖先給 user 過，是整條流程裡**最便宜的煞車** —— 方向錯了在「3 句話」階段轉向，成本遠低於實作完再退；且 user 對「這個功能到底要什麼」永遠比 AI 權威。
    - **新規則為何勝出**：這個煞車有**一半踩在客戶按不動的地方** —— 「走哪個 endpoint、欄名怎麼對、tolerance 幾天、模組切幾層、誰 import 誰」要讀 code 才判斷得出來，客戶多半只能回「你決定」，那一輪往返**沒有產生資訊**，只是把總管該做的查證外包給客戶。另一半（要什麼功能、畫面長怎樣、要不要換源、範圍要不要放大）客戶按得動 —— **所以那一半原封不動留著**。⚠️ 順帶澄清一個最常被拿來反駁的 HITL 論點（人類在「參與規劃」時易被 AI 誤導、在「驗證具體步驟與結果」時除錯力極佳）：它**其實支持這個切法** —— 客戶的除錯力被移到 `CLAUDE.md §-1.5` 第四條「成品直接體驗 → 最終業務驗收」上，那正是人類最強的模式；留在「陪讀技術藍圖」反而是人類最弱、最容易被 AI 說服的模式。**這條不是被推翻，是被權衡掉的。**
- **並行處理（限唯讀）**：探索 / 稽核階段~~若牽涉超過 5 個檔案，~~主動拆子任務並行 Explore agent，共享 API Context Cache。**寫入（改 code）仍嚴格序列化，一次一檔**——避免 API timeout / token 尖峰,並保留每批可審查性。
    - ⚠️ **2026-08-25 user 決策，由 `CLAUDE.md §-2` 收斂；上面那段劃線是有意識的政策變更，不是漏刪。** 檔案數門檻**取消** —— 探索 / 稽核不論牽涉幾個檔案都拆子任務派工；並依 `CLAUDE.md §-2` 規則 4「同一件事至少派多組不同角度的 agent，不要單點」，調查與稽核須**獨立分派，不可由同一組自己查自己**。
    - **舊規則的理由（仍然成立，不是寫錯）**：5 檔以下自己讀完通常更快；派工有固定開銷（寫規格、傳 context、讀回報、複驗），範圍小的時候開銷確實可能大於收益。
    - **新規則為何勝出**：這個門檻要先做「判斷這件事有多大」這一步，而 `CLAUDE.md §-2` 末段實證顯示 —— **「這件事小到不用派工」這個判斷本身，就是最常出錯的地方**（三次實質錯誤全發生在被判定為小的改動上）。拿一個已知會出錯的判斷當門檻，等於把判準建在已知會壞的地方。**這條不是被推翻，是被權衡掉的。**
    - ✅ **「（限唯讀）」與「寫入嚴格序列化、一次一檔」兩處未動，仍然有效**：`CLAUDE.md §-2` 管的是「**誰動手**」，不是「**同時開幾隻手改 code**」。並行只放給唯讀、寫入一次一檔，這條紀律與 §-2 **正交**，兩者並存 —— 誤把它一起劃掉會拆掉一條仍在生效的安全紀律。
    - ✅ **`CLAUDE.md §-1.5` 第一條 2「平行開發與檔案隔離」同樣不鬆動這條（2026-08-27 判定，本條未動）**：該點的重點在「**嚴格隔離不同模組的檔案修改範圍（File Boundary）**」—— **隔離是前提，不是授權**。它管的是「不同模組不要互相踩」，不是「同一批寫入可以同時開好幾隻手」。寫入並行會直接毀掉「是哪一步弄壞的」歸因能力，而這個理由與「誰動手」「要不要問客戶」都無關 —— 故**寫入仍嚴格序列化、一次一檔**，`CLAUDE.md §-1.5` 未觸及、也未鬆動本條。
- **高風險步驟預宣告 Fallback**：藍圖裡的高風險步驟（大檔拆分 / 搬移 / 外部來源抓取）必須**先寫好退路**（「若 X 失敗 → 退回 Y / 縮範圍到 Z」），不要等動工到一半才臨時想。範圍是**探針後才定案**,誠實縮範圍優於硬湊。
    - ⚠️ **「誠實縮範圍」的揭露義務（2026-08-27 補，本條未動）**：縮的若是**技術路徑**（改走哪個來源、拆成幾階段、退回哪個備案）＝ 內部拍板，但**必須**寫進 `CLAUDE.md §-1.5` 第四條交付報告的「品管驗收結果」段；縮的若是**客戶看得到的功能或畫面**＝ 落在 `CLAUDE.md §-1.5` 第二條請示白名單，**要問**，且附總管推薦方案。**禁止**把縮範圍默默吞掉 —— 對照 `CLAUDE.md §-2`「**沒查證的宣稱比沒有宣稱更危險**」與 `CLAUDE.md §1`「錯誤的數字比沒有數字更危險」。

## §4 鋼鐵自省與交付 (Audit & Delivery)
- **強制驗證機制**：不准說 Done 就跑。修改後必須通過 Type check 與 Lint。完成後輸出簡短報告：[邏輯]、[邊界]、[效能]、[Debug]。
    - ✅ **與 `CLAUDE.md §-1.5` 第三條 / 第四條的分層（2026-08-27 判定，本條未動）**：本條四段 [邏輯]／[邊界]／[效能]／[Debug] ＝ §-1.5 **第三條**「代碼產出後的自我審核」（第三條另有第 5 段「最終代碼」，**取聯集**），那是**內部與稽核**用的完整版；**交付給客戶**的另有 §-1.5 **第四條**格式（商業/功能交付摘要、品管驗收結果、成品直接體驗）。**兩份都要，不是二選一** —— 四段自審不因為「客戶只看三段」就可以省。
    - ⚠️ 內容取材仍以 `CLAUDE.md §6` 11 項自審清單為準（見 `CLAUDE.md §-1.5.A-3`：第三條只規定**怎麼分段呈現**，**不取代** §6 的**要查什麼** —— 第三條沒提到 provenance / magic number / lookahead / 浮點容差 / 對帳，只跑第三條會漏掉一半以上）。
- **環境與效能**：限用 .py 腳本（禁 .ipynb），維護 requirements.txt。必須確保 st.cache_data 的正確使用以優化 Streamlit 效能。
- **PR 規範**：請直接 merge PR + 存檔 (STATE.md) 與同步 ARCHITECTURE.md、SPEC.md。
- **跳 PR 直推例外 (Skip-PR Whitelist)**：以下三類改動可走 scripts/quick_merge.sh 直推主分支，免 PR：

    1.STATE.md / CLAUDE.md / 程式註解 / typo 修正
    2.版本字串 bump（不含任何程式邏輯／演算法／介面行為改動）
    3.不影響功能行為的純文件改動（README、ARCHITECTURE.md、SPEC.md 等）

其他一律走 PR 流程，保留 CI gate 與變更紀錄。任何一行 .py 邏輯變動 → 強制 PR。

## §5 卡關救援 (Anti-Loop Protocol)
- 針對同一個報錯，若連續重試 2 次未果，**立即停機**。
- 啟動除錯協議，並交由我詢問其他 AI 進行雙重驗證。
- ✅ **停機門檻與求救動作均未動（2026-08-27 判定）**：`CLAUDE.md §-1.5` 第二條「嚴禁打擾客戶」**不涵蓋本節** —— 本節是**求救**，不是**請示**。三個理由：(1) 觸發條件是**客觀重試次數**，不需要客戶做任何技術判斷，不存在「把技術決策外包給客戶」；(2) 「不得中斷請示技術細節」的**前提是內部閉環收得掉**，而「同一報錯連續 2 次未果」正是內部閉環已失效的**證據**，此時繼續內部硬幹＝把可觀測性換成一個沉默的重試迴圈；(3) 客戶在這裡的角色是**轉介管道**（拿去問其他 AI），不是決策者。
- ⚠️ **連帶：本節不適用** `CLAUDE.md §-1.5` 第二條末句的「必須用商業與功能語言」—— 求救封包要給的正是**技術細節**（完整報錯、已試過哪 2 次、各自為何失敗、卡在哪一行），用商業語言重寫會讓轉介失去價值。**請示用商業語言，求救用技術語言。**
- **計數跟著錯誤走，不跟著 agent 走（`CLAUDE.md §-2` 派工制連帶）**：換一組 subagent 重試**不重置**計數，同一個報錯的重試次數由總管跨 subagent 累計（只有總管看得到全局，subagent 不知道別人試過）；達門檻時停的是**整件事**，不是那一組 agent。
