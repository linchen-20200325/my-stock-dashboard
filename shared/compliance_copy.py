# -*- coding: utf-8 -*-
"""shared/compliance_copy.py — 合規文案 SSOT（L0 Infra）。

═══ 這個模組在解什麼問題 ══════════════════════════════════════════════
`docs/v2/spec/S1-6_COMPLIANCE_COPY_GUIDE.md §3.1` 把 AI 免責訂為**強制樣板**，
並在「實作紀律」第 1 條明文寫：

    **一處常數，兩處引用。**……**禁止在各頁手抄免責** ——
    手抄兩份，改的時候一定會漏改一份。

本 repo 的現況正是它要防的那一種：全站唯一一份「三件事都交代」的免責是
`src/ui/views/page_hold.py` 的 `AI_DISCLOSURE`，**住在一個頁面檔（L5）裡**。
別的頁要用就只能手抄 —— 於是 `why.qa`（🧬 AI 問答，規格 §3.0 出口盤點表的
**A1**）到 2026-09-23 為止**一個字的免責都沒有**，只有一個 🧬 前綴；
而規格自己把「**只有 🧬 符號**」判為「⚠️ 半有……**不是免責**」。

⇒ 本模組把免責樣板抬到 **L0**，讓任何一層都能合法 import 同一份。

═══ 為什麼放 L0（`shared/`）而不是放在頁面檔裡 ════════════════════════
免責文案的 consumer 橫跨 L5（各頁）與 `docs/`（線框）。放在任一個 L5 頁面檔，
別頁要用就變成 L5 → L5 的橫向 import —— §8.2 五條硬規則沒有明文禁止它，
但它會讓「`page_why` 為什麼依賴 `page_hold`」變成一個沒有答案的問題，
而且下一頁要用時又得再挑一個「誰當主檔」。**L0 是唯一一個
「誰都能合法 import、而且不製造依賴方向問題」的層**（同 `shared/edu_tokens.py`
當初從 L5 `tab_edu` 抬上來的理由）。

⚠️ **本批只新增 `why.qa` 這一個出口用的那一份**（規格的**版本 C**）。
`page_hold.py` 既有的 `AI_DISCLOSURE`（版本 A・潤稿型）**本批一個字都沒動** ——
把它搬上 L0 是**另一個出口的改動**，不在客戶 2026-09-23 裁示 D-4(a)
（「`why.qa` 補強制免責三件」）的授權範圍內（`CLAUDE.md §-1`：
沒有指派、沒有 bug 觸發就不要動）。**兩份並存是既有現況，不是本批造成的**；
要收斂成一份須另案，屆時 §3.1 還額外要求把版本 A 裡的函式名
`build_summary_prompt()` 換成白話（規格「抬成全站樣板時要改的一處」）。

═══ 誰在守這份 SSOT ═══════════════════════════════════════════════════
本模組**刻意不做 import 期自我斷言**：一句文案不對就讓整個 app 起不來，
代價遠大於它要防的風險。守衛改由測試強制（同 `CLAUDE.md §8.2.A.0` 規則 3
「清單由測試強制，不由人工維護」）——
`tests/test_p05_why_view.py::TestQaDisclosure`：

  · 三件事**各一條**正向斷言（比對值一律從本模組讀，不寫死字面量）；
  · 反向斷言：把免責拿掉、只留 🧬 → 正向那三條必須紅；
  · 手抄偵測：`page_why.py` 原始碼裡**不得**出現免責本文；
  · 跨檔漂移：線框 `docs/v2/wireframe/wf_page_why.js` 引的必須與本模組逐字相同。
"""
from __future__ import annotations

# ══════════════════════════════════════════════════════════════════
# 驗收標準 —— §3.1「三件事（缺一不可）」
# ══════════════════════════════════════════════════════════════════
#: 逐字轉錄自 `S1-6_COMPLIANCE_COPY_GUIDE.md §3.1` 的「三件事」表格第 2 欄。
#:
#: ⚠️ **這是驗收標準，不是可以直接印在畫面上的文案。**
#: 規格把它列為「樣板的驗收標準」，真正要印的是下面三個版本之一。
#: 本 tuple 存在的理由：讓「缺一不可」這句話在測試裡**有一個可以指的東西**，
#: 而不是每個測試各自默寫一遍三件事是哪三件。
AI_DISCLOSURE_REQUIRED_THINGS: tuple[str, str, str] = (
    "這段是 AI 生成的",
    "它不是投資建議，數字以卡面為準",
    "潤稿本身可能改壞語氣、漏掉限制條件",
)

# ══════════════════════════════════════════════════════════════════
# 版本 C｜自由問答型 —— `why.qa` 用的就是這一份
# ══════════════════════════════════════════════════════════════════
#: **逐字**轉錄自 `S1-6_COMPLIANCE_COPY_GUIDE.md §3.1`「✅ 全站樣板」的
#: **版本 C｜自由問答型**（規格原文的適用例：「**A1**/A2/A3/A15 AI 問答、多空辯論」）。
#:
#: **為什麼是版本 C 而不是 A 或 B**：規格的三個版本是依**出口性質**選的，
#: 而 `why.qa` 就是規格自己點名的 A1（🧬 AI 問答全文）。
#: 版本 A（潤稿型）預設「AI 只是把上面算好的結論寫成白話、數字以卡面為準」——
#: 本葉是使用者自由提問的對話框，**上面沒有卡**，照抄版本 A 等於指向一個不存在的地方。
#:
#: ⚠️ **一個字都不要改。** 規格 §3.1 訂它為**強制樣板**，
#: 而 `CLAUDE.md §-1.5.D`（v3 §03-2）把「改不改合規文案」留在客戶側。
#: 需要調整時走客戶裁示，不是就地改這個字串。
AI_QA_DISCLOSURE: str = (
    "⚠️ **這是 AI 生成的回答，不是投資建議。**\n"
    "模型只看得到本站提供的資料，**看不到你的資金、持有期間、風險承受度與稅務狀況** ——\n"
    "這些才是決定該不該做一筆交易的關鍵，而它們一個都不在它手上。\n"
    "回答中若出現任何買賣方向的說法，請當成模型的措辭問題，不是本站的結論。"
)

# ══════════════════════════════════════════════════════════════════
# 三件事 ↔ 版本 C 的對映（**承重判斷，據實標明**）
# ══════════════════════════════════════════════════════════════════
#: 下面三個常數是「版本 C 裡**哪一段**在交代第 ①／②／③ 件事」。
#: 每一個都是版本 C 的**逐字子字串**（由 `TestQaDisclosure` 當場驗證），
#: 存在的理由是：§3.1 要求「三件事缺一不可」，而「缺一不可」要能被機器查核，
#: 就必須說得出**拿掉哪一段會讓哪一件事消失**。
#:
#: ⚠️ **這個對映是實作組的判讀，不是規格明寫的。**（`CLAUDE.md §-2` 規則 6）
#: 規格把「三件事」與「三個版本」分成兩節寫，**沒有**逐版本標註哪一句對應哪一件。
#: 本組的讀法是：三個版本都是 §3.1 底下的產物，規格既然把它們列在
#: 「✅ 全站樣板」而該節的驗收標準就是三件事，那三個版本都應當是三件事的
#: **合規實例**，只是依出口性質換了說法。第 ②／③ 件在版本 C 的說法與
#: 表格用語差最多，故逐條寫明：
#:
#:   ① 這段是 AI 生成的           → 「這是 AI 生成的回答」（幾乎逐字）
#:   ② 不是投資建議＋以哪裡為準   → 「不是投資建議」＋「不是本站的結論」
#:      （自由問答沒有「卡面」可指，版本 C 改指「本站的結論」）
#:   ③ AI 環節**自己**的失效模式  → 「看不到你的資金、持有期間、風險承受度與
#:      稅務狀況」＋「請當成模型的措辭問題」
#:      （版本 A 講的是潤稿會改壞語氣／漏掉限制條件；版本 C 講的是
#:        模型手上**缺**這些限制條件 —— 同一件事的自由問答版說法）
#:
#: ⚠️ 若客戶或獨立稽核認為版本 C **沒有**交代滿三件，正確的處置是
#: **回去改規格或請客戶裁示換版本**，不是在這裡自己補一句 ——
#: 自己補等於發明一個第四版樣板（§3.1 訂的是強制樣板）。
AI_QA_THING_1_IS_AI_WRITTEN: str = "這是 AI 生成的回答"
AI_QA_THING_2_NOT_ADVICE: str = "不是投資建議"
AI_QA_THING_3_MODEL_BLIND_SPOTS: str = (
    "看不到你的資金、持有期間、風險承受度與稅務狀況")

#: 三件事在版本 C 裡的落點，**順序對應 `AI_DISCLOSURE_REQUIRED_THINGS`**。
AI_QA_DISCLOSURE_PHRASES: tuple[str, str, str] = (
    AI_QA_THING_1_IS_AI_WRITTEN,
    AI_QA_THING_2_NOT_ADVICE,
    AI_QA_THING_3_MODEL_BLIND_SPOTS,
)

# ══════════════════════════════════════════════════════════════════
# AI 敘述旗標（**它不是免責**）
# ══════════════════════════════════════════════════════════════════
#: 🧬 = 本站既有的「這一段是模型寫的」視覺旗標。
#:
#: ⚠️ **它不能拿來當免責用，這是規格明寫的。**
#: §3.0 的 AI 出口盤點表把「**只有 🧬 符號**」歸在
#: 「⚠️ 半有（過期警語／區塊層級／只有 🧬 符號）」那一列，說明欄逐字寫「**不是免責**」。
#: 旗標只說得出「這是 AI 寫的」（三件事的第 ① 件），
#: 第 ②／③ 件它一個字都沒交代。
#:
#: 抽成常數是為了讓「只有旗標不算數」這句話在測試裡可以被**反向**驗證
#: （拿掉免責只留旗標 → 正向守衛必須紅），而不是靠讀 code 的人自律。
AI_NARRATIVE_GLYPH: str = "🧬"
