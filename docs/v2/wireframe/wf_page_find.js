/* wf_page_find.js — 「🔍 找標的」線框資料（純資料，無渲染邏輯）
 *
 * ═══════════════════════════════════════════════════════════════
 * 修訂紀錄｜2026-09-14 文案瘦身組（客戶親自審稿後的回應）
 * ═══════════════════════════════════════════════════════════════
 * 客戶原話：「『資料時點：來源未提供資料日期 △ 不得猜、不得填今天、不得留白』
 * 重複數十次 —— 這是規格，不是 UI。」＋「誠實揭露 ≠ 用散文解釋所有事」。
 *
 * 本輪做的事：把「講給寫程式的人聽的話」從會被畫出來的欄位搬進 evidence。
 *
 * 1. 動到的會被畫出來的欄位：`subtitle`、3 個 `layers[].label`、14 個 block 的
 *    `states`／`name`／`flags`。states 十格**一格都沒有刪**、null 分布**一字未改**
 *    （機器複驗 `_verify.js`：NULLNESS CHANGED = 0；cols／asOf／src 全部 0 變動）。
 * 2. 搬走的是：§ 條號與「逐字」出處（S1-3B §3.2.x、UI-B:247-289、核准線框 stock_ia_v1.html）、
 *    ⛔／不得／不准 祈使句、code 內部識別字（`discriminative=False`／`on_select`／
 *    `st.dataframe`／`SCREEN_IDLE_NOW`／`PE_FAILED_WHY_HEAD`／`stale_reason`／
 *    `SCREENER_MIN_FACTOR_COVERAGE_RATIO`／`_n_below_coverage`／檔案行號）、
 *    以及兩個 block 的**整句 flags 散文**。**全部原文收進 evidence，一句都沒有丟。**
 * 3. ⚠️ **flags 是本組自行擴大的範圍**（指令只點名 states／name）：`find.screen_table`
 *    與 `find.pick_reason` 的 flags 原本是 3 句帶 ⛔ 的完整句子，renderer 會把它們
 *    當標籤 pill 畫在區塊上（`warroom_ia_v2.html:476-480`）—— 那正是客戶說的
 *    「規格印到畫面上」。本組縮成短標籤、全文搬 evidence。**這是本組的判斷，
 *    不是客戶或總管指示** → 若認為不該動，把 evidence 裡的原文貼回即可。
 * 4. ⛔ 沒有把任何缺口寫小，兩處特別點名（原文全在 evidence，states 留白話一句）：
 *    · 選股 0 檔還有「有候選但因子缺太多排不出名次」這第 4 種原因目前分不出來；
 *    · 殖利率欄的「沒配息」與「還沒公告」在來源端都是空值，目前混在一起。
 *    「未接線」一律仍寫「未接線（還沒做）」，⛔ 未改成「暫無資料」。
 * 5. asOf 仍一律 null（硬規則 3），⛔ 未填今天。
 * 6. 量測（本組，`_verify.js`）：states 8,856 → 6,209 字（−2,647，−29.9%）；
 *    name 314 → 273 字。搬進 evidence 共 14 個 block、刪除 0 句（全部搬運，無丟棄）。
 * 7. ✅ **本組自行改了一個符號，2026-09-14 總管已核准**：`find.pe_two_kinds` 提案裡的
 *    `⛔ 非正值` 改為 `⚠ 非正值` —— 理由：⛔ 在本檔其餘位置一律代表「禁止」，
 *    用它當一個資料狀態的圖示會混義（一符兩義）。
 *    **2026-09-14 總管裁示：准。** 理由與本組相同 —— ⛔ 在本檔其餘位置一律代表「禁止」。
 *
 * ⚠️ 客戶點名的那句重複數十次的文字**不在本檔**：由 renderer 印出
 *    （`warroom_ia_v2.html:487`，`blk.asOf` 為 null 時每個 block 固定印一次；
 *    規則本文另存 `wf_global.js:123`）。兩檔均由別組維護，本組**未動一個字**
 *    ⇒ **本輪瘦身無法消除客戶看到的那幾十次重複**，須由 renderer 端處理。
 *    **這是本組單組判讀，未經第二組複驗。**
 *
 * ⚠️ 依 CLAUDE.md §-2 規則 6 據實標明：本檔「規則文字已從畫面欄位清乾淨」
 *    是**本組單組**關鍵字掃描（⛔／不得／不准／禁止／§／★-／逐字／附錄／本組／單組／
 *    規格／線框／核准）的結果，**沒有第二組獨立複驗**。掃不到的規則語句可能仍有殘留。
 *    現存兩處「線框」字樣為**刻意保留**（誠實標示「這一段還沒做」，屬缺口揭露）。
 */
/* wf_page_find.js — 「🔍 找標的」線框資料（純資料，無渲染邏輯）
 *
 * ⚠️ 本檔原本**沒有檔頭**（2026-09-14 拍板落實組實測：第 1 行即 `window.WF_PAGES.push({`）。
 *    本檔頭為該輪新增，目的只有一個：**讓修訂紀錄有地方寫**。⛔ 未改動任何既有資料內容的語意。
 *
 * ═══ 修訂紀錄 ═══════════════════════════════════════════════════
 * **2026-09-14｜拍板落實組** —— 唯讀 repo，只改本檔；⛔ 未動任何 `.py`、未做任何 git 寫入。
 *
 * 【D1 · ★-02／DEV-19 已拍板：大表恢復「整列可點 → 就地展開」，標題觸發器並存】
 *  · **來歷（⚠️ 逐字照這樣寫）**：**總管拍板（客戶未表示偏好、授權總管決定）＋2026-09-14**。
 *    ⛔ **不得寫成「客戶核准」「客戶已拍板」** —— 客戶對這題明示沒有偏好、授權總管決定，
 *    **他沒有核准這個結論**。兩者不同，後人讀到會差很多。
 *  · **本組查證（回原檔實測，⛔ 不採信轉述）**：
 *    (a) 客戶母法逐字只禁「⛔ **卡片本身**點擊不觸發全層展開」（轉引自
 *        `S1-3_IA_WIREFRAME.md:1249`，**母法原件本組未見**）—— **表格的列不是卡片**；
 *    (b) 「⛔ 整列 `<tr>` 可點」的明文出處是 `S1-3_IA_WIREFRAME.md:77`，該檔 `:7` 自陳
 *        產出日 **2026-09-14**、執行者「IA 線框組（**單組**）」⇒ **規格自己加的，不是客戶講的**；
 *    (c) 2026-09-05 客戶核准的線框 `docs/wireframes/stock_ia_v1.html:839` 逐字「**點任一列**
 *        就地展開該檔明細」。
 *  · **⚠️ 反面事實（⛔ 不得省略）**：核准線框**同一個 block 內自己就有兩種手勢** ——
 *    `:859` `n` 欄「**點左表右側**就地展開」vs `:860` `live` 欄「**點任一檔**」。
 *    ⇒ 「客戶到底核准了哪一種」本身就不乾淨。
 *  · **被推翻的只有「整列」那半條**；同列「⛔ 卡片本身可點」「⛔ 標題可點」
 *    「⛔ hover 自動展開」「⛔ 卡片右上角小箭頭」四條 **⛔ 未被推翻，仍然有效**。
 *  · **改到哪裡**：`find.screen_table`（`live` 明訂整列可點 ＋ `flags`）、
 *    `find.pick_reason`（`live` 的逐列層手勢由「點該列右側」改為「**整列可點**」＋ `evidence` ＋ `flags`）。
 *  · ⚠️ **`find.pick_reason` 這個區塊本身仍是 ★待拍板** —— 拍板的只有「**手勢**」那一半，
 *    ⛔ 不是「這個區塊要不要存在」。兩件事不要混。
 *
 * 【D2 · 本輪⛔ 沒有解決、據實留著的一個連帶問題】
 *  · `DEV-19` 當初把手勢反轉的**唯一實質理由**是：整列可點時，使用者**看不出點下去是
 *    就地展開（`▸`）還是離開本頁（`↗`）**。**手勢轉回來之後這個問題並沒有消失。**
 *  · 本次拍板**只定了手勢，沒有定兩者怎麼區分** ⇒ **該問題仍然開著**，已寫進兩個 block 的
 *    `flags` 與 `find.pick_reason` 的 `evidence`。⛔ 不得當成已解決。
 *
 * 【D3 · 本輪⛔ 沒有宣稱接線】
 *  · `src/ui/views/page_find.py:1596` 的 `st.dataframe(...)` **沒有 `on_select`**
 *    ⇒ **現況點列不會有任何反應**（本組實測，同前輪 evidence）。
 *    **拍板定的是「線框長什麼樣」，⛔ 不是「它已經會動」。** 已就地寫進 `live`。
 *
 * 【本輪自我檢查】
 *  · 格式契約：`states` 十鍵全給、`asOf` 一律 `null`、`cols` 三鍵、`flags`、`src` —— 本輪⛔ 未動這五項的結構。
 *  · 禁用語七詞：修改前後各掃一次，本檔**零命中**。
 *  · 新文案過 S1-6 §1 STEP 2 缺項填補測試：新增句**全是觀測與結構描述**
 *    （哪一種手勢、接沒接、出處在哪、誰拍的板），**沒有行動半句**。
 *  · `node --check wf_page_find.js` 通過。
 *  · ⚠️ **本輪由單組完成，未經第二組獨立複驗**（`CLAUDE.md §-2` 規則 6）。
 */
window.WF_PAGES.push({
  id: 'find',
  tab: '🔍 找標的',
  subtitle: '一句話：從全市場縮到一張候選清單。',
  mainCTA: { label: '🎯 開始選股', src: 'src/ui/views/page_find.py:422' },
  leaves: [
    { id: 'l1', name: '選股網',
      note: '`page_find.LEAF_SCREEN_TITLE`（`src/ui/views/page_find.py:426`，實測逐字）。主 CTA「🎯 開始選股」住這一葉的條件表單內，是**一個 `st.form` 裡的一顆 `form_submit_button`**（`:1529` `st.form(FORM_KEY)` / `:1555` submit），符合 L0-3「首屏唯一主按鈕且必在預設葉」。' },
    { id: 'l2', name: '板塊地圖（＝產業熱力圖＋板塊資金潮汐）',
      note: '`page_find.LEAF_MAP_TITLE`（`:427`，實測逐字）。本葉的 CTA「🗺️ 載入板塊地圖」是**單顆 `st.button`（`:1621`），⛔ 不在 form 內** —— 它不是頁主 CTA。⚠️ 這個葉名是**兩個既有分頁併出來的**（產業熱力圖 ＋ 板塊資金潮汐）。' }
  ],
  layers: [

    { n: 0, label: '葉外｜全域 chrome（跨頁頂部狀態列 ＋ 資料時點列，五頁共用，不屬任何葉）',
      blocks: [

        { key: 'find.statusbar', name: '（跨頁）頂部狀態列', leaf: null,
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '`09/13 週五 ✅ 交易日　|　🟢 總經 偏多　|　🔗 已綁 Sheet`',
            idle: '`09/13 週五 ✅ 交易日　|　⬜ 總經未評估　|　🔗 尚未綁定 Sheet`',
            loading: '更新中…',
            empty: null, missing: null, na: null, partial: null,
            degraded: '`⚠️ 🟡 總經 偏多 —— **來自快照，時間不明**`（同頁1：這是上一次存下來的結論，存的時候沒有記時間）',
            unwired: null,
            error: '🔴 總經狀態讀取失敗（附錯誤型別）；Sheet 欄位維持原樣'
          },
          asOf: null,
          evidence: '五頁共用的 chrome，規格與狀態定義全部在頁1（`wf_page_today.js` 的 `today.statusbar`）—— **本頁不重新定義，只標明它在本頁也在**。\n⚠️ 本頁的總經位階**另有一個用途**，與狀態列不是同一條路：`page_find._load_regime()`（`src/ui/views/page_find.py:796`）把總經位階拿去當**空頭濾網**餵進選股。該函式失敗時回 `(None, repr(e))` 並 `print` 一行 —— 也就是**選股不因總經讀不到而中止，但濾網不套用**。這件事要在選股結果卡的 `L3 說明` 那一列講出來（L3 自己的 note 會帶），⛔ 不可靜默略過。\n【本組實測】live 那一行的日期是規格示意值，非實測。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· idle 原出處：S1-3B §3.2.1 冷啟動圖第 1 行逐字。\n· degraded 原文：同頁1，來源優先序**第 3 條** `macro_state.json`。\n· error 原文：「Sheet 欄位維持各自狀態（**逐格獨立判態**）」—— ⛔ 一格失敗不得把另一格一起染色。',
          src: 'src/ui/views/page_find.py:796',
          flags: ['單組結論'] },

        { key: 'chrome.asof', name: '資料時點揭露列（頁標題下方第一行）', leaf: null,
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '資料時點：`09-13 收盤（價量）· 2026-07 資料（月營收）`　⭕ 可點，展開逐源明細',
            idle: '資料時點：**尚未載入，本頁沒有任何本輪資料**　⭕ 可點，捲到 CTA',
            loading: '資料時點：更新中…　❌ 不可點',
            empty: null, missing: null, na: null,
            partial: '資料時點：**最舊 `〈日〉` ～ 最新 `〈日〉`；其中 N 項來源沒有給資料日期**　＋ ⚠︎　✅ 可點',
            degraded: null, unwired: null,
            error: '🔴 資料時點：**這一輪沒取到，畫面上的值可能是上一輪留下的**　✅ 可點'
          },
          asOf: null,
          evidence: '書寫規則（S1-3B §2.7）：歸屬日取 `shared/staleness.py:85` `latest_date()`，⛔ 不得用 `fetched_at`；⛔ 禁用「今天／剛剛／最新」這類相對詞。\n🔴 **本頁時點列的難處：一頁之內有三種不同節奏的時點，⛔ 不可合併成一個日期**：(a) 價量／RS ＝ 收盤日；(b) 月營收／季報 ＝ 資料歸屬月（例 `2026-07`，發布延遲見 `CLAUDE.md §2.3`）；(c) **葉2 泡泡圖 ＝ 每交易日盤後凍結的快照**（S1-3B §3.2.9 第 2 項：時點列必須標 `MM-DD 盤後`）。三者混在一起 ⇒ 本頁常態是 `partial`（「最舊～最新」），不是單一日期。\n【本組註記】本列在核准線框 `stock_ia_v1.html` 中**不存在**（該線框只有一條頂部狀態列），是 2026-09-14 客戶拍板一新增 → 屬 ★-07。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live／idle／error 三格原分別註明出自 S1-3B §3.2.3 有資料圖第 3 行／§3.2.1 冷啟動圖第 2 行／§3.2.6 錯誤態圖末行逐字。\n· partial 原文（★-07a 合併規則）：「混合」與「部分源時點不明」同時成立時**一行寫完**，⛔ 不得只寫其中一半。',
          src: null,
          flags: ['★待拍板'] }
      ] },

    { n: 1, label: '第一層｜葉1 選股網 · 條件表單（零互動可見；它同時是本頁所有灰態的出口，任何狀態下都看得到）',
      blocks: [

        { key: 'find.screen_form', name: '葉1 條件表單（① 基本面優選／② 勾條件／③ 排序與筆數 ＋ 主 CTA）', leaf: 'l1',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: '三格條件 ＋ submit 可按：\n│ ① 基本面優選　四項全過（自動）│ ② 勾條件　估值／EPS／缺貨／抗跌RS／跨季轉強 │ ③ 排序與筆數　綜合評分 · top 50 │\n　　　　　▌ 🎯 開始選股 ▐\n**按下去才會開始取數。在你按之前，這一頁不會發出任何一次網路連線。**',
            idle: '**表單永遠可見**（它就是灰態的出口），三格顯預設值，submit 可按。\n⚠️ 還沒選過與已經選過，表單**長得一樣** —— 差別在下方「選股結果」區塊。',
            loading: '三格**維持可見**（它是灰態的出口）；**submit 停用**；結果區改固定高骨架列 ＋「正在跑缺貨掃描…」。',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: '該條件旁 🟠 ＋ 就地寫「這一項的門檻現在分不出好壞」；排序會把這一項排除，並在畫面上寫明。',
            unwired: '該勾選項**未接線**（還沒做）＋ 停用它，但**不隱藏** —— 隱藏就看不出少了什麼。\n⚠️ 五個因子目前都已接線，所以這一格現在沒有實例。',
            error: '表單**仍可見**；submit 依錯誤型別決定可不可按：\n· **C-1 上游掛了**（例：FinMind 額度用罄）→ submit **仍可按**（可重試）\n· **C-2 契約壞了**（欄位對不上／值超出範圍）→ submit **停用** ＋「重跑不會好，程式要修，請回報」\n⚠️ 另有一態：**表單本身建不起來**（因子名稱取不到）→ 就地換成一張說明卡。'
          },
          asOf: null,
          evidence: '〔第四層〕展開＝本次條件、顯示筆數、存活池口徑。\n【實測 code 對位】`_render_screen_form()`（`page_find.py:1494`）docstring 自陳約束：「一個 `st.form`、**一顆** `st.form_submit_button`；form 內**沒有** `st.button` / `st.download_button`」（`:1505-1506`）。實體：`st.form(FORM_KEY)` `:1529`、`st.form_submit_button(ACTION_RUN_SCREEN_LABEL)` `:1555`。\n【F11 為什麼一定要包 form】舊實作 5 個 multiselect／checkbox **每動一次就整站 rerun**；包進 form 之後勾幾個都只送一次。F11 實測：`st.button()` 放進 `st.form()` 即拋 `StreamlitAPIException` ⇒ **CSV `download_button` 必須留在 form 外**（`:1603`，確認在 `_render_screen_leaf` 內、form 之外）。\n【③ 筆數的實際選項】`TOP_N_OPTIONS = (20, 50, 100)`（`:449`）—— 線框只畫了 `top 50`，**三個選項都要畫得出來**。\n【unwired 那一格為什麼目前沒有實例】`page_find.py:93-97` 檔頭實測記載：估值（本益比）**本批已接線**（2026-09-07 FE-18，新增 L3 `services.valuation_service.get_pe_name_maps()`），「不再屬於 `unwired` 這一條 —— 它的『沒有資料』現在走 `empty` 那條路（重按有機會好），與 `unwired`（重按永遠一樣）是兩件事」。⚠️ **本組只查證了這一句是檔頭原文**，**沒有**獨立驗證「五個因子全部真的接上了」這個全稱句（`WIRING_DISCLOSURE` `:562` 這樣宣稱，但那是 code 自己的宣稱，不是第二組的複驗結果）。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live／loading 原分別註明 S1-3B §3.2.1／§3.2.2 逐字。\n· idle 原文：⛔ 不要用表單本身去表示「有沒有選過」。\n· degraded 原文：⛔ **排序不得沿用該因子**，或明說「排序已排除該因子」（S1-3B §3.2.7 矩陣 ⑦ 欄）。\n· unwired 原文：⛔ 不是隱藏 —— 隱藏就看不出少了什麼。\n· error 原文：兩種錯誤要分開（S1-3B §3.2.6 · `UI-B:247-289`）；「表單本身建不起來（因子標籤 L3 取不到）」對應 `build_form_unavailable_card()`（`page_find.py:1479`）。',
          src: 'src/ui/views/page_find.py:1494',
          flags: ['單組結論'] },

        { key: 'find.scenario_quickpick', name: '🆕 白話情境快選（高殖利率／營收成長／低基期）', leaf: 'l1',
          cols: { desktop: 3, tablet: 3, phone: 1 },
          states: {
            live: '三顆一排的情境鈕（點一顆＝把對應因子勾好、筆數設好，**不自動送出**）：\n│ 💰 高殖利率 │ 📈 營收成長 │ 📉 低基期 │\n點下去之後，② 勾條件那一格會**看得見地被改掉**，使用者仍要自己按「🎯 開始選股」。\n⭐ 每顆鈕下方常駐一行小字口徑（不是滑過才出現，也不摺疊）：\n· 💰 高殖利率 ＝ 依殖利率由高到低排序\n· 📈 營收成長 ＝ 勾「EPS／跨季轉強」這一組成長因子\n· 📉 **低基期 ＝ 距一年低點近**',
            idle: '三顆鈕**永遠可見、永遠可按**（它們是條件表單的一部分，不是結果區）。未點過時無任何一顆處於選中樣式。',
            loading: '三顆鈕**停用**（與 submit 同步），但不隱藏。',
            empty: null,
            missing: null,
            na: null,
            partial: '情境裡有一項因子這一輪取不到 → 該鈕仍可按，但**就地標明**「這個情境裡的〈因子名〉這一輪沒有資料，按下去只會套用其餘 N 項」。',
            degraded: '情境用到的門檻現在分不出好壞 → 該鈕旁 🟠 ＋「這個情境現在分不出好壞」；該鈕仍然看得到（藏掉＝你不會知道它存在過）。',
            unwired: '🔴 **這一格就是現在的真實狀態**：這三顆鈕在現行畫面上**不存在**。線框畫的是**提案**，還沒有做。',
            error: '情境套用失敗 → 就地紅字 ＋「這是程式要修的，請回報」；**你已經勾好的條件不會被清掉**。'
          },
          asOf: null,
          evidence: '🔴 **這一塊在 code 與規格裡都不存在，是客戶本輪新交辦的需求 —— 以下逐項據實標明來歷。**\n\n【(1) 來歷】「白話情境快選：高殖利率／營收成長／低基期」＋「**低基期＝距一年低點近**」兩句，**來自客戶本輪交辦**，⛔ 不是從 `S1-3B` / `stock_ia_v1.html` / `page_find.py` 推導出來的。核准線框 `stock_ia_v1.html` 的 `id:\'find\'` 葉1 只畫了三格（① 基本面優選／② 勾條件／③ 排序與筆數），**沒有情境快選**；`S1-3B §3.2.1`～`§3.2.9` 九節同樣沒有。⇒ **屬新增視覺元件，依 `CLAUDE.md §-1.5` v3 §03-2 ①「欄位增減／版面佈局」要先出線框給客戶拍板** —— 這份線框就是那個草稿。\n\n【(2) 實測 grep 結果，逐條列】\n· `grep -rn \'低基期\' --include=*.py .` → **5 命中，全部在總經分頁的建議字串裡**（`src/ui/tabs/macro/section_chips.py:593`、`section_long.py:392`、`section_mid.py:326,497,527`），例：`\'💎 CLI 擴張中大盤超跌，分批建倉低基期優質股。\'` —— 那些是**總經敘述文案，不是選股因子**，與本塊無關。\n· `grep -rln \'52週低|52w_low|year_low|low_52|一年低點|近一年低\' --include=*.py src/ shared/` → **0 命中**。\n⇒ 「距一年低點近」這個量在 repo 裡**目前沒有實作，也沒有欄位**。要做得先定義：用哪個價（收盤／還原）、窗口是 250 交易日還是 365 日曆日、「近」的門檻是幾 %。**這三個都是客戶定義的一部分尚未給** → ★待拍板。\n· 🔴 **「高殖利率」這顆鈕有一個口徑問題，要一起拍板**：`S1-2_METRIC_SSOT.md:230-242` 實測列出本 repo **三套不同的殖利率口徑** —— **Y-1** ETF／月月配站（`ttm_dividend / price`，近一年累計，✅ 完全符合母法）／**Y-2** 個股 357（分子是**近 5 年年度現金股利平均**，❌ 不符）／**Y-3 全市場掃描**（`yield_pe_fetcher`，TWSE `DividendYield` ／ TPEX `YieldRatio` **直接取用**，⚠️ **口徑未宣告**）。**選股網用的正是 Y-3 那一套** ⇒ 鈕上寫「高殖利率」時，**畫面必須同時標明這個數字是交易所公告值、口徑未宣告**，⛔ 不得讓使用者以為它與「🔬 查一檔」的 357 殖利率是同一把尺（那是 Y-2，分子完全不同）。這與頁4「同一個名詞，兩套刻度」是**同型問題**。\n· `grep -n \'高殖利率\'` 於 `S1-6_COMPLIANCE_COPY_GUIDE.md:882` 命中一條**合規判定**：`"🟢 高殖利率（穩定性待確認）"`（`v5_modules.py:361`）被判為「『高殖利率』是相對門檻的**事實陳述**，且『穩定性待確認』是誠實的不確定性揭露。**已經合規，不要動**」⇒ **「高殖利率」這個詞本身可用**，但要帶穩定性揭露。\n\n【(3) 合規紅線 —— 這塊最容易踩線，寫死在這裡】情境鈕的標籤只能是**條件的名字**，⛔ 不得是**行動的名字**。\n· ✅ 可以：「💰 高殖利率」「📉 低基期（距一年低點近）」「這些是符合你設定條件的標的」\n· ⛔ 不可以：「💰 存股首選」「📉 抄底名單」「便宜好買」「逢低布局」「這些是值得買的」\n理由：使用者從「看到這張清單」走到「下單」，中間必須代入**只有他才知道的變數**（可動用資金／持有期間／風險承受度／既有部位）。鈕的標籤若替他把那一步跨掉，就是買賣建議。⚠️ 特別注意「**低基期**」在 repo 現有的 5 個命中裡**每一次都緊接著行動動詞**（「分批建倉」「勇敢建倉」「大膽重壓」「可適度持有」）—— 把這個詞搬到選股網時**必須切乾淨，只留條件、不帶動詞**。\n\n【(4) 為什麼不自動送出】點一顆鈕就直接跑選股 ＝ 使用者**沒看到自己按下去等於勾了什麼**。依 `CLAUDE.md §-1.5` v3 §00 前端組「防重繪 `st.form` ＋ Checkbox Gate 延遲載入」與本頁 F11 的 form 設計，情境鈕應**只改表單狀態**，送出仍由唯一那顆 `form_submit_button` 負責 —— 這同時保住「首屏唯一主按鈕」（L0-3）。\n⚠️ **技術限制（實測，非推測）**：`st.form` 內**只能有 `form_submit_button`**，放 `st.button` 會拋 `StreamlitAPIException`（F11）。⇒ 三顆情境鈕若做成 `st.button`，**必須畫在 form 外、form 上方**，靠 session_state 去改 form 內 widget 的預設值；或做成 form 內的 `st.radio` / `st.segmented_control`（不是按鈕）。**這兩條路畫面長得不一樣，要客戶拍板哪一種** → ★待拍板。\n\n【(5) 本組沒做的事】本組**沒有**去驗證「營收成長」該對到現有哪幾個因子鍵（`page_find.py:461` 起有 `PE_FACTOR_KEY` 等常數，但本組未逐一比對五個因子的語意）。上面 live 格寫的「EPS／跨季轉強」是**本組的推測對映，未經查證**。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live 原文：口徑小字 ⛔ 不是 tooltip、⛔ 不可摺疊；「📉 低基期 ＝ 距一年低點近」⚠️ **客戶已定義，照此不得改**。\n· loading／partial／degraded／error 原文：⛔ 不隱藏；⛔ 不得靜默少套一個；門檻旗標為 `discriminative=False`，⛔ 不得把該鈕藏掉；⛔ **不得**把使用者的既有勾選清空。\n· unwired 原文用詞為「三顆鈕在 production 不存在」。',
          src: null,
          flags: ['★待拍板', '偏離核准線框', '單組結論', '未驗證'] },

        { key: 'find.wiring_disclosure', name: '取數接線揭露（表單下方常駐 caption）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '**這一頁會去拿什麼**：估值（本益比）/ 缺貨動能 / 抗跌 RS / 跨季轉強 / EPS 五個因子**都已接線**；結果表的「名稱」欄與估值同一份來源，一起帶進來。\n⚠️ 估值是唯一需要**多跑一輪取數**的因子 —— 那一輪失敗或回空時，本頁會在結果卡上明講，**不會靜默把它當 0 分**。',
            idle: '**常駐** —— 你在按下去之前就該知道這一頁等一下會去拿什麼。',
            loading: '常駐', empty: '常駐', missing: '常駐', na: '常駐',
            partial: '常駐', degraded: '常駐', unwired: '常駐', error: '常駐'
          },
          asOf: null,
          evidence: '【逐字來源】`WIRING_DISCLOSURE`（`src/ui/views/page_find.py:562-569`），上面 live 那一格是該常數的**逐字**內容（本組自 code 抄出，非改寫）。\n⚠️ **但它是 code 自己的宣稱**：「五個因子都走 L3 service，已全部接線」是一句**全稱句**，本組**沒有**逐一驗證五條路徑。依 `CLAUDE.md §-2 規則 6` 標為待驗事項。\n⚠️ 另一處誠實邊界（`page_find.py:234-238` 實測）：「**本檔看不出「只有上市或只有上櫃掛了」**：L1 對兩個市場是各自 `try/except … continue` 的 fail-soft，半邊失敗時回一份只有另外半邊的 map 且不留旗標。」⇒ 覆蓋率掉一半時，畫面上只會看到「N 檔有本益比」的 N 變小，**看不出是哪個市場掛了**。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live 原文（含 code 端名稱）：五個因子**都走 L3 service**；結果表「名稱」欄與估值同一份來源 `valuation_service.get_pe_name_maps`；估值那一輪打的是 **TWSE ＋ TPEX 兩支 OpenAPI**。\n· idle 原文：⛔ 不隨狀態消失。',
          src: 'src/ui/views/page_find.py:562',
          flags: ['未驗證'] }
      ] },

    { n: 2, label: '第二層｜葉1 選股結果 · 總覽卡（零互動可見，卡片本身不可點）',
      blocks: [

        { key: 'find.screen_summary', name: '葉1 選股結果 · 總覽卡', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '🔭 **存活池 274 檔 → 綜合入選前 50 名**\n本次因子：缺貨 12/89 · 抗跌RS 31/274\n資料時點：09-13 收盤（價量）· 2026-07 資料（月營收）　〔展開佐證 ▸〕',
            idle: '⬜ **尚未選股**\n為什麼：選股要跑缺貨掃描與 RS 分位，首次約需數十秒；在你送出之前，本頁**一次 L3 取數都不會發**（沒有人叫過它）\n去哪補：勾好條件後，按「🎯 開始選股」（在表單裡）',
            loading: '固定高骨架列 ＋「正在跑缺貨掃描…」（不顯示上一輪的值）。',
            empty: '▨ **選股已完成，符合條件的標的是 0 檔**\n為什麼：**這是一個有效的結果**（已經跑完，不是還沒跑、也不是故障）—— 可能是存活池本身就空、勾的因子沒有一檔算得出分數、或空頭濾網把候選全剔除了。上面那行說明寫的是哪一種\n⚠️ **還有一種情況目前分不出來**：有候選，但因子缺資料太多而排不出名次 —— 現在會跟「0 檔」混在一起顯示\n去哪補：少勾幾個因子再按一次「🎯 開始選股」；若說明指向季報快照還沒好，那要等排程補抓，**重按不會改變它**\n⭐ **這是灰的，不是紅的**；也**不同於**「⬜ 尚未選股」—— 一個是「跑完了，結果是 0」，一個是「還沒跑」。',
            missing: '同 empty（本卡的「沒有結果」只有三種：還沒選／跑完 0 檔／上游掛了）',
            na: null,
            partial: '🔭 **存活池 274 檔 → 綜合入選前 50 名\\***\n\\* 274 檔中 **31 檔缺月營收**，該因子改用其餘 243 檔重新排名 —— **缺的 31 檔會單獨列出，不會靜默剔除**',
            degraded: '🟠 某個因子的門檻現在分不出好壞 → 就地寫「這一項的門檻現在分不出好壞，**請不要把它當入選理由讀**」；排序會把這一項排除，並寫明。',
            unwired: null,
            error: '🔴 **選股中止：FinMind 額度用罄**\n為什麼：今日 API 呼叫已達上限，**非程式錯誤**；**本站不以殘缺資料湊出名單**\n去哪補：額度每日 00:00 重置；或到「📖 憑什麼 › 資料體檢」看 MOPS／Goodinfo 備援鏈是否可用\n資料時點：本輪取得失敗，畫面上的值可能是上一輪殘留\n\n⭐ **第二種紅態，文案與上面不同**：🔴 **選股跑完了，但回傳的東西讀不出「有幾檔」** —— `重跑不會好，這要改程式`＋「請把這一行連同上方那段說明回報給維護者」。'
          },
          asOf: null,
          evidence: '〔第四層〕展開＝`facts` 逐列：本次條件／顯示筆數／存活池／估值（本益比）N 檔／名稱欄／`aux_errors`／**L3 說明**。\n【實測 code 對位】`build_screen_result_card()`（`page_find.py:1197`）。它的 docstring 寫死了本卡最關鍵的兩條：\n(1) 「**`requested=` 與 `has_value=` 刻意是兩個不同的來源**」—— 前者是「使用者按過 submit」（session key `SS_APPLIED_SCREEN` `:360` 存在性），後者是「這一輪選出東西了沒有」（列數）。⚠️ 檔頭 `:87-90` 另外明文禁掉三種錯寫法：「**不是** `bool(cands_df)`、**不是** `not df.empty`、**不是** `len(rows) > 0` —— 那三種都分不出『還沒選』與『選了但 0 檔』」。\n(2) 「**「0 檔」與「讀不出列數」不是同一件事**」：`rows == 0` → `empty`（灰，有效結果）；`rows is None` → `MISS_CONTRACT_DRIFT` → L0 **升成紅態**。docstring 自陳修前兩者都被畫成「0 檔」，等於「**替上游宣稱一件它沒說的事**」。\n【三種「沒有結果」絕不可混】`page_find.py:101-106` 檔頭逐字：`還沒選 → UI_IDLE`／`選了、跑完、0 檔 → UI_EMPTY`（灰，有效結果）／`上游掛了 → UI_FAILED`（紅，**唯一准用紅色的狀態**）。並補兩個反方向的禁令：「**空結果不得冒充綠燈**」（`Card.__post_init__` 擋著：非 live 不准帶結論文字）與「把『還沒選』畫成紅色錯誤，等於**捏造一個不存在的故障**」（`CLAUDE.md §1.A` 第 4 點）。\n【本組實測】live 與 partial 兩格的「274／50／12/89／31/274／31 檔缺月營收」全部是**規格示意值，非實測**。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live／idle／partial／degraded 原分別註明 S1-3B §3.2.3／`SCREEN_IDLE_NOW/WHY/WHERE`（`page_find.py:571-576`）／§3.2.4 ⑥／§3.2.4 ⑦ 逐字。\n· loading 原文：⛔ **不留上一輪值。**\n· ⭐ **empty 那一格搬出來的是一整段提案，全文照收**：「**還有第 4 個原因，而它現在講不出來**：候選有資料、但**勾選因子過半沒資料**，被 `SCREENER_MIN_FACTOR_COVERAGE_RATIO` 涵蓋門檻擋下（`shared/signal_thresholds.py:583` ＝ 0.5）。⇒ **「沒有一檔符合」與「有檔但資料不足以排名」是兩件事**，建議把 `_n_below_coverage`（`fundamental_screener_service.py:379` 已算好）接出來寫成一列。★待拍板」。\n  ⚠️ **這個缺口沒有被縮小**：states.empty 仍留一句白話「還有一種情況目前分不出來：有候選，但因子缺資料太多而排不出名次 —— 現在會跟「0 檔」混在一起顯示」。搬走的只有常數名與檔案行號。\n· missing 原文：⛔ 不另開第四態。\n· error 原文：第二種紅態「回傳形態與約定不符」（`page_find.py:1288-1297` 逐字），⛔ 不可與上面共用文案。',
          src: 'src/ui/views/page_find.py:1197',
          flags: [] }
      ] },

    { n: 3, label: '第三層｜葉1 選股結果 · 大表／卡片流 ＋ CSV（互動清單）',
      blocks: [

        { key: 'find.screen_table', name: '葉1 選股結果 · 大表（桌機表格／手機卡片流）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '**欄位（6 欄）**：`代號｜名稱｜評分｜本益比｜殖利率｜缺貨`\n```\n│ 代號 │ 名稱   │ 評分 │ 本益比 │ 殖利率 │ 缺貨 │\n│ 2330 │ 台積電 │ 87.5 │  18.20 │   1.85 │ ✅   │\n│ 3037 │ 欣興   │ 82.1 │ ⚠︎ —   │   2.40 │ ✅   │\n│ 6488 │ 環球晶 │ 79.8 │   0.00 │   0.00 │ —    │\n```\n⭐ **同一格裡有三種「看起來像沒有」**：\n· 3037 本益比 `⚠︎ —` ＝ **來源沒給**\n· 6488 本益比 `0.00` ＝ 真的是 0（或極小），**是有效觀測**\n· 殖利率 `0.00` ＝ **這一期真的沒配**\n數值欄一律**等寬字 ＋ 右對齊**。\n✅ **整列可點 → 就地展開該檔的「入選理由」**；區塊標題右側的〔展開佐證 ▸〕**同時保留**（整列看這一檔，標題看整個區塊）。**卡片本身仍不可點。**\n⚠️ **這一段是線框，現在還按不動** —— 現行表格點列不會有任何反應。\n⚠️ **殖利率這一欄還有一個分不出來的地方**：來源對「沒配息」與「還沒公告配息」都給空值，本欄目前把兩者混在一起。',
            idle: '**整個表不出現**（沒有列可畫）；由上方總覽卡的 `⬜ 尚未選股` 負責說明。不會畫一個空表頭假裝有東西。',
            loading: '固定高骨架列 ×N（N＝上次筆數或預設 5 列），不顯示上一輪的實際值。',
            empty: '整個表不出現；由總覽卡的 `▨ 符合條件的標的是 0 檔` 負責。\n⭐ **但 CSV 鈕仍在** —— 0 列也是一個結果。',
            missing: '**逐格**：該格 `⚠︎ —` ＋ 該欄標題旁一顆共用的〔為什麼有些是空的 ▸〕。**不會整列剔除** —— 剔除等於你不會知道它入選過。',
            na: '該格 `N/A`（不加 ⚠︎ ——「不適用」不是「壞掉」）。例：**ETF 沒有本益比**。\n🔴 **這一態與「沒拿到」長得不一樣**：`N/A` ＝ 這個標的本來就沒有這個量；`⚠︎ —` ＝ 它該有，但這一輪沒拿到。',
            partial: '值帶 `*` 上標 ＋ 總覽卡列出缺的是哪幾檔（不會靜默剔除）。',
            degraded: '🟠 標在**該欄標題旁**（不是逐格）＋ 就地寫「這一項的門檻現在分不出好壞，**請不要把它當入選理由讀**」。',
            unwired: '該欄整欄**未接線**（還沒做）＋ 欄標題就地寫原因；**這一態沒有你可以做的事**（重按也不會變）。',
            error: '表不出現；由總覽卡的兩種紅態負責（上游掛了／回傳讀不懂）。'
          },
          asOf: null,
          evidence: '【斷點（S1-3B §3.2.3 逐字）】\n· **桌機 >1280**：表格全寬。\n· **平板 881–1280**：表格全寬保留；卡片列 3 欄。\n· **平板 641–880**：表格放進**橫向捲動容器**（⛔ **不縮字**）；卡片列 2＋1。\n· **手機 <640**：表格**轉卡片流**，一檔一卡，欄名左、值右，**仍等寬、仍右對齊**；卡底 `▸ 更多` 為 per-card 例外（2.4 兩級制）。\n⇒ 本 block 的 `cols` 給 1/1/1 是因為**它是一張表不是卡牆**；真正的響應式行為是「表 → 橫捲 → 卡片流」三段，⛔ 不是欄數變化。\n【`—` 這個字元在 code 裡的出處】`page_find.py:1163`（`_fmt_count()` 的 `None` 分支回 `"—"`）—— 也就是「數不出來」與「沒有」在**計數**語境共用 `—`。⚠️ 表格欄位的 `—` 是否走同一支，本組**未查證**。\n【實測 code 對位】表體是 `st.dataframe(_result.df, hide_index=True, width="stretch")`（`page_find.py:1596`）——**只有 `state == UI_LIVE` 才畫**（`:1590` `if _card.state != UI_LIVE: return`），這正是上表 idle／empty／error 三格寫「整個表不出現」的 code 依據，**不是本組的設計主張**。\n表上方另有一行 `st.caption("本次因子：" + (" · ".join(_result.hits) if _result.hits else "僅基本面四項全過"))`（`:1593-1594`）—— ⚠️ 注意它的 fallback 是「**僅基本面四項全過**」，不是空字串。\n🔴 **`N/A` 與 `—` 這組區分在全站是漂移的（`S1-4_STATE_MATRIX.md:558-572` 實測表，非本組推論）**：`src/ui/` ＋ `shared/` 底下至少 **10 支** `fmt_value` 形狀的函式對 `None` 各自回不同字面 —— `"—"`（`macro_buckets.py:689`、`page_find.py:1163`、`page_hold.py:1361,3719`、`page_inspect.py:1562,1567,1835`）／`"無資料"`（`macro_v2_cards.py:261`）／🔴 `"N/A"`（`etf_tab_single.py:931-932`，**正是 E-3 列為錯誤的寫法**）／🔴 **沒有 `None` 分支**（`unified_verdict_render.py:42` → 畫面會出現字面 `None`）。\n⇒ **本頁把 `⚠︎ —`（沒拿到）與 `N/A`（不適用）分開，是在一個全站尚未統一的地基上做的**。要落地就得同時釘住格式化來源，否則同一個 `None` 在隔壁頁會長成另一個樣子。⚠️ `S1-4` 自己標明該表是「**單組窮舉、未經第二組複驗，10 支是下限不是全部**」——本組原樣轉錄，不加碼。\n【本組實測】表內三檔（2330／3037／6488）與其數值全部是**規格示意值，非實測**。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live 原文（本次搬出的三段）：\n  (1) 欄位那一行原註「線框 §3.2.3 逐字 6 欄」；三種數值語彙原註「S1-3B §3.2.3 逐字」；殖利率那一行原帶「⛔ 不配息標的應為 `N/A`」。\n  (2) **整列可點的拍板來歷**：2026-09-14 **總管拍板**（客戶未表示偏好、授權總管決定）—— ⛔ **不是「客戶核准」**；標題觸發器與整列並存、⛔ 不互相取代；⛔ **卡片本身仍不可點**（客戶母法那一句未被推翻）。\n  (3) **現況做不到的 code 依據**：`src/ui/views/page_find.py:1596` 的表體是 `st.dataframe(_result.df, hide_index=True, width="stretch")`，**沒有 `on_select`** ⇒ 目前點列不會有任何反應。**拍板定的是線框長什麼樣，⛔ 不是宣稱它已接線。**\n  (4) **殖利率實測補正全文**：`src/data/stock/yield_pe_fetcher.py:79-81` 逐字：「BWIBBU_d 對**未配息／尚未公告配息**的公司 `DividendYield` **給空字串**（2026-08-05 實測前 410 檔中 **99 檔為空 ≈ 24%**）」。⇒ 規格示意的 `0.00` **在現行來源下不會自然產生**；真正會出現的是**空值**，而空值同時涵蓋「未配息」與「尚未公告」**兩件不同的事**（與本頁本益比同型）。**這一欄要不要比照本益比拆開，本線框未提案，據實登記。**\n  ⚠️ **缺口未縮小**：states.live 仍留一句白話「殖利率這一欄還有一個分不出來的地方：來源對「沒配息」與「還沒公告配息」都給空值，本欄目前把兩者混在一起」。\n· na 原文另註 S1-3B §3.2.7 矩陣「⑨ 不適用」欄逐字：「該欄 `N/A`（例：ETF 的本益比欄）」。\n· **flags 原文（本次縮成短標籤，全文照收）**：\n  ① 「總管拍板（客戶未表示偏好、授權總管決定；2026-09-14）★-02／DEV-19：本表**整列可點 → 就地展開**，標題觸發器並存 —— ⛔ 不得寫成「客戶核准」」\n  ② 「偏離現行 code 現況（`page_find.py:1596` 的 `st.dataframe` 無 `on_select`，現況點列無反應；拍板定的是線框，⛔ 不是宣稱已接線）」\n  ③ 「未決的連帶項（DEV-19 提出的 `▸` 就地展開 vs `↗` 離開本頁如何在整列可點時仍分得出來 —— 本次拍板**未處理**，⛔ 不得當成已解決）」',
          src: 'src/ui/views/page_find.py:1163',
          flags: ['總管拍板', '偏離現行 code 現況', '未決的連帶項', '未驗證'] },

        { key: 'find.pick_reason', name: '⭐ 入選理由（每一列為什麼會在這張表上）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '⚠️ **這一塊還是提案，現在還沒有** —— 現行畫面點列不會展開。\n\n**呈現方式：不新增欄，改用「總覽卡講因子命中幾檔 ＋ 逐列展開講這一檔」兩層。**\n(a) **總覽卡層（已存在）**：`本次因子：缺貨 12/89 · 抗跌RS 31/274` —— 講的是「這個因子在池子裡命中幾檔」。\n(b) **逐列層（桌機：**整列可點**，點任一列就地展開；手機：卡底 `▸ 更多`）**：\n```\n▸ 2330 為什麼入選\n  · 綜合評分 87.5（前 50 名之內）\n  · 命中因子：缺貨 ✅ · 抗跌 RS ✅ · 跨季轉強 ✅\n  · 未命中：估值（本益比 18.20，未進入前分位）\n  · 這一檔的四項基本面全過（存活池條件）\n  ── 這是「符合你勾選條件」的說明 ──\n```',
            idle: '不出現（沒有列）。',
            loading: '不出現（展開層不做骨架 —— 你點開之前它本來就不可見）。',
            empty: '不出現。⚠️ **但總覽卡那行說明要講出 0 檔是哪一種原因**（存活池空／因子全缺／空頭濾網剔光），那是「為什麼沒有人入選」。',
            missing: '某因子在這一檔沒有值 → 展開內該行寫「**〈因子〉：這一檔沒有資料，沒有計入綜合分**」。**不會寫成「未命中」** —— 沒有資料 ≠ 沒通過。',
            na: '某因子對這一檔不適用（例：ETF 的本益比）→ 該行寫 `N/A（這個標的沒有這個量）`，不加 ⚠︎。',
            partial: '展開內寫明「本檔綜合分由 **N/5** 個因子算出」＋ 列出是哪幾個。',
            degraded: '🟠 該因子那一行就地寫「這一項的門檻現在分不出好壞 —— **請不要把它當入選理由讀**」。⭐ 這是本塊存在的主要理由之一：`degraded` 的因子**看起來仍然是一個入選理由**，不當場講就會被讀成理由。',
            unwired: '該因子那一行**未接線**（還沒做）；**這一態沒有你可以做的事**。',
            error: '展開內容取不到 → 就地灰字「這一檔的入選明細這一輪取不到」；**該列仍然留在表上**。'
          },
          asOf: null,
          evidence: '🔴 **「入選理由」這個區塊在 `stock_ia_v1.html` 與 `S1-3B §3.2` 九節裡都沒有被單獨畫出來** —— 本 block 是把散在三處的既有材料**組起來**的提案：(a) 線框 live 原文的 `本次因子：缺貨 12/89 · 抗跌RS 31/274`；(b) `S1-3B §3.2.4 ⑦` 的「請不要把它當入選理由讀」（**「入選理由」四個字是規格自己用的詞**）；(c) `S1-3B §3.2.3` 手機卡片流的 `▸ 更多`（per-card 例外，2.4 兩級制）。⇒ **展開的載體是既有的，內容是新的** → ★待拍板。\n\n🔴 **【本組自己實測，非轉錄】桌機「點該列就地展開」現在做不到 —— 但技術上做得到，repo 裡有兩個現成樣板。**\n· `page_find.py:1596` 的表體是 `st.dataframe(_result.df, hide_index=True, width="stretch")` —— **沒有 `on_select`**，⇒ 目前**點列不會有任何反應**。本頁 `st.dataframe` / `st.data_editor` 只有這一處（`grep -n` 實測）。\n· `grep -rn "on_select" src/ --include=*.py` 的**真實呼叫**只有兩處，且**都不在 `src/ui/views/`**：`src/ui/tabs/tab_macro_v2.py:1271` 與 `src/ui/etf/etf_tab_dividend_station.py:493`，兩者皆為 `on_select="rerun", selection_mode="single-row"`。（`src/ui/views/page_hold.py:3605` 也命中，但那是 **docstring 裡的提及，不是呼叫**。）\n· `etf_tab_dividend_station.py:444-446` 的 docstring 自陳這正是「**選列就地展開**」的樣板。\n⇒ **結論分兩半**：(a) **現況做不到**，畫面上不得畫成已經有；(b) **不是做不到** —— 照那兩處樣板加 `on_select="rerun"` 即可，⛔ 但那是**新增互動元件**，依 `CLAUDE.md §-1.5` v3 §03-2 ① 要先出線框給客戶拍板（本 block 即該草稿）。\n✅ **手勢已拍板（2026-09-14）：整列可點 → 就地展開；區塊標題右側的〔展開佐證 ▸〕並存。**\n· **來歷（⚠️ 逐字照這樣寫）**：**總管拍板（客戶未表示偏好、授權總管決定）＋2026-09-14**。⛔ **不得寫成「客戶核准」「客戶已拍板」** —— 客戶對這題明示沒有偏好、授權總管決定，**他沒有核准這個結論**。\n· **理由（三條）**：(a) 客戶母法逐字只禁「⛔ **卡片本身**點擊不觸發全層展開」（轉引自 `S1-3_IA_WIREFRAME.md:1249`，**母法原件本組未見**）—— **表格的列不是卡片**；(b) 「⛔ 整列 `<tr>` 可點」的明文出處是 `S1-3_IA_WIREFRAME.md:77`（§1.2 規格表），該檔 `:7` 自陳產出日 **2026-09-14**、執行者「IA 線框組（**單組**）」⇒ **是規格自己加的，不是客戶講的**；(c) 手機上整列的觸控目標遠大於一顆小觸發器（§2.4 自己要求觸發器補到 44×44 CSS px，正說明小觸發器是需要特別補救的那一種）。\n⚠️ **同時據實記載的反面事實（⛔ 不得省略）**：**核准線框自己就不只一種手勢，而且同一個 block 內部就有兩種** —— `docs/wireframes/stock_ia_v1.html:859` 的 `n` 欄寫「3 欄燈格 · **點左表右側**就地展開」，**同一 block** 的 `:860` `live` 欄卻寫「…**點任一檔**，右側展開週線與布林 z」（屬 `PAGES[3]` 我的持股）。⇒ **「客戶到底核准了哪一種」本身就不乾淨**，⛔ 不是某一組單方面誤讀。\n⚠️ **被推翻的只有「整列」那半條**：同一列的「⛔ 卡片本身可點」「⛔ 標題可點」「⛔ hover 自動展開」「⛔ 卡片右上角小箭頭」四條 **⛔ 未被推翻，全部仍然有效**。\n🔴 **本次拍板⛔ 沒有解決的一個連帶問題（據實留著，⛔ 不得當成已解決）**：`DEV-19` 提出「整列可點時，使用者**看不出點下去是就地展開（`▸`）還是離開本頁（`↗`）**」。**本次拍板只定了手勢，沒有定這兩者怎麼區分** —— 該問題**仍然開著**，實作前必須另行處理（那是 DEV-19 當初把手勢反轉的**唯一實質理由**，手勢轉回來之後它並沒有消失）。\n⚠️ `etf_tab_dividend_station.py:446` 自陳「全 repo 目前只有那一處與本處用 `on_select`」—— 本組的獨立 grep **與它一致**，但兩者都是單一方法（grep），⛔ 不構成第二種方法的複驗。\n\n【實測 code：逐列理由目前拿不拿得到？本組**沒有查證**】`page_find.py:1253-1256` 只證實一件事：L3 `composite_rank_candidates` 會回一個 `note`，本頁**原樣透傳、不改寫**（`("L3 說明", result.note)`），且該 note 的內容是「缺貨/RS 未掃、涵蓋門檻、空頭濾網是否套用…」這種**整批層級**的說明。**逐列**的因子命中明細是否已在回傳的 DataFrame 裡，本組**未查**。\n⚠️ 另一條實測限制（`page_find.py:216-222`）：估值因子**全空**時，L3 的「因子實際覆蓋」那句話**不會印**（`composite_rank_candidates` 的 `_missing` 只收缺貨與 RS，而覆蓋率那句有 `if _col_scores[_f]` 的前提）⇒ 「估值全空」這句話**只能本頁自己講**。**同一個結構問題會直接打到逐列理由**：如果逐列理由也靠 L3 的 note，全空的因子在逐列層也會「安靜地消失」。\n\n【🔴 合規 —— 本塊是本頁最容易踩線的地方，判準寫死】\n· ✅ 可以寫：「**這一檔符合你勾的哪幾個條件**」「綜合評分 87.5，排在前 50 名之內」「命中因子：缺貨 ✅」\n· ⛔ 不可以寫：「**值得買**」「便宜」「甜蜜價」「逢低」「建議持股 X%」「目標價」「必漲」「推薦」\n· ⛔ 尤其不可以：把「入選理由」寫成「**買進理由**」。差別在於：入選 ＝ 通過了**你自己設的篩子**；買進 ＝ 還要代入**只有你知道的**可動用資金／持有期間／風險承受度／既有部位。**畫面替使用者跨過那一步，就是買賣建議。**\n· ⚠️ 結尾那句「這是「符合你勾選條件」的說明，⛔ 不是買賣建議」**應常駐在展開內**，⛔ 不放頁尾（頁尾免責離這裡太遠，讀的人看不到）。\n\n🔴 **【合規稽核覆蓋率的誠實揭露 —— 本頁是已知的盲點】** `S1-6_COMPLIANCE_COPY_GUIDE.md:1332`（§7.3「本文**結構上**看不到的東西」表）逐字列出一列盲點：「**v2 五頁的 `page_find.py` / `page_inspect.py`** ｜ 乙組自陳「**基本沒有逐段讀**」｜ 另派一組專讀 v2」。\n⇒ **本頁的既有文案沒有被合規組逐段掃過**，也就是說「本頁目前沒有違規文案」這件事**沒有人驗過**，⛔ 不得假設它乾淨。同表另有一列直接打到本 block：「**`st.dataframe` 儲存格內容**｜乙組自陳其 AST sink 掃描只取第一個參數的表達式，**DataFrame 欄值是 runtime 的**」⇒ **大表格子裡的字串，靜態掃描本來就掃不到**，要靠跑一次 app ＋ dump DataFrame 才看得到。\n⚠️ 本 block 的合規判準是**本組寫的**，不是合規組審過的。**送 ★ 一併拍板，並建議在實作前補一輪 runtime dump。**\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live 原文（本次搬出）：「**本格的「內容」仍是提案（★待拍板），⛔ 不是現況**」「**但「手勢」這一半已於 2026-09-14 拍板：整列可點 → 就地展開**（總管拍板；客戶未表示偏好、授權總管決定 —— ⛔ **不是「客戶核准」**）。**兩件事⛔ 不要混**：**這個區塊要不要存在** 仍待拍板；**它一旦存在要用什麼手勢打開** 已定。」；例示區末行原為「這是「符合你勾選條件」的說明，⛔ 不是買賣建議」。\n· idle／loading／empty／missing／partial／na／unwired／error 原文帶 ⛔ 祈使句：⛔ 不出現；⛔ 不得寫成「未命中」；⛔ 不得只寫「部分因子」；⛔ **不得**因此把該列從表上拿掉；empty 那格原寫「總覽卡的「L3 說明」必須講出 0 檔是哪一種原因」。\n· **flags 原文（本次縮成短標籤，全文照收）**：\n  ① 「★待拍板（**這個區塊要不要存在**仍未定 —— 它在核准線框與 S1-3B 都沒被單獨畫出來）」\n  ② 「總管拍板（客戶未表示偏好、授權總管決定；2026-09-14）★-02／DEV-19：**手勢**這一半已定 —— 整列可點 → 就地展開，標題觸發器並存；⛔ 不得寫成「客戶核准」」\n  ③ 「未決的連帶項（DEV-19 的 `▸` vs `↗` 如何在整列可點時分辨 —— 本次拍板**未處理**）」',
          src: null,
          flags: ['★待拍板', '總管拍板（手勢）', '未決的連帶項', '偏離核准線框', '未驗證', '單組結論'] },

        { key: 'find.csv', name: '⬇ 下載結果 CSV（form 外）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '〔 💾 下載選股結果 CSV 〕　← **form 外**（`page_find.py:1603`）\n檔名規則：`〈頁名〉_〈區塊名〉_〈資料歸屬日〉.csv`，例：`找標的_選股結果_2026-09-13.csv`（用資料的日期，不是下載的時間）。',
            idle: '**停用但不隱藏** —— 隱藏會讓你以為這頁沒有匯出功能。',
            loading: '停用。',
            empty: '**停用但不隱藏** —— **「我跑過、結果是 0」本身就是一個要留存的結果**。',
            missing: null, na: null,
            partial: '可按；⚠️ **CSV 內會保留缺值標記** —— 畫面上標過缺的，檔案裡也一樣是缺的。',
            degraded: '可按；⚠️ CSV 內該欄會**另帶一欄註記**，或在檔頭註明該因子的門檻已失準。',
            unwired: null,
            error: '停用（沒有可匯出的結果）。'
          },
          asOf: null,
          evidence: '【實測 code 對位】`st.download_button("💾 下載選股結果 CSV", data=_csv, …)` 在 `_render_screen_leaf()` 內、`st.form` 之外（`page_find.py:1603`）。\n⚠️ **鈕的字串兩份不一致（實測）**：線框／`S1-3B` 寫「**⬇ 下載結果 CSV**」，production 寫「**💾 下載選股結果 CSV**」。⇒ 兩者擇一，**要拍板**；⛔ 不得兩邊各留一份（那正是 `CLAUDE.md §2.1` SSOT 要防的第二真相源）。\n【為什麼一定在 form 外】F11 實測：`st.button()`／`st.download_button()` 放進 `st.form()` 即拋 `StreamlitAPIException`（form 內只能有 submit）。`page_find.py:59` 檔頭與 `:1506` docstring 兩處都寫死了這條約束。\n【檔名規則出處】`S1-3B` Part 6.3 / G-24（`S1-3B_FULL_DRAFT_SPEC.md:2885` 逐字）。\n🔴 **實測：production 現行檔名不符 G-24**。`page_find.py:1604` 逐字 `file_name="screener_result.csv"` —— 是一個**寫死的定檔名**，⛔ 沒有頁名、沒有區塊名、**沒有資料歸屬日**。⇒ 使用者連續下載兩天會得到兩個同名檔，**看不出哪個是哪天的**（而這正是 G-24 訂這條規則的理由）。**待修**，且修法受 G-24 拘束：日期要用**資料歸屬日**，⛔ 不得用抓取時間。\n【另一處實測】CSV 產生失敗時走 `except` → `print` ＋ `st.caption("（CSV 匯出暫不可用：…）")` ＋ `return`（`page_find.py:1597-1601`）—— ⭐ **做對了兩件事**：(a) 不炸掉整張表（註解自陳「下載不可用不該炸掉整張表」）；(b) 例外訊息先過 `scrub_state_glyphs()` 再顯示，避免例外字串裡的字元被誤讀成狀態符號。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live 原文：檔名規則出處為 **G-24 全站**；⛔ **不用抓取時間當檔名**。\n· idle／empty 原分別註明 S1-3B §3.2.1「無資料時停用但不隱藏」／§3.2.5「0 列也是一個結果」逐字，並帶一句「這是本鈕唯一會被誤刪的一格」。\n· partial 原文：⛔ 不得在匯出時把 `⚠︎ —` 變成空白或 0 —— 那是把畫面上誠實標過的東西在檔案裡洗掉。',
          src: 'src/ui/views/page_find.py:1603',
          flags: ['★待拍板', '未驗證'] }
      ] },

    { n: 4, label: '第四層｜展開佐證（⭐ 本頁的重點：本益比的兩種「沒有」）',
      blocks: [

        { key: 'find.pe_two_kinds', name: '🔴 為什麼有些本益比是空的（PE 兩種「沒有」的分辨）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '〔 ▸ 為什麼有些本益比是空的 〕（常駐可展開）\n\n**現況**：\n```\n  ▸ 為什麼有些本益比是空的\n     本欄目前**分不出**下面兩種情況，所以一律顯示 ⚠︎ —：\n       ① 本益比 ≤ 0（**非正值** —— 這是可信的、而且很重要的資訊）\n       ② 來源這一輪沒給（**不知道**）\n     這兩件事本來不該共用同一個符號，但**現在分不出來**。\n     在來源給出區別之前，本欄只能誠實標成「分不出來」，不會替你猜是哪一種。\n```\n\n**⭐ 提案（還沒做）—— 兩種「沒有」分開顯示**：\n· `⚠︎ —`　＝ **來源沒給**（不知道）　→ 展開：「TWSE／TPEX 這一輪沒有給這一檔的本益比」\n· `⚠ 非正值` ＝ **本益比 ≤ 0**（只知道它 ≤ 0，不知道為什麼）→ 展開：「本益比為非正值，已排除在估值分之外」\n· `N/A`　　＝ **不適用**（ETF 沒有本益比）→ 不加 ⚠︎\n⇒ 三個符號，三件事，一個都不合併。',
            idle: '**常駐** —— 它解釋的是這一欄是什麼意思，不是這一輪的結果。',
            loading: '常駐',
            empty: '常駐',
            missing: '常駐（這一態正是它要解釋的東西）',
            na: '常駐',
            partial: '常駐 ＋ 就地補一行「這一輪有 N 檔拿到本益比」',
            degraded: '常駐',
            unwired: '常駐。⚠️ **本欄現在已經接線**，所以這一格目前沒有實例。',
            error: '常駐 ＋ 就地補「這一輪估值資料取不到 —— 〈錯誤訊息〉」'
          },
          asOf: null,
          evidence: '🔴 **這是客戶本輪明文交辦的重點。以下把「規格怎麼寫／code 怎麼做／措辭為什麼要改」三件事分開講。**\n\n【(1) 目前這兩種真的分不出來嗎 —— 實測，不是轉錄】\n· `page_find.py:1248` 逐字：`"≤0 的不算，那是缺值不是估值"` —— 也就是 **PE ≤ 0 在計分時被當成缺值處理**。\n· `page_find.py:554-560` `PE_EMPTY_WHY` 逐字：「本輪拿到的本益比對照表是空的（上市 TWSE 與上櫃 TPEX 兩支 OpenAPI 都沒有給資料）—— 於是**每一檔都沒有估值分**。這不是「這些股票都很貴」，是**沒有數字**」。\n⇒ 兩條路（≤0、來源沒給）**最後都變成「這一檔在 `pe_map` 裡沒有可用值」**，畫面上都顯示 `⚠︎ —`。**客戶說的「目前長得一樣」成立。**\n🔴 **【本組已補查 L1，答案在這裡 —— 兩種「沒有」是在 L1 合流的，不是 L5】**\n`src/data/stock/yield_pe_fetcher.py:228-229` 逐字（本組 `grep` 實測命中）：\n```python\nif _pe != _pe or _pe <= 0:      # NaN / ≤0 → 無本益比,不放 key\n    continue\n```\n⇒ **NaN（來源沒給）與 `≤0`（非正值）走同一個 `continue`，兩者都是「這個 key 不存在於 `pe_map`」** —— 到了 L5 已經**沒有任何資訊可以分辨**。`page_find.py:1248` 寫的「≤0 的不算，那是缺值不是估值」只是**忠實反映 L1 已經做掉的事**，不是 L5 自己再丟一次。\n⇒ **要分辨，就得改 L1**（多回一個旗標，例如 `pe_nonpositive: set[str]`），⛔ **L5 改不出來**。這是給實作批次的交接結論。\n\n🔴 **【而且「虧損」這個詞在 L1 就已經不成立 —— 這是客戶指示的技術依據】** `S1-2_METRIC_SSOT.md:175-200` 的落差分析第 3 點逐字：「**判定邊界是 PE ≤ 0，不是 EPS ≤ 0。** 兩者在數學上等價（price > 0 時），但 code 裡**沒有 EPS 這個輸入**，所以**無法區分「虧損」與「來源空值」**。」\n· 同處第 1 點：本站**不自己算 PE** —— `fetch_pe_name_maps()` **直接取用交易所公告值**（上市 `openapi.twse.com.tw/v1/exchangeReport/BWIBBU_d` 的 `PEratio`；上櫃 `tpex_mainboard_peratio_analysis` 的 `PriceEarningRatio`）。**「TWSE 的 PEratio 口徑本站沒有宣告」**。\n· 同處第 3 點另註：那個 `<= 0` 檢查的原意只是「**對來源改用 0 佔位的防呆**」（TWSE 給空字串、TPEX 給 `\'-\'`，實務上已是 NaN）。\n⇒ **系統手上從頭到尾只有一個交易所算好的數字，沒有 EPS、沒有損益表。說「虧損」是替交易所與公司宣稱一件它們沒說的事。客戶要求的「非正值」在技術上是唯一講得出口的措辭。**\n⚠️ **母法（S1-2 §1.3「母法要求」）其實寫的是「EPS ≤ 0 統一標註 `N/A（虧損）`」** —— 也就是**母法要的標籤現行 code 支撐不起**。⇒ 客戶這次的「只寫非正值」等於**把母法往 code 講得出口的方向收斂**，是**收緊、不是放寬**，本線框照辦。\n\n✅ **順帶記錄一個 L1 已經做對的防禦（⛔ 不要在改造時弄壞它）**：`S1-2 §1.3` 落差第 4 點 —— `pe_low` 因子是「**值越小分越高**」，若讓 `0`／負數進榜，**非正值的標的會被排成「全市場最便宜」**。現行的 `continue` 正是擋住這件事的那一行。⇒ 未來若改成「帶旗標進來」，**排序端必須同時擋**，⛔ 不得只加旗標就把值放進去。\n\n【(2) 🔴 措辭必須改：「虧損」→「非正值」（客戶明文交辦，本組據 code 複核成立）】\n`S1-3B §3.2.3` 原文寫「① 公司在虧損（本益比 ≤ 0 —— 這是可信的、而且很重要的資訊）」。**那句話說多了**：\n· 取數層拿到的是一個**數字 ≤ 0**（或拿不到）；`page_find.py:1248` 也只寫「≤0 的不算」。\n· PE ≤ 0 **不等於**虧損 —— 分母（EPS）為負是一種，但 **EPS 極小導致 PE 失真、或上游把「無此值」編碼成 0／負數**同樣會落在 ≤0。畫面寫「虧損」＝ **替上游宣稱一件它沒說的事**（`CLAUDE.md §1`：錯誤的數字比沒有數字更危險）。\n⇒ **本線框一律寫「非正值」，⛔ 全頁不得出現「虧損」二字**。上面 live 格已照此改寫（⚠️ **這是本線框對 `S1-3B §3.2.3` 原文的一處刻意偏離**，據實標在此）。\n\n【(3) 提案在 code 尚未落地 —— 三件事都還沒有】\n· ⛔ 沒有「非正值」的符號／樣式（`page_find.py` 全檔無此字串，本組 grep）。\n· ⛔ 沒有把 `≤0` 與 `缺值` 分開的旗標（`:1248` 的處理是把兩者**合流**）。\n· ⛔ 沒有 ETF `N/A` 的分支（`S1-3B §3.2.7` 矩陣有寫「⑨ 不適用 → 該欄 `N/A`（例：ETF 的本益比欄）」，但本組**未在 code 中找到對應實作**）。\n⇒ 三項全部 ★待拍板 ＋ 待實作。**拍板前，畫面只能維持「一律 `⚠︎ —` ＋ 誠實說分不出來」** —— ⛔ 不得先上一個猜的分類。\n\n🔴 **【(3.5) 這件事的後果比「一格顯示成 —」嚴重得多 —— 缺 PE 會讓整檔失去參賽資格】**（本組實測，兩處互相印證）\n· `src/data/stock/yield_pe_fetcher.py:79-84` docstring 逐字：未配息／尚未公告配息的公司 `DividendYield` 給空字串（**2026-08-05 實測前 410 檔中 99 檔為空 ≈ 24%**），修前的 `dropna` 會把整列丟掉，後果是「這些股會變成『無估值分 ＋ 名稱空白』——**不是「PE 貴被排後面」，而是「沒有參賽資格」**，還可能因 `SCREENER_MIN_FACTOR_COVERAGE_RATIO` 涵蓋門檻**被整檔擋出榜單**」。\n· `src/services/fundamental_screener_service.py:370-378` 實測：綜合分只平均「該股有資料的因子」，但需**勾選因子過半有資料**才進榜；`SCREENER_MIN_FACTOR_COVERAGE_RATIO = 0.5`（`shared/signal_thresholds.py:583`），註解寫明門檻是「**嚴格 >：3 勾需 ≥2、2 勾需 2、1 勾需 1**」。\n⇒ **使用者看到的「這檔不在名單上」，可能不是「它分數低」，而是「它缺的資料太多，連算都沒算」。** 這兩件事在畫面上**目前完全分不出來**，而後者才是誠實的答案。\n✅ **好消息：這個數字已經被算出來了**。同檔 `:379` 有 `_n_below_coverage = 0  # 有資料但不足過半 → 被涵蓋門檻擋下(可觀測性 §5)` —— **「被涵蓋門檻擋掉幾檔」是現成的**，接出來即可顯示。⇒ **建議在總覽卡的 `facts` 加一列「因資料不足未參與排名：N 檔」**（★待拍板，屬欄位增減）。\n⚠️ **另一個已被 code 自陳的不一致（照錄，不加判定）**：同 docstring 末句 —— 上櫃版 `fetch_tpex_yield_pe`「**早已刻意不做這個 dropna**」⇒「**同一個因子，上市／上櫃兩套母體規則（§2.1 SSOT 不一致）**」。\n\n【(4) 為什麼這件事值得一個獨立 block】`⚠︎ —` 這一格現在同時承載**三種語意**（來源沒給／PE≤0／不適用），而其中一種（PE≤0）**是有效觀測、而且是使用者最想知道的那一種**。把有效觀測畫成「沒有資料」，與把「沒有資料」畫成 0 是同一種說謊的鏡像 —— 前者是**藏掉一個真實訊號**，後者是**捏造一個不存在的訊號**。`CLAUDE.md §1` 兩邊都禁。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· live 原文：現況那段原註「S1-3B §3.2.3 逐字，只改一處措辭」；提案那段原註「★待拍板，code 尚未落地」；非正值那一行原符號為 `⛔ 非正值`（本次改為 `⚠ 非正值`，⚠️ **這是本組改的符號**，理由：⛔ 在本檔其餘位置一律代表「禁止」，用在一個資料狀態上會混義 → 送拍板）；末行原文「⇒ 三個符號，三件事，⛔ 一個都不准合併」。\n· partial／error 原文出處：`page_find.py:1240-1242` 的 `facts` 那一列／`PE_FAILED_WHY_HEAD` `page_find.py:547` 逐字。\n· unwired 原文：2026-09-07 FE-18 已接線。',
          src: 'src/ui/views/page_find.py:1248',
          flags: ['★待拍板', '偏離核准線框', '單組結論'] }
      ] },

    { n: 5, label: '葉2｜板塊地圖（＝產業熱力圖＋板塊資金潮汐）—— 兩張圖各自判態',
      blocks: [

        { key: 'find.map_cta', name: '葉2 CTA：🗺️ 載入板塊地圖', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '▌ 🗺️ 載入板塊地圖 ▐　→　左：產業漲跌熱力圖　右：三大法人資金流向泡泡圖',
            idle: '鈕常駐可按；下方兩張圖各自顯示自己的灰態（兩張圖不共用同一段文字）。',
            loading: '鈕停用。兩張圖**各自一個載入框** —— 只給一個全頁轉圈，會讓先好的那張看不到。',
            empty: null, missing: null, na: null, partial: null, degraded: null, unwired: null,
            error: '鈕仍可按（可重試）；錯誤由下方各圖自己標。'
          },
          asOf: null,
          evidence: '【實測 code 對位】`st.button(ACTION_LOAD_MAP_LABEL, key=SS_MAP_BUTTON, type="primary")`（`page_find.py:1621`）；標籤常數 `ACTION_LOAD_MAP_LABEL = "🗺️ 載入板塊地圖"`（`:423`）；gate 旗標 `SS_MAP_REQUESTED`（`:363`）—— 檔頭 `:88-90` 明文：「那個 key **只有**「🗺️ 載入板塊地圖」的 `st.button` 分支會寫」，且「`requested=False` 時本檔一行 L3 都不呼叫」（`:99`）。\n⚠️ **改名風險交接註記（S1-3B §3.2.0 ＋ 核准線框 N6 補，兩處同文）**：「🗺️ **載入板塊地圖**」**是新名字**；production 舊分頁現行是「🗺️ **載入產業熱力圖**」（`etf_render.py:836`，`key=\'heatmap_load\'`）。本 IA 把兩張圖併成一葉，那顆舊 gate 會被吸收掉 —— 新按鈕要涵蓋兩張圖，**而且任何指去舊名字的文案都要跟著改**。⚠️ 本組**未查證** `etf_render.py:836` 這個行號是否仍正確（轉錄自規格）。\n【S1-3B §3.2.9 第 3 項】兩張圖合併 gate 之後：**泡泡圖讀快照不需重抓、熱力圖需要** ⇒ 兩者的「載入中」時間不同，⛔ **不得共用一條進度文字**。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「葉2 CTA：🗺️ 載入板塊地圖（單顆 st.button，⛔ 不在 form 內）」—— 括號內容為實作約束，⛔ 該鈕不得被包進 `st.form`。\n· loading 原文：⛔ **不得只給一個全頁 spinner**（S1-3B §3.2.2 逐字）。\n· idle 原文：⛔ 不共用一段文字。',
          src: 'src/ui/views/page_find.py:1621',
          flags: ['未驗證'] },

        { key: 'find.heatmap', name: '葉2 左｜產業漲跌熱力圖', leaf: 'l2',
          cols: { desktop: 2, tablet: 1, phone: 1 },
          states: {
            live: '產業漲跌熱力圖（依類股代表批次取數）',
            idle: '⬜ **板塊資料尚未載入**\n為什麼：熱力圖要**批次抓數十檔類股代表**\n去哪補：點「🗺️ 載入板塊地圖」',
            loading: '**自己一個固定高骨架框**（與泡泡圖各自一個，不共用）。',
            empty: '▨ 本輪一檔類股代表都沒取到（跑完了，但沒有東西可畫）。',
            missing: '同 empty。',
            na: null,
            partial: '⚠️ **N/M 類股取到** ＋ 就地列出缺的是哪幾類。不會只畫拿到的那幾格假裝完整。',
            degraded: null,
            unwired: '（本圖**已經接線**，所以這一格目前沒有實例。）',
            error: '🔴 **熱力圖無法取得任何類股資料**\n為什麼：批次抓取全數失敗\n去哪補：先確認網路連線；細節在「📖 憑什麼 › 資料體檢」'
          },
          asOf: null,
          evidence: '【實測 code 對位】`load_heatmap(*, requested: bool)`（`page_find.py:1105`，docstring：「**未請求 → 一行 L3 都不呼叫**」）＋ `build_heatmap_card()`（`:1308`）。\n⚠️ **本頁的熱力圖沒有市場／區間選擇器（誠實揭露，不是漏做）** —— `page_find.py:109-116` 檔頭逐字：核准線框葉2 的 live 原文只有**一顆**按鈕 ＋ 左右兩張圖，**沒有畫任何下拉**；「加兩個 selectbox ＝ 版面元件增減，依 `CLAUDE.md §-1.5` v3 §03-2 ① 屬『要先出線框給客戶拍板』的那一類，**不是**內部自決」。故本頁固定畫 `HEATMAP_IS_US` / `HEATMAP_PERIOD_LABEL` 一組，並在卡上寫明「要切換市場或區間請到 🌍 市場環境 › 🗺️ 產業熱力圖」（**兩邊仍並存**）。\n🔴 **這句「兩邊仍並存」直接撞上 `CLAUDE.md §-1.5.D` v3 §01-2「嚴禁…兩個重複的總經 Tab／同一資料來源全站只能有一處取數實作」** —— 本組**只記錄這個事實，不作判定**（是否構成 v3 所禁的「重複」，依 `§8.3` 既有登記須由**獨立一組**查證，⛔ 不可由想動手的那一組自己查）。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· idle 原出處：`MAP_IDLE_NOW/WHY/WHERE`，`page_find.py:582-587`。\n· loading 原文：⛔ 不與泡泡圖共用 spinner。\n· partial 原出處：S1-3B §3.2.7 矩陣「⑥ 涵蓋不全」欄逐字；原帶「⛔ 不得只畫拿到的那幾格假裝完整」。\n· unwired 原文：`build_heatmap_card()` `page_find.py:1308` docstring 自陳「**2026-09-07 已接線**」。\n· error 原出處：核准線框 `stock_ia_v1.html` 葉2 `err` 逐字。',
          src: 'src/ui/views/page_find.py:1308',
          flags: ['單組結論'] },

        { key: 'find.sector_flow', name: '葉2 右｜三大法人資金流向泡泡圖', leaf: 'l2',
          cols: { desktop: 2, tablet: 1, phone: 1 },
          states: {
            live: '三大法人資金流向泡泡圖（四象限）',
            idle: '⬜ **板塊資料尚未載入**\n為什麼：泡泡圖讀的是**每個交易日盤後凍結的快照**（跟左邊那張圖的原因不一樣）\n去哪補：點「🗺️ 載入板塊地圖」',
            loading: '**自己一個固定高骨架框**。⚠️ 它讀快照**不用重抓**，通常比熱力圖快很多，所以兩張圖的進度文字不共用。',
            empty: '▨ **當日盤後任務尚未產生快照**\n⭐ **這不是紅態，也不是「還沒點」** —— 沒有人壞掉，只是**東西還沒生出來**。',
            missing: '同 empty。',
            na: null,
            partial: '部分象限沒有資料 → 就地標明；空的象限不會畫成 0。',
            degraded: '⚠️ **快照是舊的** → 就地寫出它為什麼是舊的（來源沒給原因時就寫「上游沒有給原因」）。**不會靜默拿舊快照當今天的值。**',
            unwired: null,
            error: '🔴 讀不到盤後凍結的快照 ＋ 來源給的原因原文。'
          },
          asOf: null,
          evidence: '【實測 code 對位】`load_sector_flow()`（`page_find.py:1017`，docstring：「**未請求 → 一行 L3 都不呼叫**」）＋ `build_sector_flow_card()`（`:1377`）＋ `_quadrant_counts()`（`:998`）。`SectorFlowReadout` 帶 `reason`（`:976`）與 `stale_reason`（`:979`）兩個**不同**欄位 —— 對應「讀不到」與「讀到了但是舊的」兩件事，⛔ 不可合併。\n【它從 session 讀兩個上游 key】`SS_ETF_PORTFOLIO_ROWS`（`:407`）／`SS_STOCK_SHEET_ID`（`:409`）。⚠️ `page_find.py:401-406` 記載這不是本檔自開的取數路徑：L3 `sector_flow_service` 的 docstring 明文「**L5 只負責從 session_state 取 ETF 組合的 ticker + 個股 sheet_id 傳入**（session_state 是 UI 專屬，只能在 L5 讀）」。\n🔴 **時點列必須標 `MM-DD 盤後`**（S1-3B §3.2.9 第 2 項）—— 泡泡圖**不是即時值**，它讀的是每交易日盤後凍結的快照。⛔ 不得與熱力圖共用一個時點。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· idle 原文：⛔ 與熱力圖不同原因，分開寫。\n· loading 原出處：S1-3B §3.2.9 第 3 項。\n· empty 原出處：`page_find.py:1025` docstring 逐字。\n· partial 原文：⛔ 不得把空象限畫成 0。\n· degraded 原文與 code 對位：`page_find.py:1062` 有 `stale_reason` 欄；`:1420` 會把它印出來，`上游沒有給原因` 為其 fallback；⛔ 不得靜默拿舊快照當今天的值。\n· error 原出處：`page_find.py:1430`。',
          src: 'src/ui/views/page_find.py:1377',
          flags: [] },

        { key: 'find.map_scale_disclosure', name: '⭐ 葉2 口徑揭露（常駐小字）', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: 'X＝近 5 日累計淨流入（億）· Y＝動能變化 · 泡泡＝近 20 日淨額規模。**面積不等於權重。**',
            idle: '**常駐** —— 你在**看圖之前**就該知道這張圖在量什麼。',
            loading: '常駐', empty: '常駐', missing: '常駐', na: '常駐',
            partial: '常駐', degraded: '常駐', unwired: '常駐', error: '常駐'
          },
          asOf: null,
          evidence: '【出處】核准線框 `docs/wireframes/stock_ia_v1.html` `id:\'find\'` 第 4 個 block（`{t:\'葉2 口徑揭露\', n:\'caption · 常駐\', laws:[\'e\']}`）；`S1-3B §3.2.1` 葉2 冷啟動圖末段同文；`S1-3B §3.2.8` 揭露表第 1 列標 **✅ 常駐**（代號 D-7）。\n⭐ **「面積不等於權重」為什麼是紅線**：泡泡圖的面積會被直覺讀成「這個板塊比較大／比較重要」，而它實際量的是**近 20 日淨額規模**。不寫這一句，圖本身就在誤導 —— 這是 `CLAUDE.md §1` 在圖表上的同型問題（對照 v3 §02「加權指數成交量全 0 → 隱藏該欄，不畫假地平線」）。\n【S1-3B §3.2.7 矩陣】本列**十個狀態欄全部寫「常駐」** —— 是全表唯一一列如此，⛔ 不得在任何狀態下拿掉。\n\n【文案瘦身 2026-09-14｜從 states／name／flags 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「⭐ 葉2 口徑揭露（常駐 caption，⛔ 不隨狀態消失）」。\n· live 原出處：核准線框 `stock_ia_v1.html` ＋ `S1-3B §3.2.1` 兩處逐字一致。\n· idle 原文為核准線框原話：「口徑說明常駐，不隨狀態消失 —— 使用者**看圖之前**就該知道這張圖在量什麼」。',
          src: null,
          flags: [] }
      ] }
  ]
});
