/* ═══════════════════════════════════════════════════════════════
 * 修訂紀錄｜2026-09-14 文案瘦身組（客戶親自審稿後的回應）
 * ═══════════════════════════════════════════════════════════════
 * 客戶原話：「『資料時點：來源未提供資料日期 △ 不得猜、不得填今天、不得留白』
 * 重複數十次 —— 這是規格，不是 UI。」＋「誠實揭露 ≠ 用散文解釋所有事」
 * ＋「有些頁面寫『本頁目前沒有主 CTA』，新手會迷失。」
 *
 * 本輪做的事：
 *
 * 1. 把「講給寫程式的人聽的話」從會被畫出來的欄位搬進 evidence。
 *    動到的是：`subtitle`、5 個 `layers[].label`、22 個 block 的 `states`／`name`。
 *    **flags 本檔原本就是短標籤，一個字都沒改**（機器複驗 `_verify.js`：
 *    FLAGS／NULLNESS／cols／asOf／src **全部 0 變動**）。
 * 2. 搬走的是：§ 條號與「逐字」出處（S1-3B §3.5.x、S1-4 §2.3 C、★-07a、
 *    核准線框 DECISIONS #9）、⛔／不得／不准 祈使句、以及**大量 code 內部概念**
 *    （L0／L2／L3／L5 分層代號、`@monitored`／`fetcher`／`session`／`import`／
 *    `st.session_state`／`@dataclass`／`discriminative=False`／`wired=False`／
 *    `MISS_NO_INPUT`／`threshold_display`／`build_*_card()`／`data_registry.py`／
 *    `reconcile.py`／`api_diagnostic.py`／`GEMINI_API_KEY`／檔案行號）。
 *    **全部原文收進 evidence，一句都沒有丟。**
 *    ⚠️ 本檔是五頁裡 code 術語最密的一頁（它本來就在解釋系統怎麼運作），
 *    白話化時一律採「**同義替換**」而非「刪掉」：例如
 *    `fetcher` → 「來源／取數」、`session` → 「這次瀏覽」、
 *    「登錄表」→「名單」、「快取命中」→「吃快取」。**語意未變，術語進 evidence。**
 *
 * 3. 🆕 **本頁沒有主 CTA，本組加了一句白話指路（★待拍板，見 why.statusbar evidence）**：
 *    `mainCTA: null` 是**實測結果**（`grep -c form_submit_button` → 0），
 *    ⛔ **本組沒有發明任何按鈕**。做法是在 `subtitle`（頁標題正下方、主 CTA 面板
 *    之上）加一句：「這一頁是用來查的，沒有『開始』按鈕 —— 想知道今天能不能出手，
 *    到「🚦 今天」…這一頁三個分頁都可以直接看：教學·資料體檢·AI 問答（送出就開始）。」
 *    · 三個頁名與三個葉名**全部抄自本檔既有內容**（`leaves` 的 l1/l2/l3）；
 *    · 「送出就開始」與 `why.qa.idle` 既有文案「送出本身就是啟動」同義，⛔ 非新承諾；
 *    · ⚠️ **這是本組提案，未經客戶或總管拍板**；`why.statusbar` 的 flags 已帶 `★待拍板`。
 *    · ⚠️ renderer 那句「本頁目前沒有主 CTA」由 `warroom_ia_v2.html:601-609` 印出，
 *      **本組未動**；要改那句話本身須由 renderer 那一組處理。
 *
 * 4. ⛔ 沒有把任何缺口寫小。三處特別點名（原文全在 evidence，白話版仍在 states）：
 *    · `why.edu.health6`：六因子配分**說不出來**、「這一格空著是誠實的」—— 全留；
 *    · `why.source.unmeasured.finmind_quota`：「這一批沒有做到」「不宣稱做得到也不
 *      宣稱做不到」—— 全留；
 *    · `why.source.coverage`：「**這面牆上沒有紅燈，不等於全站都好**」「只有 9 支掛了
 *      監控」—— **一字未動**。
 *    未接線一律仍寫「未接線（還沒做）」，⛔ 未改成「暫無資料」「暫時沒有」。
 * 5. asOf 仍一律 null（硬規則 3），⛔ 未填今天。
 * 6. 量測（本組，`_verify.js`）：states 11,483 → 8,077 字（−3,406，−29.7%）；
 *    name 481 → 454 字。搬進 evidence 共 22 個 block、刪除 0 句（全部搬運，無丟棄）。
 *
 * ⚠️ 客戶點名的那句重複數十次的文字**不在本檔**：由 renderer 印出
 *    （`warroom_ia_v2.html:487`，`blk.asOf` 為 null 時每個 block 固定印一次；
 *    規則本文另存 `wf_global.js:123`）。兩檔均由別組維護，本組**未動一個字**
 *    ⇒ **本輪瘦身無法消除客戶看到的那幾十次重複**，須由 renderer 端處理。
 *    **這是本組單組判讀，未經第二組複驗。**
 *
 * ⚠️ 依 CLAUDE.md §-2 規則 6 據實標明：本檔「規則文字已從畫面欄位清乾淨」是
 *    **本組單組**關鍵字掃描的結果，**沒有第二組獨立複驗**。掃不到的可能仍有殘留。
 * ═══════════════════════════════════════════════════════════════
 */
/* wf_page_why.js — 「📖 憑什麼」線框資料（純資料，無渲染邏輯）
 *
 * 唯一內容來源：S1-3B_FULL_DRAFT_SPEC.md §3.5 九節（§3.5.0~§3.5.9）；
 * 輔：S1-4_STATE_MATRIX.md(v2)、S1-1_DATA_WHITEPAPER.md、S1-6_COMPLIANCE_COPY_GUIDE.md、
 *     STATE_SEMANTICS_QA2.md（狀態標籤那一條）；
 * 權威對照：docs/wireframes/stock_ia_v1.html PAGES[4]（id:'why'，第 888 行起）；
 * 實際 code：src/ui/views/page_why.py（2216 行）、shared/macro_buckets.py、shared/ui_state.py。
 *
 * 本檔的編碼約定（讀之前先看，否則會誤判為「發明」或「漏填」）
 * ─────────────────────────────────────────────────────────────
 * 1. 🔴 mainCTA:null —— 本頁沒有主 CTA，這是**實測結果**不是漏填。
 *    `grep -c form_submit_button src/ui/views/page_why.py` → **0**（本組自己重跑，非轉述）。
 *    `grep -c "st\.form" ...` → 3，但三處**全在 docstring／註解裡**（:167 / :175 / :313），
 *    其中 :167 逐字寫「⚠️ **本頁刻意一個 `st.form` 都沒有。**」
 *    ⛔ 不准為本頁發明一顆按鈕（S1-3:514 的「🚀 重新檢查所有來源」那顆**不存在**）。
 * 2. 本頁**不照四層結論骨架**（L1 結論／L2 卡片／L3 清單／L4 佐證）。
 *    S1-3B §3.5 全節沒有一個字提到那四層 —— 它是一頁「解釋與查驗」的旁支，
 *    結構是**三葉**。故本檔 layers 依「葉 ＋ 可見層級」分，label 已寫明。
 *    ⛔ 這不是偏離核准線框：核准線框 PAGES[4].blocks 也是照葉列的（4 個 block）。
 * 3. states 十格一律給滿；不適用者填 null（硬規則 2）。
 *    null ＝「這個區塊在這個情況下結構上不存在」，⛔ 不是「我沒查」。
 * 4. asOf 一律 null（硬規則 3）。本頁沒有任何一個區塊查得到資料歸屬日 ——
 *    葉1 是 L0 常數表（沒有歸屬日這個概念）、葉2 讀的是**呼叫紀錄**不是資料、
 *    葉3 是當輪問答。⛔ 不准填今天。
 * 5. key 以 code 端 Card key 為準（why.edu.* / why.source.* / why.spec.* /
 *    why.engineer.* / why.qa）；表格、卡牆、chrome 沒有對應 Card，key 為本線框新編，
 *    evidence 逐一標明。
 * 6. flags 用語：★待拍板／偏離核准線框／未驗證／單組結論。
 * 7. ⚠️ **合規**：本檔依「缺項填補測試」（S1-6 §1 STEP 2）逐句過了一遍 —— 本組
 *    沒有找到任何一句是「觀測 → 行動」而系統替讀者填掉了只有讀者才有的變數；
 *    另跑一次禁用語機械掃描（建議買進／立即出清／應該加碼／推薦／必漲／目標價／
 *    建議持股）**0 命中**。⚠️ 前者是**本組單組人工判讀**，不是第二組驗過的（§-2 規則 6）。
 *    ⚠️ 一處**刻意不逐字轉錄**：`why.footer` 的 evidence 提到 `app.py:1174-1178`
 *    那條違規常駐條時，**改以描述代替原文** —— 在合規稿裡逐字複製一句違規文案，
 *    等於讓那句話多存在一份，而且會被機械掃描當成新的命中。原文位置已給足，可自行查。
 * 8. ⚠️ 三個灰的說法**刻意各自不同**（指令第 4 點）：
 *      ⬜ 未載入 ＝「還沒去拿」—— 有出口，去按那個鈕就會有。
 *      ⛔ 未接線 ＝「刻意沒接／接不上」—— **沒有使用者可執行的出口**，重按一百次一樣。
 *      ▨ 結構不適用 ＝「這一格對它沒有意義」—— 不是壞掉，也不是缺，是這張表沒有這一欄。
 *    把「刻意沒接」說成「不適用」或「故障」都是假話（§1）。
 * 9. ⚠️ **本組已知盲點（依 §-2 規則 6 據實揭露，不得當成「全都查過了」）**：
 *    (a) **沒有實跑 Streamlit 把任何一格畫出來。** 沙箱對外連線被政策擋，且葉2 牆上
 *        有哪幾格取決於 session 載入過哪些 L1 模組。十態內容是**讀 code ＋ 直接呼叫
 *        builder 推出來的**（`build_*` ＋ `state_meta()` 本組有實跑），**不是畫面實測**。
 *    (b) **`tests/test_p05_why_view.py` 的四條守衛本組一條都沒有重跑**
 *        （TestNamedSourcesAreReallyWired / TestMonitoredCountIsNotStale /
 *         TestStatusLiteralsMatchL0 / TestLeafOneHasNoGate）——
 *        evidence 裡提到它們時一律標「本組未重跑」。
 *    (c) **`STATE_SEMANTICS_QA2.md` 那 5 處誤標的 AST 窮舉是別組的結果**，本組沒有
 *        自己重跑 AST。本組自己重跑的只有「本頁 3 張 degraded」與「本頁 :764 餵真旗標」。
 *    (d) **「本頁沒有 X」這類 grep 全稱句（狀態列／資料時點列）是單組 grep**，
 *        沒有第二組獨立驗。已在各該格的 evidence 標 `單組結論`。
 *    (e) 本檔**沒有**查證「另外四頁的去哪補是不是真的都指得到本頁葉2」——
 *        S1-3B §3.5.8 宣稱本頁是全站指路句終點，**那句本組沒驗**。
 */
window.WF_PAGES = window.WF_PAGES || [];
window.WF_PAGES.push({
  id: 'why',
  tab: '📖 憑什麼',
  subtitle: '一句話：解釋數字怎麼來、資料新不新鮮、以及直接問。\n**這一頁是用來查的，沒有「開始」按鈕** —— 想知道今天能不能出手，到「🚦 今天」；想挑標的，到「🔍 找標的」；想查一檔，到「🔬 查一檔」。這一頁三個分頁都可以直接看：**教學**（門檻怎麼判）·**資料體檢**（哪些來源是好的）·**AI 問答**（直接問，送出就開始）。',

  /* 🔴 本頁沒有主 CTA。實測 form_submit_button = 0（本組重跑）。
     本頁實際只有兩個控制項，兩個都不是主 CTA，各自登記在下面的 block：
       ① st.expander(:2119) → 內含 st.checkbox(:2121)　＝ **兩步、兩個不同 label**
       ② st.chat_input(:2169)　＝ 天然 gate（送出即是 submit）
     ⛔ 不得把 ①② 之一升格成 mainCTA —— 它們是葉內 gate，不是首屏主按鈕。 */
  mainCTA: null,

  leaves: [
    { id: 'l1', name: '教學',
      note: '`page_why.py:330` `LEAF_EDU_TITLE`。**五頁唯一「冷啟動就有東西看」的一葉** —— 靜態、無 gate、⛔ 沒有灰態（S1-3B §3.5.1 ＋ 核准線框 grey 原文「靜態內容，永遠可讀 —— 這頁沒有灰態」）。零取數：全葉只讀 L0 `@dataclass` 常數表。' },
    { id: 'l2', name: '資料體檢',
      note: '`page_why.py:331` `LEAF_DATA_HEALTH_TITLE`。⭐ **全站指路句的終點** —— 另外四頁的「去哪補」大量指向這裡（S1-3B §3.5.8）。⇒ **使用者版必須常駐，⛔ 不得藏在 gate 後面**；只有工程師版有 gate。' },
    { id: 'l3', name: 'AI 問答',
      note: '`page_why.py:332` `LEAF_QA_TITLE`。**五頁唯一的三葉頁**（其餘四頁皆兩葉）。原為頂層分頁，核准線框 DECISIONS #8 裁決降為本頁的葉。' }
  ],

  layers: [

    /* ═══════════════════════════════════════════════════════════
       葉外｜全域 chrome
       ⚠️ 本頁與頁1 最大的結構差：這三條**本頁 code 都沒有渲染**。
       ═══════════════════════════════════════════════════════════ */
    { n: 0, label: '葉外｜全域 chrome（頂部狀態列 ＋ 資料時點列 ＋ 頁尾，五頁共用，不屬任何葉）',
      blocks: [

        { key: 'why.statusbar', name: '（跨頁）頂部狀態列', leaf: null,
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '`09/13 週五 ✅ 交易日　|　🟢 總經 偏多　|　🔗 已綁 Sheet「我的持股」`',
            idle: '`09/13 週五 ✅ 交易日　|　⬜ 總經未評估　|　🔗 尚未綁定 Sheet`\n為什麼：這一輪還沒算出總經狀態\n去哪補：到「🚦 今天」按「🚀 更新今日戰情」；Sheet 到「💼 我的持股 › 組合設定」綁',
            loading: '更新中…',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: '`⚠️ 🟡 總經 偏多 —— 來自快照，時間不明`（同頁1：上一次存下來的結論）',
            unwired: '**本頁目前沒有這一條** —— 這一格是**實況**，不是假設。',
            error: '🔴 總經狀態讀取失敗（附錯誤型別）；Sheet 欄位維持原樣'
          },
          asOf: null,
          evidence: '【本組實測，非轉述】`grep -n "status_bar|statusbar|build_status_bar_cards" src/ui/views/page_why.py` → **0 命中**。全五頁只有 `src/ui/views/page_today.py:1892` 呼叫 `build_status_bar_cards()`。\n⇒ **這一條在本頁現在畫不出來**，`states.unwired` 那一格寫的就是實況。\n【為什麼不是「app.py 會補上」】`app.py:585-600` 逐字記載新五頁分支**刻意不渲染**舊 IA 的全域置底常駐條與「🔗 我的組合」狀態列，理由是它讀的 `warroom_summary` 只在舊頁籤的 render 路徑裡寫入 → 新頁上掛它會**必然說假話**（v19.171 🔴-1 事故形態）。**那是刻意移除，不是漏接。**\n【規格要求它存在】S1-3B §3.5.1 的冷啟動線框第一行就畫了這一條。⇒ 規格與 code 不一致：要嘛本頁補上（走 L3 canonical 契約，同頁1），要嘛規格改口。**本組不代客戶決定** → ★待拍板。\n【偏離核准線框】核准線框 `stock_ia_v1.html` PAGES[4] **沒有**這一條（它只有 4 個 block，全是葉內容）—— 這一條是 S1-3B §2.10.1 新增的跨頁 chrome。\n\n【🆕 文案瘦身 2026-09-14｜本頁新增的「白話指路」句 —— ★待拍板，這是提案不是現況】\n客戶回饋：「有些頁面寫『本頁目前沒有主 CTA』，新手會迷失。」本頁 `mainCTA: null` 是**實測結果**（`grep -c form_submit_button src/ui/views/page_why.py` → 0），⛔ **不得為它發明一顆按鈕**。\n**本組的做法**：不新增任何按鈕，改在 `subtitle`（頁標題正下方、主 CTA 面板之上）加一句**白話指路**：\n「**這一頁是用來查的，沒有「開始」按鈕** —— 想知道今天能不能出手，到「🚦 今天」；想挑標的，到「🔍 找標的」；想查一檔，到「🔬 查一檔」。這一頁三個分頁都可以直接看：**教學**（門檻怎麼判）·**資料體檢**（哪些來源是好的）·**AI 問答**（直接問，送出就開始）。」\n⚠️ **這是本組提案，未經客戶或總管拍板** → 本 block 的 flags 已帶 `★待拍板`（沿用既有旗標，未新增）。\n⚠️ **三個頁名與三個葉名都是照本檔既有內容抄的**（`leaves` 的 l1／l2／l3），⛔ 沒有發明新功能；「送出就開始」一句與 `why.qa.idle` 現有文案「**送出本身就是啟動**」同義，⛔ 不是新承諾。\n⚠️ renderer 那句「本頁目前沒有主 CTA」由 `warroom_ia_v2.html:601-609` 印出，**本組未動**；本句只是在它上方先給使用者一個去處。要把那句話本身改掉，須由 renderer 那一組處理。\n\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· idle 原文：讀 **L3 canonical 契約**，該契約本輪沒有值。\n· unwired 原文：「⛔ **本頁目前沒有這一條** —— 見 evidence。」\n· error 原文：＋ 例外型別；逐格獨立判態。',
          src: null,
          flags: ['★待拍板', '偏離核准線框', '單組結論'] },

        { key: 'why.asof', name: '資料時點揭露列（頁標題下方第一行）', leaf: null,
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '資料時點：`本頁葉1 為靜態內容（無資料時點）；葉2 〈最舊日〉 ～ 〈最新日〉`　⭕ 可點，展開逐源明細',
            idle: '資料時點：**本頁葉1 為靜態內容（無資料時點）；葉2 尚未檢查**',
            loading: '資料時點：更新中…　❌ 不可點',
            empty: null,
            missing: null,
            na: '▨ 葉1 這一半**本來就沒有資料時點** —— 它讀的是一份固定的門檻對照表，**常數沒有歸屬日**。\n⚠️ 這一格寫 `N/A` 而**不帶** ⚠︎：不適用 ≠ 壞掉。',
            partial: '資料時點：**葉1 無時點（靜態）；葉2 已檢查 N 源，其中 M 源沒有給資料日期**',
            degraded: null,
            unwired: '**本頁目前沒有這一條** —— 現行畫面上還沒有這一列。',
            error: '🔴 資料時點：**這一輪沒取到，畫面上的值可能是上一輪留下的**　✅ 可點'
          },
          asOf: null,
          evidence: '【本組實測】`grep -n "資料時點" src/ui/views/page_why.py` → **0 命中**。`render_page_why()`（:2189-2216）從 `st.markdown("## …")` → `st.caption(職責句)` → `st.tabs(三葉)`，中間沒有任何時點列。\n⭐ **本頁的時點語意與另外四頁根本不同，這是本區塊最重要的一句**：另外四頁的時點列講「這些數字是哪一天的」；本頁葉1 **沒有數字**（全是 L0 常數與門檻定義），葉2 牆上那個時間講的是「**最後一次真實外抓**」——**那是呼叫紀錄，不是資料歸屬日**。兩者混寫會讓使用者以為 FRED 卡上的 `09/13 14:30` 是 CPI 的歸屬月，而它其實只是「我們在 14:30 打了一次 API」。\n⇒ 若日後要補這一條，**必須先解決這個語意分岔**，否則補上去就是一個看起來精確、實際說錯話的數字（§1）。code 端已有對應揭露常數可複用：`page_why.py:520` `CACHE_SEMANTICS`。\n【單組結論】「本頁沒有時點列」是本組單組 grep 的結論，未經第二組獨立驗。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· idle 原出處：S1-3B §3.5.1 冷啟動線框第 2 行逐字。\n· na 原文：它讀的是 L0 `@dataclass` 常數表；出處 S1-4 §2.3 C。\n· partial 原文：⛔ 不得只寫其中一半（★-07a 合併規則）。\n· unwired 原文：「同上一格：page_why.py 全檔 `資料時點` **0 命中**，本組實測」。',
          src: null,
          flags: ['★待拍板', '單組結論'] },

        { key: 'why.footer', name: '頁尾免責（每一頁都要有）', leaf: null,
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '`⚠️ 台股AI戰情室 v3.0 · 僅供學術研究，非投資建議，盈虧自負`（置中小字）',
            idle: '同 live —— **常駐，不隨載入狀態消失**',
            loading: '同 live',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '同 live —— **整頁出錯時也一定會出現**'
          },
          asOf: null,
          evidence: '【已接線，本組實測】文字 SSOT 在 `app.py:514-517` `_render_footer()`，新五頁分支於 `app.py:604` 在 `st.stop()` **之前**呼叫它。`app.py:599` 逐字註解：「免責聲明**照樣出**（法遵文字，不分頁面）」。**新舊頁共用同一份文字**（複製第二份 = 兩邊會漂移）。\n⇒ 這一條是本頁 chrome 三條裡**唯一已經在畫面上的**。\n【⛔ 明確禁止】S1-3B §2.10.3 ⚑：新五頁**不得**複製 `app.py:1174-1178` 那條全域置底常駐條（其內容是一句直接指定持股比例的話 —— **本檔依合規規則不逐字轉錄那句**，原文見 `app.py:1174-1178` 與 `COMPLIANCE_SCAN_B` F-16） —— 它是 COMPLIANCE_SCAN_B F-16 六個落點中唯一每頁都看得到的那個，與客戶拍板第 3 條「全站不產生直接買賣建議」正面牴觸。\n【未在本頁 code 內】`page_why.py` 全檔「免責」**只有 1 命中**（:507），而那是一句註解（「這是誠實揭露，**不是免責聲明**」）—— **不是頁尾**。⇒ 頁尾由 `app.py` 統一出，故 src 指 app.py。\n⚠️ 本組原本寫「0 命中」，自驗時發現錯了，已就地更正 —— 記在這裡，因為「0 命中」這種全稱句正是最容易寫錯又最不容易被發現的一種。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「頁尾 **D-1** 免責（法遵必出）」。\n· live 原文：（置中、10px、`#484f58`）。\n· idle 原文：⛔ 不隨載入狀態消失。\n· error 原文：**整頁 render 例外時也必須出**（`app.py:604` 在 `st.stop()` 之前呼叫）。',
          src: 'app.py:514-517（文字 SSOT）＋ app.py:604（新五頁呼叫點）',
          flags: [] }
      ] },

    /* ═══════════════════════════════════════════════════════════
       葉1 教學 —— 上半：四張說明卡
       ═══════════════════════════════════════════════════════════ */
    { n: 1, label: '葉1 教學 · 上半｜四張說明卡（靜態 · 零互動可見 · 這一葉沒有灰態）',
      blocks: [

        { key: 'why.edu.lights', name: '① 紅綠燈怎麼判 · 各門檻的出處', leaf: 'l1',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: '🟢 運作中　**16 盞總經燈 ＋ 12 盞持股燈**\n〔展開佐證 ▸〕門檻從哪來／怎麼判／⚠️ 灰燈有四種不是一種／⚠️ 另外還有 2 條不算燈的參考走勢／完整清單',
            idle: '**這一葉沒有灰態** —— 它讀的是一份固定的門檻對照表，沒有「還沒載入」這回事。',
            loading: '無載入態（靜態，不需要取數）',
            empty: '▨ **門檻表讀不出來** ＋ 為什麼「這一輪沒有回傳任何一盞燈的定義」＋ 去哪補「**這一態沒有你可以做的事** —— 這是門檻表本身的問題，請回報給維護者」',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '🔴 門檻表讀取時出錯 ＋ 錯誤型別原文（不會被吞掉）'
          },
          asOf: null,
          evidence: '🔴 **這是指令第 2 點（分母造假前科）的主戰場，逐項寫清楚。**\n\n【分母怎麼來 —— 實測，可重現】\n· 分子/分母的唯一計算式：`page_why.py:1413-1414`\n　`_n_macro = sum(1 for _r in scan.rows if _r.family == FAMILY_MACRO)`\n　`_n_hold  = sum(1 for _r in scan.rows if _r.family == FAMILY_HOLD)`\n· `scan.rows` 的來源：`load_specs()`（:1360）＝ `_macro_rows() + _station_rows()`，\n　**參考走勢走另一個欄位** `reference_rows`（:1278 `_reference_rows()`）。\n· 本組實跑 `len(BUCKET_DANGER_SPECS)=16`（long 3 · mid 6 · short 3 · chips 3 · news 1）、\n　`len(REFERENCE_TREND_SPECS)=2`（新台幣匯率 · 加權指數）、`len(STATION_SPECS)=12`\n　（health 4 · stock 4 · screen 3 · timing 1）→ **卡面值 = 16 盞總經燈 ＋ 12 盞持股燈**。\n\n【⭐ 分母錯了要怎麼看得出來 —— 三道機械防線，逐道列出】\n1. **型別隔離（主防線）**：`SpecScan`（:1312）把燈與參考走勢放在**兩個不同欄位**。\n　 `rows` 只裝算得上一盞燈的 ⇒ **任何 `len(scan.rows)` 都是對的**。要兩者相加\n　 必須明講 `all_rows`，而 `all_rows` 的 docstring（:1336-1338）寫死\n　 「⛔ **只給「不是計數」的用途**……任何會出現在畫面上的『N 盞』一律用 `rows`」。\n　 ⇒ 「忘了過濾」這個錯誤在本檔**寫不出來**，不是「靠記得過濾」。\n2. **L0 端的物理隔離**：`shared/macro_buckets.py:420-429` 客戶 2026-08-27 裁示原文\n　 逐字預言了這個失效模式 ——「把不算燈的東西放進去再靠旗標排除，等於要求\n　 **每一個現有與未來的消費端**都記得過濾；**漏掉一個，分母就悄悄變成 18，\n　 而畫面上完全看不出來**（§1：錯的數字比沒有數字更危險）」。\n　 同段 :430-433 另記 `REFERENCE_BUCKET = "reference"` 是一個**刻意落在五桶之外**的\n　 哨兵值：ref spec 若被誤餵進桶彙總，會落在「沒有這個桶」而**不是**悄悄混進某一桶。\n3. **畫面自證**：卡上第 4 條 fact 逐字寫出「總經頁上還畫了 `{_n_ref}` 條**參考走勢**……\n　 **不算一盞燈**：不進上面那個『N 盞』的分母」，而 `_n_ref` 是**算出來的**\n　 （`len(scan.reference_rows)`，:1415）。⇒ 分母若真的變成 18，這一行會同時掉成 0 條，\n　 **兩個數字會當場自相矛盾**，使用者看得出來。\n\n【⚠️ 這個 bug 是真的發生過，不是假想】`page_why.py:1269-1272` 與 :434-448 兩處逐字記載：\n**2026-09-09 之前** `_macro_rows()` 寫的是 `list(BUCKET_DANGER_SPECS) + list(REFERENCE_TREND_SPECS)`，\n本頁於是把 16 盞燈算成 18 盞 —— 而 `page_today` 那一頁寫的是 16。\n**兩頁對同一件事給了兩個數字，而畫面上沒有任何地方看得出誰對。**\n⇒ S1-3B §3.5.1 ⚑B-08 的判定：「**頁1 的 16 盞是對的，頁5 的 18 盞是錯的。這不是要改規格，\n是讓畫面回到規格本來就規定的樣子**」⇒ 性質＝**修正錯誤**，⛔ 不是改設計（不必走草稿先行）。\n\n【⚠️ 本組未驗的一件事】上述三道防線**目前沒有一條測試在守「16 這個數字」本身**——\n本組 grep 未在 `tests/` 找到針對 `_n_macro == 16` 的斷言（**單組結論，未窮舉**）。\n物理隔離擋得住「忘了過濾」，但擋不住「有人在 L0 多加一盞燈卻沒人發現頁1 的文案還寫 16」。\n⇒ 建議補一條同型於 `TestMonitoredCountIsNotStale`（:600-615 記載）的守衛。**本組只提出，不代決。**\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· idle 原文：「它讀的是 L0 `@dataclass` 常數表」＋「`build_l0_card` 的 `requested=` 是**字面 `True`**（:759），`page_why.py:746-756` 的 docstring 逐字說明為何寫字面常數而不是運算式」。\n· empty 原文：「L0 規格表這一輪沒有回傳任何一盞燈的定義」／「⛔ 沒有出口 —— 這是規格表本身的問題」。\n· missing 原文：`reason=MISS_NO_INPUT`，`page_why.py:1421`。\n· error 原文：`load_specs()` 的 `except` 轉紅卡，`page_why.py:1364-1368`，⛔ 不吞。',
          src: 'src/ui/views/page_why.py:1409（builder）＋ :2033（render）＋ shared/macro_buckets.py:420-429（客戶裁示原文）',
          flags: ['未驗證', '單組結論'] },

        { key: 'why.edu.health6', name: '② 健康評分六因子', leaf: 'l1',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: null,
            idle: null,
            loading: null,
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: '**未接線（還沒做）** —— **六個因子各佔幾分，本頁還說不出來**\n為什麼：那組配分目前**沒有一份可以被讀取的來源** —— 它寫死在計算程式裡，另一處的註解又各自抄了一份。本頁**不把那組數字再抄第三份** —— 抄第三份就是再製造一次前後不一致，而那正是這一頁存在的理由。\n去哪補：**這一態沒有你可以做的事** —— 這是還沒做的項目，不是你操作的問題。\n⚠️ **在補好之前，這一格空著是誠實的**：在「解釋數字怎麼來」的頁面上放一個沒有出處的數字，比空著更糟。',
            error: null
          },
          asOf: null,
          evidence: '【本組實測】`build_health6_card()`（:1465）寫死 `wired=False`（:1469） ⇒ `classify_ui_state` 第 1 條就回 `UI_UNWIRED`（`shared/ui_state.py:199-200`：`if not wired: return UI_UNWIRED`，排在所有其他條件之前）。本組實跑確認 chip = `("未接線", "⛔", "#888888")`。\n⇒ **這一格 live/idle/error 全填 null 不是漏填**：`wired=False` 是**編寫期靜態宣告**，任何值、任何請求都不改變它（`ui_state.py` 判定順序表第 1 列逐字：「刻意沒接，任何值、任何請求都不改變它」）。旗標哪天在 L0 被翻回 `True`，這張卡才會有其他狀態。\n【⚠️ 三個灰要分清楚】這一格是 **⛔ 未接線**，不是 ⬜ 未載入、也不是 ▨ 不適用：\n· 不是「未載入」—— 它**沒有**「去按一下就會有」的出口；\n· 不是「不適用」—— 六因子對這一頁**完全適用**，只是接不上。\n說成前者會讓使用者一直找那顆不存在的鈕，說成後者會讓人以為這件事不該存在。\n【順帶】L0 `shared/health_thresholds.py` 有的是**等第分界**（A/B 級下界），那是**另一件事** —— 分界說「幾分算好」，因子說「這幾分怎麼算出來的」（卡上第 3 條 fact 逐字）。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· unwired 原文（搬出 code 位置）：權重是 L2 `src/compute/scoring/scoring_helpers.py::calc_health_score` 裡的 **inline 數字**，L0 `shared/position_throttle.py` 的註解裡另外抄了一份文字版（實測 2026-09-07：兩處各一份）；本頁走 L3／L0，**不讀 L2 函式內文，也不把那組數字抄第三份**；正解是在 L0 補一份 `((因子名, 配分), …)` 常數表讓 L2 與本頁**讀同一份**。\n⚠️ **缺口一字未縮**：states.unwired 仍寫「未接線（還沒做）」「六個因子各佔幾分，本頁還說不出來」「這一格空著是誠實的」。',
          src: 'src/ui/views/page_why.py:1465（builder，wired=False 在 :1469）＋ :2033（render）',
          flags: [] },

        { key: 'why.edu.scales', name: '③ 同一個名詞，兩套刻度', leaf: 'l1',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: '🟢 運作中　**兩套刻度並存 —— 不可互比**\n〔展開佐證 ▸〕💼 戰情表「健檢」＝ 序數燈號 ⚪/🟢/🟡/🔴（四盞取最嚴重，**沒有分數**）／🔬 判決卡「健康度」＝ 連續分數 0–100 ＋ 等第（**沒有燈號**）／為什麼不可互比／⚠️ 具體會怎麼錯／兩套各自的門檻',
            idle: '無（這一葉是靜態內容）',
            loading: '無（不需要取數）',
            empty: '▨ **兩套刻度的定義讀不出來** ＋ 為什麼「這一輪沒有回傳可用的燈號定義」',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: '🟠 **兩套刻度目前有一側已失準，本頁不提供跨頁比較**\n為什麼：其中一側的門檻**現在分不出好壞**（燈會亮、也有等級，只是**別照門檻讀**）—— 逐盞原因見下方對照表，那段文字是來源自己寫的，本頁一個字都沒有改寫。\n去哪補：兩套刻度各自的出處與門檻**就在這張卡下面**，可以逐項對照；要看某一檔的實際數字，到「🔬 查一檔」或「💼 我的持股」各看各的，**不要把兩邊的數字放在一起比**。\n⚠️ **這是本頁現在的實況**（目前落在這一態的是「財報趨勢」那一盞）。',
            unwired: null,
            error: '🔴 兩套刻度讀取時出錯 ＋ 錯誤型別'
          },
          asOf: null,
          evidence: '⭐ **這是頁4（💼 我的持股）② 卡在 degraded 時指過來的地方**（`page_why.py:1499-1502` 逐字）⇒ **本頁這一段必須比頁4 完整**，不能只是同一段話再貼一次。\n【degraded 是算出來的，不是寫死的】`build_scale_disclosure()`（:1528）逐列掃 L0 旗標：`_notes = tuple((_r.label, _r.degraded_reason) for _r in (_hold + _inspect) if not _r.discriminative)`。旗標若被改回 `True`，這張卡會自己回到 live（:1526-1527 docstring 逐字）。\n【本組實跑結果】`build_scale_card(...)` → `state=degraded`、chip `("門檻已失準","🟠","#eab308")`；肇因是 `HOLD_SCALE_INPUT_SPEC_KEYS` 含 `KEY_STOCK_TREND`（財報趨勢），該 spec 在 L0 標 `discriminative=False`。\n⇒ **這一格的「門檻已失準」是真的**（來自 L0 的真旗標），與指令第 3 點講的那 5 張誤標卡**不是同一件事** —— 詳見本頁 `why.spec.flags` 那一格的完整說明。\n【交接事項（code 自陳）】`HOLD_SCALE_SHAPE` / `INSPECT_SCALE_SHAPE` 兩句 prose 在 `page_hold.py` **也各有一份**（本檔 :408-414 逐字揭露這件事），正解是在 L0 `shared/station_specs.py` 補 `scale_shape` 欄位讓兩頁讀同一份；本批不動 `shared/**`，故留紀錄。**這是已登記的漂移風險，不是本線框新增的。**\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· idle 原文：靜態葉，`requested` 字面 True。\n· missing 原文：`reason=MISS_NO_INPUT`，:1567。\n· degraded 原文：其中一側的門檻來源在 L0 規格表標了 `discriminative=False`；「逐盞原因見 facts 與下方對照表」；實況為 `stock_trend` 財報趨勢。\n· error 原文：:1530-1533 轉紅卡。',
          src: 'src/ui/views/page_why.py:1539（builder）＋ :1521（disclosure）＋ :2033（render）',
          flags: ['未驗證'] },

        { key: 'why.edu.legacy', name: '④ 完整策略邏輯說明書（既有 📚 教學）', leaf: 'l1',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: null,
            idle: null,
            loading: null,
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: '**未接線（還沒做）** —— **既有的完整教學內容還沒有搬到這一頁**\n為什麼：那是一份很長的既有內容，搬過來會動到其他分頁，**這一批刻意不夾帶**。\n去哪補：✅ **這一項有出口**（與本頁其他未接線項不同）—— 既有的 📚 教學分頁**沒有下架**，現在就可以照舊使用；本頁上面那幾格（門檻怎麼判、門檻出處、兩套刻度）是**新**的，既有教學裡沒有。',
            error: null
          },
          asOf: null,
          evidence: '【為什麼不先貼一份摘要】卡上 fact 逐字：「摘要會變成第二份教學，兩份遲早不一致 —— 而**『兩份說法不一致』正是這一頁要解的問題本身**」。\n⚠️ **這一格是本頁唯一一個「⛔ 未接線但有出口」** —— 其餘 ⛔ 卡一律走 `NO_EXIT_PREFIX`（:666 `NO_EXIT_PREFIX`，沿用 `tab_today.NO_EXIT_MARKER` SSOT）明說沒有出口。把這一格也寫成「沒有出口」會讓使用者以為教學內容不見了，而它**還在舊分頁上活著**。\n【範圍問題，不是技術問題】依 `CLAUDE.md §8.4 step 4`，跨檔搬遷 ＋ 既有 caller 的範圍要**問客戶**（須附總管的方案與理由）⇒ ★待拍板。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· unwired 原文（搬出決策來歷）：「核准線框 DECISIONS #9 裁決「**留，原樣搬到「📖 憑什麼 › 教學」**」，但那是一份 **1,582 行**的既有靜態內容，搬遷會動到既有分頁與 caller —— 屬 `CLAUDE.md §8.4` step 4 的**範圍**問題，本批（只新增這一個檔）刻意不夾帶。」',
          src: 'src/ui/views/page_why.py:1579（builder）＋ :2033（render）',
          flags: ['★待拍板'] }
      ] },

    /* ═══════════════════════════════════════════════════════════
       葉1 教學 —— 下半：逐盞門檻對照表（⚑ B-08 切成三塊）
       ═══════════════════════════════════════════════════════════ */
    { n: 2, label: '葉1 教學 · 下半｜逐盞門檻對照表（切成三塊，各自有標題與各自的欄位）',
      blocks: [

        { key: 'why.edu.table.macro', name: '⑤-a 總經燈（16 盞）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '`st.dataframe` 七欄：這一盞／分組／方向／門檻／值從哪來／在防什麼／已知限制\ncaption：**總經燈 —— 五桶紅綠燈（🚦「今天」那一頁在用的）（16 盞）**\n分組中文：🌳 長期（3）· 📈 中期（6）· ⚡ 短線急殺（3）· 🧩 籌碼（3）· 📰 新聞（1）\n⭐ 每一盞都會判燈、**都算進 16 盞的分母**',
            idle: '無（這一葉是靜態內容，沒有「還沒載入」）',
            loading: '無（不需要取數）',
            empty: '整段**不畫** —— 上方 ① 那張卡已經把「讀不出來」講完了，這裡再空一張表只是重複',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: '個別列的「門檻」欄降級：`門檻（已失準）〈原門檻加刪除線〉　⚠️ 現在不要拿它當操作依據`\n⚠️ **數字沒有藏起來，是刻意的** —— 藏掉的後果是你去別的地方找一份**更舊**的數字\n本頁實況：**融資餘額**這一盞',
            unwired: '個別列的「門檻」欄標 `門檻（尚未生效）…`；「已知限制」欄寫 `未接線`\n本頁實況：**外資現貨淨買賣**這一盞',
            error: '整段不畫（同 empty）；紅態由上方 ① 卡承擔'
          },
          asOf: null,
          evidence: '⭐ **⚑B-08：為什麼要切三塊（性質＝修正錯誤，⛔ 不是改設計 → 不必走草稿先行）**\n現況（2026-09-09 前）：**一張表、18 列、標題寫「總經燈（18 盞）」**，而頁1 寫「16 盞」。\n**資料層早就把這兩份清單物理隔開**（`shared/macro_buckets.py:414-428`），且註解裡預先寫下了「漏掉一個，分母就悄悄變成 18」這個失效模式。\n⇒ **頁1 的 16 盞是對的，頁5 的 18 盞是錯的 —— 這不是要改規格，是讓畫面回到規格本來就規定的樣子。**\n（依 S1-3B §3.5.1 ⚑ 與 `CLAUDE.md §-1.5.A-8` 的「修正錯誤 vs 改變設計」判準：改完之後畫面是「符合原本講好的樣子」→ **修正錯誤**。）\n\n【現行 code 已經是切開的（本組實測）】`_render_edu_leaf()`（:2040-2048）迴圈只跑 `(FAMILY_MACRO, FAMILY_HOLD)` 兩族，篩的是 `_specs.rows`；:2044-2045 逐字註解：「這裡篩的是 `_specs.rows`（＝只有燈）。參考走勢住在 `reference_rows`，所以下面那個 `{len(_rows)} 盞` **不可能再變成 18**。」\n⇒ 本組實跑 `len(_rows where family==總經) = 16`。**分母現在是對的。**\n\n【分母錯了怎麼看得出來】caption 的「N 盞」**不是寫死的**，是 `len(_rows)` 當場算的（:2049）。\n若有人把參考走勢加回 `rows`，這裡會變 18 **而參考走勢那張表會同時掉成 0 條**（:2061 的 `{n}` 同樣是 `len()` 算的）—— 兩張表會當場互相矛盾。\n⚠️ 但這只在**有人看** caption 時才成立 ⇒ 見 `why.edu.lights` 那一格記的「目前沒有測試在守 16 這個數字」。\n\n【手機 <640】門檻對照表**轉卡片流**（一盞一卡：盞名／門檻帶／目前值／已知限制），三塊分區標題保留（S1-3B §3.5.3）。**平板 641–880** 放進橫向捲動容器。⇒ 本區塊 `cols` 三檔皆 1（它是一張表，不是卡牆）。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· empty 原文：`if not _specs.has_rows: return`，:2035-2036。\n· degraded 原文：`threshold_display`（:1190）；`THRESHOLD_CAVEAT_HEADER`（:470-473 逐字）；實況為 `margin`（L0 標 `discriminative=False`）。\n· unwired 原文：實況為 `foreign_net`（L0 標 `wired=False`）。\n· idle／loading 原文一律為「⛔ 無（靜態）」「⛔ 無（零 I/O）」。',
          src: 'src/ui/views/page_why.py:2038-2050（render）＋ :1964（_spec_table_rows 欄位定義）',
          flags: [] },

        { key: 'why.edu.table.reference', name: '⑤-b 參考走勢（2 條 · 不算燈）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '`st.dataframe` **四欄**：這一條／單位／這條線在說什麼／值從哪來\ncaption：**參考走勢 —— 這些不是燈（2 條）**　客戶 2026-08-27 裁示：這幾條在總經頁**畫走勢**，但**不算一盞燈** —— 不進上面那個「N 盞」的分母、不進五桶彙總、不判紅綠燈。\n兩條：💱 新台幣匯率（參考線 32.00 / 33.00，僅供對照，不判燈）· 📈 加權指數（日 K 走勢，**本來就沒有門檻**）',
            idle: '無（這一葉是靜態內容，沒有「還沒載入」）',
            loading: '無（不需要取數）',
            empty: '整段不畫',
            missing: '同 empty',
            na: '▨ **這張表刻意沒有「門檻」與「已知限制」兩欄** —— 這不是省略，是本區的定義。\n⚠️ 這一格是「**本來就不適用**」，不是「未接線」、也不是「缺資料」：那兩欄對一條不判燈的走勢**沒有意義**，硬套一個不適用的欄位只會擠出一個假答案。\n（`N/A` **不帶** ⚠︎ 圖示 —— 不適用 ≠ 壞掉。）',
            partial: null,
            degraded: null,
            unwired: null,
            error: '整段不畫'
          },
          asOf: null,
          evidence: '⭐ **這一塊順帶解掉「加權指數門檻空白卻標正常」那個舊 bug**（S1-3B §3.5.1 ⚑ 逐字）。\n【舊 bug 的形態（code 自陳，:2003-2021 逐字）】\n· 「門檻」欄：加權指數在 L0 的 `yellow` / `red` 就是 `None`（客戶裁示「K 線卡不畫門檻線」）。印一個「—」會讓人以為**它有門檻只是這輪沒讀到**。\n· 「已知限制」欄：`flag_text` 對一條沒有在判燈的走勢一律回「**正常**」—— 而「正常」在這張表的語境裡會被讀成「**這盞燈現在是好的**」。\n· **2026-09-09 前的實況正是如此：加權指數門檻空白、卻標著「正常」。**\n⇒ 判定逐字：「**不判燈的東西，標『正常』或『已失準』都是對使用者說錯話。**」\n⇒ 修法不是把那兩欄填好，是**整張表換一組欄位**（`_reference_table_rows()`，:2003）。\n\n【⚠️ 這一格是指令第 4 點的教科書例子】同一個「灰」在本頁有三種截然不同的意思，這一塊佔的是第三種：\n· ⬜ 未載入 → 去按那個鈕（有出口）；\n· ⛔ 未接線 → 沒有出口，重按一百次一樣（例：本頁 `why.edu.health6`）；\n· ▨ 結構不適用 → **這一格對它沒有意義**（本格）。\n把第三種說成第二種，等於宣稱「加權指數的門檻有一天會接上」——**它本來就沒有門檻**，那是假話。\n\n【L0 端佐證】`shared/macro_buckets.py:430-433`：`REFERENCE_BUCKET = "reference"` 是**刻意落在 BUCKET_ORDER 之外**的哨兵值 —— 萬一有人把 ref spec 誤餵進桶彙總，它會落在「沒有這個桶」而**不是**悄悄混進某一桶改掉那桶的 worst-of。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「⑤-b 參考走勢（2 條 · **⛔ 不算燈**）」。\n· empty 原文：`if _specs.reference_rows:`，:2059。\n· na 原文：⛔ 不是「未接線」、⛔ 不是「缺資料」；出處 S1-4 §2.3 C。',
          src: 'src/ui/views/page_why.py:2059-2063（render）＋ :2003（_reference_table_rows 四欄定義）＋ :477-483（caption SSOT）',
          flags: [] },

        { key: 'why.edu.table.hold', name: '⑤-c 持股燈（12 盞）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '`st.dataframe` 七欄（同 ⑤-a）\ncaption：**持股燈 —— 戰情表與判決卡（💼 我的持股 / 🔬 查一檔 在用的）（12 盞）**\n分組：四燈體檢（4）· 3-3-3 篩選（3）· 個股體質（4）· 進場時機（1）',
            idle: '無（這一葉是靜態內容，沒有「還沒載入」）',
            loading: '無（不需要取數）',
            empty: '整段不畫（同 ⑤-a）',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: '個別列降級：本頁實況為 **財報趨勢**那一盞',
            unwired: '本頁實況：**持股燈 12 盞目前 0 盞未接線**',
            error: '整段不畫'
          },
          asOf: null,
          evidence: '【本組實測分組數】`shared/station_specs.py::STATION_SPECS` 共 12 筆，`Counter(group)` = health 4 / stock 4 / screen 3 / timing 1 → 與 S1-3B §3.5.1 線框寫的「四燈體檢（4）3-3-3 篩選（3）個股體質（4）進場時機（1）」**逐項對得上**（本組自己重數，非轉述）。\n【⚠️ 一件規格沒寫的事】12 盞裡有 1 盞（`stock_kd` KD 指標）在 L0 標 `emits_level=False` —— 它**依規格就不出等級**，既不是未接線、也不是門檻失準、也不是沒資料。那一盞由下面 ⑤-e 單獨一段承接，**⛔ 不塞進上表的「已知限制」欄硬給一個態**。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· degraded／unwired 原文：實況為 `stock_trend`（L0 標 `discriminative=False`）／本組實跑 `STATION_SPECS` 全部 `wired=True`。\n\n【動作語全檔掃描 2026-09-14｜本格命中「進場時機」，**經判定不改**，理由如下】\n**本格命中**：`live` 的分組列「四燈體檢（4）· 3-3-3 篩選（3）· 個股體質（4）· **進場時機**（1）」。\n**判定：⛔ 不改。** 「進場時機」是**持股燈 12 盞的既有分組名稱**（code 端的分類名），不是對使用者的指示；本頁的職責正是**照實列出燈號表長什麼樣**，改名會讓這一頁與它要解釋的那張表對不上。\n⚠️ **但它確實是一個動作語當分組名**，若日後要做合規改名，**這一處要一起改**（連同 code 端的分組名），⛔ 不能只改線框 —— 只改線框會製造第二個真相源。**送登記，本組未動。**',
          src: 'src/ui/views/page_why.py:2040-2050（render，與 ⑤-a 同一個迴圈）＋ :1289（_station_rows）',
          flags: [] },

        { key: 'why.edu.table.caveat', name: '⑤-d 被降級的門檻 —— 為什麼現在不能用（表格下方逐列）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: 'caption 抬頭：**上表中被降級的門檻 —— 為什麼現在不能用**　⚠️ 數字**沒有藏起來**，是刻意的：藏掉的後果是你去別的地方找一份**更舊**的數字。下面每一行都是來源自己寫的原因，本頁一個字都沒有改寫。\n逐列：`· 〈盞名〉（已失準／尚未生效）：〈來源給的原因原文〉`\n本頁實況 3 列：外資現貨淨買賣（尚未生效）· 融資餘額（已失準）· 財報趨勢（已失準）',
            idle: '無（這一葉是靜態內容，沒有「還沒載入」）',
            loading: '無（不需要取數）',
            empty: '**沒有任何一盞被降級時，整段不出現** —— ✅ 反向也守住了：沒有被標記的燈**原樣照出門檻，一個字都不加**（為了顯得誠實而把全部門檻都標成失效，是另一種說謊）。',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '整段不畫'
          },
          asOf: null,
          evidence: '【為什麼原因不塞進表格儲存格】`threshold_caveat_lines()` docstring（:1988-1999）逐字：`degraded_reason` 是**多段文字**（L0 自己就寫了換行與粗體），塞進 `st.dataframe` 會被壓成一長條沒有斷行、也吃不到粗體 —— **那等於把一段寫給使用者看的話變成噪音**。\n【原文透傳，不改寫】L0 寫什麼就印什麼，本頁不節錄、不改寫（`L0_REASON_FACT_TEXT`，:504-506：只在放進三要素時移除會和狀態燈打架的符號）。\n【本組實跑的 3 列來源】`SpecScan.unwired` = (外資現貨淨買賣)；`SpecScan.degraded` = (融資餘額, 財報趨勢)。**列數會隨 L0 旗標自己增減，本線框不寫死。**\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· live 原文：「下面每一行都是 **L0 規格表**自己寫的原因」；逐列格式為 `〈L0 原文〉`。\n· empty 原文：`if _caveats:`，:2053；並引 `page_why.py:1183-1184` 逐字「沒有被 L0 標記的燈，這裡**原樣回傳門檻**，一個字都不加 —— **為了誠實而把全部門檻都標成失效，是另一種說謊**。」',
          src: 'src/ui/views/page_why.py:2052-2057（render）＋ :1988（threshold_caveat_lines）＋ :470-473（抬頭 SSOT）',
          flags: [] },

        { key: 'why.edu.table.nolevel', name: '⑤-e 依規格就不出等級的燈（不是壞掉，是還沒有判燈規則）', leaf: 'l1',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: null,
            idle: '無（這一葉是靜態內容，沒有「還沒載入」）',
            loading: '無（不需要取數）',
            empty: '沒有這類燈時整段不出現',
            missing: '同 empty',
            na: '▨ **這一類刻意不給狀態燈** —— 七態裡沒有任何一態在講「有值、也照印，但依規格從來不出等級」，硬塞任何一態都會對使用者說錯話：\n· `unwired` 說它**沒接**（假的，它接得好好的）\n· `degraded` 說它**門檻失準**（假的，它從來沒有門檻）\n· `empty` 說它**沒資料**（假的，K、D 都抓得到）\n**三句都不是實情。**\n逐列：`· 〈盞名〉：〈來源給的原因原文〉`\n本頁實況 1 列：**KD 指標** —— 原文：「這盞燈**還沒有判定規則** …… 這**不是**『這類持股不適用』（KD 對個股完全適用）」',
            partial: null,
            degraded: null,
            unwired: null,
            error: '整段不畫'
          },
          asOf: null,
          evidence: '⭐ **這一段是本頁對「七態不夠用」最誠實的一次處理，值得原樣保留。**\n它沒有硬把第八種情況塞進七態裡，而是**退出狀態系統**：不給 chip、改用 caption 逐列說明。`page_why.py:2067-2073` 那段文字是 code 裡逐字寫的，本線框一字未改寫。\n【與指令第 4 點的關係】這正是「三個灰要分開講」的極限案例 —— 它連第三種（結構不適用）都不完全是：`na` 的定義是「這一格對它沒有意義」，而 KD 是「**這一格對它有意義，只是還沒有人定義規則**」。\n⇒ 本線框把它放在 `states.na`，是**十格契約下最接近的一格**，並在文案裡明說它與 unwired／degraded／empty 的差別。⚠️ **這是本組的安置判斷，不是 code 或規格指定的** → ★待拍板：若日後要補第八態，這一格是第一個受影響的。\n【L0 出處】`shared/station_specs.py` 的 `emits_level` / `no_level_reason` 兩欄（本組實跑：`STATION_SPECS` 中僅 `stock_kd` 一筆 `emits_level=False`；`BUCKET_DANGER_SPECS` **0 筆**）。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· empty 原文：`if _no_level:`，:2065。\n· na 原文：逐列格式為「〈L0 的 `no_level_reason` 原文透傳〉」；實況那一列的 code 鍵為 `stock_kd`。',
          src: 'src/ui/views/page_why.py:2065-2076（render）＋ :1355-1357（SpecScan.no_level）',
          flags: ['★待拍板', '單組結論'] }
      ] },

    /* ═══════════════════════════════════════════════════════════
       葉2 資料體檢 · 使用者版（常駐）
       ═══════════════════════════════════════════════════════════ */
    { n: 3, label: '葉2 資料體檢 · 使用者版｜⭐ 常駐，不藏在 gate 後面（全站指路句的終點）',
      blocks: [

        { key: 'why.source.wall', name: '⑥ 三欄燈號卡牆（逐源獨立判態）', leaf: 'l2',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: '每格：`〈來源名〉` ＋ 🟢 運作中 ＋ **最後一次真實抓取時間**\n〔展開佐證 ▸〕分類／更新頻率／最後一次回了幾列／最後一次耗時(ms)／這一盞的出處\n⭐ **這頁的紅燈是主角，不是意外。**',
            idle: '⬜ **未檢查**\n為什麼：這個來源**從你打開網站到現在，還沒有真的對外抓過** —— 它一載入就先登記了自己（所以你現在看得到它），但還沒有發過任何一次請求。⚠️ **吃快取不算**：如果別的頁是拿快取回答你的，這一盞仍然會停在「未檢查」。\n去哪補：到會用到它的那一頁按更新／載入，讓它真的跑一次；跑完回到「📖 憑什麼 › 資料體檢」就會看到時間。',
            loading: '三欄燈號卡牆**固定高骨架**',
            empty: '**不會出現「0 源」** —— 但**會**出現「名單讀得到、裡面一支都沒有」：\n▨ **目前還沒有任何來源登記過** ＋ 為什麼「每個來源是在**被用到的時候**才登記自己的。如果你是直接打開這一頁、還沒去過任何會取數的分頁，這份名單就是空的 —— **這是正常的，不是壞掉**」\n⚠️ **為什麼不顯示一片空白**：空白會讓人以為這面牆壞了；「讀到了、裡面沒有東西」與「讀不到」是**兩件事**，必須分得出來。',
            missing: '同 empty',
            na: null,
            partial: '`已檢查 N / M 源*` ＋ `* 〈幾〉源未檢查：〈源名〉、〈源名〉`\n**不會只寫「部分資料」** —— 一定列出缺的是哪幾筆。',
            degraded: '🟠 該源**已失準** ＋ **就地**寫出原因（照出，不自己編）。\n⚠️ 這一態在**牆上**目前沒有實例：牆上的格子讀的是**呼叫紀錄**，「已失準」這件事走下面 ⑩ 那一區。',
            unwired: '該源**未接線**（還沒做）＋ 就地寫明原因（例：「旌旗指數 🔌 未接線」）\n**這一態沒有你可以做的事** —— 會寫明「這是還沒做的項目，不是你操作的問題」。',
            error: '🔴 **最後一次真實抓取失敗了** ＋ 記下的錯誤原文（例：「FinMind 🔴 額度用罄」）\n去哪補：這是上游來源的問題，不是你操作的問題 —— 可以到會用到它的那一頁重按一次更新；若持續失敗請把上面那行訊息回報給維護者。\n⭐ **一源紅不會把另外兩源一起染色。**\n＋ 第四種紅：**這一盞的狀態本頁看不懂** → ⚠️ **看不懂一律當紅，絕不當綠**。'
          },
          asOf: null,
          evidence: '【四種畫面對得上核准線框逐格】`build_source_card()` docstring（:919-933）逐字把 `greyCells` / `cells` / `errCells` 一一對上，第四種（不認得的狀態）線框沒有 ——「**因為它不該存在**」。\n【gate 讀的是什麼】`requested=probe.called` 讀**呼叫紀錄**、`has_value=probe.ok` 讀**成敗**，兩個不同欄位（:929-932）。L0 的鐵律禁的是「拿**資料本身**回推有沒有被叫過」；`last_status` 不是資料，是 `@monitored` 在呼叫當下寫下的紀錄 —— **那正是「上游帶下來的 gate 旗標」的定義**。\n【⚠️ 這面牆最容易被誤讀的一句】牆上那個時間**不是資料的歸屬日**，是「最後一次真實外抓」的時刻 —— 見下一格 `why.source.cache_semantics`。\n【斷點】3 欄卡牆在 641–880px 降 2＋1（第三張自己一列），≤640px 單欄（S1-3B §3.5.3）。\n【⚠️ 未實測】本組**沒有**實跑一次 Streamlit 把這面牆畫出來（沙箱對外連線被政策擋，且牆上有哪幾格取決於 session 載入過哪些 L1 模組）。上面十格是**讀 code 推出來的**，非畫面實測。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· live 原文：每格標題為「`〈fetcher 名〉`」。\n· idle 原文：「這一支 **fetcher** 這個 **session** 還沒有真的對外抓過 —— 它在被 **import** 時就先登錄了自己」「**快取命中點不亮這一盞**」。\n· loading 原出處：S1-3B §3.5.2。\n· empty 原文：「⛔ **不會出現「0 源」**（S1-3B §3.5.5：源清單是常數）」「`@monitored` 是在**模組被 import 的時候**自我登錄的。本頁自己**不 import 任何 L1 模組**（L5 不得直呼 L1）」。\n· missing 原文：`build_empty_registry_card()`，:976。\n· partial 原出處：S1-3B §3.5.4 ⑥；原帶「⛔ **不得只寫「部分資料」**」。\n· degraded 原文：就地讀 `degraded_reason`（⛔ 不自己編）—— S1-3B §3.5.4 ⑦；「牆上的格子讀的是呼叫紀錄（`last_status`），L0 登錄表沒有 `discriminative` 這個欄位」。\n· unwired 原文：就地寫 `unwired_reason`（核准線框 `unwiredCells` 的例子）。\n· error 原文：「L0 記下的錯誤原文（核准線框 `errCells`）」「⛔ **逐源獨立判態**」「L0 回了本檔不認得的字面值」。',
          src: 'src/ui/views/page_why.py:2085-2093（render）＋ :919（build_source_card）＋ :976（build_empty_registry_card）',
          flags: ['未驗證', '單組結論'] },

        { key: 'why.source.named', name: '⑦ 線框具名三源的狀態一覽（常駐 caption）', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '**線框具名的三個來源，現在各自的狀態**：\n· **FRED（美國總經）｜已接線** —— 量到的是 FRED 序列取數那一支（DGS10 / CPI / NAPM 共用同一支）。\n· **TWSE（台股收盤）｜已接線** —— 量到的是 TWSE 盤後「每日市場成交資訊」那一支。\n· **FinMind（API 額度）｜仍然量不到** —— 剩餘額度是帳號層級的資訊，不在任何一次取數的回傳裡；它在下面被**具名畫成一張灰卡**。\n⚠️ **「已接線」不保證你現在就看得到那一格**：要那個來源在這次瀏覽中被用過，它才會出現在牆上。找不到它 → 代表**還沒有人用過**，不是它壞了。\n⚠️ **一支綠燈不等於那個來源整體都好** —— 這面牆量到的單位是**單一取數函式**，不是**整個來源**。',
            idle: '**常駐，不隨載入狀態變** —— 這三段是本頁對「這面牆說得出什麼」的揭露，不是隨狀態出現的補充說明。',
            loading: '同 live（常駐）',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '同 live（常駐；這一段不需要取數，不會失敗）'
          },
          asOf: null,
          evidence: '⭐ **這一段存在的理由，正是本頁第二種假綠燈的解藥。**\n`named_sources_text()` docstring（:1038-1046）逐字：接上 `@monitored` 只讓那一支**有資格**出現在牆上；它真的出現，還要「那個 L1 模組在這個 session 被 import 過」。所以使用者可能會看到一面**沒有 FRED 的牆**，而那不代表 FRED 沒接線 —— 代表**還沒有人載入過它**。\n⇒ **不讓「牆上沒有」被讀成「沒問題」。**\n【可被機器查核，不靠自律】`NamedSource` 的 `fetcher` / `module` 兩欄**不是裝飾用的**：`tests/test_p05_why_view.py::TestNamedSourcesAreReallyWired` 拿它們**直接掃 L1 原始碼** —— 宣稱 `wired=True` 的那一支若在 L1 被拔掉 `@monitored`，**當場 CI 紅燈**（:546-552 逐字）。\n【對照核准線框】線框 `cells` 具名三源（FRED / FinMind 額度 / TWSE）；FE-20 已把其中兩個在 L1 接上 ⇒ **清單縮短不等於機制可以拆掉**（:996-1002 逐字）。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· live 原文（搬出 fetcher 名）：FRED 那一支牆上的名字是 `fetch_fred`；TWSE 那一支是 `twse_volume`（FMTQIK）；「真的出現還要『那個 **L1 模組**在這個 **session** 被載入過』」；「這面牆量到的單位是 **fetcher**，不是**來源**」。\n· idle 原文：引 `page_why.py:2094-2096` 逐字「下面這三段 caption 全部**常駐**（**不得包進任何 if**）」。\n· error 原文：「這段是純函式，零 streamlit、零 I/O」。',
          src: 'src/ui/views/page_why.py:1038（builder）＋ :2096（render）＋ :569（NAMED_SOURCES SSOT）',
          flags: [] },

        { key: 'why.source.cache_semantics', name: '⑧ 「這面牆上的時間是什麼」（常駐 caption · 誠實揭露）', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '**這面牆上的時間是什麼**：大多數來源的計時是**吃快取不算**的 —— 那個時間就是最後一次真的去外面抓。\n但**有些來源的快取做法不一樣**（例如 FRED 那一支），那一類的時間代表「最後一次**呼叫**」，**可能比最後一次真的外抓更新**。\n⚠️ 本頁**分不出哪一支是哪一種**，所以這裡把差別說出來，**不統一宣稱「吃快取一律不計」**。\n✅ 不受影響的是「未檢查」這一態：第一次呼叫必然沒有快取可用 —— **吃快取不會點亮一盞還沒亮過的燈**。',
            idle: '同 live（常駐）',
            loading: '同 live',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '同 live'
          },
          asOf: null,
          evidence: '⭐ **這一格是本頁「寧可承認分不出來，也不統一宣稱」的樣板，直接對應憲法 §1。**\n`page_why.py:507-519` 的註解逐字說明為什麼**不能**統一寫「快取命中一律不計」：那句話「對掛在 cache 之內的那幾支是對的，**對 `fetch_fred` 就是假的**」（實測量測日 2026-09-07：`fetch_fred` 的 30 分鐘 TTL 是**寫在函式體內**的 module-level dict，因為那個模組規定不依賴 streamlit）。\n⇒ 一句**有一半是假的宣稱**，比承認分不出來更危險（§1：錯誤的數字比沒有數字更危險）。\n【與 `why.asof` 的關係】這一段就是本頁**為什麼不能照另外四頁的規格畫一條「資料時點」列**的理由 —— 兩者講的不是同一種時間。⇒ 若日後要補時點列，**先讀這一段**。\n【量測日已標】依 `CLAUDE.md §8.2.A.0` 規則 4，code 內已就地標明量測日 2026-09-07；本線框不重抄那個結論、只指回去。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· live 原文（搬出機制細節）：「`@monitored` 多數掛在 **cache 裝飾器之內**，所以快取命中不會刷新它」「有些 fetcher 的快取是**寫在函式體內**的（例如 FRED 那一支，因為它所在的模組刻意不依賴 Streamlit，用不了 Streamlit 的快取裝飾器）」「本頁讀的是**登錄表**，分不出哪一支是哪一種」「快取與登錄表同屬一個 **process**」。\n· idle 原文：⛔ 不得包進任何 if。',
          src: 'src/ui/views/page_why.py:520（CACHE_SEMANTICS SSOT）＋ :2097（render）',
          flags: [] },

        { key: 'why.source.unmeasured.finmind_quota', name: '⑨ 具名、但這面牆量不到的來源（FinMind API 額度）', leaf: 'l2',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: null,
            idle: null,
            loading: null,
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: '**未接線（還沒做）** —— **剩餘額度這件事本頁看不到**\n為什麼：這面牆只看得到「有掛監控」的那幾支取數；這一個不在裡面。而且額度**比另外兩個具名來源更卡一層**：它是 FinMind **帳號層級**的資訊，不會出現在任何一次取數的回傳裡 —— 掛上監控也只會知道「這次抓成功了」，不會知道「還剩多少」。\n去哪補：**這一態沒有你可以做的事** —— 下一步是**先查證 FinMind 到底有沒有可以查額度的介面**，再決定要不要新增一支取數。\n⚠️ **這一步這一批沒有做到**（沙箱對外連線被擋，查不到）—— 所以本頁**不宣稱它做得到、也不宣稱它做不到**。\n〔展開佐證 ▸〕線框對這一格的期待「🟢 額度剩 62%」／卡住在哪一步／這一批查到哪裡／⚠️ 為什麼不畫一個估計值／⚠️ 為什麼不拿「已用次數」充數',
            error: null
          },
          asOf: null,
          evidence: '⭐ **這張卡存在的唯一理由：不畫，畫面就會是滿版綠。**\n`build_unmeasured_cards()` docstring（:1067-1078）逐字：「跳過不畫」是本頁**最隱形的一種假綠燈」——使用者不會知道某個常被引用的來源根本沒被量到。\n【為什麼不畫一個估計值（卡上 fact 原文）】「額度用罄是『其他頁整片變紅』最常見的單一原因；在這裡放一個猜出來的百分比，等於把最需要準確的那一格變成假的。」\n【為什麼不拿「已用次數」充數】「就算查得到用量，『已用 N 次』與『還剩多少』是**兩個不同的數** —— 沒有分母就換算不出剩餘額度，硬換算就是造假。」\n⇒ 這兩句是本頁對「分母」這件事的第二個落點（第一個是 16 vs 18）：**沒有分母就不要算比率。**\n【機制 vs 名單】`UNMEASURED_SOURCES` 的註解（:996-1002）逐字：「這個清單是**機制**，不是名單……**清單縮短不等於機制可以拆掉**」。FE-20 接上 FRED 與 TWSE 後清單只剩一筆，但機制留著。\n【⚠️ 每一筆的「去哪補」都必須不同】:1001-1002 逐字：共用一句「未接線」會讓「補哪一步就會好」這個資訊消失（前四頁反覆踩過的同一個坑）。\n【9 這個數字怎麼守】`MONITORED_FETCHER_COUNT=9`（:616）是全檔唯一字面值，畫面文案一律內插它；`tests/test_p05_why_view.py::TestMonitoredCountIsNotStale` 每次 CI 用 **AST 重數一次** `src/**/*.py`，對不上就紅。⚠️ 本組**未重跑**該測試（單組結論）。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· unwired 原文（搬出實測數字與 code 名）：「這面牆讀的是 **L0 fetcher 登錄表**，而登錄表只看得到掛了 `@monitored` 的 fetcher —— 實測（量測日 2026-09-07）`src/` 底下共 **9 支**掛了，這一個不在裡面」「下一步是先查證 FinMind 到底有沒有可查額度的介面，再決定要不要在 **L1 新增一支 fetcher、由 `src/services/` 轉出成 L3**」「沙箱對外連線被政策擋掉（實測 `api.finmindtrade.com` 的 CONNECT 回 **403**），而本 repo 早已移除 FinMind SDK」。\n⚠️ **缺口一字未縮**：states.unwired 仍寫「未接線（還沒做）」「這一批沒有做到」「不宣稱它做得到、也不宣稱它做不到」。',
          src: 'src/ui/views/page_why.py:1004（UNMEASURED_SOURCES）＋ :1067（builder）＋ :2099-2102（render）',
          flags: ['未驗證', '單組結論'] },

        { key: 'why.spec.flags', name: '⑩ 燈號表自己標記的「未接線 / 已失準」', leaf: 'l2',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: null,
            idle: null,
            loading: null,
            empty: '**沒有任何一盞被標記時，整段不出現**',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: '🟠 **這一盞會亮，但別照門檻讀**\n為什麼：〈來源給的原因原文〉\n去哪補：**這一態沒有你可以做的事** —— 這是**這盞燈本身**的已知限制，不是這一輪抓壞了，重按幾次都一樣；下方的門檻對照表有這一盞的完整說明。\n〔展開佐證 ▸〕這段話的出處／這一盞的門檻（**已加刪除線 ＋「現在不要拿它當操作依據」**）／值從哪來\n本頁實況 2 張：**融資餘額（總經燈）**、**財報趨勢（持股燈）**',
            unwired: '**這一盞刻意沒有接**\n為什麼：〈來源給的原因原文〉\n去哪補：**這一態沒有你可以做的事**（同上）\n本頁實況 1 張：**外資現貨淨買賣（總經燈）**',
            error: '🔴 門檻表本身讀不出來時，整面牆改出一張紅卡（**這時牆上不該有任何綠格子**）'
          },
          asOf: null,
          evidence: '🔴 **這一格要處理指令第 3 點（「門檻已失準」標籤問題）。本組實跑了，結論與指令前提不同，據實回報。**\n\n【本組實測：本頁到底有幾張卡印「🟠 門檻已失準」】實跑 `build_*` 全部 builder ＋ `state_meta()`，本頁共 **3 張**：\n　(1) 融資餘額（總經燈）—— L0 `margin` 真的標 `discriminative=False`\n　(2) 財報趨勢（持股燈）—— L0 `stock_trend` 真的標 `discriminative=False`\n　(3) 同一個名詞，兩套刻度 —— 由 (2) 連帶算出來的（見 `why.edu.scales`）\n⇒ **這 3 張在本頁都是「真的失準」，標籤沒有說謊。**\n\n【指令說的那 5 張在哪裡 —— 不在本頁】依 `STATE_SEMANTICS_QA2.md:25-36, 60-64, 99`（AST 窮舉全 `src/` 的 49 處 `classify_ui_state` 呼叫）：把**覆蓋率／只抓到一部分**餵給 `discriminative=` 的共 **5 處**，全部在\n　`page_today.py:1184`、`page_find.py:1329`、`page_hold.py:2549 / 2632 / 2733`。\n其中 `page_why.py:764` 那一處餵的是**真的 `spec.discriminative` 旗標**（與 `page_today.py:879`、`page_hold.py:1703` 同屬「真旗標」那 3 處）。\n⇒ **「本頁有 5 張卡誤標」這個前提，本組實測不成立。誤標的 5 張在另外三頁。**\n\n【但本頁仍然被牽連，而且是最不該被牽連的一頁 —— 這才是要拍板的點】\n那 5 張誤標卡的病灶是**標籤本身**：`shared/ui_state.py:118` `UI_DEGRADED: ("門檻已失準", "🟠", TRAFFIC_YELLOW)` 是**全站共用的 SSOT**，本頁這 3 張讀的是同一行。\nQA2 的建議修法是「**改 `UI_STATE_META[UI_DEGRADED]` 的標籤**（改成因果中性的說法，例如「打折的數字／別當結論讀」），一行 SSOT 就同時修好 5 處」。\n⚠️ **那一行一改，本頁這 3 張的 chip 也會跟著變** —— 而本頁這 3 張的「門檻已失準」**是正確的**（L0 真旗標）。\n⇒ **改 SSOT 會把一句對的話換成一句比較模糊的話，去修另外三頁的假話。** 這是一個跨頁取捨，**不是本組可以自決的**。\n★ **總管的方案**：改 SSOT（QA2 那條），因為「假話」的代價高於「模糊」的代價；**本頁這 3 張的精確語意改由卡上的 `now` 句子承擔** —— 它們本來就已經各自寫了「這一盞會亮，但別照門檻讀」（:1383）與 L0 原文的 `degraded_reason`，**資訊一個字都不會少**。⚠️ 這是本組提出的方案，**待客戶／總管拍板**。\n\n【⚠️ 據實標明】上述 5 處的窮舉是 `STATE_SEMANTICS_QA2.md` 那一組的 AST 結果，**本組沒有自己重跑 AST**；本組自己重跑的只有「本頁 3 張」與「本頁 :764 餵的是真旗標」這兩件。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「⑩ **L0 規格表**自己標記的「未接線 / 已失準」」。\n· empty 原文：`build_spec_flag_cards()` 回空 tuple，:1400-1406；render 端 `if _flags:`，:2106。\n· degraded／unwired 原文：〈L0 `degraded_reason` / `unwired_reason` 原文透傳〉；「⛔ **沒有出口** —— 這是**規格層面**的已知限制」；「走 `threshold_display`」。\n· error 原文：:2085-2088 `if not _scan.readable:` 先畫紅卡。',
          src: 'src/ui/views/page_why.py:1371（builder）＋ :2104-2109（render）＋ shared/ui_state.py:118（標籤 SSOT，★ 待拍板的那一行）',
          flags: ['★待拍板', '單組結論', '未驗證'] },

        { key: 'why.source.coverage', name: '⑪ 涵蓋率揭露（常駐 caption · 本頁最重要的一段話）', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '**這面牆涵蓋什麼**：只涵蓋**有掛監控、而且這次瀏覽中被用過**的取數，再加上燈號表**自己標記**的「未接線／已失準」。這兩樣都不需要連網，本頁不會為了畫這面牆去打任何 API。\n**線框具名的那三個來源**：**FRED** 與 **TWSE** 都已掛上監控；**FinMind 的 API 額度仍然量不到**。\n⚠️ **「那一支綠」不等於「那個來源好」**：這面牆量到的單位是**單一取數函式**，不是**整個來源**。\n**不涵蓋什麼**：全站來源清單另外存在別的地方，本頁目前拿不到 → **大多數來源仍然不在這面牆上**。實測（量測日 2026-09-07）只有 **9 支**取數掛了監控，而全站來源清單有數十個。\n⚠️ **所以：這面牆上沒有紅燈，不等於全站都好。**',
            idle: '同 live（常駐，不隨載入狀態消失）',
            loading: '同 live',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '同 live'
          },
          asOf: null,
          evidence: '⭐ **這段話是本頁對自己的誠實揭露 —— 它說的是「我看不到什麼」。**\n`COVERAGE_DISCLOSURE` 的註解（:635-638）逐字：「FE-20 把 FRED 與 TWSE 接上之後，這段話**沒有被縮短，只有被改準**。『掛了 3 支就以為全站都掛了』正是這段話要防的那種樂觀 —— 接上兩支之後，它要防的東西**沒有變少**，只是名單換了幾個名字。」\n【分母揭露（指令第 2 點的第三個落點）】這段話明寫「9 支 vs 全站數十個」——**分母比分子大得多，而且說出來了**。⇒ 這正是「N/M 盞」類顯示該有的樣子：**分母的口徑要寫出來，而且要寫出它涵蓋不到什麼。**\n【9 這個數字的守衛】同 ⑨：全檔唯一字面值 `MONITORED_FETCHER_COUNT`（:616）＋ CI 用 AST 重數。畫面上還印了**可重現的指令** `grep -rn "^@monitored" src/ --include=*.py`（:624 `MONITORED_COUNT_RECIPE`）—— :618-623 逐字記載：原文印的是不帶 `^` 的版本，那道指令數到的是**字樣數**不是支數，讀者照著跑會得到一個對不上的數字，然後**合理地認為這面牆在說謊**。⇒ 已改正，性質＝**修正錯誤**。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· live 原文（搬出 code 位置與分層理由）：「只涵蓋掛了 `@monitored`（L0 `shared/fetch_monitor.py`）**而且本 session 已經被載入過**的 fetcher」「全站來源清單的 SSOT 住在 L1 `src/data/core/data_registry.py`，本頁是 L5、依 `CLAUDE.md §8.2` 不得直接讀它，而 `src/services/` 也沒有把它轉出來」「這面牆量到的單位是 **fetcher**，不是**來源**」。\n⚠️ **本段最重要的三句話一字未動，仍在 states.live**：「那一支綠不等於那個來源好」「大多數來源仍然不在這面牆上（只有 9 支掛了監控）」「**這面牆上沒有紅燈，不等於全站都好**」。\n· idle 原文：⛔ 不隨載入狀態消失。',
          src: 'src/ui/views/page_why.py:639（COVERAGE_DISCLOSURE SSOT）＋ :2110（render）',
          flags: [] }
      ] },

    /* ═══════════════════════════════════════════════════════════
       葉2 工程師版（單一 gate · 兩步）
       ═══════════════════════════════════════════════════════════ */
    { n: 4, label: '葉2 資料體檢 · 工程師版｜維持現行單一 gate',
      blocks: [

        { key: 'why.engineer.gate', name: '⑫ 進階診斷（要兩步才打得開）', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '🟢 運作中　**已載入 · 名單上有 N 支取數**\n展開後：☑ 已勾 → 下方出現六個面板（其中五個未接線，見下一格）\n〔展開佐證 ▸〕這一段的出處／為什麼要一顆勾選框擋著',
            idle: '⬜ **進階診斷未載入**\n為什麼：六個面板各自會組大量表格，預設不跑。\n去哪補（**兩步，兩個不同的 label**）：在「📖 憑什麼 › 資料體檢」**展開**「🔧 進階診斷（工程師用；一般使用者不需要打開）」→ **勾選**「載入進階診斷（較耗時，部分項目會實際打外部 API）」。',
            loading: '骨架',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '🔴 **進階診斷本身讀不出來** ＋ 錯誤型別原文\n去哪補：**這一態沒有你可以做的事** —— **這是名單本身的問題，不是某一個資料來源壞掉**；請把上面那行訊息回報給維護者。\n⭐ **兩件事不會被混在一起**：一盞來源燈紅了是**有效結果**（那一盞自己會紅），**不代表診斷壞了**。'
          },
          asOf: null,
          evidence: '🔴 **指令第 1 點點名的錯就在這裡：上一輪把「兩步、兩個不同 label」混成一個。**\n實測（本組自己回 code 驗，逐行）：\n　· **第一步 = 展開**　`st.expander(ENGINEER_EXPANDER_LABEL, expanded=False)` 在 **`page_why.py:2119`**；\n　　label 文字 SSOT 在 **`:337`**：「🔧 進階診斷（工程師用；一般使用者不需要打開）」。\n　· **第二步 = 勾選**　`st.checkbox(ENGINEER_CHECKBOX_LABEL, key=SS_ENGINEER_GATE, …)` 在 **`page_why.py:2121`**；\n　　label 文字 SSOT 在 **`:338`**：「載入進階診斷（較耗時，部分項目會實際打外部 API）」。\n⇒ **兩個不同的控制項、兩個不同的字串。** 指路句 SSOT 已經寫成兩步：`ENGINEER_WHERE`（:366-369）＝ `在{資料體檢}{展開「摺疊標題」}→ {勾選「checkbox 標題」}`。\n【舊線框的兩個錯，S1-3B §3.5.0 已一併更正】(a) 把摺疊標題**截短了後半句**（少了「一般使用者不需要打開」）；(b) 指路只寫了一步、而且寫的是**另一個控制項的名字**。\n【⚠️ 方法教訓（核准線框 N5 逐字，寫下來防重犯）】上一輪的補畫清單**五項全是按鈕**，對照組也是按鈕 —— **一個按鈕形狀的搜尋，看不到一顆 checkbox。** 字表選錯 → 結構上就掃不到，再跑一百次也一樣。\n【⛔ 為什麼沒有 form（F7／DECISIONS #7 撤回）】v1 說「6 個獨立 checkbox 包成一個 form」，實測是 **1 個** checkbox 全有全無、六個 panel 檔內 checkbox **各為 0** ⇒ **要解的問題不存在**，`form_diag` 撤回。`build_engineer_card` 的 fact 逐字：「form 的價值是『N 個 widget 只送一次』；這裡**只有一個** widget，包了省不到任何一次 rerun，只是多一次點擊」。\n【⚠️ 摺疊 ≠ 省執行】卡上 fact 逐字：「摺疊只收合**視覺**，body 每次 rerun 照樣執行 —— 埋起來一分效能都沒省。真正省下執行的是這顆 gate。」沒勾 → **一次 L0 登錄表都不讀**（:2124）。\n【⛔ 這顆不是主 CTA】它是葉內 gate，不在首屏、不是 form submit。`mainCTA` 仍為 null。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「⑫ 進階診斷 gate（⚠️ **兩步、兩個不同 label**）」。\n· live 原文：「登錄表有 N 支 **fetcher**」「為什麼要一顆 **checkbox** 擋著／⚠️ 為什麼**沒有包 form**」。\n· loading 原出處：S1-3B §3.5.7 葉2 工程師版列。\n· error 原文：「⛔ 沒有出口 —— 這是 **L0 登錄表**的問題」「⭐ **三態不得混的結構性落點**…用一格的狀態去代表一整段，會讓「某個源這輪沒回」與「診斷本身掛了」在畫面上長得一樣」。',
          src: 'src/ui/views/page_why.py:2119（expander）＋ :2121（checkbox）＋ :337-338（兩個 label SSOT）＋ :366（兩步指路 SSOT）＋ :1618（builder）',
          flags: [] },

        { key: 'why.engineer.monitor', name: '⑬ 🛰️ 取數監控（六個面板中唯一接得上的）', leaf: 'l2',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '表格八欄：取數名／分類／更新頻率／最後一次真實抓取／回了幾列／耗時(ms)／錯誤／本頁看不懂的狀態\n下方小字：「未執行」＝ 這次瀏覽中還沒真的對外抓過（**吃快取不會點亮它**）。⚠️ 綠燈上那個**時間**是另一回事，見使用者版牆下的「這面牆上的時間是什麼」。',
            idle: '沒勾選時**整段不畫** —— ⬜ 只留上一格那張灰卡',
            loading: '骨架',
            empty: '`　名單目前是空的 —— 說明見上方使用者版的那張灰卡`\n⚠️ **理由不重寫第二次**：同一句話在畫面上出現兩次，兩份遲早不一致。',
            missing: '同 empty',
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '🔴 該面板；**另外五個維持原樣**'
          },
          asOf: null,
          evidence: '【六選一：只有這一個接得上】:2131-2133 逐字：「**六個面板** —— 目前只有「Fetcher 監控」接得上；其餘五個各自卡在不同的地方，逐項寫在下面。」\n【八欄裡最容易被忽略的一欄】「**本頁看不懂的狀態**」—— L0 給了本檔不認得的狀態字面值時，原文放這裡，⚠️ **不認得一律當紅，絕不當綠**（`SourceProbe.unknown_status` docstring，:794-795）。\n【⚠️ 已登記的第二真相源】`STATUS_NEVER_RUN` / `STATUS_OK` / `STATUS_FAILED`（:385-387）是**重抄**的 —— L0 `shared/fetch_monitor.py` 沒有把這三個狀態匯出成常數（實測量測日 2026-09-07：三個都是 inline 字面值）。code 端已依 `CLAUDE.md §-2 規則 6` 據實揭露，並以 `tests/test_p05_why_view.py::TestStatusLiteralsMatchL0` 直接掃 L0 原始碼，字面值一漂移就 CI 紅燈（**不是靠自律**）。⚠️ 本組**未重跑**該測試。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· name 原文：「⑬ 🛰️ **Fetcher** 監控（六面板中唯一接得上的）」。\n· live 原文：caption 為「**🛰️ Fetcher 監控（`@monitored` 自我登錄）**」；「本 session 尚無真實外抓」。\n· idle／empty 原文：`if not _opened: return`，:2128-2129／:2153。\n· error 原文：⛔ **不染另外五個**（逐面板獨立判態）。',
          src: 'src/ui/views/page_why.py:2131-2154（render）＋ :385-390（狀態字面值，已登記的第二真相源）',
          flags: ['未驗證'] },

        { key: 'why.engineer.panels', name: '⑭ 其餘五個面板（資料源清單／雙演算法對帳／API 根因／原始資料表／門檻校準）', leaf: 'l2',
          cols: { desktop: 3, tablet: 2, phone: 1 },
          states: {
            live: null,
            idle: '沒勾選時**整段不建構** —— 不畫，只留一張灰卡。「**還沒打開**」與「**打開了但這一項沒接線**」是**兩件事，不會同時出現在畫面上**。',
            loading: '骨架',
            empty: null,
            missing: null,
            na: null,
            partial: null,
            degraded: null,
            unwired: '**五張全部未接線（還沒做）**。\n⚠️ **五張的「去哪補」刻意各自不同** —— 它們卡住的位置不一樣：\n· **資料源清單**：完整清單存在別的地方，本頁目前拿不到，而且不想要求你「得先去過另一頁」才看得到。\n· **雙演算法對帳**：算法本身已經有了，但還沒有人把它接到這一頁，輸入也還得由上游餵進來。\n· **API 根因診斷**：現行做法會**當場對外送出請求**去比對連線方式 —— 那件事不該由這一頁做。⚠️ 在搬好之前，既有的 🔎 資料診斷分頁**沒有下架**，照舊可用。\n· **原始資料表**：要的是「這一輪各頁抓回來的表格本體」，那些只存在於各頁自己身上。本頁去翻別頁的暫存等於跟它們暗自綁死 —— 對方一改，這裡就會**靜默變空**（而空白看起來像沒問題）。\n· **門檻校準**：校準是排程的離線工作。**「顯示現在的門檻」是唯讀、可以做；「重跑校準」會改到全站判燈，那要另外拍板才會開。**',
            error: '🔴 該面板；其他四個維持原樣'
          },
          asOf: null,
          evidence: '【為什麼五張的 live 是 null】五個 `L0CardSpec` 全部寫死 `wired=False`（:1683 / :1695 / :1708 / :1721 / :1733），而 `classify_ui_state` 的第 1 條就是 `if not wired: return UI_UNWIRED`（`shared/ui_state.py:199-200`），**排在所有其他條件之前** ⇒ 任何值、任何請求都不改變它。旗標在 L0 被翻回 `True` 之前，這五張**只有 unwired 一態**。\n【⚠️ 每一筆的「去哪補」都不同，這是刻意的】:1678-1679 逐字：「每一項的『去哪補』都不同 —— 它們卡住的位置不一樣。」共用一句「未接線」會讓「補哪一步就會好」這個資訊消失。\n⇒ 這是指令第 4 點在**同一態內**的延伸：三個灰要分開講，而**同一種灰的不同原因也要分開講**。\n【⛔ 這五張不准被說成「不適用」】它們對這一頁**完全適用**（線框對每一格都有明確期待，逐格寫在 fact「線框對這一格的期待」裡），只是接不上。說成「不適用」等於宣稱這五件事不該存在。\n【與 S1-3B §3.5.9 的對應】§3.5.9 列的三件「本頁已知做不到的事」中，第 1 項（「備援鏈跑到第幾層」的揭露）**repo 裡不存在這個資訊**，⛔ **不承諾尚未存在的東西** —— 本線框因此**沒有**為它開任何一格。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· idle 原文：引 `page_why.py:1750-1757` 逐字「**只有在 gate 打開時才會被建構**…『三態不得混』的落點」。\n· unwired 原文（**五條的完整技術理由，全文照收**）：\n  ① 本組實跑：`build_engineer_panel_cards()` → 5 張，state 皆 `unwired`，chip 皆 `("未接線","⛔","#888888")`。\n  ②「**資料源清單**：清單 SSOT 住 L1 `data_registry.py`，現行診斷面板直接讀 `st.session_state[...]`（由總經頁寫入）。本頁是 L5，**既不得直呼 L1，也不想依賴「你得先去過另一頁」** → 要在 `src/services/` 補一支唯讀 L3。」\n  ③「**雙演算法對帳（§4.3）**：純函式在 L2（`src/compute/risk/reconcile.py`），但 `src/services/` **沒有**任何一支把它包起來，而且輸入也得由上游餵進來 → 補 L3 wrapper。」\n  ④「**API 根因診斷**：現行實作（`src/ui/pages/api_diagnostic.py`）會**當場對外送出請求**比對「走 proxy vs 直連」—— 那是 L5 直接打外部 API，正是 `CLAUDE.md §8.2` 明文禁止的；把它 import 進來**等於把違憲一起繼承** → 探測要先搬到 L1／L3。」\n  ⑤「**原始資料表**：要的是「這一輪各頁抓回來的 DataFrame 本體」，那些只存在於各頁自己的 `st.session_state`。本頁去翻別頁的 session key 等於**跟那些頁面用約定俗成的字串耦合** —— 對方改一個 key，這裡就會**靜默變空**。」\n  ⑥「**門檻校準**：校準是 `scripts/` 下的離線工作（季度 cron ＋ PR 審閱後才寫入），現行面板從 UI 直接 import `scripts/`…「**重跑校準**」會改到全站判燈，屬不可逆操作，**需要客戶拍板**。」\n· error 原文：⛔ 不染其他四個。',
          src: 'src/ui/views/page_why.py:1680（ENGINEER_PANEL_SPECS 五筆）＋ :1750（builder）＋ :2133（render）',
          flags: [] }
      ] },

    /* ═══════════════════════════════════════════════════════════
       葉3 AI 問答（chat_input · 天然 gate）
       ═══════════════════════════════════════════════════════════ */
    { n: 5, label: '葉3 AI 問答｜送出就是啟動（不需要另外按鈕）',
      blocks: [

        { key: 'why.qa', name: '⑮ AI 問答（對話區 ＋ 狀態卡）', leaf: 'l3',
          cols: { desktop: 1, tablet: 1, phone: 1 },
          states: {
            live: '🟢 運作中　**已回答（內容在上方對話裡）**\n對話氣泡（**由上而下，同一則訊息**）：\n① **強制免責**（`S1-6_COMPLIANCE_COPY_GUIDE.md §3.1`「✅ 全站樣板」的**版本 C・自由問答型**；規格原文的適用例就點名本葉所屬的 **A1**）—— 逐字如下，⛔ **不得改寫或縮寫**：\n⚠️ **這是 AI 生成的回答，不是投資建議。**\n模型只看得到本站提供的資料，**看不到你的資金、持有期間、風險承受度與稅務狀況** ——\n這些才是決定該不該做一筆交易的關鍵，而它們一個都不在它手上。\n回答中若出現任何買賣方向的說法，請當成模型的措辭問題，不是本站的結論。\n② 回答本文，前綴 **🧬**（AI 敘述旗標）。⚠️ **🧬 本身不是免責** —— `§3.0` 的 AI 出口盤點表把「**只有 🧬 符號**」列在「⚠️ 半有」那一列，說明欄逐字寫「**不是免責**」。它標的是「這一則是模型寫的」，在長對話捲動後仍指得出位置，**是加在免責之外的東西，不是免責的替代品**。\n〔展開佐證 ▸〕這一葉的出處／AI 拿得到什麼／模型／這一輪用到的工具\n⚠️ **免責與回答是同一個字串**（`page_why.compose_qa_message()`）：對話紀錄存的就是這一份，下一次 rerun 從紀錄重播時免責**不會消失**。\n⚠️ **免責只在真的有 AI 文字時才印** —— 失敗態與 `empty` 態畫面上一個字的 AI 文字都沒有，對不存在的內容做免責等於替一個沒發生的事情背書（`CLAUDE.md §1`）。\n📌 **免責文案的 SSOT 是 L0 `shared/compliance_copy.AI_QA_DISCLOSURE`**，本線框與 `page_why.py` 都只是引用它；上面那段文字與 L0 逐字相同，漂移會被 `tests/test_p05_why_view.py::TestQaDisclosure::test_the_wireframe_quotes_the_same_ssot` 當場打紅。',
            idle: '⬜ **尚未提問**\n為什麼：AI **只根據本站已經載入的資料**作答，不自行上網查 —— 所以它不會憑空生出數字；某一頁如果還沒載入，它會直接說「那一頁還沒有資料」，而不是猜一個。\n去哪補：直接在下方輸入問題並送出，**送出本身就是啟動**（不需要再按別的鈕）。\n⭐ **第一次進來時是灰的，不是紅的 —— 即使根本沒有金鑰。**',
            loading: 'AI 回答時**逐字流出**（送出之後）',
            empty: '▨ **這一輪沒有回答內容**\n為什麼：上游沒有回報錯誤，但也沒有給任何文字 —— 這是一個**有效結果**（模型真的沒話說），**不是故障**。\n去哪補：換一個更具體的問法再送一次（例如指名一檔代號）。',
            missing: '▨ 「那一頁還沒有資料」—— **它不會猜一個**。\n⚠️ 這一句是 **AI 自己回的**，不是本頁畫的狀態；本頁能保證的只有「它被要求這樣做」。',
            na: null,
            partial: null,
            degraded: null,
            unwired: null,
            error: '🔴 **AI 問答未啟用**\n為什麼：未偵測到 Gemini API 金鑰（部署端沒有設，或這個環境讀不到）。\n去哪補：**其他分頁的功能完全不受影響** —— 只有這一葉需要金鑰。金鑰目前由部署端設定；**這一頁沒有就地輸入金鑰的地方**，因為那等於在畫面上收你的憑證。\n＋ 另一種紅：**這一輪問答失敗了** ＋ 錯誤原文 ＋ 「可以換個問法再送一次；若持續失敗，請把上面那行訊息回報給維護者。**其他分頁的功能完全不受影響**」'
          },
          asOf: null,
          evidence: '🔴 **這一格與規格／核准線框有一處實質偏離，據實標出，★待拍板。**\nS1-3B §3.5.6 ＋ 核准線框 `err.where` 都寫「**本頁就地顯示設定入口與步驟**」（並註明 F5 更正：⛔ 不得把使用者指去改 `.streamlit/secrets.toml`，客戶不碰那個檔）。\n**但 code 端沒有做那個入口**：`NO_KEY_WHERE`（:1767-1771）逐字寫「⛔ **沒有**在這一頁就地設定，因為那等於**在畫面上收憑證** —— 要開這個入口必須先由客戶拍板『這一頁可以收金鑰』，本批不做」。\n⇒ **兩邊都對了一半**：規格對的是「不要把使用者丟去改 toml」，code 對的是「畫面上收憑證要先拍板」。**本線框照 code 寫**（它是現況），並把這個缺口標成 ★待拍板。\n\n【天然 gate，為什麼不能包 form】`st.chat_input` 只有在「使用者這一輪剛送出」時才回字串，其餘 rerun 回 `None` —— 它**天生**就是一個乾淨的 gate 旗標（:1784-1787）。而 Streamlit **明文禁止**把 `chat_input` 放進 `st.form`（:174-175）。⇒ 這也是 `mainCTA: null` 的第二個理由：**本頁連放一顆 submit 的地方都沒有。**\n【⛔ 不用 `len(history)` 頂替 gate】:1786-1787 逐字：那會讓「上一輪問過、這一輪沒問」被誤判成「這一輪問了」，正是 L0 鐵律禁止的**從資料反推**。\n【⭐ 為什麼冷啟動不先檢查金鑰】`load_qa()` docstring（:1817-1822）逐字：「**連『有沒有金鑰』都不先問。**」讀金鑰是 L3 呼叫（會碰 `st.secrets` 與環境變數），在使用者送出問題之前去問一次，就違反了本頁對自己的承諾「沒叫過就不發任何請求」，而且會讓一個從沒用過 AI 的使用者一進頁面就看到紅色 —— **那正是 v3 §02 要杜絕的假性錯誤**。\n【合規】「AI 拿得到什麼」fact 逐字：「只有本站工具回傳的結構化結果。它被要求**『需要數字時一律呼叫工具，嚴禁自行計算或杜撰』** —— 工具回不出來時它必須說是哪個來源、為什麼。」\n⚠️ **本頁能保證的只有「它被要求這樣做」**，不能保證模型每次都照做 ⇒ `states.missing` 那一格已標明那句話是 AI 自己回的。**這是本組的判讀，不是 code 明寫的保證** → 單組結論。\n【S1-3B §3.5.9 第 3 項】AI 回答的引用**無法**追溯到 `file:line` —— 它引用的是「本站已載入的數字」，不是程式碼。⛔ 不承諾。\n\n【文案瘦身 2026-09-14｜從 states／name 搬進來的實作備註（原文照收，非新結論）】\n· live 原文：〔展開佐證〕第一項為「這一葉的出處（**L3 金鑰 ＋ L3 AI 問答**）」。\n· idle 原文：「⭐ **冷啟動時是 idle，⛔ 不是紅色**」。\n· loading 原文：`chat_input` 送出後，S1-3B §3.5.2。\n· missing 原出處：S1-3B §3.5.7 葉3 ④⑤ 欄；原帶「**⛔ 不猜**」與「（見 evidence）」。\n· error 原文：「金鑰目前由部署端提供（`st.secrets` 或環境變數的 `GEMINI_API_KEY`）；⛔ **沒有**在這一頁就地設定的入口…要開這個入口必須先由客戶拍板「這一頁可以收金鑰」，本批不做。」＋「例外原文」。',
          src: 'src/ui/views/page_why.py:2169（chat_input）＋ :1862（狀態卡 builder）＋ :1816（load_qa 邊界 a~e）＋ :1765-1777（金鑰與 idle 文案 SSOT）',
          flags: ['★待拍板', '單組結論'] }
      ] }
  ]
});
