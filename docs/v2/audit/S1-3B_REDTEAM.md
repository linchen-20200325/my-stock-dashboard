# 【S1-3B】完整草稿規格 — 獨立紅隊稽核報告

> **產出日** 2026-09-14　**執行** 完整草稿紅隊組（QA/Audit，單組）
> **稽核對象** `scratchpad/S1-3B_FULL_DRAFT_SPEC.md`（3,157 行）
> **範圍（總管 2026-09-14 縮小後）** **§1 會炸的地方** ／ **§3 C-05 兩條宣稱重驗** ／ **§6 三情境壓測** ＋ §8 查過沒錯的 ＋ §9 沒查到的。
> §2（發明）／§4（漏標偏離）／§5（稿內違規句）／§7（完整性）**不在本組射程**，已另派兩組。
> **唯讀聲明** 本組未修改 repo 任何檔案、未執行任何 git 寫入。唯一寫入為本檔（scratchpad）。

## §0 方法

1. **不採信轉述。** 草稿引用的每一條 `file:line` 都回原檔 `sed -n` 讀出。
2. **能實跑的就實跑。** §1.1／§1.2／§1.3 三條**用 python 直接構造草稿規定的字串丟進 production 的驗證器**，貼出實際的例外訊息（見各條「實跑」欄）。這不是推論。
3. **正向 ＋ 反向。** 除了「它寫的對不對」，另外掃「**它沒寫、但 code 會擋**」的東西（`Note.__post_init__` / `where_to_find` / CI 守衛）。§1 的前三條全部來自反向掃描。
4. **控制流複驗**（§3(b)）：不只看「誰呼叫 `headline()`」，另外問「**改掉那個字串之後，誰會靜默壞掉**」，並把 repo 自己的 CI regex 抽出來對草稿建議的新文案跑一次。

---

# §1 會炸的地方

> 排序 = 嚴重度。每條標「**是不是 raise**」「**證據等級**」。
> ⚠️ 先講結論：**主筆組宣稱採納的 R-A~R-D 四條，R-B/R-D 採對了，R-A 只修了一半、R-C 少了實作前置。**
> 而**四條之外另有三類會當場 raise 的東西，草稿從頭到尾沒提**——它們都不在 `S1-4` 原矩陣裡，所以「照紅隊修正抄」抄不到。

---

## 1.1 🔴🔴 **最嚴重：草稿規定的「現在怎樣」文案自帶狀態 glyph → `Note` 建構期 `raise ValueError`（約 20 處）**

### 是 raise。證據等級：**實跑證明**。

`src/ui/tabs/tab_today.py:190-213`（`Note.__post_init__`）逐字：

```
206:            _bad = sorted(_g for _g in _STATE_GLYPHS if _g in _v)
207:            if _bad:
208:                raise ValueError(
209:                    f"Note.{_f} 含狀態 glyph {_bad} —— "
210:                    "glyph 只由 `state_meta()` 供給一次，文案不得自己再帶；"
211:                    "`shared/ui_state.py` 明文『灰的四個狀態必須靠 glyph 分辨』，"
212:                    "兩個 glyph 就是兩個互相矛盾的說法")
```

`_STATE_GLYPHS`（`tab_today.py:80`）＝ `UI_STATE_META` 的七個 glyph ＝ **`⬜ ⏳ 🔴 ▨ 🟠 ⛔ 🟢`** —— **草稿全篇用來開頭「現在怎樣」那一句的，正好就是這七個。**

**實跑**（把草稿原文逐字丟進 `Note(now=..., why='x', where='y')`）：

```
RAISE  draft 3.1.1 ①  '⬜ **市場位階 · 尚未評估**'            -> Note.now 含狀態 glyph ['⬜']
RAISE  draft 3.2.1     '⬜ **尚未選股**'                       -> Note.now 含狀態 glyph ['⬜']
RAISE  draft 3.2.5     '▨ **選股已完成，符合條件的標的是 0 檔**' -> Note.now 含狀態 glyph ['▨']
RAISE  draft 3.4.6     '🔴 **無法讀取持股清單**'                -> Note.now 含狀態 glyph ['🔴']
RAISE  draft 5.2       '▨ **尚未設定目標比例**'                 -> Note.now 含狀態 glyph ['▨']
RAISE  draft 3.1.7 ①⑧ '⬜ 位階已評估，分母標 14／16（未接線 2）' -> Note.now 含狀態 glyph ['⬜']
```

**production 的寫法從來不帶 glyph**（對照，這是它一直都對的地方）：
`page_today.py:1063` `now=f"{_spec.label}　**尚未載入**（還沒有人去取這份資料）"`；
`page_hold.py:2171` `now="**再平衡未接線**"`；`page_today.py:1006` `now=f"{_spec.label}　**這盞燈沒有在運作**（決策端刻意未接線）"`。

### 草稿的哪些行會踩到（本組掃出 33 條 Note-shaped 行，其中下列**不可能用「那是 chip」解釋**）

| 草稿行 | 原文 | 為什麼不能解釋成 chip |
|---|---|---|
| **L1373（3.1.7 ① 結論燈 ⑧ 未接線欄）** | `⬜ 位階已評估，分母標 14／16（未接線 2）；**重按不會改變它**` | 該格的狀態是 **⑧ 未接線**，chip 是 **⛔**。這裡的 **⬜ 只能來自 body** ⇒ 必 raise，而且它**正是 `必修-2` 註解裡記載的那個原型 bug**（原文：「稽核實測：chip 印「⛔ 未接線」、body 印「⬜ … 尚未接線」」）。草稿把那個 bug 逐字抄回來了。 |
| L727／L728／L729（2.10.4c 頁級空狀態） | `▨ 你的帳本裡沒有任何持股` ／ `🔴 已綁定，但讀不到任何持股列` ／ `▨ 選股已完成…0 檔` | 三格下面緊接 **為什麼／去哪補** ⇒ 結構上就是 `Note` 三要素，glyph 寫在 `now` 裡 |
| L1154 / L1176 / L1183（3.1.1 冷啟動 ①／④／⑤⑥） | `⬜ **市場位階 · 尚未評估**` 等 | 同上，下方兩行是 `為什麼：`／`去哪補：` |
| L1457 / L1473 / L1565 / L1661 / L1746 / L1791 / L2057 / L2213 / L2248 / L2534 | 頁2~頁5 ＋ Deep Link 的灰/紅態 | 同上 |
| 五張 `x.7` 矩陣的**每一格** | `⬜ 尚未選股＋「勾好條件後按🎯開始選股」` 這種寫法 | 矩陣是給實作照填的，格內文字就是 `now` |

### 為什麼主筆組沒抓到

它採的是 `S1-4 紅隊 §2.1`（**`Card.value`** 那條守衛）。`Note` 那條守衛在**另一個 dataclass、另一個 `__post_init__`**，紅隊報告沒提，`S1-4` 原矩陣也沒提 ⇒ **「照紅隊修正抄」結構上抄不到它**。
這與草稿自己在 `3.5.0` 寫下的教訓同型：**「一個按鈕形狀的搜尋，看不到一顆 checkbox」**——一個 `Card` 形狀的搜尋，也看不到一個 `Note`。

### 最小修法（給主筆組，不必動 code）

在 **Part 2.5.3 的那張位置表**補第 0 列，並在**每一張 `x.7` 矩陣的表頭**加一句：

> **狀態 glyph 只出現在「狀態徽章列」，⛔ 不得寫進 `Note` 的三要素（`now`/`why`/`where`）任何一句。**
> 本稿所有框線圖與矩陣裡寫在句首的 `⬜/▨/🔴/⛔/🟠/⏳/🟢`，**一律代表徽章，不是文案的一部分**。
> 實作時 `now` 從 glyph 之後那個字開始。守衛：`tab_today.Note.__post_init__`（會 raise）。

⚠️ 沒有這句，`x.7` 矩陣照填就是 §1.1 這一串 raise。

---

## 1.2 🔴 **R-A 只修了一半：⑦ 門檻失準「值照給」仍在大字數值區 → `Card` 建構期 `raise`**

### 是 raise。證據等級：**實跑證明 ＋ production 已有正解可對照**。

**草稿自己前後矛盾**：

| 草稿處 | 怎麼寫 |
|---|---|
| **2.5.1 表（L248）⑦ 門檻失準** | 「大字數值區」欄寫 **「值照給，判決語留白」** |
| **2.5.3 表（L300）** | 「**只有「① 正常」與「⑥ 涵蓋不全」能放值。** 其餘一律**留空**」 |
| **3.1.4 ⑦ 框線圖（L1326）** | 畫 `│ **+1.2**     │` ＋ `│ 🟠 門檻已失準 │` —— **採的是會炸的那一版** |
| **3.3.4（L1773）** | 「⑦ 門檻失準：`⚠️ 78 分（趨勢因子無 MA，不計入）`」 —— 同上 |

**實跑**：

```
RAISE  Card(state=UI_DEGRADED, note=..., value='+1.2')
   -> 卡 'k' 不是正常態卻帶了結論文字 '+1.2' —— 只有 live 能給結論
RAISE  Card(state=UI_DEGRADED, value='')  # 沒給 Note
   -> 卡 'k' 的狀態是 'degraded' 卻沒有 Note —— 非正常態一律要附三要素（鐵律 4）
```

（`UI_DEGRADED = "degraded" ≠ UI_LIVE`，`shared/ui_state.py:126`；守衛在 `tab_today.py:237-240`。）

### production 早就解掉了，草稿反而繞回去

`src/ui/views/page_today.py:993-995` 逐字：

```
993:    # 非 live 一律要三要素。`Card` 規定非 live 不得帶 `value`，
994:    #   所以**已失準**那一態的現值改掛在 facts（它是有值的，不能藏起來）。
995:    if _state == UI_DEGRADED:
996:        _facts.insert(0, ("現值", _shown or "—"))
```

`page_hold.py:2582` 同款：`_facts.insert(0, ("現值（已失準，別照門檻讀）", _shown))`，且同處註解寫「**數字不藏起來**，只是不再當結論」。

⇒ **★-03 的結論（放 `facts`）是對的，但草稿只把它套用在 `—`／`N/A` 上，沒套用在 ⑦ 的真值上。**
R-A 原本就是同一條守衛，主筆組只抄了紅隊舉的那五行例子，沒有把規則推廣回自己的 2.5.1 表。

### 最小修法

2.5.1 表 ⑦ 列的「大字數值區」改成：**留空；現值改掛 `facts` 第一列，欄名「現值（已失準，別照門檻讀）」**（沿用 `page_hold.py:2582` 既有字樣，零新字串）。
3.1.4 與 3.3.4 的框線圖跟著改：`**+1.2**` 那一行移到 `facts`。

---

## 1.3 🟠 **兩條指路句目標在 `ia_nav` 沒有登錄 → `where_to_find()` `raise`，而草稿自己把它們標成 ✅**

### 是 raise。證據等級：**實跑證明**。

`shared/ia_nav.py:111-138` `where_to_find(dest)`：不在 `PAGE_LABELS`（5 個頁 id）也不在 `SECTION_LABELS`（**只有 4 個**：`today.conclusion` / `today.detail` / `why.data_health` / `hold.portfolio_setup`）→ **`raise ValueError`**。

**實跑**：

```
RAISE find.screen  -> 未知的分區 id 'find.screen'；合法值：['find','hold','inspect','today','why'] + ['hold.portfolio_setup','today.conclusion','today.detail','why.data_health']
RAISE why.edu      -> 未知的分區 id 'why.edu'；…
OK    why.data_health      -> 「📖 憑什麼 › 資料體檢」
OK    hold.portfolio_setup -> 「💼 我的持股 › 組合設定」
```

**草稿把這兩個標成「畫得出來 ✅」**：
- `3.1.8`（L1399）指路句表：「🔍 找標的 › 選股網」**✅**
- `3.4.8`（L2095）：「🔍 找標的 › 選股網」**✅**／「📖 憑什麼 › 教學」**✅**
- `3.4.7` 矩陣 ④ 欄（L2075）：「觀察清單到🔍找標的›選股網勾選加入」

⚠️ **注意這裡的細微之處**：這兩句**畫面上找得到那個東西**（選股網是頁2 葉1、教學是頁5 葉1），所以草稿的 ✅ 從「使用者找不找得到」的角度沒錯。**它錯在 `x.8` 那張表自稱是「指路句目標檢查」——而全站唯一產生指路句的函式對這兩個 id 會 raise。** 實作者若照 SSOT 寫（`ia_nav.where_to_find('find.screen')`）→ 當場炸；若手抄字串繞過 SSOT → 就是草稿自己在 C-02 罵的那件事（按鈕改名時指路句不會跟著改）。

### 草稿漏讀的那幾行（就在它引用的行號正上方）

草稿引用 `page_find.py:422-427` 取按鈕與葉名，但 **`:415-421` 是交接事項，它沒引**：

```
415: #: **沒有**第 2 頁的 `ACTION_*` 與 `LEAF_*` 登錄（實測：`ACTION_LABELS` 只有
416: #: `update_today`；`SECTION_LABELS` 只有 today 的兩葉 ＋ why / hold 各一）。
420: #: → 應在 `shared/ia_nav.py` 補 `ACTION_RUN_SCREEN` / `ACTION_LOAD_SECTOR_MAP`
421: #:   與 `LEAF_FIND_SCREEN` / `LEAF_FIND_SECTOR_MAP`，本檔改為讀它。
```

`page_inspect.py:341-347`、`page_hold.py:383-387` 各有一份同樣的交接註記。

### 最小修法

`x.8` 的指路句表加第三欄「**SSOT 登錄了嗎**」，並把這兩條標為 **❌ 未登錄 → 實作前置：先在 `shared/ia_nav.py` 補 `LEAF_FIND_SCREEN` / `LEAF_WHY_EDU`（＋對應 `SECTION_LABELS`）**，列進 Part 8.2。

---

## 1.4 🟠 **把 production 的 ⛔ 未接線卡改寫成「按按鈕就會有」→ 至少 5 支 CI 測試轉紅**

### 不是 raise，是 **CI 紅燈**。證據等級：**實測 grep ＋ 讀測試斷言**。

`tab_today.py:117-122` 逐字：

```
117: #: 「這一格現在沒有你可以執行的出口」的**唯一寫法**。
119: #: 為什麼要抽成常數而不是各處自己寫：守衛（`test_..._no_user_exit`）就是靠
120: #: 這個標記在**每一張卡**上確認「本批沒有給出一個按了也沒用的指路」。
122: NO_EXIT_MARKER: str = "沒有你可以執行的出口"
```

**實際守衛（雙向都守）**：

| 測試 | 斷言 |
|---|---|
| `tests/test_p01_today_skeleton.py:121` | 未接線卡 `assert T.NO_EXIT_MARKER in card.note.where` |
| `tests/test_p03_inspect_view.py:572` | 同上 |
| `tests/test_p03_inspect_view.py:722` | **已接線**卡 `assert NO_EXIT_MARKER not in ...`（反向） |
| `tests/test_p04_hold_view.py:950, 3292` | 同上 |
| `tests/test_p01_macro_refresh.py:528` | 「沒帶 `NO_EXIT_MARKER` —— 使用者不知道這一格按了沒用」 |

**草稿改寫了哪幾格**（這幾格在 production 是 `_staged_card(...)` / `UnwiredSpec`，`state=UI_UNWIRED`，`where=STAGED_ROLLOUT_WHERE`＝含 `NO_EXIT_MARKER`）：

| 草稿處 | 草稿寫的 `去哪補` | production 的 `where` |
|---|---|---|
| **3.1.1 ⑤⑥（L1185）** | 「**先完成上方更新。**」 | `tab_today.py:449-456` `_staged_card("warroom.todo"/"warroom.ai")` → `STAGED_ROLLOUT_WHERE`（含 NO_EXIT_MARKER） |
| **3.1.7 ⑤⑥ 列 ③ 欄（L1377）** | 「⬜ 尚無作戰項目＋「先完成上方更新」」 | 同上 |
| **3.1.1／3.1.7 ③ 三欄摘要的 動能／風險** | 「⬜ 尚未評估」＋按鈕 | `tab_today.py:435-439` 兩張 `_staged_card`（動能／風險）→ 未接線、無出口 |
| **3.3.1（L1664-1670）「按下去之後這裡會出現」8 段清單** | 承諾 7 段明細會出現 | `page_inspect.py:2190-2205` `build_detail_card(wired=False)` —— 「`wired=False` → 一律 `unwired`，**按幾次都一樣**」 |

⇒ **這同時違反草稿自己 2.5.2 的強制檢查第三條**（「`why` 說「永遠不會亮」 ⇒ `where` **必須**明說「沒有出口」」）。
草稿把那條規則寫在 Part 2，卻在 Part 3 的框線圖裡自己違反它 —— 而且 CI 會抓到。

⚠️ **射程說明**：這幾格「畫面上實際是什麼」屬 §2（發明稽核組）。本組只認列**會讓 CI 轉紅**的那一面，並提供上表給該組交叉使用。

---

## 1.5 🟡 **CSV 下載鈕的位置，草稿自己兩處互相矛盾；照 3.2.1 的圖畫會拋 `StreamlitAPIException`**

不是必 raise，是 **有一種讀法必 raise**。證據等級：**讀 code ＋ 讀草稿**。

- **草稿 6.3（L2881）**：「⛔ **不得放進 `st.form`** —— form 內只能有 submit…`download_button` 同理」✅ 對。
- **草稿 3.2.1 框線圖（L1452-1454）**：

```
║ │            ▌ 🎯 開始選股 ▐                                          │ ║
║ │  按下去才會開始取數。…                                              │ ║
║ │  〔 ⬇ 下載結果 CSV 〕 ← form 外；無資料時**停用但不隱藏**            │ ║
```

下載鈕**畫在 submit 正下方、同一個「條件表單」區塊框裡**，只用一行註記說「form 外」。
而 **6.3 說的位置是「該區塊**表格的正下方**」**——那是「選股結果」區塊，**不是條件表單**。兩處指的是兩個不同位置。
`_ui_kit.py:394-396` 明文：「form 內不得放 `st.button` / `st.download_button`（Streamlit 實跑即拋 `StreamlitAPIException`）。**本函式在結構上不給你放的縫**」。

⇒ 依 3.2.1 的圖實作 ＝ 放進 form ＝ 拋例外；依 6.3 ＝ 放在結果表下方。**兩份規格在同一份文件裡。**

### 最小修法
3.2.1 的框線圖把 `〔 ⬇ 下載結果 CSV 〕` 移到「選股結果」區塊那一格之下，與 6.3 對齊。

---

## 1.6 🟡 **⑥「六項並列」把「葡萄串領息」算進去 —— code 自述那會 `DuplicateWidgetID` 整頁拋例外**

證據等級：**code 第一手宣稱，本組未獨立重現**（⚠️ 見 §9）。

`src/ui/views/page_hold.py:2193-2206` `UnwiredSpec(key="hold.deep.grape", label="葡萄串領息")` 的 `why` 逐字：

> 「這一格的實作**住在 L5**（`tabs.grape_ladder`）而不是 L3，而且它自帶寫死的 widget key —— 在本頁再掛一次會撞 `DuplicateWidgetID`，那不是「畫得醜」，是**整頁當場拋例外**」

本組實測 `src/ui/tabs/grape_ladder.py` 確有 4 個寫死 key：`_grape_max_etfs`(426)／`_grape_min_stars`(432)／`_grape_excl_monthly`(437)／`_grape_btn_propose`(438)。

**草稿 3.4.3（L2024）**：「⑥ 組合深度分析（區塊並列，各自 gate）：再平衡 · 核心／衛星 · 壓力測試 · VaR · 配息現金流 · **葡萄串領息**」
**草稿 3.4.7 矩陣 ⑥ 列 ①正常**：「六項並列，各自 gate」

⇒ 實作者照這句把六項都掛上去，葡萄串那一項就是 code 預言的那顆地雷。
草稿 3.4.9「本頁已知做不到的事」**沒有這一條**（它列的 5 條是匯兌／target_pct／CTA 停用／涵蓋率分母／N 的數字）。

### 最小修法
3.4.3 與 3.4.7 的 ⑥ 明列：**再平衡、葡萄串領息兩項維持 ⛔ 未接線卡**（各自原因不同，沿用 `DEEP_SPECS` 既有文案），並在 3.4.9 加一列。

---

## 1.7 🟢 **R-B／R-D 採對了；R-C 採對但漏了實作前置**

逐條複驗主筆組宣稱的四條修正：

| 紅隊條目 | 草稿怎麼處置 | 本組複驗 |
|---|---|---|
| **R-A**（`—`/`N/A` 進大字區會 raise） | 2.5.3 ★-03 改放 `facts` | ✅ **方向對**，但**只修了一半** → 見 §1.2（⑦ 的真值仍在大字區） |
| **R-B**（CTA 九行六行會 raise；`single_submit_form` 無 `disabled`） | 2.14.1「按鈕文字一律不變，取自 SSOT」＋ 8.2 前置 1 | ✅ **採對了。** 實測 `_ui_kit.py:378-420` 簽名確無 `disabled`；`ia_nav.action_label()` 對未知 id 確 raise（`:105-108`）。<br>⚠️ **一個數字要更正**：草稿說「頁2/頁3 是就地實作的 submit，**三處**都要改」。實測 `form_submit_button` 呼叫點共 **4 處**：`_ui_kit.py:414`（kit，服務頁1＋頁4）、`page_find.py:1555`、`page_inspect.py:2342`、`page_inspect.py:2373`。頁1 **確實走 kit**（`page_today.py:1812` `single_submit_form(...)`），所以是 **1 kit ＋ 3 就地 ＝ 4 個呼叫點**。 |
| **R-C**（`miss_fn` 回 `contract_drift` 掉進灰色 📭 縫） | 2.14.2 ⑩·契約漂移 ⚑ 升紅、不得走 📭 | ✅ **採對了**。實測 `station_charts.py:337-339` 確為 `_r = miss_fn(series)` → `st.info(miss_text_for(_r), icon="📭")` → `return`，契約漂移確實走灰路。<br>⚠️ **但 Part 8.2 實作前置表沒有這一條** —— 要升紅得改 `station_charts._chart_block` 的分流。草稿對 `LEVEL_STYLES`（前置 3）與 `disabled`（前置 1）都誠實列了前置，唯獨漏這一個。 |
| **R-D**（燈號徽章四種畫成同一個虛線框） | 2.14.3 八種畫法 ＋ 8.2 前置 3 | ✅ **採對了**。實測 `station_cards.py:122-131` `LEVEL_STYLES` 中 `dashed=True` 確實只有 `LEVEL_UNJUDGED` 一個鍵；`:552-557` 對未登錄 level 確實 `raise KeyError`（「新增判定符號時必須同時決定它怎麼畫」）；`:215-218` 確有「無資料 ≠ 未判定」的獨立說明。 |

---

## §1 小結（按實作順序）

| # | 會怎樣 | 修在哪 | 要不要動 code |
|---|---|---|---|
| 1.1 | 建構 `Note` 時 raise，約 20 處 | 草稿 2.5.3 ＋ 五張 `x.7` 表頭加一句 | ❌ 純文件 |
| 1.2 | 建構 `Card` 時 raise（⑦ 那一態） | 草稿 2.5.1 ⑦ 列 ＋ 3.1.4／3.3.4 | ❌ 純文件（production 已有正解） |
| 1.3 | `where_to_find()` raise（2 個 id） | 草稿 `x.8` 加一欄 ＋ Part 8.2 加前置 | ✅ 要補 `ia_nav` 登錄 |
| 1.4 | 5 支 CI 測試轉紅 | 草稿 3.1.1／3.1.7／3.3.1 的 `去哪補` | ❌ 純文件 |
| 1.5 | `StreamlitAPIException`（一種讀法） | 草稿 3.2.1 框線圖 | ❌ 純文件 |
| 1.6 | `DuplicateWidgetID` 整頁例外 | 草稿 3.4.3／3.4.7／3.4.9 | ❌ 純文件 |
| 1.7 | — | 草稿 8.2 補 `station_charts` 前置、更正「三處」→ 4 個呼叫點 | ❌ 純文件 |

⚠️ **1.1~1.6 有五條是純文件修正** —— 這不是「code 要大改」，是**草稿把規格寫成了 code 會拒絕的形狀**。

---

# §3 C-05（頁1 主數值）兩條承重宣稱的重驗

## 3.A 宣稱 (a)：「這句**已逐字存在**於 `shared/allocation_decision.py:316-317` 的 `drivers[0]`」

### 判定：**行號對、語句半對、推論被誤用。**

**實測原文**（`shared/allocation_decision.py:315-318`）：

```python
315:    _drivers: list[str] = [
316:        f'總經健康分 {float(_health):.0f} → 姿態「{_posture}」，'
317:        f'油門帶 {_fmt_range(_p_lo, _p_hi)}',
318:    ]
```

⇒ `drivers[0]` 實際輸出：**`總經健康分 68 → 姿態「偏多」，油門帶 60–80%`**

| 草稿哪一句 | 判定 |
|---|---|
| L1093「**「健康分 X → 姿態『Y』，油門帶 Z」這句話已經逐字存在於 `drivers[0]`（`:316-317`）**」 | ✅ **完全正確。** 行號、語句、格式（走唯一 formatter `_fmt_range`，`:193-200`）全部對得上。 |
| L1079 展開佐證裡寫 `· 總經健康分 68 → 姿態「偏多」，油門帶 60–80%　← drivers[0] 原文` | ✅ **逐字正確。** |
| L1099「**這句話已經逐字存在於 L0，不是新發明。**…本方案只是把它從第四層搬到第一層」 | 🔴 **這一句把兩件事混起來了。** 推薦的**第一層**字串是 `🟢 總經健康分 68 —— 落在「偏多」區間`（L1070）。`drivers[0]` 裡**沒有**「落在…區間」這五個字，它寫的是「→ 姿態「偏多」，**油門帶 60–80%**」。<br>**真正發生的事是：把 `drivers[0]` 改寫，砍掉「油門帶 60–80%」那半**（那半正是合規上必須砍的），**再換一個新的連接語**。那是**新字串**，不是搬運。 |

⚠️ **為什麼這個差別要緊**：草稿用「已逐字存在 ⇒ 零新增」來支撐「改動面是單點、純文字」。**砍掉油門帶那半是語意變更，不是搬運** —— 它把一個「這個健康分對應多少部位」的句子，改成「這個健康分落在哪個名字裡」。這是 C-05 真正要客戶拍板的東西，不該被「已逐字存在」蓋過去。

**建議**：L1099 改寫為「**`drivers[0]` 的前半（總經健康分 N → 姿態「X」）已逐字存在；後半「油門帶 A–B%」本方案刻意移除並改寫為第四層的客觀對照。新增的只有「落在『X』區間」這個連接語。**」

---

## 3.B 宣稱 (b)：「改 `:222-226` 的 `headline()` 一次，**6 個畫面同步修好**」

### 判定：🔴 **不成立。實測：`headline()` 在 production 只有 1 個真正的畫面呼叫點，而且它在舊七頁籤那一邊 —— 新五頁一次都不叫它。**

### (b-1) 全 repo `headline()` 呼叫點（非測試）

```
src/ui/tabs/macro/section_cross_ai.py:170    _ai2_lbl = _ai2_alloc.headline()
src/ui/render/etf_render.py:96               return alloc.headline(), '#1a1f2e', '#1f6feb'
src/services/allocation_service.py:8         st.markdown(_alloc.headline())
```

逐一判讀：

| # | 位置 | 是不是真的呼叫 | 說明 |
|---|---|---|---|
| 1 | `section_cross_ai.py:170` | ✅ **是** | 唯一真正會畫到螢幕上的一處 |
| 2 | `etf_render.py:96` | ⚠️ **防禦分支** | `if _mid is None:` 才走；而 `build_allocation_decision()` 在 `is_loaded=True` 時**必定**設 `final_mid=round((_f_lo+_f_hi)/2)`（`:333`）。同行註解自陳「is_loaded=True 理論上必有值；防禦性降級」 |
| 3 | `allocation_service.py:8` | ❌ **不是呼叫** | 它在**模組 docstring 裡**（`:1-20` 三引號區塊內的用法範例） |

**實測 `grep -rn "headline(" src/ui/views/` → 0 命中。新五頁沒有任何一處呼叫它。**

### (b-2) 草稿列的 6 個落點，逐一對「是不是吃 `headline()`」

| # | 草稿寫的落點 | 吃 `headline()`？ | 實測它到底是什麼 |
|---|---|---|---|
| **a** | `app.py:1174-1178` 全域置底常駐條 | ❌ **不吃** | 真正印「建議持股」的是 **`app.py:1188`**：`f'<span …>建議持股 <b>{_gl_pos}</b></span>'`，而 `_gl_pos = _alloc.range_text`（`:1162`）。燈的 label 走 `traffic_light(...)`（`:1169-1175`）。**草稿給的行號 1174-1178 那五行是 `traffic_light` 的參數與 `cap_text` 串接，裡面一個「建議持股」都沒有。** |
| **b** | `section_cross_ai.py:161-183` | ✅ **吃** | `:170`。⚠️ 但**草稿自己說這一格的違規是「卡片標題就叫『② 建議配置』」**（L812）——那個標題在 `:159` 的區塊註解／`st.markdown` 那一層，**改 `headline()` 不會改到它**。 |
| **c** | `etf_tab_dividend_station.py:590-591` | ❌ **不吃** | 實測 `:591`：`_rng = f"（建議持股 {switch['posture_range']}）"` —— 自己組的 f-string，讀 `switch` dict |
| **d** | `page_today.py:1356-1359` | ❌ **不吃** | 實測 `:1357-1359`：`Card(key="verdict.exposure", label="能不能出手 · 出手到幾成", state=_alloc_state, value=alloc.range_text)`。**這張卡上根本沒有「建議持股」四個字**；有的是一個裸百分比當大字值 |
| **e** | `page_hold.py:2101+` | ❌ **不吃** | 實測 `:2140`／`:2157`：`Card(key="hold.position_cap", label="建議持股水位（市場端）", value=alloc.range_text)` —— **label 是本檔寫死的字串常量** |
| **f** | `macro_stock_link.py:37` | ❌ **不吃** | 實測 `:37`：`st.caption("🧭 載入「總經」Tab 後，這裡會顯示大盤 regime 背景（多空 / 建議持股）")` —— 靜態字串 |

⇒ **6 個落點裡，改 `headline()` 會動到的是 1 個（b），而那 1 個的違規點（卡片標題）還不在 `headline()` 裡。**
**⇒ 嚴格說：改 `headline()` 修好的畫面數 ＝ 0～1，不是 6。**

### (b-3) ⭐ 更關鍵：新五頁與舊七頁**互斥**，所以對客戶要驗收的那五頁，這個改動的效果是 **0**

`app.py:603-605` 實測：

```
603:    _render_tab_isolated(_IA_VIEWS[_IA_PAGE], _IA_PAGE_LABELS[_IA_PAGE])
604:    _render_footer()
605:    st.stop()
```

側欄選到新五頁任一頁 → `st.stop()` → **舊七個頁籤一行都不跑**。而 `headline()` 的唯一真呼叫點 `section_cross_ai` 只被 `tab_macro.py:627` 掛載（舊 🌍 總經 頁籤）。
⇒ **使用者站在新五頁時，`headline()` 這支函式根本不會被執行。**

### (b-4) 🔴 **反向風險：照 ★-10／B-04 改 `:224` 的 fallback，會把舊頁籤指去一顆舊頁籤上沒有的按鈕**

`shared/allocation_decision.py:222-226` 實測：

```python
222:    def headline(self) -> str:
223:        """一句話結論：'🟠 轉守 → 建議持股 30–50%'。"""
224:        if not self.is_loaded:
225:            return '⬜ 總經未評估 — 請先按「🚀 一鍵更新全部數據」'
226:        return f'{self.icon} {self.posture} → 建議持股 {self.range_text}'
```

（⚠️ 順帶更正一個行號：fallback 字串在 **`:225`**，草稿 B-04／8.2-6 寫 `:224`。`:224` 是 `if not self.is_loaded:`。）

草稿 B-04 說「舊按鈕名是**舊分頁**的；本 IA 的 SSOT 是 `ia_nav.py:83`『🚀 更新今日戰情』…**兩處一起改**」。
**但 `headline()` 唯一會被畫出來的地方就是舊分頁。** 在舊 🌍 總經 頁籤上，「🚀 一鍵更新全部數據」**是真的存在的那顆按鈕**；改成「🚀 更新今日戰情」之後，舊頁籤的使用者會被指去一顆**舊頁籤上沒有**的按鈕 —— **正是 B-04 說它在防的那個失效模式，只是方向相反。**

### (b-5) 同一句 fallback 在 repo 裡不只兩份 —— 實測 4 份

```
shared/allocation_decision.py:225              '⬜ 總經未評估 — 請先按「🚀 一鍵更新全部數據」'
src/services/allocation_service.py:307         '⬜ 總經未評估 — 請先按「🚀 一鍵更新全部數據」'
src/ui/tabs/macro/section_chips.py:607         '⬜ 總經未評估 — 請先按「🚀 一鍵更新全部數據」'
src/ui/tabs/macro/section_traffic_light.py:57  '⬜ 建議持股油門：總經未評估 — 請先按「🚀 一鍵更新全部數據」'
```

草稿說「**還有一份**」；實測**至少四份**（含一個變體）。
（全 repo 該按鈕名共 **71 次命中、28 個 production 檔**——但那絕大多數是舊七頁籤**正確**的用法，⛔ 不該全改。真正的問題是草稿把「要改幾處」講成兩處，而正確答案是「**只該改新五頁到得了的那些，而 `headline()` 不是其中之一**」。）

### (b-6) ⭐ 控制流複驗：**換掉之後會不會靜默壞掉？——會，而且是 CI 那一端**

本組把 `tests/test_no_hardcoded_position_pct.py:117-126` 的 `_PATTERN` **逐字抽出來**，對草稿建議的新文案跑了一次：

```
pass  C-05 第一層（候選甲）  「🟢 總經健康分 68 —— 落在「偏多」區間…」
HIT   C-05 第四層 客觀對照   「…歷史上「偏多」這個位階，對應的持股區間為 60–80%。」 -> 命中片段：持股區間為 60–80%
HIT   2.12.2 替代表述 F      「歷史上此位階對應的持股區間為 30–50%」            -> 命中片段：持股區間為 30–50%
pass  2.12.2 替代表述 B（偏離提示）／D（情境試算）
pass  3.4.3 ⑤-a（核心 76%/衛星 24%）
HIT   舊 headline「🟠 轉守 → 建議持股 30–50%」                                  -> 持股 30–50%
```

該測試的掃描範圍是 `_SCAN_DIRS = ('src/ui', 'src/compute')` ＋ `_SCAN_FILES = ('app.py',)`，白名單 `_ALLOWED_FILES` 只有 5 個檔（`allocation_decision.py` / `position_throttle.py` / `risk_control.py` / `tab_edu.py` / `etf_render.py`），**`src/ui/views/**` 一個都不在**。

⇒ **草稿推薦的「合規替代寫法」本身，會讓 repo 現有的「禁止硬編碼持股百分比」CI 守衛轉紅。**
⇒ 而草稿 ★-10 寫的是「**屬純文字、零計算影響**」「**改動面是單點**」，Part 8.2 的 6 條實作前置裡沒有這一條。

⚠️ 這是本組認為 §3 最重要的一條：**它不是「會不會炸」，是「這條建議會把百分比從一個受 SSOT 管的地方（`allocation_decision.py`，在白名單內），搬進一個不受管的地方（`src/ui/views/page_today.py`）」**——方向與該守衛存在的理由相反。

**修法（兩條，擇一）**：
1. 第四層的客觀對照句**不寫死區間**，改為讀 `alloc.range_text`（與大字值同源）；或
2. 把 `src/ui/views/page_today.py` 寫進 `_ALLOWED_FILES` **並附理由**（該表明文要求「每一筆都必須有『為什麼這裡是合法的』的理由」）。
⭐ 本組傾向 **1**：它同時解掉「第二個真相源」問題。

### (b-7) 還有一個「改字串會讓東西消失」的同族風險（🧠 判斷，未實測到實例）

`section_chips.py:410` 註解記載：UI 邊界用 regex 把 v4 引擎文案裡的「建議持股 ≤20%」**剝掉**，以免與 SSOT 打架（`test_no_hardcoded_position_pct.py:140-142` 也記著這件事）。
⇒ repo 裡**存在「靠中文字串比對來剝字/判斷」的既有做法**。本組**沒有**找到任何一處對 `headline()` 輸出做字串比對，但這個模式存在本身，就是「改中文字串會讓東西靜默消失」的溫床。
⚠️ 🧠 **本組判斷，未窮舉**：本組掃的是 `headline` / `posture` / `range_text` 的字串比對用法，命中 1 處（`portfolio_coherence.py:117` 用 `'防禦' in macro_posture` / `'轉守' in macro_posture` 判防禦狀態）。**那一處吃的是 `posture`，不是 `headline()` 的輸出，候選甲不動 `posture` ⇒ 不受影響。**

---

## §3 小結

| 宣稱 | 判定 |
|---|---|
| (a)「已逐字存在於 `drivers[0]:316-317`」 | ✅ 行號與語句正確；但 **L1099「不是新發明、只是搬到第一層」不成立** —— 推薦的第一層是改寫後的新字串，而被砍掉的正是合規爭點本身 |
| (b)「改 `headline()` 一次，6 個畫面同步修好」 | 🔴 **不成立。** 6 個落點裡 5 個不吃 `headline()`；唯一吃的那個，違規點還不在 `headline()` 裡；而且它在舊七頁籤那邊，**新五頁執行不到它** |
| (b) 附帶 | 🔴 改 fallback 會把**舊頁籤**指去一顆**舊頁籤沒有**的按鈕；同句 fallback 實測 4 份不是 2 份；`:224` 應為 `:225` |
| (b) 控制流 | 🔴 **推薦的替代文案本身會讓 `test_no_hardcoded_position_pct` 轉紅**（實測 regex 命中 `持股區間為 60–80%`），而 ★-10 宣稱「零計算影響、單點」 |

⚠️ **★-10 的推薦方向（把謂語從「你該做什麼」換成「現在是什麼狀態」）本組認為仍然對。** 被否證的是它的**成本論證**，不是它的**設計論證**。送客戶時建議把「一次修好 6 個畫面」整句拿掉，改成誠實版：**「這一改主要影響新五頁的頁1 卡①（要改 `page_today.py` 的 `label`＋`value`），舊七頁籤那邊另有 5 處各自獨立的字串，本批不動。」**

---

# §6 三情境壓測

> 方法：走 production 的實際 render 路徑（`app.py` → `_IA_VIEWS[page]` → `render_page_*`），逐塊寫出「**狀態／顯示什麼字／主 CTA 在哪**」，再問**草稿答不答得出來**。

---

## 6.a 情境 A｜第一次打開、什麼都沒點、網路正常

### 先講一個草稿沒寫進 `x.1` 的前置

`app.py:82` `initial_sidebar_state='collapsed'`、`:315` `st.radio(…, index=0)`、`:290` 第一個選項＝`不使用（用下方原功能）`、`:288-291` 沒選就 late import 都不跑。
⇒ **「第一次打開」看到的是舊七頁籤，新五頁一行都不跑。** 要進頁1 得先展開側欄、選一頁。
草稿 **2.10.2 有講到這件事**（L608「一個全新使用者打開 app，看到的是舊七頁籤…『新手的第一次』目前等於 0 次」）✅，但**五頁的 `x.1` 冷啟動沒有任何一段承接它**——它們都從「已經站在頁1」開始畫。

### 進到頁1 之後，逐塊實況（`page_today.render_page_today()`，L1863-1954）

| # | 區塊 | 實際狀態 | 畫面上的字（出處） | 草稿答得出來嗎 |
|---|---|---|---|---|
| 0 | `## 🚦 今天` ＋ caption | — | 「回答「今天能不能出手、出手到幾成」。」(`:1864`) | ✅ |
| 1 | **頁首「上一次更新的結果」** | **不出現**（`SS_REFRESH_REPORT` 為 None，`:1868`） | — | ⚠️ 草稿全篇 **0 次**提到這個區塊（見 6.b） |
| 2 | 頂部狀態列 · 交易日 | **⛔ 未接線** | 「今天是不是交易日，尚未接線」(`tab_today.py:382-383`) | 🔴 草稿畫 `✅ 交易日` |
| 3 | 頂部狀態列 · 總經 | **⬜ 尚未載入**（`classify_macro_contract` → `source=unloaded` → `requested=False` → `UI_IDLE`） | 「總經未評估」＋「L3 canonical 契約四條來源本輪皆無值 → 回 `unknown`，**不是** `neutral`」(`tab_today.py:359-366`) | ✅ 草稿 2.10.1 ③ 這一格**寫對了** |
| 4 | 頂部狀態列 · Sheet 綁定 | **⛔ 未接線** | 「Sheet 綁定狀態尚未接線」(`tab_today.py:386-393`) | 🔴 草稿畫 `🔗 尚未綁定 Sheet`（灰態，不是未接線） |
| 5 | （M-1 有的「更新於 14:32」） | **不存在** | `build_status_bar_cards` docstring 自陳只有**三個元素** | 🔴 草稿 2.1／2.10.1 都列了四項 |
| 6 | 葉列 | `st.tabs(['今日結論','指標明細（五桶逐段）'])`(`:1894-1897`) | 取自 `ia_nav.SECTION_LABELS` | ✅ |
| 7 | 葉1 ① 今日結論 | **三張並排卡**（`verdict.exposure` / `verdict.danger` / `verdict.regime`），皆 ⬜ | `exposure` 的 why：「L3 總經契約這一輪**還沒有人叫過**（`source` 仍是 `unloaded`…）—— 這是「還沒叫」，不是「叫了沒回」，更不是「今天不能出手」」(`page_today.py:1364-1367`) | ⚠️ 草稿把 ① 畫成**一行結論句**，沒寫它是三張卡；文案本身（0／16 ＋ 未接線拆解）✅ 對 |
| 8 | 葉1 ② 操作列 | 可按、`type="primary"` | radio「更新模式」＋「🚀 更新今日戰情」；下方 caption 印「已套用的更新模式：（尚未送出過）」＋ `PAGE_REFRESH_SCOPE_WHY`(`:1824-1827`) | ⚠️ 草稿沒畫那一行 caption |
| 9 | 葉1 ③ 三欄摘要 | 位階 ⬜／**動能 ⛔ 未接線**／**風險 ⛔ 未接線** | `tab_today.py:435-439` 兩張 `_staged_card` | 🔴 草稿畫三格皆「⬜ 尚未評估」 |
| 10 | 葉1 ④ 今日關鍵橫幅 | ⬜（`requested=False`） | ✅ 與草稿一致 | ✅ |
| 11 | 葉1 ⑤⑥ | **兩張 ⛔ 未接線卡**（`warroom.todo` / `warroom.ai`） | `where` ＝ `STAGED_ROLLOUT_WHERE`（含「沒有你可以執行的出口」） | 🔴 草稿寫「⬜ 尚無作戰項目／去哪補：先完成上方更新」→ §1.4 CI 紅 |
| 12 | 葉2 五桶摘要 ＋ 16 盞燈 | 全 ⬜（其中 `wired=False` 的燈是 ⛔，優先序見下） | `page_today.py:1134-1139` | ⚠️ 見下方「⑧ vs ③ 優先序」 |

**主 CTA**：葉1 ② 操作列，**在預設葉，看得到** ✅（符合草稿 L0-3）。

### 🔴 草稿沒寫、而冷啟動**立刻**會遇到的一條：**⑧ 未接線先於 ③ 未載入**

`shared/ui_state.py:199-202` 實測：

```
199:    if not wired:
200:        return UI_UNWIRED
201:    if not requested:
202:        return UI_IDLE
```

⇒ **一盞 `wired=False` 的燈，在冷啟動（`requested=False`）時顯示的是 ⛔ 未接線，不是 ⬜ 尚未載入。**

前一輪紅隊 `S1-4_REDTEAM §2.10` 就點名過這個缺口。本組實測草稿：`grep "優先序|誰贏|同時成立"` → **3 次命中全部與此無關**（都在講「來源優先序第 3 條」與「混合時點」）。
⇒ **這條缺口在草稿裡沒有被補上。** 而草稿 2.5.2 花一整節講三個灰的說法必須不同，正好沒說「同時成立時誰贏」。
⇒ 實作者最直覺的寫法是「冷啟動 → 全部 ⬜」，那會**蓋掉所有 unwired 燈的真相**——也就是把上面第 9、11 格從 ⛔ 洗成 ⬜。

**情境 A 判定**：**草稿答得出大半，但有 5 格答錯（2、4、5、9、11）、1 條規則缺（⑧/③ 優先序）、1 個前置沒接（要先選側欄）。**

---

## 6.b 情境 B｜點了載入，總經到了、個股 API 逾時

### 先講一件會讓這個情境無法照草稿敘述的事

**頁1 的按鈕碰不到個股，也碰不到卡①。**
`src/services/macro_refresh_service.py:184-206` `UNTOUCHED_BLOCKS` 逐字：

```
193:    UntouchedBlock(
194:        label="建議持股（卡①）與市場位階（卡③）",
195:        session_key="warroom_summary / macro_state.json",
196:        why=("兩張卡都讀 L3 `macro_state_locker.get_macro_state()`，"
197:             "而它的四條來源是 `warroom_summary` 與 `macro_state.json` —— "
198:             "本路徑一個都沒寫。**它們顯示的是上一次跑舊分頁的結果**"),
```

另兩項：**「今日關鍵橫幅的門檻層（④）」**、**「新聞桶」**。

⇒ **在一個乾淨的 session 按下「🚀 更新今日戰情」之後，卡①／卡③／④ 的門檻層／新聞桶 全部維持原狀**（⬜ 或 ▨），**不會變成 🟢**。
⇒ 而 `page_today.py:1865-1886` 在頁首會**固定畫出一個「上一次更新的結果」區塊**，`_render_refresh_report()` docstring（`:1705-1711`）逐字：

> 「§1：這一段的存在理由是「沒更新到的東西如果留白，看起來跟更新過一模一樣」。所以它**一定**會列出 `untouched`（本路徑摸不到的區塊），即使那一輪一切順利。」

**草稿實測：`UNTOUCHED` / `摸不到` / `更新報告` / `refresh_report` → 全篇 0 命中。**

⇒ 兩個後果：
1. **草稿漏畫了一個 production 已存在、且是 §1 強制的頁首區塊。**
2. **草稿 3.1.3「有資料」的 ① 畫成 `🟢 總經健康分 68`，而那正是這顆按鈕更新不到的那張卡。** ★-10（C-05）整條建議，改的就是這張按鈕碰不到的卡。

### 逐塊實況（本情境要分頁答，因為頁1 沒有「個股 API」）

**頁1（按了更新、總經來源成功）**

| 區塊 | 狀態 | 字 |
|---|---|---|
| 頁首更新報告 | ✅/⚠️「上一次更新的結果」 | 成功項＋失敗項＋**固定列出 4 項 untouched** |
| 狀態列 · 總經 | 仍 ⬜（除非同 session 跑過舊分頁） | 「總經未評估」 |
| ① 三卡 | 仍 ⬜ | 同冷啟動 |
| 葉2 16 盞燈 | **這裡才會變**（`_readout` 走 `section_inputs`） | 逐盞 🟢/▨/⛔ |

⇒ **使用者按了唯一那顆鈕，最醒目的那三張卡不動。** 這是本情境最重要的一句話，**草稿給不出來**。

**頁3（個股 API 逾時）** — `page_inspect`：判型/健康度/估值/籌碼/獲利能力走 L3、各自 `try/except`（`:240-245` 誠實邊界段）；逾時 → 該格 🔴、**其餘格照常**。草稿 3.3.6（L1786-1793）**寫對了**這一段（逐格獨立、失敗列保留）✅。

**頁4（總經到了、報價逾時）** — 草稿 3.4.4 ⑥涵蓋不全「12 檔中 2 檔缺報價…⛔ 不靜默剔除」✅ 方向對。
⚠️ 但草稿 3.4.x 的頂部狀態列畫 `🟢 總經 偏多` —— 實測 **`render_page_hold()` 根本不畫狀態列**（`grep build_status_bar_cards src/ui/views/page_{find,inspect,hold,why}.py` → **0 命中**；四頁都只有 `st.markdown(page_label)` ＋ `st.caption(...)`）。

### 時點列在本情境的答案

草稿 **2.10.1 第2條 ＋ ★-07a 對這一格答得很好**：「混合」與「部分源時點不明」同時成立時一行寫完（「最舊 `09-11` ～ 最新 `09-13`；其中 2 項來源未提供資料日期」＋⚠︎）。✅ 這是草稿在本情境唯一完整答出的一格。

**情境 B 判定**：**草稿在「單一區塊怎麼畫」這一層答得出來；在「按了按鈕之後這一頁整體會變成什麼樣」這一層答不出來**，因為它漏了 `UNTOUCHED_BLOCKS` 與頁首更新報告這兩個 production 事實。

---

## 6.c 情境 C｜手機 375px、開在「💼 我的持股」的第二葉

### 實況（`render_page_hold()`，`page_hold.py:3725-3751`）

葉2 的 body **被分成兩段、在同一輪裡進入兩次**：

```
3741:    with _leaf2:
3742:        _render_setup_form_block()      # 標題＋表單＋兩段常駐揭露
3746:    _req = applied_hold_request(_session)
3747:    _holdings = load_holdings(_req)
3748:    with _leaf1:
3749:        _render_warroom_leaf(_req, _holdings)
3750:    with _leaf2:
3751:        _render_setup_result_block(_req, _holdings)
```

葉2 由上而下實際有：

| # | 元素 | 狀態 | 字（出處） |
|---|---|---|---|
| 1 | 區塊標題 ＋ caption | — | 「線框：**就地完成，不必去別頁** —— 但本頁對你的持股帳本**一律唯讀**，需要寫入才做得到的部分會標「未接線」並說明原因」(`:3662-3665`) |
| 2 | **表單**（radio ＋「🚀 跑存股戰情室」） | 可按 | `single_submit_form`(`:3485-3493`)；radio label「這一輪要跑到哪裡（**兩個選項都不會寫入你的 Sheet**）」 |
| 3 | `WIRING_DISCLOSURE` ＋ `READONLY_DISCLOSURE` | 常駐 | `:3667-3668` |
| 4 | Google 登入與 Sheet 綁定（唯讀）— 2 張卡 | 冷啟動 ⬜ | `:3677-3681` |
| 5 | 持股列預覽卡 | 已接線、唯讀 | `:3686` |
| 6 | **Sheet 選擇** | **⛔ 未接線** | 「**本頁不提供選 Sheet 的控制項**」＋`READONLY_WHY`／`READONLY_WHERE`(`:3354-3359`) |
| 7 | **觀察清單管理** | **⛔ 未接線** | 「**本頁不提供觀察清單的新增／刪除**」(`:3360-3366`) |
| 8 | 逐列預覽表 | 有持股才畫；缺張數/均價顯示「—」不顯示 0(`:3691-3702`) | |

**主 CTA**：**在葉2 內**（現況）。若 ★-09 通過則搬到分頁列之上 → 在葉2 一樣看得到。兩種都看得到 ✅。

### 草稿答得出來嗎

🔴 **答不出來。實測：草稿全篇沒有任何一張頁4 葉2 的框線圖。**

- `grep "組合設定"` → 13 次命中，**全部**是 IA 全表、矩陣一列、指路句、B-05 的論證；**沒有一張圖**。
- 草稿對頁4 的手機規格只有兩句：L1982「radio 兩項直向；`🚀 跑存股戰情室` 滿版寬；目錄清單單欄；分頁列可橫向捲動」、L2036「戰情表轉卡片流；結論卡直向堆疊」——**兩句講的都是葉1**。
- 草稿唯一的葉2 規格是 3.4.7 矩陣那一列（L2080），而**那一列有兩格與 code 相反**：
  - ①正常欄寫「登入狀態·**Sheet 選擇**·持股預覽·**觀察清單**」→ 實測後兩者是 **⛔ 未接線**；
  - ③未載入欄寫「⬜ 尚未綁定持股 Sheet＋『**在本頁用 Google 登入後選一本 Sheet —— 就地完成，不必去別頁**』」→ **本頁沒有選 Sheet 的控制項**，code 的 `where` 指向「既有的 📁 組合管理分頁」。
  - ⇒ 這條指路句**指向一個本頁不存在的控制項**，是草稿自己在 C-02／N2 罵的那件事。

### 手機特有、草稿沒處理的兩點（🧠 本組判斷）

1. **分頁標籤太長。** 兩個葉名是 `戰情室（1️⃣~6️⃣ ＋ 80/20 配置偏離）` 與 `組合設定（＝組合管理＋Sheet 綁定）`（`page_hold.py:391-392`，取自 M-1 逐字）。375px 下兩個標籤合計遠超螢幕寬；草稿寫「分頁列可橫向捲動」，但**沒有規定第二葉在初始視窗外時怎麼讓人知道它存在**——而本情境的前提就是使用者要到第二葉。
2. **葉2 沒有 D-3 的手機短版落點。** 草稿 5.6 的八個落點只有 #7 提到葉2（且條件是「若該表顯示 `target_pct` 欄」），而 #4 的手機短版只掛在葉1 ⑤-b。

**情境 C 判定**：**規格缺口。** 客戶若在驗收時打開手機的第二葉，草稿給不出「每一塊是什麼狀態、顯示什麼字」。

---

## §6 小結

| 情境 | 草稿答得出來嗎 | 最要緊的缺口 |
|---|---|---|
| **(a)** 冷啟動 | ⚠️ **大半可以，5 格答錯** | 頂部狀態列 2/3 格是 ⛔ 未接線、③ 動能風險是 ⛔、⑤⑥ 是 ⛔；**⑧ 先於 ③ 的優先序規則缺席** |
| **(b)** 總經到、個股逾時 | ⚠️ **區塊層可以，頁層不行** | 漏了頁首「上一次更新的結果」＋ `UNTOUCHED_BLOCKS`；**按了鈕卡①不會變**這件事完全沒寫 |
| **(c)** 375px × 頁4 葉2 | 🔴 **答不出來** | **全篇沒有頁4 葉2 的框線圖**；唯一那列矩陣有兩格與 code 相反 |

---

# §8 逐條比對後**找不到錯**的區塊（可直接引用）

> 下列每一項本組都回原檔讀過。**列出來的價值等同抓到錯** —— 後續組別可以不必重查。
> ⚠️ 標「行號 ✅」＝ 草稿給的 `file:line` 本組逐一比對正確（±1 行內）。

## 8.1 code 引用的正確性（抽驗 30 處，29 處正確）

| 草稿引用 | 複驗 |
|---|---|
| `tab_today.py:229-240`（`Card.value` 守衛，逐字引用） | ✅ **一字不差**，行號精準 |
| `shared/ia_nav.py:82-84` `ACTION_LABELS` 只有一個 label | ✅ 行號 ✅，`:105-108` 未知 id raise ✅ |
| `page_find.py:422 / :423 / :426 / :427 / :1555 / :1705` | ✅ 六個行號全部精準 |
| `page_inspect.py:348 / :349 / :352 / :353 / :51 / :1808 / :1886 / :1985 / :2342 / :2373 / :2542` | ✅ 十一個行號全部精準；三張判決卡 label 實測就是 `健康度` / `估值（357 評價）` / `籌碼` |
| `page_hold.py:388 / :391 / :392 / :3485 / :3729 / :3731-3740` | ✅ 全部精準；「執行順序 ≠ 顯示順序」在 `:3731`、「`st.tabs` 這一輪會把兩葉的 body 都跑完」在 **`:3733`** ✅ |
| `page_why.py:330 / :331 / :332 / :2119 / :2121 / :2169 / :2203`；`grep -c form_submit_button page_why.py` ＝ **0** | ✅ 全部精準，**0 實測複驗成立** |
| `page_today.py:71-75`（契約 9 個 key）／`:494`／`:499`／`:506-511`／`:610`／`:618-622`／`:1008-1013` | ✅ 全部精準 |
| `page_inspect.py:1058-1060` `_MISSING_STATUSES` | ✅ **逐字一致** |
| `_ui_kit.py:112`／`:169-173`／`:240-256`／`:378-420` | ✅ 全部精準；`single_submit_form` 簽名**確無** `disabled` ✅ |
| `station_charts.py:337-339`（`miss_fn` → `st.info(…📭)` → return） | ✅ 精準 |
| `station_cards.py:122-131`（`dashed=True` 僅 `LEVEL_UNJUDGED`）／`:215-218`（無資料≠未判定）／`:552-557`（未登錄 level raise） | ✅ 全部成立 |
| `shared/ui_state.py:113-121` `UI_STATE_META`／`:112` 註解「glyph 刻意各不相同」 | ✅ |
| `shared/colors.py:1-2`（AUTO-SYNCED, DO NOT EDIT）／`:25` `TRAFFIC_NEUTRAL="#888888"` | ✅ 兩處都精準 |
| `shared/staleness.py:26` `expected_latest_trading_day(today, holidays)`／`:85` `latest_date` | ✅；且 `holidays=` 確實只在該檔內部出現 |
| `macro_helpers.py:606` `rp_scalar`（`last_updated=proxy_date`）／`fetch_monitor.py:56`（`datetime.now(_TW_TZ)`） | ✅ 兩處都精準，**「⛔ 絕對不要顯示這個欄位當時點」的判斷成立** |
| `station_specs.py:471` `MISS_NOT_APPLICABLE`／`:498` `MISS_TEXT` | ✅ |
| `gsheet_portfolio.py:379`（`[name, tk, lots, avg, ts]` 寫死 5 寬）／`:384-388`（clear → append_row → append_rows）／`:86-88`（watchlist 物理隔離） | ✅ **三處全部精準，M2 根因描述完全成立** |
| `app.py:82` `initial_sidebar_state='collapsed'`／`:290` `_IA_NAV_OFF`／`:315` radio index=0／`:497-506` `_render_tab_isolated`／`:603-605` footer 在 `st.stop()` 之前 | ✅ 全部成立 |
| `tab_stock_grp.py:463` `st_mod.columns(7)`（全 repo 唯一 7 欄） | ✅ 行號精準 |
| `macro_buckets.py:256-262`（`us10y` 永久灰燈） | ✅ |
| `unified_verdict_render.py:38` `_fmt_value` 無 `None` 分支 | ✅ 行號精準；草稿已正確標為「未追上游、⛔ 不得當成已證實的 bug」 |
| `etf_tab_portfolio.py:297-298`（`target_pct_user`，`None = 使用者沒填`）／`:422`（原碼 `target_pct = actual_pct` → 偏離恆 0） | ✅ 兩處都成立 |
| `shared/allocation_decision.py:193-200` `_fmt_range` 唯一 formatter／`:316-317` `drivers[0]`／`:327-329` `drivers[-1]` | ✅ |
| **唯一一處行號有誤** | 🔻 `station_specs.py:318`（草稿說那裡寫著「真要看趨勢請到 🏆 個股組合…」）→ **實測在 `:349`**，且屬 `KEY_STOCK_TREND`（財報趨勢）不是 `:318` 的 財報體檢。**該字串存在、內容正確、草稿的結論成立**，只有行號錯。順帶複驗 `app.py:730` 實測為 `st.tabs(['🔬 個股', '📊 多檔個股比較', '🔭 選股網'])` ⇒ **「沒有叫『🏆 個股組合』的頁籤」成立** ✅ |

## 8.2 承重事實的複驗（本組獨立跑過）

| 草稿宣稱 | 本組複驗 |
|---|---|
| **M-1 有 29 區塊 ＋ 1 條跨頁狀態列** | ✅ **成立。** 本組用 `re.findall(r"\{t:'([^']*)'", html)` 抽出 **30 個 block 標題**，其中 1 個是「（跨頁）頂部狀態列」⇒ 29＋1。且逐頁數：頁1=6+1、頁2=4、頁3=7、頁4=8、頁5=4，與草稿 2.1 表**逐頁吻合** |
| **`UI_LOADING` 在 `src/` 零 production 使用** | ✅ `grep -rn "UI_LOADING\|in_flight" src/ app.py` → **唯一命中是 `_ui_kit.py:10` 的 docstring 列舉** |
| **`st.spinner` 在 `src/ui/views/` 只有 1 處** | ✅ `page_find.py:1628` |
| **`st.tabs` 兩葉 body 同輪都跑** | ✅ 註解在 `:3733`；而且**程式碼本身就是證據**：`with _leaf2:` 出現兩次、夾著 `with _leaf1:`，三段同輪執行。草稿由此推出的 L0-1（⛔ 不得以「放葉2 比較省」當理由）**推論成立** |
| **頁5 沒有任何 `form_submit_button`** | ✅ `grep -c` ＝ 0，且本組另掃 `st.button` 亦 0；實際控制項就是 `:2119` expander ＋ `:2121` checkbox ＋ `:2169` chat_input，**兩步兩個 label 的描述完全正確**（`ENGINEER_EXPANDER_LABEL` / `ENGINEER_CHECKBOX_LABEL` 兩個常數實測存在於 `:339-340`） |
| **`SATELLITE_TAKE_PROFIT_PCT` 走 SSOT** | ✅ `shared/dividend_station_thresholds.py:167` ＝ `15.0`（供 §5 合規自查組參考：**它是系統常數，不是使用者設定的**） |

## 8.3 規格判斷本組認為**正確且有依據**的

- **★-03 的方向**（`—`/`N/A` 放 `facts` 而不放大字區、也不動 L5 dataclass）—— production 已用同一招（`page_today.py:996`、`page_hold.py:2582`），**零契約變更**的理由成立。
- **★-05**（時點列不做新鮮度判斷）—— `expected_latest_trading_day()` 實測只收 `date`、無時刻概念，`holidays=` 全 repo 零 caller 注入。**「每個交易日整個上午都會發假警示」的推論成立。**
- **★-07a**（混合＋部分不明一行寫完）—— 補的是真缺格，寫法合理。
- **2.6 的數值三分決策樹 ＋ E-1~E-10 十個實例** —— 本組逐條推導，**十個都能從決策樹推出來，沒有自相矛盾**。
- **2.14.1「按鈕文字一律不變，變的是旁邊的說明字」** —— 這是繞開 `action_label()` raise 的正解。
- **Part 8.3 的誠實揭露品質** —— 「一個畫面都沒跑過」「五支 View 都沒通讀」「基金站只讀了 2 個檔」三條結構性揭露**與本組實測一致**（草稿確實沒跑畫面；它答不出來的地方，幾乎都落在它自己標的那三條範圍內）。這一點值得說：**它錯的地方，它大多預告過**。

---

# §9 我沒查到的（`CLAUDE.md §-2` 規則 6）

## 9.1 結構性沒查到

1. **一個畫面都沒跑過。** 本組與主筆組同一個限制：全部是讀 code ＋ 靜態驗證 ＋ 對 dataclass 建構器做單元級實跑。**沒有啟動 Streamlit、沒有 AppTest、沒有截圖、沒有實機量測。** §6 三情境是**沿著 render 路徑逐行讀出來的推演，不是跑出來的**。
2. **五支 View 沒有通讀。** 本組讀的是：檔頭 docstring、葉/CTA 常數區、`render_page_*` 主流程、`build_*_card` 的狀態分支、所有 `UnwiredSpec` / `_staged_card` / `wired=False` 命中處。**其餘約八成行數沒讀。**
3. **基金站（`my-fund-dashboard`）一個檔都沒開。** 草稿 Part 4 的所有宣稱（9 分頁、五態 SSOT、`render_state.py:1-27`、主 CTA 字串、Deep Link 兩段都壞）**本組完全未複驗**。
4. **沒跑任何一支 pytest。** §1.4 說「5 支 CI 測試轉紅」是**讀斷言推出來的**，不是跑出來的。同理 §3(b-6) 的 CI 紅燈是**把 `_PATTERN` 抽出來自己 match**，不是跑 `pytest tests/test_no_hardcoded_position_pct.py`。

## 9.2 ⚠️ 全稱句自標（只能當待驗事項）

| # | 我寫過的全稱句 | 為什麼只能當待驗 |
|---|---|---|
| 1 | 「`headline()` 在 production 只有 1 個真呼叫點」 | 以 `grep -rn "headline()"` 得出（3 命中，逐一判讀）。**若有動態呼叫（`getattr(obj,'headline')()`）本組掃不到。** 本組認為可能性低但未排除 |
| 2 | 「新五頁沒有任何一處呼叫 `headline()`」 | `grep -rn "headline(" src/ui/views/` ＝ 0。同上限制 |
| 3 | 「草稿裡約 20 處 Note 文案會 raise」 | 本組用「含狀態 glyph ＋ 6 行內出現『為什麼』與『去哪補』」這個 signature 掃出 **33 條**，再逐條人工判讀。**signature 之外的寫法掃不到，實際只會更多不會更少** |
| 4 | 「`SECTION_LABELS` 只有 4 個 key ⇒ 兩條指路句會 raise」 | 已實跑證明那兩個 id 會 raise。但**本組沒有窮舉草稿裡所有指路句的目標**，只查了 `x.8` 四張表裡點名的那幾條 |
| 5 | 「草稿全篇 0 次提到 `UNTOUCHED_BLOCKS` / 頁首更新報告」 | 四個關鍵字（`UNTOUCHED` / `摸不到` / `更新報告` / `refresh_report`）全部 0 命中。**若草稿用別的措辭描述過同一件事，本組掃不到** |
| 6 | 「`form_submit_button` 呼叫點共 4 處」 | `grep -rn "form_submit_button" src/ui/views/` 逐條判讀（扣掉 docstring 提及）。**只掃了 `src/ui/views/`，沒掃 `src/ui/tabs/` 與 `src/ui/etf/`** |
| 7 | 「同一句 fallback 實測 4 份」 | 以「總經未評估 —」為 key 掃 `src/` `shared/` `app.py`。**同義改寫（如「總經尚未評估」）掃不到** |

## 9.3 明確**沒有**複驗的第三方宣稱

- **`grape_ladder` 在 page_hold 再掛一次會 `DuplicateWidgetID`**（§1.6）：本組只證實了那 4 個 widget key 確實寫死，**沒有實際掛兩次重現例外**。該因果是 `page_hold.py:2196-2198` 的**第一手宣稱**，本組據實轉錄。⚠️ 另外要注意一個可能的反向：新五頁與舊七頁互斥（`st.stop()`），所以「一次 run 內掛兩份」的路徑**本組沒有找到**。**這條的嚴重度可能被我高估，請實作組實測一次再定。**
- **`test_no_hardcoded_position_pct` 的 `_scan()` 是否會跳過我假設的那些行**：本組讀到它對註解有 `startswith('#')` 排除規則，但**沒有逐行讀完 `_scan()` 的全部過濾**（`_FALSE_POSITIVE_RE` 兩條我讀了，兩條都不會救 `持股區間為 60–80%`）。⚠️ **請以實跑該測試為準。**
- **`S1-4_REDTEAM` §2.9 的四處缺格，草稿補了幾處**：本組只查了其中的「§3.2 燈號徽章缺 Partial 行」（草稿 2.14.3 **已補** ⑥ 涵蓋不全那一行 ✅）與「§3.8 全域揭露列只規範 4/8 種」（草稿 2.10.1 第 2 條**仍然只有 7 列**，⚠️ 但草稿把它重新定義成「不是狀態」，**這是不是有效的解法，本組未判定**）。另外兩處**沒查**。
- **草稿 Part 4（基金站）、Part 6（兩站一致性）、Part 7 的 25 條 ★ 內容**：除了 ★-10 之外，**本組一條都沒有獨立驗過**。

## 9.4 我認為我可能錯的地方（先自己招）

1. **§1.1 的嚴重度** 建立在「框線圖裡寫在句首的 glyph，實作者會照抄進 `now`」這個假設上。**一個很小心的實作者會把它讀成徽章**。我把它列為最嚴重，理由是 (a) 草稿自己對 R-A 用的就是「照字面抄會炸」這個標準、(b) `x.7` 矩陣的格子沒有徽章/文案的欄位分隔、(c) **3.1.7 ①⑧ 那一格的 ⬜ 用徽章解釋不通**（徽章該是 ⛔）。**若客戶或實作組認為 (a)(b) 太嚴，(c) 那一格仍然單獨成立。**
2. **§3(b) 我否證的是「6 個畫面」這個數字與機制，不是 ★-10 的設計方向。** 我認為候選甲的設計論證仍然成立，只是成本被低估。**若總管讀成「紅隊否決了候選甲」，那是誤讀。**
3. **§6 是推演不是實跑。** 每一格我都附了 `file:line`，但「這一格在那個情境下真的長那樣」**沒有一格是我看到的**。
