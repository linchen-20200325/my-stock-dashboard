# 【S1-3B 紅隊 C 組】稿內違規自查 ＋ 完整性判斷 ＋ 三條拍板落地驗證

> **稽核對象**：`S1-3B_FULL_DRAFT_SPEC.md`（3,157 行，量測日 2026-09-14）
> **判準**：`S1-6_COMPLIANCE_COPY_GUIDE.md` §1.1 STEP 2「缺項填補測試」＋ §1.1 STEP 3 三道自我檢查
> （(a) 動詞 (b) 同義詞 (c) 遮蔽）＋ §1.3 五級量尺（線畫在 L2-a / L2-b 之間）
> **職責邊界**：本組只查 (a) 稿內違規句、(b) 完整性、(c) 三條拍板落地。
> A 組（會不會炸）、B 組（有沒有發明畫面上不存在的東西）不在本組射程內。
> **唯讀紀律**：本組**未修改** repo 任何檔案、**未改寫**被稽核文件、**未 git 寫入**。
> 唯一一次讀 repo 是為查證 F-3 的承重事實（`src/services/dividend_station_service.py`，只讀不寫）。

---

## ⭐ 一句話結論

**這份稿的合規工程是「詞表 + 逐處列舉」，不是「言語行為判準」。**
它在**頁1** 把「建議持股 70%」花 60 行論證掉了（做得非常好），
但在**頁4** 用**一模一樣的病、一模一樣的位置**（第一層主數值）寫下 `加碼 3` / `減碼 1`，
而且**沒有任何一行察覺**。根因可實證：

> **`S1-6_COMPLIANCE_COPY_GUIDE.md` 完全不在本稿的材料清單裡。**
> 實測：全稿 grep `S1-6` / `缺項填補` / `量尺` / `裸標籤` / `遮蔽` → **各 0 命中**；
> `0.3 材料輩分表`（`:33-46`）六列中無此檔。本稿的合規基礎只有
> `S1-3:171-183`（**詞表**）＋ `COMPLIANCE_SCAN_B`（**個案清單**）。

而 `S1-6` 開宗明義那句正是在講這件事：**「詞表抓不到行為，只有判準抓得到。」**
下面 §1 的 8 條違規，**沒有一條含禁用詞**（除 F-2 的「建議」二字），
**全部都是詞表結構上掃不到的偽陰性。**

---

# §1 稿內違規句（逐條行號）

> 每條格式：行號 → 原句 → 為什麼違規（跑哪一道測試）→ 建議改法。
> 嚴重度：🔴 承重（在第一層／頁標題／客戶一眼會看到）／🟠 次要（第二~四層）／⚠️ 邊界（兩面都有道理，請客戶或另一組裁）。

---

### 🔴 **F-1｜頁4 ① 結論卡主數值寫「加碼 3 / 減碼 1 / 3 加碼·1 汰換」—— 與本稿自己殺掉的「建議持股 70%」是同一個病、同一個位置**

**行號**：`1993`、`1994`、`1995`（3.4.3 有資料 · 桌機線框），連帶 `2007`、`2072`

```
1993: ║ │  │ 該做什麼      ││ 訊號可信度    ││ 需要處理      │  ← 卡片標題
1994: ║ │  │ **12 檔**    ││ **12 / 12**  ││ **加碼 3**    │  ← 大字主數值
1995: ║ │  │ 3 加碼·1 汰換││ 已判定        ││ 減碼 1·未判 0 │  ← 副行
2007: ║ │  12 檔 × 235 加碼燈 / 3-3-3 判定 —— …
2072: | **① 結論三卡** | 該做什麼／可信度／需要處理 | …（x.7 矩陣同樣寫「該做什麼」）
```

**為什麼違規**
1. **缺項填補測試**：觀測 ＝「12 檔持股」；行動 ＝「加碼／減碼／汰換」。
   要從觀測走到「加碼」，讀者**必須**代入可投入資金、持有期間、風險承受度、既有部位 ——
   **系統一個都沒有，卻已經把答案填好了**（`加碼 3`）。→ **違規。**
2. **(c) 遮蔽檢查**：遮住旁邊所有文字，只留 `加碼 3`。讀者說得出判定門檻嗎？**說不出。**
   → 落在 L2-b 裸標籤以下；`加碼`／`減碼` 本身是**動作**，實際落在 **L4 直接指令**那一格。
3. **(b) 同義詞檢查**：`應該加碼` 已在本稿 `:791` 的禁用語表上。
   `加碼 3` **只是把情態助動詞拿掉**，言語行為一字未變 —— 正是 `S1-6 §2.0` 明文禁止的
   「換同義詞／加軟化詞」的**反向操作**（拿掉軟化詞反而更像指令）。
4. ⭐ **最致命的一點：本稿自己已經寫出正確判準，只是沒套到這裡。**
   `:1127-1128` 逐字：「**違規的是那個百分比在第一層的位置，不是『建議』兩個字。**」
   `:1063` 逐字：「替代方案必須**改變謂語本身**：從『你該做什麼』改成『現在的狀態是什麼』。」
   —— `加碼 3` 的謂語**就是「你該做什麼」**，而且就在**第一層主數值**。
   本稿花 `:1051-1132` 共 82 行處理頁1 的同一個病，**頁4 一行都沒有。**

**⚠️ 一個必須講清楚的前提**：`加碼`／`減碼` 很可能是既有 235 燈的**狀態名**（`:2007` 自稱「235 加碼燈」）。
即使如此**也不改變判定**，因為：
- 本稿 `:791` 的禁用語射程明寫「**全站任何位置：標題、卡片、表格、佐證區、提示、推播、AI 產出**」；
- 本稿 `:875-886` §2.12.4「已知的既有違反（僅回報，本階段不修）」**四列裡沒有 235 燈這一條** ——
  也就是它**既沒被改寫，也沒被登記**，是本稿合規盤點的一個**真空**。

**建議改法**（三選一，皆不需新增計算）
- **甲（推薦）**：主數值改 `需要處理 3 檔`，副行改**門檻關係** ——
  `3 檔觸發 235 燈第 2 段 · 1 檔跌破 3-3-3 判定 · 未判 0`。
- **乙**：主數值改 `🟢2 🟡1 🔴1`（純燈號計數），門檻寫在〔展開佐證 ▸〕第一行。
- **丙（最小改動）**：至少把卡片標題 `該做什麼` 改成 `需要看一下的`，並把 `加碼／減碼／汰換`
  三個**動作詞**換成燈號名（本稿 §2.13 已有同型改寫範式）。
- ⚠️ 無論哪一案，**`:2007` 的「235 加碼燈」與 `:2072` 矩陣的「該做什麼」要一起改**，否則又是一處不一致。

---

### 🔴 **F-2｜「換股建議」四處未改名，直接牴觸本稿自己的附錄 B-03（且畫面最顯眼那一處用的正是違規版）**

**違規行號**：`115`、`1927`、`1970`、`2011`
**已改名行號（證明本稿知道要改）**：`2043`、`2075`（「換股對照」）、`3058`（附錄 B-03 明文裁定改名）

```
2011: ║ │ ④ 換股建議（搭配總經位階）        ← 有資料態的區塊標題（最顯眼）
1927: ║ │   ④ 換股建議 ＋ 總經位階           ← 冷啟動「按下去會出現什麼」目錄
1970:          需要處理 · 戰情表 · 換股建議 · 配置偏離 · …  ← 冷啟動未讀清單
 115: … ④ 換股建議 ⚑ …                     ← Part 2.1 資訊架構全表
--- 對照 ---
3058: | **B-03** | 頁4 ④ 名稱 ＝「**換股建議**」 | 「**換股對照**」… 改的只有那兩個字
```

**為什麼違規**
- `建議` 二字**逐字命中**本稿 `:791` 自己的禁用語表，位置是**卡片／區塊標題**。
- 本稿 `:3058` 已經裁定改名、已經說明理由、已經歸類為「修正錯誤不是改設計」——
  **裁定寫了，線框沒跟上。** 一份規格裡兩個名字並存，實作組會照**線框**做（線框才是他們看的那一頁），
  也就是**照著違規的那個做**。
- ⚠️ 這不是 B 組的「發明畫面上沒有的東西」——**恰恰相反**，是本稿**已經決定要改、卻漏改了四處**。

**建議改法**：`115` / `1927` / `1970` / `2011` 一律改「**換股對照**」。
`2011` 下方既有的「⚠️ **客觀對照，非買賣建議。**」（`:2014`）**保留**，那一句是對的。

---

### 🔴 **F-3｜⑤-a 衛星停利：本稿在 `:1894` 親口判定「指名哪幾檔該停利 ＝ 直接買賣建議」，`:1895`／`:2018-2019` 的「改寫」卻仍然指名 —— 而且「你設定的」是假的**

**行號**：`1895`（改寫定案句）、`2016`、`2018`、`2019`（線框落地），連帶 `1893`、`1894`、`2841`

```
1893: 「💰 衛星停利（獲利達 15% **建議嚴格停利、滾回核心**）：2382（+23%）…」   ← 現行（本稿判為違規）
1894: —— **指名哪幾檔該停利 ＝ 直接買賣建議**。⇒ 本稿的 ⑤-a 文案改為**偏離提示**：
1895: `… 衛星 2 檔已達你設定的停利門檻（客觀對照，非建議）`                      ← 本稿的改寫
--- 落地成線框 ---
2016: ║ │ ⑤-a 80/20 配置偏離 ＋ 衛星停利
2018: ║ │  衛星 2 檔已達你設定的停利門檻（**客觀對照，非建議**）：
2019: ║ │    2382（+23%）· 3037（+18%）      ← ⭐ 還是指名了
```

**為什麼違規（三層，逐層都不成立）**

**第一層 —— 改寫沒改到病灶。**
本稿自己說病灶是「**指名哪幾檔**」（`:1894`）。改寫後 `:2019` **照樣指名 2382、3037**。
它拿掉的是「建議嚴格停利、滾回核心」那半句，**留下了被自己點名的那半**。
跑 `S1-6 §1.1 STEP 3 (a) 動詞檢查`：改寫後還有沒有指定讀者該做什麼？
「**停利**」本身就是動作（賣出獲利了結），且後面直接列出是哪兩檔。→ **有。沒改乾淨。**

**第二層 —— 「（客觀對照，非建議）」是免責括號，不是改寫。**
`S1-6 §2.0` 鐵則：❌ 加軟化詞 ——「只是更難被詞表抓到」。
一句「這不是建議」加在一個指名個股的停利提示後面，**言語行為完全沒變**。

**第三層 —— ⭐「你設定的停利門檻」是**事實錯誤**，而這句話正是本條唯一的逃生門。**
`S1-6 §1.1 STEP 2` 給過一條合格路徑：「比較基準是**讀者自己給的** → 合格」。
本稿寫「**你設定的**」，就是在走這條路。但**那個門檻不是使用者設的**：

> **實測（本組本次 `grep` + `sed` 讀出，read-only）**
> `src/services/dividend_station_service.py:938-953` `flag_take_profit()` docstring 逐字：
> 　「郭俊宏:衛星獲利 15~20% 嚴格停利。門檻 `SATELLITE_TAKE_PROFIT_PCT`(=15)。」
> 判定式：`if _pnl >= T.SATELLITE_TAKE_PROFIT_PCT`
> ⇒ **策略常數，沒有任何使用者輸入路徑。**
> 旁證：本稿自己在 `:1883`／`:2838` 的 ⑤-a／⑤-b 對照表寫得很清楚 ——
> ⑤-a 的「目標從哪來」＝「**系統內建的 80/20（策略常數）**」，⑤-b 才是「使用者自己填」。
> **本稿知道 ⑤-a 是常數，卻在 ⑤-a 的文案裡寫「你設定的」。**

⇒ 逃生門不成立。**這句話同時是一個合規違規與一個事實錯誤**，而後者是前者的支撐。
⚠️ 這也讓它落進 §2.12.2 那條被本稿自己禁掉的形狀：
「**禁止預設最佳配置推薦**…系統⛔ 不得預填、⛔ 不得建議」——
一個系統常數被描述成使用者設定值，正是「預設配置被偽裝成使用者選擇」。

**建議改法（兩步，缺一不可）**
1. **先修事實**：⛔ 不得寫「你設定的」。改為
   `門檻 +15%（策略常數 SATELLITE_TAKE_PROFIT_PCT，**非你設定**；出處見 📖 憑什麼 › 教學）`。
2. **再修位置**：第一層**只給計數與門檻**，個股代號降到〔展開佐證 ▸〕：
   ```
   ⑤-a 80/20 配置偏離
     核心 76% / 衛星 24%（目標 80/20，偏離 −4pp）
     衛星部位中 2 檔的未實現報酬率 ≥ +15%（門檻為策略常數，非你設定）  〔展開佐證 ▸〕
       └ 展開後：2382 +23%（超出門檻 8pp）· 3037 +18%（超出門檻 3pp）
   ```
   —— 這樣 (a) 動詞沒了、(b) 不是同義詞替換、(c) 門檻印在字面上，三道檢查全過。
3. ⚠️ **「停利」二字建議一併換掉**（換成「達門檻」），它不在 `:791` 詞表上，但 STEP 2 抓得到。

---

### 🔴 **F-4｜頁1 螢幕標題「今天能不能出手、出手到幾成」—— 本稿在 `:814` 自己把這句列為「建議持股 X%」六個違規落點之一（落點 d，[v2] 新五頁），然後在 `:1149` 把它原樣放上螢幕**

**行號**：`1149`（螢幕上的頁標題）、`1044`（頁職責）、`743`（頁尾「第 N 步」一句話）
**對照行號**：`814`（本稿自己的違規落點表）

```
 814: | d | `views/page_today.py:1356-1359` 卡① label「能不能出手 · 出手到幾成」 | **[v2]** |
      ↑ 這一列在 §2.12.1「六個落點」表內，表頭寫的是「建議持股 X%」的落點
--- 然後 ---
1044: | **職責** | 回答「**今天能不能出手、出手到幾成**」 |
1149: ║ 🚦 今天 ── 今天能不能出手、出手到幾成              ← 螢幕上的頁標題
 743: | 第 1 步 | 🚦 今天 | 能不能出手、到幾成 |          ← 頁尾「第 N 步」文字
```

**為什麼違規**
- **缺項填補測試**：「出手**到幾成**」＝ 你該把資金投入到幾成 ——
  這是「建議持股 X%」的**台語口語版**，語意一字不差。
  要回答「幾成」需要資金、期間、風險承受度；系統沒有，卻承諾回答。
- ⭐ **本稿自己的判定就在同一份文件裡**：`:1059` 逐字說「建議持股 X%」是
  「客戶拍板第 3 條『全站不產生直接買賣建議』的**正面命中**」，而 `:814` 把這句口語版
  列為那條違規的落點 d。**列為違規落點的字串，不能同時當頁標題。**
- ⚠️ 這一條的嚴重度高於 F-1，因為它是**頁標題**：頁1 的**每一種狀態**（冷啟動／載入中／
  有資料／部分／空／錯誤）**都會顯示它**，等於一句違規文案出現在該頁的全部六種畫面上。

**建議改法**：改成**描述本頁在量什麼**，不描述讀者該做什麼 ——
`🚦 今天 ── 今天的市場位階、訊號可信度與風險上限`
（與 `:1069` 已定案的候選甲主數值同一個謂語，**全頁謂語才會一致**）。
⚠️ `1044` / `743` 一併改，否則頁尾「第 1 步」那行小字會留下一份。

---

### 🟠 **F-5｜頁4 螢幕標題「我已經持有的，該加、該換、該減」＋ ① 卡標題「該做什麼」**

**行號**：`1906`（螢幕標題）、`1842`（頁職責）、`746`（頁尾第 4 步）、`1924`／`1969`／`1993`／`2072`（「該做什麼」）

```
1906: ║ 💼 我的持股 ── 我已經持有的，該加、該換、該減      ← 螢幕上的頁標題
1924: ║ │   ① 今天該做什麼 · 訊號可信度 · 需要處理
1993: ║ │  │ 該做什麼      ││ …                            ← 結論卡標題
```

**為什麼違規**
- `該加／該換／該減` 的「**該**」是規範性情態動詞，與禁用語表上的「**應該**加碼」同型，
  只是縮成一個字。**(b) 同義詞檢查**直接不過。
- 「今天**該做什麼**」是一個**承諾**：系統承諾告訴你該做什麼 ——
  而 F-1 顯示它真的做到了（答案是 `加碼 3`）。**標題與內容互相坐實，不是修辭。**

**⭐ 附帶發現：本稿 §2.13 新手化字典的改寫，自己沒通過 (a) 動詞檢查。**
```
915: | 「本頁職責『該加、該換、該減』的那個『該換』」 | 手上哪一檔**該換掉**、可以換成什麼。 |
916: | 「它回答『該減』，與 ④ 的『該換』互補」       | 哪一檔部位太重、哪一檔漲到**可以減**。 |
```
`S1-6 §1.1 STEP 3 (a)`：改寫後還有沒有指定讀者該做什麼？
「該換掉」**有**、「可以減」**有**（且 `S1-6` 明文：「**可 / 值得 / 宜 / 留意**」不是免責詞）。
⇒ **§2.13 這兩列是把一句違規句改寫成另一句違規句**，只是變白話了。

**建議改法**
- 頁標題：`💼 我的持股 ── 你手上這些的燈號、偏離與需要看一下的項目`
- ① 卡標題：`該做什麼` → `需要看一下的`
- §2.13 `:915` → 「哪幾檔的體質燈由綠轉黃／轉紅，以及你的觀察清單裡有哪些同類替代。」
- §2.13 `:916` → 「哪一檔佔比高於你設的目標、哪一檔已越過門檻。」（**陳述門檻，不給動詞**）

---

### 🟠 **F-6｜§2.12.2 標題自稱「本稿全篇只用這一組」是不成立的全稱句**

**行號**：`831`（標題「### 2.12.2 允許的替代表述（**本稿全篇只用這一組**）」）

**為什麼**：該表只有六種替代表述（市場位階／權重偏離／相對位置／如果調整／價格區間／歷史對照）。
但本稿線框實際使用的判讀語彙遠多於六種，且多數**不在表內**：
`加碼 3`（1994）、`減碼 1`（1995）、`汰換`（1995）、`抗震極強`（1713）、`好生意`（1714）、
`本業獲利`（1714）、`中性偏低`（1696）、`主力連買`（1696）、`貼近淨值`（1733）、
`近 4 季未縮`（1733）、`衛星停利`（2016）。
⇒ 這句話是 `CLAUDE.md §-2 規則 6` 意義上的**未經驗證全稱句**，而且**可以直接否證**。

**建議改法**：改成「**替代表述的六種基本型（本表為最小集，不是窮舉）**」，
並在表尾補一行：「表外的判讀語彙一律須通過 (a)(b)(c) 三道檢查後才可使用。」

---

### ⚠️ **F-7｜大量「裸標籤」出現在卡片大字數值區（門檻不在同一個渲染單位裡）**

**行號**：`1696`（`中性偏低`／`主力連買`）、`1713`（`抗震極強`）、`1714`（`好生意`／`本業獲利`）、
`1733`（`貼近淨值`／`近 4 季未縮`）

**為什麼是⚠️ 而不是 🔴（兩面都寫出來）**
- **是違規那一面**：跑 **(c) 遮蔽檢查** —— 遮住旁邊，只留「抗震極強」，讀者說不出門檻。
  `S1-6 §1.3` 把線畫在 L2-a / L2-b 之間，這幾個**全部落在線下（L2-b）**。
  而本稿 `:1713` 的「抗震極強」更嚴重：它佔的是**大字數值區**，**整格沒有數字**。
- **不算違規那一面**：`S1-6 §1.3` 自己標註「⚠️ **線的位置待客戶確認**，見 §6 議題 1」，
  且註明是「**丙組單組推導，未複驗**」。**客戶母法只禁「直接買賣建議」，沒有禁裸標籤。**
  「好生意／抗震極強」是**品質描述**，不是**行動指令** —— 缺項填補測試它們**過得了**
  （從「這是好生意」到「所以買」仍需讀者自己代入全部六個變數，系統沒替他填）。

⇒ **本組判定：不列為母法違規，列為「本稿未處理的已知裁量點」。**
**建議**：`:1713` 的 `抗震極強` **至少要補數字**（安全邊際是可量化的），否則大字區是純形容詞；
其餘四處建議比照 `:1733` 已做對的樣式（數字在大字區、形容詞降到副行）——
實測 `:1733` 的 `+0.12%` 在大字、`貼近淨值` 在副行，**那個排法是對的**，
問題只出在 `:1713` 把形容詞升到了大字區。

---

### ⚠️ **F-8｜第四層佐證裡的「油門帶 60–80%」與明細清單的「什麼時候買賣」**

**行號**：`1079`（`· 總經健康分 68 → 姿態「偏多」，油門帶 60–80%　← drivers[0] 原文`）、`1704`（`→ 什麼時候買賣 → 心理檢查`）

**為什麼是⚠️**
- **是違規那一面**：`油門帶 60–80%` 與「建議持股 60–80%」**語意同一**（要投入幾成資金）。
  本稿 `:791` 的禁用語射程明寫包含「**佐證區**」。
  `什麼時候買賣` 作為一個區塊名，**承諾回答買賣時點** —— 那是最典型的缺項填補（時點取決於讀者的期間與資金）。
- **不算違規那一面**：`:1079` 被本稿明確標為 `drivers[0] 原文`（provenance 引用），
  且**緊接下一行**（`:1082-1084`）就把同一個區間改寫成「歷史上此位階對應的持股區間為 60–80%（**歷史統計，非建議**）」——
  也就是本稿**在同一個框內同時給了原文與改寫**。這在「可追溯」上是加分的。
  `什麼時候買賣` 則是 production 既有區塊名（`section_when_buy_sell.py`），本稿只是轉錄。

⇒ **本組判定：不單獨列為違規，但列為「本稿合規盤點的漏網」** ——
因為 §2.12.4「已知的既有違反（僅回報）」**四列裡兩者都沒有**，
等於它們既沒被改、也沒被登記，重演 F-1 的同一個真空。
**建議**：把「`油門帶`」「`什麼時候買賣`」「`235 加碼燈`／`3-3-3`」**補進 §2.12.4 登記表**
（不必本階段改，但必須被看見）。

---

## §1 小結：三個被點名查的位置，逐一回答

| # | 派工問的 | 答案 | 行號 |
|---|---|---|---|
| **1** | 「建議持股 X%」加進禁用語表後，**別節有沒有又用了它或變體**（「持股水位 70%」「股票部位建議 X 成」）？ | ⭐ **有，而且是本稿自己沒察覺的那個變體。** 「持股水位」「部位建議 X 成」作為**畫面文案**全稿 **0 命中** —— `持股水位` 只出現兩次，
**兩次都是 meta**：`:815`（引用 production 的違規落點 e）與 `:1126-1128`（**本稿主動把它列為
「⛔ 不列為候選」並逐條否決**）。⭐ 這段否決寫得很好，**本組完全同意其理由**。**但語意變體命中：`:1149` / `:1044` / `:743` 的「出手到幾成」** —— 而本稿 `:814` 自己就把這句列為該違規的落點 d。**見 F-4。** | 1149 / 1044 / 743 |
| **2** | 🔬 查一檔估值卡有沒有原樣抄 code 的「**估值過高，建議逢高減碼**」？ | ✅ **沒有抄，這一處乾淨。** 全稿 grep `逢高` **0 命中**、`過高` **0 命中**。估值卡實際寫的是 `估值（357）` ＋ `中性偏低`（`:1695-1696`），是狀態描述不是行動。⚠️ 唯一可挑的是 `中性偏低` 是裸標籤（見 F-7），但**與「建議逢高減碼」不是同一個量級**。 | 1695-1696 |
| **3** | 再平衡文案有沒有殘留「動作／建議股數／買進 XXX 股」這種交易指令形狀？ | ✅ **沒有，Part 5 是全稿合規做得最好的一段。** 逐項確認：**無**「動作」欄（表頭是 `標的/目前%/目標%/偏離(pp)/提示`，`:2639`）、**無**「建議股數」、**無**「買進 N 股」、**無**一鍵按鈕（`:2690` 明文列出三種禁止的按鈕名）、輸入欄空白且明文禁預填（`:2617`）、**無**具名範例列（`:2700` 明文用 `〔＿＿＿＿〕` 佔位）。⚠️ **一處邊界**見下。 | 2589-2701 |

**⚠️ Part 5 唯一的邊界（不列為違規，但請客戶知道）**：`:2675-2681` 情境試算表多了一欄
「**換算（參考）**：約 0.89 張（不足 1 張）／約 1.72 張」。
本稿自己 §2.12.2 `:838` 核准的替代表述只寫到「需增加約 **NT$XX**」—— **金額，沒有張數**。
- **可以接受的理由**：它是純單位換算；「不足 1 張」等於**明說你執行不了**；
  上方 `:2670` 有「試算，不是建議」、下方 `:2683-2689` 有五條限制含「**此表不是下單清單**」。
- **應該留意的理由**：「約 1.72 張」與被禁的「建議買進 5 張」**只差一個動詞**；
  一旦有人把這一欄單獨截圖／放進推播，防護全部脫落。
- **本組建議**：保留該欄，但**欄名改成「換算張數（僅供對照，非下單量）」**，
  並把「不足 1 張」的標註**改成常駐**（現在只有不足時才出現，足額時反而沒有警示）。

---

## §1 未複驗聲明（`CLAUDE.md §-2` 規則 6）

⚠️ **「除上述 8 條外沒有其他違規句」—— 這句話本組不寫，因為沒有第二組驗過。**
本組實際做的是：(1) 對 25 個行動詞關鍵字全稿 grep（142 命中逐條判讀）；
(2) 對全部線框方框（`║` 開頭）逐段人工讀過；(3) 針對派工點名的三處做定向查證。
**掃不到的形狀**：一句零關鍵字、非方框內、且不含任何本組想到的動詞的偽陰性 ——
這正是 F-4（「出手到幾成」）差點漏掉的那一類，本組是靠**比對本稿自己的違規落點表**才抓到的。
⇒ **建議另派一組，用「反向法」重跑一次**：不掃違規詞，改**逐一檢查每一個卡片標題與頁標題的謂語**
（問「這個標題在描述狀態，還是在承諾告訴你該做什麼？」）。本組認為那條路徑還會再抓到東西。

---

# §2 完整性逐項表

> 判定標準：✅完整 ＝ 內容寫到可以直接交給實作；⚠️部分 ＝ 有骨架但有指名的缺口；🔴缺 ＝ 沒寫。

## 2.1 台股五頁 × 九節（x.0~x.9）

| 頁 | x.0 職責/葉/CTA | x.1 冷啟動 | x.2 載入中 | x.3 有資料 | x.4 部分 | x.5 空 | x.6 錯誤 | x.7 矩陣 | x.8 揭露/指路 | x.9 做不到 | 判定 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **🚦 今天** | ✅ | ✅ 桌機+手機雙寬 | ✅ | ✅ 含葉2 | ✅ | ✅ 明說「本頁沒有整頁空狀態」並說明理由 | ✅ 區塊隔離+整頁兩段 | ✅ 10 欄 | ✅ | ✅ | **✅完整** |
| **🔍 找標的** | ✅ | ✅ 葉1+葉2 分開 | ✅ 明禁單一全頁 spinner | ✅ 含三種數值語彙同框 | ✅ | ✅ | ✅ C-1/C-2 兩種錯誤分開 | ✅ | ✅ | ✅ | **✅完整** |
| **🔬 查一檔** | ✅ +C-12 命名定案 | ✅ | ✅ | ✅ **三分支全有**（個股/ETF/unknown） | ✅ | ✅ 三情境 | ✅ | ✅ | ✅ | ✅ | **✅完整** |
| **💼 我的持股** | ✅ +★-09 CTA 位置 | ✅ 含「目錄式」冷啟動 | ✅ | ✅ 七區塊 | ✅ | ⚠️ **只有一行指回 2.10.4(c)**，無本頁線框 | ✅ | ✅ | ✅ | ✅ | **⚠️部分** |
| **📖 憑什麼** | ✅ 誠實寫「本頁沒有主 CTA」 | ✅ | ✅ | ✅ 三葉 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **✅完整** |

**頁4 的兩個具體缺口**
1. **🔴 葉2「組合設定」沒有任何線框。** 全稿唯一出現處是 `:2080`（3.4.7 矩陣最後一列）與 `:1921` 冷啟動的分頁列示意（`┌ 戰情室 ┬ 組合設定 ┐`）。
   ⚠️ 連 `:1923-1930`「按下上面那顆鈕之後，這裡會出現」的目錄都**只列 ①~⑦，沒有列葉2**。
   而頁4 是**兩葉頁**，葉2 承載「Google 登入／選 Sheet／持股預覽／觀察清單」四件事 ——
   **四件事沒有一件有文案**。⚠️ 這也是 §3 拍板二 D-3 落點 #7 無法落地的原因（見下）。
2. **⚠️ `3.4.5 空狀態` 只有一行**（`:2051`「依 Part 2.10.4(c)：持股 0 檔灰／欄位名不符紅。⛔ 不可對調。」）。
   2.10.4(c) 的表確實寫了那兩格，所以**不算漏**，但它是全稿唯一一個「x.5 不自帶線框」的格子。

## 2.2 基金儀表板

| 項 | 判定 | 說明 |
|---|---|---|
| 📊 資產配置（L1~L4 全層） | **✅完整** | `:2359-2476`。四層齊全、三種數值語彙同框示範（`:2402-2405`）、五態表、矩陣表、L4 四條揭露全文 |
| **收益視角摘要卡** | **✅完整** | `:2380-2388` 三張卡（年化配息率/配息覆蓋率/同類型平均），含「⛔ 不寫『應該贖回』『建議換檔』」的合規註（`:2388`）—— **這一處寫得很好** |
| **Deep Link 動線** | **⚠️部分** | 產生端（`:2411-2421`）✅／接收端落地畫面（`:2526-2540`）✅／不支援標的誠實文案 ✅。⚠️ **但現況兩段都是壞的**（`:2503-2511`），三方案 ★-13b **待拍板**，所以這條動線目前是「規格寫完了、能不能成立待客戶決定」 |
| 🌐 市場總覽／💊 持倉體檢／🔍 標的探索／⚙️ 設定與診斷 | **🔴缺（刻意）** | `:2477-2500`。見下方專章判斷 |

## 2.3 ETF 再平衡偏離提示動線（跨兩站）

**判定：⚠️部分** —— **台股站那一半 ✅完整，「跨兩站」那個詞沒有兌現。**

- ✅ 三狀態（A 未設定／B 已設定／C 總和≠100%）**全部有完整線框**，且三者的空值符號
  刻意設計成**互不相同**（`▨ 未設定`／`0.00` 有效零／`▨ 不計算`），`:2626-2629`、`:2799-2801` 有逐條說明。
  ⭐ 這是全稿最紮實的一段。
- ✅ 情境試算 ＋ 五條限制 ＋ 三條硬禁令 ＋ 平板/手機 ＋ 十種呈現情況矩陣（`:2817-2833`）全有。
- ⚠️ **但 `:2555` 的「位置」欄自陳：「股票/ETF 戰情室 › 頁4 › 葉1 ⑤-b」——**
  `:2559-2578` 的動線總覽圖**只有一個站**，沒有任何基金站分支。
  基金站對 ETF 的處理是 `:2407-2409`「**⛔ 不做任何 ETF 運算**，只顯示配息與佔比」。
  ⇒ **設計上自洽**（避免兩站各算一次），但**標題「跨兩站」會讓讀者以為有跨站流程**。
  **建議**：Part 5 標題改「**ETF 交易與再平衡偏離提示（台股站主體 ＋ 基金站唯讀引用）**」，
  並在 5.1 圖右側補一個虛線分支指向「基金站 L3-b → Deep Link（見 4.6，★-13b 待拍板）」。

## 2.4 全站共用

| 項 | 判定 | 行號 / 說明 |
|---|---|---|
| **頂部資料時點揭露列** | **✅完整** | `:396-467`。含「歸屬日＋性質」兩段格式、六種性質詞、**拿不到時點時怎麼寫**（`:422-435`）、🔴 拿不到歸屬日的來源清單（`:436-445`）、落地檢查清單。⭐ `:446-457` 還處理了「時點列⛔ 不做新鮮度判斷」的規則對撞 |
| **側欄** | **✅完整** | `:597-641`。含 ★-08 與「這批不動（維持 collapsed）」的客戶逐字回覆佐證 |
| **頁間導航與返回** | **✅完整** | `:734-786`。兩種下一步都做（頁尾「第 N 步」列 ＋ 就地「帶我去」鈕）、⛔ 不做麵包屑的理由、**凡指向舊七頁籤必須寫「先切回舊版」**（`:758`，這一條抓得很好） |
| **冷啟動** | **✅完整** | 五頁各自有，非共用一份 |
| **載入中** | **✅完整** | 區塊級（各頁 x.2）＋ 頁級（`:674-698`）。⭐ 誠實標明「**本節整節是要新建的東西，不是現況描述**」＋ `st.empty()` 實作前置 |
| **空狀態** | **✅完整** | 頁級三情境表（`:722-730`）＋ 各頁 x.5 |
| **錯誤態** | **✅完整** | 區塊隔離 ＋ 整頁（`:700-720`），含「⛔ 不得含模組路徑、但原始錯誤訊息要留」（`:2060-2063`） |

## 2.5 三個響應式斷點

**判定：⚠️部分 —— 寫完了，但本稿自己的數字在一段上互相打架，而且與派工單的規格不同。**

| 派工單寫的 | 本稿 `:507-513` 定案的 | 差異 |
|---|---|---|
| <640px 單欄 ＋ 表格轉卡片流 | ≤480 / 481–640 皆 **1 欄**；轉卡片流規則 §2.9（`:542-553`）✅ | ✅ 一致（且更細） |
| 641~1024px **雙欄且每欄 >280px** | **641–880 → 2 欄（300–420px）**；**881–1280 → 3 欄（265–400px）** | 🔴 **不一致** |
| >1024px 三欄 | >1280 → 3 欄 | ⚠️ 分界線從 1024 移到 1280 |

**🔴 本稿自己的內部矛盾（實測）**
- `:518` 定義硬底線：「**274px ＝ 再窄金額就折行（可讀性底線，正常情況要守住）**」
- `:512` 卻寫：「881–1280px｜**3** 欄｜**265–400px**｜三欄成立，**不降階**」
- ⇒ **265 < 274。** 在 881px 這一端，本稿的定案表**違反了它自己上一行才定的可讀性底線**，
  而「超出時怎麼降階」欄寫的是「不降階」。**本稿沒有任何一行提到這件事。**

⚠️ **本稿確實已誠實揭露另一件事**（`:471-477`、★-06b `:2907`）：
「**整個響應式層從未經客戶拍板**（實測核准線框 0 命中手機/響應式字樣）」，
並攤開了 `S1-3`（640/1024 通則）與 `UI-C`（有量測依據）的衝突、給了推薦與三條理由。
**這部分做得對。** 缺的是 265px 那一格的自我檢查。

**建議**：★-06 的推薦表加一列誠實註 ——
「881–920px 區間三欄每欄約 265–275px，**低於本稿自訂的 274px 金額不折行底線**；
建議把三欄的起點抬到 **~920px**，或接受該窄段金額折行並明列為已知限制。」

## 2.6 第四層「展開佐證」觸發規則 ＋ 加權移動平均 vs FIFO

| 項 | 判定 | 說明 |
|---|---|---|
| **觸發器規格** | **✅完整** | `:196-210` 逐條寫死（區塊標題右側次級文字按鈕、⛔ 卡片本身不觸發、⛔ 整列不可點） |
| **顆粒度定案 ★-02** | **✅完整** | `:211-229` 兩級制（區塊級為預設 ＋ ⛔未接線/🟠失準/🔴錯誤三種情況逐元件例外），三條理由，含「不決定的後果」 |
| **D-2 加權移動平均 vs FIFO 逐字文案** | **✅完整** | `:864-873` 五行全文釘死，含「**同一組交易，兩種算法會得到不同的平均成本與已實現損益。兩個都不是錯的，只是口徑不同**」 |
| **D-2 落點** | **⚠️部分** | 規定四個落點（`:854`：頁4 葉1③ ＋ 頁4 葉2 ＋ Part 5 ＋ 基金站）。實際寫進線框的：頁4 葉1③ `:2009` ✅／基金站 `:2442` ✅／**頁4 葉2 ❌（該葉無線框）**／**Part 5 再平衡區塊 ❌（5.2~5.4 三個線框都沒有這一行）** |

## 2.7 「每個區塊的每一種呈現情況都有文案」

**判定：⚠️部分**

- ✅ **x.7 矩陣層面：完整。** 五頁 + 基金站 + ⑤-b 共 7 張矩陣，**每一格都填了字**，
  沒有留白、沒有「同上」。這是本稿最大的工作量所在，而且做到了。
- ⚠️ **但矩陣的格子是「一句話規則」，不是「文案」。** 例如 `:2077` 的
  「⑤-a 80/20 偏離｜⑦ 門檻失準｜🟠」—— 🟠 這一格**沒有對應的句子**。
  真正寫出**完整文案**的，是各頁 x.1/x.3/x.4/x.5/x.6 的線框方框，
  而那些方框**覆蓋的是正常態與幾個代表性異常態**，不是 10×N 全格。
- 🔴 **AI 區塊完全沒有輸出文案樣板。** ⑦ AI 戰情總結（`:2034-2036`）只有按鈕與說明字；
  葉3 AI 問答（`:2224`）只寫「回答＋引用來源」。
  ⚠️ **而 `:791` 的禁用語射程明文包含「AI 產出」** ——
  **一條管 LLM 輸出的規則，全稿沒有任何落地機制**（沒有 prompt 護欄規格、沒有輸出後過濾規格、
  沒有一句示範輸出）。對照 `COMPLIANCE_SCAN_B` 顯示 AI 路徑是違規高發區，**這是實質缺口。**
- 🟠 **§2.15「反例快查（釘在螢幕邊上用）」17 條裡，0 條是合規反例。** 全部 17 條都是
  §1 fail-loud / 資料誠實的反例。實作者照著這張表做，**永遠不會被提醒買賣建議禁令**。
  **建議補一條 R-18**：「⛔ 用行動詞當卡片主數值（`加碼 3` / `減碼 1` / `該做什麼`）」——
  這正好就是 F-1。

---

# ⭐ 專章：基金站其餘四頁標「待實地盤點」，算不算半成品？

## 本組判斷：**不算半成品，但現在這個樣子客戶審不了。差一樣東西就能跨過那條線。**

### 先說為什麼「不算半成品」（拒絕虛構的那一半是對的，而且必須守住）

1. **它拒絕的是「把推導升級成規格」，不是「拒絕工作」。** `:2483-2485` 引的是
   `S1-3` **自己**的自陳：「§3.2 的四頁落點表**是依母法骨架推導的落點，不是對既有畫面的盤點**」，
   且該文自己要求「階段 2 開工前應先派一組實際盤點」。
   ⇒ 本稿要是照抄，交付的會是**一份看起來完整、但四頁全是猜的線框**。
2. **這個 repo 有過這種事故的實證，就在同一份稿裡。** `:1018-1036`（C-02）：
   `S1-3` 的主 CTA 命名**五頁錯四頁**，因為它是推導的不是查的。
   本稿因此把「production code」列為材料輩分第 3 位、且是「**畫面上實際有什麼的唯一權威**」（`:39`）。
   **在這個前提下去虛構四頁，就是明知故犯。**
3. **虛構的代價是不可逆的。** 一份線框一旦進了核准流程，後面所有人都會拿它當前提蓋。
   對照 `CLAUDE.md §-2` 的實證：「**沒查證的宣稱比沒有宣稱更危險**」。

### 再說為什麼「客戶現在審不了」（留白那一半不夠）

**客戶收到的是一段拒絕 ＋ 一張六列的共用骨架表 ＋ 一個 ★-14c。** 他沒有辦法回答：
- 這四頁**還要多久**？
- 盤點完會不會又冒出第五個 ★？
- **我現在能不能先拍板台股那五頁、讓實作開工**，還是得等基金站一起？
- 這四頁是**大工程還是小工程**？（它可能是 4 個畫面，也可能是 40 個區塊）

⇒ 缺的不是線框，**缺的是「這個洞有多大、什麼時候補得完、擋不擋路」**。
一個沒有邊界的 🔴 在交付文件裡，讀起來跟「還沒做完」沒有區別 —— **即使它的成因完全正當。**

### ⭐ 可執行的線（本組建議，三件事，都不需要虛構任何畫面）

> **原則：可以不知道「長什麼樣」，但不可以不知道「有多大、擋不擋路」。**

**① 把 🔴 從「待補」改成「已界定的待補」——**
補一張**只含實測事實**的盤點表（**這些數字 grep 得到，不是猜的**）：

| 頁 | 檔案 | 行數 | 有幾個 `st.form_submit_button` | 有幾個 `st.tabs` 葉 | 有幾個 `st.expander` |
|---|---|---|---|---|---|
| 🌐 市場總覽 | （實測填） | | | | |
| 💊 持倉體檢 | | | | | |
| 🔍 標的探索 | | | | | |
| ⚙️ 設定與診斷 | | | | | |

—— 這是**純機械量測**，一次 `grep -c` 就有，**零虛構風險**，
但它讓客戶第一次看得到「這四頁是 4 個畫面還是 40 個區塊」。
⚠️ 本稿其實**已經在頁5 示範過這一招**（`:2130` 逐字寫
「實測 `grep -c form_submit_button src/ui/views/page_why.py` → **0**」）——
**同一招套到那四頁就行，不是新方法。**

**② 明寫「這個洞擋不擋台股五頁開工」。**
本組讀完的判斷是 **擋不住**（兩站的唯一耦合點是 Deep Link，而 Deep Link 已獨立成 ★-13b，
基金站 📊 資產配置 那一頁也已完整）。
⇒ 交付時應該有一句：「**台股五頁 ＋ 基金站 📊 資產配置可先行拍板開工；
其餘四頁的盤點與本批實作可並行，不互為前置。**」
—— 這一句把「半成品」變成「**分批交付的第一批**」，是兩個完全不同的東西。

**③ 讓 ★-14c 多回答一個問題：不只「畫不畫」，還要「擋不擋交付」。**

> ⚠️ **本組先更正自己的一個誤判（據 `CLAUDE.md §-2` 規則 6 據實留下）**：
> 本組初稿曾寫「★-14c 沒有附總管推薦方案」。**那是錯的** ——
> 本組當時只讀到 `:2499` 的指標句（「⇒ G-18 列為 Part 7 ★-14c：本稿交付前應派一組實地盤點」），
> **沒有翻到 Part 7 的本體**。實測 `:2923` 的 ★-14c **格式完整**：
> 選項 (a) 照抄／(b) 先盤點、⭐ 推薦 (b)、理由（`S1-3 §7.2:1527` 自陳 ＋ C-02 重演風險）、
> 不決定的後果，**四欄齊全**。且實測 Part 7 **25 條 ★ 全部都有 ⭐ 推薦，0 條例外**。

**真正缺的不是推薦，是推薦的射程。**
★-14c 回答的是「**要不要照抄（不要，先盤點）**」——這個判斷是對的、也該維持。
它**沒有回答**的是客戶真正卡住的那個問題：**「那我現在能不能先拍板台股五頁？」**
⇒ **建議在 ★-14c 底下加一條子項 ★-14c-2**（或直接寫進交付報告第 1 段）：

> **★-14c-2｜四頁盤點擋不擋本批交付？**
> 選項：(a) 一起交　(b) 分批交
> ⭐ **推薦 (b) 分批交：台股五頁 ＋ 基金站 📊 資產配置先行拍板開工，四頁盤點另開一批。**
> 理由三條：① **兩站唯一耦合點是 Deep Link，而它已獨立為 ★-13b**，不依賴那四頁；
> ② **盤點是唯讀工作**，與台股實作可並行，不搶同一組人的寫入邊界
> （`PROCESS.md §3`「寫入嚴格序列化」不受影響）；
> ③ **拖著一起交，等於讓已經完整的五頁陪著等一件唯讀工作。**
> 不決定的後果：客戶看到一個 🔴 就整份壓著不拍，**五頁完整的規格白寫。**

### 一句話交給客戶

> **不是半成品，是「第一批」。** 拒絕虛構那四頁**是對的，必須守住**；
> 缺的不是勇氣也不是工時，是**沒有告訴客戶這個洞有多大、以及它擋不擋路**。
> 補上 ①（機械盤點表：行數／submit 數／葉數／expander 數）
> ②（明寫「擋不擋台股五頁開工」）
> ③（★-14c-2：分批交付的請示，附推薦）—— **三件事都不需要畫任何一張新線框**，
> 這份稿就可以當成一個**有邊界的分批交付**送審。

---

# §3 客戶三條拍板落地驗證

## 拍板一｜資料狀態逐項列名 —— ✅ **完全落地**

| 要求 | 判定 | 證據 |
|---|---|---|
| **逐項列名** | ✅ | `:238-253` §2.5.1 表，**十一列逐項列名**（① 正常～⑩ 錯誤 ＋ 橫跨全部的時點揭露），每列給 glyph／卡上中文／色／底層態／值級原因／大字數值區**六個欄位** |
| **⛔ 標題不得出現「8 種」「7 種」** | ✅ | 全稿 grep：`8 種`／`7 種`／`八種`／`七種` **0 命中**。另掃「N 種」全部命中（`:929`「七種按鈕文字」、`:969`「四種畫法」、`:1620`「三種命名」等）**逐條判讀，沒有一處是在概括資料狀態態數** |
| **「未載入」與「未接線」各有獨立且不同的說法** | ✅ **超額達成** | `:254-277` §2.5.2 專章，**不只兩個，是三個灰的並排**（③ 未載入 ⬜／⑧ 未接線 ⛔／⑨ 結構不適用 ▨），逐項給**七個維度**的差異：一句話／why／where／時間性／使用者該做／大字區／**實作原文出處** |

⭐ **本組認為這一條做得比拍板要求的更好的兩點**
1. `:270` 三者的 `where` 寫法是**結構性互斥**的：未載入 →「**指向按鈕**」；未接線 →「**明說沒有出口**——它永遠不會亮，重按一百次也一樣」；不適用 →「**明說不用做任何事**——這不是壞掉」。
   ⇒ 三句話**沒有任何一句可以套到另外兩格**，這正是拍板要的「說法必須互不相同」。
2. `:272-277` 追加了 `why`/`where` **世界觀一致性的三條強制檢查**，並附 2026-09-09 真實 bug 佐證
   （「同一張卡上兩句互相打臉，而且照 `where` 做的人會白按」）——**這是拍板沒要求、但抓到了真問題。**

## 拍板二｜第 6 欄 `target_pct` ＋ 「靜默擦掉」警語 —— ⚠️ **表訂 8 處，實際寫進線框 5 處**

### (a) 第 6 欄本身：✅ 落地
`:2702-2736` ★-04b 完整處理，且**正面攤開「客戶拍板與 `S1-5` 推薦相反」**（`:2704`），
援引 `CLAUDE.md §-1.5.E` 邊界 (c)「user 明示者不得被留住」判給客戶（`:2722`），
附四條理由 ＋ 誠實揭露殘留風險（`:2731` 舊版 app 部署期間視窗無解）。**這一段做得很好。**

### (b) ⭐ 派工要我「數一數是不是真的 8 處」—— **數完了：宣告 8 處，線框裡實際有 5 處。**

| # | §5.6 表列的落點（`:2744-2752`） | 線框裡**真的寫了嗎** | 行號 |
|---|---|---|---|
| **1** | ⑤-b 狀態 A（尚未設定）· 桌機 · 完整版 | ✅ **有** | `:2620-2623` |
| **2** | ⑤-b 狀態 B（已設定）· 桌機 · 完整版 | ✅ **有** | `:2661` |
| **3** | ⑤-b **狀態 C（總和≠100%）** · 桌機 · 完整版 | 🔴 **沒有** | `:2776-2800` 整個狀態 C 線框**無此行** |
| **4** | ⑤-b 全部三狀態 · 手機 <640 · 短版 | ✅ **有** | `:2814` |
| **5** | ⑤-b 全部三狀態 · **平板 641–1280** · 完整版 | 🔴 **沒有** | `:2804` 平板段只寫「表格保留／橫向捲動／不得縮字」，**未提 D-3** |
| **6** | ⑤-b **情境試算展開區頂端** · 短版 ＋「本試算用的是你這一輪填的值」 | 🔴 **沒有** | `:2667-2692` 情境試算線框**無此行**，也無那句加註 |
| **7** | **頁4 葉2 組合設定**的持股列預覽 · 完整版 ＋「這一欄不會寫回你的 Sheet」 | 🔴 **無法落地** | **葉2 全稿沒有線框**（見 §2.1） |
| **8** | ⑤-b 第四層展開佐證 · 技術原因完整版 | ✅ **有**（全文寫出） | `:2755-2767` |
| ＋ | （表外）頁4 3.4.3 有資料態 ⑤-b | ✅ **有** | `:2021` |

**⇒ 實際寫進線框：5 處（#1 #2 #4 #8 ＋ 表外的 3.4.3）。缺 3 處（#3 #5 #6），1 處無載體（#7）。**

### (c) 🔴 由此產生一句**不成立的全稱句**
`:2942` 逐字：「`target_pct` 保存｜…｜**D-3 警語，八處全部在位**（Part 5.6）」
—— 這一句出現在 **Part 8.2「實作前置」** 表裡，也就是會被實作組當成**已完成事實**讀。
**實測不成立**（缺 3 處、1 處無載體）。依 `CLAUDE.md §-2` 規則 6，這是一句
「未經驗證、卻寫進會被後人讀的記錄」的全稱句。

⚠️ **公允地說：本稿自己已經標了兩層防護**——
`:2769` 寫「本表是『應該標在哪』的規格，⛔ 不是『現況標了幾處』的盤點」；
`:2770-2772` 與 `:2971`、`:3110` 都標了「未窮舉 production」。
**但那兩層防護管的是「production 有沒有標」，不是「本稿的線框有沒有寫」。**
`:2942` 的「八處全部在位」講的正是後者，而後者**是可以在本文件內自驗的** —— 沒驗。

### (d) 另一處不一致
`:2088`（頁4 §3.4.8 揭露表）寫 D-3 的位置是「**葉1 ⑤-b**」；
`:2751`（§5.6 落點 #7）寫**葉2 也要**。**兩處對同一條揭露的落點規定不同。**

### (e) 建議（三件事）
1. **補 3 處**：狀態 C 線框（`:2800` 後）、平板段（`:2804`）、情境試算線框頂端（`:2670` 上方）
   各補一行 D-3（狀態 C 與情境試算用完整版、平板用完整版）。
2. **改 1 句**：`:2942` 的「八處全部在位」→「**八處落點已界定；本稿線框已落地五處，
   其餘三處待補、一處待葉2 線框（見 §5.6 表註）**」。
3. **對齊 1 處**：`:2088` 的 D-3 位置補上葉2，或 `:2751` 的 #7 改標「待葉2 線框」。

## 拍板三｜合規（禁用語表 ＋ D-1~D-10 落點表）

### (a) 「建議持股 X%」加進禁用語表：✅ **落地，且論證紮實**
`:792` 逐字列入；`:794-830` 給出完整判定理由（含六落點實測表 ＋ 唯一字串產生點
`shared/allocation_decision.py:222-226` ＋「改一個字串同時修好 6 個畫面」的 ROI 論證）。
⭐ `:1126-1128` 還主動否決了「換個說法（持股水位 70%）」這條捷徑，
理由寫得完全正確：「**違規的是那個百分比在第一層的位置，不是『建議』兩個字**」。
⚠️ **但這個正確的判準沒有被套用到頁4** —— 見 **F-1**；也沒有被套用到本頁自己的標題 —— 見 **F-4**。

### (b) ⭐ 派工要我「驗這 10 個落點是不是真的都寫了」—— **逐點驗完：7 ✅ / 3 ⚠️**

| # | 揭露 | 表列落點（`:853-862`） | 實際寫了嗎 | 行號 / 缺什麼 |
|---|---|---|---|---|
| **D-1** | 免責聲明 | 每一頁 · 頁尾 · 常駐 | ✅ **完整** | 全域 chrome `:648`/`:663` 定義 SSOT；線框實出 `:663`、`:717`、`:1189`、`:1464`；五頁 x.8 表各列一次（`:1390`/`:1592`/`:1822`/`:2091`/`:2273`）。⭐ 連**整頁錯誤版面**都照出（`:717`），這一點抓得很好 |
| **D-2** | 加權移動平均 vs FIFO | 頁4 葉1③ ＋ **頁4 葉2** ＋ **Part 5** ＋ 基金站📊 | ⚠️ **逐字文案 ✅，四個落點只落地兩個** | 全文 `:864-873` ✅；頁4 葉1③ `:2009` ✅；基金站 `:2442` ✅；**頁4 葉2 ❌**（無線框）；**Part 5 ❌**（5.2/5.3/5.4 三個線框都沒有） |
| **D-3** | 目標比例不會被保存 | 凡出現目標比例欄的每一處（8 處） | ⚠️ **5/8** | 見上方拍板二 (b) 逐處表 |
| **D-4** | 試算五條限制 | 情境試算展開後 · 第三層試算表正下方 | ✅ **完整** | `:2683-2689` 五條逐條寫出（未計手續費/未考慮最小交易單位/以某日收盤價計/**不是下單清單**/不考慮稅務），**與表列的五條一字對得上** |
| **D-5** | 未計入清單 | 頁4 葉1① ＋ Part 5 Partial · 有缺就出 | ✅ **完整**（⚠️ 未貼 D-5 標籤） | 頁4：`:2040-2041` 逐字「12 檔中 2 檔缺報價（2317、6488），已從分母排除並在下方單獨列出 —— ⛔ 不靜默剔除」；Part 5：`:2826` ⑥ 涵蓋不全列「缺報價那檔單獨列出，⛔ 不靜默剔除」。**內容到位，只是沒標「D-5」三個字** |
| **D-6** | 兩套刻度不可互比 | 頁4 葉1② · 第二層 · **常駐非摺疊** | ✅ **完整** | `:2001-2005` 線框內全文，且明標「**常駐揭露 · ⛔ 非摺疊**」；矩陣 `:2073` 在**九種呈現情況下全部寫「常駐」**（含錯誤態），這一列做得對 |
| **D-7** | 口徑揭露（面積不等於權重） | 頁2 葉2 · 第三層圖下方 caption · 常駐 | ✅ **完整** | `:1479-1481` 線框內全文：「X＝近 5 日累計淨流入（億）· Y＝動能變化 · 泡泡＝近 20 日淨額規模。**面積不等於權重。**」＋ `:1484` 常駐理由 ＋ 矩陣 `:1584` 九格全「常駐」 |
| **D-8** | Deep Link 現況 | 基金站📊 L3-b · 第四層 · 常駐 | ✅ **完整** | 佐證區觸發器 `:2445`；全文在 `:2503-2511`（兩段都壞的實測）＋ `:2533-2538`（不支援標的的誠實文案全文） |
| **D-9** | 同類型平均 ≠ 中位數 ＋ 約三成查不到 | 基金站📊 L2 · 第四層 · 常駐 | ✅ **完整** | `:2426-2433` 全文，含「**它是平均數不是中位數**」「錨定 MoneyDJ，⛔ 不是投信投顧公會、⛔ 不是晨星」「約三成查不到 → `⚠︎ —`」「**更新週期未取得**」四條 |
| **D-10** | 近一年報酬含息四層來源標示 | 基金站 **逐檔明細表每一列** · **第三層表內一欄** · 常駐 | ⚠️ **四層說明 ✅，但指定的那一欄不存在** | 四層定義 `:2435-2441` ✅ 寫得清楚（① MoneyDJ 官方含息 ② 自算還原含息 ③ 自算僅淨值不含息 ④ 取不到）。**但 `:2395` 的 L3 表頭是 `基金/佔比/年化配息/近一年/覆蓋率/同類型平均` —— 沒有任何一欄標示來源層級**，`:2397-2400` 四列示範資料也沒有。⇒ **「每一列」「表內一欄」這個落點沒有落地** |

**⇒ 十個落點：D-1 / D-4 / D-6 / D-7 / D-8 / D-9 ＝ 6 個完整；D-5 內容完整只缺標籤；
D-2 / D-3 / D-10 ＝ 3 個落點未完全落地。**

**建議（三處，都很小）**
- **D-2**：Part 5 的三個線框（`:2620` 附近／`:2661` 附近／`:2683-2689` 五條限制內）擇一補
  「▸ 這裡的『成本』怎麼算的（全文見 2.12.3）」；頁4 葉2 待該葉線框補上時一併。
- **D-3**：見拍板二 (e)。
- **D-10**：`:2395` 表頭在「近一年」後加一欄 `來源`，`:2397-2400` 四列示範填 `①`/`②`/`③`/`⚠︎ —`，
  並在表下加一行 `來源 ①②③ 的定義見〔展開佐證 ▸〕`。**這樣「表內一欄」才算落地。**

---

# §4 我查過、確認沒問題的區塊（明列）

> 下列各項是本組**本次逐行讀過並實際比對**的，**不是**「掃過沒看到問題」。
> ⚠️ 仍屬**單組判讀，未經第二組複驗**。

## 合規面（§1 射程內）確認乾淨的

| # | 區塊 | 行號 | 我用什麼方法確認 |
|---|---|---|---|
| 1 | ⭐ **Part 5 再平衡全段（三狀態＋情境試算＋三禁令）** | `:2553-2846` | 逐框讀 ＋ 定向比對派工點名的四種指令形狀（動作欄／建議股數／買進 N 股／一鍵按鈕）→ **四種全部 0 命中**。表頭 `:2639`（狀態B）、`:2675`（情境試算）逐欄核對 |
| 2 | ⭐ **頁1 ★-10 兩候選的文案本身** | `:1069-1090`、`:1112` | 對候選甲跑 (a)(b)(c) 三道檢查：無動詞 ✅／非同義詞替換（謂語從「該做什麼」換成「狀態是什麼」）✅／遮蔽後「總經健康分 68 落在偏多區間」門檻在字面上 ✅。**三道全過** |
| 3 | **🔬 查一檔 三格判決卡與 ETF 分支卡** | `:1695-1697`、`:1732-1734` | 定向查派工點名的「估值過高，建議逢高減碼」→ `逢高`／`過高` **全稿 0 命中**。實際文案是狀態描述 |
| 4 | **🔬 查一檔 💰 獲利能力診斷的缺值三律** | `:1707-1721` | 讀該段對「⚪ 資料缺漏 ⛔ 不是紅的『辛苦生意』」的處理 —— 這是**把「沒資料」講成「爛生意」**的修正，方向正確 |
| 5 | **🔍 找標的 空狀態／錯誤態** | `:1553-1575` | 「符合條件的標的是 0 檔｜**這是一個有效的結果**」＋ C-1/C-2 兩種錯誤分流。無任何行動指令 |
| 6 | **基金站 收益視角摘要卡** | `:2380-2388` | `:2388` 主動寫「→ **客觀對照**用語，⛔ 不寫『應該贖回』『建議換檔』」——**本組認為這是全稿合規意識最好的一處** |
| 7 | **基金站 Deep Link 不支援標的文案** | `:2533-2538` | 走「⑧ 未接線」語氣（明說沒有出口），無行動指令，且誠實說「這不是連結壞了，是那邊真的沒有這一檔」 |
| 8 | **CSV 下載第一列揭露** | `:2886` | `# 資料時點：… · 僅供學術研究，非投資建議` —— 匯出檔也帶免責，這一條想得很周到 |
| 9 | **§2.12.1 禁用語表本身 ＋ 六落點論證** | `:789-830` | 逐條核對 `COMPLIANCE_SCAN_B:365-379` 原文，**六個落點 a~f 一字不差、[v1]/[v2] 標記正確**、`headline()` 程式碼片段與原檔一致 |
| 10 | **§2.12.2 六種替代表述的內容** | `:833-841` | 六列逐條跑缺項填補測試 —— **六種改寫本身都過得了**（問題只在標題那句全稱句，見 F-6） |

## 完整性面確認到位的

| # | 區塊 | 行號 | 確認了什麼 |
|---|---|---|---|
| 11 | **五頁 × 九節模板** | `:1010-2287` | **50 個小節全部存在、全部有實質內容**，逐節開啟確認無「本節待補」、無留白、無「同上」 |
| 12 | **七張 x.7 逐區塊 × 呈現情況矩陣** | `:1371-1381`、`:1579-1585`、`:1804-1813`、`:2070-2081`、`:2258-2264`、`:2465-2471`、`:2820-2833` | 逐張數欄位、逐格確認有字。**沒有一張留空格** |
| 13 | **資料時點揭露（2.7 全節）** | `:396-467` | 含「拿不到時點時怎麼寫」與「🔴 拿不到歸屬日的來源清單」—— 這兩塊是最容易造假的地方，本稿正面處理了 |
| 14 | **§2.5.2 三個灰的差異表** | `:254-271` | 七維度逐格確認三欄互不相同（見拍板一） |
| 15 | **§2.6 數值三分決策樹 ＋ 判錯實例** | `:320-395` | `0.00` / `N/A` / `—`＋⚠︎ 三分法有決策樹、有 E-1~E-9 判錯實例、有「上游回傳字串 `"N/A"` ⛔ 不得直接當呈現層 `N/A`」 |
| 16 | **§2.10.4 頁級三版面** | `:670-732` | 載入中／整頁錯誤／空狀態三段皆有線框或表；⭐ 且誠實標明「**本節整節是要新建的東西**」＋ `st.empty()` 實作前置 |
| 17 | **§2.11 跨頁動線與返回** | `:734-786` | 兩種下一步都做、⛔ 不做麵包屑有理由、「凡指向舊七頁籤必須寫『先切回舊版』」（`:758`）—— 這一條解的是一個真實的動線死洞 |
| 18 | **§2.9 表格轉卡片流七條規則** | `:542-553` | 含最容易被忘的兩條（數值仍等寬右對齊／缺值三種寫法一字不變照搬） |
| 19 | **Part 7 待拍板清單格式** | `:2893-2926` | **機械驗證**：`awk` 抽出 `^\| \*\*★-` 起頭列 → **25 列**；再濾掉含 `⭐`（推薦標記）者 → **0 列**。⇒ **25 條 ★ 全部附總管推薦，無一例外**，符合 `:2895` 自訂格式與 `CLAUDE.md §-1.5.D §03-2` 請示規範。⚠️ 本組初稿曾誤判 ★-14c 缺推薦，已於基金站專章 ③ 更正並留下紀錄 |
| 20 | **Part 8.3 未驗事項自標** | `:2955-2992` | 四個分區（結構性驗不到／全稱句自標／承重判斷／沒做的事）＋ 附錄 C 三張表。**本稿的誠實揭露密度在本組讀過的材料裡是最高的**，這一點必須講 |

---

# §5 我沒查到的（範圍聲明）

> ⚠️ 依 `CLAUDE.md §-2` 規則 6 據實列出。**下列每一項都不得被當成「已查過沒問題」。**

## 5.1 職責邊界外（刻意不查，屬 A/B 組）

1. **「會不會炸」** —— 本稿宣稱的實作前置（`Card.value` 守衛、`action_label()` raise、
   `single_submit_form()` 無 `disabled`、`LEVEL_STYLES` fail loud、`st.empty()` 需求）
   本組**一律未驗**。屬 **A 組**。
2. **「有沒有發明畫面上不存在的東西」** —— 全稿的 `檔名:行號` 引用（附錄 A 約 40 列）、
   按鈕名、葉名、卡名、常數名，本組**只驗了一個**（F-3 的 `flag_take_profit`，因為它是本組判定的承重支撐）。
   其餘**一律未回頭比對 production**。屬 **B 組**。

## 5.2 本組射程內、但確實沒查的

3. 🔴 **「除 F-1~F-8 外沒有其他違規句」本組不主張。** 掃描方法與已知盲點見 §1 末段
   「未複驗聲明」。**本組用的是關鍵字 + 逐框人工讀，對「零關鍵字的偽陰性」沒有結構性保證。**
4. **上游四份合規材料（`COMPLIANCE_SCAN_A/C`、`COMPLIANCE_BLAST_D`、`COMPLIANCE_CONTROLFLOW_E`）沒讀。**
   本組只讀了 `S1-6`（判準）＋ `COMPLIANCE_SCAN_B`（因為本稿引用它）。
   ⇒ **若 A / C / D / E 裡另有本稿該吸收而未吸收的違規類型，本組掃不到。**
   ⚠️ **這一條特別值得補**：本稿 `:34` 的引用鏈顯示它只吸收了 B 組，
   而 `S1-6:34` 的材料表寫「丙 C `COMPLIANCE_SCAN_C.md` —— 法遵界線（L0~L4 量尺、**缺項填補測試**、替代語庫）」
   ⇒ **本稿漏掉的正是丙組那一份**，這與本報告開頭的根因判定互相印證。
5. **前四份紅隊報告（`S1-3B_REDTEAM.md`、`S1-4_REDTEAM.md`、`DRAFT_GAP_AUDIT.md`、
   `STATE_SEMANTICS_QA2.md`）沒讀。** ⇒ **本報告的發現可能與它們部分重複，本組未去重。**
6. **`S1-3`／`S1-4`／`UI-B`／`UI-C`／`UI-E`／`M-1 核准線框` 六份上游沒讀。**
   ⇒ 附錄 B 的 18 項「偏離標示」**是否漏標**，本組**完全沒驗**。
   （本組只驗了附錄 B **內部**與線框的一致性，抓到 F-2。）
7. **§2.13 新手化用語字典的來源（`UI-C:186-209`、`UI-E:130`）沒查。**
   本組只對該表**已寫出的兩列改寫**跑了 (a) 動詞檢查（見 F-5 附帶發現）。
   ⇒ **該表其餘各列是否也有同樣的「改寫後仍是指令」問題，未逐列跑。**
8. **五頁的頁2「🔍 找標的」與頁5「📖 憑什麼」的第四層佐證內文沒有逐句跑三道檢查。**
   本組對這兩頁是**通讀**而非**逐句判讀**（時間分配在頁1/頁3/頁4/Part 5 這四塊風險最高的）。
9. **基金站 4.0 五態模型與台股站十態的對映沒有逐格驗。** `:2331-2357` 讀過，未逐格比對。
10. **響應式的 265px 那一格，本組只做了算術比對（265 < 274），沒有實際量測任何畫面。**
    ⇒ 「881px 三欄會不會真的折行」屬 A 組／實作驗證，本組只指出**本稿自己的兩個數字互相矛盾**。

## 5.3 ⭐ 建議優先覆核的三條（本組自己的承重判斷）

| # | 判斷 | 為什麼承重 |
|---|---|---|
| **1** | **F-3 的「你設定的停利門檻」是事實錯誤** | 本組讀了 `dividend_station_service.py:938-953` 一處就下判斷。若該門檻另有本組沒看到的使用者設定路徑，F-3 的第三層就不成立（前兩層仍成立）。**請 B 組或實作組覆核一次。** |
| **2** | **F-1 的 `加碼 3` 是違規** | 這條會導致頁4 ① 卡重畫。若客戶認為「加碼/減碼」作為**燈號狀態名**（而非動作）可接受，本組的判定會被推翻。⚠️ **本組認為不可接受**，理由是遮蔽檢查不過；但**這是量尺線位置的問題，而 `S1-6 §1.3` 自陳線的位置待客戶確認。** |
| **3** | **「基金站四頁不算半成品」** | 這是一條**會被拿去當交付決策前提**的判斷。本組是讀完 `:2477-2500` 與 `S1-3 §7.2` 的自陳後下的，**沒有實地看過那四頁**（正是本稿拒絕虛構的同一個理由，在本組身上同樣成立）。 |

---

## 📌 唯讀紀律驗證

- ✅ **未修改** repo 任何檔案（本次唯一觸及 repo 的是 `grep` 與 `sed -n` 讀取
  `src/services/dividend_station_service.py`，**只讀不寫**）
- ✅ **未改寫**被稽核文件 `S1-3B_FULL_DRAFT_SPEC.md`（本報告所有引用皆為轉錄）
- ✅ **未 git 寫入**（無 commit / add / push / branch）
- ✅ 產出僅本檔一個：`scratchpad/S1-3B_REDTEAM_C.md`
