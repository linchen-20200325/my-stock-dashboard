# 紅隊 B 組稽核報告 —— 只查「發明」與「漏標的偏離」

| | |
|---|---|
| **稽核對象** | `/tmp/.../scratchpad/S1-3B_FULL_DRAFT_SPEC.md`（3,157 行） |
| **權威基準** | `/home/user/my-stock-dashboard/docs/wireframes/stock_ia_v1.html`（客戶 2026-09-05 逐字核准，下稱 **M-1**）＋ production code |
| **方法** | ⛔ **全程不採信被稽核文件的任何轉述**。M-1 以 `node` 反序列化 `PAGES` 陣列取出結構；code 一律 `sed -n` 讀原行 |
| **職責邊界** | 只查 §1 發明／§2 漏標偏離／§3 B-01 裁決。**不做 A 組（會不會當場炸掉）、不做 C 組（稿內違規句與完整性）** |
| **唯讀紀律** | 本組未修改 repo 任何檔案、未改寫被稽核文件、未做任何 git 寫入。只新增本報告一檔 |

## 基準事實（本組實測，非轉述）

反序列化 `stock_ia_v1.html` 的 `PAGES`：**5 頁、30 個 block**
（29 個頁內 block ＋ 1 條「（跨頁）頂部狀態列」，該條登記在 `PAGES[0].blocks[6]`）。
逐頁：頁1 = 7（含跨頁列）、頁2 = 4、頁3 = 7、頁4 = 8、頁5 = 4。
⇒ 該文件 §2.1 寫的「**五頁、十一葉、二十九區塊 ＋ 一條跨頁頂部狀態列**」**與 M-1 一致**。✅

---

# §1 發明清單

> 判準：該文件畫出來的區塊／按鈕／欄位／動線，在 **M-1 或 production code** 找不到，**而且沒有標「新增／★待拍板／偏離」**。

## 🔴 INV-1｜CSV 下載鈕的 label 是編的 —— M-1 沒有、code 也不是這個字

| | |
|---|---|
| **該文件畫的** | `〔 ⬇ 下載結果 CSV 〕` —— 出現 **3 次**：`S1-3B_FULL_DRAFT_SPEC.md:1454`、`:1513`、`:1557` |
| **M-1 怎麼寫** | 只寫「CSV」，**沒有給 label**（`[2.2] n` ＝「總覽卡 ＋ dataframe ＋ CSV」；`[2.1] note` ＝「CSV `download_button` 留在 form 外」） |
| **code 怎麼寫** | `/home/user/my-stock-dashboard/src/ui/views/page_find.py:1603`：<br>`st.download_button("💾 下載選股結果 CSV", data=_csv, file_name="screener_result.csv", ...)` |
| **差在哪** | glyph `💾`→`⬇`；字 `下載選股結果 CSV`→`下載結果 CSV`。**實測：`💾` 在該文件全檔 0 命中** |
| **有沒有標** | ❌ **沒有**。附錄 B 全表無此項；`§6.3` 只把「CSV **全站規則**」標成新訂（B-16），**沒有提 label 與 code 不一致** |

**為什麼這條要算**：該文件自己的 **C-02 定案** 寫「**五頁主 CTA 一律回到 code，⛔ 不准發明**……找不到的就誠實寫『本頁目前沒有主 CTA』」，並逐頁列出 `S1-3` 發明的 4 顆假按鈕當反例。CSV 鈕不是主 CTA，**字面上不受 C-02 拘束** —— 但它是一顆會被實作照著做出來的畫面控制項，而它的名字**兩個權威來源都查無**。這正是 C-02 要防的那一類錯，只是掉在 C-02 射程外一格。

**連帶（同一處，一併記錄）**：`§6.3` 訂的檔名規則是 `〈頁名〉_〈區塊名〉_〈資料歸屬日〉.csv`（例 `找標的_選股結果_2026-09-13.csv`），而 code 現行是 `file_name="screener_result.csv"`。規則本身已由 B-16 標為新訂 ✅，**但「現行檔名長什麼樣、要改」這件事沒寫**。

## 🔴 INV-2｜「展開佐證 ▸」這顆觸發器，M-1 **0 命中**，卻沒有列進附錄 B-3「新增」

| | |
|---|---|
| **實測** | `grep -c '展開佐證' docs/wireframes/stock_ia_v1.html` → **0**。`grep -c '佐證' …` → **1**，且那一筆是頁2 板塊地圖的 `r:` 欄「並排才有互相**佐證**的價值」，與觸發器無關 |
| **該文件畫的** | 「展開佐證 ▸」在該文件出現 **46 次**，並在 `§2.4` 訂成一顆具體控制項：次級**文字**按鈕、固定文字「展開佐證 ▸」／「收合佐證 ▾」、位置在**區塊標題列最右側**、行動裝置點擊區 ≥ 44×44 CSS px。五頁的每一張線框圖都把它畫在區塊標題右邊 |
| **出處自陳** | `§2.4`：「觸發器規格（**沿用 `S1-3:68-84`**，逐條寫死）」—— 而 `S1-3` 在該文件自己的 `§0.3` 材料輩分表是**第 6 序、「參考，不是權威」** |
| **有沒有標** | ❌ **沒有**。附錄 **B-3 類｜新增（核准線框完全沒有的東西）** 共 8 項（B-11~B-18），**沒有一項是四層骨架或展開佐證觸發器** |

**B-18 反而讓這個洞更不容易被看見**：B-18 登記的是「**葉（第 0 層）**」，理由寫「核准線框的 `leaves` 只是一個字串陣列，**沒有規定它與四層的關係**」—— 這句話**預設四層已經是既有的東西**。實際上 `四層` 在 M-1 同樣 **0 命中**，`§1.2 既有拍板` 六條也沒有任何一條是四層骨架或展開佐證。**結果是：真正新增的那一層（四層 ＋ 觸發器）沒登記，只登記了掛在它上面的第 0 層。**

⚠️ **同一處還有一句 mis-attribution**（詳見 §2 的 DEV-19）：`§2.4` 與 `★-02` 都寫「**M-1 核准的就是區塊級**（「點左表右側就地展開」）」。M-1 那句話描述的是**點列／點檔 → 右側展開**的 master-detail 手勢，**不是**一顆叫「展開佐證 ▸」的標題列按鈕。拿它當「M-1 已核准展開佐證」的依據，是把一個**沒被核准的控制項**掛到核准線框名下。

## ⚠️ INV-3｜`§3.5.0` 說「舊線框的兩個錯，本稿一併更正」—— 但 **M-1 那一格沒有錯**

- 該文件 `§3.5.0` 與 C-02 表寫：「**舊線框**把它們混成一個、而且只寫了摺疊標題那一步的一半」「(a) 把摺疊標題**截短了後半句**」。
- **實測 M-1 `[5.3]`**：`n` ＝「摺疊 ［ 🔧 進階診斷（工程師用；**一般使用者不需要打開**） ］ → 內含 1 個 checkbox 全有全無 · 不加 form」，`live` 兩步都畫了、`grey.where` 也寫「展開「…」→ 勾選裡面的「…」」。**M-1 一字未截短、兩步都在。**
- **真正截短的是 M-1 的前一版**：`git show 1e76101` 的 diff 顯示該修正**已經發生在核准之前**，M-1 自己的 `note` 也逐字記著「線框**上一輪**截短了後半句」。
- **判定**：⚠️ **不是發明**（內容全對、與 M-1 一致），是**措辭把已修好的事講成待修**。「舊線框」若指 `線框 v1`（核准前那版）則為真；若讀成 M-1 則為假，而 `§0.3` 把 M-1 就叫「線框」，**極易誤讀成核准基準有兩個錯**。建議改寫成「線框 v1（核准前）」。

## ✅ 主 CTA：五頁逐條回 code 驗過，**一條都沒有發明**

> 這是本組奉命重點查的一項。**每一條都自己 `sed -n` 讀原行**，不採信該文件轉述。

| 頁 | 該文件宣稱 | 本組實測 | 判定 |
|---|---|---|---|
| 🚦 今天 | 「🚀 更新今日戰情」＠`shared/ia_nav.py:83` | `awk 'NR==83' shared/ia_nav.py` → `    ACTION_UPDATE_TODAY: "🚀 更新今日戰情",` | ✅ **逐字命中** |
| 🔍 找標的 | 「🎯 開始選股」＠`page_find.py:422`／「🗺️ 載入板塊地圖」＠`:423` | `:422` → `ACTION_RUN_SCREEN_LABEL: str = "🎯 開始選股"`；`:423` → `ACTION_LOAD_MAP_LABEL: str = "🗺️ 載入板塊地圖"` | ✅ **兩條逐字命中** |
| 🔬 查一檔 | 「🔍 載入完整分析」＠`page_inspect.py:348`／「🚀 批次分析」＠`:349` | `:348` → `ACTION_LOAD_INSPECT_LABEL: str = "🔍 載入完整分析"`；`:349` → `ACTION_RUN_BATCH_LABEL: str = "🚀 批次分析"` | ✅ **兩條逐字命中** |
| 💼 我的持股 | 「🚀 跑存股戰情室」＠`page_hold.py:388` | `:388` → `ACTION_RUN_WARROOM_LABEL: str = "🚀 跑存股戰情室"` | ✅ **逐字命中** |
| 📖 憑什麼 | 「**本頁目前沒有主 CTA**」，自稱 `grep -c form_submit_button page_why.py` → 0 | 本組實跑 → **0** | ✅ **成立** |

**頁5 的兩個控制項也逐一驗過（前一輪抓到錯的那一格）**：

| 該文件寫的 | 本組實測 | 判定 |
|---|---|---|
| `page_why.py:2119` expander「🔧 進階診斷（工程師用；一般使用者不需要打開）」 | `:2119` ＝ `with st.expander(ENGINEER_EXPANDER_LABEL, ...)`；`:337` ＝ `ENGINEER_EXPANDER_LABEL: str = "🔧 進階診斷（工程師用；一般使用者不需要打開）"` | ✅ **行號與字串都對，後半句沒被截短** |
| `:2121` checkbox「載入進階診斷（較耗時，部分項目會實際打外部 API）」 | `:2121` ＝ `st.checkbox(ENGINEER_CHECKBOX_LABEL, ...)`；`:338` ＝ 該字串逐字 | ✅ |
| `:2169` `st.chat_input("例如：2330 現在健康度多少？")` | `:2169` ＝ `st.chat_input(QA_PLACEHOLDER, ...)`；`:343` ＝ `QA_PLACEHOLDER: str = "例如：2330 現在健康度多少？"` | ✅ |
| 「Streamlit 明文禁止把 `chat_input` 放進 `st.form`（`:174-175`）」 | `:174-175` 逐字寫著該限制 | ✅ |

⇒ **「兩步、兩個不同 label」該文件寫對了**，指路句「展開「…」→ 勾選裡面的「…」」與 M-1、與 code 三方一致。

⚠️ **一個沒被說破的事實**：`grep -c form_submit_button src/ui/views/page_today.py` → **0**。頁1 的 submit 不在 `page_today.py` 裡，而是 `page_today.py:1813` 把 label 傳給 `single_submit_form()`，真正的 `st.form_submit_button` 在 `src/ui/views/_ui_kit.py:414`。**這不影響該文件的宣稱成立**（它只宣稱 label 出自 `ia_nav.py:83`，屬實），列出只為讓後續稽核不要用同一條 grep 誤判頁1「也沒有主 CTA」。

---

# §2 漏標的偏離（附錄 B 已列 18 項；以下是**它沒列進去**的）

> 方法：把 M-1 `PAGES[].blocks[]` 30 塊逐塊拉出來（`t` / `n` / 五態 / `cells` / `note`），與該文件逐塊比對**清單本身**，⛔ 不看它的說法。

## 🔴 DEV-19｜**下鑽手勢被換掉了** —— §2.4 明文禁止 M-1 明文寫的東西

**M-1 三處逐字寫「點列／點檔 → 就地展開」**（本組反序列化取出）：

| M-1 位置 | 原文 |
|---|---|
| `[1.6]` 頁1 葉2 指標明細 · `live` | 「每段一張 3 欄卡牆 ＋ 可下鑽的逐盞燈明細（**點左表右側就地展開**）」 |
| `[3.7]` 頁3 葉2 多檔比較 · `live` | 「批次評分表 → **點任一列就地展開該檔明細**（依型別走 A 或 B）」 |
| `[4.3]` 頁4 ③ 戰情表 · `n` ＋ `live` | `n`＝「3 欄燈格 · **點左表右側就地展開**」；`live`＝「12 檔 × 235 加碼燈 / 3-3-3 判定 —— **點任一檔，右側展開**週線與布林 z」 |

**該文件 `§2.4` 的觸發器規格表（`:206`）**：

> \| **⛔ 禁止** \| **卡片本身可點**、**整列 `<tr>` 可點**、標題可點、hover 自動展開、卡片右上角小箭頭 \|

**⇒ `§2.4` 的禁令與 M-1 `[3.7]`／`[4.3]` 的 `live` 原文指令相反。**

**落地後果，逐頁實測**：

| 頁 | 該文件怎麼處理 | 行號 |
|---|---|---|
| 頁1 葉2 | **照抄 M-1**，仍寫「（點左表右側就地展開）」 | `:1294` |
| 頁3 葉2 | **同一格內自相矛盾**：上一行照抄 M-1「**點任一列就地展開該檔明細**」，下一行立刻寫「**⛔ 整列不可點（2.4）**；下鑽走每列最右的〔展開佐證 ▸〕」 | `:1762` ＋ `:1763` |
| 頁4 ③ | **改掉 M-1**：M-1 的「點任一檔，右側展開週線與布林 z」→ 改成「點任一檔右側〔**展開佐證 ▸**〕」 | `:2007` |
| 頁4 ③ 說明文 | `§2.13` 用語字典把 M-1 的「3 欄燈格 · **點左表右側就地展開**。燈牆與 VIX 各自判態…」整句換成「每一檔一列燈。VIX 讀不到不會影響燈牆」—— **下鑽手勢那半句直接消失** | `:914` |

**有沒有標**：❌ **附錄 B 全表 0 命中**（`整列`／`任一列`／`展開佐證`／`可點` 在 `:3045-3100` 區間皆無）。全稿只有兩處提到 `整列`，一處是禁令本身（`:206`）、一處是套用它（`:1763`）。

**⚠️ 而且它被寫成「M-1 已經核准」**：`§2.4` 理由 1 與 `★-02` 都寫「**M-1 已經核准的是區塊級**……M-1 頁1 葉2、頁4 ③ 的 note 寫的都是『點左表右側就地展開』」。這句話**只在「顆粒度」那個軸上成立**（一個區塊一個展開區）；在「**觸發器是什麼**」那個軸上，M-1 寫的是 row-click、該文件禁的就是 row-click。**兩個軸被合併成一句話，結果是一個未經核准的控制項被記成核准項。**

**建議**：立 **B-19**，歸 **B-2 類（政策變更，拍板前不得動工）** —— 它改的是使用者**手上的動作**，屬 v3 §03-2 ① 的「分頁動線／互動異動」。並把 `:1762` 與 `:1763` 的自相矛盾收掉（該歸 C 組，一併轉知）。

## 🔴 DEV-20｜頁3 💰 獲利能力診斷：M-1 的 **5 格（3 欄 × 2 排）→ 該文件變 3 格**，兩格不見了

| | |
|---|---|
| **M-1 `[3.4]` `n` 逐字** | 「原 `tab_stock.py:1230` **`st.columns(5)` → 3 欄 × 2 排**」 ← **5 個指標排成兩排**，不是 3 個 |
| **production code** | `src/ui/tabs/tab_stock.py:1229` ＝ `st.markdown('#### 💰 獲利能力診斷（**5大指標**）')`；`:1230` ＝ `_p5c = st.columns(5)`，五格 label 依序為 **毛利率／營業利益率／安全邊際／稅後淨利率／ROE**（`_p5c[0]`~`_p5c[4]`） |
| **新 code 自己承認缺口** | `src/ui/views/page_inspect.py:49-50` 逐字：「💰 獲利能力診斷原 `tab_stock.py` 的 `st.columns(5)` → **3 欄 × 2 排**（本頁**只畫線框列出的 3 格**，**第 2 排留給後續補齊的淨利率 / ROE**）」；`:2011` docstring 亦寫「的**三格**」 |
| **該文件怎麼寫** | `§3.3.3`（`:1710-1716`）只畫 **3 格**；`§3.3.7` 矩陣（`:1810`）寫「**三格**真值／**三格** ⬜」 |
| **實測缺漏** | `grep '淨利率\|ROE'` 在該文件 → **0 命中**；`grep '2 排'` → **僅 1 命中**（`:1768`，講的是 **6 個 MA checkbox**，不是這一格） |
| **有沒有標** | ❌ 附錄 B 無此項；`§3.3.9 本頁已知做不到的事` 三列也沒有它；`§2.1` 的 ⚑ 標記**沒有**標在這一格 |

**為什麼這條要算**：M-1 的 `n` 是**規格欄**，「3 欄 × 2 排」六個字就是「**5 個指標、排兩排**」的意思 —— 把它做成一排 3 個，是**少做兩個指標**，不是版面重排。而且 **code 自己已經把這個缺口寫成待補事項**（「第 2 排留給後續補齊的淨利率 / ROE」），該文件是在它上面**又蓋掉一層**：既沒畫第 2 排，也沒把它列進「本頁已知做不到的事」。⇒ 後人讀這份規格會以為 💰 這一格**規格就是 3 格**，那兩個指標會**無聲消失**。

**建議**：立 **B-20**。歸類看客戶意向：若要補回 → **B-1 類（事實更正）**＋寫進 `§3.3.9`；若確定砍掉兩個指標 → **B-2 類（政策變更）**，因為那是**功能減項**。

## 🟠 DEV-21｜`§2.1` 自己標了 ⚑ 的「③ 三欄摘要」，附錄 B **沒有對應條目**

- `§2.1`（`:112`）頁1 的區塊欄寫：「① 結論燈 **⚑**／② 操作列／③ 三欄摘要 **⚑**／④ 今日關鍵橫幅／⑤⑥ …／葉2 指標明細 **⚑**」，並在表下定義「**⚑ ＝ 本稿對該區塊有偏離或補充，逐項見附錄 B**」。
- 附錄 B 開頭（`:3050`）再次宣告「**本表即本稿全部 ⚑ 的集中地**」。
- **實測**：`① 結論燈 ⚑` → B-09 ✅；`葉2 指標明細 ⚑` → B-01 ✅；頁4 `④ 換股建議 ⚑` → B-03 ✅；
  **`③ 三欄摘要 ⚑` → 附錄 B 區間（`:3045-3100`）`三欄`／`③` 皆 0 命中，查無條目。**
- ⇒ 這是**該文件依自己的規則自我宣告、卻沒有兌現的一個洞**。⚠️ 本組**沒有查出**這個 ⚑ 原本想指的是哪一項偏離（可能是 `§3.1.1` 的「大字區留空（★-03）」，也可能是 code `page_today.py:631-635` 記載的「③ 曾被五桶摘要換掉、2026-09-07 改回」那段歷史）—— **本組只確認「⚑ 沒有對應條目」這個事實，不替它推斷內容。**

## 🔴 DEV-22｜**跨頁頂部狀態列的「· 建議 70%」被拿掉了** —— B-09 只登記了頁1 ①，沒登記這一條

| | |
|---|---|
| **M-1 `[1.7]` `live` 逐字**（`stock_ia_v1.html:776`） | `09/05 週五 ✅ 交易日　\|　🟢 總經 偏多 **· 建議 70%**　\|　🔗 已綁 Sheet「我的持股」　\|　更新於 14:32` |
| **該文件怎麼寫** | **「· 建議 70%」整段消失**，五處一致：`:571`（`§2.10.1` ① 正常）／`:1244`（頁1 有資料）／`:1379`（`§3.1.7` 矩陣）／`:1645`（頁3）／`:1903`（頁4） |
| **有沒有標** | ❌ **沒有**。**B-09 登記的是 M-1 `:741`**（＝ `blocks[0]` 葉1 ① 結論燈那一塊的標題行），**不是 `:776`**（＝ `blocks[6]` 跨頁頂部狀態列）。兩者在 M-1 是**兩個獨立的 block**，各自帶一份「建議 %」字樣 |
| **也不在合規落點表裡** | `§2.12` 列的六個落點（a `app.py:1174-1178`／b `section_cross_ai.py:161-183`／c `etf_tab_dividend_station.py:590-591`／d `page_today.py:1356-1359`／e `page_hold.py:2101+`／f `macro_stock_link.py:37`）**沒有一項是跨頁頂部狀態列** —— 合理，因為那六項是**既有 code 掃出來的**，而狀態列是 **M-1 才有的規格新元件**，掃 code 掃不到它 |
| **也不是 ★-07b** | ★-07b 管的是**舊路徑的全域置底常駐條**（`app.py:1174-1178`），與 M-1 的**頂部**狀態列是兩個東西 |

**判定**：⚠️ **改動本身是對的**（拍板三：全站不產生直接買賣建議），但**沒有登記**。
**為什麼要算**：① 母法寫「凡偏離核准線框處，**必須逐項標示**」，而同性質的兩件事（B-03 換股建議→換股對照、B-09 頁1 主數值）**都登記了**，獨漏這一條；② 它是**全站唯一五頁都看得到的元件**，M-1 上那行字是客戶逐字核准過的；③ B-09 的自我定位是「本稿**最大**的一項偏離」並宣稱該字串有 6 落點，**讀者會以為 B-09 已經把「建議 %」這件事收乾淨了** —— 實際上 M-1 上還有第二處，而它被無聲刪掉。

**建議**：立 **B-22**，與 B-09 併列於 **B-2 類**（或作為 B-09 的第二列），寫明「M-1 `:776` 狀態列的『· 建議 70%』一併移除，替代寫法比照 ★-10 候選甲」。

## ⚠️ DEV-23｜附錄 B 的**分類欄位**與內容不符（B-08 放錯類）

- **B-08 被放在「B-1 類｜事實更正」**，該表第一欄的欄頭是「**核准線框（M-1）**」。
- 但 B-08 那一格寫的是「（頁5 **門檻表現況**）一張表、18 列、標題「總經燈（18 盞）」」—— 那是**現行實作**，不是 M-1。
- **實測 M-1 `[5.2]`**：`n` ＝「**3 欄燈號卡牆**（原 columns(7) 降階）」，`cells` ＝ FRED／FinMind／TWSE（**資料源卡**）。**M-1 頁5 葉2 根本沒有「18 列門檻表」這個東西。**
- **判定**：⚠️ **不是漏標的偏離**（B-08 要做的事有登記），是**登記欄位填錯** —— 讀者會以為 M-1 寫了「18 盞」。**建議把第一欄改標「現況（production）」或移出 B-1 類。**

## ✅ B-4 那三處「我沒有偏離、但容易被誤讀成偏離」—— 本組逐條驗過

> 這是本組奉命驗證的一項。**結論：三條都成立，確實沒有偏離。**

| B-4 # | 宣稱 | 本組實測 | 判定 |
|---|---|---|---|
| **1** | 頁1 ② 操作列 ＝ radio ＋ 單一 submit，「完全照抄」 | M-1 `[1.2]`：`n`＝「`st.form(form_today)` · 單一 submit」，`live`＝「`◉ 正常更新（吃暖快取）／○ 強制重抓（清快取）` radio → **一顆** `form_submit_button`：「🚀 更新今日戰情」」。該文件 `:1162-1165` ＝「◉ 正常更新（**吃暖快取**）　○ 強制重抓（**清快取**）／▌ 🚀 更新今日戰情 ▐」 | ✅ **照抄成立**，連括號內的「吃暖快取／清快取」都在 |
| **2** | 頁1 ① 冷啟動「完全照抄，⛔ 一字不改」 | M-1 `[1.1].grey.now` ＝「⬜ **市場位階 · 尚未評估**　訊號可信度 **0／16** **個指標正常運作中**（無資料 15 · 未接線 1）」。該文件正文 `:1154-1155` ＝「⬜ **市場位階 · 尚未評估**／訊號可信度 **0／16** **個指標正常運作中**（無資料 15 · 未接線 1）」 | ✅ **正文逐字成立**。⚠️ 唯一瑕疵：**B-4 表格那一格的摘錄**把「個指標正常運作中」7 字省掉了，與它自己「一字不改」的宣稱不符 —— **正文沒錯，錯在附錄的引文**（屬 C 組射程，此處僅記錄） |
| **3** | 頁3 葉1-C `unknown` 走**灰態、不是紅態** | M-1 `[3.6]`：只有 `live`（不顯示）與 `grey`，**沒有 `err`**；`note` 逐字「判型失敗是『沒有值』，走灰態，**不是紅態**」。該文件 `§3.3.3`（`:1751-1759`）與 `§3.3.7` 矩陣皆走灰態、並寫「⛔ **不得用紅態**」 | ✅ **成立**，且 `where`（確認代碼／可手動指定型別後重新載入）與 M-1 逐字一致 |

---

# §3 B-01 裁決（頁1 葉2 「五桶 vs 七段」）

## 一、那個矛盾是**真的**（本組回 M-1 原文實測，不採信轉述）

反序列化 `PAGES[0]` 得到：

| 欄位 | 原文 |
|---|---|
| `leaves[1]` | 「葉2 指標明細（**五桶逐段**）」 |
| `blocks[5].t` | 「葉2 指標明細 — **七段**」 |
| `blocks[5].n` | 「**A 市場狀態 / B 長期 / C 中期 / D 短線 / E 籌碼 / F 全球風險 / G 跨桶裁決**」 |

⇒ ✅ **M-1 同一頁的 `leaves` 與 `blocks` 確實互相矛盾，B-01 的前提成立。** 這不是該文件編出來的。

## 二、採五桶**對不對** —— 本組獨立判斷：**對，但理由要換一個講法**

**支撐五桶的實測事實（本組自己跑的）**：
- `shared/macro_buckets.py:109` → `BUCKET_ORDER = ["long", "mid", "short", "chips", "news"]`（**5**）
- `shared/macro_buckets.py:267` → `BUCKET_DANGER_SPECS`，實際長度 **16**（`python3 -c` 匯入後 `len()` 實測）
- `src/ui/tabs/tab_today.py:258-266` → `DETAIL_SEGMENTS` **七段確實存在於 code**（`a_state`~`g_cross`），但它是**另一套分類**
- `src/ui/views/page_today.py:115-120` 逐字：「16 盞燈涵蓋的是 **B/C/D/E**，**A 與 G 沒有對應的燈、F 只被 `us10y` / `dxy` / `vix` 部分涵蓋**」

⇒ **以「16 盞燈的歸屬」為分段依據時，七段裡有兩段是空的、一段只填了三盞。** 拿七段當燈的容器，會產生 2 個必然空殼 ＋ 1 個半空殼。**採五桶正確。**

⚠️ **但該文件給的理由少講了一半，本組必須點出來**：
M-1 的七段**不只是燈的分類，它同時是十二個 `section_*` 的落點表** —— `blocks[5].note` 逐字寫「`section_state`／`section_long`＋`section_long_term`／`section_mid`＋`_render_china_drag_panel`／`section_short`／`section_chips`／`_render_global_risk_bucket`／`section_cross_ai` 逐一對應七段，**這十二個 `section_*` 沒有一個沒落點**」。
⇒ **七段與五桶回答的是不同問題**：七段答「**舊的十二個 section 搬到哪**」，五桶答「**16 盞燈怎麼分組**」。
⇒ 換成五桶之後，**`section_state`（A 市場狀態）與 `section_cross_ai`（G 跨桶裁決）的內容落到哪裡，該文件從頭到尾沒有交代**（實測：`section_state` 在該文件只在 `:565` 以「寫入端在 render 路徑」的 bug 脈絡出現一次；`section_cross_ai` 只在 `:812` 以「卡片標題叫『② **建議**配置』」的合規違規清單出現一次 —— **兩次都不是落點交代**）。
⇒ **本組的補充建議**：B-01 維持五桶，但**加一句落點交代**（A 的內容是否已被葉1 ③「位階」吸收、G 的內容是否已被葉1 ① 結論燈吸收），否則 M-1 那句「十二個 `section_*` 沒有一個沒落點」的保證**在新結構下沒有人接手**。
⚠️ **本組沒有查證那兩段的內容實際落到哪裡** —— 只查證了「該文件沒有交代」這件事。

## 三、畫面上**有沒有誠實交代**那兩段沒有燈 —— **有，而且是讀 SSOT 常數，不是抄的**

| 查證項 | 結果 |
|---|---|
| 揭露文字存在嗎 | ✅ `src/ui/views/page_today.py:618-622` `SEGMENT_COVERAGE_NOTE`，逐字：「本葉照 16 盞燈**真正的歸屬**分成五桶（長期 / 中期 / 短線急殺 / 籌碼 / 新聞）。線框寫的七段另含「A 市場狀態」與「G 跨桶裁決」—— 那兩段**沒有對應的燈**，「F 全球風險」也只被其中三盞部分涵蓋。與其擺三段空殼假裝有結構，不如照實分段並在這裡講明。」 |
| **真的渲染到畫面上嗎** | ✅ `src/ui/views/page_today.py:1938` → `st.caption(SEGMENT_COVERAGE_NOTE)`。**不是只寫在註解裡** |
| 該文件有照抄嗎 | ✅ `:1279-1284` 把這段畫進葉2 頁首框內，文字與常數一致 |
| B-01 的引文準不準 | ✅ 逐字比對通過（B-01 宣稱「`page_today.py:618-622` 逐字」，屬實） |

## 四、裁決

> ✅ **B-01 成立，維持五桶，維持那段揭露文字。**
> M-1 的內部矛盾是真的；五桶是 16 盞燈的正確歸屬；畫面已誠實交代兩段無燈且揭露會真的渲染出來。
> ⚠️ **但 B-01 的「為什麼」欄漏了一半** —— 七段同時是十二個 `section_*` 的落點表，換成五桶後 `section_state`／`section_cross_ai` 的落點**無人接手**。**建議在 B-01 補一列落點交代，或明列為待查證事項。**

---

# §4 我查過、確認**沒有問題**的區塊（逐項列名）

> 依派工說明，這一節的價值不亞於抓到錯。以下每一項都是本組**回 M-1 原文或 code 原行**驗過的，⛔ 不是看該文件的說法。

## 4.1 結構層（全部通過）

| 查證項 | 結果 |
|---|---|
| 五頁、十一葉、二十九區塊 ＋ 1 條跨頁列 | ✅ 與 M-1 反序列化結果完全一致（30 blocks，逐頁 7/4/7/8/4） |
| 十一葉的葉名 | ✅ 五頁 `st.tabs(...)` 的葉名常數與 M-1 `leaves` **逐字相同**：`page_today.py:1894-1897`／`page_find.py:426-427,1705`／`page_inspect.py:352-353,2542`／`page_hold.py:391-392,3729`／`page_why.py:330-332,2203` —— **所有行號本組逐一 `sed` 驗過，全部命中** |
| 各頁 `x.7` 矩陣的**列數** | ✅ 頁1 = 7 列、頁2 = 4 列、頁3 = 7 列、頁5 = 4 列，與 M-1 塊數一一對應；頁4 = 9 列（＝ M-1 的 8 塊，⑤ 拆成 ⑤-a/⑤-b，**已由 B-10 登記**） |
| 十個「呈現情況」是不是憑空多出來的態 | ✅ **不是**。`shared/ui_state.py:93-105` 實測有 7 個底層態（`UI_IDLE/LOADING/FAILED/EMPTY/DEGRADED/UNWIRED/LIVE`），`§2.5.1` 把 10 種呈現情況逐一對回這 7 個 ＋ `MISS_*` 值級原因，符合客戶拍板一「**底層維持現有態數不動**」 |

## 4.2 頁1 🚦 今天（7 塊逐塊）

| M-1 塊 | 判定 |
|---|---|
| `[1.1]` ① 結論燈 | ✅ 主數值偏離**已由 B-09 明確登記**，且候選甲的每一條 code 出處本組實測命中：`shared/allocation_decision.py:179`（`posture`）／`:180`（`icon`）／`:222-226`（`headline()`）／`:224`（fallback 內寫死舊按鈕名「🚀 一鍵更新全部數據」）／`:316-317`（`drivers[0]` ＝「總經健康分 X → 姿態「Y」，油門帶 Z」**逐字存在**）／`:327-329`（`drivers[-1]` ＝「最終 = min(姿態…, 天花板…)」）／`:333`（`health=float(_health)` 建構時帶入）。⇒ **「這句話已逐字存在於 L0，不是新發明」屬實** |
| `[1.2]` ② 操作列 | ✅ 見 §2 B-4 #1 |
| `[1.3]` ③ 三欄摘要 | ⚠️ 三格名（位階／動能／風險）與 M-1 一致 ✅；**但 ⚑ 無對應條目 → 見 DEV-21** |
| `[1.4]` ④ 今日關鍵橫幅 | ✅ 灰態「**未評估 ≠ 無異常**」、`None` vs `[]` 兩種都不是「沒事」、⑦ 失準態「本列僅含急變層」皆與 M-1 逐字同向；**M-1 該塊無 `unwired` 態，該文件矩陣也寫「—」**，一致 |
| `[1.5]` ⑤⑥ 作戰室＋AI 摘要 | ✅ 「AI 摘要預設收合」保留；灰態三要素與 M-1 一致 |
| `[1.6]` 葉2 指標明細 | ✅ 五桶偏離**已由 B-01 登記**（裁決見 §3）；⑧ 未接線那格保留 M-1 最關鍵的一句「**這一態沒有使用者可執行的出口**」 |
| `[1.7]` 跨頁頂部狀態列 | 🔴 **`live` 少了「· 建議 70%」→ DEV-22**。其餘 ✅：`§2.10.1` 正確標「M-1 `:775-782` 已核准」；四條來源優先序第 3 條（`macro_state.json['market_regime']`）走**失準態**、寫「**時間不明**」而不編時間 —— 與 M-1 `degraded` 逐字同向；「讀 L3 契約、⛔ 不依賴任何 render 副作用」的 F13 教訓也逐字保留 |

## 4.3 頁2 🔍 找標的（4 塊，全部通過）

- `[2.1]` 條件表單：✅ 三格（①基本面優選／②勾條件／③排序與筆數）與 M-1 `cells` 一致；M-1 note 的「**CSV `download_button` 留在 form 外**」該文件保留（`:1430`、`:1454`、`:1513`）。⚠️ label 另有問題 → INV-1
- `[2.2]` 選股結果：✅ 「存活池 274 → 前 50」「缺貨 12/89 · 抗跌RS 31/274」「FinMind 額度用罄**非程式錯誤**」「額度每日 00:00 重置」皆與 M-1 逐字同向
- `[2.3]` 板塊地圖：✅ 「單顆 `st.button`，**不在 form 內**」保留；M-1 的 N6 改名交接註記（production 舊名「🗺️ 載入產業熱力圖」）**已經不再是問題** —— 本組實測 `page_find.py:423` 已是新名「🗺️ 載入板塊地圖」，code 追上了
- `[2.4]` 口徑揭露：✅ 「**面積不等於權重**」保留為常駐；矩陣十格全寫「常駐」，與 M-1 `grey`「不隨狀態消失」一致

## 4.4 頁3 🔬 查一檔（7 塊）

| M-1 塊 | 判定 |
|---|---|
| `[3.1]` 輸入＋判型 | ✅ 「判型結果就地顯示且**可手動覆寫**」＋ `station_cards.py` 那起「型別大小寫打錯一個字母 → 可信度虛高到 100%」事故理由，與 M-1 note 逐字同向 |
| `[3.2]` 個股判決卡 | ✅ 三格（健康度／估值／籌碼）與 M-1 一致，**B-02 也正確指出 `S1-3`／`UI-E` 的兩種改名才是偏離** |
| `[3.3]` 個股明細 | ✅ **8 段逐字對得上 M-1 `live`**：K 線＋均線 → 💰 獲利能力診斷 → 357 評價河流圖 → 財報領先指標 → VCP／布林 → 月營收 → 什麼時候買賣 → 心理檢查。**「8 段」不是發明** |
| `[3.4]` 💰 獲利能力診斷 | 🔴 **少兩格 → DEV-20**。但缺值三律的內容 ✅：「缺值是灰的⚪ 資料缺漏、**不是**紅的辛苦生意」「`== 'Good'` 布林判定落 else」「正確樣板是同檔 80 行後的負債比卡 `{'Pass':綠,...}.get(status, 灰)`」與 M-1 note 逐字同向；「兩份複本各 8 處共 16、寬尺會變 10 但多的 2 處是對的、**此數未窮舉**」也逐字保留 |
| `[3.5]` ETF 分支 | ✅ **7 段明細逐字對得上 M-1 note**：淨值 vs 市價走勢 → 折溢價帶 → 配息紀錄與以息養股試算 → 成分股與集中度 → 同儕 7 維評分 → 標準差買賣帶 → 破發檢查。三格（折溢價／配息／追蹤·同儕）✅。「與個股重疊近零（13 個 vs 6 個關鍵詞 0 命中）」逐字保留 |
| `[3.6]` unknown 分支 | ✅ 見 §2 B-4 #3 |
| `[3.7]` 多檔比較 | 🔴 下鑽手勢 → DEV-19。其餘 ✅（表單貼一串可混型代碼、🚀 批次分析、依型別走 A 或 B） |

⚠️ **一個看起來像偏離、實際不是的**：`§3.3.7` 矩陣把「葉1-C unknown」排在**第 2 列**（M-1 是第 6 塊）。但 `§3.3.3` 的**實際版面順序**是 A（判型→判決卡→明細）→ 💰 → B → C → 葉2，**與 M-1 的 blocks 順序一致**。⇒ **只是矩陣表的列序，不是版面偏離。**

## 4.5 頁4 💼 我的持股（8 塊）

| M-1 塊 | 判定 |
|---|---|
| `[4.1]` ① 結論三卡 | ✅ 該做什麼／訊號可信度／需要處理，與 M-1 `cells` 一致 |
| `[4.2]` ② 兩套刻度 | ✅ 「**常駐揭露 · ⛔ 非摺疊**」保留；矩陣十格全「常駐」；失準態「兩套刻度目前不一致，**已停止跨頁比較**」與 M-1 `degraded` 一致 |
| `[4.3]` ③ 戰情表 | 🔴 下鑽手勢 → DEV-19。其餘 ✅（12 檔 × 235 加碼燈 / 3-3-3、**燈牆與 VIX 各自判態**） |
| `[4.4]` ④ 換股建議 | ✅ **改名為「換股對照」已由 B-03 登記**，且 B-03 明確寫「區塊本身⛔ 不動、位置⛔ 不動、內容⛔ 不動」。M-1 `degraded`「已產生但**未套用總經位階**、依規則不以中性代替」逐字保留 |
| `[4.5]` ⑤ 80/20 ＋ 衛星停利 | ✅ **「衛星停利」沒有被弄丟** —— 本組專門查過：該文件 `:2016` 標題「⑤-a 80/20 配置偏離 ＋ 衛星停利」、`:2018` 內容「衛星 2 檔已達你設定的停利門檻」。⚠️ 唯一瑕疵：`§3.4.7` 矩陣的列名簡寫成「⑤-a 80/20 偏離」漏了後半，屬**表格列名不一致**，內容沒少。拆成 ⑤-a/⑤-b **已由 B-10 登記** |
| `[4.6]` ⑥ 組合深度分析 | ✅ **「六項」不是發明** —— M-1 `live` 逐字列了六項：再平衡 · 核心／衛星 · 壓力測試 · VaR · 配息現金流 · 葡萄串領息，該文件 `:2026` 六項逐字一致。「expander 收合只是視覺收合、body 每次 rerun 照跑」理由也保留 |
| `[4.7]` ⑦ AI 戰情總結 | ✅ 「⚡ 生成 AI 總結（**單顆 `st.button`，本區塊內**）」＋「📋 複製」保留；M-1 `live` 的「含當日**建議**與理由」該文件改寫為合規版（受拍板三拘束），⚠️ 該改寫**沒有單獨登記**，但它落在 B-03 同一類（禁用語替換）射程內，本組**不另立條目** |
| `[4.8]` 葉2 組合設定 | ✅ 四項（Google 登入狀態 · Sheet 選擇 · 持股列預覽 · 觀察清單管理）保留；「**就地完成，不必去別頁**」保留。submit 搬家 **已由 B-05 登記**，且其理由本組實測成立：`page_hold.py:3731-3740` 確實有那 10 行「**執行順序 ≠ 顯示順序**」的繞法註解 |

## 4.6 頁5 📖 憑什麼（4 塊，全部通過）

- `[5.1]` 教學：✅ 「**這頁沒有灰態**（靜態內容，永遠可讀）」與 M-1 `grey` 逐字一致
- `[5.2]` 使用者版：✅ 三欄燈號卡牆（FRED／FinMind／TWSE）；「**這頁的紅燈是主角，不是意外**」保留；M-1 F5 更正「⛔ 不承諾 repo 裡不存在的『備援鏈跑到第幾層』」該文件 `:2228` 明文照辦
- `[5.3]` 工程師版：✅ 兩步、兩個 label 全對（見 §1 主 CTA 表）；「**⛔ 不加 form**」保留
- `[5.4]` AI 問答：✅ `chat_input` 天然 gate；「**只根據本站已載入的資料作答，⛔ 不自行上網查**」「AI 會說『那一頁還沒有資料』**而不是猜**」逐字保留；錯誤態「**就地給設定入口**」而非叫使用者改 `secrets.toml`（M-1 F5 更正）✅

## 4.7 該文件**主動拒絕發明**的地方（值得記錄）

- `§4.2~4.5`（基金站其餘四頁）：🔴 標題逐字寫「**本節待補 —— 且本組明確拒絕在此虛構線框**」，並在 `★-14c` 引 `S1-3 §7.2:1527` 自己的要求「先派一組實地盤點」。⇒ **面對沒有材料的四頁，它選擇留白而不是編。這是正確做法。**
- `§3.5.0`：「⭐ **⛔ 不准為本頁發明一顆按鈕。**`S1-3:514` 寫「🚀 重新檢查所有來源」—— **那顆按鈕不存在。**」本組實測 `S1-3_IA_WIREFRAME.md:513-514` 確實畫著那顆鈕並標「← 本頁唯一主 CTA」，而 `進階診斷` 在 `S1-3` 全檔 **0 命中** ⇒ **該文件對 `S1-3` 的指控屬實。**
- 拍板一的三條實測支撐、B-11 的「核准線框 0 命中手機/響應式」、B-12 的「線框從來沒有設計過跨頁動線」—— **三處都自標為新增並送 ★ 待拍板**，沒有默默塞進去。

---

# §5 我**沒有**查到／沒有查的（依 `CLAUDE.md §-2` 規則 6 誠實標註）

## 5.1 ⚠️ 本報告全部結論由**單組產出，未經第二組複驗**

- **本組⛔ 不宣稱「發明只有 3 項」「漏標的偏離只有 4 項」。** 上面列的是**本組這一輪比對到的**，不是窮舉。
- 本組的比對軸是：**block 標題／`n` 規格欄／五態／`cells`／`note` 裡的具名控制項**。
  **凡不落在這五個欄位裡的偏離，本輪結構上掃不到。**（同型於 M-1 自己記載的失效模式：「一個按鈕形狀的搜尋，看不到一顆 checkbox」。）

## 5.2 明確沒查的範圍

| # | 沒查什麼 | 為什麼 |
|---|---|---|
| 1 | **Part 4 基金站全部（`§4.0~4.7`，約 250 行）** | 權威基準是**姊妹 repo `my-Fund-dashboard`**，不在本 session。`🚀 更新淨值與配息`（4 次）、`〔到戰情室看明細 ↗〕`（3 次）、9 分頁清單、`📊 資產配置` 逐區塊線框 —— **一項都沒驗**。⚠️ **不得把本報告讀成「基金站那段也查過了」** |
| 2 | **Part 5 逐檔再平衡（`§5.1~5.10`，約 180 行）** | ⑤-b 整段已由 **B-10 登記為新增**，屬「有標」；本組只確認**它有標**，**沒有**查 `target_pct`、狀態 A/B/C、情境試算五條限制的內容正確性 |
| 3 | **`§2.8` 響應式斷點的數字**（197px／274px／480/640/880/1280） | 已由 **B-11 ＋ ★-06/06b 登記為「從未經客戶拍板」**；本組**沒有**回 `UI-C` 驗那些量測值 |
| 4 | **`§2.12` 禁用語表、`§2.13` 用語字典的逐條正確性** | 屬 C 組（稿內違規句）射程。本組只在 `§2.13` 撞到一條與 DEV-19 相關的刪句，已就地記錄 |
| 5 | **A 組射程的一切**（會不會當場炸掉、`★-03` 的 `raise ValueError`、`_ui_kit` 契約） | 已交件於 `S1-3B_REDTEAM.md`，本組不重複 |

## 5.3 查到一半、**沒有查完**的三條（留給下一組）

1. **`§2.1` 的 ⚑「③ 三欄摘要」原本想指什麼**（DEV-21）——
   本組只證實「**附錄 B 沒有對應條目**」，**沒有**推斷它原本要登記哪一項偏離。
2. **五桶之後 `section_state`（A 市場狀態）／`section_cross_ai`（G 跨桶裁決）的內容落到哪裡**（§3 四）——
   本組只證實「**該文件沒有交代**」，**沒有**去 code 追那兩段內容現在由誰渲染。
3. **B-07「`_idle_note(scope_idle=True)` 同一段文字掛到 14 張卡」的 14 這個數字** ——
   本組實測 `page_hold.py` 的 `_idle_note(` **靜態呼叫點是 9 處**（`:1521/1897/2032/2079/2321/2588/2680/2805/3411`，簽名在 `:546`），
   且**全部是動態傳入** `station.scope_idle` 等，`scope_idle=True` 字面 **0 命中**。
   ⚠️ **9 個呼叫點與 14 張卡並不矛盾**（一次呼叫可扇出多張卡），而該文件自稱這個 14 來自「**紅隊實跑**」。
   ⇒ **本組沒有實跑，故⛔ 不判它對錯**，只記錄「靜態 9、宣稱 14、差額須靠實跑解釋」。B-07 是一項政策變更，**這個數字是它的主要依據，建議獨立一組實跑覆核。**

## 5.4 本組**沒有**做的事（範圍聲明）

- ⛔ 未修改 repo 任何檔案、⛔ 未改寫 `S1-3B_FULL_DRAFT_SPEC.md`、⛔ 未做任何 git 操作（`git status` 僅唯讀查詢）。
- 本輪唯一寫入：本報告 ＋ 兩支唯讀解析腳本 `_rtb_extract.js` / `_rtb_dump.js`（皆在 scratchpad，不進 repo）。

---

# 附：建議客戶／總管優先處理的五條

| 序 | 條目 | 為什麼優先 |
|---|---|---|
| **1** | **DEV-19 下鑽手勢** | 它改的是**使用者手上的動作**，落在 v3 §03-2 ① 必須草稿先行的射程；而且目前**被記成「M-1 已核准」**，不攔下來就會以核准項的身分進實作 |
| **2** | **DEV-20 💰 少兩格** | 這是**功能減項**（稅後淨利率／ROE），而且 code 本來已把它寫成待補事項，規格書蓋掉之後就沒有人會記得補 |
| **3** | **DEV-22 狀態列的「建議 70%」** | 合規動作本身正確，但**沒登記**；而 B-09 自稱是「最大的一項偏離」並列了 6 落點，會讓人誤以為這件事已經收乾淨 —— 實際上 M-1 上的第二處是被無聲刪掉的 |
| **4** | **INV-2 四層 ＋ 展開佐證未登記為新增** | 它是**全站每一個區塊都會長出來的控制項**，卻不在 B-3「新增」表裡；客戶審這份稿時會以為它是 9/05 就核准過的東西 |
| **5** | **§3 四 的落點缺口** | M-1 用「十二個 `section_*` 沒有一個沒落點」擔保過的事，換結構後**沒有人接手** |

---

## 📌 全報告的計數（**本組單組，未複驗**）

- **§1 發明**：🔴 **2 項**（INV-1 CSV label／INV-2 展開佐證未登記）＋ ⚠️ **1 項措辭問題**（INV-3）。
- **§2 漏標偏離**：🔴 **3 項**（DEV-19 下鑽手勢／DEV-20 💰 少兩格／DEV-22 狀態列建議 %）
  ＋ 🟠 **1 項自我宣告的洞**（DEV-21 ③ 三欄摘要的 ⚑ 無條目）＋ ⚠️ **1 項登記欄位填錯**（DEV-23 B-08 放錯類）。
- **§3 B-01**：矛盾**屬實**，裁決**維持五桶**，畫面揭露**已驗證會真的渲染**；另指出 B-01 理由欄漏了「落點表」那一半。
- **主 CTA 五頁 ＋ 頁5 兩個控制項**：**逐條回 code 驗過，一條都沒有發明。**
- **B-4 三處「沒有偏離」**：**三條都成立。**

⚠️ 以上是**本組這一輪比對到的**，⛔ **不是窮舉**，⛔ **未經第二組複驗**（`CLAUDE.md §-2` 規則 6）。
**⛔ 不得寫成「發明只有 2 項」「漏標只有 3 項」。**
