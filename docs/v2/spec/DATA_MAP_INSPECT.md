# 查一檔頁資料對照表（IA v2 戰情室 · block 級）

> **這是什麼**：🔬「查一檔」頁每一個 block「畫面上那個東西從哪來、怎麼算、沒資料時怎麼辦」的對照表。
> **射程**：本頁 **22** 個 block（順序照 `docs/v2/prototype/gen_today_v2.py::INSPECT_LAYOUT`）。彙整日 **2026-09-23**・branch `ui-v2`（素材量測日 **2026-09-22**）。**索引與共通讀表前提**＝`docs/v2/spec/DATA_MAP_ALL_PAGES.md`。
> **來歷前綴**：〔GA〕＝**程式碼端**（AST ＋ 逐層讀 code）・〔GB〕＝**規格端**（讀規格與線框，⛔ 未回 code 驗證）・**無標＝兩組共同**。⚠️ **兩組的待確認都用 ⓐ~ 圓圈字母且撞號** ⇒ 引用時**必須連組別一起寫**（`GA ⓑ` ≠ `GB ⓑ`）。
> 📌 **規格洞與 ⚠️ 待確認一律登記在 `docs/v2/spec/DATA_MAP_GAPS.md` 的 `## 🔬 查一檔頁`，⛔ 本檔不留第二份。**
> ⚠️ **本檔是現況記錄，⛔ 不構成主動動工的授權**（`CLAUDE.md §-1`）。⛔ 全檔不寫行號（`§8.2.A.0` 規則 1）；程式碼引用一律**全路徑** `檔名::符號`、規格一律 `檔名 §章節名`；TTL 一律寫 `shared/ttls.py` 常數名；查不到一律「⚠️ 待確認」，⛔ 不猜、⛔ 不發明資料源／算式／中文標籤。

---

### 0. 讀表前提（⛔ 少了這段，下表每一列都會被讀反）

- **`[A:無此頁]`（全 22 塊）**：`ls src/ui_v2/*.py` **無 `page_inspect`**（總管前輪實測，GA 沿用、⛔ 未重查）⇒ 下表「資料來源」一律填 **B＝現行** `src/ui/views/page_inspect.py`（掛載 `app.py::_ia_view_inspect` → `::render_page_inspect`；側欄 radio、late import，該輪以 `st.stop()` 結束）。
- 🔴 **`grep=0` ⛔ 不等於未實作**：實作端用**動態 card key**（AST 實測 `key=f"inspect.profit.{_key}"`），表單與 caption 又本來就沒有 card key ⇒ **本頁 5 塊字面 grep ＝ 0 卻有實作**：`chrome.footer`（實作在 `app.py`）／`inspect.form`／`inspect.wiring_single`／`inspect.batch.form`／`inspect.profit`。⛔ 不得照數字誤判成未實作（**方法論 ①**，見 `DATA_MAP_GAPS.md` 檔頭）。
- 🔴 **`#7`／`#8`／`#9` 在實作端零落地 ⇒ 下表凡寫這三號者都是「應然」**：L0 `shared/ui_state.py::UI_STATE_META[UI_EMPTY]` 的 glyph ＝ **`▨`**、文字「無資料」，**單一態、未依 `miss_reason` 拆 #7／#8**；`⚠︎`／`◧` 在本頁與 `shared/ui_state.py` **皆 0 命中**〔GA AST〕。`reason=` **有**被傳進 `classify_ui_state()`，但**只影響是否轉紅**（`FAILED_REASONS = {MISS_FETCH_FAILED, MISS_CONTRACT_DRIFT}`），**⛔ 不影響 glyph**（**方法論 ②**，見 `DATA_MAP_GAPS.md` 檔頭）。
- **三種狀態⛔ 不可混為一談**〔GA，三法交集：字面 grep ＝ 0 **且** AST 動態 key 無涵蓋 **且** `section_header` 全清單（11 處）無對應段〕：**✅ 有實作 14 塊**／**🟡 已實作但 `wired=False` 灰卡 2 塊**（`inspect.stock.detail`／`inspect.etf.detail` —— 畫得出來、會逐項列出少了哪七段）／**🔴 真正未接線、畫面完全不存在 6 塊**（`inspect.statusbar`／`chrome.asof`／`inspect.pattern_measure`／`inspect.evi.cards`／`.overlap`／`.cost`）。⚠️ **「6 塊」是 GA 單組窮舉的全稱句。**
- **TTL／fallback 兩欄由程式碼端補**：`UI_PAGE_INSPECT.md` 的 `TTL`／`fallback`／`備援`／`頻率` **全部 0 命中**〔GB 實測；`S2-ENG_SPEC §3.3` 逐字「**「備援鏈跑到第幾層」這個資訊在 repo 裡不存在**」〕⇒ 下表 TTL 一律寫 **GA AST 掃 `@st.cache_data` 裝飾器**得到的 `shared/ttls.py` 常數名，**⛔ 不寫秒數**；程式碼端也追不到者才寫「⚠️ 待確認」。
- **徽章一律用 `UI_COMPONENTS §2` 的 `#n`；兩套 `D-*` 同名不同義、⛔ 不得補零統一**：`S2-UI_SPEC §9.3` 的 **`D-1`**／**`D-2`**（＝**揭露落點**）⛔ **不是** `INDICATOR_SPEC §5`／`DECISION_RULES §8` 的 `D-01`／`D-02`（＝**應然差異**）—— **逐字照抄出處寫法**。⚠️ **本頁畫面上只有 5 態：#1／#3／#5／#6／#7**（`UI_PAGE_INSPECT ②`）；**#4 刻意不出現**（`page_inspect.py` 檔頭逐字「本頁沒有任何一格會判 `UI_DEGRADED`，這是刻意的」）、**#2／#8／#9 待補**、**#10 本頁 0 命中 ⛔ 不畫**。

### 1. 二十二個 block（順序照 `INSPECT_LAYOUT`）

| block key | 畫面顯示什麼 | 資料來源 | 計算 | 缺值處理 |
|---|---|---|---|---|
| `inspect.statusbar` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**。規格要的頁級常駐狀態條（`live`＝「`09/13 週五 ✅ 交易日 ｜ 🟢 總經 偏多 ｜ 🔗 已綁 Sheet`」） | 🔴 **畫面上沒有這一條**〔GA，grep `statusbar`／`status_bar` 在 `page_inspect.py` ＝ **0**〕。⭐ **而且是刻意的**：`app.py::render_portfolio_status_bar` 住在 `st.stop()` **之後** ⇒ IA v2 路徑不執行；`app.py` 註解逐字說明全域置底條「**整條不出現**，不是畫成灰色、也不是改文案」，理由是它讀 `warroom_summary`、舊頁籤不渲染時**必然**退回「⬜ 總經未評估」＝ 假訊息。**規格端指定**〔GB〕：`S2-ENG_SPEC §3.2` 的 **L3 canonical 契約 `src/services/macro_state_locker.py::get_macro_state`**（**只有 9 個 key**、無 `as_of`），⭐「**狀態列⛔ 不得依賴任何 render 副作用**」；線框 `src` 指向 `src/ui/views/page_today.py`（**跨頁共用、非本頁實作**）。**TTL／fallback ⚠️ 待確認**（`GB ⓐ`／`GB ⓑ`） | 無計算（三格直讀 L3 契約欄位） | 畫面上不存在 ⇒ **本頁無態可判**。規格端十格〔GB〕：`idle`→**#3**（可重試，出口在 🚦 今天主 CTA）／`degraded`→**#4**／`error`→**#6**（「總經狀態讀取失敗（附錯誤型別）；Sheet 欄位維持原樣」）；`empty`／`missing`／`na`／`partial`／`unwired`＝**null**（⛔ 不得畫成空卡）。⚠️ 失敗時無可顯示 ⇒ ⛔ **不得用判決卡的紅態冒充一條不存在的狀態列**。⚠️ 線框 `live` 的 `09/13` 是**規格示意值、⛔ 非事實**。⚠️「本頁要不要實作」＝ `GB ⓙ`。**編號 ⚠️ 待確認**（`GB F-2` 對不上）；洞 `GA-I-7` |
| `chrome.asof` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**。規格要的頁級資料時點列（`live`＝「資料時點：`〈歸屬日〉 〈性質〉`　⭕ 可點，展開逐源明細」） | 🔴 **0 命中**〔GA，`page_inspect.py` grep `asof`／`as_of` ＝ 0；`app.py` IA v2 分支亦無〕。⭐ **線框自己也沒給來源**：`gen_today_v2.py::INSPECT_PAGE_SHAPE_NOTE` 逐字「22 塊的 asOf 欄**全部是 null**（⇒ 本頁一個時間都不准填）」。**規格端**〔GB〕：歸屬日取 `shared/staleness.py::latest_date`，⛔ **不得用 `fetched_at`**；⛔ 禁用「今天／剛剛／最新」相對詞；⛔ 時點列**不做新鮮度判斷**（`S2-UI_SPEC §5.4 ★-05`）。🔴 **本頁與頁1 最大不同：每卡各自一個時點**（健康度 `2026-Q2 財報`／估值 `09-13 收盤`／籌碼 `09-13 盤後`）——「**共用一個頁級時點會對其中兩格說謊**」；但 `ValuationReadout`／`StockReadout`／`ChipsView` **都沒有 `as_of`／`timestamp` 欄位** ⇒ **得先改 L3 契約**（洞 `INS-G-02`）。**TTL／fallback 不適用**（揭露列，非取數） | 規格端 `partial` ＝「最舊 `〈日〉` ～ 最新 `〈日〉`」**區間式** ＋ 未給日期的來源數 N；⛔ **非單一最新值** | 🔴 **兩組對「本頁算不算已撤回」說法不同，⛔ 並陳不裁決**：〔GA 程式碼端〕今天頁把它登記在 `src/ui_v2/page_today.py::WITHDRAWN_BLOCKS`（**已撤回**），而 `INSPECT_LAYOUT` **仍列它** ⇒ **三頁三種處置**（今天＝已撤回／找標的＝仍列／查一檔＝仍列但 asOf 全 null／持股＝沒列，洞 `GA-I-8`）；〔GB 規格端〕本頁線框 flags `['★待拍板','單組結論']`、`src: null` ＝ **尚未拍板、未實作**，⛔ **不得沿用今天頁的「已撤回」結論**（洞 `INS-G-01`）。`empty`／`missing`／`na`／`degraded`／`unwired`＝**null**。**編號 ⚠️ 待確認** |
| `chrome.footer` `[A:無此頁]` | ✅ **有實作** —— 置底免責一行：`⚠️ 台股AI戰情室 v3.0 · 僅供學術研究，非投資建議，盈虧自負`（**常駐，不隨狀態消失**） | ⭐ **實作不在頁檔，在 L6**：**`app.py::_render_footer`**（IA v2 分支於 `st.stop()` **前**呼叫，註解逐字「免責聲明**照樣出**(法遵文字,不分頁面)」）。🔴 **這就是「`grep=0` ⛔ 不等於未實作」的本頁實例**（`page_inspect.py` grep ＝ 0）〔GA〕。〔GB〕`UI_PAGE_INSPECT ④A` 為**總管 2026-09-16 更正**：原登記「本頁 0 命中＝實作待修」是**誤判**，⇒ **頁面 0 命中是正確設計，不是缺陷**。⛔ **不得在 `page_inspect.py` 內另畫一份**（`app.py` 註解逐字「複製第二份 ＝ 兩邊會漂移」＝ `CLAUDE.md §2.1` SSOT）。**零資料源**（寫死字串）。**TTL／fallback 不適用** | **無計算**（靜態字串） | **沒有缺值態** —— 恆顯示，不依賴任何取數 ⇒ 永遠 live；十格只有 `live`／`idle`／`loading`／`error` 有值，其餘 6 格 null。**無徽章**。**編號＝`S2-UI_SPEC §9.3` 的 `D-1`**（⚠️ ⛔ **不是** `INDICATOR_SPEC §5` 的 `D-01`），該表逐字「每一頁／頁尾／✅ 常駐」。⚠️ **`INSPECT_LAYOUT` 列 3 塊 chrome、`HOLD_LAYOUT` 列 0 塊，但兩頁畫面行為完全相同** ⇒ **差異純在 layout 定義**（洞 `GA-I-7`） |
| `inspect.form` `[A:無此頁]` | ✅ **有實作** —— 葉1 輸入表單三格並排：① 代碼 `st.text_input` ＋ 判型 `st.selectbox`；② 期間 `st.selectbox`(120/250/500)；③ 6 個 MA `st.checkbox`（**3 欄 × 2 排**）＋ 主 CTA「🔍 載入完整分析」。`idle` 常駐一句〔GB 線框〕「**按下去才會開始取數。在你按之前，這一頁不會發出任何一次網路連線。**」 | `page_inspect.py::_render_ticker_form` —— **單一 `st.form(FORM_TICKER_KEY)` ＋ 單顆 `st.form_submit_button`**，form 內⛔ 無 `st.button`〔GA〕。主 CTA 名稱 SSOT ＝ `page_inspect.py::ACTION_LOAD_INSPECT_LABEL`（`S2-UI_SPEC §11.3 C-02` 定案：**主 CTA 名稱一律取自 production code 常數**）〔GB〕。widget key：`SS_TICKER_WIDGET`／`SS_KIND_WIDGET`／`SS_PERIOD_WIDGET`／`f"{SS_MA_WIDGET_PREFIX}{ma}"`（**動態 key**）。**已套用值寫 `SS_APPLIED_TICKER` —— 全檔唯一寫入點**。**表單本身零取數** ⇒ **TTL 無快取／fallback 不適用** | **無計算**（widget 當下值 → 已套用值的搬運） | **無缺值態**（表單恆顯示）；`empty`／`missing`／`na`／`partial`／`degraded`／`unwired`＝**null**〔GB〕。⭐ **`SS_APPLIED_TICKER` 的「存在性」＝葉1 全部區塊的 `requested` gate** —— ⛔ 不是 `bool(ticker)`、⛔ 不是 `metrics is not None`（檔頭記載頁1 曾被紅隊抓到恆真式；`S2-ENG_SPEC §2.5` 逐字 ⛔「`requested` 不得由資料反推」）。冷啟動一律 **#3 ⬜ ＋ 灰色說明**，⛔ **不得畫成紅色錯誤**（`CLAUDE.md §1.A` 第 4 點）；`loading` 用 `UI_COMPONENTS §3` 停用態，**⛔ 不會清掉你剛打的代碼**。可重試：✅（改參數再送出）。**編號 ⚠️ 待確認** |
| `inspect.wiring_single` `[A:無此頁]` | ✅ **有實作** —— 表單下方**常駐** `st.caption` 的接線揭露：哪幾項已接線（判型／健康度／估值 357／籌碼／💰 獲利能力三格／ETF 折溢價·配息·同儕）、哪幾項還沒（K 線與其餘明細，「**不會拿空白冒充結果**」） | `page_inspect.py::WIRING_DISCLOSURE_SINGLE`，由 `::_render_ticker_form` 末尾 `st.caption()` 渲染〔GA〕。〔GB〕線框 evidence 逐字：這一格的文字是 **code 內既有常數的逐字轉錄**，「**⛔ 本組沒有改寫它，也沒有發明它**」；葉2 另有一份 `::WIRING_DISCLOSURE_BATCH`。**零取數** ⇒ **TTL 無快取／fallback 不適用** | **無計算**（靜態字串） | **無缺值態**（常駐，不隨狀態消失）、**無徽章**；十格中六格＝**null**、其餘四格全寫「同上」〔GB〕。⭐ 它負責講清楚一個易誤解點（檔內逐字）：表單的「期間」只決定**載入多長的日線**，**⛔ 不是判讀窗** —— 近 20 日籌碼的窗長住在 L0（`df.tail(20)`），**改期間不會改變籌碼結論**。線框 flags `[]`（本頁唯一 flags 全空的非 chrome 塊之一）。**編號 ⚠️ 待確認** |
| `inspect.kind` `[A:無此頁]` | ✅ **有實作** —— 判型卡。live 值＝`verdict.kind_label`（線框示意「`2330 → 判型：個股（可手動覆寫 ▾）`」）；facts 六列（代碼／系統判型／這一輪採用／判型來源／比較基準／期間·均線） | `page_inspect.py::build_kind_card`（**字面 key**）→ `::classify_kind` → **L2 `src/compute/etf/asset_lag.py::classify_asset_kind`**（純函式、**零 I/O**）〔GA〕。覆寫選項讀 `SS_KIND_WIDGET`，預設 `KIND_CHOICE_AUTO`。⭐ **就地可覆寫的理由**〔GB，核准線框 F4 逐字〕：repo 已記錄的同型事故 —— 「型別大小寫打錯一個字母 → 可信度**虛高到 100% 並打開巡航 gate**」，⇒ 型別分支推到流量最高的頁「**必須讓使用者看得到系統判成什麼、並且改得動**」；⚠️ 該事故句**線框自標未回 `station_cards.py` 原檔複驗**。**TTL 無快取**（純函式）／**fallback 不適用** | **L2 純前綴規則**（`classify_asset_kind` 逐字）：`bare_etf_code(ticker)` → `00` 開頭 ⇒ `etf`；首位為數字（4 碼 1-9）⇒ `stock`；其餘 ⇒ `unknown`。**本頁⛔ 不自寫前綴規則、⛔ 不補 heuristic**。使用者可手動覆寫，**兩列並陳**（系統判的 vs 這一輪用的） | `idle`→**#3**（冷啟動）／`empty`＋unknown→**#7 ⚠️ 應然；實作畫 `▨`**（傳 `reason=MISS_NOT_APPLICABLE`，⛔ 不在 `FAILED_REASONS` ⇒ **留灰、不轉紅**）／`empty`＋空代碼→同上但**文案不同**（`BLANK_TICKER_*`，與冷啟動 `idle` **刻意分開**）／`error`→**#6 🔴**「判型失敗（附錯誤型別）—— **這種時候不會替你猜一個型別**」，且**兩支判決卡都不畫**〔GB〕。⚠️ `has_value=verdict.is_resolved`，⛔ **不是 `bool(verdict.kind)`** —— 後者會把 unknown 畫成綠燈。可重試：✅。**編號 ⚠️ 待確認** |
| `inspect.unknown` `[A:無此頁]` | ✅ **有實作** —— 葉1-C 獨立卡「**判不出型別 —— 已停在這裡**」，facts 三列（代碼／系統判型／本站的處置）。⭐ **十格裡唯一一個 `live` 等於「結構上不存在」的區塊**（代碼可判型時**不顯示此區**；線框逐字：寫成 null 會讓讀者以為漏填） | `page_inspect.py::build_unknown_card`（**字面 key**）。**同一份 `KindVerdict`，⛔ 零額外取數**〔GA〕。〔GB〕核准線框逐字：「判型失敗是「沒有值」，**走灰態，不是紅態**。v1 的查一檔只有『未輸入』灰態與『抓取失敗』紅態，`unknown` 無處可去 —— 那會逼實作把它塞進其中一個，**兩個都是說謊**」；另記「`unknown` 之後自動判型」**做不到，而且這是設計如此、不是缺陷**。**TTL 無快取／fallback 不適用** | **無計算**（僅呈現 `classify_asset_kind` 的 `unknown` 結果，判型的第三條路） | 🔴 **兩組給的徽章號不同，⛔ 並陳不裁決**：〔GA〕狀態恆 `empty` ＋ `MISS_NOT_APPLICABLE` ⇒ **應然 #8**；〔GB〕`UI_PAGE_INSPECT ⑤` 逐字 **#3／#7 灰系**。**兩組一致的是**：⛔ **不得用 #6 🔴**（「第三條路，不是紅態」），且**實作實際畫 `▨`**。⭐ 檔內逐字三個「不是」：⛔ 不是 `failed`（判型接好了、正常運作）、⛔ 不是 `unwired`（接了）、⛔ 不是 `idle`（使用者確實叫過）。**可否重試：❌**（換一個代碼才有用，重按同代碼無效）。⚠️ 與 `inspect.kind` 的 empty 分支**⛔ 不是同一張卡**：判型卡答「判成什麼」，本卡答「接下來會發生什麼＝什麼都不會發生」。⛔ **不替使用者猜一邊**。**編號 ⚠️ 待確認** |
| `inspect.stock.health` `[A:無此頁]` | ✅ **有實作** —— 個股判決卡①「健康度」。live 值＝`{score}/100 · {grade} 級`（線框示意 `78 / 100`／`B 級`／時點 `2026-Q2 財報`）；facts（名稱／現價／L3 總結／不及格項目／跨季趨勢） | `page_inspect.py::build_health_card` ← `::load_stock_readout` ← `::_fetch_metrics` ← **L3 `src/services/dividend_station_service.py::fetch_metrics`**(`asset_kind='stock'`) → `::_fetch_stock_metrics` → **L1 `src/data/core/financial_statements_fetcher.py::fetch_financial_statements`（`TTL_1HOUR`）** ＋ **L1 `src/data/stock/app_stock_fetchers.py::fetch_price_data`（`TTL_30MIN`）**；欄位 `mj_score_pct`／`mj_grade`／`mj_headline`／`mj_fail_items`／`trend_verdict`／`name`／`current_price`〔GA AST TTL 掃描〕。**fallback**：季報 FinMind → MOPS → Goodinfo（`src/data/core/data_registry.py`；⚠️ **兩組皆未逐段實跑** ＝ `GA ⓔ`）。〔GB〕規格端 TTL／fallback **0 命中** ＝ `GB ⓐ`／`GB ⓑ` | 🔴 **兩組指到兩套不同的算分引擎，⛔ 並陳不裁決**：〔GA 程式碼端〕**L3 `src/services/financial_health_engine.py::analyze_financial_health`**（no-AI 路徑）算綜合分與等第，門檻走 **L0 `shared/financial_health_thresholds.py`（`FH_*` 19 個）**，**L5 只讀字串、⛔ 不重判**；〔GB 規格端〕上游算式＝**L2 `src/compute/scoring/scoring_helpers.py::calc_health_score`** 的**六因子配分**（`DECISION_RULES §2 R2-1` 逐字：趨勢 30／RSI 20／量比 15／KD 15／IBS 10／布林 10；總分 `round(Σ得分ᵢ ÷ Σ滿分ᵢ × 100, 1)`），等第 `≥HEALTH_GRADE_A_MIN(80)`→A／`≥HEALTH_GRADE_B_MIN(50)`→B／其餘 C（`shared/health_thresholds.py`）。⚠️ **GB 自陳⛔ 未讀任何 `.py`**、並自己寫「與 GA 交叉時以 GA 的實測為準」，**但本檔⛔ 不代為裁決**。分數值域 `[0,100]`（`CLAUDE.md §3.2`） | `idle`→**#3**（判型非個股 ⇒ `requested=False` ⇒ **一行 L3 都不呼叫**）／`score_pct is None`→**`empty` #7 ⚠️ 應然；實作畫 `▨`** —— ⭐ 檔內逐字「**⛔ 也不寫 0 分 —— 0 分是一個結論（「這公司爛透了」），缺值不是**」／L3 拋例外→**#6**（「**估值與籌碼照常顯示**」）。⛔ **缺漏不得落到 C 級；C 與「⚪ 未評估」不得同形**（`INDICATOR_SPEC R-3`）。涵蓋 gate ＝ `有值因子數 > 6 × SCREENER_MIN_FACTOR_COVERAGE_RATIO(0.5)`（**嚴格 `>` ⇒ ≥4**）；`partial=True` 時**必須**同時輸出 `missing`＋`coverage` 並在畫面標示，⛔ **不得與完整分數混排比較或共用星等**（`INDICATOR_SPEC §4`）。⚠️ **fail-safe：N／M 分子分母拿不到 → 一律降 #7，⛔ 不得退回 #1**（實作端是否落地 ＝ `GA ⓖ`）。**可重試：✅**（`where` 指向 📖 憑什麼 › 資料健康度看 FinMind／MOPS 備援鏈）。**編號**〔GB〕：`D-01`／`D-28`／`D-29`／`D-35` |
| `inspect.stock.valuation` `[A:無此頁]` | ✅ **有實作** —— 個股判決卡②「估值（357 評價）」。live 值＝`殖利率 X%`（線框示意 `5.8%`／`🟡 殖利率 5~7%`／時點 `09-13 收盤`）；facts 五列（近 5 年平均年現金股利／有配息年數／配息資料來源／這一輪用的現價／L2 說明） | `page_inspect.py::build_valuation_card` ← `::load_valuation(verdict, stock)`〔GA〕。⭐ **現價復用 `inspect.stock.health` 那一輪的 `current_price`，⛔ 不再抓一次**（檔內逐字：再抓一次會有兩個可能不一致的價格）。配息來源三段 **fallback 順序：FinMind → yfinance → TWSE**（`page_inspect.py::VALUATION_NO_SOURCE_WHY` 逐字「三段都沒有給」）；L1 `src/data/stock/dividend_fetcher.py::fetch_annual_dividends`（**`TTL_1DAY`**）；⚠️ GA **未逐段實跑**該三段鏈。🔴 〔GB〕**線框 `live` 那五行是「合規改寫提案」，⛔ 不是現況** —— 現行 code **原樣透傳 L2 自己寫的一句話**（產生端 **L2 `src/compute/strategy/v5_modules.py::calc_dividend_yield_357`**，五分支原文含「— 策略1 存股首選」「需確認配息穩定性」等）；**本格是本頁唯一一個「線框文字 ≠ 現行 code 輸出」的地方**。規格端 TTL／fallback ⚠️ 待確認 | 🔴 **357 算式（`DECISION_RULES §3 R3-1` 逐字；GB 補上 GA 標為「由欄位語意推讀」的缺口）**：`cheap 便宜價 ＝ avg_div ÷ YIELD_HIGH_DEC(0.07)`｜`fair 合理價 ＝ avg_div ÷ YIELD_MID_DEC(0.05)`｜`dear 昂貴價 ＝ avg_div ÷ YIELD_LOW_DEC(0.03)`，**皆 `round(x,1)`**；`avg_div` 量綱＝**元/股**（5 年平均現金股利）。分級：`if price<=0 or avg_div<=0: 'na'`（→「不適用」，⛔ **不得判成昂貴**）`elif price<=cheap:'cheap' elif price<=fair:'fair' elif price<=dear:'dear' else:'overpriced'`。SSOT ＝ `shared/thresholds.py::YIELD_HIGH_DEC／YIELD_MID_DEC／YIELD_LOW_DEC` ＋ `::classify_stock_357_price`。⭐ **中文說法（便宜／合理／昂貴）一律讀 L2 的 `signal`／`msg`，⛔ 不在本頁另寫一份**（`CLAUDE.md §2.1` SSOT） | ⭐ **`na` 是 `empty`（灰）⛔ 不是 `failed`（紅）**：L2 對「無股價」與「無配息紀錄」都回 `zone_code='na'` —— 那是**這套法則不適用這一檔** ⇒ **#7 ⚠️ 應然；實作畫 `▨`**；⛔ **也不寫 0%**（「0% 會被同一套門檻判成『超貴』＝拿缺資料當看空結論」，對應 `D-30`）。**R3-2 EPS 三分流（虧損 ≠ 無資料）**〔GB〕：TTM EPS 空／全 NaN→`MISS_NO_INPUT`→「上游這輪失敗，可以重跑一次」；有效季數 `<4`→`MISS_NOT_ENOUGH`→「**等時間累積（重跑無用）**」；TTM EPS `<=0`→`MISS_NOT_APPLICABLE`→`N/A`（**真虧損**）。⛔ **三條不得共用同一句文案；⛔ 無資料不得被渲染成「虧損」**。⚠️ **缺值括號說明只在 `empty` 時給** —— `idle`／`failed` 根本沒跑到取數，印「沒有拿到配息紀錄」等於**替一輪沒發生的取數宣稱結果**。⚠️ `msg` 進 `Note` 前**必須**過 `tab_today.scrub_state_glyphs()`，否則上游放一個 `🔴` 會炸**整頁**。可重試：✅。**編號**：`D-07`／`D-08`／`D-13`／`D-14`／`D-30` |
| `inspect.stock.chips` `[A:無此頁]` | ✅ **有實作** —— 個股判決卡③「籌碼」。live 值＝`集中度 X%` ＋ 大字訊號（線框示意「主力連買 · 近 20 日」）；卡內補充列：連續性、天數、**單日爆量檢查**徽章 | `page_inspect.py::build_chips_card` ← `::load_chips` → **L3 `src/services/stock_chips_service.py::get_chips_readout(code, days=)`** → **L1 `src/data/stock/app_stock_fetchers.py::fetch_price_data`（`TTL_30MIN`）** → **L0 `shared/macro_compute.py::analyze_20d_chips_from_df`**；爆量走 **L2 `src/compute/risk/inst_sanity.py::flag_latest_inst_outlier_from_df`**，門檻 **L0 `shared/signal_thresholds.py::INST_NET_OUTLIER_VOLUME_RATIO`(=5.0)**〔GA〕。**fallback**：TWSE → TPEX（`src/data/core/data_loader_inst_fetchers.py`；⚠️ GA 未實跑）。〔GB〕規格端 TTL／fallback ⚠️ 待確認 | **多層**：**L1** 日線＋三大法人 → **L0** `analyze_20d_chips_from_df` 算集中度／連續性（**窗長 `df.tail(20)` 住 L0**）→ **L2** 爆量判定：`單日法人淨額 ÷ 30D 均量 > 5.0` ⇒ 異常。**L5 只搬運、⛔ 不重判** —— 吸籌／倒貨／發散三個字面與門檻住 L0，「在畫面上再比一次就是**第二把尺**」（`CLAUDE.md §2.1`），且會在對面改門檻時無聲漂移。〔GB〕`INDICATOR_SPEC §3` 籌碼集中度列＝`(外資+投信淨買賣超) ÷ 成交量`，**`0%` 屬有值**（買賣超剛好抵銷），⛔ 不得以 `0%` 頂替缺值 | `idle`→**#3**／**日線回來但法人欄缺／全 0／量 0／筆數不足**→**`empty` #7 ⚠️ 應然；實作畫 `▨`** ＋ **L0 給的 `miss_reason` 原文**（⛔ 不改寫；`ChipsView.miss_reason` docstring 逐字「**這不是故障** → empty（灰），不是 failed（紅）」）／L3 拋例外 **或 L1 回錯誤字串**→**#6**（「另外兩格照常顯示」）。⚠️ **動詞用「回報失敗」而非「拋出例外」** —— 那條路真的沒有例外，寫成例外會讓下一個人去 traceback 找一個不存在的東西。⭐ **爆量徽章與 `miss_reason` 無關地照搬**（df 回得來就一定判過一次）；只有 `idle`／`failed` 不帶徽章。`partial`＝值照給 ＋ `*`（「近 20 日只湊到 N 個交易日」）。可重試：✅。**編號 ⚠️ 待確認**（`GB F-2` 對不上；相鄰可引 `INDICATOR_SPEC §3` 籌碼集中度列、`CLAUDE.md §3.2` 三大法人 outlier 列） |
| `inspect.etf.premium` `[A:無此頁]` | ✅ **有實作** —— ETF 判決卡①「折溢價」。live 值＝`X%`（線框示意 `+0.12%`／`🟢 貼近淨值`／時點 `09-13 收盤`） | `page_inspect.py::build_premium_card` → `::_etf_card`（共用建構器）← `::load_etf_readout` ← **L3 `dividend_station_service.py::fetch_metrics`**(`asset_kind='etf'`) → **L1 `src/data/etf/etf_fetch.py::fetch_etf_nav_history`（`TTL_2HOUR`）** ＋ `::fetch_etf_official_premium`（**`TTL_15MIN`**）〔GA〕。⚠️ **折溢價僅 ETF**（個股無 iNAV，L3 直接跳過）。⭐ **fallback 鏈刻意在末端止血**：L3 檔內記著一次**已修事故** —— 末端 fallback 到 yfinance `navPrice` 會回「**最後已公告淨值**」被硬戳今日 ⇒ **假溢價 +5.07%** 觸發假燈 ⇒ **現行契約是拿不到就⛔ 不填這個欄位**。〔GB〕規格端**唯一一處 `fallback` 命中就是上述已修事故的敘述，⛔ 不是備援鏈規格** | `premium% ＝ (P − iNAV) ÷ iNAV × 100`（**同日 inner-join ＋ 3 道守門員**；出處 `INDICATOR_SPEC §3` 折溢價列〔GB〕—— ⭐ **GB 補上了 GA 標為「由欄位語意推讀、⛔ 非逐字命中」的 `GA ⓑ`**）。門檻 SSOT `shared/dividend_station_thresholds.py::PREMIUM_ALERT_PCT`／`shared/signal_thresholds.py::ETF_PREMIUM_*_PCT`。**L5 只格式化** | ⭐ **缺值一定是 `empty`（灰），⛔ 不能填 0%** ⇒ **#7 ⚠️ 應然；實作畫 `▨`**。文案逐字「**這是「沒有淨值」，不是「折溢價為 0」**；本站不拿最後一次公告的淨值硬戳今天的價格算一個假溢價」；⛔ **把它顯示成 0%（＝「貼近淨值」這個結論）是同一個錯的另一半**。**可否重試：❌** —— 「等當日官方 iNAV 公告；規模小或剛掛牌的 ETF 常態如此。**重按不會讓淨值提早出現**」。⚠️ **`empty` 與 `missing` 在本格寫同一句是刻意的**（上游契約分不出兩者）；`stale_nav`／`iNAV<=0`→**缺漏，不填該欄**（⛔ 不得除 0）。⚠️ **ETF 分支對日線是 fail-loud**（L3 拿不到直接 `raise`）⇒ `failed`→**#6**，與個股的 best-effort **刻意不抹平**。⭐ **三格各自判態**，一格缺⛔ 不把另外兩格染色。**`INDICATOR_SPEC §3` 該列「應然 vs 實然」＝相同（無 `D-*` 編號）** |
| `inspect.etf.dividend` `[A:無此頁]` | ✅ **有實作** —— ETF 判決卡②「配息」。live 值＝`年化 X%`、副標「年化配息率」（線框示意 `5.8%`／`🟢 近 4 季未縮`／時點 `2026-Q2 配息`） | 同 `::_etf_card` 共用建構器 ← `::load_etf_readout` ← L3 `fetch_metrics` → **L1 `src/data/etf/etf_fetch.py::fetch_etf_dividends`（`TTL_1HOUR`）**，欄位 `annual_yield_pct`〔GA〕。⚠️ **三格各自判態、缺值理由各自寫** —— `_etf_card()` docstring 逐字：「共用的只有『怎麼判態、怎麼組卡』；每一格的 label／value／缺值理由**全部由呼叫端給** —— **折溢價缺與配息缺是兩件完全不同的事，共用一句『資料缺漏』等於對其中一邊說了謊**」。〔GB〕規格端 TTL／fallback ⚠️ 待確認 | ⚠️ **待確認 —— 兩組都查不到，⛔ 未猜**：〔`GA ⓐ`〕該欄位在 `fetch_metrics` 後半段計算，GA **未讀到該段的逐字算式**；〔`GB ⓒ`／洞 `INS-G-05`〕查過 `INDICATOR_SPEC §3` 全表 22 列、`§4` 6 列、`S1-2 §1~§3` 全部小節、`DECISION_RULES §1~§9`，**無「ETF 年化配息率」這一列**（`S1-2 §1.4 殖利率`／`§2.12 ETF 月月配健檢` 皆非本欄）。**L5 只格式化**（`_fmt_num(digits=2, unit="%")`） | `empty`→**#7 ⚠️ 應然；實作畫 `▨`**。⭐ 文案逐字承認**兩種都可能**：「**這是一個有效的結果**：可能它真的沒配過息（累積型／剛掛牌），也可能上游這一輪沒回配息序列。**本站不把兩者都寫成 0%，因為 0% 是「不配息」這個結論**」—— ⛔ **不猜是哪一種**。`partial`＝值照給 ＋ `*`（例：只取到 3 季）。可重試：⚠️ **有限**（`where` 指去公開資訊觀測站／發行商對帳；持續缺漏去 📖 憑什麼 › 資料健康度）。**編號 ⚠️ 待確認** |
| `inspect.etf.peer` `[A:無此頁]` | ✅ **有實作** —— ETF 判決卡③「追蹤／同儕」。live 值＝近 12 月分位（線框示意 `0.25`／副標「0 ＝ 同類最強」）；facts：近 3／6／12 月分位、品質評等 stars、成立年數 | 同 `::_etf_card` ← `::load_etf_readout` ← **L3 `dividend_station_service.py::_fetch_peer_ranks`** → **L1 `etf_fetch.py::fetch_etf_peer_history`（`TTL_1HOUR`）**；品質評等來自 L3 `quality` dict（內扣費用／AUM／Beta／殖利率 CV）〔GA〕。⚠️ **本益比欄現行 code 沒有實作**〔GB，線框 flags `['偏離現行 code 現況']`〕。⚠️ **這一格是否現況恆為 empty ＝ `GA ⓗ`**：L3 `fetch_metrics` docstring 逐字「同儕排名（3-3-3 ③）Phase 2 未接 → `peer_ranks=None`」，但 `_fetch_peer_ranks` **確實存在且被 `build_peer_card` 讀** ⇒ **兩者看起來矛盾，GA 未實跑、⛔ 不下結論** | ⭐ **分位（0＝最強、1＝最弱）由 L3 換算好，本頁⛔ 不重新換算**（檔內逐字引 L3 `_fetch_peer_ranks` docstring 的 percentile → 分位方向）。品質評等相關〔GB〕：`INDICATOR_SPEC §4` **ETF 綜合分** 7 維加權 `ret1y .25／cagr3y .20／sharpe .15／mdd .15／expense .12／aum .08／div_cv .05`（Σw＝1.0，gate 需 `>7×0.5` ⇒ ≥4 維）；3-3-3 門檻 `shared/dividend_station_thresholds.py::MIN_INCEPTION_YEARS／MIN_ANN_RETURN_3Y_PCT／MIN_CUM_RETURN_3Y_PCT／PEER_TOP_FRACTION`。L5 只 `_fmt_num(digits=2)` | ⭐ **本頁唯一真正用得到「結構上不適用」＝ `#8 N/A` 的地方**〔GB，線框 evidence 逐字：**本益比那一欄對 ETF 天生不存在**，「**不加 ⚠︎、不寫「壞掉」、也不會叫你去做什麼**」〕；⚠️ **但實作端 #8 未落地** ⇒ 實際畫 `▨`（洞 `INS-G-03`；⛔ **不得把 `missing` 畫成 #8** —— #8 重跑無效，畫錯＝給錯指引）。`empty`（同類檔數不足以排名／同儕段這一輪抓取失敗）→**#7 ⚠️ 應然；實作畫 `▨`**，「算不出就是算不出，**不會拿一個中位數頂替**」、**重按通常不會改變**。**同類 ETF < `PEER_MIN_GROUP_SIZE` → 不適用（非缺漏）** ＝ `D-18`。`partial`＝只湊到近 3 月分位時值照給 ＋ `*`。⭐ 三格各自判態。**編號**：`D-03`（Sharpe）／`D-11`（流動性）／`D-18` |
| `inspect.profit` `[A:無此頁]` | ✅ **有實作（容器 block，動態 key）** —— 💰 獲利能力診斷，區塊標題下一句話「**這家公司每賣 100 元，最後留下多少？**」。🔴 **兩組對格數的描述不同**：〔GA 實作端〕**只有第 1 排 3 格**（毛利率／營業利益率／安全邊際）；〔GB 規格端〕線框要 **3 欄 × 2 排共 5 格**，第 2 排＝稅後淨利率／ROE／（空位） | 🔴 **字面 grep `inspect.profit` ＝ 2，但真正的卡 key 是動態的**：`page_inspect.py::build_profit_cards` 產 **`key=f"inspect.profit.{_key}"`**，`_key ∈ {gross_margin, operating_margin, safety_margin}` ⇒ **3 張子卡**〔GA AST〕。鏈：`::load_profitability` → **L3 `src/services/stock_grp_service.py::get_financial_statements`** → **L1 `financial_statements_fetcher.py::fetch_financial_statements`（`TTL_1HOUR`）** → **L3 `src/services/financial_health_engine.py::analyze_financial_health("", code, fin_data)`** → `["profitability_module"]`。⭐ **`api_key=""` 是刻意的** ＝ 走 L3 **純 no-AI 路徑**（「這三格是數字與門檻，⛔ 不該讓一次 LLM 呼叫決定顏色」）。〔GB〕**來歷逐字：「總管拍板（客戶未表示偏好、授權總管決定）＋2026-09-14」，⛔ 不是「客戶核准」**；線框自陳「**核准線框在這一格自己前後不一致**」（`n` 欄寫 2 排、格子清單只列 3 個），登記於 `wf_global.js::DEV-20`。**TTL／fallback 規格端 ⚠️ 待確認** | 🔴 **五項算式規格端全部查無 ⇒ ⚠️ 待確認，⛔ 不猜**〔`GB ⓓ`／洞 `INS-G-06`：查過 `INDICATOR_SPEC §3` 全表 22 列＋`§4` 6 列＋`S1-2 §1~§3` 全部小節＋`DECISION_RULES §1~§9`，`INDICATOR_SPEC §2 R-1` 僅在分支舉例提到「金融股無毛利率」〕。**程式碼端能追到的只有資料流**〔GA〕：**L1** 原始損益表 → **L3** 算 `Gross_Margin.Value`／`Operating_Margin.Value`／`Margin_Of_Safety.Value` ＋ 各自 `Status` → **L0 `shared/financial_health_thresholds.py`** 供門檻（毛利率 40%／安全邊際 60%…）→ **L5 只讀 `Status` 字串做顯示對映，⛔ 不重新判一次好壞**。已知的判讀語常數＝`GROSS_/OPERATING_/SAFETY_MARGIN_LABELS` 三張對照表（⚠️ **沒有淨利率與 ROE 的**） | ⭐ **三格各自判態**（線框 `errCells` 形狀：毛利率 `▨`／營業利益率 42.1%／安全邊際 `▨`）。**兩種缺值、兩句話，⛔ 不共用**：整張損益表沒回來→`PROFIT_GAP_WHY`（由 **L3 `financial_health_engine.GAP_INCOME_STATEMENT`** 判定，**L5 ⛔ 不自己看營收欄位**＝⛔ 不把 sentinel 複製成第二份）vs 單欄沒抓到／單位異常→`PROFIT_MISS_WHY`；兩者皆 **#7 ⚠️ 應然；實作畫 `▨`**。⭐ **這一組就是線框點名要修的那一格**：現行 `src/ui/tabs/tab_stock.py` 用 `== 'Good'` 布林判定，欄位取不到就落 else ⇒ **紅底 ＋ N/A ＋「辛苦生意」＝把「沒有資料」講成「這是爛生意」**。`_MISSING_STATUSES = {"", "N/A", "None", "none"}` 是**輸入側過濾**，⛔ 非輸出 glyph。🔴 **`degraded` 本頁不會出現**（檔頭逐字「本頁沒有任何一格會判 `UI_DEGRADED`，這是刻意的」）。🔴 **`unwired` 的符號與三份規格打架**（洞 `INS-G-04`）：線框寫 `N/A`（不加 ⚠︎），但 `UI_PAGE_INSPECT ②` 把 `unwired` 對映 **#5 ⛔**、`UI_COMPONENTS §2` 明分 #5 ≠ #8。⚠️ **第 2 排兩格在畫面上連一張「未接線」的卡都沒有**，使用者看不出自己少看了兩個指標（洞 `GA-I-2`）。⚠️ L3 回的 `error`（查無此代碼／額度用罄）**⛔ 不是例外**，原樣進 facts「L3 說明」。⛔ **ROE 那格上游回的是 `Leverage_Warning`（槓桿警語）而非 pass/fail 判決語，⛔ 不得照抄前三格的形狀、⛔ 不得替它編一句中文判讀語**。可重試：✅（季報補齊後自己回來）。**編號 ⚠️ 待確認** |
| `inspect.pattern_measure` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**（型態測幅）。線框 `live` 逐字「**本頁還沒有這一段**」＋ 改名後的欄位提案（`等幅量測位`／`型態起算價位`／`型態失效價位`／`量測位／失效位距離比`） | 🔴 **0 命中**〔GA，`page_inspect.py` grep `型態`／`pattern`／`目標價`／`測幅`／`measure` ＝ **0**；AST 動態 key 盤點亦無涵蓋它的容器〕。⚠️ **唯一沾邊的是** `::build_valuation_card` 的 facts「L2 說明」原樣透傳 L2 `msg`（檔內註解說該句「含三檔目標價或「不適用」的理由」）—— 但那是 **357 估值**的價位，**⛔ 不是型態測幅**，且**沒有獨立 block**。repo 另有 L5 `src/ui/tabs/pattern_targets_ui.py::render_pattern_targets_for_ticker`（既有舊分頁），**本頁⛔ 未掛載**。〔GB〕線框 `src` 指 **L2 `src/compute/strategy/pattern_targets.py`**。**TTL／fallback 不適用**（無實作） | ⚠️ **待確認**〔`GB ⓔ`／洞 `INS-G-07`〕：`等幅量測位 ＝ 頸線 ＋ 型態高度` 是線框給的**欄位提案文字，⛔ 非數學式**；`INDICATOR_SPEC §3` **無此列** | **無態可判**（無實作）；規格端 `unwired`→**#5 ⛔**、`degraded`＝值照給·判決語留白、`na`＝**null**。⚠️ **與 `inspect.stock.detail`／`inspect.etf.detail` 不同**：那兩塊是**已實作的 `wired=False` 灰卡**（畫得出來、會說明少了什麼），本塊是**畫面上完全沒有這個東西** ⇒ ⛔ 不可混為一談。🔴 **合規**〔GB〕：本區塊是**合規改名提案** —— 現行功能名「型態目標·價」，「**目標價**」明列於 `S2-UI_SPEC §9.1` 全站禁用語表（`S1-3B §2.12.4` 另記「全站 16 個 `.py` 檔出現該字樣、真正上畫面 7 處」），替代表述＝**形態測幅參考值**（幾何量測結果、**非價格預測**）；⛔ **全頁禁止買賣建議**，**357 三段價位是門檻反推價，⛔ 不得標成進出場點**。線框 flags `['★待拍板（合規改名）','★待拍板（本頁要不要收）','偏離核准線框','單組結論']`。**編號 ⚠️ 待確認** |
| `inspect.stock.detail` `[A:無此頁]` | 🟡 **已實作，但內容是 `wired=False` 灰卡** —— 「個股明細」未接線卡，**刻意逐項列出少了哪七段**：K 線＋均線 → 357 評價河流圖 → 財報領先指標 → VCP／布林 → 月營收 → 什麼時候買賣 → 心理檢查 | `page_inspect.py::build_detail_card(key="inspect.stock.detail")`（**字面 key，非動態**）；七段字串常數＝`::STOCK_DETAIL_SECTIONS`。**`wired=False` ⇒ 恆為 `UI_UNWIRED`，⛔ 零取數、⛔ 零 L3 呼叫**（`wired=False` 在本頁 AST **恰 1 處**）。facts 三列：「這一段本來會有」／「現行入口」＝🔬 個股分頁／「接線後的樣子」。⚠️〔GB〕`S1-3B §3.3.9` 第 3 項：「8 段明細全部接線」**做不到** —— 目前**只接了 1 段**（＝💰 獲利能力），其餘須收成「⛔ 未接線」卡（**該條 `S1-3B` 自標單組**）。**TTL 無快取／fallback 不適用** | **無計算**（靜態）。⚠️ 圖表：K 線與 357 河流圖**只出現在本段 `live` 文字**，而本段現況 `unwired` ⇒ **目前無任何一塊需要掛圖表容器**；接線時對映 `UI_COMPONENTS §5`「標準」`260px`（`l=8,r=8,t=35,b=20`）。⛔ **不得預先把圖表容器畫成佔位** | **狀態恆為 `unwired` ⇒ #5 `⛔`**；**可否重試：❌** —— 檔內逐字「**按幾次都一樣**」。⭐ **刻意把七段的名字列出來**（逐字理由：「只寫『明細未接線』會讓使用者不知道自己少看了什麼；列出來他至少知道該去舊分頁找哪幾段」）＝ `CLAUDE.md §1` 誠實揭露的落地。⚠️ **本頁⛔ 不重複掛載舊分頁**（會撞 `DuplicateWidgetID`）。規格端十格另給〔GB〕：`na`＝`N/A` 該段（**#8**）／`partial`＝該段標 `N/M`（**#9 `◧`**）／`degraded`→**#4**／`error`→**#6**（「**其餘段不受影響**」）—— ⚠️ **#8／#9 實作端零落地**（見 §0 第三條）。⚠️ 其中「月營收那一段的『等公布』**不是抓取失敗**」（約於次月 10 日前公布）、「歷史只有 12 週算不出 20 週布林」屬「**等時間累積**」＝**重跑一百次也一樣，⛔ 不會叫你重跑**。線框 flags `['★待拍板','未驗證']`。**編號 ⚠️ 待確認** |
| `inspect.etf.detail` `[A:無此頁]` | 🟡 **已實作，但內容是 `wired=False` 灰卡** —— 「ETF 明細」未接線卡，逐項列出少了哪七段：淨值 vs 市價走勢 → 折溢價帶 → 配息紀錄與以息養股試算 → 成分股與集中度 → 同儕 7 維評分 → 標準差買賣帶 → 破發檢查 | 同 `::build_detail_card(key="inspect.etf.detail")`；七段常數＝`::ETF_DETAIL_SECTIONS`。核准線框的明細順序**逐字一致**（另註「標準差買賣帶（`etf_tab_smart` 原樣搬）」）〔GB〕。**TTL 無快取／fallback 不適用** | **無計算**（靜態） | **狀態恆為 `unwired` ⇒ #5 `⛔`**；**可否重試：❌**。⭐ **與個股那張⛔ 內容全換**（檔內逐字「骨架同 A、**內容全換**」）——〔GB〕紅隊逐關鍵詞比對：**13 個個股概念在 ETF 頁 0 命中、6 個 ETF 概念在個股頁 0 命中**，「**合併的是入口與骨架，⛔ 不是內容**」；⚠️ **13／6 兩數線框自標「紅隊比對結果、本組未複驗」，⛔ 不得當成已查證的事實**。`missing`＝「▨ 該段這一輪沒有輸入（例：剛掛牌，沒有足夠的淨值序列）」；其餘十格寫法同 `inspect.stock.detail`。線框 flags `[]`。**編號 ⚠️ 待確認** |
| `inspect.batch.form` `[A:無此頁]` | ✅ **有實作** —— 葉2 批次輸入表單：`st.text_area`（可混貼個股／ETF，逗號／空白／換行分隔）＋ 型別篩選 `st.selectbox` ＋ CTA「🚀 批次分析」＋ 下方常駐 `st.caption(WIRING_DISCLOSURE_BATCH)` | `page_inspect.py::_render_batch_form`。**單一 `st.form(FORM_BATCH_KEY)` ＋ 單顆 submit**，form 內⛔ 無 `st.button`。widget key：`SS_BATCH_WIDGET`／`SS_BATCH_FILTER_WIDGET`。**已套用值寫 `SS_APPLIED_BATCH` ＝ 葉2 唯一 gate 寫入點**〔GA〕。label 常數 `::ACTION_RUN_BATCH_LABEL` ＝ **🚀 批次分析**（`S2-UI_SPEC §11.3` 實測 SSOT）〔GB〕。**表單零取數** ⇒ **TTL 無快取／fallback 不適用** | **解析**：`page_inspect.py::parse_tickers(raw)` —— 逗號／空白／換行分隔，**上限 `BATCH_MAX_TICKERS = 20`**，超出回 `truncated` 計數 | **無缺值態**（表單恆顯示）；`missing`／`na`／`partial`／`degraded`／`unwired`＝**null**〔GB〕；`empty`＝「▨ **你還沒貼任何代碼**」。⭐ **「型別篩選」逐字「只影響顯示，⛔ 不影響取數」** —— 篩掉的列**已經算過**；「使用者會以為篩掉就不抓，**實際上全部都抓**。⛔ 不得在線框裡把它省掉」。⚠️ **超出 20 檔被截掉的那幾檔「沒有查過」**，必須在 `inspect.batch` 的 facts 講出來（⛔ 不得讓使用者以為全查了）。✅ **按鈕對映更正**〔GB〕：葉2 CTA ＝ `UI_COMPONENTS §3` **主 CTA 同款**（實作 `type="primary"`），⛔ **不是「次級」**；但**它不是頁主 CTA**（線框 `mainCTA` 欄只列葉1 那顆）。可重試：✅。**編號 ⚠️ 待確認** |
| `inspect.batch` `[A:無此頁]` | ✅ **有實作** —— 批次總覽卡（**字面 key**，live 值＝`N / M 檔算出來`，label「批次分析結果」）＋ **`st.dataframe` 四欄表**（代碼｜型別｜狀態｜明細）＋ **下鑽 `st.selectbox`** | `page_inspect.py::build_batch_card` ＋ `::_render_batch_leaf` ← `::load_batch_rows` → 逐檔 `::_batch_row` → **同一支 `::_fetch_metrics`** → L3 `dividend_station_service.py::fetch_metrics`（**本頁全部 L3 取數都經過 `_fetch_metrics` 這一支**，所以「出事時說哪一句話」只有一份）。TTL 隨型別：個股 `TTL_1HOUR`（財報）＋`TTL_30MIN`（日線）；ETF `TTL_1HOUR`（日線／配息）＋`TTL_2HOUR`／`TTL_15MIN`（NAV／折溢價）。下鑽 widget key `SS_DRILL_WIDGET`〔GA〕。🔴 **「整列可點」未落地**〔GB，`UI_PAGE_INSPECT ④B` 自標「WM 新發現、須覆核」〕：DEV-19 已拍板恢復整列可點（**來歷逐字「總管拍板（客戶未表示偏好、授權總管決定）」，⛔ 不得寫成「客戶核准」**），但實作 `st.dataframe` 的 `on_select`／`selection_mode` **0 命中**，下鑽走的是另一顆 `st.selectbox`；**分類待判**（實作待修 vs 線框待改） | **逐檔獨立判型 → 各走自己那套欄位**，**無新計算**（沿用葉1 判決卡的算式）。⭐ 表**刻意攤成四欄**、指標以 `key：value` 串在「明細」欄 —— 個股與 ETF 欄位**重疊近零**，硬併成寬表「**會有一半的格子永遠是空的**」。⭐ **下鑽⛔ 零額外 L3 呼叫**（顯示的是同一輪已算完的結果） | ⭐ **「其中幾檔失敗」⛔ 不讓整張卡轉紅** —— 「其餘 2 檔照常顯示，**⛔ 不整批作廢**」，失敗檔數上 facts、**失敗的那一列保留、列首標紅、各格 `—` ＋ ⚠︎**，⛔ **不得把失敗列篩掉**；**紅色只留給「整批都算不出來」**（`failed`→**#6**）。`empty`＝送出但沒解析出任何代碼→**#7 ⚠️ 應然；實作畫 `▨`**（逐字「**這是一個有效的結果**（已經送出，不是還沒送）」）；`missing`＝整批輸入本身沒有→**整張表不畫**，改畫一句說明；`idle`→**#3**；`na`＝`N/A` 該格（例：ETF 列的本益比欄，**#8**，⚠️ 實作端未落地）；`partial`＝「**N/M 檔成功**」。⚠️ **篩選後空表⛔ 不是缺值**：`st.caption` 逐字「**失敗的列不會被篩掉**，所以這裡是空的就代表真的沒有」。⚠️ **原型端無表格能力**（`src/ui_v2/markup.py` 公開函式恰 5 個、**無任何 `<table>`／`<tr>`／`<td>`**），**實作 B 是真的 `st.dataframe`** ⇒ **A/B 相反，洞只在原型端**（`GA-I-3`／缺口 `P1`）。表格三斷點走 `UI_COMPONENTS §4`（手機 ≤640 走卡片流，每卡至多 4 對）。可重試：✅（單獨重試失敗那幾檔即可）。**編號 ⚠️ 待確認** |
| `inspect.evi.cards` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 展開佐證區塊**。規格要的是逐項列：健康度六因子各自的分數與權重、估值 357 三段門檻與對應價位、籌碼 20 日窗口與判定式、**每一項各自的資料時點與來源名** | 🔴 **0 命中**〔GA，`page_inspect.py` grep `inspect.evi` ＝ 0；`section_header` 全清單（11 處）無任何 n4 佐證段；AST 動態 key 盤點無涵蓋它的容器〕。⭐ **線框自己也沒給來源**：`gen_today_v2.py::INSPECT_PAGE_SHAPE_NOTE` 把它列在「**`src` 為 null 的 5 塊**」內。⚠️ **佐證內容在實作端是「併進各張卡的 facts 列」**，⛔ 不是獨立 block（例：357 算式併在 `inspect.stock.valuation` 的 facts「L2 說明」）。〔GB〕**「357 算式」在線框也沒有獨立 block —— 它併在本塊 `live` 的第 2 條列項**（`UI_PAGE_INSPECT ①` 逐字）。**TTL／fallback 不適用** | 〔GB〕數學式逐字取 `DECISION_RULES §3 R3-1`：`cheap ＝ avg_div ÷ 0.07`／`fair ＝ avg_div ÷ 0.05`／`dear ＝ avg_div ÷ 0.03`，皆 `round(x,1)`；`avg_div` 量綱＝**元/股**。SSOT ＝ `shared/thresholds.py::YIELD_*_DEC` ＋ `::classify_stock_357_price`。⛔ **不得在本頁另寫一份算式** | **無態可判**（無獨立實作）。⚠️ ⛔ **不可寫成「已實作」** —— facts 列是**別的 block 的一部分**，`INSPECT_LAYOUT` 要的是 n4 **一個獨立的 1/1/1 塊**。規格端十格〔GB〕：`empty`／`missing`＝展開後寫**為什麼判不出來**（**原樣照出上游給的原因，⛔ 不改寫**）；`na`＝「這類標的天生沒有這個欄位」（⛔ 不寫「壞掉」、⛔ 不叫你去做什麼）；`partial`＝**逐項列出未計入的是哪幾個因子（分子分母都要寫明）**；`degraded`／`unwired`／`error` ⭐ **每一個各給一顆觸發器**（`S2-UI_SPEC §2.2 ★-02` 顆粒度兩級制的**逐元件例外**：「三種情況的原因**逐元件不同，合起來一顆就看不出是哪一個**」）。觸發器規格（`§2.1`）：文字固定「展開佐證 ▸」／展開後「收合佐證 ▾」、**位置在區塊標題列最右側**、行動裝置點擊區 ≥ 44×44 CSS px；⛔ 禁止卡片本身可點／標題可點／hover 自動展開／卡片右上角小箭頭。⚠️ **實作 `st.expander` AST 0 處**（檔內多處明寫「不用 expander」）⇒ 無既有阻礙，但 `st.expander` 控制項在**標題左側、方向相反**。**編號 ⚠️ 待確認** |
| `inspect.evi.overlap` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 區塊**。規格要的是「為什麼兩種標的共用一頁」的**設計理由揭露**：「**合併的是入口與骨架，不是內容** —— 所以你查 ETF 時看不到本益比、查個股時看不到折溢價，**那不是漏了，是它本來就不存在**」 | 🔴 **0 命中**〔GA，grep `inspect.evi.overlap` ＝ 0〕。⚠️ **「重疊近零」這個概念在實作端只出現在 docstring／區塊 caption**（`::_render_etf_branch` 的 `section_header` 副標「骨架同 A、**內容全換**」、`::_render_batch_leaf` 的表格註解），**⛔ 沒有任何一塊卡承載它**；線框端同屬「`src` 為 null 的 5 塊」。〔GB〕落點指定＝`S1-3B §3.3.8` 揭露表第 2 列逐字「**葉1 第四層展開佐證**」；**repo 內只有 ETF × ETF 持股重疊**（`src/compute/etf/etf_calc.py::calc_holdings_overlap_pct`（Σ min 權重）與 `::calc_jaccard_overlap`（`|A∩B|/|A∪B|`），**兩者都吃兩份 ETF 持股**），「某檔個股被哪些 ETF 持有」的**反查查無實作**（單次 grep，0 命中）。**TTL／fallback 不適用**（純揭露文字，`(c)` 逐字「**它不需要任何資料**」） | **無計算**。（若日後要做反查：需新增 L1 取數 ＋ L2 反向索引 —— 「**那是新功能、不在本頁範圍**」） | **無態可判**；規格端**六格＝null**（本頁 null 最多的塊之一）、**無缺值態、無徽章**。🔴 **合規逐字**（`UI_PAGE_INSPECT ③③`）：⛔ **不得寫成、也不得實作成「這檔股票被哪些 ETF 持有」** —— 已登記 **`K14`（否定式引述，未裁決）**，⚠️ **GA ⛔ 不重判**，只據實補一句 code 端事實：**該否定式引述在 `page_inspect.py` grep ＝ 0** ⇒ **K14 的射程在規格／產生器端，⛔ 不在實作端**。⛔ **「13 個／6 個」不得當成已查證的事實、⛔ 不得寫成畫面上的權威統計**；要讓它成為可宣稱的數字須**另派一組逐項重做該比對**。線框 flags `['未驗證']`。**編號 ⚠️ 待確認** |
| `inspect.evi.cost` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 區塊**（成本口徑）。規格要的是 `D-2` 逐字文案：本系統採**加權移動平均法**，與部分券商 **FIFO** 對帳單可能產生差異（＋兩種算法各一句說明＋「兩個都不是錯的，只是口徑不同」） | 🔴 **0 命中**〔GA，grep `inspect.evi.cost`／`成本口徑`／`總費用`／`內扣` 在 `page_inspect.py` ＝ **0**；`內扣` 唯一命中在 `EtfReadout.quality` 的 docstring，描述 L3 dict 內容，**⛔ 不是畫面**〕；線框端同屬「`src` 為 null 的 5 塊」。🔴 〔GB〕**三個未解問題（`UI_PAGE_INSPECT ③②`，⛔ 不代為決定）**：**(1) 落點偏離** —— `S2-UI_SPEC §9.3` 的 `D-2` 落點是「頁4 葉1／頁4 葉2／再平衡區塊」，**裡面沒有本頁**，線框自陳依派工單加入（洞 `INS-G-08`）；**(2) 權重未定義** —— `S1-5 §6 P-1` 指出母法只說「統一採加權移動平均法」、**沒說權重是金額還是單位數**；**(3) 實作不存在** —— `FIFO`／`加權移動平均` 在 `src/`＋`shared/` **全 0 命中**，`avg_price` 是**使用者自己填在 Google Sheets 的欄位**（`src/data/portfolio/gsheet_portfolio.py::_HEADERS`），**系統不計算成本**，且**查一檔頁根本不碰成本**（`成本`／`均價`／`avg_price` 在 `page_inspect.py` **0 命中**）。**TTL／fallback 不適用** | 〔GB〕算式端見 `S1-5 §2.1`：⭐ **`F ≡ B ÷ (q·C)`**（平均買入匯率為**衍生值**，`B`＝台幣成本基底為累加的一等狀態）⇒ `q·C·F ≡ B`（恆等、無誤差）；等價於**用原幣成本金額加權**。⚠️ **本頁未實作任何成本計算**。⚠️ `S1-5` 自陳「**設計文件。未建表、未寫 migration**」＋ §7 列 U-1~U-8「沒能驗到的事」（含「真實 Google Sheets 的行為完全沒驗」「§2 全部算式皆為紙上推導」）⇒ **引用時照此打折** | **無態可判**；規格端**六格＝null**。🔴 **合規硬規則**：⛔ **在「實作不存在」成立的前提下，畫面上不得寫「本系統採加權移動平均法」** —— 那會是宣稱一件系統沒做的事（違 `CLAUDE.md §1`）；⛔ **使用者看到的字面不得出現「FIFO」「加權移動平均」「D-2」「L1」這類內部詞或系統做不到的口徑宣稱**。⚠️ **GB 給的誠實替代文案自標「擬稿、⛔ 非客戶逐字」**。⚠️ 最接近的既有資料是 `inspect.etf.peer` facts 的「品質評等 stars」，其 L3 `quality` dict **內含內扣費用**，但**畫面只顯示 stars、⛔ 不拆出費用** ⇒ **成本口徑在畫面上完全看不到**〔GA〕。**編號＝`S2-UI_SPEC §9.3` 的 `D-2`**（⚠️ ⛔ **不是** `INDICATOR_SPEC §5` 的 `D-02`；落點 ★待拍板） |

### 2. 🔴 `#9` 的母體在本頁有兩種、⛔ 不可互引（本頁最容易被讀錯的一條）

- **`inspect.profit` 的「3 / 5 格」** —— 分母單位＝**格**，講的是**接線度**（5 格裡接了 3 格）。
- **`inspect.batch` 的「N / M 檔」** —— 分母單位＝**標的**，講的是**資料涵蓋度**（M 檔裡算得出來 N 檔）。
- ⇒ 🔴 **母體不同類卻共用 `#9`**：`UI_PAGE_INSPECT ②` 要求「**必須在 tooltip 寫明母體**」，但**規格端沒有給那兩句 tooltip 的文字**（洞 `INS-G-09`，GB 自標「WM 判定，須覆核」）。⛔ **兩個分母不得互相引用當對方的母體**；⚠️ 且 **`#9 ◧` 在實作端零落地**（見 §0 第三條）⇒ 現況兩者都畫不出 `◧`。

### 3. 誠實揭露（`CLAUDE.md §-2` 規則 6）

- **GA（程式碼端）／GB（規格端）兩組皆為單組結論、⛔ 未互相複驗。** 所有「0 命中／只有 N 處／全部」都是單次掃描的**全稱句**，依 `§8.2.A.0` 規則 2（禁窮舉宣稱）**請據此打折**，⛔ 不得作為後續動作的前提、⛔ 不得寫進 commit message／PR 描述當成已完成的事實。⚠️ **本頁⛔ 沒有任何一條經總管親自複驗**（對照：憑什麼頁有 `W-0` 六條、找標的頁有 `F-0` 三條）。
- **GA 有做 AST**（`ast.parse` ＋ `ast.walk` 三支腳本：card key／卡面 `label`·`value`／TTL 裝飾器），但 **⛔ 未實跑任何一頁、⛔ 未跑 pytest、⛔ 未打任何外部 API** ⇒ 上表「畫面顯示什麼」**全是讀 code 推得的，⛔ 不是看畫面看到的**。**GB ⛔ 完全沒有讀任何 `.py`** —— 凡 GB 端出現 `檔名::符號` 一律是**規格文件或線框自己轉述的**；GB 自陳「與 GA 交叉比對時以 GA 的實測為準」，**但本檔⛔ 不代為裁決**，兩組打架處一律並陳（見 `DATA_MAP_GAPS.md §🔬 查一檔頁` 的兩組不一致節）。
- **兩組都沒讀完**：`S1-3B_FULL_DRAFT_SPEC`（287KB）與 `S1-3_IA_WIREFRAME`（127KB）**GB 完全沒讀正文**，只讀到兩頁主規格與線框對它們的**轉錄**；線框各 block 的 `evidence` 欄 GB **多數只讀前 ~600–900 字元** ⇒ **⛔ 不得逕自宣稱「規格沒寫」，只能說「在所掃範圍內沒找到」**。GB 自陳這是本輪**最大的單一風險**：若 `S1-3B` 正文另有 `INS-G-06`（三率＋淨利率＋ROE）的算式，那幾條洞會被推翻。
- ⚠️ **線框 `live`／`partial` 的數字一律是規格示意值、⛔ 非實測**（本頁已指認：`inspect.statusbar.live` 的 `09/13`／`inspect.stock.health.live` 的 `78 / 100`／`inspect.stock.valuation.live` 的 `5.8%`／`inspect.etf.premium.live` 的 `+0.12%`）；**量測值一律帶日期** —— GA 的 AST 量測日 **2026-09-22**・HEAD `415c184`，GB 量測日 **2026-09-22**，本檔彙整日 **2026-09-23**。
