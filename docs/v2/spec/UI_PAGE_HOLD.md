# UI_PAGE_HOLD —— 「💼 我的持股」頁完整版面（第 6 份）

**基準**：線框 `docs/v2/wireframe/wf_page_hold.js`＝版面來源；元件一律引用第 2 份 `UI_COMPONENTS.md`、token 一律引用第 1 份 `UI_TOKENS.md`（⛔ 本檔不改這兩份、⛔ 不新造元件、⛔ 不動線框）。
實作對照 `src/ui/views/page_hold.py::render_page_hold()`（`:3722`，全檔 3751 行）。斷點沿用第 2 份 `≤640 / 641–880 / ≥881`；`n→t` 自動對映與 `grid(MAX_COLS=3)` 規則見第 2 份 §1／第 3~5 份，⛔ 本檔不重述。
⚠️ 本檔為 UI 設計組 WN 單組產出，未經第二組複驗（§-2 規則 6）；**全檔免責只講這一次**。⚠️ **客戶 2026-09-16 已就六件事裁示**（逐條見檔尾「複驗分級」的**客戶裁示**列；客戶逐字就地收錄於各該節，標 ✅）；**除該六件外客戶未看過本份** —— 其餘全檔無「已核准／已拍板／已同意」字樣，除非該處另註出處。
🔴 **本頁是五頁中買賣建議風險最高的一頁**（它同時握有你的持股、你的成本、與一組偏離數字）⇒ ③ 的三條硬禁令是全頁最高約束，凌駕本檔其餘版面規定。

## ① 五層 23 block（WN node 實測：5 層 23 block、2 葉；`cols`／`leaf` 掛在 block 上）

葉：`l1`「戰情室（1️⃣~6️⃣ ＋ 80/20 配置偏離）」／`l2`「組合設定（＝組合管理＋Sheet 綁定）」（＝ code `LEAF_WARROOM_TITLE`／`LEAF_SETUP_TITLE`，`:391-392`，WN 實查逐字相符）。⚠️ **葉不省執行**：`st.tabs` 一輪跑完兩葉 body（`:3733` 註解逐字），葉只是視覺分組。

| 層 | n | block key（leaf・cols D/T/P） | 卡密度 | 主徽章 |
|---|---|---|---|---|
| **第○層** 頁級常駐與入口 | n0 | `hold.run_scope`／`hold.scope_note`（**leaf `null`**，1/1/1）／`hold.scale_disclosure`（l1，1/1/1） | `t3` `6px 9px` | `.sbadge b3` `11px`／min-h 28 |
| **第一層** 今日大白話結論 | n1 | **只有** `hold.cold_start_toc`（l1，1/1/1） | `t1` `15px 17px`；border **2px** | `.sbadge b1` `14.5px`／min-h 46 |
| **第二層** 核心大卡片 | n2 | `hold.conclusion_cards`（l1，**3/2/1**）／`hold.alloc_deviation`（l1，**2/1/1**） | `t2` `10px 12px` | `.sbadge b2` `12.5px`／min-h 36 |
| **第三層** 互動清單／大表 | n3 | l1 七塊：`war_table`(3/2/1)／`swap_compare`／`rebalance_deviation`／`scenario_calc`／`fx_pnl`／`deep_analysis`(3/2/1)／`ai_summary`；**l2 六塊**：`setup`(1/1/1)／`binding`(2/1/1)／`portfolio_count`(2/1/1)／`setup.preview`(1/1/1)／`setup.pick_sheet`(2/1/1)／`setup.watchlist`(2/1/1) | `t3` `6px 9px` | `.sbadge b3` |
| **第四層** 展開佐證 | n4 | `evi_cost`／`evi_take_profit`／`evi_target_pct`／`evi_notready`（**全 l1、全 1/1/1**） | `t4` `5px 9px`；1px **dashed** | `.sbadge b4` `10.5px`／border 0 |

**第○層的兩個特別讀法**：(a) `run_scope`／`scope_note` 的 `leaf:null` ＝ **畫在分頁列之上、兩葉共用**（主 CTA 因此在預設葉可見 —— 靠位置達成，⛔ 不是把 CTA 複製進 `l1`）；(b) `scale_disclosure` 標 `l1` 卻列在第○層 ＝ 它屬葉1 但**十態全部在場、不隨載入狀態消失**（線框 evidence 逐字自陳「矩陣該列十格全寫『常駐』」；WN 實測十格確實 0 null，惟 `degraded` 那格另有替代文案、非「常駐」二字），⛔ 不是「跑到葉外」。
**⑤-a 與 ⑤-b 並存分層（客戶第 3 點）**：⑤-a 在 **n2**（`t2` 核心卡，目標＝**系統常數** `CORE_TARGET_PCT=80.0`／`SATELLITE_TARGET_PCT=20.0`，`shared/dividend_station_thresholds.py:164-165`）、⑤-b 在 **n3**（`t3` 互動表，目標＝**使用者自己輸入**）⇒ **層不同、卡密度不同、天然不會是同一張表**。⛔ 不得為了版面整齊把兩者併表：併了之後「這個 80% 是誰定的」在畫面上就消失了，而那正是 ⑤-a `live` 逐字要防的（「這是**系統內建的預設值，不是你設定的**」）。
**按鈕（一律取第 2 份 §3，⛔ 不新造）**：主 CTA `🚀 跑存股戰情室`（`ACTION_RUN_WARROOM_LABEL`，`:388`＝線框 `mainCTA.src` 指的那一行，WN 實查逐字命中）＝ §3 **主 CTA**（`min-height:40px`／`6px 16px`／`13.5px/700`／底 `--ink`／框 `2px solid --ink`）；`⚡ 生成 AI 總結`（`:3573` `st.button`，AST 全檔 `st.button` **恰 1 處**）＝ §3 **次級（說明）**同規格但**不是頁主 CTA**（線框 `mainCTA` 只列前者）；四個展開佐證觸發器＝ §3 **次級（展開佐證）** 44×44；`loading` 的 submit 停用態＝ §3 **停用態**（⚠️ 見 ⑤-G7）。
**排列／表格／圖表**：裸 `st.columns` **0 處**、`st.expander` **0 處**（WN AST），一律 `grid()`；`st.dataframe` **恰 2 處**。`war_table`／`rebalance_deviation`／`scenario_calc`／`evi_take_profit` 四張表走第 2 份 §4（桌機表頭 `min-height:36px`、列高 `38px`、**數值欄 `text-align:end` ＋ `--mono` ＋ tabular-nums ＋ nowrap**；**≤640 轉卡片流**、每卡至多 4 對 key/value）。⛔ **本頁無任何圖表容器**（`war_table.live` 的「週線與布林 z」下鑽現況未落地，⛔ 不得預先畫佔位）。
**首屏可見範圍**：**三個斷點一律「需實機量測」**（比照第 3~5 份）。⛔ 不猜、⛔ 不得寫推導值冒充量測。**唯一可宣稱的是順序**（不含高度）：`## 💼 我的持股` → `st.caption` → 兩葉 tab 頭（`:3729`）→〔葉2 表單先**執行**〕→ 葉1 body → 葉2 下半。
✅ **斷點不一致 —— 已決：改成三段制（客戶 2026-09-16 裁示）**。**客戶逐字**：「兩塊四段斷點：改成三段制。**四段是舊版殘留，改掉**。」⇒ `hold.rebalance_deviation`／`hold.deep_analysis` 兩塊一律照第 2 份 `UI_COMPONENTS` §4 的 **`≤640 手機 / 641–880 平板 / ≥881 桌機`**（`UI_COMPONENTS.md:10` 逐字，WO 實查命中），**與本頁其餘 21 塊同制**，原「登記為待判」解除。⚠️ **連帶（WO 延伸，⛔ 非客戶逐字）**：**線框端仍寫四段**（`<640 / 641–1280 / >1280`）⇒ **線框待同步**，⛔ **本份不改線框**（線框是基準檔，本檔無授權）。

## ② 狀態覆蓋（線框十鍵 → `UI_COMPONENTS` #1~#10）

WN node 實測：**23 塊十鍵全同序** `live|idle|loading|empty|missing|na|partial|degraded|unwired|error`；**230 格中 186 有值、44 為 null**（`live`／`idle`／`loading`／`error` **四鍵 0 null**；null 集中在 `na` 12／`unwired` 8／`partial` 7／`degraded` 7／`missing` 6／`empty` 4）。`null` ＝「該情況下這塊結構上不存在」，⛔ 不得畫成空卡。

| 線框鍵 | 常數 | 徽章 | 本頁實作現況（WN AST 實測） |
|---|---|---|---|
| `live` | `UI_LIVE` | **#1** 🟢 | ✅ Name **41 處**（全頁最大宗） |
| `idle` | `UI_IDLE` | **#3** ⬜ | ✅ Name **16 處** |
| `loading` | `UI_LOADING` | **#2** ⏳ | ❌ **Name 0 處**、`st.spinner` **0 處** ⇒ **完全未落地**，⛔ 在補上之前不得拿 #3 冒充載入中 |
| `empty` | `UI_EMPTY` ＋ `MISS_NO_INPUT` | **#7** `⚠︎ —` | ⚠️ Name **0 處**，但該態**會出現** —— 走各卡 `else:` 分支間接產生 |
| `missing` | 同上 | **#7** | 同上；⛔ 不得與 `na` 共用符號 |
| `na` | `UI_EMPTY` ＋ `MISS_NOT_APPLICABLE` | **#8** `N/A` | ❌ **`N/A` 全頁 grep 0 命中**，缺值一律畫 `—` ⇒ 見 ⑤-G4 |
| `partial` | 七態無 | **#9** `◧` | ❌ 0 處；⚠️ 依第 2 份 fail-safe：**分子分母拿不到 → 降 #7，⛔ 不得退回 #1** |
| `degraded` | `UI_DEGRADED` | **#4** 🟠 | ✅ Name **7 處** |
| `unwired` | `UI_UNWIRED` | **#5** ⛔ | ⚠️ Name **0 處**，但該態會出現 —— 由 `build_unwired_card(..., wired=False)` 間接產生（AST **恰 2 次呼叫** → 4 張卡） |
| （線框無此鍵） | `emits_level=False` | **#10** ◆ | 本頁不畫；⛔ 不得併進 #8 |

⇒ **會出現在畫面上的是 6 態：#1／#3／#4／#5／#6／#7。** #2 未落地、#8／#9 待接線、#10 不適用。
📌 **2026-09-26 補（上表與上句原文一字未動）**：客戶同日在 `UI_COMPONENTS.md §2` 新增 **#11「▨ 無資料」**（有效的空結果，**登記制**；有意識的規格變更）。本頁**僅 `hold.portfolio_count` 的空態**（`COUNT_EMPTY_NOW`「Sheet 綁好了，但裡面還沒有任何一本組合」，L3 `STATUS_BOUND_EMPTY`）改畫 **#11**，**⛔ 不再是 `empty` 列所寫的 #7**；其餘所有走 `else:` 分支的 `empty` 卡**仍畫 #7**（含 `hold.switch`／`hold.take_profit`／`hold.deep.dividend_cash`「近一年查不到任何一筆配息」—— 審查未登記，理由見 `HANDOFF.md §6.4`）。⇒ 本頁會出現的徽章多一顆 **#11**。
⚠️ **#11 ≠ ③ 的 `▨ 未設定`／`▨ 不計算`**：下段客戶 2026-09-16 逐字「A/C 兩態屬第三類，不發明第 11 種徽章」**原文不動、仍然有效** —— 那兩個是**格內值**的第三類寫法，⛔ 不得據 #11 改畫成徽章。
✅ **`▨` 處置 —— 已決（客戶 2026-09-16 照本份處置拍板）**。**客戶逐字**：「符號 ▨：照你處置。#7/#8 語意改用 ⚠︎ — 與 N/A；A/C 兩態屬第三類，不發明第 11 種徽章。」⇒ **處置內容維持原判、一字不改**：`▨` 在 `wf_page_hold.js` **26 處**，但第 2 份 §2 已於 2026-09-16 **把 `▨` 從 #7／#8 撤掉**（改 `⚠︎ —` 與 `N/A`，複驗分級段逐字「同輪一併修掉 #7／#8 共用 `▨` 的問題」）⇒ **線框的 `▨` 對不到任何一種徽章**。故：**凡屬 #7／#8 語意者一律改用第 2 份符號**；**唯獨 ③ 的 `▨ 未設定`／`▨ 不計算` 例外** —— 它們是**第三類**（見 ③），⛔ **不發明第 11 種徽章**（客戶逐字確認），原「登記待判」解除。⚠️ **連帶（WO 延伸，⛔ 非客戶逐字）**：**線框 26 處 `▨` 待同步**，⛔ **本份不改線框**。　⚠️ **`⚠︎` 必須是 `U+26A0 U+FE0E` 兩碼**（第 2 份 #7 逐字即此二碼）；`N/A` ⛔ **一律不加 ⚠︎**（不適用 ≠ 壞掉）、⛔ **不得放進大字數值區**，走 `facts` 中繼列。⛔ **缺值不得顯示成 `0`。**

## ③ 🔴 三個「零」＋ 三條硬禁令（本頁核心，⛔ 不得以任何理由放寬）

**三條硬禁令逐字**（出處 `S2-PRD.md:252-253`＋`S2-UI_SPEC.md:697-700`，WN 實查兩處命中；線框整包收在 `hold.rebalance_deviation.evidence`，`wf_page_hold.js:474`，標題逐字「⛔ **三條硬禁令（不得以任何理由放寬）**」）。**必須寫進頁面**：

1. ⛔ **禁止「一鍵再平衡」按鈕。** 任何會產生下單清單、或一次套用全部調整的按鈕**都不得存在**。
2. ⛔ **禁止預設最佳配置推薦。** 目標比例**只能由使用者自己填**，系統⛔ 不得預填、⛔ 不得建議、⛔ 不得給「參考配置」。
3. ✅ **只做三件事**：偏離提示／客觀對照／情境試算。

⇒ 落地三則：(a) **輸入欄一律空白**，要示範只用**不可計算的佔位符** `〔＿＿＿＿〕`，⛔ 不放具名標的（線框 evidence 點名 `etf_tab_portfolio.py:211-215` 現行那組四檔具名範例列是本區要防的病）；(b) 情境試算兩欄名**刻意選為**「金額變化(TWD)」與「換算（參考）」，⛔ **不得叫「動作」「建議股數」**（現行 `etf_tab_portfolio.py:822/845/858/868-871` 會印出「📈 買進 0050 約 1,234 股」—— 那是直接買賣建議）；(c) ⑦ AI 總結**不得**把上述禁令繞過去輸出行動語。

🔴 **三個「零」長相⛔ 不得共用同一寫法**（線框硬規則，本份最需要落地的一項）：

| 狀態 | 偏離欄長相 | 它的意思 | ⛔ 禁止 |
|---|---|---|---|
| **A** 尚未設定目標比例（`missing`） | `▨ 未設定` | **沒有值可以算** —— 帳本只有 5 欄，沒有「目標比例」 | ⛔ 顯示 `0.00`；⛔ 系統替你預設一組目標 |
| **B** 已設定目標比例（`live`） | `0.00` | **有效的零** —— 真的剛好等於目標（例：2330.TW 32.10→32.10） | ⛔ 當成缺值處理、⛔ 當成「沒算」 |
| **C** 目標比例總和 ≠ 100%（`degraded`） | `▨ 不計算`，合計格 `⚠︎ 差 8.00` | **你填了，但填的還不構成一個組合** —— 偏離會系統性偏高 | ⛔ 照算並顯示；⛔ 與 A 共用 `▨ 未設定` |

⚠️ **B 態的 `0.00` 是三條線裡唯一的真數字，⛔ 不得被「缺值不得填 0」那條誤殺** —— 那條管的是 A 與 C。
✅ **A/C 兩態的容忍值 —— 已決（客戶 2026-09-16 裁示）**。**客戶逐字**：「容忍值：三個並陳，接線時用 SSOT 常數 1.0pp。判法照 SSOT：abs(...) > tol，**不用 math.isclose**。寫進規格，**不准自己挑一個**。」⇒ **三個數字並陳保留、不刪**（線框 B 態「容忍 **±5.0pp**」／C 態「容許 ±0.1pp」／實作 SSOT `shared/signal_thresholds.py:1271` `PORTFOLIO_TARGET_SUM_TOLERANCE_PP = 1.0`，**互不相同**），**但接線時一律用 SSOT 的 `1.0pp`**，判法一律 `abs(_sum - 100.0) > tol`（`_sum` 先 `round(...,4)`；現行實作對照 `src/compute/etf/portfolio_gates.py:321`，WO 實查命中），⛔ **不用 `math.isclose`**。
⚠️ **這是具名例外、⛔ 不是全稱句（WO 延伸，⛔ 非客戶逐字）**：本檔他處與 `CLAUDE.md §4.3`／§6 的「浮點比較禁止 `==`、一律用容差（`math.isclose`／`np.isclose`）」通則**仍然成立、一條未減**；本條之所以是例外，是因 **SSOT 判法本身已自帶容差 `tol`**，外面再包一層 `math.isclose` 會變成**兩層容差（容差的容差）**、反而讓 `1.0pp` 這個 SSOT 失效。⛔ 不得據本條推出「本專案不用 `math.isclose`」這種全稱句。
📌 **舊分頁的失效模式（本區要防的那個病，線框逐字引用 production）**：`etf_tab_portfolio.py:422` 註解「原碼:`target_pct = actual_pct` → `deviation` 恆 0 → 再平衡永遠印…」⇒ **拿現況當目標 → 偏離恆 0.0% → 永遠綠燈**，而**那個綠燈代表「沒算」不是「已平衡」**（違憲法 §1）。

## ④ ✅ D-3 常駐警語 —— 已決：八處全列、全做（客戶 2026-09-16 裁示）

**完整版逐字**：`⚠️ 你在這裡填的目標比例目前不會被保存`／**短版（手機）逐字**：`⚠️ 目標比例目前不會被保存（重新整理就要重填）`。
✅ **客戶逐字（2026-09-16）**：「D-3 八處落點：**全列，全做**。照規格 `S1-3B:2942`。**不是只做第 #6 處**。」⇒ **已決：八處全列、全做**，與 `S1-3B_FULL_DRAFT_SPEC.md §5.6`／`:2942` 逐字「**八處全部在位**」（WO 實查命中）一致；原「客戶點名 #6 vs 規格要八處」的並陳**解除**。⛔ **不得只做 #6 一處就當完成**、⛔ **不得刪下表**：

| # | 落點 | 寬度／情境 | 形態 |
|---|---|---|---|
| 1 | 葉1 ⑤-b **狀態 A** | 桌機 | 完整版 |
| 2 | 葉1 ⑤-b **狀態 B** | 桌機 | 完整版 |
| 3 | 葉1 ⑤-b **狀態 C** | 桌機 | 完整版 |
| 4 | 葉1 ⑤-b **全部三狀態** | 手機 <640 卡片流 | **短版** |
| 5 | 葉1 ⑤-b **全部三狀態** | 平板 641–1280 | 完整版（寬度夠，⛔ 不縮） |
| **6** | **葉1 ⑤-b 情境試算展開區頂端**（客戶前輪點名；2026-09-16 起與其餘七處**同級必做**） | 全寬度 | **短版 ＋「本試算用的是你這一輪填的值」** |
| 7 | 葉2 組合設定持股列預覽（若該表顯示目標比例欄） | 全寬度 | 完整版 ＋「這一欄不會寫回你的 Sheet」 |
| 8 | 葉1 ⑤-b **第四層展開佐證** `hold.evi_target_pct` | 全寬度 | **技術原因完整版** |

**技術原因（#8 的內容，WN 實查成立）**：帳本 `_HEADERS`（`src/data/portfolio/gsheet_portfolio.py:82`）＝ `['name','ticker','lots','avg_price','updated_at']` **5 欄**；`:379` `new_rows.append([name, tk, lots, avg, ts])` **位置式寫死 5 寬**；`:384-388` `ws.clear()` → 重寫 ⇒ **第 6 欄每存一次被擦一次，且無任何錯誤訊息**。另 `page_hold.py` 的 `st.session_state[...] =` 指派 **AST 0 處**，唯一寫入在 `_ui_kit.py:417-419` 且只寫 `{"mode": ...}` ⇒ **重新整理即消失**。　⚠️ **`S1-3B §5.6` 自陳未窮舉 production 中所有 `target_pct` 畫面落點**（實測另有 `portfolio_gates.py:276,277,300,317,337`／`portfolio_fx.py:42`／`etf_tab_portfolio.py:297,298,422,434`）⇒ **實作時須再獨立掃一次**，⛔ 不得把上表當窮舉。

## ⑤ 🔴 六處「線框說有、實作沒有」

> ⛔ **每一處都分三段：(a) 應然／(b) 現況實測／(c) 要做到需要什麼。⛔ 不得只讀 (a)。**

**G1｜第 0 層整層（含 radio 預設值相反）**
- **(a)** 線框 n0 三塊畫在**分頁列之上、兩葉共用**：radio 逐字「◉ 我的持股（連 Google 讀你的持股清單）　○ 只看市場（不連 Google）」（**我的持股為預設**）＋ 主 CTA ＋ 唯讀承諾「🔒 這一頁只讀不寫 …」；L0-3 要求**主 CTA 必須在預設葉可見**。
- **(b)** **整層在實作中不存在**：`render_page_hold()`（`:3722-3751`）在 `st.tabs`（`:3729`）之前**只有** `st.markdown` 頁標題 ＋ `st.caption`；radio 與 CTA 都在 `_render_setup_form_block()`（`:3742`）、包在 `with _leaf2:` 內，而 Streamlit 預設打開**葉1** ⇒ **「主 CTA 在預設葉可見」現況不成立**（線框標 `★-09 待客戶拍板`）。唯讀承諾頁級查無，只有葉2 的 `READONLY_DISCLOSURE`（`:539-544`）。radio 走 `_ui_kit.py:411 single_submit_form`，**無 `index=`** ⇒ 預設 index 0 ＝ `SCOPE_MARKET`「只讀市場端（不連 Google，也不讀你的持股）」，**與線框相反**。
- **(c)** 把三塊移到 `st.tabs` 之上 ＋ 給 radio 明確預設值。📌 **順帶拆掉一個現有技術債（不只是版面調整）**：`:3731-3739` 有一整段註解 ＋ `tests/test_p04_hold_view.py::TestFormRunsBeforeItsConsumers` 守著「表單必須**執行**在葉1 之前，否則 gate 還沒寫入、按鈕看起來沒反應」—— **搬到 tabs 之上後這個順序問題自動消失**（tabs 之前的 code 本來就先跑）。⚠️ 但**版面佈局異動 ＋ 改預設值**落在 `CLAUDE.md §-1.5.D §03-2 ①`：**須客戶拍板**（⛔ 本檔不代決）；且改預設值後「按一次鈕就對使用者 Google 帳號發一次唯讀讀取」，線框已另開 `★-09b`。

**G2｜⑤-b 與狀態 A/B/C**
- **(a)** 線框 `hold.rebalance_deviation` 要三態各自出表／出警語（見 ③）。
- **(b)** **未接線**：它是 `DEEP_SPECS[0]`（`:2169-2171`，`build_unwired_card(..., wired=False)`），⚠️ 且實作把它掛在 **⑥ 區、不是 ⑤ 區**。**目標比例沒有任何輸入來源**：帳本 5 欄（同上），`target_pct` 在 `page_hold.py` grep **0 命中**。狀態 A/B/C 的判定實作在 L2 `portfolio_gates.evaluate_rebalance_gate:262`，而 **`portfolio_gates` 未被 import**（WN AST 確認；2 處字串命中在 `:174`／`:2178` 皆為 docstring／文案）。
- **(c)** 需要 **(i)** 一個讓使用者填「目標比例%」的畫面元件（＝新增視覺元件 → §-1.5.D §03-2 ① **草稿先行**，⛔ 不在本份授權內）、**(ii)** import L2 gate、**(iii)** 八處 D-3 到位。⛔ **在 (i) 之前不得先接 (ii)** —— 沒有輸入端的 gate 只會回「無法判定」，而那會被誤讀成「已平衡」。

**G3｜情境試算 Checkbox Gate**
- **(a)** 線框 `hold.scenario_calc.idle` 逐字「☐ 顯示情境試算（**勾選後才計算，預設不跑**）—— 沒勾之前本區塊不展開，**也不會先算**」。
- **(b)** **`st.checkbox` AST 0 處**（唯一 `checkbox` 字面在 `:3472` docstring）⇒ **gate 不存在，整個情境試算區塊亦不存在**。
- **(c)** 加一個 `st.checkbox` 把重運算擋在點擊之後。✅ **畫面結構不變的效能 gate ⇒ 依 `§-1.5.F` 判定 7 屬內部自決，⛔ 不是版面異動**（但它承載的表格本身屬 G2 的新元件，仍受 G2 拘束）。

**G4｜匯兌損益欄（永遠 `N/A`）**
- **(a)** 線框 `hold.fx_pnl` 十態**全部** `N/A`，`live` 逐字「**本欄在任何情況下都是 `N/A`**」；`unwired` 態逐字「**不會標成「未接線」**—— 未接線代表『接上就會有』，本欄**接上也不會有**，缺的是資料本身」。
- **(b)** **`匯兌` 全頁 grep 0 命中**（無此欄）；且 **`N/A` 全頁 0 命中**，缺值一律畫 `—`。
- **(c)** 🔴 **總管判定（本份採用）**：客戶要的「永遠 `N/A`」**比現況正確** —— 依第 2 份 #8＝`N/A`（結構上不適用）、#7＝`⚠︎ —`（缺漏），匯兌損益是「帳本沒存買入匯率」＝**結構上不適用**，本來就該是 `N/A`；現況用 `—` 把兩種缺值混成同一符號。⇒ **採 `N/A`，並連帶修掉現況的混用。** 佐證：`src/compute/etf/portfolio_fx.py` 檔頭「已知限制（誠實揭露,§1）」已逐字寫明「本頁沒有那筆資料，故**不估**（估了就是捏造）。**caller 須把這句話顯示給使用者。**」—— **本頁就是那個 caller。**

**G5｜六項「各自 gate」**
- **(a)** 線框 `hold.deep_analysis.live` 逐字「六項並列：再平衡 · 核心／衛星 · 壓力測試 · 風險值(VaR) · 配息現金流 · 葡萄串領息　（**各自 gate**）」，且 `empty`／`error` 逐字「**其餘五項不受影響**」。
- **(b)** **六項共用同一個 submit gate**：`DEEP_CAPTION`（`:2834-2839`）同一段先寫「改**並列 ＋ 各自 gate**（3 欄 × 2 排，不是一排六欄）」，下一句又寫「**它們共用送出鈕那一個 gate**」—— **自相矛盾**。未接線的是 `DEEP_SPECS` 兩項（**再平衡**＝G2；**葡萄串領息**＝實作在 L5、自帶寫死 widget key），其餘四項已接線。
- **(c)** 把三格重取數（壓力測試／VaR／配息現金流，各自逐檔向上游要 Beta／近一年日收／近一年配息）各掛一個獨立 gate。⚠️ **同時要改 `DEEP_CAPTION` 那段自相矛盾的文字**，⛔ 不得只改 code 留著錯的說明。⛔ **3 欄 × 2 排的版面本身已在核准線框內**（線框 evidence 逐字），**故不另計偏離**；但若要改成別的欄數 → 版面異動 → 草稿先行。

**G6｜「有 N 項本來就還沒做好」計數器**
- **(a)** 線框 `hold.evi_notready` 逐字「▸ 有 N 項本來就還沒做好（點開看是哪些）　〔摺疊，但**標題本身永遠在**〕」，且 `unwired` 不摺、`error` **絕不摺起來各自一張紅卡**。
- **(b)** **沒有計數器**：未接線固定 4 張（`DEEP_SPECS` 2 ＋ `SETUP_UNWIRED_SPECS` 2，由 `build_unwired_card` 被叫 2 次產生），**畫面上不顯示 N**。
- **(c)** 需要一個**跨區塊彙總**：把本輪所有 `unwired`／`degraded`／`error` 的項目數加總成 N。⚠️ 依第 2 份 fail-safe，**N 算不出來時 ⛔ 不得退回「沒有未完成項」** —— 那是把缺口掩蓋成完整（違 §1）。

**G7｜（附帶）`loading` 停用態做不到** —— 線框 `run_scope.loading` 自陳「⚠️ **這一格目前做不到**」；WN 實查 `_ui_kit.single_submit_form` **無 `disabled` 參數** ⇒ §3 停用態在補上該參數前**是規格、不是現況**。

## ⑥ 反例自檢（三種）

**A. 全部失敗** —— 表單本身不壞（`run_scope.error` 逐字「送出後的錯誤由下方各區塊**各自**顯示（不會整頁變紅）」）。`war_table.error` 紅時 **① 的「燈號分布」卡同步紅，但 VIX 那一格照常顯示**（`fetch_vix()` → L1 `macro_core.fetch_yf_close("^VIX", range_="6mo")`；門檻 `VIX_LIGHT1/2/3=20/25/30`，`dividend_station_thresholds.py:12-14`，本頁**只印成 facts、不參與判定**）。`alloc_deviation.error` **兩半各自判斷** —— 比例算不出來⛔ 不把「達門檻」那半一起染紅。**四塊佐證常駐可點**（`evi_cost.error` 逐字「真故障時更需要看得到口徑說明」）。⛔ **不得摺起紅卡**、⛔ 大字區不得顯示 `0` 或上一輪殘值。**體質**：WN AST 實測 **13 個 `try`／13 個 handler、bare except 0、`except: pass` 0**（⚠️ INV-8A2 報「14 處」，WN 重跑得 **13**，據實並陳）。
**B. 部分成功** —— 三條**互相獨立**的通道，⛔ 不得合併成一句：(1) `totals["partial"]`「N/M 檔缺張數或均價，沒有納入」；(2) `watchlist_error`「觀察清單讀不到，換入退回選股池」；(3) ⑥ 的 `reconciled=False`／`no_price`／`full_coverage=False` → 轉 **#4 🟠**、數字改掛 facts「現值（已失準，別照門檻讀）」且**燈號留白**（＝「觀測照出、判決留白」）。⭐ **缺的那幾檔一律單獨列出，⛔ 不靜默剔除**；⭐ `alloc_deviation.missing` 逐字「**缺成本就不判** …… 『判不了』不會被寫成『沒達標』」。⚠️ 依第 2 份 fail-safe：**N／M 拿不到 → 降 #7，⛔ 不得退回 #1**。
**C. 沒綁 Sheet** —— 🔴 **灰的 `UI_EMPTY`（#7 家族），⛔ 不是紅的**。文案逐字「**你還沒有綁定持股 Sheet**……這是一個有效的結果……**本站不拿範例持股頂替**」，指路到既有 📁 組合管理分頁。⭐ **「綁了但空」是第三態、文案完全分開**（`scope_note.empty` 逐字「▨ 已讀完，你的帳本裡沒有任何持股列（**這是有效結果**，不是錯誤）」）。⛔ 三者（沒綁／綁了但空／讀取失敗）不得共用同一張卡。⭐ 另有第四種「**這一輪沒讀你的持股**」（radio 選「只看市場」）—— `scope_note.missing` 逐字「**這不代表你沒有持股**」＋ 一鍵修好〔改成讀我的持股，重跑一次〕。
⚠️ **凡指向舊七頁籤的指路句（本頁葉2 有四條指向「📁 組合管理」），⛔ 一律必須寫出「先把左邊側欄切回『不使用（用下方原功能）』」這一步** —— 新五頁與舊七頁互斥（`app.py:605` `st.stop()`），少寫這一步使用者永遠找不到。

## ⑦ ✅ D-2 成本口徑：**落點正確，但已決「先不放」**（客戶 2026-09-16 裁示）

✅ 「頁4」＝ 我的持股**已確認**（`S2-UI_SPEC.md:843` 逐字「頁4 💼 我的持股」，WN 實查命中）⇒ 不像查一檔頁是偏離；D-2 三個現行落點（頁4 葉1 ③ 下方／頁4 葉2／再平衡區塊）**全在本頁內**。線框載體＝ `hold.evi_cost`（n4，`t4` 虛線卡）。
✅ **客戶逐字（2026-09-16）**：「D-2 逐字文案：**先不放**。畫面不能寫『本系統採加權移動平均法』，因為**實作沒有成本計算**。逐字文案**列為獨立任務**，等 L1/L3 補完才放。寫進規格，標明『**L1 未補前不得放**』。」
⇒ **已決三件**：**(a)** 畫面 ⛔ **不得寫**「本系統採**加權移動平均法**，與部分券商 **FIFO** 對帳單可能產生差異。」（該句出自 `S1-3B:864-872`「**D-2 逐字文案（客戶指定必含，本稿釘死）**」首句，WN 實查命中）—— 理由即客戶逐字「**實作沒有成本計算**」，本檔既有實測與之一致（`FIFO`／`加權移動平均` 在 `page_hold.py` grep **各 0 命中**；`avg_price` 是**使用者自己填在 Sheet 的欄位**，`gsheet_portfolio.py:82`）⇒ 照寫即**宣稱一件系統沒做的事**（違 §1）。**(b)** 逐字文案**列為獨立任務**，⛔ **等 L1／L3 補完成本計算才放**。**(c)** 硬規則：⛔ **「L1 未補前不得放」**（客戶逐字）。原 WN「須先查證、若不成立須回報客戶」一項**已由客戶直接裁定**，不再是待辦。
⚠️ **落點判定不受本裁示影響（WO 延伸，⛔ 非客戶逐字）**：上一行「**本頁是 D-2 的正確落點**」（`S2-UI_SPEC.md:843` 逐字「頁4 💼 我的持股」）**仍然成立、⛔ 不得刪除** —— **落點對**與**時機未到**是兩件事；本次決的是「什麼時候放」，不是「放哪裡」。L1／L3 補完後，落點仍是本頁 `hold.evi_cost`。

## ⑧ ✅ 命名衝突 —— 已決（客戶 2026-09-16 裁示）

✅ **客戶逐字**：「結論三卡：用線框改名版（這次讀到的持股／訊號可信度／燈號分布）。**合規理由成立**，改回舊名。改 `UI_PAGE_HOLD.md`，把這條從待裁改成已決。」⇒ **採線框改名版：「這次讀到的持股」／「訊號可信度」／「燈號分布」**。**裁示理由**（客戶逐字「**合規理由成立**」）⇒ 線框 flags 逐字 `["合規改名（卡名過度承諾）","總管拍板（客戶未表示偏好、授權總管決定；2026-09-14）"]` 的**「卡名過度承諾」判定獲客戶確認**：「該做什麼」讀起來像指示動作，落在全頁禁止買賣建議的判準射程內。
⚠️ **「舊名」語意消歧義（WO 判讀，⛔ 非客戶逐字）**：客戶括號內**已明列三個名字**＝**線框那一版**，故其「改回舊名」指的是「**改回線框那一版**」；而本檔原敘述把**實作端**那組（該做什麼／訊號可信度／需要處理）稱為「舊名」、把線框那組稱為「改名版」⇒ **客戶的「舊名」與本檔的「舊名」不是同一組**。**依客戶括號內明列的三個名字為準**，即採線框版。
🔴 **連帶：實作端由此成為待修（⛔ 不得寫成「實作已符合」）**：`build_conclusion_cards()`（`src/ui/views/page_hold.py:1671`，WO 實查）現行三個 label 是「該做什麼」（`:1577`/`:1585`）／「訊號可信度」（`:1635`/`:1638`）／「需要處理」（`:1659`/`:1664`），keys `hold.conclusion.action`／`.confidence`／`.todo`，三張已接線 ⇒ **第 1、3 張 label 須改 code**（「該做什麼」→「這次讀到的持股」、「需要處理」→「燈號分布」）；**第 2 張「訊號可信度」兩版同名、不必動**。改後 `conclusion_cards.live` 的線框範例列（「這次讀到的持股 **12 檔** …… 燈號分布 **加碼 3**」）**與卡名自然對齊**，原「接線時須重新對齊」一項隨之消解。
⛔ **全頁禁止買賣建議**（判準：從「畫面顯示 X」到「讀者該做 Y」之間若需代入只有讀者知道的變數 —— 資金／期間／風險承受度／既有部位 —— 而規格替他填了，就是買賣建議）。本頁所有輸出一律是**偏離提示／客觀對照／情境試算**三者之一；`alloc_deviation` 的「（該減）」與 `evi_take_profit` 的「達門檻」皆為**狀態陳述**（`SATELLITE_TAKE_PROFIT_PCT = 15.0`，`dividend_station_thresholds.py:167`；清單由 L3 `dividend_station_service.flag_take_profit`：`held` ＋ 種類=="個股" ＋ 損益% ≥ 15.0），⛔ 不得延伸為「該賣／該加碼」。⚠️ 實作 `:2022` 現行 label 仍是「80/20 配置偏離（**該減**）」、`:1983` 區段註解仍寫「衛星**停利**」—— **含動作語，登記為合規待處理（須改 code）**。　📌 **2026-09-16 客戶裁示只涵蓋本節首的三卡卡名**（WO 補述）：`:2022`／`:1983` 這兩處**未被該裁示涵蓋**，**登記原封維持** —— ⛔ 不得因「該做什麼」退場就順手宣告這兩處一併解決（它們是不同位置、不同字串、須各自改 code）。

---
⚠️ **複驗分級**

**總管實查**＝(1) 匯兌損益欄採 `N/A` 的判定與其依據（第 2 份 #7／#8 之分、現況 `—` 混用）；(2) 第三層現況六項盤點「全部總管已複驗」（`st.checkbox` 0／`匯兌` 0／`N/A` 0／`portfolio_gates` 未 import／六項共用 gate／無 N 計數器）；(3) G1 (c) 的「搬到第 0 層順帶拆掉 `:3731-3739` 技術債」；(4) 三條硬禁令的**真正逐字**（客戶簡稱不是原字串）。
**INV-8A1 單組（第 0~2 層實作面，未複驗，⛔ 不得當前提）**＝`render_page_hold` 結構、radio 預設值、`READONLY_DISCLOSURE`、結論三卡接線、⑤-a 算式與常數。
**INV-8A2 單組（第 3~4 層實作面，未複驗，⛔ 不得當前提）**＝⑤-b 未接線、目標比例無輸入源、容差判法、停利門檻、VIX 鏈路、AI 總結五種失敗文案與「結果不存 session」、狀態涵蓋計數、排版 0 命中、錯誤路徑三通道與「沒綁 Sheet」三態文案。🔴 **WN 重跑不一致一處**：`try` 數量 INV-8A2 報 **14**、WN AST 實測 **13**（handler 亦 13）—— 已於 ⑥A 據實並陳，**分類待判**。
**INV-8B 單組（線框面，未複驗，⛔ 不得當前提）**＝`wf_page_hold.js` 解析結果。⚠️ **WN 已 node 重跑並確認一致**：**5 層 23 block、2 葉**、`cols`／`leaf` 掛 block、`mainCTA` 逐字（`src` ＝ `src/ui/views/page_hold.py:388`，INV-8B 簡寫為 `page_hold.py:388`，**同一行、非誤**）、十態鍵 **23/23 同序**、`live`/`idle`/`loading`/`error` **0 null**（另實得 230 格中 **186 有值／44 null**，INV-8B 未報此數）、n0~n4 各塊逐字文案與 flags。
**WN 本組實查（2026-09-16）**＝上段標「已重跑」者，另加：`page_hold.py` AST（`st.checkbox` 0／`st.expander` 0／裸 `st.columns` 0／`st.button` 1／`st.dataframe` 2／`st.tabs` 1／`grid` 1／`build_unwired_card` 2／`st.session_state[..]=` **0**／`UI_LIVE 41`・`UI_IDLE 16`・`UI_FAILED 14`・`UI_DEGRADED 7`・`UI_LOADING`／`UI_EMPTY`／`UI_UNWIRED` **各 0**）；grep（`匯兌` 0／`N/A` 0／`FIFO` 0／`加權移動平均` 0／`target_pct` 0／`checkbox` 1〔docstring〕／`portfolio_gates` 2〔皆 docstring/文案〕）；`_ui_kit.py:405-420`（`st.radio` 無 `index=`、無 `disabled`、唯一 session 寫入只寫 `{"mode":...}`）；`shared/dividend_station_thresholds.py:163-167`／`shared/signal_thresholds.py:1271`／`:12-14`；`S2-PRD.md:252-253`／`S2-UI_SPEC.md:697-700`／`:843`／`S1-3B:864-872`／`:2738-2775`（八處落點表）／`:2942`；`wf_page_hold.js:474` 三禁令標題逐字；`▨` 在 `wf_page_hold.js` **26 處**、在 `UI_COMPONENTS.md` 僅 1 處（且是**撤銷紀錄**）；`◧` 在 `warroom_ia_v2.html` **5 處**（⇒ 第 2 份 #9 的出處宣稱成立）。
**✅ 客戶裁示（2026-09-16）＝ 已決（⛔ 不是 WN／WO 判定、⛔ 不是量測；客戶逐字就地收錄於各該節）**＝**(1) ⑧ 結論三卡**採**線框改名版**「這次讀到的持股／訊號可信度／燈號分布」，客戶逐字「合規理由成立」⇒「卡名過度承諾」判定確認；**(2) ④ D-3 八處落點**「全列，全做」，照 `S1-3B:2942`，⛔ 不是只做 #6；**(3) ③ 容忍值**三個並陳保留、**接線一律用 SSOT `PORTFOLIO_TARGET_SUM_TOLERANCE_PP=1.0pp`**、判法 `abs(...) > tol`、⛔ 不用 `math.isclose`，客戶逐字「不准自己挑一個」；**(4) ⑦ D-2 逐字文案**「先不放」、列為獨立任務、⛔「L1 未補前不得放」（落點判定**不受影響、仍成立**）；**(5) ① 兩塊四段斷點**改**三段制**（客戶逐字「四段是舊版殘留，改掉」）；**(6) ② `▨` 符號**照本份處置（#7／#8 語意改 `⚠︎ —`／`N/A`；A/C 兩態屬第三類，⛔ 不發明第 11 種徽章）。　🔴 **本次裁示連帶產生的新待修（三項，⛔ 一項都不得寫成「已符合」）**＝**(a) 實作端三卡 label 待改 code** —— `build_conclusion_cards()`（`page_hold.py:1671`）：`:1577`/`:1585`「該做什麼」→「這次讀到的持股」、`:1659`/`:1664`「需要處理」→「燈號分布」（`:1635`/`:1638`「訊號可信度」兩版同名、不動）；**(b) 線框 26 處 `▨` 待同步**；**(c) 線框兩塊四段斷點待同步**（`<640/641–1280/>1280` → `≤640/641–880/≥881`）。⛔ **(b)(c) 本份不改線框**（無授權）。　⚠️ **⛔ 未被本次裁示涵蓋、原登記全部維持**＝⑧ 末 `:2022`「（該減）」／`:1983`「停利」含動作語；⑥A `try` 13 vs 14 不一致；G1／G2 的版面異動與新增視覺元件（**仍須草稿先行**）；G3～G7 其餘各項。
**WN 本組判定（⛔ 不是量測、⛔ 不是客戶裁示，未標「已決」者全部須覆核）**＝(1) ✅**已由客戶裁示 (6) 取代** ② 末「線框的 `▨` 沒有元件依據、A/C 兩態的 `▨ 未設定`／`▨ 不計算` 屬第三類、⛔ 不發明第 11 種徽章」與其待判分類；(2) ✅**已由客戶裁示 (5) 取代** ① 末「線框在本頁用 641–1280／>1280 四段制、與第 2 份三段制不一致，本檔以第 2 份為準」；(3) ✅**已由客戶裁示 (3) 取代** ③ 末「容忍值三個數字互不相同（線框 ±5.0pp／±0.1pp vs SSOT 1.0pp），⛔ 接線時不得自行挑一個」；(4) ✅**已由客戶裁示 (4) 取代** ⑦ 「D-2 逐字文案在本頁能否照寫取決於未解事實，須先查證、若不成立須回報客戶」；(5) ✅**已由客戶裁示 (1) 推翻** ⑧ 「採客戶指定舊名 ＋ 合規疑慮送客戶確認 ＋ 線框範例值須重新對齊」（客戶改採線框版，合規疑慮**成立**）；(6) G5 「須同時改 `DEEP_CAPTION` 自相矛盾的文字」；(7) G6 「N 算不出來時 ⛔ 不得退回『沒有未完成項』」；(8) G2 「⛔ 在輸入端做出來之前不得先接 L2 gate」；(9) ⑧ 末「`（該減）`／`停利` 含動作語，登記合規待處理」；(10) ⑥A 的 `try` 13 vs 14 不一致與其待判分類。
**窮舉宣稱的方法（逐項標明）**＝凡「0 處／恰 N 處」涉及 `page_hold.py` 的**呼叫與常數名**者，一律 **Python `ast` 全檔掃描**；涉及**字串**者（`匯兌`／`N/A`／`FIFO`／`target_pct`／`▨`／`◧`）為**單次 `grep -cF`**，⛔ 非 AST、⛔ 不得當窮舉；線框計數一律 **node 解析** `wf_page_hold.js`。⛔ **跨檔全稱句（例：「全 repo 只有這一處」）本檔一律未作。**
