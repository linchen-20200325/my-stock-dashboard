# 五頁資料對照表（IA v2 戰情室 · block 級）

> **這是什麼**：五頁戰情室每個 block「畫面上那個東西從哪來、怎麼算、沒資料時怎麼辦」的對照表。
> **射程**：五頁；**本輪已做「今天」頁＋「找標的」頁** —— 客戶確認格式後再做其餘三頁。量測日 **2026-09-22**・branch `ui-v2`。
> 📌 **規格洞與 ⚠️ 待確認一律登記在 `docs/v2/spec/DATA_MAP_GAPS.md`（按頁分節），⛔ 本檔不留第二份。**
> ⚠️ **本檔是現況記錄，⛔ 不構成主動動工的授權**（`CLAUDE.md §-1`）。⛔ 全檔不寫行號（`§8.2.A.0` 規則 1）；TTL 一律寫 `shared/ttls.py` 常數名；查不到一律「⚠️ 待確認」，⛔ 不猜、⛔ 不發明資料源／算式／中文標籤。

---

## 🚦 今天頁

### 0. 讀表前提 —— repo 裡有**兩套**今天頁（總管親自複驗；⛔ 少了這段，下表每一列都會被讀反）

- **A＝原型** `src/ui_v2/`（6 個 `.py`）：總管以 **AST** 掃描（非 grep），取數 import **＝零** —— 只有 `__future__`／`html`／`src.ui_v2.*`／`streamlit`／`types`／`typing` ⇒ **原型對應的那一套，八塊全部未接線**。
- **B＝現行** `src/ui/views/page_today.py` ＋ `src/ui/tabs/tab_today.py`：總管實測五頁各有一個實作（`page_today` 118KB／`page_why` 126KB／`page_find` 100KB／`page_inspect` 147KB／`page_hold` 223KB），`CLAUDE.md §8.2` 自陳「IA v2 五頁戰情室 View 層，**逐頁落地中**」⇒ 線上實際會動的是這一套。
- **⇒ 下表「資料來源」一律填 B**（＝資料**現在實際**從哪來），A 的接線狀態另以 `[A:未接線]` 標記。**決策者：AI 總管（技術判斷）** —— 照 A 填則八列全是「未接線」、表沒有資訊量；照 B 填，這張表才是**接線藍圖**。⚠️ ⛔ **B 有值 ≠ 今天頁已經做好了**：兩套共用同一組 block key，**接線狀態相反**。
- 🔴 `today.holdings`：總管複驗 `today.holdings` 在 `src/ui/` **0 命中** ⇒ **A、B 兩套都沒接**（`build_today_blocks()` 只產 7 塊）。

### 1. 八個 block

| block key | 畫面顯示什麼 | 資料來源 | 計算 | 缺值處理 |
|---|---|---|---|---|
| `today.statusbar` | 3 張狀態卡：**交易日**／**總經**／**Sheet 綁定** | **總經格**＝L3 `src/services/macro_state_locker.py::get_macro_state`；**欄位**＝契約 9 key（`regime`/`light`/`source`/`trend_regime`/`health`/`defense`/`exposure_limit_pct`/`traffic_light`/`is_loaded`，🔴 **無 `as_of`**）；**頻率**＝每次 rerun；**TTL**＝**無**（非 `@st.cache_data`，吃 session ＋ 讀 `macro_state.json`；上游 L1 為 `TTL_1HOUR`）；**fallback 4 層**（上層贏、⛔ 禁平均）：`warroom_summary['effective_regime']` → 原始欄位重算 `shared/regime_arbiter` → `macro_state.json['market_regime']` → `'unknown'`（⛔ **不是** `'neutral'`）。**交易日格／Sheet 格＝未接線**（`src/ui/tabs/tab_today.py::_staged_card`）—— 見洞 ②③。`[A:未接線]` | **無計算**（直讀 L3 契約；L3 端是來源優先序查表，⛔ 不是數學式） | `src/ui/tabs/tab_today.py::classify_macro_contract` → `shared/ui_state.py::classify_ui_state`：未載入→**#3 ⬜**（**可重試**＝按主 CTA）／快照來源→**#4 🟠**「總經位階**來自快照，時間不明**」（觀測照出、判決留白）／契約拋例外→**#6 🔴**／其餘→**#1 🟢**；兩個未接線格→**#5**（**重按無效**）。**逐格獨立判態**，一格壞⛔ 不染色另兩格。**編號**：`DECISION_RULES R6-1`・`INDICATOR_SPEC R-3`；徽章清單 ⚠️ 待確認（洞 ⑦） |
| `today.verdict` | **應然**（`UI_PAGE_TODAY ①`，user 2026-09-16 裁示）＝**單一結論燈 ＋ 一句話結論**，一格滿版 `t1`；**實然 B**＝仍畫 **3 張並排卡**（`verdict.exposure`／`verdict.danger`／`verdict.regime`）⇒ 規格已判**實作待修** | `exposure` ← L3 `src/services/allocation_service.py::get_allocation`（→ `src/services/macro_state_locker.py::get_macro_state` ＋ L0 `shared/position_throttle.py::compute_position_throttle` ＋ `shared/allocation_decision.py::build_allocation_decision`）；**TTL**＝**無 `@st.cache_data`**，改 **session 簽章快取**（`cl_ts`／`health`／`regime`／`defense`／`exposure_limit_pct`／caps 任一變動即失效）；**fallback** 同上 4 層。`danger` ← L5 `src/ui/tabs/tab_macro_v2.py::overall_verdict` ← L2 `src/compute/macro/macro_helpers.py::compute_five_bucket_summary(readiness_out=)`；門檻 SSOT `shared/macro_buckets.py::BUCKET_DANGER_SPECS`；**fallback**＝規格逐字「**無多源；值取不到即 gray**」。`regime` ← 同 statusbar。**頻率**＝每次 rerun。`[A:未接線]` | **多層**：L1 抓 → **L2** `health = Σ(vᵢ·wᵢ) ÷ Σwᵢ`（`i ∈ 有值腿`；`HEALTH_WEIGHT_JQ=0.6`／`HEALTH_WEIGHT_SCORE=0.4`）→ **L3** `compute_position_throttle(health, regime, defense)` 分段查表 → `build_allocation_decision` **取 caps 的 min**（3 條硬否決）→ **L5** danger 側：桶內 worst-of（`red>yellow>green`，`gray` 不在 `LEVEL_RANK`、**無分母不 rescale**）→ 五桶再 worst-of。⚠️ **並列揭露**：危險度與位階⛔ 不得平均／取 worst／合成第三顆 | 兩腿全缺 → `health=None`（⛔ **不是 0 分**、⛔ 也不是 🟡）→ `is_loaded=False` → **#3／#7**，大字區**留白**，⛔ 不以缺值推導一個安全水位；`alloc_error` → **#6 🔴** ＋ `where=EXIT_FIX_CODE`（**重按不會好**）；partial → **恆 fail-safe 降級 #7**（⛔ 不得退回 #1）。符號 **#7 `⚠︎ —`（缺漏·可重跑）vs #8 `N/A`（不適用·重跑無效）**，**以 `miss_reason` 分辨、⛔ 不得只靠顏色**。**編號**：`DECISION_RULES R1-1／R1-2／R1-3／R6-1`・`INDICATOR_SPEC R-2／R-3`＋`§3「總經健康評分」列`＋`D-16` |
| `today.summary` | **位階／動能／風險 三張獨立卡**（客戶 2026-09-22 裁示 B：`BLOCK_COLS=(1,1,1)`，卡內⛔ 不巢狀網格；層級網格不變） | **位階＝已接線**：`src/ui/tabs/tab_today.py::build_today_blocks` 直接複製 `statusbar.macro` 那張卡 ⇒ 來源／欄位／頻率／TTL／fallback **完全同 `today.statusbar` 總經格**。**動能＝未接線**（`_staged_card`）—— 客戶 2026-09-16 明示「**exposure 不是動能，不搬**」⇒ **刻意的洞**（洞 ⑩），⛔ 不得拿 `verdict.exposure` 頂替。**風險＝未接線**（`_staged_card`）；規格已指定接法（客戶 2026-09-16「**風險 ← `danger`，直接對映**」），**實作未落地**。`[A:未接線]` | 位階＝**無計算**（直讀契約）；動能＝**無計算（未接線）**；風險＝（接上後）同 `verdict.danger` 的 `classify_danger` ＋ worst-of | **逐格獨立判態**（`S1-4 §3.0 C-1`）—— 一格壞⛔ 不染色另兩格。動能／風險的**常態就是 #5「未接線」**（⛔ 不得畫成 #1），且 **#5 沒有出口**（`S1-4 §3.1`：`where` 明說沒有出口，**這一態沒有你可以做的事**）。**編號**：`DECISION_RULES R1-1／R1-3／R6-1`・`INDICATOR_SPEC R-3`＋`§1 維度 B` |
| `today.key_banner` | 一張 tile（`cols=1`）：今日關鍵警示 chip（`emoji`／`severity`（0 紅／1 黃）／`text`／`detail`／`layer`）＋ `n_red`／`n_yellow` 計數 | L2 `src/compute/macro/daily_key_alerts.py::collect_key_alerts(threshold_alerts, macro_info)`。**輸入①門檻層**＝`session['macro_alerts']`，唯一寫入點 `src/ui/tabs/macro/section_mid.py` ⇒ ⚠️ **今天頁主 CTA 摸不到**（`src/services/macro_refresh_service.py::UNTOUCHED_BLOCKS`，洞 ⑱）。**輸入②急變層**＝`session['macro_info']` ← L3 `src/services/macro_trio_orchestrator.py::run_macro_trio_and_persist` ← L1 `src/data/macro/macro_snapshot.py::fetch_vix_block`／`fetch_cpi_block`／`fetch_fed_funds_block`／`fetch_us10y_block`／`fetch_tw_pmi_block` 等。**TTL＝`TTL_1HOUR`**。**fallback**：`_cache_success_only` ＝ **只快取成功結果、失敗穿透下次真重試**（`CLAUDE.md §1.A` 第 3 點）；M1B/M2 走 CBC → FRED → IMF（⛔ 禁跨幣別平均）；PMI 走 `macro_core.PMI_SOURCE_REGISTRY` 賽跑取第一命中（⛔ 不平均）。🔴 **規格目錄內 0 命中**（洞 ④）。`[A:未接線]` | **L1 抓 → L3 寫 session → L2 合併排序**：`items = _threshold_items(threshold_alerts) + _delta_items(macro_info)` → 依 `severity` 升冪（紅先）→ `n_red`／`n_yellow` ＝各 severity 的計數 | 🔴 **本 block 最重要的一條：`items == []` ⛔ 不等於「今天沒事」** —— 三種情況同得 `[]`：`None`（門檻層這輪沒跑＝**未評估**）／`[]`（跑了但 snapshot 全 `None` ＝**無資料可評**）／非空全綠（**真的評估過且無異常**），**只有第三種才准顯示綠燈**；caller **必須**傳 `threshold_scanned = bool(session['macro_alerts'])`。未評估 → **#3／#5 灰**；`collect_key_alerts` 拋例外 → **#6 🔴** ＋ 出處 `SRC_KEY_ALERTS`（**L2**，⛔ 不謊報成 L3 契約）。**可重試**：急變層那半可；**門檻層那半重按不會好**（寫入點不在本頁射程）。**編號**：⚠️ **無專屬編號**（洞 ④；⛔ **不得**拿 `DECISION_RULES D-32` 頂替 —— 規格未綁） |
| `today.actions` | 主 CTA **`🚀 更新今日戰情`**（`type="primary"`，**每頁首屏唯一一顆**）＋ 同 form 內 `st.radio("更新模式")` 兩選項：`正常更新（吃暖快取）`／`強制重抓（清快取）` | **無資料源 —— 它是操作，不是觀測。** **字串 SSOT**＝L0 `shared/ia_nav.py::ACTION_LABELS[ACTION_UPDATE_TODAY]`（⛔ 不得手抄）。**行為**＝`src/ui/views/page_today.py::_render_update_form` → L3 `src/services/macro_refresh_service.py::refresh_macro_now(mode=)`（`MODE_WARM`／`MODE_FORCE`；後者呼叫 `clear_macro_caches()`）。**射程 SSOT**＝`UNTOUCHED_BLOCKS`（本按鈕摸不到的 6 塊，逐塊帶 `session_key`／`why`／`writer`）。**TTL**＝不適用。`[A:未接線]` | **無計算。** 狀態機 `src/ui_v2/page_today.py::main_cta_state(failure_reason=)`：`None` → 可按；`MISS_CONTRACT_DRIFT` → 見右欄；其餘 → 可按 ＋「可以重試 —— 上游這輪失敗，不是程式錯誤。」 | 🔴 **本格有一條規格 vs 規格的直接牴觸（契約漂移時停用 vs 照常可按）→ 見 §3，⛔ 本檔未裁決。** 其餘：一般上游失敗 → **可按 ＋「可以重試」**；模式對不上 → `st.error` 出聲，⛔ 不替使用者挑一個模式跑。**編號**：`INDICATOR_SPEC R-1／R-2／R-3`＋`§1 三態表 MISS_CONTRACT_DRIFT 列`・`DECISION_RULES R9-6`（`⚠︎ —`＝缺漏·可重跑／`N/A`＝不適用·重跑無效）；徽章清單 ⚠️ 待確認（洞 ⑦） |
| `today.warroom` | 2 張卡：`warroom.todo`「今天該做的事」／`warroom.ai`「AI 摘要（預設收合）」 | 🔴 **兩張全部未接線，A、B 兩套皆然** —— `src/ui/tabs/tab_today.py::_staged_card`（`now` 分別為「尚無作戰項目」／「AI 摘要尚未接線」）。規格只給一句語意「**作戰室由五桶結論推導**」，**⛔ 未給欄位／頻率／TTL／fallback／endpoint**（洞 ⑤）。⚠️ repo 內有語意相近模組（`ai_structured_summary`／`daily_checklist`／`market_strategy`），但**⛔ 沒有任何一條線接到本 block** ⇒ ⛔ 不填（那是「找一個應該會用到的來源」＝本輪明文禁止的事）。`[A:未接線]` | ⚠️ **待確認 —— 規格未給任何推導式**（待確認 ⓔ），⛔ 本檔不補 | `state=UI_UNWIRED` → **#5「這項還沒做」**，大字區留白，**重按無效／沒有出口**；**逐子項各自標，⛔ 不整塊染色**。⭐ 合規硬規則：⑤ 的「3 件事」⛔ 不得代入使用者才知道的變數（資金／期間／風險承受度／既有部位），⛔ 不得寫「該加碼／該減碼／建議持股 X%」。**編號**：`DECISION_RULES R1-1 結構性旗標①`（`wired=False` → **不進分母、不計未評估**）／`R9-4`・`INDICATOR_SPEC §1 維度 B`；**內容怎麼產**⚠️ 無編號（洞 ⑤） |
| `today.detail` | **應然（線框七段）**＝A 市場狀態／B 長期／C 中期／D 短線／E 籌碼／F 全球風險／G 跨桶裁決；**實然 B**＝改畫**五桶**（`shared/macro_buckets.py::BUCKET_ORDER` ＝ `long`／`mid`／`short`／`chips`／`news`）＋ 五桶摘要 ＋ 逐盞燈 tile。⚠️ 落差已就地揭露（`src/ui/views/page_today.py::SEGMENT_COVERAGE_NOTE`）⇒ ⛔ 不擺空殼假裝有結構 | L3 `src/services/section_inputs.py::load_section_inputs(session)`（**只從 session 取**，12 key：`macro_info`／`mkt_info`／`warroom_summary`／`m1b_m2_info`／`bias_info`／`cl_data`／`li_latest`／`jingqi_info`／`_macro_news_items`／`_last_inst`／`cl_ts`／`futures_net`）→ L2 `compute_five_bucket_summary`。**16 盞門檻 SSOT**＝`shared/macro_buckets.py::BUCKET_DANGER_SPECS`（欄位 `key`／`label`／`bucket`／`unit`／`direction`／`yellow`／`red`／`valid_min`／`valid_max`／`wired`／`discriminative`／`emits_level`）。**頻率**＝每次 rerun。**TTL**＝上游 L1 六個 macro block 皆 `TTL_1HOUR`；`macro_core` 另有 `_MACRO_CACHE_TTL_DAYS`（PMI／出口過期快取）。**fallback**：`macro_core.PMI_SOURCE_REGISTRY`（PMI 賽跑）／`tw_macro.CBC_MS1_URLS`（M1B）／`src/data/core/data_registry.py::DATA_REGISTRY`（`state_key` 欄把 endpoint 對回 session 路徑）；⚠️ 五桶端規格逐字「**無多源**」（洞 ①⑨）。`[A:未接線]` | **四層**：**L1** 各 fetcher 抓 raw → **L3** `load_section_inputs` 取 session（＋一處 `jingqi` 相容回填）→ **L2** 產 `{bucket: {level,label,headline,color,emoji,details}}` ＋ **readiness 側車** → **L5** `tab_macro_v2` worst-of。**逐燈**：`high_bad` ＝ `v ≥ red → 紅`、`v ≥ yellow → 黃`、else 綠；`low_bad` 反向；`band` 雙邊。**16 盞不加權**（唯一權重在 `health` 內部，見 `today.verdict`）。**分母句逐字**（`Coverage.text()`）：`訊號可信度 {live}／{total} 個指標正常運作中（無資料 {gray} · 已失準 {stale} · 未接線 {unwired} · 故障 {fault}）` —— ⚠️ `unwired` 與 `stale` **都在分母裡**（藏起來會製造永遠 100% 的假可信度） | `value is None` 或 `float()` 拋錯 → `gray` ⬜（檔內明文「**不偽綠**」），**gray ⛔ 不參與 worst 計算**。翻譯表 `src/ui/views/page_today.py::READINESS_REASON_TO_MISS`：`no_extraction`／`out_of_range` → `MISS_CONTRACT_DRIFT`（**✅ 升紅**；**不翻譯就永遠不會升紅、會被畫成一般灰態而沒有人去修**）；`no_value`／`not_loaded` → `MISS_NO_INPUT`；`not_wired` → `MISS_NOT_APPLICABLE`。畫面 **#7 `⚠︎ —`（缺漏·可重跑）vs #8 `N/A`（不適用·重跑無效）**，**以 `miss_reason` 分辨、⛔ 不得只靠顏色**（兩者同屬灰系）。**#4 🟠 degraded**（`discriminative=False`）→ 觀測照出、判決留白；**#5 unwired**（`wired=False`）→ 不進分母、不計未評估；**#9 partial** → 本頁 tile 層查無分子分母 ⇒ **恆 fail-safe 降級 #7**。**逐桶判態**，一桶失敗**其餘四桶維持原樣**，⛔ 不拿估計值頂替。**編號**：`DECISION_RULES R1-1／R1-2／R1-3／R9-5／D-32／D-33`・`INDICATOR_SPEC R-2／R-3`＋`§3「五桶 16 燈」列` |
| `today.holdings` 🆕 | 一張 `.blk.t2` ＋ 一顆 `b2` 徽章 ＋ 卡內 key-value（**白名單 9 項：`n_total`／`n_classified`／`n_unclassified`／`n_industries`／`coverage_pct`／`top1_pct`／`top3_pct`／`hhi`／`n_eff` —— 是「至多這些」，⛔ 不是「至少這些」，渲染端可只畫子集**）＋ **等權假設揭露句一行（必填）**。🔴 ⛔ **不得顯示**：損益／部位佔比／金額／張數／均價 | 🔴 **A、B 兩套都沒接**（總管複驗：`src/ui/` **0 命中**）—— 目前只存在於契約常數 ＋ 原型 demo 值裡。**規格指定的來源（存在，只是沒接過來）**：L1 `src/data/portfolio/gsheet_portfolio.py::load_stock_watchlist`（worksheet `stock_watchlist`，**schema 只有 `['name','ticker','updated_at']` ⇒ 這張卡拿不到部位大小**），**TTL＝`TTL_15MIN`**，**fallback＝無**，頻率＝事件驅動（user 編輯）；產業別 L1 `src/data/core/data_loader.py::fetch_industry_category`（FinMind `TaiwanStockInfo`，欄位 `industry_category`，**TTL＝`TTL_1DAY`**，**fallback＝無**，失敗回 `''` ⇒ 該檔記為未分類）；L3 `src/services/stock_grp_service.py::get_portfolio_concentration`（去重＋大寫正規化、**逐檔降級**，⛔ 不中斷其餘檔）；L2 `src/compute/risk/concentration.py::compute_industry_concentration`。頻率＝隨頁面 | **三層**：**L1** 抓（清單 ＋ 逐檔產業別）→ **L3** 逐檔降級聚合 dict → **L2** 純函式算集中度。**算式見 §2**（`w_i`／`Top1`／`Top3`／`HHI`／`Neff`／`coverage_pct`／`n_unclassified`）。⛔ **本模組不提供任何燈號／門檻**（user 2026-08-14 裁示、2026-09-16 再次拍板維持）—— DOJ 的 HHI 1500/2500 是衡量**產業市場結構**用的，套到投資組合沒有依據，憑空定一個就是新的 magic number（`CLAUDE.md §3.3`） | `n_classified == 0` → `is_computable=False`，`hhi`／`n_eff`／`top1_pct`／`top3_pct` 皆 **`None`** → 徽章 **#7「缺漏·可重跑」**（Sheet／FinMind 失敗**可重試**）；🔴 ⛔ **不得顯示 `0` 或「完美分散」**（那是把「不知道」渲染成「很好」）。`0 < n_classified < n_total` → **只對已分類部分計算 ＋ 強制同時顯示 `coverage_pct`**，⛔ 不得靜默縮分母。空投組（`n_total=0`）→ `is_computable=False`，**⛔ 不拋例外**（空投組是正常狀態）。`hhi <= 0` 或非有限 → `n_eff = None`（⛔ 不吐 `inf`）。`basis='equal_weight'` → **UI 強制揭露等權假設**，⛔ 不得把等權結果講成實際權重。**整塊一顆徽章**（⛔ 不是逐格）。**編號**：`DECISION_RULES R4-1`＋`§4 已決事項`＋`D-31`・`INDICATOR_SPEC R-3`；⚠️ **`INDICATOR_SPEC §3` 查無此指標列**（洞 ⑫ —— 表內「籌碼集中度」是**另一個量**） |

### 2. `FIELD_LABELS_TODO.md` 那 5 個無中文標籤欄位 —— **位置確認**（⛔ 本輪不給標籤）

- **兩組獨立確認、結論一致：5 個全部只在 `today.holdings`（第二層 · 持倉健檢卡）** —— `n_unclassified`／`coverage_pct`／`top3_pct`／`hhi`／`n_eff`。定義處＝`src/compute/risk/concentration.py::ConcentrationResult`；白名單＝`src/ui_v2/page_today.py::HOLDINGS_DISPLAY_FIELDS`；規格唯一出處＝`UI_PAGE_TODAY ① today.holdings` 的「可顯示」白名單句。⚠️ 規格只規定**屬於哪個 block**，⛔ **沒有**規定卡面第幾條。
- **算式逐字**（`S1-2_METRIC_SSOT §2.11(A)` ＝ `concentration.py` docstring，兩組逐字一致；設已分類共 `M` 檔、分屬 `K` 個產業、產業 `i` 有 `n_i` 檔）：`w_i = n_i/M`（`Σ w_i = 1`）・`Top1 = max_i w_i`・`Top3 = Σ_{i∈top-3} w_i`・`HHI = Σ w_i²`（`1/K ≤ HHI ≤ 1`）・`Neff = 1/HHI`（`1 ≤ Neff ≤ K`）；`coverage_pct = n_classified/n_total×100`（`n_total==0` → `0.0`）・`n_unclassified = n_total − n_classified`。⚠️ **後兩條只有 code 端有** —— `S1-2 §2.11(A)` 算式段⛔ 沒給（洞 ⑧）。
- 🔴 **陷阱（DA 組實測，⛔ 不可照抄）**：`src/ui/tabs/stock_grp_sections/section_industry_concentration.py::render_industry_concentration_section`（**🏆 個股組合分頁，⛔ 不是今天頁**）對 `n_eff`／`top1_pct`／`top3_pct` 已有 production 中文字串（「有效產業數」／「**最大產業佔比**」／「前三大合計」）—— 但「最大產業佔比」與 `UI_PAGE_TODAY ①` 明文要求 `top1_pct` 的逐字標籤「**最大單一產業（檔數等權）**」**直接衝突**，照抄＝違反規格明文；且 `DECISION_RULES` 出現的「有效產業數」是在描述**應該印什麼**、⛔ 不是在替 `n_eff` 指定標籤（與 `n_industries` 極易混淆）。`n_unclassified`／`hhi` 全 `src/` **查無中文標籤**。
- ⛔ **本檔未替任何一個欄位發明中文標籤**（客戶逐字：「**不是現在就給標籤，是先確認位置**」）。

### 3. 🔴 規格 vs 規格的直接牴觸（`today.actions` 主 CTA 在契約漂移時；就地標明、⛔ 本檔不裁決）

- **(a) `UI_PAGE_TODAY ④`**：`MISS_CONTRACT_DRIFT`（在 `FAILED_REASONS` 內）⇒ 按鈕**停用** ＋「🔴 **重按不會好，程式要修，請回報。**」⛔ 不得對契約漂移給「可重跑」指引。
- **(b) `S1-4_STATE_MATRIX §3.7`**：⛔「**在 P2 落地之前，『停用』做不到**」（`single_submit_form()` 尚無 `disabled` 參數）—— 正確做法是**照常可按**，讓文字講清楚「按了也不會變」（**文字誠實 > 假裝按鈕壞了**）；並且「**絕對不要**為了『看起來停用』而把按鈕藏起來」。
- ⇒ **兩份規格對同一格給了相反指令。總管已親自讀過兩邊原文。⛔ 本檔不自行裁決**（客戶 standing rule：規格有洞就地標明），等客戶／總管拍板。

### 4. 規格洞與 ⚠️ 待確認 —— **整段已搬到 `docs/v2/spec/DATA_MAP_GAPS.md` 的 `## 🚦 今天頁`**（編號 ①~⑲／ⓐ~ⓕ 與來歷前綴 `G-*`／`H-*`／`N-*`／`W-*` 原樣保留；⛔ 本檔不留第二份 ＝ 避免第二真相源）

### 5. 誠實揭露（`CLAUDE.md §-2` 規則 6）

- **DA／DB 兩組皆為單組結論，⛔ 未經第二組複驗**；所有「0 命中／只有一處／全部未接線」都是單次掃描的**全稱句**，依 `§8.2.A.0` 規則 2（禁窮舉宣稱）**請據此打折**。⛔ 不得作為後續動作的前提。
- **DA（程式碼端）⛔ 未做 AST**（`src/ui_v2/` 那一道 AST 掃描是**總管另補**的）、⛔ 未實跑任何程式，「畫面會顯示什麼」全是讀 code 推得；**DB（規格端）⛔ 未回 code 驗證任何一條**（寫的是**規格怎麼寫**，不是**程式怎麼跑**）。
- `S1-3B_FULL_DRAFT_SPEC`（3,157 行）／`S1-3_IA_WIREFRAME`（1,597 行）／`S1-6_COMPLIANCE_COPY_GUIDE`（1,355 行）／`S2-PRD`／`docs/v2/stage1/*` **兩組都未逐字讀完** ⇒ §4 的部分「洞」與「待確認」**可能躺在那幾份裡**，⛔ 不得逕自宣稱規格沒寫。

---

## 🔍 找標的

### 0. 讀表前提（總管親自複驗；⛔ 少了這段，下表每一列都會被讀反）

- 🔴 **本頁的 `[A:…]` 標記與今天頁不同 —— 是 `[A:無此頁]`，⛔ 不是 `[A:未接線]`（兩者意思完全不同）**：總管複驗 `src/ui_v2/` 只有 `page_today.py`／`app_today.py` ＋ 四個共用層，**根本沒有 find 頁**。本頁版面定義住在 `docs/v2/prototype/gen_today_v2.py::FIND_LAYOUT`，**非契約層、無 `tests/ui_v2/` 守護**（改壞了沒有任何測試會攔）⇒ **下表「資料來源」一律填 B＝現行** `src/ui/views/page_find.py` ＋ `app.py::_ia_view_find`。
- **14 個 key 在 `src/` ＋ `app.py` 的原始 grep**（總管實測）：`chrome.asof` 2／`find.screen_form` 1／`find.heatmap` 2／`find.sector_flow` 3，**其餘 10 個皆 0**。⚠️ `chrome.asof` 那 2 處**不是本頁實作**，是 `src/ui_v2/page_today.py::WITHDRAWN_BLOCKS`（逐字「**已撤回、不是待補**」）⇒ **ED 說「只有 3 塊有實作」與「4 塊有命中」不衝突，是計數單位不同**：**本頁真有實作的是 3 塊**。
- 🔴 **原型與實作的 key 對不上**：總管複驗 `src/ui/views/page_find.py` 兩處 `Card(key="find.screen_result", label="選股結果")` —— **原型用 `find.screen_summary`、實作用 `find.screen_result`**，⛔ 兩邊都沒寫「另一個是舊名」（哪個對＝`DATA_MAP_GAPS.md` `ED-d`）。
- **本表「缺值處理」欄的徽章號一律用 `UI_COMPONENTS §2` 的 `#n` 系統**（與今天頁一致）；出處寫 `①~⑩` 者**逐字照抄並標明是哪一套，⛔ 不自行換算** —— 三套編號撞號（同一個 8 兩套意思相反）見 `DATA_MAP_GAPS.md` `F-0-2`。

### 1. 十四個 block（順序照 `FIND_LAYOUT`）

| block key | 畫面顯示什麼 | 資料來源 | 計算 | 缺值處理 |
|---|---|---|---|---|
| `find.statusbar` `[A:無此頁]` | 規格 ① 列為**葉外 chrome**（三格：交易日／總經／Sheet 綁定，cols `1/1/1`）。⚠️ 線框 `live`／`idle` 文案內的日期是**規格示意值、非實測**〔EE〕 | 🔴 **兩組同證：本頁無此塊。**〔ED 程式碼端〕`src/ui/views/page_find.py` 全檔無 `statusbar`／`status_bar`；`app.py` IA v2 分支**刻意不渲染**舊全域常駐條（那條讀的 `warroom_summary` 必退回「⬜ 總經未評估」＝ `CLAUDE.md §1` 假訊息）。〔EE 規格端〕`wf_page_find.js` evidence 逐字「狀態定義**全部在頁1**，本頁不重新定義」；`UI_PAGE_FIND ⑤` 逐字本頁**皆無實作**。**但本頁另有一條不同的路**：總經位階拿去當**空頭濾網**餵進選股，走 `src/ui/views/page_find.py::_load_regime`，失敗回 `(None, repr(e))` ⇒「**選股不因總經讀不到而中止，但濾網不套用**」，**這件事要在選股結果卡的「L3 說明」列講出來、⛔ 不可靜默略過**。頻率／TTL／fallback **⚠️ 待確認**〔`EE-GAP-09`〕 | **無計算**（整塊不存在）。空頭濾網判定式規格端未給 **⚠️ 待確認**〔`ⓛ`〕 | 🔴 **畫面上沒有這一塊** ⇒ 無徽章、無重試語意。⛔ 不得寫成 **#5**「這項還沒做」（`UI_PAGE_FIND ② 處置 2` 只把 #5 留給「保留欄位而不接線」）；⛔ 不得用總覽卡的紅態冒充一條不存在的狀態列。🔴 **交易日格無合規來源**（`S1-4 §4.5`／`S2-UI_SPEC §5.5` 禁用需交易日曆才能判的字眼、repo 無交易日曆）〔`ED-1`／`EE-GAP-10`，與今天頁洞 ② 同源〕。**編號 ⚠️ 待確認**〔`ⓙ`〕 |
| `chrome.asof` `[A:無此頁]` | 葉外 chrome 的跨頁時點列（`live`／`idle`／`loading`／`partial`／`error` 五態文案見線框） | 🔴 **程式碼端 0 命中**〔ED〕：`src/ui/views/page_find.py` 無 `asof`／`as_of`；全 repo 唯一命中在 `src/ui_v2/page_today.py::WITHDRAWN_BLOCKS`（**今天頁**的撤回登記）。**歸屬日取 `shared/staleness.py::latest_date`，⛔ 不得用 `fetched_at`**〔EE，`S1-4 §4.2` 規則 1〕。🔴 **本頁時點列的難處**（線框 evidence 逐字）：**一頁之內有三種節奏、⛔ 不可合併成一個日期** —— (a) 價量／RS＝收盤日；(b) 月營收／季報＝資料歸屬月；(c) 葉2 泡泡圖＝每交易日盤後凍結的快照（須標 `MM-DD 盤後`）⇒ **本頁常態是 `partial`** | **無計算**（取 `latest_date`，`None` 就是 `None`）。**格式**＝`S1-4 §4.2` 表逐字：`MM-DD 收盤`／`YYYY-MM 資料`／`YYYY-Qn 財報`／`MM-DD 盤後`／`YYYY-MM-DD 凍結` | 🔴 **兩組結論相反，⛔ 本檔不裁決**：〔ED〕今天頁已判「已撤回」但 `FIND_LAYOUT` 仍列本頁有它 ⇒ **跨頁不一致**；〔EE〕⛔ **不得把今天頁的撤回套到本頁** —— 今天頁撤回的理由是 L3 契約 9 key 無 `as_of`，**本頁來源是序列型資料、有 `date` 欄**。詳見 `DATA_MAP_GAPS.md` `F-5-2`。**鐵律**（`S1-4 §4.3`）：⛔ 不得猜／不得填今天／不得留白；⛔ 禁用「今天／剛剛／最新」相對詞；⛔ 不得印「落後 N 天」。**本塊橫跨全部狀態、⛔ 不配徽章**（`S1-4 §3.8`）。**編號 ⚠️ 待確認**〔`ⓙ`〕 |
| `find.screen_form` `[A:無此頁]` | ①基本面優選（自動，caption 印 `4 項全過：負債比<50% · 三率三升 YoY · 淨流動值>0 · EPS>0`）／②因子 `st.multiselect`（5 選項）／③`st.selectbox` 顯示筆數（**20/50/100 三個都要畫得出來**，預設 50）＋ 主 CTA `🎯 開始選股`。線框逐字：「**按下去才會開始取數。在你按之前，這一頁不會發出任何一次網路連線。**」 | **它是輸入，⛔ 不是觀測。選項字串**＝L3 `src/services/fundamental_screener_service.py::SCREEN_ANGLE_LABELS`（label→key 共 5 項：`pe_low`／`eps_high`／`shortage`／`rs_leader`／`trend`），由 `src/ui/views/page_find.py::load_factor_labels` 取；**門檻字串**＝L0 `shared/fundamental_prescreen_thresholds.py::DEBT_RATIO_MAX`／`EPS_MIN`／`PRESCREEN_REQUIRED_PASSES`。**TTL＝無**（純常數 import）；**fallback＝無**；**頻率**＝每次 rerun。⚠️ **規格端未指名該清單的 L3 符號／endpoint**〔`EE-GAP-03`〕；**「四項全過」的四項規格端查無定義**〔`EE-GAP-04`／`ⓖ`，⛔ 不得拿 `INDICATOR_SPEC §4`「基本面四維」逕自對映〕 | **無計算**（表單不算東西）。submit handler 把 widget 當下值寫進 `SS_APPLIED_SCREEN`（`{"factors":[...],"top_n":int}`），**該 key 的存在性就是下游的 `requested=` gate** | 因子清單載不進來 → `src/ui/views/page_find.py::build_form_unavailable_card`（Card key `find.screen_form`）→ **#6 🔴**＋`where=NO_EXIT_MARKER`（**重按無效**，逐字「本頁不會拿一份自己編的因子清單頂替它」）⇒ 該情境**全頁無主 CTA**。預設因子 `eps_high` 從 SSOT 消失 → `st.warning` 降級＋**不預選任何因子**（⛔ 不靜默退回第一項）。`degraded` → **#4**「這一項的門檻現在分不出好壞」＋**排序把該項排除並寫明**；`unwired` → **#5**「停用它但**不隱藏**（隱藏就看不出少了什麼）」〔EE 引線框，⚠️ 五因子現皆已接線、此格無實例〕。🔴 **規格 vs 規格牴觸（⛔ 不裁決）**：契約壞了線框要 CTA **停用**，`S1-4 §3.7.2` 逐字「**P2 落地前『停用』做不到**…正確做法是**照常可按**，文字誠實 > 假裝按鈕壞了」〔`EE-GAP-11`，**與今天頁 §3 同一條**〕。**編號**：`INDICATOR_SPEC R-1`・`DECISION_RULES R9-6` |
| `find.scenario_quickpick` `[A:無此頁]` | 規格 ① 列在 n1（cols `3/3/1`）：三顆情境鈕（`💰 高殖利率`／`📈 營收成長`／`📉 低基期`），點一顆＝把因子勾好、**不自動送出**；每顆鈕下方**常駐一行口徑小字** | 🔴 **兩組同證：程式碼端 0 命中〔ED〕、規格端也不存在〔EE〕** —— 線框 evidence 逐字「這一塊在 code 與規格裡**都不存在**，是客戶本輪新交辦的需求」；線框 `src:null`。🔴 「距一年低點近」這個量**規格端無來源、repo 無欄位**（`52週低`／`year_low`／`一年低點` 全 0）。「高殖利率」口徑＝`S1-2_METRIC_SSOT §1.4` 的 **Y-3**（`src/data/stock/yield_pe_fetcher.py`，TWSE `DividendYield`／TPEX `YieldRatio` 直接取用，⚠️ **口徑未宣告**） | **⚠️ 待確認 —— 規格端沒有算式**：「低基期」三個定義（用哪個價／窗口/「近」的門檻 %）線框逐字標「**客戶尚未給 → ★待拍板**」〔`EE-GAP-08`／`ⓕ`〕 | 🔴 **規格 ② 處置 2(a) 明文：線框 `src:null`、畫面上整塊不存在 ⇒ 不畫，⛔ 不得用 #5 冒充「有這塊但沒接線」** ⇒ 無徽章、無重試語意。🔴 **合規紅線（線框 evidence 寫死）**：情境鈕標籤**只能是條件的名字、⛔ 不得是行動的名字** —— ✅「💰 高殖利率」「📉 低基期（距一年低點近）」；⛔「存股首選」「抄底名單」「便宜好買」「逢低布局」「這些是值得買的」。⚠️「低基期」在 repo 現有 5 個命中**每一次都緊接著行動動詞**（「分批建倉」「勇敢建倉」「大膽重壓」），搬過來**必須切乾淨、只留條件**。鈕上寫「高殖利率」時**必須同時標明這是交易所公告值、口徑未宣告**，⛔ 不得讓使用者以為它與「🔬 查一檔」的 357 殖利率（Y-2）是同一把尺。**編號 ⚠️ 待確認**〔`ⓙ`〕 |
| `find.wiring_disclosure` `[A:無此頁]` | 表單下方一行 `st.caption`：五個因子**已全部接線**／名稱欄與估值同源／**估值是唯一需要多跑一輪取數的因子**，那一輪失敗或回空時會在結果卡上明講、**不會靜默把它當 0 分** | 實作＝`src/ui/views/page_find.py::WIRING_DISCLOSURE`（**寫死的常數字串，⛔ 不是資料**），由 `src/ui/views/page_find.py::_render_screen_form` 在函式**最後一行**印出。**TTL／fallback＝不適用**。⚠️ **它是 code 自己的宣稱、⛔ 不是規格**〔EE〕：「五個因子都已接線」是一句**全稱句**，線框組**沒有**逐一驗證五條路徑（依 `CLAUDE.md §-2` 規則 6 標為待驗）。⚠️ **另一條誠實邊界**：L1 對兩市場是各自 `try/except … continue` 的 fail-soft，**半邊失敗時回一份只有另外半邊的 map 且不留旗標** ⇒ 覆蓋率掉一半時畫面**只看得到 N 變小、看不出是哪個市場掛了** | **無計算**（它是揭露文字，不是觀測值） | 🔴 **兩組結論相反，⛔ 本檔不裁決**：〔EE 規格端〕線框矩陣**十態全部「常駐」**、`idle` 原文「⛔ 不隨狀態消失」；〔ED 程式碼端實測〕**「常駐」不成立** —— L3 因子清單載不進來時 `_render_screen_leaf` **整支跳過** `_render_screen_form`，而這句長在被跳過那支的**最後一行** ⇒ **最需要它的時候它不在**（同一次實測：`SECTOR_FLOW_AXIS_NOTE` 有畫、`WIRING_DISCLOSURE` 沒有）。**登記未修**〔`ED-6`／`F-5-3`〕。⛔ **無 Card key ⇒ 無徽章**。**編號 ⚠️ 待確認**〔`ED-a`／`ⓙ`〕 |
| `find.screen_summary` `[A:無此頁]` | 一張總覽卡：`live` 時大字「存活池 N 檔 → 綜合入選 M 檔」＋ facts（本次條件／顯示筆數／存活池／估值檔數／名稱欄／`aux_errors`／L3 說明）。⚠️ **線框裡的 `274`／`50`／`12/89`／`31/274` 全是規格示意值、⛔ 非實測** | ⚠️ **實作的 Card key 是 `find.screen_result`、⛔ 不是 `find.screen_summary`**（`src/ui/views/page_find.py::build_screen_result_card`；見 §0）。**上游六路全 L3**：`src/services/fundamental_screener_service.py::get_fundamental_survivors`（**TTL_1DAY**；L1 `src/data/stock/fundamentals_snapshot_loader.py::load_fundamentals_snapshot`，**TTL_1DAY**，讀 `data_cache/fundamentals/` parquet＋`latest.json`，寫入端 cron `scripts/update_fundamentals_snapshot.py`，**fallback＝無**，缺檔 `raise FileNotFoundError`）／`src/services/valuation_service.py::get_pe_name_maps`（**L3 無 cache**；L1 `src/data/stock/yield_pe_fetcher.py::fetch_twse_yield_pe`〔TWSE OpenAPI `BWIBBU_d`〕＋`fetch_tpex_yield_pe`〔TPEX OpenAPI `tpex_mainboard_peratio_analysis`〕，**皆 TTL_1DAY**，HTTP 走 `src/data/proxy/proxy_helper.py::fetch_url`〔**NAS Squid → 直連 → fail**〕；**合併＝union＋`setdefault` 上市先填，⛔ 不平均**；**任一市場失敗 fail-soft、不留旗標**）／`src/services/shortage_screener_service.py::run_shortage_scan`（**TTL_1DAY**；L1 `src/data/stock/monthly_revenue_fetcher.py::fetch_batch_monthly_revenue`〔**TTL_6HOUR**，fallback＝FinMind 主 → TWSE+TPEx OpenAPI keyless〕＋`src/data/stock/quarterly_financials_fetcher.py::fetch_quarterly_shortage_frame`〔**TTL_1DAY**〕）／`src/services/rs_leader_service.py::run_rs_leader_scan`（**TTL_1HOUR**；L1 `src/data/macro/macro_core.py::fetch_yf_close`＋`src/data/stock/picker_fetcher.py::fetch_stock_history_1y`〔**L1 無 `@st.cache_data`**，fallback＝`.TW`→`.TWO` 雙後綴重試〕）／`src/services/fundamental_screener_service.py::build_trend_map`（**TTL_1DAY**）／`src/services/allocation_service.py::get_macro_regime`（**無 cache**）。**頻率**＝每次 submit 後的 rerun；`requested=False` 時**一行 L3 都不呼叫**。⚠️ **規格端未給 endpoint／欄位／TTL／fallback**〔`EE-GAP-09`／`ⓐⓑⓒ`〕 | **四層**：L1 各源抓 → L3 六路各自 `try/except` 收集 → L3 `composite_rank_candidates`：逐因子 `_percentile_scores`＝`pct_rank(valid)×100`（`pe_low` 反向；**缺值不放 key、⛔ 不填 0**）→ **涵蓋門檻** `len(present) > SCREENER_MIN_FACTOR_COVERAGE_RATIO × n_eff`（`=0.5`，**嚴格大於**）→ `綜合分 = round(Σ present ÷ len(present), 1)`，不過門檻 → `None`（⛔ **不得回 0 分**）→ L3 `_apply_bear_market_filter`（`regime ∈ THROTTLE_VETO_REGIMES` → 只留贏過大盤者）→ L5 只取 `len(df)` 當大字。⚠️ 規格端寫法（`INDICATOR_SPEC §4`）`n_all` 寫死 **5 因子**、code 端 `n_eff` 是**全域實際掃到的已勾因子數** ——〔EE 引規格〕與〔ED 讀 code〕**措辭不同，⛔ 未自行統一** | **三種「沒有結果」⛔ 絕不可混**：`requested=False` → **#3 ⬜ idle**（`where`＝按「🎯 開始選股」，**可重試**）／`rows==0` → **#7 UI_EMPTY 灰**，逐字「**這是一個有效的結果**（已經跑完，不是還沒跑、也不是故障）」「⭐ **這是灰的，不是紅的**」／`rows is None` → `MISS_CONTRACT_DRIFT` 落在 `shared/ui_state.py::FAILED_REASONS` ⇒ **#6 🔴**＋`where=NO_EXIT_MARKER`（**重按不會好，要改程式**，⛔ 不得給「可以重跑」指引；大字區**留白**，⛔ 不顯示 `0` 或上一輪殘值）。L3 拋例外 → **#6 🔴**＋`repr(e)`（**可重試**）。⭐ **單一因子失敗⛔ 不讓整卡轉紅** —— 進 `aux_errors` facts 列（逐源：存活池／估值／缺貨／RS／跨季／總經位階，「一源壞**不染色**其餘」），該因子「不計入綜合分、⛔ 不記 0」。**估值兩種「沒有」分開講**：`pe_n is None`＝取不到／`pe_n==0`＝兩市場都沒給。`partial` → **#9 `◧ N／M`**，⚠️ **`M` 的母體必須寫明**（現成 `_cov_bits` 分母是**存活池全體**、不是表上列數，**兩個母體差一個數量級，⛔ 不得直接貼**〔`ED-8`〕）。⚠️ **empty 還有第 4 種成因分不出來**：「有候選但因子缺太多排不出名次」目前與「0 檔」混在一起〔`EE-GAP-12`〕。**編號**：`INDICATOR_SPEC R-1／R-2／R-3`＋`§4` 選股網綜合分列・`DECISION_RULES R9-6`・`D-24` |
| `find.screen_table` `[A:無此頁]` | 🔴 **應然與實然不同，⛔ 兩邊都要寫**（`UI_PAGE_FIND ⑥`）。**應然（線框）**：6 欄固定 `代號｜名稱｜評分｜本益比｜殖利率｜缺貨`、值為原始值＋整列可點展開；**實然**：`代碼｜名稱｜綜合分`＋**每個已勾因子一欄**（`估值分`／`EPS分`／`缺貨分`／`RS分`／`跨季分`，**依勾選動態增欄**），因子值為**百分位 0–100、1 位小數**，**無殖利率欄**。**分類＝設計變更（線框待改）；⛔ 無欄位例外**（客戶 2026-09-16 裁示逐字「**結果表原始值欄：維持不要**」）。實作為 `st.dataframe(hide_index=True, width="stretch")`＋上方 `st.caption("本次因子：…")` | 同上（`get_ranked_picks` 回的 `df`，**原樣透傳**）。**⛔ 無 Card key、⛔ 無 `column_config`**（ED 實測 `column_config`／`N/A`／`⚠︎`／`:.2f` 在 `src/ui/views/page_find.py` **全 0 命中**）。**欄位上游口徑**〔EE〕：**本益比** ＝ `S1-2_METRIC_SSOT §1.3`「**本站不自己算**」，取 TWSE `BWIBBU_d` 的 `PEratio`／TPEX `tpex_mainboard_peratio_analysis` 的 `PriceEarningRatio`，SSOT ＝ `src/data/stock/yield_pe_fetcher.py::fetch_pe_name_maps`；**名稱欄與估值同一份來源** | **各欄算式（逐層）**：`EPS分` ← 存活池自帶 `eps` 欄的全市場百分位；`估值分` ← `pe_map` 百分位（**反向**）；`缺貨分` ← L2 `src/compute/screener/shortage_screener.py::score_shortage` 四訊號加總（合約負債／毛利／存貨天數／營收 YoY，0–100）的百分位；`RS分` ← L2 `src/compute/screener/rs_leader_screener.py::score_rs_leader` → `src/compute/strategy/v5_modules.py::calc_relative_strength`：`RS(σ) = (s_ret − m_ret) ÷ m_std`（**`m_std ≤ RS_MARKET_SIGMA_MIN_PCT` 或 NaN → `rs=None`，⛔ 不回 0.0σ**）；`跨季分` ← L2 `src/compute/screener/cross_quarter_trends.py` 的 `favorable_count ∈ [0,4]` 的百分位。⛔「**不適用**」與「**缺漏**」**都不算有值、兩者都不進分子分母** | 🔴 **規格三語彙 vs 實作兩態，⛔ 兩邊都要寫**。**規格（`UI_PAGE_FIND ③` 逐字）**：真的是 0 → `數值`＋**#1**（⛔ 不得因為值是 0 就畫成灰）｜結構上不適用 → `N/A`＋**#8**＋tooltip「這類標的沒有這一項」＋**⛔ 不計入未評估**｜缺漏 → `⚠︎ —`＋**#7**＋tooltip 走 `MISS_TEXT`（**⛔ 不得裸查表**）＋**計入未評估 +1**｜部分計入 → 值帶 `*`＋**#9 `◧ N／M`（標在欄標題）**；⛔ **不得另立第四種寫法**（含線框提案的 `⚠ 非正值`）。**實作（ED 實測）**：🔴 **表上只有「有數字」與「空白」兩種** —— 缺料因子該格為 `None`（pandas 渲染成空白），**⛔ 不是 0**（`composite_rank_candidates` 逐字「缺料因子該股為空白（None），不是 0，避免誤導」）⇒ **#7／#8 在表格內未落地**。**整張表只在 `state == UI_LIVE` 時才畫** ⇒ ⛔ 沒有「表在、但是空的」這一態。⛔ **不得只靠顏色**分辨 #7／#8（`INDICATOR_SPEC R-3`）。🔴 **被涵蓋門檻擋掉的個股⛔ 不得從畫面消失**（`R-3` 末句：須另列「**資料不足，未列入排序**」到**個股層級**；`UI_PAGE_FIND ⑤ B` 標明**本頁現行違反**，且 ⛔ **不得在 L3 直接翻掉 `drop_unscored`** —— 會同時改到 forward-test 每月凍結名單）。**編號**：`INDICATOR_SPEC R-1／R-2／R-3`＋`§3` 本益比列＋`§4`・**`D-08`**・**`D-24`**・`DECISION_RULES R9-6` |
| `find.pick_reason` `[A:無此頁]` | 規格 ① 列在 n3（cols `1/1/1`）：逐列展開「這一檔為什麼入選」（命中因子／未命中／綜合評分名次）。⚠️ **線框全段自陳是提案**，現行畫面點列不會展開 | 🔴 **兩組同證：程式碼端 0 命中〔ED〕、規格端也不存在〔EE〕** —— 線框 evidence 逐字「『入選理由』這個區塊在 `stock_ia_v1.html` 與 `S1-3B §3.2` 九節裡**都沒有被單獨畫出來**」；線框 `src:null`。⚠️ 語意最接近的**既有**東西是總覽卡的 `st.caption("本次因子："…)`（走 L5 純函式 `src/ui/tabs/tab_stock_picker.py::summarize_factor_hits`）與 facts 裡的「L3 說明」（**整批層級**，本頁原樣透傳）——**但那兩者都不是本 block，⛔ 不填** | **無計算（整塊不存在）**。「命中／未命中」的分界**規格端未給**〔`EE-GAP-05`〕；逐列因子命中明細**拿不拿得到，線框組自陳未查**〔`ⓗ`〕 | 🔴 **規格 ② 處置 2(a)：整塊不存在 ⇒ 不畫，⛔ 不得用 #5 冒充。** 🔴 **合規紅線（線框 evidence 寫死）**：✅ 可寫「**這一檔符合你勾的哪幾個條件**」；⛔ 不可寫「值得買」「便宜」「甜蜜價」「逢低」「建議持股 X%」「目標價」「必漲」「推薦」；⛔ **尤其不可以把「入選理由」寫成「買進理由」**；結尾那句「這是『符合你勾選條件』的說明，⛔ 不是買賣建議」**應常駐在展開內、⛔ 不放頁尾**。⚠️ 線框另定：缺資料那一行要寫「**這一檔沒有資料，沒有計入綜合分**」，**⛔ 不會寫成「未命中」**（**沒有資料 ≠ 沒通過**）。**編號 ⚠️ 待確認**〔`ⓙ`〕 |
| `find.csv` `[A:無此頁]` | `st.download_button("💾 下載選股結果 CSV")`，`file_name="screener_result.csv"`、`mime="text/csv"`，**在 form 外**（form 內放 `st.button` 實跑即拋 `StreamlitAPIException`）。⚠️ **鈕字串兩份真相源**：線框／`S1-3B` 寫「⬇ 下載結果 CSV」、production 寫「💾 下載選股結果 CSV」；`UI_PAGE_FIND ⑥` 推薦取 production 那串、線框改齊，**⛔ 不得兩邊各留一份** —— **待客戶拍板**〔`F-5-5`〕 | 資料＝同一份 `ScreenResult.df`，`df.to_csv(index=False).encode("utf-8-sig")`（**BOM，供 Excel**）。**⛔ 不另取數、⛔ 無 TTL、⛔ 無 fallback**。🔴 **規格已要求改成兩份**（`UI_PAGE_FIND ⑤ C` 逐字）：「**CSV 必須另備一份 df**：數值欄保留**數值**、缺值原因**另立一欄**承載；⛔ 不得直接 `to_csv` 那份已格式化的表」，且「**本項必須與軌 A 同一批落地、⛔ 不得延後**（延後＝使用者拿到一份看起來正常、卻算不了的檔案）」 | **無計算**（逐字匯出）。**全站規則**＝`S2-UI_SPEC §9.5`＝`S1-3B §6.3`（編號 **`G-24`**）：**位置**＝該區塊表格正下方；**檔名**＝`〈頁名〉_〈區塊名〉_〈資料歸屬日〉.csv`（**用資料的日期、⛔ 不用抓取時間**）；**第一列**＝`# 資料時點：… · 僅供學術研究，非投資建議`；⛔ **不得放進 `st.form`** | 🔴 **缺格怎麼寫進 CSV（`S2-UI_SPEC §9.5` 逐字）**：⭐ **照畫面寫** —— `0.00` 寫 `0.00`；`N/A` 寫 `N/A`；**`—` 寫空字串 ＋ 另開一欄 `〈欄名〉_missing_reason` 寫原因**。⛔ **不得把 `—` 寫成 0**（線框 evidence：那是把畫面上誠實標過的東西在檔案裡洗掉）。**規格**：沒資料時＝**停用但⛔ 不隱藏**（「隱藏會讓你以為這頁沒有匯出功能」）；0 列時＝**仍可下載**（只有表頭，「我跑過、結果是 0」本身就是一個要留存的結果）。🔴 **實作端做不到**〔ED 實測〕：非 live 時 `_render_screen_leaf` 在畫表前就 `return` ⇒ **按鈕整顆不存在**，「0 列停用態」**不可達**〔`ED-5`〕；⚠️「停用」另受 `S1-4 §3.7.2 P2` 拘束（現行無 `disabled` 參數）。`to_csv` 自己拋例外 → `st.caption("（CSV 匯出暫不可用：…）")`＋`return`（**不炸整張表**）。**編號**：**`G-24`** —— ⚠️ 它**不在** `INDICATOR_SPEC §5`／`DECISION_RULES §8`，出處是 `docs/v2/audit/DRAFT_GAP_AUDIT.md` ＋ `docs/v2/wireframe/S1-3B_FULL_DRAFT_SPEC.md §6.3` |
| `find.pe_two_kinds` `[A:無此頁]` | 規格 ④ 要求的「本益比兩種『沒有』」分辨卡（n4，`t4` dashed）。🔴 **現行生效態＝客戶 2026-09-16 裁示逐字**：「**本益比兩種「沒有」：UI 層先誠實顯示「本機分不出，需 L1 補旗標」。L1 修改列為獨立任務，等 UI 全部做完再評估解凍。不要假裝做得到。**」⇒ 本塊**常駐**，內含一句**顯式誠實說明** | 🔴 **程式碼端 0 命中**〔ED〕—— `src/ui/views/page_find.py` 無此 key、無第四層展開元件。**SSOT ＝ `src/data/stock/yield_pe_fetcher.py::fetch_pe_name_maps`**（TWSE `BWIBBU_d` 的 `PEratio`／TPEX 的 `PriceEarningRatio`）。⚠️ 語意相關但**⛔ 不是本 block** 的既有物：總覽卡的 `PE_EMPTY_WHY`／`PE_MISSING_WHERE`，那些講的是「**整批估值這一輪有沒有拿到**」（檔數層級），**⛔ 不是「這一檔為什麼空白」**（個股層級） | 🔴 **`D-08` 照規格寫法做不出來（總管親自讀 code 複驗）**：`fetch_pe_name_maps` 逐字 `if _pe != _pe or _pe <= 0: continue` —— **NaN（沒這個數字）與 ≤0（虧損）走同一個 `continue`**，下游只拿得到「key 不在 `pe_map` 裡」；**這個合流與上游送什麼無關** ⇒ 要分辨兩種「沒有」**需要第二個資訊源（EPS）**，現行兩支 endpoint 生不出來。規格端佐證 `S1-2_METRIC_SSOT §1.3` 逐字「code 裡**沒有 EPS 這個輸入**，所以**無法區分「虧損」與「來源空值」**」。**⛔ 不自算**（`INDICATOR_SPEC §3`：「不自算，取交易所公告值」，單位「倍」，`<=0`→不適用／`NaN`→缺漏／逾 sanity `(0,200]`→缺漏） | 🔴 **`D-08` 落地前的生效態：一律 `⚠︎ —`（#7）＋ 誠實說明。** ⛔ **不得把 `PE<=0` 與 `NaN` 畫成兩種不同符號**（L1 已合流、L5 沒有第二個資訊源 ⇒ 畫成兩種＝**捏造一個系統做不到的區分**；客戶逐字「**不要假裝做得到**」）。⛔ **不採用線框提案的第三符號 `⚠ 非正值`**（它是 #7／#8 之外的**第四種寫法**；`INDICATOR_SPEC R-1` 已把 `PE<=0` 判為 `MISS_NOT_APPLICABLE` ⇒ 併入 **#8**，「非正值」三字寫進 tooltip 與展開文字）。🔴 **合規禁令（`UI_PAGE_FIND ④(d)` 逐字）**：使用者看到的字面**⛔ 不得出現「L1」「旗標」「`D-08`」這類內部詞；⛔ 亦不得出現「虧損」**（**系統手上沒有 EPS**，說「虧損」＝**替上游宣稱一件它沒說的事**）。⚠️ **一個未解的不一致**：`MISS_CONTRACT_DRIFT` 落在 `FAILED_REASONS` 內 ⇒ 會升成 **#6 🔴**，與「逾 sanity → `⚠︎ —`（#7）」對不上，⛔ 不得就地改 `FAILED_REASONS`。**可否重試**：#7 ✅ 可／#8 ❌ 重跑無效。**編號**：**`D-08`**（⚠️ **⛔ 不是 `D-07`**，見 `DATA_MAP_GAPS.md` `F-0-1`）・同族 **`D-30`**・`INDICATOR_SPEC R-1／R-3`・`DECISION_RULES §3 R3-2`。⚠️ **排程狀態⛔ 不得寫成「已排程」或「已核准」** —— 該檔屬資料層，受 `CLAUDE.md §-1.2` 資料層凍結拘束 |
| `find.map_cta` `[A:無此頁]` | 單顆 `st.button("🗺️ 載入板塊地圖", key="p02v_load_sector_map", type="primary")`，**在 form 外**；葉2 的**唯一** gate 寫入點。線框 `idle` 逐字：鈕常駐可按，**下方兩張圖各自顯示自己的灰態**（⛔ 兩張圖不共用同一段文字） | **無資料源 —— 它是操作，⛔ 不是觀測。** 字串＝`src/ui/views/page_find.py::ACTION_LOAD_MAP_LABEL`（⚠️ **本頁自己定義，⛔ 不在 `shared/ia_nav.py`** —— 該檔 `ACTION_LABELS` 實測**只有 `update_today` 一項** ⇒ **跨頁缺一份 SSOT**〔`ED-9`〕）。**行為**＝寫 `st.session_state["_p02_map_requested"]=True`，同時 gate 熱力圖與泡泡圖兩張。**TTL＝不適用**。⚠️ **改名風險**〔EE〕：「🗺️ 載入板塊地圖」**是新名字**，production 舊分頁現行是「🗺️ 載入產業熱力圖」—— 本 IA 把兩張圖併成一葉、**那顆舊 gate 會被吸收掉**，「**任何指去舊名字的文案都要跟著改**」（⚠️ 線框自陳**未查證**舊檔位置是否仍正確） | **無計算** | 按鈕**無狀態、恆可按**（⛔ 無停用態）。它是葉2 兩張卡 **#3 idle** 的**唯一出口**（`where` 皆為「按『🗺️ 載入板塊地圖』」）。⚠️ 旗標 `_p02_map_requested` **跨 rerun／跨頁存活，⛔ 不會自動復位**〔ED〕。🔴 **合規**：`loading` 時 ⛔ **不得只給一個全頁 spinner** —— 兩張圖**各自一個載入框**（`S1-3B §3.2.2`），且「泡泡圖讀快照**不需重抓**、熱力圖**需要** ⇒ 兩者『載入中』時間不同，**⛔ 不得共用一條進度文字**」（`S1-3B §3.2.9`）；`error` → 鈕**仍可按**（可重試），**錯誤由下方各圖自己標**。⚠️「停用」是否受 `S1-4 §3.7.2 P2` 拘束**⚠️ 待確認**（該條講的是 `single_submit_form`，**本鈕是 `st.button`**）〔`ⓚ`〕。**編號 ⚠️ 待確認**〔`ⓙ`〕 |
| `find.heatmap` `[A:無此頁]` | 狀態卡（Card key `find.heatmap`，label「產業漲跌熱力圖」）＋卡下方 `st.plotly_chart` treemap。facts：市場/區間・顏色怎麼讀（**紅＝漲、綠＝跌，台灣慣例**）・切換市場或區間・資料覆蓋率・⚠️ 台股口徑 | 🔴 **兩組結論互補、⛔ 不是矛盾**：〔EE 規格端〕**0 命中 —— 本頁最大的規格洞**（`熱力圖`／`heatmap`／`類股代表`／`色階` 在 `docs/v2/spec/` 幾乎全 0，唯二命中是 `UI_PAGE_FIND` 層表與 `S1-6` 的**免責缺口清單**）〔`EE-GAP-06`／`ⓓ`〕；〔ED 程式碼端〕**鏈完整**：L3 `src/services/sector_heatmap_service.py::get_sector_heatmap_view` → L3 `src/services/etf_sector_service.py::get_sector_returns` → **L1 `src/data/etf/etf_fetch.py::_fetch_sector_returns`**（`@st.cache_data(ttl=`**TTL_30MIN**`, max_entries=10)`，`yf.download(..., auto_adjust=True)`）。**類股宇宙 SSOT**＝L0 `shared/sector_heatmap.py::TW_SECTORS`／`US_SECTORS`＋`sector_universe()`／`coverage_counts()`；**區間查表**＝`SECTOR_LOOKBACK_TRADING_DAYS`（**根 bar**）與下載窗 `SECTOR_YF_DOWNLOAD_WINDOW` **刻意分離**。**本頁固定台股＋最短區間、⛔ 無選擇器**（線框沒畫下拉；加下拉＝版面元件增減，屬 `CLAUDE.md §-1.5` v3 §03-2 ① **客戶 gate**）。**繪圖**＝L4 `src/ui/render/sector_heatmap_render.py::build_sector_treemap`。**fallback＝無（單一源 yfinance）**。⚠️ 線框自陳「兩邊仍並存」**直接撞上 v3 §01-2**（嚴禁重複 Tab／重複取數），**線框只記錄事實、不作判定，須獨立一組查證** | **兩層**：**L1** `rate_pct = (C_{-1} ÷ C_{-1-n} − 1) × 100`，`n = lookback_trading_days(label)`，基期固定 `iloc[-1-n]`；各 ticker **自己 `dropna()` 後取自己的 bar**（**⛔ 刻意不做 `close.ffill()`** —— 停牌被 ffill 後報酬恆 `0.00%`＝把「沒交易」偽裝成「持平」，`CLAUDE.md §4.6` 明文禁）。**L3** `coverage_counts()` 算 `parent_fetched/parent_total`＋`sub_fetched/sub_total`。**⛔ 無門檻、無分級**。⚠️ **色階規則規格端不存在**〔`EE-GAP-06`〕 | **四態，`reason=MISS_FETCH_FAILED` 交給 L0 升紅（⛔ 不自己寫 `state=UI_FAILED`）**：未按鈕 → **#3 ⬜**（**可重試**）／L3 或組圖拋例外 → **#6 🔴**／**一檔都沒回來（`any_data=False`）→ #6 🔴**（⛔ **不是灰的 empty**：已按過＋一檔都沒回＝真故障，逐字「**不會拿 0% 把格子填滿冒充全部持平**」，**可重試**）／只抓到一部分（`complete=False` → `discriminative=False`）→ **#4 🟠 degraded**，**圖照畫、缺格留白、hover 顯示「無資料」、⛔ 不填 0**（**可重試**）／全抓到 → **#1 🟢**。缺值文案 SSOT＝`shared/sector_heatmap.py::HEATMAP_MISSING_TEXT`；缺值節點面積 `HEATMAP_AREA_MISSING_PARENT` **嚴格小於** `HEATMAP_AREA_FLOOR_PARENT`（⛔ 不讓「沒資料」霸佔版面）。`st.plotly_chart` 自己炸 → `st.error` 紅字，**⛔ 不吃掉下半頁**。⚠️ `wired=` **已不再傳 False**（2026-09-07 接線後撤回，規格 ② 逐字「⛔ 不得回填」）。🔴 **合規**：`S1-6 §3` 免責出口盤點把「**產業熱力圖（A11）**」列在「**❌ 完全沒有**」那一格。⚠️ **`#2 loading` 全頁 0 處傳 `in_flight=`，但葉2 已有 `st.spinner` ⇒ spinner 有了、徽章沒接**〔`ED-7`〕。**編號**：`INDICATOR_SPEC R-1／R-3`・`UI_PAGE_FIND ② 處置 5` |
| `find.sector_flow` `[A:無此頁]` | 狀態卡（Card key `find.sector_flow`，label「三大法人資金流向泡泡圖」）＋`st.plotly_chart` 泡泡圖＋**3 欄象限圖例**。facts：快取更新於・採用交易日・交易日不足 10 天（未列入象限）・你的持股板塊 | ✅ **本頁唯一規格端給得出資料源的 block**〔EE，`S1-1_DATA_WHITEPAPER §1.2 實體 I／J`【實測】〕：**來源**＝`daily_net.parquet` 的 `source` 欄 ＝ `TWSE:T86+MI_INDEX / TPEX:3itrade+otc_*`；`ticker_sector.json` 代碼→產業對映；**頻率＝daily，cron `30 9 * * *` UTC ＝ TW 17:30**；**延遲＝【查不到】**（白皮書原文）；**幣別＝TWD、單位＝億**，欄名帶單位（`net_amt_yi`／`foreign_yi`／`trust_yi`／`dealer_yi`）。〔ED 程式碼端〕L3 `src/services/sector_flow_service.py::get_sector_flow_view` → **L1 `src/data/sector_flow/reader.py::read_sector_flow_cache`**（純檔案 I/O，讀 `data_cache/sector_flow/` 三檔：`bubble_latest.json`〔**必要**〕／`metadata.json`〔判 staleness〕／`ticker_sector.json`）；**寫入端**＝cron `scripts/update_sector_flow.py`。**持股疊加**＝L5 只從 session 取 `etf_portfolio_rows`／`stock_portfolio_sheet_id`（L3 契約**明文指派給 L5**）。**TTL＝無 `@st.cache_data`**（本地檔讀），新鮮度改以 L0 `shared/sector_flow_thresholds.py::SECTOR_FLOW_STALE_MAX_CALENDAR_DAYS` 判。**fallback＝無**（缺檔回 `ok=False` sentinel，⛔ 不偽造空圖、⛔ 不編板塊） | **三層**：**L2**（cron 端）三軸 —— `X = Σ 近 WINDOW_X 交易日淨流入`（億）／`Y = 近 WINDOW_Y_RECENT 日均 − 前 WINDOW_Y_PRIOR 日均`（億/天）／`size = abs(近 WINDOW_SIZE 交易日淨額)`（億），**視窗常數一律讀 L0 `shared/sector_flow_thresholds.py`**；象限判定亦在後端。**L3** 持股 ticker 去 `.TW`／`.TWO` 後綴 → `map_tickers_to_sectors` → `highlight_sectors`。**L5** `_quadrant_counts()` 只數這一輪各象限的板塊數（**象限名讀資料本身、⛔ 零轉抄** —— 抄一份門檻到 UI 就是第二個 SSOT）。**L4** 繪圖 `src/ui/render/sector_flow_render.py::build_sector_flow_figure`。⚠️ **Y 軸「動能變化」的算式規格端 0 命中**〔`EE-GAP-07`／`ⓔ`〕—— 上式是**程式碼端**補的 | 未按鈕 → **#3 ⬜**（**可重試**）／L3 拋例外 → **#6 🔴**（**可重試**）／**`ok=False`（快照還沒產生）→ #7 UI_EMPTY 灰，⛔ 不是紅**（逐字「這不是故障，是當日盤後任務還沒產生資料」；**重按無效**，`where`＝等盤後排程或手動跑 GitHub Actions）／`is_stale=True` → **#4 🟠 degraded**（「**畫的是最後一次成功凍結的快照，不是今天的**」；**重按無效**）／其餘 → **#1 🟢**。`metadata` 缺或 `updated_at` 解析不了 → **一律視為過期**（⛔ 不假設新鮮）。交易日不足 `WINDOW_Y_MIN_DAYS` 的板塊 → **⛔ 不硬塞象限位置**，另行列名。🔴 **合規**：⛔ **不會靜默拿舊快照當今天的值**；`reason` 與 `stale_reason` 是**兩個不同欄位、⛔ 不可合併**（對應「讀不到」與「讀到了但是舊的」）；時點列**必須標 `MM-DD 盤後`、⛔ 不得與熱力圖共用一個時點**（`S1-3B §3.2.9`）；`partial` → **#9**「空的象限**不會畫成 0**」。**編號**：`INDICATOR_SPEC R-1／R-3`・`UI_PAGE_FIND ② 處置 5` |
| `find.map_scale_disclosure` `[A:無此頁]` | 葉2 **常駐** `st.caption`，逐字即畫面文案：`X＝近 5 日累計淨流入（億）· Y＝動能變化 · 泡泡＝近 20 日淨額規模。`**面積不等於權重。** | 實作＝`src/ui/views/page_find.py::SECTOR_FLOW_AXIS_NOTE`，三個視窗數字**一律讀 L0** `shared/sector_flow_thresholds.py::WINDOW_X`／`WINDOW_Y_RECENT`／`WINDOW_Y_PRIOR`／`WINDOW_SIZE`／`WINDOW_Y_MIN_DAYS`（**⛔ 不手抄數字**）。**出處三處一致**〔EE〕：核准線框 `docs/wireframes/stock_ia_v1.html` `id:'find'` 第 4 個 block／`S1-3B §3.2.1`／`S1-3B §3.2.8`（代號 **`D-7`**）／`S2-UI_SPEC §9.3` `D-7` 列（位置「頁2 葉2」、層「第三層圖下方 caption」、✅ 常駐）。⚠️ **台股熱力圖那半的口徑揭露是另一條**：`shared/sector_heatmap.py::TW_SINGLE_STOCK_PROXY_DISCLOSURE`（L0 SSOT，經 L3 `disclosure` 欄帶上來，**本頁原樣印出、⛔ 不改寫**），且它**不是常駐** —— 只在 `single_stock_proxy and disclosure` 皆真時才進 facts | **無計算**（f-string 組裝 L0 常數）。它描述的是 `find.sector_flow` 的三個軸，Y 軸算式缺口見上一列〔`EE-GAP-07`〕 | ✅ **兩組同證：真常駐** —— 〔EE〕`S1-3B §3.2.7` 矩陣本列**十個狀態欄全部寫「常駐」，是全表唯一一列如此，⛔ 不得在任何狀態下拿掉**；〔ED 程式碼端實測〕`st.caption(SECTOR_FLOW_AXIS_NOTE)` 在 `_render_map_leaf` **最後一行、無任何條件**（L3 全掛時仍畫得出來，**與 `find.wiring_disclosure` 相反**）。⭐ **「面積不等於權重」為什麼是紅線**（線框 evidence 逐字）：泡泡面積會被直覺讀成「這個板塊比較大／比較重要」，實際量的是**近 20 日淨額規模** —— **不寫這一句，圖本身就在誤導**（`CLAUDE.md §1` 在圖表上的同型問題）。⛔ **無 Card key ⇒ 無徽章、無重試語意**。⚠️ **本頁有兩份口徑揭露、⛔ 不是一份**，後者（台股單一代表股）**會消失**。**編號**：**`D-7`** —— 🔴 ⛔ **不是 `INDICATOR_SPEC` 的 `D-07`**，兩套同名不同義（見 `DATA_MAP_GAPS.md` `F-0-1`） |

### 2. 三類「沒有」⛔ 不可混為一談（ED 組分類；⛔ 混用會讓畫面說謊）

- **A ＝ 規格自己說不畫**（線框 `src:null`；`UI_PAGE_FIND ② 處置 2(a)` 明文「畫面上整塊不存在 ⇒ 不畫，**⛔ 不得用 #5 冒充**」）：`find.scenario_quickpick`／`find.pick_reason`
- **B ＝ 規格要、實作無**（該畫而沒畫，**是洞不是設計**）：`find.statusbar`／`chrome.asof`／`find.pe_two_kinds`
- **C ＝ 實作有、但沒有 Card key**（畫得出來，但**無 `state=` 可讀 ⇒ 無徽章**）：`find.wiring_disclosure`／`find.screen_table`／`find.csv`／`find.map_cta`／`find.map_scale_disclosure`。另 `find.screen_summary` 屬 **C 的變形**：實作有、但 key 名不同（`find.screen_result`）

### 3. 🔴 `D-08` 照規格寫法做不出來（總管親自讀 code 複驗，⛔ 不是 ED 的單組推論）

- `src/data/stock/yield_pe_fetcher.py::fetch_pe_name_maps` 逐字 `if _pe != _pe or _pe <= 0: continue` ⇒ **NaN（沒這個數字）與 ≤0（虧損）走同一個 `continue`**，下游只拿得到「key 不在 `pe_map` 裡」。**這個合流發生在本機、與上游送什麼無關** ⇒ 規格 `UI_PAGE_FIND ④(c)`「在 L1 補旗標」**單靠這兩支 endpoint 補不出 `MISS_NOT_APPLICABLE`**，要分辨需要**第二個資訊源（EPS）**。
- 規格端佐證：`S1-2_METRIC_SSOT §1.3` 逐字「code 裡**沒有 EPS 這個輸入**，所以**無法區分「虧損」與「來源空值」**」。⇒ **現階段照客戶裁示走「誠實說明」一途**，⛔ 不得動 `src/data/stock/yield_pe_fetcher.py`（`CLAUDE.md §-1.2` 資料層凍結）。

### 4. 誠實揭露（`CLAUDE.md §-2` 規則 6）

- **ED（程式碼端）／EE（規格端）兩組皆為單組結論、⛔ 未互相複驗**；所有「0 命中／只有 3 個／唯一一處」都是**單次 grep 的全稱句**，依 `§8.2.A.0` 規則 2 **請據此打折**，⛔ 不得作為後續動作的前提。**§0 的四條與 §3 是總管另行親自查證的，效力不同。**
- **ED ⛔ 未做 AST、⛔ 未實跑任何程式、⛔ 未打過任何 endpoint、⛔ 未開過畫面**（「畫面會顯示什麼」全是讀 code 推得）；**EE ⛔ 完全沒有回 code 驗證任何一條**（寫的是**規格怎麼寫**，不是**程式怎麼跑**；凡引 `檔名::符號` 一律是規格文件自己轉述的）。
- **兩組都沒讀完** `S1-3B_FULL_DRAFT_SPEC`（3,157 行）／`S1-3_IA_WIREFRAME`（1,597 行，EE 完全沒讀）／`S1-6_COMPLIANCE_COPY_GUIDE`／`S2-PRD`（EE 完全沒讀）／`docs/v2/stage1/*` ⇒ 本頁的「洞」與「待確認」**可能躺在那幾份裡，⛔ 不得逕自宣稱規格沒寫**。⚠️ 線框 `live`／`partial` 的數字（`274`／`50`／`12/89`／`2330 87.5 18.20`）**全是規格示意值、非實測，本表⛔ 未當事實寫入**。
