# 五頁資料對照表 · 索引（IA v2 戰情室 · block 級）

> **這是什麼**：五頁戰情室每個 block「畫面上那個東西從哪來、怎麼算、沒資料時怎麼辦」的對照表 —— **一頁一檔**，**本檔只是索引**。
> **射程**：五頁。量測日 **2026-09-22**・branch `ui-v2`。
> 📌 **規格洞與 ⚠️ 待確認一律登記在 `docs/v2/spec/DATA_MAP_GAPS.md`（按頁分節），⛔ 五個對照表檔與本索引都不留第二份**（那是第二真相源）。
> 📌 **2026-09-22 拆檔（客戶拍板「三頁改成每頁一檔，`DATA_MAP_ALL_PAGES.md` 降為索引」）**：原本住在本檔的「🚦 今天頁」與「🔍 找標的」兩整節，已**逐字搬入**下表前兩個檔（**內容一字未改、一字未加**），本檔同批降為索引。
> ⚠️ **本檔是現況記錄，⛔ 不構成主動動工的授權**（`CLAUDE.md §-1`）。

---

## 1. 五個對照表檔

| 頁 | 檔案 | 行數（`wc -l` **實測** 2026-09-22） | block 數 |
|---|---|---|---|
| 🚦 今天 | `docs/v2/spec/DATA_MAP_TODAY.md` | **52** | 8 |
| 🔍 找標的 | `docs/v2/spec/DATA_MAP_FIND.md` | **54** | 14 |
| 📖 憑什麼 | `docs/v2/spec/DATA_MAP_WHY.md` | **57** | 22 |
| 🔬 查一檔 | `docs/v2/spec/DATA_MAP_INSPECT.md` | **⚠️ 尚未製作** | ⚠️ 尚未製作 |
| 💼 我的持股 | `docs/v2/spec/DATA_MAP_HOLD.md` | **⚠️ 尚未製作** | ⚠️ 尚未製作 |

⚠️ 後兩檔**本輪尚未製作** —— 行數欄據實留空，**⛔ 未先填一個數字**。⚠️ 行數是**會漂移的量測值**，依 `CLAUDE.md §8.2.A.0` 規則 4 已標日期；**需要時現場 `wc -l`，⛔ 不要引用本表當往後的事實**。

📌 **規格洞與 ⚠️ 待確認** → `docs/v2/spec/DATA_MAP_GAPS.md`（現有分節：`## 🚦 今天頁`／`## 🔍 找標的頁`／`## 📖 憑什麼頁`）。

---

## 2. 共通讀表前提 —— repo 裡有**兩套**實作（總管親自複驗；⛔ 少了這段，每一頁的表都會被讀反）

- **A＝原型** `src/ui_v2/`：總管以 **AST** 掃描（非 grep），取數 import **＝零** —— 只有 `__future__`／`html`／`src.ui_v2.*`／`streamlit`／`types`／`typing`。
- **B＝現行** `src/ui/views/page_*.py`（今天頁另含 `src/ui/tabs/tab_today.py`）：總管實測五頁各有一個實作（`page_today` 118KB／`page_why` 126KB／`page_find` 100KB／`page_inspect` 147KB／`page_hold` 223KB），`CLAUDE.md §8.2` 自陳「IA v2 五頁戰情室 View 層，**逐頁落地中**」⇒ **線上實際會動的是這一套**。
- **⇒ 各頁表的「資料來源」欄一律填 B**（＝資料**現在實際**從哪來），A 的狀態另以 `[A:…]` 標記。**決策者：AI 總管（技術判斷）** —— 照 A 填則整表全是「未接線」、沒有資訊量；照 B 填，這些表才是**接線藍圖**。⚠️ ⛔ **B 有值 ≠ 那一頁已經做好了**：兩套共用同一組 block key，**接線狀態相反**。
- 🔴 **`[A:未接線]` 與 `[A:無此頁]` 是兩件完全不同的事，⛔ 不可互換**：🚦 今天頁為 `[A:未接線]`（`src/ui_v2/` **有** `page_today.py`，只是取數沒接）；🔍 找標的頁與 📖 憑什麼頁為 **`[A:無此頁]`**（`src/ui_v2/` **根本沒有**對應檔）。**逐頁實況見各頁檔自己的 §0，⛔ 本索引不留第二份。**
- **徽章一律用 `UI_COMPONENTS §2` 的 `#n` 系統**；出處寫 `①~⑩`（`S2-UI_SPEC`）或「呈現層 1~8」（`S1-4 §1.2`）者**逐字照抄並標明是哪一套，⛔ 不自行換算** —— 三套編號撞號（同一個 8 兩套意思相反），見 `DATA_MAP_GAPS.md` `F-0-2`。
- **兩套 `D-*` 同名不同義，⛔ 不得補零統一**：`S2-UI_SPEC §9.3` 的 `D-1`／`D-6`／`D-7`／`D-8`（揭露落點）**⛔ 不是** `INDICATOR_SPEC`／`DECISION_RULES` 的 `D-01`／`D-06`／`D-07`／`D-08`（應然差異）—— 補零就指到另一條規則，見 `DATA_MAP_GAPS.md` `F-0-1`。
- **共通書寫紀律**（五檔一致）：⛔ 全檔不寫行號（`CLAUDE.md §8.2.A.0` 規則 1）；程式碼引用一律**全路徑** `檔名::符號`、規格引用 `檔名 §章節名`；TTL 一律寫 `shared/ttls.py` 常數名；查不到一律「⚠️ 待確認」＋寫明查過哪裡，**⛔ 不猜、⛔ 不發明資料源／算式／中文標籤**；線框示意值**⛔ 不得寫成事實**；兩組結論相左時**兩邊並陳 ＋ 標明來自哪一組，⛔ 不自行統一**。
