# UI_PRINCIPLES —— 戰情室 v2 視覺原則 24 條（第 7 份）

**基準**：客戶指定 24 條；落地值一律引用 `UI_TOKENS.md`（第 1 份）／`UI_COMPONENTS.md`（第 2 份）／`UI_PAGE_TODAY.md`（第 3 份）／原型 `docs/v2/prototype/ui_prototype_today.html` 的**實際值**，⛔ 本檔不改上述任何一份、⛔ 不發明 token 或元件。
**狀態判準（全 24 條同一把尺）**：🆕＝規格與原型**都沒有任何相關實作**（真空白）；⚠️＝**有實作但與該原則不符**（含只做到一半）；✅＝有實作且符合，且無已登記例外。
⚠️ 本檔為 WP 撰寫組單組產出，未經第二組複驗（§-2 規則 6）；**全檔免責只講這一次**。

| # | 原則 | 落地值（真實 token／元件／數值） | 狀態 |
|---|---|---|---|
| 1 | **〔視覺節奏〕**每頁只能有一個視覺焦點 | `UI_PAGE_TODAY` ①：第一層 `today.verdict` ＝ `.blk.t1` 一格滿版（border **2px**、`.sbadge b1` min-h 46、卡標 `17.5px/700`、大字 `.big 24px`）；第二層 3 卡為 `.blk.t2`（border 1px、`b2` min-h 36、卡標 `14.5px/700`）→ 焦點恰 1 個 | ✅ 一致 |
| 2 | 第二層卡片要有主次 | `UI_PAGE_TODAY` ①：`today.summary`／`today.key_banner`／`today.holdings` **三卡規格完全相同**（同 `.blk.t2`、同 `10px 12px`、同 1px border、同 `.sbadge b2`），僅 `cols` 不同（3/2/1 vs 1/1/1） | ⚠️ 牴觸 |
| 3 | 資訊密度遞減 | `UI_COMPONENTS` §1 四層單調階梯：內距 `15px 17px`→`10px 12px`→`6px 9px`→`5px 9px`；卡標 `17.5`→`14.5`→`13`→`12.5px`；徽章 min-h `46`→`36`→`28`→`26` | ✅ 一致 |
| 4 | 空白是設計的一部分 | `UI_TOKENS` §C 八級間距 `--sp-1`~`--sp-8` ＝ `4/6/8/10/12/16/22/26px` ＋ 主網格 `gap:26px` | ✅ 一致 |
| 5 | **〔數字呈現〕**數值等寬、tabular-nums、右對齊 | `UI_TOKENS` §B 數值（等寬）＝ `--mono` ＋ `font-variant-numeric:tabular-nums` ＋ `white-space:nowrap`；`UI_COMPONENTS` §4「數值欄一律 `text-align:end`」＋ `.tw td.n` | ✅ 一致 |
| 6 | KPI ≥ 20px，明細 ≤ 13px | KPI＝`UI_COMPONENTS` §4 主數值 `24px/700 --mono`（原型 `.big` 24px、`_ui_kit.render_card()` 大字 `24px/1.25`）；明細＝`UI_TOKENS` §B 數值 token `13px`、`.tw td` `13px`。⚠️ §B **無 KPI 專屬字級列** | ✅ 一致 |
| 7 | 單位與數字留半格空白 | 原型**有**寫法但走**一般空白**：`18 檔`（`:300`）／`4.12 %`（`:389`）／`-2.0 pts/月`（`:410`）；四份來源＋原型對「半格／`thinsp`／`U+2009`」**全 0 命中** ⇒ 有實作、但不是半格 | ⚠️ 牴觸 |
| 8 | 正負值用顏色＋符號雙重標示 | 原型唯一負值 `:410` `<td class="n" data-k="門檻">-2.0 pts/月</td>` —— **只有負號、沒有顏色**（`.n` 僅管等寬與右對齊）；可用綠／紅是 `UI_TOKENS` A-2 **狀態保留色**（⛔ 不得挪用）⇒ 雙通道只做到一半 | ⚠️ 牴觸 |
| 9 | **〔狀態呈現〕**狀態色配圖示＋文字 | `UI_TOKENS.md:35` A-2 逐字「⛔ **一律**配圖示＋文字，不得只靠顏色」，且 `:43` 已為 `--sig-grey` 指派 ⚪「還沒載入 · 不適用」；但 `UI_COMPONENTS.md:81` 停用態按鈕字色用 `--sig-grey` 卻**未帶該圖示**，原型 `:452` `<button type="button" class="cta dis" disabled>` 同樣無圖示 ⇒「一律」有例外 | ⚠️ 牴觸 |
| 10 | 壞消息不全紅 | `UI_COMPONENTS` §2：`--sig-red` **僅** #6「🔴 取得失敗」；判斷面走 #4 `--sig-amber`「🟠 門檻已失準」與 #10 `--sig-blue`「◆ 只描述，不判等級」；訊號頻道只出中文標籤，`_ui_kit.assert_signal_text_clean()` 建構期擋 | ✅ 一致 |
| 11 | 缺值用 `⚠︎ —` 或 `N/A` | 徽章列守規（§2 #7 `⚠︎ —`／#8 `N/A`；原型 `:302/:304/:306` 實作相符）；但 `UI_PAGE_TODAY.md:122-123` 逐字「大字區**留白**（`render_card` 灰態與紅態一律留白），⛔ 不得顯示 `0` 或上一輪殘值」⇒ KPI 數值區走**留白**、不走那兩個語彙 | ⚠️ 牴觸 |
| 12 | **〔版面留白〕**卡片內距 ≥ 12px | **兩份規格對同一對象給兩個值**：`UI_TOKENS.md:93` `.blk` 卡片內距 `9px 11px`（≤640 `6px 9px`）vs `UI_COMPONENTS` §1 四層 `15px 17px`／`10px 12px`／`6px 9px`／`5px 9px`（t1 ≤640 `11px 12px`）；`.pan` `12px 14px`。僅 t1 桌機與 `.pan` 達標 | ⚠️ 牴觸 |
| 13 | 卡片間距 ≥ 8px | 同一型矛盾：`UI_TOKENS.md:95` 卡片間距 `margin-top:9px`（≤640 `6px`）vs `UI_COMPONENTS` §1 `margin-top` t1 `15px`／t2 `10px`／t3 **`6px`**／t4 **`5px`**（手機 t2~t4 皆 `6px`）。t1／t2 達標 | ⚠️ 牴觸 |
| 14 | 區塊間距 ≥ 20px | `UI_TOKENS` §C 區塊間距 `margin-top:12px`、網格 `gap:12px`（`--sp-5`）；主網格 `gap:26px`／`margin-top:22px` | ⚠️ 牴觸 |
| 15 | 首屏留白 ≥ 20% | **無對應 token、亦無首屏高度基準** —— 五頁規格對首屏**一律**要求「需實機量測」（`UI_PAGE_TODAY` ①、`UI_PAGE_FIND.md:32`）、⛔ 不猜 ⇒ 本條無基準可比對 | 🆕 新增 |
| 16 | **〔字型層級〕**一頁 ≤ 4 種字級 | `UI_TOKENS` §B（`:75-81`）**7 列／相異 6 種**（`:80` 說明與 `:81` 徽章同為 `11.5px`）；原型單頁實測 **13 種** font-size（`clamp(26px,4.4vw,40px)`／`clamp(20px,2.6vw,27px)`／`24`／`17.5`／`14.5`／`13.5`／`13`／`12.5`／`12`／`11.5`／`11`／`10.5`／`9.5px`） | ⚠️ 牴觸 |
| 17 | 字重差 ≥ 300 | `UI_TOKENS` §B 字重階梯 `900`(L1)／`800`(L2)／`700`(L3卡標)／`600`(徽章)／`500`(數值)／`400`(內文·說明)：相鄰級差多為 **100**，僅「內文 400 ↔ 卡標 700」達 300 | ⚠️ 牴觸 |
| 18 | 註解 ≥ 11px | `UI_TOKENS` §B 說明 `11.5px` ✅；但 `UI_COMPONENTS` §4 手機卡片流 key **`9.5px`**（原型 `.key`／`.tw td::before` 同值）、§2 `.sbadge b4` **`10.5px`** | ⚠️ 牴觸 |
| 19 | **〔互動〕**hover 有回饋 | `UI_COMPONENTS` §3 五類按鈕各有 hover 欄：主 CTA 底＋框→`--ochre`、次級（說明）底 `--panel-2`、次級（展開佐證）底 `--panel-2`＋框 `--rule`、文字按鈕 字 `--ink`＋框 `--rule-2`；停用態 hover 欄逐字「無」（刻意） | ✅ 一致 |
| 20 | 動效 ≤ 200ms | **無對應 token**（兩份規格 `transition`／`動效` 0 命中）；原型 `:123` `.caret{…transition:transform .12s linear}` ＝ 120ms 達標，`:147` `.sk{…animation:pulse 1.6s ease-in-out infinite}` ＝ **1600ms** | ⚠️ 牴觸 |
| 21 | 焦點環全站統一 | `UI_COMPONENTS` §3「全站唯一一條 `:focus-visible{outline:2px solid var(--focus);outline-offset:2px}`，⛔ 不准個別 `outline:none`」；`--focus` dark `#8fc0ff`／light `#14508f` | ✅ 一致 |
| 22 | **〔響應式〕**手機優先 | 兩種方向並存：原型 `.g3` 基準單欄 ＋ `@media (min-width:641px) and (max-width:880px)`（`:190`）／`@media (min-width:881px)`（`:195`）加欄（手機優先）；但 §1 四層內距以桌機值為基準、手機值括號覆寫，原型 `.blk.t1` 亦為基準 `15px 17px` ＋ `@media (max-width:640px)` 降級（桌機優先），§4 表格三斷點表自「桌機 ≥881」起列 | ⚠️ 牴觸 |
| 23 | 表格手機降級為卡片流 | `UI_COMPONENTS` §4 手機卡片流：≤640 不畫表頭、一列→一張 `.blk.t3`（`6px 9px`／`radius:2px`／`margin-top:6px`）、識別欄升 `.bt .t 13px/700`、主數值升 `24px/700`、其餘降 `key　value`（key `9.5px --ink-3`／value `11.5px/700`）、每卡至多 4 對。⚠️ 揭露：`UI_PAGE_TODAY.md` 對「表格／卡片流／`.tw`」**0 命中**（頁級規格未寫），本條靠 §4「三斷點共通」通則 ＋ 原型 `:169-188` 實作承接 | ✅ 一致 |
| 24 | 觸控目標 ≥ 44×44 | `UI_COMPONENTS` §3：僅「次級（展開佐證）」`min-height:44px`／`min-width:44px` 達標；主 CTA `40px`、次級（說明）`40px`、停用態 `40px`、文字按鈕（表頭明細）**`28px`** | ⚠️ 牴觸 |

## 牴觸清單（14 條；每條並陳處置，⛔ 本組不決定）

**⚠️ 2 · 第二層卡片要有主次** —— `UI_PAGE_TODAY` ① 第二層三卡全為 `.blk.t2`，內距／border／卡標／徽章尺寸**逐項相同**；`UI_COMPONENTS` §1「層級只靠 border-width／dashed／padding 三個通道區分」只規範**跨層**，層內無主次通道（`.blk.flagged` 是標記態，非主次）。
處置：(A) 改規格 —— 於 §1 增訂一條層內主次通道；(B) 改原則 —— 本條改為「跨層有主次即可，層內平權」。

**⚠️ 7 · 單位與數字留半格空白** —— 原型三處單位寫法皆為**一般空白**（`:300` `18 檔`／`:389` `4.12 %`／`:410` `-2.0 pts/月`）；四份來源＋原型對「半格／`thinsp`／`U+2009`」**全 0 命中** ⇒ 有寫法、只是不是半格（非真空白）。
處置：(A) 改規格 —— 於 `UI_TOKENS` §B 數值 token 增訂單位分隔字元（⚠️ 動到第 1 份，須客戶另行授權）；(B) 改原則 —— 改為「單位與數字之間**須有分隔**，一般空白即可」。

**⚠️ 8 · 正負值顏色＋符號雙重標示** —— 原型唯一負值 `:410` `<td class="n" data-k="門檻">-2.0 pts/月</td>`：**有負號、無顏色**（`.n` 僅管等寬與右對齊）；可用的綠／紅是 `UI_TOKENS` A-2 **狀態保留色**，A-2 逐字「⛔ 不得挪用為圖表第 N 條線」⇒ 雙通道只做到符號那一半。
處置：(A) 改規格 —— 為正負值另立**非保留色**的色對（⚠️ 屬新增 token，須客戶授權，且須補 deuteranopia ΔE 實算）；(B) 改原則 —— 改為「正負值**至少**以符號標示；顏色僅在不與狀態保留色衝突時附加」。

**⚠️ 9 · 狀態色配圖示＋文字** —— `UI_TOKENS.md:35` A-2 逐字「⛔ **一律**配圖示＋文字，不得只靠顏色」，`:43` 並已為 `--sig-grey` 指派 ⚪「還沒載入 · 不適用」；但 `UI_COMPONENTS.md:81` 停用態按鈕字色用 `--sig-grey` 而**未帶 ⚪**，原型 `:452` 實作亦無圖示 ⇒「一律」有例外。📌 **據實更正**：裁定書寫「圖示欄逐字為『無』」—— 實測 §3 按鈕表**沒有圖示欄**（欄位為 類／高度／內距／字級／圓角／底色／框色／hover／範例），逐字「無」者是 **hover 欄**；**牴觸本體不受影響**（該列從未指派圖示）。
處置：(A) 改規格 —— §3 停用態列補上 A-2 已指派的 ⚪ 或改用非保留色字色；(B) 改原則 —— A-2 的「一律」限縮為「**徽章／訊號頻道**一律配圖示＋文字，停用態等**不承載訊號**的元件豁免」。

**⚠️ 11 · 缺值用 `⚠︎ —` 或 `N/A`** —— 兩種缺值表達並存：徽章列走 `UI_COMPONENTS` §2 #7 `⚠︎ —`／#8 `N/A`（原型 `:302/:304/:306` 相符），但 `UI_PAGE_TODAY.md:122-123` 逐字要求大字區**留白**（「`render_card` 灰態與紅態一律留白」、⛔ 不得顯示 `0` 或上一輪殘值）⇒ KPI 數值區**不走**那兩個語彙，且**無**「何時留白／何時出語彙」的分工規則。
處置：(A) 改規格 —— 增訂分工條（例：徽章出語彙、大字區留白，同卡並存）；(B) 改原則 —— 改為「缺值⛔ 不得顯示 `0` 或殘值；表達方式（語彙或留白）依元件類型另定」。

**⚠️ 12 · 卡片內距 ≥ 12px** —— **真正的問題是兩份規格對同一對象給了兩個值**：`UI_TOKENS.md:93` `9px 11px`（≤640 `6px 9px`）vs `UI_COMPONENTS` §1 四層 `15px 17px`／`10px 12px`／`6px 9px`／`5px 9px`。**兩者互斥、不是「§C 預設＋§1 覆寫」的分工** —— `UI_COMPONENTS.md:17` `.blk` 基準逐字只有 `background`／`border`／`border-radius`，**無 padding 欄可被覆寫**；且 `grep -F "9px 11px"` 在四份來源＋原型**只命中 `UI_TOKENS.md:93` 自己那一行**（§1 與原型從未實作該值）。旁證：§C 的 ≤640 值 `6px 9px` 恰等於 §1 的 **t3 桌機值** ⇒ §C 是**四層化之前的單層模型殘留**。⇒ **先裁定哪一份是 SSOT，再談 ≥12px 門檻**；現況僅 t1 桌機與 `.pan` `12px 14px` 達標。
處置：(A) 改規格 —— 四層內距整組抬到 ≥12px（會壓縮 t1→t4 密度階梯，§1 三通道之一失效）；(B) 改原則 —— 門檻改為「t1 ≥12px、t2~t4 依密度階梯遞減」；(C) **先消除跨檔矛盾** —— 將 `UI_TOKENS` §C「卡片內距」列標為**已被 §1 四層取代**、SSOT 歸 §1，再套 (A)／(B)（⛔ 本組不得改第 1 份，僅在此登記）。

**⚠️ 13 · 卡片間距 ≥ 8px** —— 同一型跨檔矛盾：`UI_TOKENS.md:95` `margin-top:9px`（≤640 `6px`）vs `UI_COMPONENTS` §1 t1 `15px`／t2 `10px`／t3 `6px`／t4 `5px`（手機 t2~t4 一律 `6px`）。`grep -F "margin-top:9px"` 在四份來源＋原型**只命中 `UI_TOKENS.md:95` 自己那一行** ⇒ §1 與原型從未實作 `9px`。⇒ **先裁定 SSOT，再談 ≥8px 門檻**；現況 t1／t2 達標，t3／t4 與全部手機值未達。
處置：(A) 改規格 —— t3／t4 與手機值抬到 `--sp-3`(8px)；(B) 改原則 —— 門檻降為 ≥5px，或僅對 t1／t2 適用；(C) **先消除跨檔矛盾** —— 將 `UI_TOKENS` §C「卡片間距」列標為**已被 §1 四層取代**、SSOT 歸 §1（⛔ 本組不得改第 1 份，僅在此登記）。

**⚠️ 14 · 區塊間距 ≥ 20px** —— 實際值：`UI_TOKENS` §C 區塊間距 `margin-top:12px`、網格 `gap:12px`（`--sp-5`）；只有主網格 `gap:26px`／`margin-top:22px` 達標。
處置：(A) 改規格 —— 區塊間距改用 `--sp-7`(22px)；(B) 改原則 —— 門檻降為 ≥12px，20px 僅適用主網格。

**⚠️ 16 · 一頁 ≤ 4 種字級** —— 實際值：`UI_TOKENS` §B `:75-81` 為 **7 列／相異 6 種**（`:80` 說明與 `:81` 徽章同為 `11.5px`）；原型單頁實測 **13 種**。
處置：(A) 改規格 —— 合併字級 ramp（如 12/12.5/13/13.5 四檔收成一檔、10.5/11/11.5 收成一檔）；(B) 改原則 —— 改為「**同一卡層內** ≤4 種字級」，讓四層階梯本身不算超標。

**⚠️ 17 · 字重差 ≥ 300** —— 實際值：`UI_TOKENS` §B 為 900／800／700／600／500／400，L1→L2→L3 相鄰級差皆為 **100**（該三級另以字級 40→27→17.5px 區分）。
處置：(A) 改規格 —— 字重收成 900／600／400 三檔；(B) 改原則 —— 改為「**字重為唯一區分通道時**才需差 ≥300；已另有字級差者不受限」。

**⚠️ 18 · 註解 ≥ 11px** —— 實際值：最小 **9.5px**（`UI_COMPONENTS` §4 手機卡片流 key ＋ 原型 `.key`／`.tw td::before`），次小 **10.5px**（`.sbadge b4`）；說明 token `11.5px` 與 `.sbadge b3`／文字按鈕 `11px` 達標 ⇒ 小於 11px 者共 2 檔。
處置：(A) 改規格 —— key 9.5→11px、b4 10.5→11px（⚠️ `UI_TOKENS` A-1 與 §4 的 `--ink-3` 對比實算是按 9.5px 走小字 4.5:1 門檻做的，抬字級不影響該 PASS，但兩份「改一邊就要改另一邊」的鎖定關係須同步）；(B) 改原則 —— 門檻降為 ≥9.5px。

**⚠️ 20 · 動效 ≤ 200ms** —— 實際值：原型 `:123` `.caret` 逐字 `transition:transform .12s linear` ＝ 120ms（達標）；`:147` 載入骨架 `.sk` 逐字 `animation:pulse 1.6s ease-in-out infinite` ＝ **1600ms**。兩者皆已帶 `@media (prefers-reduced-motion:reduce)` 關閉（`:124`／`:150`）。
處置：(A) 改規格 —— 骨架動畫週期壓到 ≤200ms（⚠️ 會變成每秒 5 次閃爍，屬閃爍風險，本組據實揭露此後果）；(B) 改原則 —— 改為「**狀態轉場** ≤200ms；載入中的迴圈動畫不受此限，但須提供 reduced-motion 關閉」。

**⚠️ 22 · 手機優先** —— 實際值：`.g3` 走 `@media (min-width:641px) and (max-width:880px)`（`:190`）／`@media (min-width:881px)`（`:195`）加欄（手機優先），而四層內距／表格章節走 `max-width:640px` 降級與「桌機 ≥881 起列」（桌機優先）。同一份規格兩種方向並存。另 `UI_COMPONENTS` 開頭已自行登記「CSS 現況 `640/940/560` 與宣告版 `640/880/881` 不一致，屬實作待修」。
處置：(A) 改規格 —— 四層內距改以手機值為基準、桌機以 `min-width` 往上疊加，全檔統一為手機優先；(B) 改原則 —— 改為「**斷點宣告順序**手機優先即可，CSS 基準值方向不拘」。

**⚠️ 24 · 觸控目標 ≥ 44×44** —— 實際值：5 類按鈕中僅「次級（展開佐證）」`44×44` 達標；主 CTA／次級（說明）／停用態皆 `min-height:40px`，文字按鈕（表頭明細）`min-height:28px`。`UI_COMPONENTS` §3 自陳主／次高度是「由 padding＋border＋行高推得 39px，**進位 40**」的新訂值。
處置：(A) 改規格 —— 三類 40px 抬為 44px、文字按鈕 28→44px（⚠️ 文字按鈕在表頭狀態列，抬高會改變 §4 表頭 `min-height:36px` 的行高關係）；(B) 改原則 —— 改為「**主要觸控目標** ≥44×44；表頭等次要觸發器 ≥28px 並留 ≥8px 間隙」。

**以上 14 條，待客戶裁示。**
