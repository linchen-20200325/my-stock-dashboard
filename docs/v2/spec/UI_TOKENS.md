# UI_TOKENS —— 戰情室 v2 設計 token（第 1 份）

**基準：線框稿 `docs/v2/wireframe/warroom_ia_v2.html:root` 色票**（客戶逐條審過、自帶深色覆寫與等寬字欄寬，
而 app 現況是三套狀態色／四種灰／兩種黑並存的累積漂移）。**深色為實際運行模式，dark 值為主要交付；light 值次要。**
⚠️ 本檔為 UI 設計組 WB 單組產出，未經第二組複驗（§-2 規則 6）；全檔免責只講這一次。

## 0. 落地位置與兩個既有約束

| 項目 | 規定 |
|---|---|
| token 落地 | **新訂** `shared/ui_tokens_v2.py`（本 repo 自有）＋ 單一 `<style>:root{}` 注入；⛔ 不得改 `shared/colors.py` |
| `shared/colors.py` | 檔頭自陳 AUTO-SYNCED FROM my-fund-dashboard；本 repo 改了會被 `scripts/sync_to_stock.sh` 蓋掉 → **維持鏡像不動** |
| `.streamlit/config.toml` | 釘死 `base="dark"`。`backgroundColor` 現為 `#0e1117`，與線框 `--paper` dark `#0e141b` 差 3 碼 → **須改的那一鍵＝`backgroundColor` = `#0e141b`**（⛔ 本檔不動它） |
| 同檔後續鍵 | `secondaryBackgroundColor`→`#151d26`、`textColor`→`#dbe5ef`、`primaryColor`→`#d59a5e`；`font="sans serif"` 是 Streamlit 列舉值、**不是**字型堆疊，堆疊只能由 §B 的 CSS 提供 |
| `--review-*` | **只在審稿模式渲染**（客戶 2026-09-16 明令）；正式畫面 `[data-review-only]` 一律 `hidden` |

## A. 色彩 token

### A-1 主色／輔色／面與墨

| token | dark | light | 用途 | 配套圖示/文字 | 對應現況 |
|---|---|---|---|---|---|
| `--ochre` | `#d59a5e` | `#96591f` | 主色：強調、待辦標記 | 文字標籤 | `app.py:.strategy-card` border-left `#ffd700` |
| `--ochre-bg` / `--ochre-line` | `#2a2013` / `#8a6234` | `#f6ecdf` / `#c08a4e` | 主色底／框 | — | 新訂 |
| `--focus` | `#8fc0ff` | `#14508f` | 輔色：焦點環、連結 | — | `app.py:.stTabs [aria-selected]` `#1f6feb` |
| `--paper` / `--panel` / `--panel-2` | `#0e141b` / `#151d26` / `#1b242f` | `#f6f5f1` / `#fffefc` / `#edece7` | 畫布／卡面／次卡面 | — | `.streamlit/config.toml:backgroundColor` `#0e1117` |
| `--grid` / `--rule` / `--rule-2` | `#1a242e` / `#2e3b48` / `#46545f` | `#e5e3dc` / `#c6ccd2` / `#9aa4af` | 圖表格線／分隔線／強分隔線 | — | `macro/section_short.py:yaxis.gridcolor` `#21262d` |
| `--ink` / `--ink-2` | `#dbe5ef` / `#9fb0c1` | `#15293f` / `#4b5b6d` | 主文字／次文字 | — | `config.toml:textColor` `#e6edf3`；`macro_helpers.py:'color'` `#8b949e` |
| `--ink-3` | **`#7d8d99`** | **`#5f6c77`** | 三級文字（說明） | — | 線框 light 原 `#7d8b99` **對 `--paper`** 3.19:1；前輪 `#626f7a` 也**只對 `--paper`** 調到 4.72:1、卡面仍 FAIL → 本輪改以**三背景最差值**定錨（見下註） |
| `--review-bg`/`-line`/`-ink` | `#1d1b28`/`#8b82b4`/`#c3bbe4` | `#eceaf2`/`#6f6790`/`#4a4270` | 治理標記（**僅審稿模式**） | ★📌 ＋文字 | 新訂 |

⚠️ **`--ink-3` 一律對 `--paper`／`--panel`／`--panel-2` 三個背景都量，⛔ 不得只對 `--paper` 定錨** —— 它的落點幾乎全在卡面（§B「說明」字級、`UI_COMPONENTS` §4 手機卡片流的 key、卡內佐證文字）。**本輪六格實算**（WCAG 相對亮度公式，非目測）：dark `#7d8d99` ＝ **5.41／4.97／4.58**、light `#5f6c77` ＝ **4.94／5.34／4.55** → **六格全 ≥ 4.5 PASS**。⛔ 不得套 WCAG 大字 3:1 寬鬆標準：key `9.5px`、說明 `11.5px`，遠低於大字門檻（18.66px bold／24px）。
📌 **舊值實測（缺陷本體，2026-09-16 修）**：dark `#73838f` ＝ 4.73／**4.35 FAIL**／**4.01 FAIL**、light `#626f7a` ＝ 4.72／5.11／**4.36 FAIL**，**六格中三格 FAIL**；舊註只寫「下修至 4.72:1」而**未標背景**，讀起來像通吃的 PASS —— **這就是本缺陷沒被早點抓到的原因**。**層次未被犧牲**（自算）：對 `--ink-2` 的 ΔL\* dark 13.30／light 6.94、ΔE2000 dark 11.20／light 7.08，且 `--ink`→`--ink-2`→`--ink-3` 的 L\* 兩模式皆嚴格單調；新值**只動 L\***（dark h 249.8→249.6·C\* 8.96→8.86；light h 253.3→253.4·C\* 8.07→8.10）→ 冷灰調不變。⚠️ **副作用據實揭露**：`--ink-3`↔`--sig-grey` 拉近（dark ΔE2000 7.62→4.09、light 4.16→3.04），兩者不同通道（`--sig-grey` 僅徽章前景）且 A-2 已硬性要求「配圖示＋文字」、顏色非唯一載體 —— 但 ⛔ **不得讓兩者同框而只靠顏色分辨**。

### A-2 狀態色（保留色：⛔ 不得挪用為圖表第 N 條線；⛔ 一律配圖示＋文字，不得只靠顏色）

| token | dark ink / bg | light ink / bg | 用途 | 配套圖示＋文字 | 對應現況 |
|---|---|---|---|---|---|
| `--sig-green` | `#57b985` / `#11241a` | `#1c6f43` / `#e3f0e9` | 正常 | 🟢「正常」 | `shared/colors.py:TRAFFIC_GREEN` `#22c55e` |
| `--sig-amber` | `#d3a43c` / `#292110` | `#87600a` / `#f6efdb` | 警告 | 🟡「警告」 | `shared/colors.py:TRAFFIC_YELLOW` `#eab308` |
| `--sig-red` | `#e8706b` / `#2b1615` | `#ae2f2b` / `#f8e7e5` | 錯誤 | 🔴「出錯了」 | `shared/colors.py:TRAFFIC_RED` `#ef4444` |
| `--sig-blue` | **`#4b8efe`** / `#141f2b` | **`#044cb6`** / `#e4ecf3` | **已評估·結論中性** | ◆「已評估 · 中性」 | **新訂**（線框無此格） |
| `--sig-grey` | `#8b98a4` / `#1b232b` | **`#586470`** / `#eae9e4` | **缺漏／不適用／未評估** | ⚪「還沒載入 · 不適用」 | `macro_v2_cards.py:STATE_META["missing"]` `#8a8e96` |
| `--sig-neutral` / `--sig-neutral-bg` | **`#8fa3b8`** / **`#26313d`**（原 ~~`#1f2a36`~~） | **`#1e3b4f`** / **`#d7dce1`**（原 ~~待訂~~） | **資料不完整（N／M 部分計入）** | ◧「N／M 計入」 | **新訂**（客戶 2026-09-21 裁示；`--sig-neutral-bg` 同日自 `#1f2a36` 改採總管建議值，`--sig-neutral` 未動，見 A-2.1；**light 兩碼為 AI 總管拍板（客戶只指派「補這個洞」、⛔ 未指定值），量測與可推翻性見附錄 A-2.2**；線框無此格） |

⛔ `--sig-grey` 是缺漏態**專用**，與「數值 0」及 `--sig-green` 不共用任何視覺；`--sig-blue` 與 `--sig-grey`
在 dark 的未模擬 ΔE 15.8、light 16.3（均 ≥15 門檻；⚠️ **度量＝OKLab 歐氏距離 ×100，⛔ 非 CIEDE2000**，見附錄 A-2.2）→ **「缺漏」與「判過了但中性」眼睛分得開**（D-15／N-7）。
light `--sig-grey` 自線框 `#6c7883` 下修（原對自身 bg 僅 3.71:1）。

#### A-2.1 `--sig-neutral` / `--sig-neutral-bg` 的量測與未達成事項（2026-09-21 新增；**同日換色後更新**）

**用途**：僅供 `UI_COMPONENTS` §2 狀態徽章 **#9「資料不完整」**（底色＋文字色）。⛔ 框色不在本對範圍內，#9 框色維持 `--sig-blue`。
~~**light 值＝待訂**：客戶 2026-09-21 只指定 dark 兩碼，⛔ 本檔不得自行發明 light 值；light 模式落地前必須回頭補量。~~ → **2026-09-21 已補上 light 兩碼**：`--sig-neutral` **`#1e3b4f`**／`--sig-neutral-bg` **`#d7dce1`**。**有意識的政策變更，不是漏刪**。⚠️ **決策者＝AI 總管，⛔ 不是客戶** —— 客戶做的是**指派**（原話：「【先補兩個洞】…2. `--sig-neutral` 的 light 兩碼」），**⛔ 沒有指定值**；兩碼是總管從**兩組獨立量測**（A 組 `#3c5063`／`#d9dce0`、B 組 `#1e3b4f`／`#d7dce1`）中**選 B 組**，理由是前景 vs light `--sig-grey`：B 組 OKLab×100 **16.02 PASS** vs A 組 **7.66 FAIL**。⛔ **不得寫成「客戶核准」「客戶已拍板」** —— 那會讓後人以為這個值已經過客戶拍板、不敢動也不會回頭問。⚠️ **代價**：`#1e3b4f` 的 L\* 23.57，比其餘五個 light 狀態色前景（35.14~43.57）深約 12 ⇒ 徽章字會比同儕明顯重。⭐ **客戶可一句話改採 A 組的 `#3c5063`／`#d9dce0`**（換得字重與同儕一致，代價是放棄 ≥15 分離）—— 顏色是客戶決定的，總管只是在客戶沒指定值時做了一個**可被推翻**的選擇。舊句在它寫下的當天是對的（當時確實只有 dark 兩碼，「⛔ 不得自行發明」至今仍然有效），只是「待訂」這個前提已不成立。完整量測、B／A 取捨與未驗事項見**附錄 A-2.2**。
📌 **2026-09-21 換色（決策者 user，客戶裁示採用總管建議值）**：`--sig-neutral-bg` 自 `#1f2a36` 改為 **`#26313d`**；`--sig-neutral` **維持 `#8fa3b8` 不變**。依本檔慣例，**舊值量測保留不刪 + 加刪除線 + 註明理由**，⛔ 不得整段刪掉。

- ~~✅ **文字對比達標**：`#8fa3b8` on `#1f2a36` ＝ **5.61:1**，高於 WCAG AA 小字門檻 4.5:1（徽章字級 11px／10.5px，⛔ 不適用大字 3:1 寬鬆標準），亦高於客戶要求的 3:1。~~
  → **舊值 `#1f2a36` 的量測，2026-09-21 客戶改採 `#26313d` 後不再適用。**
  ✅ **新值仍達標**：`#8fa3b8` on `#26313d` ＝ **5.10:1**，高於 WCAG AA 小字門檻 4.5:1（徽章字級 11px／10.5px，⛔ 不適用大字 3:1 寬鬆標準）。
- ~~⚠️ **但「與 `--sig-blue-bg` 明顯不同」這項未達成**：`#1f2a36` vs `--sig-blue-bg` `#141f2b` ＝ **對比 1.14:1、ΔE2000 3.45**；vs 卡面 `--panel` `#151d26` ＝ **1.17:1、ΔE2000 4.26**。對照：**原本 #9 用的 `--panel-2` 對卡面是 1.08:1** ⇒ 新值只改善 0.09，**「中性底在卡上看不見」這個原始問題並未解決**。~~
  → **舊值 `#1f2a36` 的量測與「未達成」判定，2026-09-21 客戶改採 `#26313d` 後不再適用。**
  ✅ **新值達成**：`#26313d` vs `--sig-blue-bg` `#141f2b` ＝ **ΔE2000 5.67**；vs 卡面 `--panel` `#151d26` ＝ **ΔE2000 6.36** —— 兩者皆進入「一眼可辨」區間（ΔE2000 2~10）⇒ **「與 #10 明顯不同」本次達成。**
- 📌 ~~**總管建議值（待客戶裁示，本輪未採用）**~~ → **客戶已於 2026-09-21 採納**：**`#26313d`**（量測同上條）。
  （另評估過 `#2b3947`：ΔE 8.18 更清楚，但文字對比掉到 **4.55**，只比 AA 底線高 0.05，餘裕太薄，**不建議**；⛔ 未採用。）
- ~~⚠️ **本輪照客戶指定的 `#1f2a36` 落地**~~ → **現行落地值為 `#26313d`**；顏色屬客戶決定，總管只負責量測與揭露。

⚠️ **查證分級（2026-09-21 換色後更新）**：上列**舊值 `#1f2a36`** 各項為總管實算，且**撰寫組 WE2 已獨立複算、逐項數字相符**（WCAG 相對亮度公式＋ΔE2000，非目測）。
⚠️ **新值 `#26313d` 的三項數字（5.10:1／ΔE2000 5.67／6.36）為總管實算，本輪撰寫組僅照錄、未獨立複算**（§-2 規則 6）→ ⛔ 不得當成已複驗的事實引用。
⚠️ **以下為 WE2 自算、總管交辦範圍外的相鄰風險（單組未複驗，⛔ 不得當既定前提）**：
- 🔴 **仍未解決（前景，本輪未動）**：`--sig-neutral` `#8fa3b8` vs `--sig-grey` `#8b98a4` ~~＝ **ΔE2000 4.85**~~ ＝ **OKLab×100 3.70**，**遠低於本檔 A-2 自訂的 ≥15 可辨門檻（度量＝OKLab×100）**。（⚠️ **2026-09-21 度量單位事實更正，不是漏刪**；決策者 **AI 總管**。舊數字是 **CIEDE2000**，卻拿去比 **OKLab×100** 的 15 門檻 —— **兩把不同的尺互比**；用門檻自己的度量，該對是 **3.70**。⚠️ **結論 FAIL 不變，只是引用的數字單位錯了。** 兩邊理由並陳：舊寫法的用意是「用一把大家都熟的尺（CIEDE2000）表達差很遠／差很近」，**這個用意至今成立**；被權衡掉的原因是**它跟門檻不是同一把尺**，讀者會以為 4.85 vs 15 的差距就是實際差距。完整說明見附錄 A-2.2。）#9 在 §2 徽章表中與 #2／#3／#5／#7／#8 五列同框，那五列前景全是 `--sig-grey` ⇒ **新前景與灰系前景幾乎同色**，靠 A-2「⛔ 一律配圖示＋文字」擋，⛔ 不得讓兩者同框而只靠顏色分辨。
  ⚠️ **2026-09-21 換色只換底色、⛔ 沒換前景** —— **底色（vs `--sig-blue-bg`／`--panel`）已解決，前景（vs `--sig-grey`）仍未解決**，⛔ 不得讀成「換色把兩個問題一起修好了」。
- ~~`--sig-neutral-bg` `#1f2a36` vs `--sig-grey-bg` `#1b232b` ＝ **ΔE2000 2.98**；vs `--panel-2` `#1b242f` ＝ **ΔE2000 1.98**（即新底色與它要取代的舊底色本身也近乎同色，與上條「只改善 0.09」同一件事的另一個量法）。~~
  → **舊值 `#1f2a36` 的量測，2026-09-21 客戶改採 `#26313d` 後不再適用。**
  ~~⚠️ **但新值 `#26313d` 對這兩個背景（`--sig-grey-bg`／`--panel-2`）本輪未重算** —— ⛔ 不得讀作已解決，light 值補量時一併補。~~ → **2026-09-21 light 值補量時已一併補算**（**有意識的更正，不是漏刪**；決策者 **AI 總管**）：vs `--sig-grey-bg` `#1b232b` ＝ **ΔE2000 4.77**、vs `--panel-2` `#1b242f` ＝ **ΔE2000 4.10** —— **兩格都進了 2~10 可辨帶**（對照舊值 `#1f2a36` 的 **2.98**／**1.98**，兩格都貼著 2.0 底線、其中 `--panel-2` 那格根本在帶外）⇒ **那次換色連帶把這個缺口也補上了，只是當時沒人算。** ⭐ **查證分級：這兩個數字是總管＋A 組＋B 組三方各自獨立算出、彼此相符**，是本檔少數真的有三方複驗的數字 —— ⚠️ 其可信度**高於**本檔其餘單組數字，引用時請注意這個差別。詳見附錄 A-2.2。
- `--sig-neutral` 對三背景的文字對比：`--panel` **6.55:1**／`--paper` **7.14:1**／`--panel-2` **6.04:1**（比照 A-1 `--ink-3` 三背景定錨慣例，三格皆 ≥ 4.5 PASS）。**前景未動 ⇒ 本組數字不受換色影響。**

### A-3 圖表序列識別色（7 槽，固定順序、⛔ 不循環；第 8 條以上折成「其他」或分面）

| 槽 | dark | light | 用途 | 對應現況（⛔ 全部**被取代**，非沿用） |
|---|---|---|---|---|
| 1 | `#2e64a6` | `#2482eb` | 序列 1 | `shared/colors.py:COLORS_7[0]` `#58a6ff` |
| 2 | `#c7843c` | `#b47227` | 序列 2 | `COLORS_7[2]` `#ffd700` |
| 3 | `#209993` | `#1b9690` | 序列 3 | `COLORS_7[5]` `#79c0ff` |
| 4 | `#c15dde` | `#b118a5` | 序列 4 | `COLORS_7[4]` `#bc8cff` |
| 5 | `#6f650d` | `#5b5e00` | 序列 5 | `COLORS_7[1]` `#22c55e` |
| 6 | `#934d6b` | `#ff3282` | 序列 6 | `COLORS_7[3]` `#ef4444` |
| 7 | `#7a3ae2` | `#7612e0` | 序列 7 | `COLORS_7[6]` `#ff9f43` |

**框架：這不是「3 槽擴 7 槽」，是把一組不合格的色整組換掉** —— 現行 `COLORS_7` 實跑自己 FAIL 三項：明度帶 7 之 6 出界、CVD `#bc8cff↔#58a6ff` ΔE 2.7、**一般視覺 `#79c0ff↔#58a6ff` ΔE 8.1**（驗證器註「低於 15，連全色覺的人都分不出來」）。
**驗證器實跑**（`validate_palette.js --pairs all`）：dark（surface `#0e141b`）**5/5 PASS**（最差 protan/deutan 槽 4↔7 ΔE 8.3、一般視覺 15.4）、light（`#f6f5f1`）**5/5 PASS**（最差 protan/deutan 槽 2↔6 ΔE 9.4、一般視覺 槽 4↔7 ΔE 16.0）；
狀態色五枚 ink-on-own-bg WCAG AA 4.5:1 **dark 5/5、light 5/5 PASS**；`--sig-blue` vs 序列槽 1 ΔE 17.0 **PASS**（修過才過的槽 1 自 `#4996f5` 改 `#2e64a6`；light `--sig-grey` 見 A-2、`--ink-3` 見 A-1）。
⚠️ **本行範圍＝「A-3 七槽＋狀態色 ink-on-own-bg＋`--sig-blue`↔槽 1」三組，⛔ 非全檔宣告。** 原文「**無 FAIL 殘留**」是**已被否證的全稱句** —— 它當時就不涵蓋 `--ink-3`，而 `--ink-3` 在 `--panel`／`--panel-2` 上實為 FAIL（六格中三格，見 A-1 註，2026-09-16 已修）。⛔ 日後不得再寫不標範圍的「無 FAIL 殘留」。
🔴 **有拘束力的附帶條件（必須實作；客戶 2026-09-16 裁示，門檻自「第 6、7 槽」下修）**：**同時使用 ≥4 槽時，一律強制直接標籤或紋理（`dash`／`dot`），⛔ 不得只靠顏色區分。**（≤3 槽**僅 dark 免除**：實測前 3 槽最差 protan/deutan 11.3、tritan 12.5，不在帶內；**light 不適用此免除**，見下行。）
實測落在 **6–8 下限帶**的對（dark，`--pairs all`）：**槽 2↔4** `#c7843c↔#c15dde` tritan **6.6**（⚠️ 一用到槽 4 就進帶，前 4／5／6／7 槽的最差 tritan 都是這一對）、**槽 5↔6** `#6f650d↔#934d6b` tritan **7.9**；另 **槽 4↔7** `#c15dde↔#7a3ae2` protan **8.3** 貼著門檻（剛過 8.0 目標，其自身 tritan 為 17.3）。驗證器明文「落在該帶只有配合次要編碼（直接標籤／間隙／紋理）時才合法」。
light **無任何對落在 6–8 帶**（最差 protan/deutan 9.4，槽 2↔6）；但**凍結的槽 1↔3** `#2482eb↔#1b9690` tritan **3.5、低於 6**，自 3 槽起就存在 —— 非新槽引入，且 tritan 不在驗證器五項門檻內（故仍 5/5 PASS）→ **light 畫滿 3 槽即已須次要編碼，⛔ 不得引用上面的 ≤3 槽免除。**
狀態色分離：**新增 4 槽**對狀態色最差 ΔE dark **18.5**、light **16.2**（槽 4/6/7）；⚠️ **light 槽 5 `#5b5e00` 對 `--sig-amber` 僅 7.8** —— light 狀態色已佔滿綠／琥珀／紅／藍／灰五色族，橄欖色無處可退，與既有槽 2 對 `--sig-amber` 9.9 同一性質，靠 A-2「⛔ 一律配圖示＋文字」擋。
⚠️ 狀態色的 dark 值是**線框自己選出來的一套**，非 light 值翻轉；綠/紅在 deuteranopia 下 ΔE 3.9 無法靠色相分辨 —— 這正是「⛔ 一律配圖示＋文字」在本專案是硬規則、不是建議的原因。

## B. 字型 token（⚠️ app 端幾乎空白：無全域 `font-family`、無字級 ramp、`11px` 用了 158 次卻從未具名 → 本節**大部分新訂**）

| 類 | `font-family` | `font-size` | `line-height` | `font-weight` | 對應現況 |
|---|---|---|---|---|---|
| 標題 L1 | `--sans` | `clamp(26px,4.4vw,40px)` | `1.15` | `900` | 新訂（線框 `h1`） |
| 標題 L2 | `--sans` | `clamp(20px,2.6vw,27px)` | `1.3` | `800` | 新訂（線框 `h2`） |
| 標題 L3（卡標） | `--sans` | `17.5px` | `1.5` | `700` | 新訂（線框 `.blk.t1 .bt .t`） |
| 內文 | `--sans` | `14.5px` | `1.72` | `400` | 新訂（線框 `body`） |
| 數值（等寬） | `--mono` ＋ `font-variant-numeric:tabular-nums` ＋ `white-space:nowrap` | `13px` | `1.5` | `500` | `macro_v2_cards.py:.v2-src`；其餘 7 處為裸 `monospace` |
| 說明 | `--mono` | `11.5px` | `1.45` | `400`（色 `--ink-3`） | 新訂（線框 `.bpnote`） |
| 徽章 | `--sans` | `11.5px` | `1.45` | `600` | 新訂（線框 `.bdg`） |

`--sans` = `"Noto Sans TC","PingFang TC","Microsoft JhengHei",system-ui,-apple-system,"Helvetica Neue",sans-serif`
`--mono` = `"IBM Plex Mono",ui-monospace,SFMono-Regular,Menlo,Consolas,monospace`（線框 30 處，**數值一律走這條**）

## C. 間距 token（⚠️ app 端零 token、零 SSOT，僅 8 處 `st.columns(gap=)` → 本節**大部分新訂**）

| token | 值 | 用途 | 對應現況 |
|---|---|---|---|
| `--sp-1` / `--sp-2` / `--sp-3` | `4px` / `6px` / `8px` | 徽章內距、行內元素 gap | 新訂 |
| `--sp-4` / `--sp-5` / `--sp-6` | `10px` / `12px` / `16px` | 元件間距 | 新訂 |
| `--sp-7` / `--sp-8` | `22px` / `26px` | 頁首、主網格欄距 | 新訂 |
| **卡片內距** | `9px 11px`（≤640px：`6px 9px`） | `.blk` 卡片 | `app.py:.strategy-card{padding:10px 14px}` |
| **面板內距** | `12px 14px` | `.pan` 面板 | `app.py:.health-A/B/C{padding:16px}` |
| **卡片間距** | `margin-top:9px`（≤640px：`6px`） | 同層卡片垂直堆疊 | `app.py:.strategy-card{margin:6px 0}` |
| **區塊間距** | `margin-top:12px`；網格 `gap:12px` | 段落／`.two`／`.three` 之間 | `st.columns(gap="medium")` |
| **主網格** | `gap:26px`；`margin-top:22px` | 側欄＋內容 `.shell` | 新訂 |
| **圓角** | `2px`（卡）／`3px`（面板·徽章）／`999px`（膠囊） | ⚠️ 遠比 app 現況銳利 | `app.py` `8px`(75 次)／`.health-*` `12px` |
| **欄寬下限** | `--col-min:308px`（含金額）／`--col-min-text:260px`（兩欄）／`--col-min-text-s:210px`（三欄） | 自適應網格 | 新訂 |

---
⚠️ **本檔的查證分級**：**總管實查**＝`.streamlit/config.toml` 三鍵（`base`/`backgroundColor` 等）、`shared/colors.py` 第 1 行不可編輯、線框稿 token 存在、**A-3 dark 七槽驗證器實跑**。
**WD-1 實跑（2026-09-16）**＝**A-3 light 七槽**（含新挑的槽 4~7）與上列其餘驗證器數字、WCAG 對比值。**WK 實算（2026-09-16，單組未複驗）**＝`--ink-3` 新舊值的六格 WCAG 對比、與 `--ink-2` 的 ΔL\*／ΔE2000 層次判準、A-3 七槽 `validate_palette.js` 複跑（dark／light 均 5/5 PASS、exit 0，序列色一格未動）。**INV-2 單組盤點（未複驗）**＝「對應現況」欄的次數與 SSOT 判讀、「app 無全域 font-family」「間距無 SSOT」三句全稱句。
**原「最站不住腳的一處」（序列色只給 3 槽、`COLORS_7` 有 4 條無色可對）已解**：本輪擴為 7 槽（兩模式），與 `UI_COMPONENTS` §6 逐字對齊，⛔ 不需折成「其他」或分面。

---

## 附錄 A-2.2 — `--sig-neutral` light 兩碼補量 ＋ ≥15 門檻的**度量單位更正**（2026-09-21）

> 📌 **本附錄的內容在邏輯上屬於 §A-2.1，為什麼放在檔尾？**
> 因為在 A-2.1 就地插入會**把本檔 line 74 以後的所有行號往下推**，而
> `src/ui_v2/components.py`、`tests/ui_v2/test_components.py`（各引用 `UI_TOKENS.md:96`）與
> `src/ui_v2/page_today.py`（兩處引用 `:107`）**都不在本輪的檔案邊界內**，改不到。
> 本附錄因此**一行都不插在檔中**，A-2.1／A-2 只做**淨零行數**的就地註記並指回這裡。
> ⚠️ **這不是偏好，是邊界限制下的取捨** —— 根因是「用行號當交叉引用」本身
> （對照 `CLAUDE.md` §8.2.A.0 規則 1：行號是「保證會過期的資訊」）。
> 📌 **另據實記錄**：`docs/v2/spec/UI_PRINCIPLES.md` 的 `UI_TOKENS.md:93`／`:95` 兩處引用
> **在本輪動手前就已經過期**（現 `:93`／`:95` 是 A-3 的 CVD 段，卡片內距／間距實際在 `:120`／`:122`）。
> **本輪未動該檔**（不在邊界內），僅在此記錄事實，⛔ 不是本輪造成的。

### 1. 採用值與**來歷**（2026-09-21）

> 🔴 **決策者＝AI 總管，⛔ 不是客戶。** 客戶做的是**指派**（原話：「【先補兩個洞】…
> 2. `--sig-neutral` 的 light 兩碼」），**⛔ 沒有指定值**。兩碼是總管從**兩組獨立量測**
> （A 組 `#3c5063`／`#d9dce0`、B 組 `#1e3b4f`／`#d7dce1`）之中**選了 B 組**，
> 選 B 組不選 A 組的理由也是**總管的判斷**（取「≥15 分離」而非「字重與同儕一致」，見 §3）。
> ⛔ **不得寫成「客戶核准」「客戶已拍板」** —— 把總管的判斷記成客戶的裁示，
> 會讓下一個人（含未來的 AI）以為這個值已經過客戶拍板、**不敢動也不會回頭問**。
> ⭐ **這是一個可被推翻的選擇**：**客戶可一句話改採 A 組**（見 §3.1）。
> ✅ **對照，⛔ 不要反向改錯**：`--sig-neutral-bg` 從 `#1f2a36` 改為 `#26313d` 那一次
> **確實是客戶裁示採用總管建議值**（決策者＝**客戶**，見 §A-2.1）—— 兩者歸屬不同，⛔ 不得混為一談。

| token | dark（未動） | **light（本次新增，總管拍板）** |
|---|---|---|
| `--sig-neutral` | `#8fa3b8` | **`#1e3b4f`** |
| `--sig-neutral-bg` | `#26313d` | **`#d7dce1`** |

**用途不變**：僅供 `UI_COMPONENTS` §2 狀態徽章 **#9「資料不完整」**（底色＋文字色）。
⛔ 框色不在本對範圍內，#9 框色維持 `--sig-blue`。

### 2. 驗收條件實測（C1~C4）

| 條件 | 對象 | 實測 | 判定 |
|---|---|---|---|
| **C1** 文字對比 | `#1e3b4f` on `#d7dce1` | **8.482 : 1** | **PASS**（AA 小字 4.5，餘裕 **+3.98**） |
| **C2** 底色可辨 | `#d7dce1` vs `--sig-blue-bg` `#e4ecf3` | **ΔE2000 3.669** | **PASS**（2~10 帶） |
| **C2** 底色可辨 | `#d7dce1` vs `--panel` `#fffefc` | **ΔE2000 8.406** | **PASS**（2~10 帶） |
| **C3** 前景三背景定錨 | on `--panel` `#fffefc` | **11.615 : 1** | **PASS** |
| **C3** | on `--paper` `#f6f5f1` | **10.732 : 1** | **PASS** |
| **C3** | on `--panel-2` `#edece7` | **9.897 : 1** | **PASS** |
| **C4** 色族（CIELCh） | fg：light h **255.38** vs dark `#8fa3b8` h **259.96** | **Δh −4.58°** | **PASS** |
| **C4** 色族（CIELCh） | bg：light h **255.97** vs dark `#26313d` h **263.03** | **Δh −7.06°** | **PASS**（見下方更正） |

⚠️ **C4 bg 一格的數字更正（2026-09-21，本輪實算時發現）—— 事實更正，不是漏刪；判定不變。**
總管裁示原文寫 bg「**Δh −3.99°**」。實算：**−3.99 是拿 light 的 bg（h 255.97）去減 dark 的 _fg_（h 259.96）**，
比對對象錯位；**對到 dark 的 bg `#26313d`（h 263.03）時，正確值是 −7.06°**。
✅ **PASS 判定不受影響** —— 判準是「落在本色票自身 dark→light 色相漂移範圍內」，而本色票藍灰族的
|Δh| 實測為：`--sig-grey` 3.09、`--ink-3` 3.78、**`--sig-neutral` 4.58**、`--ink-2` 5.32、
**`--sig-neutral-bg` 7.06**、`--sig-blue` 7.57、`--focus` 8.46、`--sig-blue-bg` 12.93、`--ink` 13.44
⇒ **−7.06 落在中段，比 `--sig-blue` / `--focus` / `--ink` 都小。**
⚠️ **另據實揭露**：`#d7dce1` 的 **C\* 僅 3.13**（低彩度），低彩度色的色相角本來就較不穩定 ——
**C4 這一格的判準敏感度低於 C1~C3**，⛔ 不宜當成強證據使用。
（`--paper`／`--panel`／`--panel-2`／`--sig-grey-bg` 的 |Δh| 看起來高達 158~173，是因為它們近乎中性灰、
色相角數值本身無意義，⛔ 不得拿來當對照組。）

### 3. ⭐ 為什麼採 **B 組**而不是 A 組（本次最重要的判斷）

A 組推薦 `#3c5063`／`#d9dce0`。**兩組的底色幾乎是同一個顏色**（`#d9dce0` vs `#d7dce1` ＝ **ΔE2000 0.92**）
—— **真正的分歧在前景**：

| 前景候選 | vs light `--sig-grey` `#586470` | 判定 |
|---|---|---|
| A 組 `#3c5063` | **OKLab×100 7.66**（CIEDE2000 8.14） | ❌ **過不了 A-2 的 ≥15** |
| **B 組 `#1e3b4f`** | **OKLab×100 16.02**（CIEDE2000 15.42） | ✅ **PASS** |

**B 組對全部五個 light 狀態色前景都過**（度量 OKLab×100，門檻 15）：
green `#1c6f43` **18.14**／amber `#87600a` **23.34**／red `#ae2f2b` **26.36**／blue `#044cb6` **17.32**／
grey `#586470` **16.02**（最窄的一格）。

⚠️ **A 組曾宣稱「15 在 light 模式、在冷藍灰族約束內是結構性無解」** ——
**該全稱句已被 B 組用一個構造解否證**（`#1e3b4f` 就是那個解），總管複算確認、本組再算一次確認。
⛔ **該宣稱不得被引用。**

#### 3.1 ⚠️ 代價（誠實揭露，⛔ 不得藏）

`#1e3b4f` 的 **L\* ＝ 23.57**，其餘五個 light 狀態色前景為
blue **35.14**／red **39.93**／green **41.25**／grey **41.78**／amber **43.57**
⇒ **比最接近的同儕還深 11.57 個 L\* 單位（約 12）** ⇒ **這顆徽章的字會比同儕明顯重。**
**這是本推薦唯一的實質取捨。**

**為什麼仍選「分得開」而不是「字重一致」**（總管判斷）：A-2 的 ≥15 門檻存在的目的，
是讓「**缺漏**」與「**判過了但中性**」**用眼睛分得開**；丟掉它是**正確性**問題
（使用者分不出兩個不同狀態），字重只是**觀感**。且 dark 那一對本來就 FAIL（3.70），
light 既然清得過就該清。

⚠️ **客戶可一句話改採 A 組的 `#3c5063`／`#d9dce0`** —— 換得字重與同儕一致，
代價是放棄 ≥15 分離。**顏色是客戶決定的，總管只負責量測與揭露。**
（A 組值的對照量測：C1 ＝ 6.058:1 PASS；bg vs `--sig-blue-bg` ΔE2000 4.02、vs `--panel` 8.00，
C2 同樣 PASS —— **A 組唯一過不了的就是前景的 ≥15 分離。**）

### 4. ⭐ 度量單位更正：A-2 的「≥15 可辨門檻」原生度量是 **OKLab 歐氏距離 ×100**，⛔ 不是 CIEDE2000

**證據（三項，本輪逐項實查）**：
1. `validate_palette.js`（在 `dataviz` skill，**repo 內沒有**）檔內逐字
   `Delta E is Euclidean distance in OKLab ×100` ＋ `const NORMAL_FLOOR = 15.0;`；
2. 同檔 FAIL 訊息逐字 `below 15, hard to tell apart even with full color vision`
   ＝ 本檔 `:88` 引用的「低於 15，連全色覺的人都分不出來」—— **同一個門檻**；
3. **用 OKLab×100 重算本檔 `:46-47` 逐字寫的兩個數字**：
   dark `--sig-blue`↔`--sig-grey` ＝ **15.83**（文件寫 15.8 ✅）、light ＝ **16.34**（文件寫 16.3 ✅）；
   **同一對用 CIEDE2000 ＝ 15.57／15.11，對不上文件。**
   ⇒ **文件裡 15.8／16.3 這組數字本來就是 OKLab×100 算的，門檻 15 也是這把尺。**

**缺陷本體**：本檔 `:69` 拿 **CIEDE2000 的 4.85** 去比 **OKLab×100 的 15 門檻** —— **兩把不同的尺互比**。
**用門檻自己的度量，該對是 3.70。⚠️ 結論（FAIL）不變，只是引用的數字單位錯了。**
（處置已就地落在 `:69`：原句保留不刪 ＋ 加刪除線 ＋ 註明理由 ＋ 標日期與決策者 ＋ 兩邊理由並陳。）

⚠️ **日後在 A-2 引用 ≥15 門檻時，一律標明度量是 OKLab×100，⛔ 不得再混用。**
✅ **本規則已由測試強制**：`tests/ui_v2/test_tokens.py::test_the_15_floor_metric_is_oklab_x100_not_ciede2000`
同時斷言「OKLab×100 **重現**得了 15.8／16.3」與「CIEDE2000 **重現不了**」——
**任何人日後改用錯尺都會當場紅燈。**

#### 4.1 ⚠️ **防呆：用錯尺會憑空生出一個「幽靈缺陷」**（真實例子，⛔ 不是新缺陷）

dark `--sig-neutral` `#8fa3b8` vs `--sig-blue` `#4b8efe`：

| 度量 | 值 | 對 15 門檻 |
|---|---|---|
| CIEDE2000 | **13.19** | 看起來像「第二個未登記的違規」 |
| **OKLab×100（門檻自己的尺）** | **15.06** | ✅ **PASS** |

⛔ **不得把 13.19 寫成新缺陷。**
⚠️ **但餘裕只有 0.06，極薄，值得記一筆** —— 動到 `--sig-neutral` 或 `--sig-blue` 任一碼都可能掉到門檻下。
（已由 `test_dark_sig_neutral_vs_sig_blue_passes_the_floor_but_only_just` 守住。）

#### 4.2 dark 前景那一對**仍未解決**（本輪未動）

`--sig-neutral` `#8fa3b8` vs `--sig-grey` `#8b98a4` ＝ **OKLab×100 3.70**（原記 CIEDE2000 4.85，單位錯）
⇒ **仍遠低於 15**。靠 A-2「⛔ 一律配圖示＋文字」擋，⛔ 不得讓兩者同框而只靠顏色分辨。
✅ **對照：light 那一對本輪已清過門檻（16.02）** —— ⛔ 不得讀成「dark 也一起修好了」。

### 5. 補掉 `:73` 自陳「本輪未重算」的缺口

| 對 | 新值 `#26313d` | 舊值 `#1f2a36` |
|---|---|---|
| vs `--sig-grey-bg` `#1b232b` | **ΔE2000 4.77** ✅ 帶內 | 2.98（勉強在帶內，貼著 2.0 底線） |
| vs `--panel-2` `#1b242f` | **ΔE2000 4.10** ✅ 帶內 | 1.98（**在帶外**） |

⇒ **那次換色連帶把這個缺口也修好了，只是當時沒人算。**
⚠️ **據實修正一處措辭**：舊值**不是「兩格都在帶外」** —— `--sig-grey-bg` 那格（2.98）其實勉強在帶內，
只有 `--panel-2` 那格（1.98）真的出界。兩格的共同點是「**貼著底線**」，⛔ 不是「都出界」。

⭐ **查證分級（這一組與本檔其餘數字不同）**：**4.77／4.10 是總管＋A 組＋B 組三方各自獨立算出、彼此相符**
—— **本檔少數真的有三方複驗的數字**，可信度**高於**其餘單組數字。⚠️ 引用時請注意這個差別。

### 6. ⛔ 未驗事項（依 `CLAUDE.md` §-2 規則 6 據實標註，⛔ 不得當既定前提）

1. 🔴 **完全沒做色盲（CVD）檢查。** 兩組量測組都明確回報：**沒做 protan／deutan／tritan 模擬、
   沒跑 `validate_palette.js` 的五項門檻**。⛔ **不得宣稱新值通過 CVD 檢查。**
2. 🔴 **沒有對 A-3 七槽序列色複跑 `--pairs all`。** 本次新增的是**狀態色**，
   但 A-3 §:90 記載過「`--sig-blue` vs 序列槽 1」這類跨組檢查 ——
   **`--sig-neutral` light 兩碼 vs A-3 light 七槽，本輪沒有量過。**
3. ⚠️ **本附錄第 2~5 節的數字為 WS2 規格組實算**，實作經
   **Sharma / Wu / Dalal (2005) 34 組 CIEDE2000 標準向量逐項驗過（34/34 相符，容差 1e-4）**，
   且已全部寫成 `tests/ui_v2/test_tokens.py` 的斷言（改任一碼即紅燈）。
   **與總管數字逐項相符者：C1／C2／C3／§3 全部／§4 全部／§5 全部。**
   **唯一不符者為 C4 的 bg Δh**（總管 −3.99 vs 實算 −7.06，見 §2 更正）。
4. ⛔ **本附錄⛔ 不宣稱「本檔已無 FAIL 殘留」。** 本檔 `:91` 已記載過一次這種不標範圍的全稱句
   被否證的前例。**本附錄的量測範圍僅為**：`--sig-neutral` / `--sig-neutral-bg` 兩個 token
   在 C1~C4、≥15 門檻（對五個 light 狀態色前景）、以及 §5 的兩格 —— **以外一概未查。**
