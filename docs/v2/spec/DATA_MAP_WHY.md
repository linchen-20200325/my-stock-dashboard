# 憑什麼頁資料對照表（IA v2 戰情室 · block 級）

> **這是什麼**：📖「憑什麼」頁每一個 block「畫面上那個東西從哪來、怎麼算、沒資料時怎麼辦」的對照表。
> **射程**：本頁 **22** 個 block（順序照 `docs/v2/prototype/gen_today_v2.py::WHY_LAYOUT`）。量測日 **2026-09-22**・branch `ui-v2`。**索引與共通讀表前提**＝`docs/v2/spec/DATA_MAP_ALL_PAGES.md`。
> 📌 **規格洞與 ⚠️ 待確認一律登記在 `docs/v2/spec/DATA_MAP_GAPS.md` 的 `## 📖 憑什麼頁`，⛔ 本檔不留第二份。**
> ⚠️ **本檔是現況記錄，⛔ 不構成主動動工的授權**（`CLAUDE.md §-1`）。⛔ 全檔不寫行號（`§8.2.A.0` 規則 1）；程式碼引用一律**全路徑** `檔名::符號`；TTL 一律寫 `shared/ttls.py` 常數名；查不到一律「⚠️ 待確認」，⛔ 不猜、⛔ 不發明資料源／算式／中文標籤。

---

### 0. 讀表前提（**前三條為總管親自複驗**；⛔ 少了這段，下表每一列都會被讀反）

- 🔴 **【總管複驗】`[A:無此頁]`，⛔ 不是 `[A:未接線]`（兩者意思完全不同）**：`ls src/ui_v2/*.py` 只有 `__init__`／`app_today`／`components`／`markup`／`page_today`／`render`／`tokens`，**無 `page_why`** ⇒ **與找標的頁同型**。⇒ 下表「資料來源」一律填 **B＝現行** `src/ui/views/page_why.py`（掛載 `app.py::_ia_view_why` → `src/ui/views/page_why.py::render_page_why`）。
- 🔴 **【總管複驗】TTL 欄一律「無快取」、fallback 欄一律「不適用」，⛔ 不是「待確認」**：以 **AST** 掃描（含函式體內 late import）`src/ui/views/page_why.py` 的 **L1 import ＝ 零** —— 只有 `shared.*`／`src.services.{ai_qa_service,app_ia_service}`／`src.ui.{tabs.tab_today,views._ui_kit}`／`streamlit`／stdlib，**`src.data.*` 0 命中**。⚠️ **唯一例外是 `why.qa`**（葉3 送出問題後走 L3 打外部 API）⇒ 那一列 TTL 據實寫「⚠️ 待確認」。⛔ **不得寫成「全頁零取數」**（`UI_PAGE_WHY ③ 規則一 (c)` 逐字：「葉1／葉2 確實零取數；**葉3 送出問題後會走 L3 打外部 API**」）。
- 🔴 **【總管複驗】`grep=0` ⛔ 不等於未實作 ——（這是對前一輪方法的更正）**：實作端用**動態 card key**（實測 `key=f"why.source.{probe.name}"`／`key=f"why.spec.{row.key}"`）⇒ `why.source.wall`／`why.spec.flags`／`why.engineer.panels` 這類**容器** block 的字面 grep **必然是 0**。⛔ 不得照數字誤判成未實作。
- ⭐ **本頁是「揭露頁」，不是取數頁**：唯一的「資料源」是 repo 內的 **L0 常數表與 in-process 登錄表** —— `shared/macro_buckets.py::BUCKET_DANGER_SPECS`(16)／`shared/macro_buckets.py::REFERENCE_TREND_SPECS`(2)／`shared/station_specs.py::STATION_SPECS`(12)／`shared/fetch_monitor.py::get_monitor_registry`（in-process dict，**零 I/O 零網路**）。
- **三種狀態⛔ 不可混為一談**〔FA 分類〕：**真正未接線（畫面完全不存在）＝ `why.statusbar`／`why.asof` 2 塊**；**已實作、但內容是 `wired=False` 灰卡 ＝ 8 塊**（`why.edu.health6`／`why.edu.legacy`／`why.source.unmeasured.finmind_quota` ＋ `why.engineer.panels` 的**五張子卡**）；**其餘 12 塊有實作**。⚠️ 三個數的**計數單位不同**（中段含子卡），⛔ 不可直接相加去驗 22。
- **徽章一律用 `UI_COMPONENTS §2` 的 `#n` 系統**；出處寫 `①~⑩`（`S2-UI_SPEC`）或「呈現層 1~8」（`S1-4 §1.2`）者**逐字照抄並標明是哪一套，⛔ 不自行換算**。**兩套 `D-*` 同名不同義**：`S2-UI_SPEC §9.3` 的 **`D-1`**／**`D-6`** ⛔ **不是** `INDICATOR_SPEC §5` 的 `D-01`／`D-06` ⇒ **逐字照抄出處寫法，⛔ 不得補零統一**。

### 1. 二十二個 block（順序照 `WHY_LAYOUT`）

| block key | 畫面顯示什麼 | 資料來源 | 計算 | 缺值處理 |
|---|---|---|---|---|
| `why.statusbar` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**（B 全檔 0 個 statusbar）。規格要的是三格：交易日／總經／Sheet 綁定 | 🔴 **畫面上沒有這一條**〔FA，AST 實測；僅 `src/ui/views/page_today.py::build_status_bar_cards` 有〕。**規格端指定**〔FB〕：`S2-UI_SPEC §7.1` 逐字「來源＝L3 canonical 契約 `src/services/macro_state_locker.py::get_macro_state`，⛔ 不依賴任何 render 副作用」＋「回傳 dict **只有 9 個 key**、**沒有把 `timestamp` 帶出來**…**寧可寫「時間不明」，也不編一個時間**」。`app.py` 明文：全域置底常駐條**整條不出現**（它讀 `warroom_summary`，新頁不寫入 ⇒ 必然說假話）。**TTL 無快取／fallback 不適用** | 無計算（三格直讀 L3 契約欄位） | 畫面上不存在 ⇒ **本頁無態可判**。規格端十態〔FB〕：`idle`→**#3**（可重試，出口在 🚦 今天的主 CTA）／`degraded`→**#4**／`unwired`→**#5**（不可重試）／`error`→**#6**；`empty`／`missing`／`na`／`partial`＝`null`（⛔ 不得畫成空卡）；**逐格獨立判態**。⚠️ 線框 `live` 的 `09/13 週五` 是**規格示意值、⛔ 非事實**。**編號 ⚠️ 待確認**（兩檔 0 命中「狀態列」）。洞 `WHY-G-10`／待確認 `ⓐ` |
| `why.asof` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**（B 0 個 `asof`／`as_of`）。規格要的是頁標題下方第一行的資料時點列 | 🔴 **畫面上沒有這一條**〔FA，`src/ui/views/page_why.py` 與 `shared/ia_nav.py` 皆 0 命中〕。⚠️ 各盞來源燈**自帶**時間（`last_called_at`），**但無全頁 as-of**。**規格端**〔FB〕：`S2-UI_SPEC §7.1` 指定來源＝`shared/staleness.py::latest_date` 逐源彙總；`S1-4 §4.2` 逐字「**『歸屬日』是資料自己的日期，不是你抓到它的日期**…**不要**用 `fetched_at`」。🔴 **本頁語意分岔（線框 evidence 逐字）**：「本頁葉1 **沒有數字**，葉2 牆上那個時間講的是**最後一次真實外抓** —— **那是呼叫紀錄，不是資料歸屬日**」⇒ 要補這條**必須先解決語意分岔**，否則補上去就是一個看起來精確、實際說錯話的數字。**TTL 無快取／fallback 不適用** | 規格端 `live` ＝「最舊 `<日>` ～ 最新 `<日>`」區間（`S1-4 §4.3` 末列逐字；**❌ 禁止只顯示最新的那一個**） | 規格端：`na`→**#8 `N/A`**（**⛔ 不帶 `⚠︎`**，不適用 ≠ 壞掉，不可重試）／`partial`→**#9 `◧`**（分子分母拿不到 → **一律降 #7，⛔ 不得退回 #1**）／`unwired`→**#5**／`error`→**#6**（可重試）／`idle`→**#3**。⭐ **鐵律**（`S1-4 §4.3`＋`§4.2` 規則 3）：**⛔ 不得猜、不得填今天、不得留白**；⛔ **禁用「今天／剛剛／最新」相對詞**；`S2-UI_SPEC §5.4 ★-05`：只寫歸屬日＋性質，⛔ 不做「新不新鮮」判斷。⚠️ 符號未同步（線框仍用 `▨`）＝`WHY-G-08`。**編號 ⚠️ 待確認**。洞 `WHY-G-10`／待確認 `ⓑ` |
| `why.footer` `[A:無此頁]` | 置底免責一行（靜態字串）：`⚠️ 台股AI戰情室 v3.0 · 僅供學術研究，非投資建議，盈虧自負`（置中小字） | `app.py::_render_footer`（**L6 靜態字面值，非 B**）〔FA〕。**文字 SSOT 在 `app.py`，新舊頁共用同一份**（複製第二份＝兩邊會漂移）〔FB，`S2-UI_SPEC §7.3`〕。**TTL／fallback 不適用**（寫死常數字串，⛔ 不是資料） | 無計算 | **沒有缺值態** —— 恆顯示（`st.stop()` 前先呼叫）〔FA〕；法遵文字**不分狀態，⛔ 不得因為載入失敗就不印**〔FB〕。**無徽章**。🔴 **硬禁令**（`S1-3B §2.10.3 ⚑`＋`S2-UI_SPEC §7.3 ⚑` 逐字）：新五頁**⛔ 不得複製** `app.py` 那條**全域置底常駐條**（內容是一句直接指定持股比例的話，與客戶拍板第 3 條「全站不產生直接買賣建議」正面牴觸）。**編號＝`S2-UI_SPEC §9.3` 的 `D-1`**（⚠️ **⛔ 不是** `INDICATOR_SPEC §5` 的 `D-01`）。洞 `WHY-G-01`（`§7.3` 是**三條帶**，本頁只落地免責那一帶） |
| `why.edu.lights` | 卡「① 紅綠燈怎麼判 · 各門檻的出處」：`16 盞總經燈 ＋ 12 盞持股燈` ＋ 5 條 facts ＋〔展開佐證 ▸〕 | `src/ui/views/page_why.py::build_lights_card` ← `src/ui/views/page_why.py::load_specs` ← **L0** `shared/macro_buckets.py::BUCKET_DANGER_SPECS`(16) ＋ `shared/station_specs.py::STATION_SPECS`(12)；`shared/macro_buckets.py::REFERENCE_TREND_SPECS`(2) **另存、不入分母**。頻率＝**每次 rerun**（`S1-2 §2.4` 逐字）。**TTL 無快取／fallback 不適用**（`S1-2 §2.4` 逐字「**無多源；值取不到即 gray**」） | L5 計數：`n_macro` ＝ `rows` 中 `family=總經` 的筆數、`n_hold` ＝ `family=持股` 的筆數，**當場算、⛔ 不寫死**。⛔ 無 L2/L3。判燈式 `classify_danger`〔FB，`S1-2 §2.4`〕：`high_bad` `v≥red→紅`／`v≥yellow→黃`／else 綠；`low_bad` 反向；`band` 雙邊 | `load_specs` 例外→**#6 🔴**；`has_rows=False`→**#7 `⚠︎ —`**（`MISS_NO_INPUT`）；⛔ **無重試出口**（`NO_EXIT_MARKER`）〔FA〕。🔴 **兩份規格對同一態給相反指引**〔FB〕：`INDICATOR_SPEC §2 R-3` 對 `(None, MISS_NO_INPUT)` 寫「**上游這輪失敗，可以重跑一次**」，線框逐字寫「**這一態沒有你可以做的事**…請回報給維護者」⇒ `WHY-G-02`（根因：本頁的「缺漏」來自 **L0 常數表**、不是上游取數）。**編號**：`DECISION_RULES R1-1`（進不進分母）＋`R1-2`（16 盞**不加權** worst-of） |
| `why.edu.health6` | ⛔ **未接線灰卡**（**已實作**、內容是 `wired=False`）：「**六個因子各佔幾分，本頁還說不出來**」＋ 卡住原因 ＋「⚠️ 在補好之前，這一格空著是誠實的」 | ⚠️ **無資料來源** —— `src/ui/views/page_why.py::build_health6_card` 寫死 `wired=False`〔FA〕。⭐ **【總管複驗】這個洞是 SSOT 沒建、⛔ 不是規格沒寫**：`DECISION_RULES §2 R2-1` **已逐因子列出權重**（趨勢 30／RSI 20／量比 15／KD 15／IBS 10／布林 10），但實作端是 L2 `src/compute/scoring/scoring_helpers.py::calc_health_score` 的 **inline literal**，L0 `shared/position_throttle.py` 註解另抄一份文字版 —— **兩處皆非可讀結構** ⇒ 登記 `D-28`。**TTL 無快取／fallback 不適用** | ⭐ **規格有算式、但本頁刻意不顯示**（⛔ 不是「無計算」，也⛔ 不是「查不到」）：總分 `round(Σ得分ᵢ ÷ Σ滿分ᵢ × 100, 1)`、涵蓋門檻 `有值因子數 > 6 × SCREENER_MIN_FACTOR_COVERAGE_RATIO`（`DECISION_RULES R2-2`）。線框逐字「本頁**不把那組數字再抄第三份** —— 抄第三份就是再製造一次前後不一致，**而那正是這一頁存在的理由**」 | `wired=False`→**#5 ⛔ 未接線**（dashed），**⛔ 不可重試**。⭐ **三灰分辨（線框逐字）**：⛔ **不是** ⬜ 未載入（沒有「去按一下就會有」的出口）、⛔ **不是** ▨ 不適用（六因子對本頁**完全適用**，只是接不上）—— 說成前者會讓使用者一直找那顆不存在的鈕，說成後者會讓人以為這件事不該存在。**編號**：`DECISION_RULES §8 D-28`；相鄰 `INDICATOR_SPEC §5 D-01`（應回 `(None, MISS_NO_INPUT)` 而非 0 分）＋`DECISION_RULES §8 D-29`（RSI<30→14、量比>3.0→12 **非單調是刻意的**，⛔ 不得當 bug 修） |
| `why.edu.scales` | 卡「③ 同一個名詞，兩套刻度」：持股＝序數燈號 ⚪🟢🟡🔴（**沒有分數**）vs 查一檔＝連續 0–100（**沒有燈號**）＋ 不可互比理由 | `src/ui/views/page_why.py::build_scale_card` ← `src/ui/views/page_why.py::build_scale_disclosure` ← **L0** `shared/station_specs.py::SPECS_BY_KEY`（key：`health_a~d` ＋ `stock_health`／`stock_trend`／`stock_kd`／`stock_swap`）。零 I/O。⭐ **本塊是頁4（💼 我的持股）② 卡 degraded 時指過來的終點** ⇒ **本頁這一段必須比頁4 完整**〔FB〕。**TTL 無快取／fallback 不適用** | `degraded ＝ ∃r: r.discriminative=False`（**算出來、⛔ 非寫死**；旗標翻回 `True` 這張卡會自己回 live）。查無 key **直接跳過該列，⛔ 不補假列** | 任一側 `discriminative=False`→**#4 🟠 門檻已失準**（實測命中「財報趨勢」），**⛔ 不可重試**；讀取例外→**#6**；`has_rows=False`→**#7**。`degraded_reason` **L0 原文透傳**。⭐ `DECISION_RULES R1-1` 結構性旗標③ 逐字反例：`discriminative=False` 仍**照常判燈、照常進分母**，另掛 `degraded_reason`（**⛔ 不得因不具鑑別度就移出分母**）。**編號**：`DECISION_RULES R1-1` 旗標③ ＋ **`S2-UI_SPEC §9.3` 的 `D-6`**（⚠️ 揭露落點那一套，**⛔ 不是 `D-06`**） |
| `why.edu.legacy` | ⛔ **未接線灰卡**（**已實作**）：「既有的完整教學內容還沒有搬到這一頁」＋ 指路既有 📚 教學分頁（**未下架**） | ⚠️ **無資料來源** —— `src/ui/views/page_why.py::build_legacy_edu_card` 寫死 `wired=False`〔FA〕。內容來源＝既有 📚 教學分頁（**1,582 行**既有靜態內容；核准線框 `DECISIONS #9` 裁決「留，原樣搬」），⚠️ **規格端未給該分頁的 `檔名::符號`**（FB 查過 `docs/v2/spec/` 全檔）⇒ **⚠️ 待確認** `ⓓ`／洞 `WHY-G-11`。**TTL 無快取／fallback 不適用** | 無計算（靜態教學內容） | `wired=False`→**#5 ⛔**。⭐ **本頁唯一一個「未接線但**有**出口」的格**（線框逐字）：其餘 ⛔ 卡一律走 `NO_EXIT_PREFIX` 明說沒有出口；本格**給得出**替代出口（舊 📚 教學分頁還活著）⇒ 文案**刻意與 #5 通則不同，⛔ 不得統一**。⚠️ **「不可重跑」與「有替代出口」是兩件事，⛔ 不可混寫**。⛔ 不先貼摘要：「摘要會變成第二份教學，兩份遲早不一致」。理由：跨檔搬遷屬 `CLAUDE.md §8.4 step 4` **範圍問題** ⇒ ★待拍板。**編號 ⚠️ 待確認** |
| `why.edu.table.macro` | 總經燈 `st.dataframe` **七欄**：這一盞／分組／方向／門檻／值從哪來／在防什麼／已知限制（實測 **16 列**）；caption 分組 🌳 長期(3)·📈 中期(6)·⚡ 短線急殺(3)·🧩 籌碼(3)·📰 新聞(1) | `src/ui/views/page_why.py::_render_edu_leaf` → `src/ui/views/page_why.py::_spec_table_rows` ← `src/ui/views/page_why.py::_macro_rows` ← **L0** `shared/macro_buckets.py::BUCKET_DANGER_SPECS`；分組名 ← `shared/macro_buckets.py::BUCKET_META`。「值從哪來」欄 ← 各筆 `DangerSpec.source`（`"SSOT:<位置>"` 或 `"DESIGN"`）〔FB，`S1-2 §2.4`〕。**TTL 無快取／fallback 不適用**（`S1-2 §2.4` 逐字「無多源；值取不到即 gray」） | 無數值計算。欄位轉換：`SpecRow.group_text`／`direction_text`／`threshold_display`／`flag_text`。caption 的「N 盞」＝`len(_rows)` **當場算，⛔ 不寫死** | 旗標燈**門檻降級但⛔ 不隱藏**：`threshold_display` 加刪除線(U+0336)＋「現在不要拿它當操作依據」（`degraded`→**#4**，實況＝融資餘額；⚠️ 藏掉的後果是你去別的地方找一份**更舊**的數字）；未接線→「已知限制」欄印「尚未生效」（**#5**，實況＝外資現貨淨買賣）。`empty`／`missing`／`error`→**整段不畫**（⛔ 不是畫空表）。`value is None` 或 `float()` 拋錯→`gray` ⬜，檔內明文「**不偽綠**」，且 **gray ⛔ 不參與 worst 計算**。表內⛔ 無徽章（徽章在 `why.spec.flags`）。🔴 **合規（`UI_PAGE_WHY ⑤` 逐字）**：「門檻」欄是**判燈邊界，⛔ 不得標成進出場點**；**⛔ 全頁禁止買賣建議**。**編號**：`DECISION_RULES R1-1` 全式 ＋ `INDICATOR_SPEC §3` 五桶 16 燈列（**應然 vs 實然相同，本站最佳樣板**） |
| `why.edu.table.reference` | 參考走勢 `st.dataframe` **四欄**：這一條／單位／這條線在說什麼／值從哪來（實測 **2 列**：💱 新台幣匯率、📈 加權指數） | `src/ui/views/page_why.py::_reference_table_rows` ← `src/ui/views/page_why.py::_reference_rows` ← **L0** `shared/macro_buckets.py::REFERENCE_TREND_SPECS`；同檔哨兵值 `REFERENCE_BUCKET = "reference"` **刻意落在 `BUCKET_ORDER` 之外**（誤餵進桶彙總時會落在「沒有這個桶」，⛔ 不會悄悄混進某一桶改掉那桶的 worst-of）。**TTL 無快取／fallback 不適用** | **無計算** —— 這幾條**本來就沒有門檻**、不判燈、不進 16 盞分母、不進五桶彙總（客戶 2026-08-27 裁示）。⚠️ 匯率的 `32.00 / 33.00` 是**參考線**，逐字「僅供對照，**不判燈**」⇒ ⛔ 不得寫進「門檻」欄 | ⭐ **刻意無「門檻」與「已知限制」兩欄（非省略）**：印「—」會讓人以為有門檻、印「正常」會被讀成燈是好的 ⇒ `na`→**#8 `N/A`**（**⛔ 不帶 `⚠︎`**），**不可重試**（結構性）。`empty`／`missing`／`error`→整段不畫。⭐ **舊 bug 判定逐字**（`S1-3B §3.5.1 ⚑`）：「**不判燈的東西，標『正常』或『已失準』都是對使用者說錯話**」⇒ 修法是「**整張表換一組欄位**」，⛔ 不是把那兩欄填好。**編號**：`INDICATOR_SPEC §1`「為什麼『不適用』與『缺漏』必須分開」表 ＋ `DECISION_RULES R1-1` 分母排除 |
| `why.edu.table.hold` | 持股燈 `st.dataframe`，**欄位同 ⑤-a**（實測 **12 列**）；分組：四燈體檢(4)·3-3-3 篩選(3)·個股體質(4)·進場時機(1) | `src/ui/views/page_why.py::_spec_table_rows` ← `src/ui/views/page_why.py::_station_rows` ← **L0** `shared/station_specs.py::STATION_SPECS`；分組名 ← `shared/station_specs.py::STATION_GROUP_TITLES`。⚠️ 與 ⑤-a **共用同一支欄位定義與同一個 render 迴圈**。**TTL 無快取／fallback 不適用** | 同 ⑤-a 的 `classify_danger`（依 `station_specs` 各自門檻）；**12 ＝ `len(STATION_SPECS)` 當場算** | 同 ⑤-a；`_rows` 為空則整段 `continue`（⛔ 不畫空表）。`degraded`→**#4**（實況＝財報趨勢）；`unwired`→**#5**，**本頁實況 0 盞**。⚠️ 12 盞裡 1 盞（`stock_kd`）標 `emits_level=False`，**由 ⑤-e 單獨承接，⛔ 不塞進「已知限制」欄硬給一個態**。🟡 **合規登記（線框逐字，⛔ 兩組皆未動）**：分組名「**進場時機**」是**動作語當分組名**，而 `S2-UI_SPEC §9.1` 禁用語表**未涵蓋分組名**；日後改名**⛔ 不能只改線框**（只改線框會製造第二真相源）⇒ `WHY-G-13` 未決。**編號**：`DECISION_RULES R1-1` 結構性旗標②（`emits_level`，2026-08-26 user 裁示）＋`INDICATOR_SPEC §1 維度 B` |
| `why.edu.table.caveat` | 表格**下方**逐列 caption：`· **<燈名>**（尚未生效｜已失準）：<L0 原因原文>`（實測 **3 列**：外資現貨淨買賣／融資餘額／財報趨勢）；抬頭「上表中被降級的門檻 —— 為什麼現在不能用」 | `src/ui/views/page_why.py::threshold_caveat_lines` ← **L0** `unwired_reason`／`degraded_reason`（**必填欄，有 L0 測試守衛**）。⚠️ **刻意不塞進表格儲存格**：`degraded_reason` 是**多段文字**（L0 自帶換行與粗體），塞進 `st.dataframe` 會被壓成一長條 ⇒「**把一段寫給使用者看的話變成噪音**」。**TTL 無快取／fallback 不適用** | 列數＝`len(SpecScan.unwired) + len(SpecScan.degraded)`，**隨 L0 旗標增減、⛔ 不寫死**。純過濾 `r.threshold_flag != ""`，無數值計算 | 原因**原文透傳，⛔ 不節錄、不改寫**。無旗標時**整段不出現**（⛔ 不是畫成 #7）—— ⚠️ 這是**有意義的空**：沒有任何一盞被降級＝好消息。已降級的列本身→**#4**／**#5**，**皆⛔ 不可重試**。🔴 **合規逐字**：⛔ **不得為了顯得誠實而把全部門檻都標成失效**（「那是另一種說謊」）；沒有被標記的燈**原樣照出門檻，一個字都不加**。⚠️ `S1-4 §1.5` 規則 2：SSOT 標了 `wired=False` 卻沒填 `unwired_reason` 時**⛔ 不編字**，改印「這是程式要修的訊號」。**編號**：`DECISION_RULES R1-1` 結構性旗標①③ |
| `why.edu.table.nolevel` | caption 區「依規格就不出等級的燈」＋逐列 `· **<燈名>**：<no_level_reason>`（實測 **1 列**：KD 指標） | `src/ui/views/page_why.py::_render_edu_leaf` → `SpecScan.no_level` ← **L0** `shared/station_specs.py` 的 `emits_level`／`no_level_reason` 兩欄（FB 實跑：`STATION_SPECS` **僅 `stock_kd` 一筆**、`BUCKET_DANGER_SPECS` **0 筆**）。**TTL 無快取／fallback 不適用** | 無計算。⚠️ KD 算式（`INDICATOR_SPEC §3`）**本頁不顯示、只解釋「為什麼沒有等級」** | ⭐ **刻意不給任何狀態燈**：七態無一態在講「有值、照印、但依規格從來不出等級」，硬塞 `unwired`（假的，它接得好好的）／`degraded`（假的，它從來沒有門檻）／`empty`（假的，K、D 都抓得到）**三句都不是實情**。🔴 **正確徽章＝`UI_COMPONENTS §2` 的 `#10 ◆`「只描述，不判等級」**；⛔ **不得併進 #5／#4／#7／#8 任何一種**。🔴 **兩處未落地，兩邊並陳**：〔FA 程式碼端〕B **尚未徽章化**（`WHY_BADGES_ON_PAGE` 不含 10）；〔FB 規格端〕線框把它放在十格契約的 **`na` 格（＝#8）**，與 `INDICATOR_SPEC §1 維度 B` 逐字「⛔ **不得**把 `emits_level=False`…併進 `MISS_NOT_APPLICABLE`」**直接相反**（**【總管複驗】兩邊原文已逐字驗，牴觸成立**）⇒ `WHY-G-06`；線框自標 ★待拍板，**⛔ 本檔只登記、不裁決**。**不可重試**（結構性）。**編號**：`DECISION_RULES R1-1` 結構性旗標② ＋`INDICATOR_SPEC §1 維度 B`；KD 本身＝`INDICATOR_SPEC §5 D-09` |
| `why.source.wall` | 3 欄燈號卡牆，**每格一支 fetcher**：🟢＋最後一次真實抓取時間／⬜ 未檢查／🔴＋錯誤原文（**格數動態**＝本 session 已載入的 `@monitored` 支數） | `src/ui/views/page_why.py::_render_user_health_wall` → `src/ui/views/page_why.py::build_source_card`（**動態 card key** `why.source.<fetcher>`）← `src/ui/views/page_why.py::load_sources` ← **L0 `shared/fetch_monitor.py::get_monitor_registry`**（**in-process dict，零 I/O 零網路**）。欄位：`last_status`／`last_called_at`／`last_rows`／`last_ms`／`last_error`／`category`／`frequency`。🔴 **時間語意**（`S1-4 §4.4(c)`／`S2-UI_SPEC §5.3` 逐字同列）：`last_called_at` ＝ **呼叫時間，不是歸屬日** ⇒ 顯示須寫「`MM-DD HH:MM` **取得**」，⚠️ **必須寫「取得」兩字**。**TTL 無快取／fallback 不適用** | 無計算。`requested←probe.called`（`last_status != '未執行'`）、`has_value←probe.ok`（`== 'ok'`）—— **兩個不同欄位**，⛔ **不從資料反推**（`S2-ENG_SPEC §2.5`＋`S1-4 §5 R-6`）。`partial` 的「已檢查 N / M 源」：**M ＝ ⚠️ 待確認（規格端無口徑）**，見 §2 | `未執行`→**#3 ⬜**（**可重試、但出口不在本頁** —— 到會用到它的那一頁按更新；⚠️ **吃快取不算**）；`ok`→**#1 🟢**；`failed`→**#6 🔴**＋L0 錯誤原文（`MISS_FETCH_FAILED`，**可重試**）；`empty`／`missing`→**#7**，逐字「名單讀得到、裡面一支都沒有…**這是正常的，不是壞掉**」（⛔ 不得顯示一片空白）；`partial`→**#9 `◧`**（分子分母拿不到 → **一律降 #7，⛔ 不得退回 #1**）；`degraded`→**#4**（牆上**目前無實例**，走 `why.spec.flags`）。🔴 **合規逐字**：認不得的狀態→**#6 🔴** —— ⛔ **看不懂一律當紅，絕不當綠**；⛔ **一源紅不把另外兩源一起染色**；⛔ **不得只寫「部分資料」**，一定列出缺的是哪幾筆。**編號 ⚠️ 待確認**（兩檔對 `fetch_monitor` 0 命中）；相鄰可引 `S1-4 §1.2` 呈現層 3／4／6／7／8 |
| `why.source.named` | **常駐** caption：線框具名三源現況 —— FRED（已接線 `fetch_fred`）／TWSE（已接線 `twse_volume`，FMTQIK）／FinMind 額度（**仍然量不到**）＋兩句警語 | 模組常數 `src/ui/views/page_why.py::NAMED_SOURCES`（`@dataclass(frozen=True)`，**本檔宣告、非 L0**）〔FA 實測〕。⚠️ **兩組對它住哪一層說法不同**：FB（規格端）寫的是「**L0 常數 `NAMED_SOURCES`**」⇒ **兩邊並陳，⛔ 本檔不統一**。守衛 `tests/test_p05_why_view.py::TestNamedSourcesAreReallyWired` **直掃 L1 原始碼**，宣稱 `wired=True` 的那一支被拔掉 `@monitored` 即 CI 紅。**TTL 無快取／fallback 不適用**（純函式、零 I/O） | 無計算（三段常駐文字 ＋ 各源 `wired` 旗標） | **沒有缺值態** —— 三段 caption **常駐，⛔ 不得包進任何 if**。無徽章。⭐ **存在理由（逐字）**：使用者可能看到一面**沒有 FRED 的牆**，那**不代表 FRED 沒接線**，代表**還沒有人載入過它** ⇒ **不讓「牆上沒有」被讀成「沒問題」**。⚠️ 兩句警語：「**已接線」≠「你現在看得到那一格**」（還需該 L1 模組本 session 被 import 過）；「**一支綠燈 ≠ 那個來源整體都好**」（量到的單位是**單一取數函式**，不是整個來源）。**編號 ⚠️ 待確認** |
| `why.source.cache_semantics` | **常駐** caption「這面牆上的時間是什麼」：多數來源**吃快取不算**；**有些來源的快取做法不一樣**（例如 FRED 那一支），那一類代表「最後一次**呼叫**」 | 模組常數 `src/ui/views/page_why.py::CACHE_SEMANTICS`（靜態文字）。✅ **FA 實測佐證**：`twse_volume` 是 `@_safe_cache(ttl=TTL_30MIN)` **在** `@monitored` **之上**（命中不記）；`fetch_fred` 的 TTL 走 `src/data/macro/macro_core.py::_FRED_TTL`、判斷寫在**函式體內**、**在** `@monitored` **之下**（命中仍記）⇒ 該揭露屬實。**TTL 無快取／fallback 不適用** | 無計算 | **沒有缺值態**（常駐）。🔴 **合規逐字**：⛔ **不統一宣稱「吃快取一律不計」** —— 那句話對掛在 cache 之內的那幾支是對的、**對 `fetch_fred` 就是假的**；「一句**有一半是假的宣稱**，比承認分不出來更危險」（`CLAUDE.md §1`）。✅ 不受影響的是「未檢查」態：**吃快取不會點亮一盞還沒亮過的燈**。🔴 **洞 `WHY-G-04`**：`CLAUDE.md §2.4`（過期 cache 須帶 `is_stale`、⛔ 禁靜默返回）＋`S2-ENG_SPEC §4.2`（L-a 命中 vs L-c **90 天前快照**「**兩者在畫面上必須分得出來**」）**在本頁 22 block 完全沒有落點**；本塊講的是**時間戳的語意**，**⛔ 不是** `is_stale`。**編號 ⚠️ 待確認** |
| `why.source.unmeasured.finmind_quota` | ⛔ **具名未接線灰卡**（**已實作**）「FinMind（API 額度）」：額度本頁看不到 ＋ 卡住在哪一步 ＋ 為何不畫估計值 | ⚠️ **無資料來源** —— `src/ui/views/page_why.py::UNMEASURED_SOURCES` 寫死 `wired=False`〔FA〕。⚠️ 本 repo 已移除 FinMind SDK，**沙箱 CONNECT `api.finmindtrade.com` 回 403 ⇒ 有無額度 API 未實測**〔FA〕。🔴 **規格端無源可填**：`S1-1_DATA_WHITEPAPER.md` 對「額度／quota」**全檔 0 命中** ⇒ `WHY-G-05`〔FB〕；相鄰可引 `S1-1 §1.1`「需 `FINMIND_TOKEN` ＝ **13** 筆」（額度是跨 13 個 dataset 共用的**帳號層級**資源）。**TTL 無快取／fallback 不適用** | ⛔ **不得有** —— 逐字「**沒有分母就不要算比率**」；⛔ 不拿「已用次數」冒充「剩餘額度」（**兩個不同的數**，沒有分母換算不出來，硬換算就是造假）。⚠️ 線框對這一格的期待值「🟢 額度剩 62%」是**規格示意值、⛔ 非實測** | `wired=False`→**#5**，**⛔ 不可重試**。⛔ **不得用 #7**（不是「這輪沒拿到」，是**結構上量不到**）；⛔ **也不得用 #8**（FinMind 額度對本頁**完全適用**，只是接不上）。⭐ **存在的唯一理由**：**不畫，畫面就會是滿版綠** ——「跳過不畫」是本頁**最隱形的一種假綠燈**。⚠️ **機制 vs 名單**：`UNMEASURED_SOURCES` 是**機制不是名單**，「**清單縮短不等於機制可以拆掉**」。守衛 `tests/test_p05_why_view.py::TestUnmeasuredSourcesAreVisible`。`CLAUDE.md §1`＋`§-1.5.A-7 (2)`（無法沙箱驗的限制要誠實標明）。**編號 ⚠️ 待確認** |
| `why.spec.flags` | L0 自標「未接線／已失準」卡（**動態 card key** `why.spec.<燈key>`；實測 **3 張**：外資現貨淨買賣／融資餘額／財報趨勢） | `src/ui/views/page_why.py::build_spec_flag_cards` ← `SpecScan.unwired` ＋ `SpecScan.degraded` ← **L0** `wired`／`discriminative` 旗標 ＋ `unwired_reason`／`degraded_reason` 原文（`shared/macro_buckets.py` ＋ `shared/station_specs.py`）。零 I/O。標籤 SSOT ＝ `shared/ui_state.py`（`UI_DEGRADED: ("門檻已失準", "🟠", TRAFFIC_YELLOW)`）。**TTL 無快取／fallback 不適用** | 卡數＝`len(SpecScan.degraded) + len(SpecScan.unwired)`（純過濾 `all_rows`；**旗標翻回 True 卡自動消失**）。無數值計算 | `wired=False`→**#5**；`discriminative=False`→**#4**；兩者**皆⛔ 不可重試**（「重按幾次都一樣」）。`why` 欄＝L0 原文（**僅移除會與狀態燈打架的 glyph**）。`empty`／`missing`→**整段不出現**（有意義的空）；`error`→**#6**，且硬規則「**這時牆上不該有任何綠格子**」。🔴 **★待拍板的跨頁取捨（⛔ 本檔不代決）**：改 `shared/ui_state.py` 的 `UI_DEGRADED` 標籤可**一行**修好另外 3 頁的假話，**但本頁這 3 張的「門檻已失準」是正確的**（L0 真旗標）⇒「**改 SSOT 會把一句對的話換成一句比較模糊的話，去修另外三頁的假話**」＝ 待確認 `ⓗ`（尚無裁示）。守衛 `tests/test_p05_why_view.py::TestSpecFlagsComeFromL0`。**編號**：`DECISION_RULES R1-1` 結構性旗標①③ ＋`INDICATOR_SPEC §1 維度 B` |
| `why.source.coverage` | **常駐** caption 涵蓋率揭露：這面牆只涵蓋掛 `@monitored` **且本 session 載入過**的 fetcher（實測 **9 支**）；全站來源清單**數十個** ⇒「**這面牆上沒有紅燈，不等於全站都好**」 | `src/ui/views/page_why.py::COVERAGE_DISCLOSURE` ← 內插 `MONITORED_FETCHER_COUNT`（＝9，**全檔唯一字面值**）。⚠️ 守衛 `tests/test_p05_why_view.py::TestMonitoredCountIsNotStale` 每次 CI 用 **AST 重數** `src/`，對不上即紅；畫面另印可重現指令 `grep -rn "^@monitored" src/ --include=*.py`（⚠️ 原文漏 `^` 時數到的是**字樣數不是支數**，已修正＝**修正錯誤**）。**TTL 無快取／fallback 不適用** | **⛔ 不計算比率**（沒有可靠分母）。⚠️ 明文揭露不涵蓋什麼：全站清單 SSOT 在 L1 `src/data/core/data_registry.py`，L5 依 `CLAUDE.md §8.2` **不得直讀**、`src/services/` 也未轉出 | **沒有缺值態**（常駐，⛔ 不隨載入狀態消失）。無徽章。⭐ **本塊是「分母本身的口徑揭露」，⛔ 不是一個 N/M 比率** —— 見 §2。⚠️ **量測值一律帶日期**：「9 支掛監控」＝**量測日 2026-09-07**（線框自標）；`S1-1 §1.1` 的 `DATA_REGISTRY` **78 筆**／可 ping **15/78** ＝ 該檔量測日 ⇒ **⛔ 不得當今天的值**。**編號 ⚠️ 待確認**；相鄰可引 `UI_COMPONENTS §2 #9` 的 fail-safe |
| `why.engineer.gate` | 摺疊區「🔧 進階診斷（工程師用；一般使用者不需要打開）」＋**一顆** checkbox「載入進階診斷（較耗時，部分項目會實際打外部 API）」；未勾＝⬜ idle 卡，勾＝🟢「已載入 · 登錄表有 N 支 fetcher」 | `src/ui/views/page_why.py::build_engineer_card` ← `EngineerRequest.opened`（`st.checkbox`，key `p05v_engineer_gate`）＋ `src/ui/views/page_why.py::load_sources`（**未勾時一次 L0 都不讀**）。⭐「**摺疊 ≠ 省執行**」：摺疊只收合**視覺**，body 每次 rerun 照樣執行 —— 真正省下執行的是這顆 gate（`S2-ENG_SPEC §5.5`「按了才算」＋`§5.7 K-6`「⛔ 不得為了效能而預抓」）。**TTL 無快取／fallback 不適用** | `N` ＝ 登錄表上的取數支數（＝`MONITORED_FETCHER_COUNT` 的執行期值） | 未勾→**#3 ⬜**（⛔ **不是紅**；**可重試＝展開＋勾選**，指路句**必須兩步兩 label**）；登錄表讀不出→**#6 🔴**（**整段唯一准用紅色的狀態**，**⛔ 不可重試** —— 那是**名單本身**的問題）。⚠️ **單格沒回⛔ 不把整段染色**（逐格獨立判態）；⭐「一盞來源燈紅了是**有效結果**，**⛔ 不代表診斷壞了**」。⛔ **刻意無 `st.form`**（只有 1 個 widget，包了省不到 rerun；v1 的「6 個 checkbox 包成 form」實測**要解的問題不存在**，`form_diag` 撤回）。🔴 **合規逐字**：⛔ **不准為本頁發明一顆按鈕** —— `mainCTA: null` 是實測結果，`S1-3` 寫的「🚀 重新檢查所有來源」**那顆按鈕不存在**；**這顆 gate 是葉內 gate，⛔ 不是主 CTA**。守衛 `tests/test_p05_why_view.py::TestEngineerGateHasNoForm`／`TestThreeStatesNeverMix`。**編號 ⚠️ 待確認** |
| `why.engineer.monitor` | gate 開啟後的 `st.dataframe` **八欄**：取數名／分類／更新頻率／最後一次真實抓取／回了幾列／耗時(ms)／錯誤／**本頁看不懂的狀態** | `src/ui/views/page_why.py::_render_engineer_block` ← `SourceScan.probes` ← **L0 `shared/fetch_monitor.py::get_monitor_registry`**（與 `why.source.wall` **同源、⛔ 不同呈現**，零 I/O）。🔴 **已登記的第二真相源**：`STATUS_NEVER_RUN`／`STATUS_OK`／`STATUS_FAILED` 是**重抄的** —— L0 沒把這三個狀態匯出成常數（**量測日 2026-09-07**：三個都是 inline 字面值）；以 `tests/test_p05_why_view.py::TestStatusLiteralsMatchL0` 直掃 L0 原始碼，字面值一漂移即 CI 紅。⚠️ **「分類」「更新頻率」兩欄值從哪來 ＝ ⚠️ 待確認** `ⓕ`（`S1-1 §1.1` 有的是 `DATA_REGISTRY` 的分佈，**與 `fetch_monitor` 登錄表是不同集合**）。**TTL 無快取／fallback 不適用** | 無計算（欄位原樣透傳，空值填 `—`）。`耗時(ms)` ＝ fetcher 進出時間差（`S2-ENG_SPEC §5.6`：⭐「repo 已有 `shared/fetch_monitor.py` —— **優先擴充它，⛔ 不要另起一套**」） | 未勾→**整段不畫**（⛔ 不畫成第二張灰卡，「只留上一格那張灰卡」）。登錄表為空→caption「登錄表目前是空的」＋指回使用者版灰卡（⛔ 不畫空表），且逐字「**理由不重寫第二次**」（同一句話出現兩次，兩份遲早不一致）。`error`→**#6**，**逐面板獨立，⛔ 不染另外五個**。表內無徽章。⭐ **第八欄的用途**：L0 給了本檔不認得的狀態字面值時**原文放這裡**，⚠️ **不認得一律當紅，絕不當綠**（`S1-4 §1.3(d)`「未登記的缺值原因會**靜默落成灰色**」的反向防線）。⚠️ 揭露：「未執行」＝本 session 尚無真實外抓，**快取命中點不亮它**。**編號 ⚠️ 待確認** |
| `why.engineer.panels` | gate 開啟後 3 欄卡牆，**六項中未接線的五項**（**動態 card key**）：資料源清單／雙演算法對帳(`§4.3`)／API 根因診斷／原始資料表／門檻校準（第六項「取數監控」＝上一列） | ⚠️ **五項皆無資料來源** —— `src/ui/views/page_why.py::ENGINEER_PANEL_SPECS` 五筆全 `wired=False`。各自卡在**不同**位置：① L1 `src/data/core/data_registry.py` **無 L3 轉出**（L5 不得直呼 L1）；② L2 `src/compute/risk/reconcile.py` **無 L3 wrapper**、輸入也得由上游餵；③ 現行 `src/ui/pages/api_diagnostic.py` 會**當場 L5 直接對外送請求**（違憲，⛔ 不繼承；搬好前既有 🔎 資料診斷分頁**未下架**）；④ 只有各頁私有 session key、**無具名介面**（去翻別頁 session 會**靜默變空**）；⑤ 現行面板從 UI import `scripts/`。**TTL 無快取／fallback 不適用** | 無計算（五張皆未接線） | 五張全 **#5 ⛔ 未接線**（dashed），**⛔ 皆不可重試**。⭐ **五張的「去哪補」刻意各自不同** —— 共用一句「未接線」會讓「**補哪一步就會好**」這個資訊消失。⛔ **不得說成「不適用」**（這五件事對本頁**完全適用**，只是接不上 ⇒ ⛔ 不得用 #8）。**只在 gate 打開時建構** ⇒「**還沒打開**」與「**打開了但這一項沒接線**」⛔ 不同時出現在畫面上。⚠️ ⑤ 另註：「**顯示現行門檻（唯讀）**」vs「**重跑校準（會改到全站判燈、不可逆）**」**須分開**，後者屬**須客戶拍板**的操作。⛔ **不承諾尚未存在的東西**：「備援鏈跑到第幾層」repo 裡**不存在這個資訊**（`S1-3B §3.5.9` 第 1 項＋`S2-ENG_SPEC §3.3`）⇒ 本頁**沒有**為它開任何一格。**編號 ⚠️ 待確認** |
| `why.qa` | `st.chat_input` ＋ 對話氣泡（**AI 回答一律加 🧬 前綴** —— 讓使用者一眼看出那是模型寫的、不是本站算出來的數字）＋ 狀態卡「AI 問答」 | ⭐ **本頁唯一有真外部呼叫的一塊**：`src/ui/views/page_why.py::load_qa` → **L3 `src/services/app_ai_service.py::get_gemini_api_key`** → **L3 `src/services/ai_qa_service.py::run_agent`**（皆為**函式體內 late import ＋ 各自 `try/except`**，且被 `if not req.asked: return` 擋在前面）。金鑰＝`st.secrets` 或環境變數 `GEMINI_API_KEY`（**部署端設定**）。**TTL ＝ ⚠️ 待確認** `ⓔ`（`shared/ttls.py` 九個常數無一標為 AI 用；`S2-ENG_SPEC §4.1`／`§6.3` 未點名 AI 問答）；**fallback ＝ 無**（單一 provider，金鑰缺＝直接紅） | 無數值計算（模型端）。⚠️ AI 被要求「需要數字時**一律呼叫工具，嚴禁自行計算或杜撰**」。⛔ **不用 `len(history)` 頂替 `chat_input` 的 gate**（那是 L0 鐵律禁止的**從資料反推**）。⚠️ 線框自標：「**本頁能保證的只有「它被要求這樣做」**」 | **六條互斥分支**（`UI_PAGE_WHY ⑤`）：(a) 未提問→**#3 ⬜**「尚未提問」（**⛔ 即使沒金鑰也不先亮紅** —— 冷啟動**連「有沒有金鑰」都不先問**，否則就是 `CLAUDE.md §1.A` 第 4 點的**假性錯誤**）；(b) 讀金鑰拋例外→**#6** 帶 `repr(e)`；(c) 無金鑰→**#6**「AI 問答未啟用」（❌ 不可重試）；(d) `run_agent` 拋例外→**#6**＋錯誤原文（✅ 換問法再送）；(e) `ok=False` 或有 `error`→**#6**，訊息空時印「（上游沒有給訊息）」（⛔ 不留白、⛔ 不編一句）；(f) `ok=True` 但 text 空→**#7 灰**「這一輪沒有回答內容」（**有效結果，⛔ 不是故障**）。⭐ **(f) 與 (d)(e) 的分色是本段最重要的一刀**。🔴 **洞 `WHY-G-07`【總管逐字複驗成立】**：`S1-6 §3.1` 標題逐字「**全站免責樣板（訂為強制樣板）**」且列「三件事**缺一不可**」，而線框十格**無任何一格出現免責句**、「免責／投資建議」字樣**沒有一處落在 `why.qa`** ⇒ **AI 問答那一格缺強制免責**。⚠️ **計數兩數並陳、⛔ 不統一**：**FB 報 3 處／總管 regex 量到 6 處**（其中 4 處總管的方法歸屬不到 key）；**承重結論兩邊一致**。🔴 **規格偏離（★待拍板，兩邊並陳⛔ 不代決）**：`S1-3B §3.5.6` 要「**本頁就地顯示設定入口**」＋⛔「**不得把使用者指去改 `.streamlit/secrets.toml`**」；code 逐字「**沒有**就地設定，因為那等於**在畫面上收憑證**」⇒ 線框判定「**兩邊都對了一半**」。守衛 `tests/test_p05_why_view.py::TestNothingIsCalledBeforeYouAsk`／`TestQaLeaf`。**編號 ⚠️ 待確認** |

### 2. 🔴 三個分母彼此不同、⛔ 不可互引（FB 組發現；本頁最容易被讀錯的一條）

- **今天頁「訊號可信度 N／16」** —— 分母單位＝**燈**（五桶 `DangerSpec`），值＝**16**（出處 `S1-3B` Part 3.1）。
- **本頁 `why.source.wall` 的 `partial`「已檢查 N / M 源」** —— 分母單位＝**取數函式**，**M ＝ ⚠️ 待確認（規格端沒有口徑）**：線框寫泛用 `M`，但 `S1-3B §3.5.4`／`§3.5.7` 逐字寫 **16**（那是「燈」的分母，**不是源的分母**）⇒ 洞 `WHY-G-03`；另查 `S1-1 §1.1` 得 78 筆、可 ping 15/78 ⇒ **16／9／78 三個數沒有一個被規格指定為 `M`**（待確認 `ⓒ`）。
- **本頁 `why.source.coverage`** —— **9 支掛監控 vs 全站數十個**（`S1-1` 實測 `DATA_REGISTRY` **78 筆**）⇒ ⭐ 它是「**分母本身的口徑揭露**」，**⛔ 不是一個比率**。
- ⇒ 🔴 **合規硬規則：⛔ 沒有分母就不要算比率**（本頁第二個落點，第一個是 16 vs 18）；⛔ **三個分母不得互相引用當對方的母體**。

### 3. 誠實揭露（`CLAUDE.md §-2` 規則 6）

- **FA（程式碼端）／FB（規格端）兩組皆為單組結論、⛔ 未互相複驗。** 所有「0 命中／只有 N 處／全部」都是單次掃描的**全稱句**，依 `§8.2.A.0` 規則 2（禁窮舉宣稱）**請據此打折**，⛔ 不得作為後續動作的前提。**標【總管複驗】者為例外**（共 6 項：`[A:無此頁]`／零 L1 import／動態 card key／`WHY-G-07`／`WHY-G-06`／`why.edu.health6` 的洞性質）。
- **FA 有做 AST，但⛔ 未實跑畫面、⛔ 未跑 pytest**；**FB ⛔ 完全未回 code 驗證任何一條**（寫的是**規格怎麼寫**，不是**程式怎麼跑**）—— 凡 FB 端出現 `檔名::符號` 一律是**規格文件自己轉述的**。
- **兩組都沒讀完**：`S1-3B_FULL_DRAFT_SPEC`（287KB，FB 只讀 `§3.5` 九節）／`S1-3_IA_WIREFRAME`／`wf_global.js`／`docs/v2/stage1/*`／`S2-PRD`／`S1-5`／`S1-7`／`UI_TOKENS`／`UI_PRINCIPLES` ⇒ **⛔ 不得逕自宣稱「規格沒寫」，只能說「在所掃範圍內沒找到」**。⚠️ **線框示意值⛔ 不得寫成事實**（已指認至少 3 處：`09/13 週五`／「🟢 額度剩 62%」／「🟢 09/13 14:30」）。
