# 【S2-PRD】產品需求文件 — 台股／ETF 戰情室 IA v2

> **產出日** 2026-09-14　**執行** 文件分層組（單組）
> **性質** 三份拆分文件之一。**本份只回答「要做什麼、給誰用、怎麼算成功」。**
> ⛔ 本份不含：UI 長什麼樣（→ `S2-UI_SPEC.md`）、怎麼實作（→ `S2-ENG_SPEC.md`）。
> **唯讀聲明** 本組未修改兩 repo 任何檔案、未修改任何既有 `wf_*.js` / `warroom_ia_v2.html`、未執行任何 git 寫入。

---

## 0. 這份文件怎麼來的、怎麼讀

### 0.1 為什麼要拆成三份（客戶 2026-09-14 審稿結論，逐字）

> 這份線框稿混合了：**產品需求**（要做什麼）／**UI 設計**（長什麼樣）／**工程實作**（怎麼做）／**資料治理**（狀態與合規）。
> 建議拆成三份：1. **PRD**：使用者任務、核心流程、成功指標。2. **UI 規格**：四層結構、狀態、元件、響應式、文案。3. **工程實作規格**：資料字典、狀態機、API、快取、測試。

**本組的拆法（三份對應）**

| 客戶點名的四類內容 | 去哪一份 |
|---|---|
| 產品需求（要做什麼） | **本份 `S2-PRD.md`** |
| UI 設計（長什麼樣） | `S2-UI_SPEC.md` |
| 工程實作（怎麼做） | `S2-ENG_SPEC.md` |
| **資料治理（狀態與合規）** | ⭐ **拆兩半**：**呈現面**（狀態怎麼畫、合規文案怎麼寫）→ `S2-UI_SPEC.md`；**契約面**（狀態機、缺值語彙 enum、資料字典）→ `S2-ENG_SPEC.md` |

> 🧠 **本組判斷（未經第二組複驗）**：客戶列的是四類內容、三份文件，**第四類必須被分配**。
> 把「資料治理」整包放進任一份都會出問題 —— 放 UI 規格會讓 enum 契約找不到、放工程規格會讓文案寫手讀不到。
> 依「**誰會照著它做事**」拆：畫面的人讀 UI 規格、寫程式的人讀工程規格。
> ⚠️ 若客戶原意是「資料治理獨立成第四份」，本拆法即刻作廢，**請當場推翻**。

### 0.2 材料來源與搬遷原則

- **本份是「拆分 ＋ 補缺」，不是重寫。** 既有內容一律**標明搬自哪一份的哪一節**，原文的判斷、警語、未複驗標註**一併搬過來，不重新下結論**。
- **凡標「⚠️ 單組」「🧠 本組判斷」的既有結論，搬過來之後仍然是「⚠️ 單組」「🧠」** ——
  ⛔ **不因為被引用第二次就升格成事實**（`CLAUDE.md §-2` 規則 6）。
- 🆕 **本份新寫的只有一塊：§5 使用者測試計畫 ＋ §6 回饋與行為追蹤（含隱私評估）**，逐節標 🆕。

### 0.3 證據等級標記法

> **搬自** `S1-3B_FULL_DRAFT_SPEC.md` Part 0.2（該節自陳沿用 `S1-4 §0` 三欄表）。**一字未改。**

| 標記 | 意思 | 你可以怎麼用 |
|---|---|---|
| 無標記 ＋ 附 `檔名:行號` | **實測事實** | 可當前提 |
| 🧠 | **本組判斷** | ⛔ 不得當已查證事實引用 |
| ⚠️ 單組 | **未複驗的全稱句** | 只能當**待驗事項** |
| ★ | **待客戶拍板** | 拍板前不得動工 |
| ⚑ | **與 2026-09-05 核准線框不同** | 拍板前不得動工 |
| 🆕 | **本份新寫**（四個缺漏的補件） | 尚未經客戶審閱 |

### 0.4 ⛔ 本份是內部文件（客戶明示，三份皆適用）

> **客戶原話**：偏離表與已知限制**不應出現在使用者介面**。

**寫死三條**

1. **本份、`S2-UI_SPEC.md`、`S2-ENG_SPEC.md` 三份都是內部文件。** 任何一段文字⛔ **不得**原樣搬上畫面。
2. ⛔ **「與核准線框的偏離表」（`S1-3B` 附錄 B）、「已知做不到」（`S1-3B` Part 8.1）、「未複驗事項」（Part 8.3）⛔ 一律不上畫面。**
   它們的用途是**內部驗收與稽核**，不是使用者資訊。
3. ✅ **但「已知做不到」在畫面上有一個對應物，兩者不可混為一談** ——
   畫面上該出現的是**那一張卡的誠實狀態與說明**（例：「這一欄目前分不出『公司虧損』與『來源沒給』」），
   ⛔ **不是**整張「repo 做不到什麼」的清單。
   **一句話判準**：**使用者看到這張卡時需要知道的限制 → 上畫面；我們內部知道自己哪裡沒做完 → 不上畫面。**

---

## 1. 產品定位與使用者

> **搬自** `S1-3B` Part 1.2、Part 2.11（五頁線性關係表）、Part 3 各頁 `x.0` 的「職責」欄。

### 1.1 一句話定位

**一個給散戶自用的台股／ETF 決策輔助站**：把「今天能不能出手 → 找什麼 → 這一檔行不行 → 手上的怎麼調」串成一條線，
**全程不產生買賣建議**，只做**偏離提示、客觀對照、情境試算**。

### 1.2 使用者

| | |
|---|---|
| **主要使用者** | **客戶本人（單一使用者）**。實測支撐：持股資料存在客戶自己的 Google Sheet（`S1-7 §2` 第 1 項）；OAuth token 只存在 `st.session_state`、不落地（`S1-7 §2.2`）。 |
| **⚠️ 這件事影響很大** | 「單一使用者」直接決定了 §5 使用者測試與 §6 行為追蹤的可行範圍 —— **樣本數天生受限**，且**任何跨使用者的 A/B 分流在這個規模下沒有統計意義**。見 §5.4 與 §6.3。 |
| **不是誰** | ⛔ 非機構、⛔ 非多租戶 SaaS、⛔ 非下單系統（**系統沒有下單功能，也不會有** —— `S1-3B` Part 5.4 硬禁令 1）。 |

### 1.3 五頁 ＝ 五個使用者任務（線性流程）

> **搬自** `S1-3B` Part 2.11「五頁的線性關係」表，**一字未改**。

| 步 | 頁 | 使用者任務（一句話） |
|---|---|---|
| 第 1 步 | 🚦 今天 | 能不能出手、到幾成 |
| 第 2 步 | 🔍 找標的 | 縮成一張候選清單 |
| 第 3 步 | 🔬 查一檔 | 這一檔行不行（可從第 2 步帶代號過來） |
| 第 4 步 | 💼 我的持股 | 該加、該換、該減 |
| **旁支 · 不編號** | 📖 憑什麼 | 對某個數字有疑問時隨時跳進去，**看完能回到你來的那一頁** |

**資訊架構規模**（搬自 `S1-3B` Part 2.1）：**五頁、十一葉、二十九區塊 ＋ 一條跨頁頂部狀態列**。
⚠️ **單組**：「M-1 有 29 個 blocks ＋ 1 條跨頁狀態列」是逐行讀 `stock_ia_v1.html:735-1023` 得出，
若某個 block 的 `t:` 欄用了不同寫法（跨行、雙引號）會漏掉（`S1-3B` §8.3.2 第 1 項）。**搬過來仍是單組。**

---

## 2. 母法與已拍板事項（產品層級）

> **搬自** `S1-3B` Part 1.1／1.2，**逐條原文**。本節只收**產品層級**的拍板；
> 技術落地位置見 `S2-UI_SPEC.md` / `S2-ENG_SPEC.md` 的對應節。

### 2.1 客戶 2026-09-14 三條拍板

| # | 拍板（客戶原文） | 產品層意義 | 落地在哪一份 |
|---|---|---|---|
| **一** | 底層維持現有態數不動；**Off-Market 改為「資料時點揭露」**（每張卡標資料歸屬日與性質，例如 `09-13 收盤`），**不判斷市場開沒開**。 | **產品承諾改變**：本站**不再宣稱**自己知道市場開沒開；改為承諾「**每一個數字都告訴你它是哪一天的**」 | 呈現規格 → `S2-UI_SPEC §5`；契約 → `S2-ENG_SPEC §2` |
| **二** | 持股帳本加第 6 欄 `target_pct`（再平衡偏離用）。⚠️ 但舊分頁存檔是「整表清空再重寫五欄」，會把第 6 欄靜默擦掉 —— 凡用到目標比例處，**一律標明此限制尚未解除**。 | **功能新增 ＋ 一條必須對使用者誠實揭露的限制** | 揭露落點 → `S2-UI_SPEC §9`；資料模型 → `S2-ENG_SPEC §1.3` |
| **三** | 全站不產生直接買賣建議；再平衡一律「偏離提示、客觀對照、情境試算」，**嚴禁一鍵再平衡按鈕、嚴禁預設最佳配置推薦**。 | **產品紅線**（見 §3.3） | 文案 → `S2-UI_SPEC §9`；守衛 → `S2-ENG_SPEC §8` |

### 2.2 既有拍板（仍然有效）

> **搬自** `S1-3B` Part 1.2 表，**一字未改**。

| 日期 | 拍板內容 | 出處 |
|---|---|---|
| 2026-09-05 | **五頁 IA 線框逐字核准** | `docs/wireframes/stock_ia_v1.html` |
| 2026-09-07 | **雙軌並行**：新 UI 一律以新增 Tab 掛載，不取代舊頁 | ~~`ui/helpers/story_nav.py:98-102`（基金站）~~ → 出處改讀法見表下 **📌 R-1**。⚠️ **拍板本身⛔ 不擱置** |
| 2026-09-09 | **頁1 主 CTA 原地重抓、不跳轉** | `spec_21.md:3-4` |
| 2026-09-09 | **跨頁動線**：兩種下一步都做（頁尾「接下來」列 ＋ 就地「帶我去」鈕）；**側欄曝光這批不動** | `warroom:504-538` d10／d11 |
| 2026-08-27 | **參考走勢 2 條不判燈、不算進 16 盞分母** | `UI-B:328-336` 轉錄 |

> 📌 **R-1｜「雙軌並行」這一條⛔ 不因基金移除而擱置（2026-09-14 逐條判斷，決策者：客戶）**
>
> **被擱置的只有「出處」的可讀性，不是「拍板」本身。**
> - **拍板管的是什麼**：**台股站**自己的新 UI 要用**新增 Tab 掛載、不取代舊頁**。
>   這句話的主詞是台股站，**基金儀表板在不在，完全不影響它成不成立**。
> - **為什麼還是要標**：`ui/helpers/story_nav.py:98-102` 是**姊妹 repo（基金站）**的檔案 ——
>   基金儀表板移出產品範圍之後，**這個出處在本產品範圍內查不到了**，
>   後人照著去 grep 會 0 命中，然後誤以為「這條拍板沒有依據」。
> - ⇒ **處置**：拍板**維持有效**；**出處降級為「跨 repo 先例」**，
>   ⚠️ **待辦（非本組職權）**：實作組首次在台股站落地新 Tab 時，**把出處改釘到台股站自己的 code**。
> - ⛔ **禁止的誤讀**：「出處在基金站 → 所以這條跟著基金一起擱置」。
>   **那是連坐，不是判斷**（同類判準見 §2.3 第 3 點）。

### 2.3 🆕 客戶 2026-09-14 第二輪審稿拍板：**移除基金儀表板**（本輪新增，三份文件共用的處置母條）

> **客戶 2026-09-14 第二輪審稿逐字**：
> 「你移除了基金儀表板，但：**母法還寫「雙儀表板」**／**UI_SPEC 還寫「基金儀表板以收益視角摘要卡片唯讀引用」**／
> **ACCEPTANCE 還寫「基金儀表板的配息覆蓋率與同類中位數」**。
> 這些都還在文件裡，只是線框稿把它們擱置了。**母法與子法應該同步更新，或至少加註「本條因基金移除而擱置」，
> 否則後人讀母法會以為基金還在。**」

**本節是三份 `S2-*` 文件裡所有「基金擱置」標註的母條。** 各處的刪除線一律回指本節。

#### 2.3.1 處置方式（沿用本專案既有慣例，⛔ 不是本組發明的新作法）

依 `CLAUDE.md §-2` / `§-1.5.E` 的既有處置慣例：

1. ⛔ **原文一律保留不刪**，原地加**刪除線**；
2. 加註 **「因客戶 2026-09-14 明示『移除基金』而擱置」**＋**日期 2026-09-14**＋**決策者：客戶（user）**；
3. **兩邊理由並陳** —— 舊條文的理由**多半仍然成立**，只是被權衡掉；
4. ⭐ **每一處都必須寫恢復條件**（見 §2.3.2）；
5. ⚠️ **例外：逐字轉錄上不畫刪除線。** 凡屬「逐字搬自上游文件、且該句是上游對自己的自陳」者
   （例：§8.4 的「基金站只讀了 2 個檔」），**在轉錄本體上動刀 ＝ 竄改轉錄本身**
   （`CLAUDE.md §-1.5.E` 處置原則 1 的同一理由）⇒ **改在轉錄旁邊加註**。

#### 2.3.2 ⭐ 「擱置」≠「已解決」——恢復條件（三份文件共用）

> ⚠️ **這是本節最容易被讀漏的一格。** 加了刪除線只代表**現在不做**，⛔ **不代表那個問題被解決了**。

| | |
|---|---|
| **擱置的意思** | 該條**暫停適用**，因為它所管的對象（基金儀表板）**目前不在產品範圍內** |
| ⛔ **擱置不代表** | ⛔ 不代表該條寫錯；⛔ 不代表它被推翻；⛔ 不代表它所揭露的限制已經消失 |
| ⭐ **恢復條件（通則）** | **客戶明示把基金儀表板重新納入產品範圍時，被擱置的條文即刻恢復效力** —— 原文完整保留在刪除線內，**⛔ 不需要重新設計、⛔ 不需要重新審一次** |
| ⭐ **恢復時必須重驗的事** | ⚠️ 恢復**不等於**原文仍然正確：被擱置期間 repo 與姊妹 repo 都可能變動 ⇒ **恢復時必須重新查證該條所引的 `檔名:行號` 與實測事實是否還成立**（`CLAUDE.md §8.2.A.0` 規則 1／規則 4：行號與量測值會過期） |
| **個別條文另有更嚴的恢復條件者** | 以該處所寫為準（各處會寫「本條的恢復條件另加：…」） |

#### 2.3.3 ⛔ 三條「不得連坐」判準（本輪客戶明示點名）

> **客戶原話**：「要分辨哪些其實跟基金無關、不該連坐。已知一例：**台股側「未實現損益不含匯兌」這個限制跟基金站在不在無關，依然成立** —— ⛔ 不得因為它原本寫在「跨站對照」那一段就一起擱置。」

| # | 判準 | 一句話問法 |
|---|---|---|
| **N-1** | **看主詞，不看它被放在哪一段。** 一條規則寫在「跨站對照」「兩站差異」段落裡，⛔ **不代表它的主詞是兩站** | **「這句話拿掉『基金』之後，還有沒有一個台股站的對象被它管到？」有 → ⛔ 不擱置** |
| **N-2** | **看它是「出處」還是「規則」。** 出處落在基金站（或姊妹 repo）而規則管台股站者 ⇒ **規則不擱置，只把出處標為跨 repo 先例** | 見 **📌 R-1**（雙軌並行）與 `S2-UI_SPEC §4.4`（`shared/colors.py` AUTO-SYNCED 註） |
| **N-3** | **ETF ≠ 基金。** 本站的 **ETF**（配息、折溢價、殖利率、逐檔明細）屬**台股／ETF 站本體**，⛔ **一律不擱置** | 誤把 `S2-UI_SPEC §4.3` 的 E-1／E-2（ETF 配息 `0.00` vs `N/A`）當成基金條文砍掉，會**直接刪掉一條憲法 §1 的落地規格** |

**本輪依 N-1～N-3 判為「⛔ 不連坐」的實例（逐條列出，供覆核）**

| 實例 | 在哪 | 為什麼不擱置 |
|---|---|---|
| **未實現損益不含匯兌** | `S2-UI_SPEC §4.3` E-7、`S2-ENG_SPEC §9` 第 2 項 | ⭐ **客戶本輪明示點名**。主詞是**台股／ETF 帳本沒有存買入匯率**（結構性缺欄位），與基金站無關 |
| **ETF 配息 `0.00` vs `N/A`** | `S2-UI_SPEC §4.3` E-1／E-2 | N-3：ETF 是本站本體 |
| **`shared/colors.py` 是 AUTO-SYNCED、DO NOT EDIT HERE** | `S2-UI_SPEC §4.4` | N-2：**姊妹 repo 沒有消失** —— 移除的是「基金儀表板在本產品的範圍」，不是那個 repo。跨 repo 寫入限制**依然成立** |
| **「雙軌並行」拍板** | 本節 §2.2 **📌 R-1** | N-2 |
| **多幣別 / `*_native` / `*_twd`** | `S2-ENG_SPEC §1.2` 鐵律 3、§1.1 索引「多幣別」 | N-1：美股 ETF 也是外幣，拿掉基金之後這條仍有對象 |

#### 2.3.4 ⚠️ 本節的三條誠實揭露（`CLAUDE.md §-2` 規則 6）

1. ⚠️ **客戶點名的三句原文，本組在三份 `S2-*` 裡一句都 grep 不到**（逐句實測）：
   - **「雙儀表板」** ⇒ 三份 **0 命中**；repo（含 `CLAUDE.md`／`PROCESS.md`／`docs/`）與 scratchpad 全掃**同樣 0 命中**。
   - **「基金儀表板以收益視角摘要卡片唯讀引用」** ⇒ `收益視角` 三份 **0 命中**（本份 §2.3 引用客戶原話那一行除外）。
     **內容上最接近的對應物**是 `S2-UI_SPEC §9.3` 的 **D-8／D-9／D-10** 與 `§11.2` 索引的「基金站」列 —— **已於本輪全部擱置**。
   - **「基金儀表板的配息覆蓋率與同類中位數」** ⇒ 見下一點。
   ⇒ **三句都不在本組可觸及的三份檔案裡** ⇒ 客戶所指的「母法」與「UI_SPEC」**應是客戶自己持有的那幾份頒布本文／早期版本**，
   **不在本組可觸及的範圍內**。
   ⇒ **本節是本組能做到的替代處置**：把「移除基金」這條拍板**登記進本份（產品層母法的鏡像）**，
   讓後人在讀 `S2-PRD` 時**第一時間看到基金已移除**。⛔ **本組沒有、也無權修改客戶那份母法。**
2. ⚠️ **`ACCEPTANCE` 這份子法，本組 grep 全 repo 與 scratchpad ⇒ 檔案不存在**（`find -iname "*ACCEPTANCE*"` 0 命中、
   `grep -rni "acceptance" --include="*.md"` 於 repo 0 命中）。⇒ **客戶點名的那一條「基金儀表板的配息覆蓋率與同類中位數」，
   本組無法在該檔上處置。** 其**內容上的對應物**在 `S2-UI_SPEC §9.3` 的 **D-9**（「同類型平均」不是「同類中位數」＋約三成基金查不到），
   **已於本輪擱置**。⚠️ **但那不等於 `ACCEPTANCE` 被處理了** —— 若該檔存在於本組看不到的地方，**它仍未同步**。
3. ⚠️ **「三份文件裡所有提到基金的地方都處理了」是本組單組掃描的結論，未經第二組複驗**（掃描關鍵字見 §8.2 第 4 列）。


---

## 3. 核心流程與產品紅線

### 3.1 主流程：四步一旁支

> **搬自** `S1-3B` Part 2.11（兩種下一步）＋ Part 2.11「頁2 → 頁3 帶代號過去之後」。

**兩種「下一步」，客戶已拍板兩種都做**

| | ① 頁尾「接下來」列 | ② 就地「帶我去」鈕 |
|---|---|---|
| **回答的問題** | 「**然後呢**」（主線） | 「**這個壞掉的東西去哪修**」（修復動線） |
| **位置** | 每頁頁尾，葉外 | 灰卡「去哪補」那一行後面 |
| **數量** | 固定 2 顆下一步 ＋ 1 顆「← 回舊版」 | **壞幾個出幾顆**，全綠的日子 **0 顆** |
| **新手引導** | 右上角小字「**第 ② 步 / 共 4 步**」—— ⭐ **這行小字就是新手引導的全部** | — |

**⛔ 明確不做**（客戶理由：字太多）：導覽浮層、歡迎頁、使用說明、麵包屑。

**⭐ 交接後的落地狀態是「代號已填、誠實停著等你按」，⛔ 不是自動開跑。**
理由（`warroom:525` 實測轉錄）：自動開跑會讓使用者在沒按任何東西的情況下被扣一次 API 額度，
且與本站「**按了才取數**」的一貫承諾相反。

### 3.2 「按了才算」是產品承諾，不只是效能手段

> 🆕 **本節是本份新寫的定位陳述**（既有材料把它當效能手段處理，見 `S2-ENG_SPEC §5.4`）。
> **產品層的意義**：它是一條**對使用者的承諾**，必須寫進文案，也必須被 §5 的使用者測試驗收。

- **承諾原文（已存在於規格）**：「按下去才會開始取數。**在你按之前，這一頁不會發出任何一次網路連線。**」
  （`S1-3B` Part 2.14.1「③ 未載入」列，原文）
- **為什麼它是產品需求而不是實作偏好**：它同時買到三件事 ——
  (a) 使用者的 API 額度不會被默默花掉；(b) 「未載入」這個狀態變成**誠實且可解釋的**（因為它就是「你還沒按」）；
  (c) 冷啟動的等待時間變成**使用者自己選的**，而不是被強加的。
- ⛔ **禁止的違反**：任何「進頁面就自動抓一點點」的設計，會同時打破 (a)(b)(c) 三件事。

### 3.3 產品紅線（客戶拍板三的完整落地）

> **搬自** `S1-3B` Part 2.12.2「再平衡三條硬禁令」＋ Part 5.4，**逐字**。

1. ⛔ **禁止「一鍵再平衡」按鈕。** 任何會產生下單清單、或一次套用全部調整的按鈕**都不得存在**。
2. ⛔ **禁止預設最佳配置推薦。** 目標比例**只能由使用者自己填**，系統⛔ 不得預填、⛔ 不得建議、⛔ 不得給「參考配置」。
3. ✅ **只做三件事**：偏離提示／客觀對照／情境試算。

**⭐ 狀態 A 的設計是這條紅線最重要的落地**（搬自 `S1-3B` Part 5.1）：
目標比例沒設時，**畫面一個偏離數字都不准出現**。
📌 舊分頁的失效模式是「**拿現況當目標**」→ 偏離度**恆等於 0.0%** → **永遠顯示綠燈**，
而**那個綠燈代表「沒算」而不是「已平衡」**（實測佐證：`etf_tab_portfolio.py:422` 註解）。

### 3.4 合規禁用語（產品層宣告）

> 完整表 ＋ 替代表述在 `S2-UI_SPEC §9.1`。本節只宣告**產品層的三件事**。

- 禁用語清單**含本稿新增的「建議持股 X%」**（⚑ 偏離 `S1-3:171-183`，理由見 `S1-3B` Part 2.12.1）。
- ⚠️ **「全站」的射程含不含 LINE 推播，客戶尚未裁示** —— `S1-3B` ★-14d，本份同樣**不裁決**。
  這不是技術問題：`station_cards.py:535-537` 明文記載 `suggest_action` **同時餵畫面與 `scripts/push_holdings_daily.py`**，
  **只有客戶知道推播算不算「全站產出」**。→ 併入 §7 待拍板清單。

---

## 4. 成功指標

> ⚠️ **拆分說明**：既有材料**沒有一份寫過產品成功指標**（本組掃 `S1-1`~`S1-7` ＋ `S1-3B` ＋ `DRAFT_GAP_AUDIT`，
> 關鍵字 `成功指標` / `KPI` / `驗收標準` **0 命中**）。⚠️ **單組，未窮舉同義詞**（未掃 `metric` / `目標值` / `達成`）。
> ⇒ 本節**不是搬遷，是本份依既有拍板推導**，逐條標 🧠。**★ 送客戶拍板（★-P1）。**

### 4.1 三類指標，分開看

> 🧠 **本組判斷**：這個產品**沒有營收、沒有留存壓力、使用者只有一個人**，
> 所以⛔ **不適用** DAU／轉換率／留存曲線那一套。能定義的只有下面三類。

| 類 | 指標 | 怎麼量 | 目標 |
|---|---|---|---|
| **A. 誠實度**（最高優先） | **畫面上出現「看起來有效、其實是猜的」數字的次數** | 由 `S2-ENG_SPEC §8` 的守衛測試計數（反例快查 R-1~R-17 每一條各一支測試） | ⭐ **0**。這是唯一一條「不是越好越好，而是必須為 0」的指標 |
| | **缺值三分判錯率** | 抽查 N 個格子，逐格跑 `S2-UI_SPEC §4` 決策樹，比對畫面 | 0 |
| | **`why` / `where` 世界觀對撞數** | `S2-UI_SPEC §3.2` 的三條強制檢查 | 0 |
| **B. 可理解度** | **5 秒新手測試通過率** | §5.2 | 見 §5.2 成功標準 |
| | **10 秒老手測試通過率** | §5.3 | 見 §5.3 成功標準 |
| | **指路句可達率**：每一句「去哪補」指去的東西，畫面上找得到 | 逐條比對（`S1-3B` 每頁 `x.8` 已有此表） | ⭐ **100%**。📌 實證：M-1 `:884` N2 更正記載，上一版宣稱「每個『去哪補』都指向畫得出來的地方」，逐條比對後發現**五顆被指去的按鈕根本沒畫出來** |
| **C. 效能** | Skeleton／主內容時間 | **量測方法見 `S2-ENG_SPEC §5`** | ★ 目標值待拍板（★-P2，見下） |

### 4.2 ⚠️ 效能目標值的來歷必須講清楚

> 客戶在審稿回饋中引述了「**Skeleton < 0.3s、主內容 < 2.5s**」這組目標。

**本組查證結果（實測）**：這組數字**不在**現有任何一份交付物裡 ——
`grep -rn "0\.3s|2\.5s|Skeleton|skeleton|LCP|FCP|效能預算"` 掃遍 scratchpad 全部 `S1-*.md` / `UI-*.md` /
`DRAFT_GAP_AUDIT.md` / `wf_*.js` / `warroom_ia_v2*.html` ＋ repo `docs/`，**除了無關的二進位檔外 0 命中**。
⇒ ⚠️ **單組窮舉**（未掃同義詞如 `首屏` / `TTI` / `毫秒`）。

**⇒ 本份的處置（兩條）**
1. **把它當成「客戶口述的目標候選值」收錄，⛔ 不當成已拍板規格。** 送 **★-P2**。
2. ⭐ **無論拍板成哪個數字，`S2-ENG_SPEC §5` 的「怎麼量」都必須先定案** ——
   **一個沒有定義量測環境的門檻值，是不可驗收的**（客戶回饋點名的正是這一點）。

---

## 5. 🆕 使用者測試計畫

> 🆕 **全節新寫。** 客戶點名缺漏第 3 項：「5 秒新手測試、10 秒老手測試」的**腳本、樣本數、成功標準、測試環境都沒有寫死**。
> ⚠️ **本節全部由本組單組設計，未經第二組複驗，也未經客戶審閱。** 依 `CLAUDE.md §-2` 規則 6，只能當**提案**。

### 5.1 為什麼先做這兩個測試，而不是別的

> 🧠 **本組判斷 —— 這兩個測試不是通用模板，是為了驗收兩條「規格自己承認有風險」的決定而設計的。**

| 測試 | 它在驗收哪一條規格風險 | 該風險的原文出處 |
|---|---|---|
| **5 秒新手測試** | ★-10 候選甲把頁1 主數值從「建議持股 70%」改成「總經健康分 68 —— 落在『偏多』區間」。**規格自己承認代價**：「健康分 68 比『持股 70%』**更抽象**。使用者第一次看到會問『68 是高還是低』」 | `S1-3B` Part 3.1「★ 誠實揭露的代價」 |
| **10 秒老手測試** | 客戶拍板第 1 條**點名要求**：「未載入」與「未接線」**各有獨立且不同的說法**。**三個灰的狀態（③ 未載入 ⬜／⑧ 未接線 ⛔／⑨ 不適用 ▨）只靠 glyph 與文字分辨** | `S1-3B` Part 2.5.2；SSOT `shared/ui_state.py:112` 逐字「**glyph 刻意各不相同：顏色只有灰／橘／紅／綠四種，灰的四個狀態必須靠 glyph 分辨**」 |

⇒ **這兩條都是「我們相信使用者看得懂，但沒有任何證據」的判斷。** 測試存在的唯一目的是**取得那個證據**。
⛔ **不要把它擴充成「全面可用性測試」** —— 那會讓它跑不動，而這兩個問題會繼續沒有答案。

### 5.2 5 秒新手測試（First Impression Test）

**目的**：一個**沒看過這個站**的人，看 5 秒之後，能不能說出**這一頁在告訴他什麼**。

#### 受測者條件

| | |
|---|---|
| **必要** | 從未使用過本站；⛔ 不曾看過任何線框稿或本份文件 |
| **投資經驗** | **有基本認知即可**（知道「股票有漲有跌」「大盤」是什麼）。⛔ **不要求**懂本益比／殖利率／PMI |
| **排除** | 參與過本專案的任何人（含客戶本人 —— 客戶已看過線框，**不能當新手樣本**） |

#### 逐字腳本（主持人照唸，⛔ 不得改寫、⛔ 不得補充說明）

```
【開場，測試前唸一次】
  「我等一下會給你看一個畫面 5 秒鐘，然後把它蓋起來。
   你不用記住細節，我只想知道你看到的第一印象。
   這不是在測你，是在測這個畫面。答不出來很正常，那是我們的問題。」

【展示畫面 5 秒，計時，到秒立刻蓋掉】

【蓋掉後，依序問。⛔ 問完一題再問下一題，⛔ 不得提示、不得複述畫面內容】
  Q1「這個畫面在講什麼？用你自己的話。」
  Q2「如果你要根據這個畫面做一件事，你會做什麼？」
  Q3「畫面上有沒有哪個地方你看不懂、或者想再看一眼？」

【⛔ 主持人禁止說的話】
  ⛔「你有沒有看到那個健康分？」（提示答案）
  ⛔「這個意思是說……」（代為解釋）
  ⛔「對，沒錯」／「不是喔」（給對錯回饋，會污染後續題目）
```

#### 展示哪一個畫面

| 輪 | 畫面 | 為什麼 |
|---|---|---|
| **第 1 輪** | **頁1 🚦 今天 · 葉1 · 「① 有資料」狀態**（採 ★-10 **候選甲**的主數值） | 驗收候選甲的可理解度 |
| **第 2 輪**（同一批受測者，⛔ 不同人） | 同一畫面，但主數值採 **候選乙**（只留位階與可信度） | ⭐ **這就是 A/B，但它是離線的**（見 §6.3 為什麼線上 A/B 不可行） |

> ⚠️ **兩輪必須用不同的受測者**（between-subjects）。同一個人看過甲再看乙，第二次已經不是「新手」。

#### 成功標準（逐條，⛔ 不得事後放寬）

| # | 判準 | 通過條件 |
|---|---|---|
| **S1-a** | Q1 答案中，受測者說得出**「現在市場（大盤／整體行情）處於什麼狀態」**這個意思 | 語意命中即可，⛔ **不要求**用到「位階」「偏多」這些字 |
| **S1-b** | Q2 答案中，受測者**沒有**把它讀成「叫我買」或「叫我賣」 | ⭐ **這條是合規驗收，不是可用性驗收。** 只要有一位受測者說「它叫我買」，該版本主數值即**不通過**，無論其他題答得多好 |
| **S1-c** | Q3 中，受測者**沒有**把主數值那個數字本身列為「看不懂」 | 針對候選甲「68 是高還是低」那個已知風險 |

**判讀規則**
- **逐位受測者獨立判定 pass / fail**，⛔ 不平均、⛔ 不加權。
- **S1-b 是一票否決制**（理由見上）。
- 甲乙兩版**分別**計算通過率，**併陳給客戶**，⛔ 不由本組代為選一個 —— 選哪一版是 ★-10，客戶拍板事項。

#### 樣本數

| | |
|---|---|
| **建議** | **每版 5 人，兩版共 10 人** |
| **理由（🧠 本組判斷）** | 這是**質性發現**，不是統計推論：目的是「**找出多少人看不懂**」，不是「**估計母體比例**」。5 人是業界常見的質性測試起點。 |
| ⚠️ **必須誠實標明的限制** | **5 人的結果⛔ 不具統計代表性**，⛔ 不得寫成「N% 的使用者看得懂」。正確寫法是「**5 位受測者中有 N 位……**」。 |
| ⚠️ **本專案的額外限制** | §1.2：本站**只有一個真實使用者**。⇒ 受測者必然是**招募來的外部人士**，他們**不是目標使用者本人**。**這個差距無法用增加樣本數消除**，只能誠實標明。 |

### 5.3 10 秒老手測試（Diagnostic Test）

**目的**：一個**已經用過這個站**的人，看 10 秒之後，能不能**正確分辨三個灰色狀態**，並說出**下一步該做什麼**。

#### 受測者條件

| | |
|---|---|
| **必要** | **已完整走過一次五頁流程**（可由主持人在測試前 10 分鐘帶一輪），且**已看過至少一次「有資料」的正常畫面** |
| ⚠️ **關鍵** | 「老手」**不等於**「懂投資的人」，而是「**看過這個站正常長什麼樣的人**」——本測試量的是**這套狀態語言學不學得會**，不是投資知識 |

#### 逐字腳本

```
【開場】
  「這個畫面上有一些地方是灰色的。
   我等一下給你看 10 秒，然後問你那些灰色是什麼意思。
   看不出來很正常，那正是我們想知道的。」

【展示畫面 10 秒，計時，到秒立刻蓋掉】

【蓋掉後，逐題問】
  Q1「畫面上有幾個地方是灰的？大概在哪裡？」   ← 暖身題，不計分
  Q2「那些灰色的地方，是同一個原因嗎？還是不同原因？」
  Q3「如果是不同原因，分別是什麼原因？」
  Q4「這些灰色的地方，你要做什麼它才會變成有資料？」
     ⭐ 若受測者回答「按按鈕」，追問：「每一個都按按鈕就會好嗎？」

【⛔ 主持人禁止說的話】
  ⛔「那個 ⛔ 符號是未接線的意思」（直接給答案）
  ⛔「你再看一次就知道了」（本測試不給第二次）
```

#### 展示哪一個畫面

**頁1 🚦 今天 · 葉2 指標明細**，人工構造成**同時含三種灰**的狀態：
- 至少 1 盞 **③ 未載入 ⬜**
- 至少 1 盞 **⑧ 未接線 ⛔**（📌 真實先例：`us10y` 註冊後永久灰燈 4 個版本沒人發現，`macro_buckets.py:256-262`）
- 至少 1 盞 **⑨ 結構不適用 ▨**

> ⚠️ **這個畫面需要人工構造** —— `S2-ENG_SPEC §5.2` 的固定測試資料集（Fixture）必須包含這一組。
> ⛔ 不得等「真的壞了」才測。

#### 成功標準

| # | 判準 | 通過條件 |
|---|---|---|
| **S2-a** | Q2 答「**不同原因**」 | ⭐ **這是本測試的核心。** 答「都一樣」＝ 客戶拍板第 1 條**沒有落地** |
| **S2-b** | Q3 至少正確分辨出 **「還沒去拿」vs「這盞燈沒在運作」** 兩類 | ⛔ **不要求**背出「未接線」三個字，要求的是**語意分得開** |
| **S2-c** | Q4 追問時，受測者**知道「按按鈕」不能解決全部** | ⭐ 針對 `S1-3B` Part 2.5.2 的 `why`／`where` 世界觀一致性 —— 對「⑧ 未接線」若受測者以為按按鈕會好，代表**畫面在騙他白按** |
| **S2-d** | 對「⑨ 不適用」，受測者**沒有**認為那是壞掉 | 原文要求「明說**這不是壞掉**」 |

**判讀規則**：S2-a **一票否決** —— 若多數受測者答「都一樣」，代表三個灰的區分**在畫面上不成立**，
⛔ 不得以「那是使用者不夠專心」結案，必須回頭改 `S2-UI_SPEC §3.2` 的呈現。

#### 樣本數

**5 人**（同 §5.2 的理由與同樣的限制標註）。⚠️ 老手需要前置導覽，**每人耗時較長**，排期時要算進去。

### 5.4 測試環境（兩個測試共用，⛔ 不得各測各的）

> ⚠️ **本組在本次 session 中沒有跑過任何一個畫面**（`S1-3B` §8.3.1 第 1 項同樣自陳「一個畫面都沒跑過」）。
> ⇒ 下表是**規定**，不是**現況描述**。

| 項目 | 規定 | 為什麼要釘死 |
|---|---|---|
| **裝置** | **兩台都要測**：① 桌機 ≥1280px；② 手機直向 ≤480px | `S2-UI_SPEC §6` 的斷點在這兩段的欄數不同（3 欄 vs 1 欄），**同一句文案在兩個版面的可讀性不一樣** |
| **⛔ 不測的** | 641–880px 那一段 | 🧠 它本身還是 ★-06 待拍板，**在未定案的版面上測可讀性沒有意義** |
| **瀏覽器** | 受測者**自己平常用的那一個**，主持人記錄是哪一個 | 測的是理解度不是相容性；⛔ 不要為了控制變因逼人換瀏覽器，那會降低情境真實度 |
| **畫面來源** | ⭐ **靜態截圖，⛔ 不是可互動的 app** | 三條理由：(a) 5 秒／10 秒內受測者不會互動，可互動反而引入「他點到哪裡」這個雜訊；(b) app 冷啟動耗時（`tab_macro.py:167,405` 記載總經頁冷啟動 **40~75 秒**）會把 5 秒測試變成 75 秒等待；(c) 需要人工構造的三灰畫面在真 app 上**不一定跑得出來** |
| **⚠️ 這一條的代價（誠實標明）** | 靜態截圖**測不到**：載入中的體感、按下去有沒有反應、捲動、展開佐證的手勢 | ⇒ 這些⛔ **不在**本測試射程內。要測它們需要另一種測試，**本份不設計**（超出客戶點名的缺漏範圍） |
| **時間控制** | 用**碼錶或手機計時器**，到秒立刻蓋掉；⛔ 不得「差不多了」 | 5 秒與 10 秒是**判準的一部分**，不是氣氛 |
| **主持人** | **1 位主持 ＋ 1 位記錄**，⛔ 不得由同一人兼任 | 主持人一邊唸題一邊記錄會漏掉受測者的猶豫與反問 |
| ⭐ **主持人不得是規格作者** | 依 `CLAUDE.md §-1.5` 第一條 3「實作與稽核嚴格分離：**實作者不得自己驗收自己**」 | 寫這句文案的人主持測試，會不自覺地替它解釋 |

### 5.5 記錄方式

| 記什麼 | 怎麼記 | 為什麼 |
|---|---|---|
| **逐題原話** | **逐字記錄，⛔ 不要摘要** | 「我看不太懂那個 68」和「68 是什麼」是兩個不同的訊號 |
| **首次開口的秒數** | 碼錶 | 遲疑本身是資料 |
| **受測者的反問** | 全部記下 | 反問＝畫面沒答到的問題，是最有價值的產出 |
| **pass / fail 逐條** | 依 §5.2／§5.3 的判準表逐條打勾 | ⛔ 不得只記一個總分 |
| ⛔ **不記** | 受測者姓名、email、投資部位、持股內容 | 見 §6.4 隱私原則 —— **測試記錄本身也是 PII 來源** |

**產出格式（規定）**
- 一份 `S3-USERTEST_RESULT.md`（**scratchpad，⛔ 不進 repo**），含：逐位受測者的逐條 pass/fail ＋ 原話摘錄 ＋ **本組沒做到的部分**。
- ⭐ **必須明列「N 位中有 M 位」，⛔ 不得寫百分比**（5 人算百分比會產生 20% 的假精度）。

---

## 6. 🆕 回饋、A/B 測試與行為追蹤（含隱私評估）

> 🆕 **全節新寫。** 客戶點名：「**沒有 A/B 測試、回饋按鈕、使用行為追蹤的規劃**」，
> 並要求**先評估隱私**（本專案處理使用者持股）。
> ⭐ **本節的順序是刻意的：先評估隱私，再談要不要做。** 反過來寫會變成「先想做，再找理由」。

### 6.1 先講隱私 —— 這個專案的特殊性

> **搬自** `S1-7_PERMISSIONS_PII.md §2`（PII 盤點）與 `§2.4`，**未改動其結論**。

**本站已經在處理的 PII（實測，逐項）**

| # | 資料項 | 存在哪 | 已經離開本機了嗎 |
|---|---|---|---|
| 1 | **持股明細** `ticker` / `lots` / `avg_price` / `updated_at` | 客戶自己的 Google Sheet | ✅ **會**（cron 路徑每交易日在 GitHub Actions runner 上完整讀出） |
| 2 | 個股觀察清單 | 同上，獨立 worksheet | ✅ 會 |
| 3 | **前進式驗證凍結紀錄** | ⚠️ **git 追蹤的** `data_cache/forward_test/picks.parquet` | ⚠️ **已 commit 進 git** |
| 4 | OAuth token bundle | `st.session_state`（行程記憶體） | ❌ **不落地**（`S1-7 §2.2` 正面發現） |
| 5 | Google 帳號 email | `st.session_state` | ❌ 未外送（`S1-7 §2.3`） |
| 8 | 健康度追蹤清單 | ⚠️ **git 追蹤的** `data_cache/health_watchlist.json` | ⚠️（目前 `stocks` 為空陣列） |

**⭐ 這張表對「行為追蹤」的意義（🧠 本組判斷，三條）**

1. ⭐ **這個 repo 已經有「PII 進了 git」的先例（第 3、8 項）。**
   ⇒ **任何新增的追蹤資料，只要落在 repo 目錄下，就有被 commit 的實際風險** —— 這不是假設，是同一個目錄已經發生過的事。
2. **持股內容是本站最敏感的資料。** 行為追蹤若記下「使用者查了哪一檔」，
   **等於在另一個地方重建一份持股／觀察清單**，而那份新的複本⛔ 不受第 1~3 項既有保護（Sheet 權限、OAuth）的保護。
3. **email 目前沒有外送（`S1-7 §2.3`）—— 這是一條不該被追蹤功能打破的線。**

### 6.2 ⇒ 行為追蹤的硬規則（若要做，必須全部滿足）

> ⚠️ **本節是「做的話要怎麼做」，⛔ 不是「建議去做」。** 要不要做是 **★-P3**，客戶拍板。

| # | 規則 | 為什麼 |
|---|---|---|
| **T-1** | ⛔ **禁止記錄任何標的識別資訊** —— `ticker`、股名、產業、`lots`、`avg_price`、金額、目標比例，**一律不得進入追蹤紀錄** | §6.1 第 2 點。記了就等於重建一份持股清單 |
| **T-2** | ✅ **只記「事件種類 ＋ 頁面 ＋ 區塊 ＋ 狀態」**，例：`{event:"cta_click", page:"today", block:"02_action_bar", ui_state:"idle"}` | 這組欄位足以回答「哪一個區塊沒人展開」「哪一種狀態最常被按」，且**不含任何標的** |
| **T-3** | ⛔ **禁止記錄使用者識別碼** —— 不記 email、不記 OAuth sub、不生成持久化的 user id | §6.1 第 3 點；且 §1.2 只有一個使用者，**識別碼本來就沒有資訊量** |
| **T-4** | ⭐ **存放位置必須在 `.gitignore` 內，且不得在 repo 目錄下** | §6.1 第 1 點的先例。⚠️ **僅僅寫進 `.gitignore` 不夠** —— 第 3 項就是被明確 commit 進去的 |
| **T-5** | **必須有保留期限與自動刪除**，且期限寫進文件 | 沒有期限＝無限期保留 |
| **T-6** | ⭐ **使用者必須看得到「現在有沒有在記」，且可以關掉** | 不可見的追蹤，在一個處理持股的站上不可接受 |
| **T-7** | ⛔ **禁止第三方分析服務**（GA／Mixpanel／Sentry 之類） | 那等於把上述全部規則的執行權交給外部；且 `.streamlit/config.toml:24` 已設 `gatherUsageStats = false`，**現行設定本身就是「不外送」的表態** |
| **T-8** | 追蹤紀錄**⛔ 不得進入任何 LLM prompt** | 呼應 `S1-7 §6` 的 G6 建議（補一個「PII 不得進 prompt」的守衛測試） |

> ⚠️ **T-1~T-8 由本組單組擬定，未經第二組複驗、未經客戶審閱、⛔ 未經法遵檢視。**

### 6.3 A/B 測試 —— 本組評估為「線上不可行，離線可行」

| | |
|---|---|
| **結論** | ⛔ **不建議做線上 A/B 分流**；✅ **改用 §5.2 的離線兩版對照** |
| **理由一（統計）** | §1.2：**只有一個真實使用者**。線上 A/B 需要把使用者分到兩桶再比較 —— **n=1 分不出兩桶**。任何「A 版轉換率較高」的結論在 n=1 下都是雜訊 |
| **理由二（技術，實測）** | A/B 分流需要**穩定的分桶識別碼**。本站唯一的使用者識別是 OAuth email（`S1-7 §2.3`，**目前刻意未外送**）。要做分桶就得**持久化一個識別碼** → **直接違反 T-3**，並打破 §6.1 第 3 點那條線 |
| **理由三（框架，實測）** | Streamlit 每次互動整頁 rerun（見 `S2-ENG_SPEC §5.4`）。分桶若存在 `session_state`，**重整瀏覽器就換一桶**；要跨 session 穩定就得落地儲存 → 回到理由二 |
| ✅ **可行的替代** | **§5.2 第 1/2 輪的 between-subjects 對照**：同一個畫面做兩版截圖，給不同受測者看。**它是 A/B 的本質（比較兩個版本），只是不在線上跑、不需要識別碼、不碰 PII** |

### 6.4 回饋按鈕 —— ✅ 建議做，但有三條限制

| | |
|---|---|
| **建議** | ⭐ **做**。這是三件事裡**唯一**成本低、隱私風險可控、且能直接回答「使用者卡在哪」的 |
| **位置** | **頁尾「接下來」列的同一條**（`S1-3B` Part 2.10.3 的三段式頁尾第 1 段），⛔ 不做浮動按鈕、⛔ 不做彈窗（違反客戶「⛔ 不做導覽浮層」的同一條理由：字太多、擋畫面） |
| **F-1 限制** | ⭐ **送出前必須顯示「這則回饋會包含什麼」的完整內容，讓使用者自己看過再送。** ⛔ **禁止自動夾帶畫面快照、session 內容、持股資料** |
| **F-2 限制** | 預設只送**使用者自己打的文字 ＋ 當前頁面代號**。若要附帶狀態（例如「當時哪幾盞是灰的」），**必須逐項列出來給使用者看，並可逐項取消** |
| **F-3 限制** | ⛔ **回饋內容不得自動進入任何 LLM**（同 T-8）。使用者可能在回饋裡打進任何東西 —— 📌 先例：`S1-7 §2` 第 285 行記載 `etf_tab_ai.py::_render_free_qa` 雖然「只有使用者自己打的問題字串，無組合資料注入」，但**使用者可能自行輸入敏感內容** |
| **收到之後存哪** | 同 **T-4**（不在 repo 目錄、不被 git 追蹤） |

### 6.5 ⇒ 三件事的建議總結（附推薦，依 `CLAUDE.md §-1.5.D §03-2` 請示規範）

| 項目 | **總管推薦** | 具體原因 |
|---|---|---|
| **回饋按鈕** | ⭐ **做** | 成本最低、隱私風險可控（F-1~F-3）、能直接回答「使用者卡在哪」 |
| **A/B 測試** | ⛔ **不做線上，改做 §5.2 離線兩版對照** | n=1 沒有統計意義；線上分桶必然要持久化識別碼，直接撞上 T-3 |
| **行為追蹤** | ⚠️ **本組不推薦現在做，但也不反對** —— **請客戶決定** | 它是三者中**唯一**會新增一份持續累積的資料、且⛔ 沒有既有需求觸發（`CLAUDE.md §-1`：沒實際 bug／沒具體需求 → 不要動）。⭐ **若客戶確實要做，T-1~T-8 是前置條件，不是建議事項** |

---

## 7. ★ 待客戶拍板清單（產品層）

> **格式沿用** `S1-3B` Part 7：問題一句話／選項／**總管推薦 ＋ 具體原因**／不決定的後果。
> 依 `CLAUDE.md §-1.5.D §03-2`：**必須附推薦，⛔ 不得只丟選項。**

### 7.1 本份新增的 ★（★-P 系列）

| ★ | 問題 | 選項 | **總管推薦 ＋ 具體原因** | 不決定的後果 |
|---|---|---|---|---|
| **★-P1** | 成功指標採本份 §4 這三類嗎？ | (a) 採用　(b) 改 | ⭐ **(a)**。三條：① 既有材料**一份都沒寫過**成功指標（本組掃描 0 命中），不定就是繼續沒有；② 這個產品沒有營收與留存壓力，通用 KPI 不適用；③ **A 類（誠實度）目標值必須是 0**，那是憲法 §1 的直接落地，⛔ 不宜當成「越高越好」的軟指標 | 交付時沒有任何客觀驗收依據，「做完了」由實作組自己說了算 |
| **★-P2** | 效能目標值採「Skeleton < 0.3s、主內容 < 2.5s」嗎？ | (a) 採用　(b) 改數字　(c) 先量了再定 | ⭐ **(c) 先量了再定**。三條：① 這組數字**不在任何一份既有交付物裡**（本組掃描 0 命中），來源是客戶口述，**尚未有 repo 依據**；② repo 內既有的唯一同類數字是**總經頁冷啟動 40~75 秒**（`tab_macro.py:167,405`）—— **與 2.5s 差兩個數量級**，直接套用會讓每一次交付都是紅字；③ `S2-ENG_SPEC §5` 已經定好**怎麼量**，量一次就有依據了。⚠️ **(a) 不是錯，但在還沒量過的情況下它是一個沒有依據的整數** | 門檻值無法驗收；或訂在一個必然達不到的位置，於是所有人開始忽略它 |
| **★-P3** | 要不要做使用行為追蹤？ | (a) 做（須先滿足 T-1~T-8）　(b) 不做　(c) 只做回饋按鈕 | ⭐ **(c) 只做回饋按鈕**。三條：① 回饋按鈕**成本最低、隱私可控、資訊量最高**；② 行為追蹤在 n=1 下能回答的問題，回饋按鈕大多也能回答，而且是使用者主動說的、更準；③ ⭐ 本 repo **已經有 PII 被 commit 進 git 的先例**（`data_cache/forward_test/picks.parquet`、`health_watchlist.json`，`S1-7 §2`）—— 新增一份持續累積的資料，**風險不是理論上的** | 三件事被當成同一件事一起做或一起不做；追蹤在沒有 T-1~T-8 的情況下上線 |
| **★-P4** | 使用者測試的受測者從哪裡來？ | (a) 招募外部人士　(b) 客戶本人的親友　(c) 不做 | ⭐ **(a)**，並**誠實標明「受測者不是目標使用者本人」這個無法消除的差距**（§5.2 樣本數欄）。⛔ **(c) 不可行** —— ★-10（頁1 主數值）與客戶拍板第 1 條（三個灰要分得開）**兩條規格自己都承認有風險而沒有證據**，不測就是把那兩個風險直接帶進實作 | ★-10 甲乙之爭沒有證據、只能靠喜好決定；三灰區分是否成立永遠不會有人知道 |

### 7.2 從 `S1-3B` Part 7 繼承、屬**產品層**的 ★（⛔ 本份不重抄全文）

> 完整 25 條在 `S1-3B` Part 7，含逐條推薦與理由。下表只列**本份認定屬產品決策**的幾條，
> 以及它們在拆分後歸哪一份。**⛔ 本份不改變任何一條的推薦內容。**

| ★ | 一句話 | 拆分後歸屬 |
|---|---|---|
| **★-10** | 頁1 主數值換成什麼（候選甲／乙／維持） | **產品層（本份）** —— 它改變的是「這一頁對使用者承諾什麼」。⭐ **本份 §5.2 的 5 秒測試就是為它設計的驗收** |
| **★-14d** | 合規「全站」射程含不含 LINE 推播 | **產品層（本份 §3.4）** —— `S1-3B` 明文「本組不裁決，只有客戶知道」，**本份同樣不裁決** |
| **★-13c** | 頁1 要不要加 CSV 下載鈕 | **產品層** —— `S1-3B` 推薦 **(b) 本批不加**，理由「這是新需求，不是漏做」。本份沿用，不改 |
| ~~**★-12b**~~ | ~~基金站 4 個 `[新]` 預覽分頁怎麼辦~~ | 🚫 **擱置** —— **因客戶 2026-09-14 明示「移除基金」而擱置**（日期 2026-09-14；決策者：**客戶**；**有意識的政策變更，不是漏刪**）。<br>**舊條的理由（仍然成立）**：那 4 個預覽分頁在基金站確實存在，`S1-3B` 推薦「(c) 一個都不動」的理由是「本批不碰非本次範圍的東西」—— **那個理由沒有被推翻**。<br>**被權衡掉的原因**：基金儀表板已移出產品範圍 ⇒ **它的分頁怎麼辦，現在不是本產品的待拍板事項**，留著會讓 ★ 清單看起來還有一項卡在客戶身上。<br>⭐ **恢復條件**：客戶明示把基金儀表板重新納入範圍 → 本列即刻恢復為待拍板（見 §2.3.2）。**本條的恢復條件另加**：恢復時須先重新查證「那 4 個 `[新]` 分頁是否還存在於姊妹 repo」—— ⚠️ 擱置期間姊妹 repo 可能已自行增減 |
| **★-06b** | ⚠️ 整個響應式層從未經客戶拍板 | **UI 層** → `S2-UI_SPEC §6` |
| **★-01 / ★-02 / ★-03 / ★-05 / ★-07 / ★-09** | 葉與四層、展開顆粒度、缺值符號放哪、時點列要不要判新鮮度、兩條頁首、頁4 CTA 位置 | **UI 層** → `S2-UI_SPEC` |
| **★-04 / ★-04b** | 頁級載入中版面、`target_pct` 存哪裡 | **工程層** → `S2-ENG_SPEC` |

---

## 8. 範圍聲明與未複驗事項（`CLAUDE.md §-2` 規則 6）

### 8.1 本份**沒有**做的事

- ⛔ 沒有修改 repo 任何檔案、沒有動任何既有 `wf_*.js` / `warroom_ia_v2.html`、沒有執行任何 git 寫入。
- ⛔ **沒有跑過任何一個畫面**（零渲染、零截圖、零實機量測）。§5 的測試計畫是**設計**，**一次都沒跑過**。
- ⛔ 沒有實際招募任何受測者、沒有任何測試結果。
- ⛔ 沒有寫任何前端代碼、沒有畫任何高保真視覺稿。
- ⛔ **沒有法遵檢視**：§6 的隱私評估是**本組依 `S1-7` 的實測盤點推導**，⛔ **不是法律意見**。
- ⛔ 沒有複驗 `S1-1`～`S1-7`、三份紅隊報告、`COMPLIANCE_SCAN_*` 的內容正確性 —— **搬遷時一律沿用其原始標註**。

### 8.2 ⚠️ 全稱句自標（只能當待驗事項）

| # | 全稱句 | 為什麼只能當待驗 |
|---|---|---|
| 1 | 「**既有材料沒有一份寫過產品成功指標**」（§4） | 單組關鍵字掃描（`成功指標`／`KPI`／`驗收標準`），**未窮舉同義詞**（`metric`／`目標值`／`達成`／`驗收`） |
| 2 | 「**Skeleton < 0.3s / 2.5s 不在任何既有交付物裡**」（§4.2） | 單組掃 scratchpad `S1-*.md`／`UI-*.md`／`wf_*.js`／`warroom_ia_v2*.html` ＋ repo `docs/`，**未掃**同義詞（`首屏`／`TTI`／`毫秒`），**未掃** repo 全部 `.py` 註解 |
| 3 | 「**本站只有一個真實使用者**」（§1.2） | 由 `S1-7 §2` 的 PII 結構推得（持股存在「客戶自己的」Sheet），**不是**對部署現況的直接查證。若實際有多人使用，§6.3 的 A/B 結論會翻掉 |
| 4 🆕 | 「**三份 `S2-*` 文件裡所有提到基金站的地方都已處理**」（§2.3） | ⚠️ **本組單組掃描，未經第二組複驗。** 掃描關鍵字（三份全檔）：`基金`／`雙儀表板`／`儀表板`／`fund`／`Fund`／`FUND`／`NAV`／`配息`／`折溢價`／`收益視角`／`同類中位數`／`覆蓋率`／`兩站`／`跨站`／`my-fund`／`dashboard`／`ACCEPTANCE`。<br>**第二輪同義詞掃描（本輪補做，三份全檔）**：`淨值`／`申購`／`贖回`／`共同基金`／`基金型`／`收益站`／`配置站`／`投信`／`境外`／`月配`／`季配`／`年化配息`／`收益視角` ⇒ **除本份 §2.3 引用客戶原話那一行外，0 命中**。<br>⛔ **仍未掃**：英文代稱（`mutual`／`OEIC`／`SICAV`／`unit trust`）、以代號或縮寫指稱基金站的寫法、以及**三份以外的任何檔案**（`S1-*`、`UI-*`、`wf_*.js`、`warroom_ia_v2.html`、repo 全部 **未動也未掃**）。<br>⚠️ ⭐ **一個結構性盲點**：本輪掃的是「**提到基金的地方**」；若某條規格**當初是為了基金站而寫、但行文裡一個基金字樣都沒有**，這種寫法**本輪結構上掃不到**（同 `CLAUDE.md §8` 前言記載的「用『要問客戶』的關鍵字永遠掃不到豁免句」那一類方法論失效）|

### 8.3 🧠 承重判斷（後續會有人拿來當前提，**請優先覆核**）

| # | 判斷 | 承重在哪 |
|---|---|---|
| **P-J1** | **「資料治理」拆成呈現面／契約面兩半**（§0.1） | 三份文件的邊界；拆錯會讓某一類內容無家可歸 |
| **P-J2** | **5 秒／10 秒測試該測的是 ★-10 與「三個灰」**（§5.1） | §5 全節的腳本與判準都建立在它上面 |
| **P-J3** | **線上 A/B 在本專案不可行**（§6.3） | ★-P3 的推薦；若客戶認為使用者不只一人，這條要重做 |
| **P-J4** | **行為追蹤的風險來自「repo 已有 PII 進 git 的先例」**（§6.1） | T-4 與 ★-P3 的推薦強度 |

### 8.4 繼承自上游材料、本份同樣未複驗的

`S1-3B` §8.3.1～8.3.4 全部（含「一個畫面都沒跑過」「沒讀完任何一支 View」「基金站只讀了 2 個檔」
「響應式 197px／274px 是手算不是實機量」），以及其 §8.3.2 的六條全稱句、§8.3.3 的五條承重判斷 J-1~J-5。
⭐ **搬過來之後仍是未複驗，⛔ 不因為被引用第二次而升格。**

> 📌 **2026-09-14 基金擱置註（⛔ 刻意不在上面那段轉錄上畫刪除線，理由見 §2.3.1 第 5 點）**
> 上段是**逐字轉錄 `S1-3B` 對自己的自陳**，在轉錄本體上動刀 ＝ 竄改轉錄。⇒ 改在此加註：
> - **「基金站只讀了 2 個檔」這條未複驗風險，自 2026-09-14 起⛔ 不再影響本產品的交付**
>   —— 因為基金站已移出範圍，**那 2 個檔所支撐的結論本輪已一併擱置**。
> - ⚠️ **但這句轉錄本身仍然為真、仍然保留**：它記錄的是「`S1-3B` 當時只讀了 2 個檔」這個**事實**，
>   而該事實是後人判斷 `S1-3B` 其餘結論可信度的依據之一。**刪掉它會刪掉一條稽核線索。**
> - ⭐ **恢復條件**：基金儀表板若重新納入範圍，**這條未複驗風險即刻恢復為現役風險**，
>   且依 §2.3.2 須**重新查證**（當時只讀 2 個檔，恢復時要讀幾個檔是新的問題）。
> - ✅ **「響應式 197px／274px 是手算不是實機量」⛔ 不擱置**（N-1：主詞是台股站版面）——
>   且它正是客戶本輪第二點（308px token 化 ＋ CI 檢查）要處理的同一族問題 → `S2-ENG_SPEC §8.4`。

---

## 附錄｜本份逐節出處對照

| 本份節 | 搬自／新寫 | 出處 |
|---|---|---|
| §0.1 三份怎麼拆 | **新寫** | 客戶 2026-09-14 審稿結論逐字 ＋ 🧠 本組對第四類的分配判斷 |
| §0.3 證據等級 | 沿用 | `S1-3B` Part 0.2（其自陳沿用 `S1-4 §0`） |
| §0.4 內部文件宣告 | **新寫** | 客戶明示「偏離表與已知限制不應出現在使用者介面」 |
| §1.1 定位 | 沿用改寫為一句話 | `S1-3B` Part 3 各頁 `x.0` 職責欄 ＋ Part 5 母法紅線 |
| §1.2 使用者 | **新寫**（由實測推導） | `S1-7 §2`／`§2.2`／`§2.3` |
| §1.3 五頁五任務 | **沿用，一字未改** | `S1-3B` Part 2.11「五頁的線性關係」 |
| §2.1 三條拍板 | **沿用，逐字** | `S1-3B` Part 1.1 |
| §2.2 既有拍板 | **沿用，一字未改** | `S1-3B` Part 1.2 |
| §3.1 主流程 | 沿用 | `S1-3B` Part 2.11 |
| §3.2 「按了才算」是產品承諾 | **新寫**（定位陳述） | 承諾原文引自 `S1-3B` Part 2.14.1 |
| §3.3 產品紅線 | **沿用，逐字** | `S1-3B` Part 2.12.2 ＋ Part 5.1／5.4 |
| §3.4 合規射程 | 沿用 | `S1-3B` Part 2.12.1 ＋ ★-14d |
| **§4 成功指標** | 🆕 **新寫** | 依既有拍板推導；A 類取自 `S1-3B` Part 2.15 反例快查 ＋ 每頁 `x.8` 指路句表 |
| **§5 使用者測試** | 🆕 **全節新寫**（客戶點名缺漏 3） | 測試標的取自 `S1-3B` Part 3.1（★-10 代價）＋ Part 2.5.2（三個灰）＋ `shared/ui_state.py:112` |
| **§6 回饋／A-B／行為追蹤** | 🆕 **全節新寫**（客戶點名缺漏 3 的附帶項） | 隱私評估**全部建立在** `S1-7_PERMISSIONS_PII.md §2`／`§2.2`／`§2.3`／`§2.4`／`§6` 的實測盤點 |
| §7.2 繼承的 ★ | 沿用，**不改內容** | `S1-3B` Part 7 |
| §8 未複驗 | 新寫 ＋ 繼承 | `S1-3B` §8.3 全部繼承 |
| **§2.3 移除基金儀表板** | 🆕 **本輪（2026-09-14 第二輪審稿）新寫** | 客戶 2026-09-14 第二輪審稿逐字 ＋ 處置慣例沿用 `CLAUDE.md §-2`／`§-1.5.E` |

---

## 修訂紀錄

| 版 | 日期 | 改了什麼 | 依據 | 執行 |
|---|---|---|---|---|
| **初版** | 2026-09-14 | 三份拆分文件之一，首次產出 | 客戶 2026-09-14 第一輪審稿「建議拆成三份」 | 文件分層組（單組） |
| **r2** | **2026-09-14** | ⭐ **基金移除同步（客戶第二輪審稿第一點）**：<br>① 新增 **§2.3**（移除基金儀表板的處置母條：處置方式／恢復條件／⛔ 三條不得連坐判準／三條誠實揭露）；<br>② §2.2「雙軌並行」出處加 **📌 R-1**（**拍板不擱置**，只把基金站出處降級為跨 repo 先例）；<br>③ §7.2 **★-12b** 加刪除線 ＋ 擱置註 ＋ 恢復條件；<br>④ §8.4 繼承轉錄加**基金擱置註**（⛔ 不在轉錄上畫刪除線）；<br>⑤ §8.2 全稱句表新增第 4 列（本輪掃描的射程與未掃關鍵字）；<br>⑥ 本修訂紀錄 | 客戶 2026-09-14 第二輪審稿逐字；處置慣例 `CLAUDE.md §-2` 規則 6／`§-1.5.E` 處置原則 | 文件同步組（單組，**未複驗**） |

> ⚠️ **r2 的範圍聲明**：本輪**只動 `S2-PRD.md`／`S2-UI_SPEC.md`／`S2-ENG_SPEC.md` 三個檔**。
> ⛔ **未動** repo 任何檔案（含 `CLAUDE.md`／`PROCESS.md`／`docs/`）、⛔ 未動任何 `wf_*.js`／`warroom_ia_v2.html`、⛔ 未執行任何 git 寫入。
> ⚠️ **r2 的所有判斷（哪些擱置、哪些不連坐）由本組單組產出，未經第二組複驗** —— 依 `CLAUDE.md §-2` 規則 6，**只能當待驗事項**。
