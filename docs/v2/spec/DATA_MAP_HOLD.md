# 我的持股頁資料對照表（IA v2 戰情室 · block 級）

> **這是什麼**：💼「我的持股」頁每一個 block「畫面上那個東西從哪來、怎麼算、沒資料時怎麼辦」的對照表。
> **射程**：本頁 **23** 個 block（順序照 `docs/v2/prototype/gen_today_v2.py::HOLD_LAYOUT`）。彙整日 **2026-09-23**・branch `ui-v2`（素材量測日 **2026-09-22**）。**索引與共通讀表前提**＝`docs/v2/spec/DATA_MAP_ALL_PAGES.md`；**方法論 ①（動態 card key 之下 `grep`＝0 ⛔ 不等於未實作）與 ②（`#7`／`#8`／`#9` 實作端零落地）見 `docs/v2/spec/DATA_MAP_GAPS.md` 檔頭，⛔ 本檔不複製第二份。**
> **來歷前綴**：〔GA〕＝**程式碼端**（AST ＋ 逐層讀 code）・〔GB〕＝**規格端**（讀規格與線框，⛔ 未回 code 驗證）・**無標＝兩組共同**。⚠️ **兩組的待確認都用 ⓐ~ 圓圈字母且撞號** ⇒ 引用時**必須連組別一起寫**（`GA ⓘ` ≠ `GB ⓘ`）。**兩組說法相左處一律並陳 ＋ 標明組別，⛔ 不自行統一、⛔ 不代為裁決。**
> 📌 **本頁的規格洞與 ⚠️ 待確認壓在本檔末尾 §2 一行帶過** —— 客戶本輪**未授權動 `DATA_MAP_GAPS.md`**，故 ⛔ 未進 GAPS、⛔ 不另立第二份。
> ⚠️ **本檔是現況記錄，⛔ 不構成主動動工的授權**（`CLAUDE.md §-1`）。⛔ 全檔不寫行號（`§8.2.A.0` 規則 1）；程式碼引用一律**全路徑** `檔名::符號`、規格一律 `檔名 §章節名`；TTL 一律寫 `shared/ttls.py` 常數名；查不到一律「⚠️ 待確認」，⛔ 不猜、⛔ 不發明資料源／算式／中文標籤。

---

### 0. 讀表前提（⛔ 少了這段，下表每一列都會被讀反）

- **`[A:無此頁]`（全 23 塊）**：`ls src/ui_v2/*.py` **無 `page_hold`**（總管前輪實測，GA 沿用、⛔ 未重查）⇒ 下表「資料來源」一律填 **B＝現行** `src/ui/views/page_hold.py`（掛載 `app.py::_ia_view_hold` → `::render_page_hold`；側欄 radio、late import，該輪以 `st.stop()` 結束）。🔴 **`chrome.*` 三塊⛔ 不在 `HOLD_LAYOUT` 內**（本頁 23 塊無任何 `chrome.` 前綴），但 **`app.py::_render_footer` 一樣會畫**（五頁共用，`st.stop()` **前**呼叫，註解逐字「免責聲明**照樣出**(法遵文字,不分頁面)」）⇒ **差異純在 layout 定義**（`INSPECT_LAYOUT` 列 3 塊 chrome、`HOLD_LAYOUT` 列 0 塊；洞 `GA-I-7`）。〔GB ⓛ〕本頁線框亦無 `chrome.footer`／`hold.footer`，而 `S2-UI_SPEC §9.3` 的 `D-1` 要求「**每一頁**」⇒ ⚠️ 待確認。
- 🔴 **本頁多一條前三頁沒有的鐵律（實作檔頭逐字）：「本頁一律唯讀。不寫入、不刪除、不改動任何一列持股。」** 四道落實：① 只 import 讀取型 L3；② 需寫入才做得到的一律標 `unwired`；③ **⛔ 一個字元都不印 Sheet 識別碼**；④ `tests/test_p04_hold_view.py::TestReadOnly` 以 **AST 白名單／黑名單**釘死（`save_portfolio`／`delete_portfolio`／`save_stock_watchlist`／`add_to_stock_watchlist`／`create_new_sheet`／`rename_sheet` 等整個寫入面一個都不准出現 ⇒ **⛔ 不是自律，是 CI 紅燈**）。⚠️ **一個誠實的例外（檔頭自陳）**：`src/services/allocation_service.py::get_allocation` 會寫 `st.session_state` 兩個**記憶化快取鍵**，那是 session 內計算快取、**⛔ 不碰 Google Sheets**，且寫入發生在 L3 內部。
- 🔴 **三條硬禁令（逐字；`S2-PRD.md`＋`S2-UI_SPEC §9.2`，標題逐字「不得以任何理由放寬」，`UI_PAGE_HOLD` 前言逐字「**凌駕本檔其餘版面規定**」）**：① ⛔ 禁止「一鍵再平衡」按鈕（任何會產生下單清單、或一次套用全部調整的按鈕都不得存在）／② ⛔ 禁止預設最佳配置推薦 ——「**目標比例只能由使用者自己填，系統⛔ 不得預填、⛔ 不得建議、⛔ 不得給「參考配置」**」／③ ✅ 只做偏離提示／客觀對照／情境試算。**落地三則**：(a) **輸入欄一律空白**，示範只用**不可計算的佔位符** `〔＿＿＿＿〕`、⛔ 不放具名標的；(b) 情境試算兩欄名**刻意選為**「金額變化(TWD)」與「換算（參考）」，⛔ **不得叫「動作」「建議股數」**；(c) ⑦ AI 總結⛔ 不得把上述禁令繞過去輸出行動語。⇒ `§9.2` 另加一句「**目標未填完之前，偏離欄一律 `▨ 未設定`，⛔ 不得顯示 `0.00`**」。
- 🔴 **三個「零」⛔ 不得共用同一寫法**（`UI_PAGE_HOLD ③`）：**A 尚未設定目標**（`missing`）＝`▨ 未設定`（沒有值可以算）／**B 有效的零**（`live`）＝`0.00`（真的剛好等於目標）／**C 目標總和 ≠ 100%**（`degraded`）＝`▨ 不計算` ＋ 合計格 `⚠︎ 差 8.00`。⚠️ **B 的 `0.00` 是三條線裡唯一的真數字，⛔ 不得被「缺值不得填 0」那條誤殺**（那條管 A 與 C）。⚠️ **`▨` 處置（客戶 2026-09-16 拍板逐字）**：「符號 ▨：照你處置。#7/#8 語意改用 ⚠︎ — 與 N/A；**A/C 兩態屬第三類，不發明第 11 種徽章**。」✅ **D-3 常駐警語八處落點「全列，全做…不是只做第 #6 處」**（客戶同日裁示逐字）：完整版逐字 `⚠️ 你在這裡填的目標比例目前不會被保存`／短版（手機）逐字 `⚠️ 目標比例目前不會被保存（重新整理就要重填）`；**編號＝`S2-UI_SPEC §9.3` 的 `D-3`**（⛔ **不是** `INDICATOR_SPEC` 的 `D-03`）。⚠️ `S1-3B §5.6` **自陳未窮舉** production 所有 `target_pct` 畫面落點 ⇒ ⛔ 不得把八處當窮舉。
- 🔴 **block key ≠ 實作 key（本頁最容易被讀錯的一條；GA AST 實測，⛔ 非 grep）** —— 這就是方法論 ① 在本頁的實例：`hold.scale_disclosure`→**`hold.scales`**（同一塊、兩個名字）／`hold.rebalance_deviation`→**`hold.deep.rebalance`**（且住在 `hold.deep_analysis` 容器底下）／`hold.scenario_calc`→**`hold.deep.stress` ＋ `hold.deep.var`**／`hold.conclusion_cards`→`hold.conclusion.action`·`.confidence`·`.todo`／`hold.alloc_deviation`→`hold.alloc_split`·`hold.take_profit`·`hold.position_cap`／`hold.war_table`→`hold.lightwall`·`hold.vix`／`hold.swap_compare`→`hold.switch`·`hold.macro_stage`／`hold.deep_analysis`→`hold.deep.*`（6 張）／`hold.setup`＝`section_header` **容器、⛔ 沒有同名卡**。
- 🔴 **`#7`／`#8`／`#9` 在實作端零落地 ⇒ 下表凡寫這三號者都是「規格要求」、⛔ 不是「畫面現況」**（方法論 ②）：L0 `shared/ui_state.py::UI_STATE_META[UI_EMPTY]` 的 glyph ＝ **`▨`**、文字「無資料」，**單一態、未依 `miss_reason` 拆 #7／#8**；`⚠︎`／`◧` 在本頁與 `shared/ui_state.py` **皆 0 命中**〔GA AST〕。`reason=` **有**被傳進 `classify_ui_state()`（例：`hold.scales` 傳 `MISS_NO_INPUT`、`hold.portfolio_count` 傳 `binding.count_missing_reason`），但**只影響是否轉紅**（`FAILED_REASONS = {MISS_FETCH_FAILED, MISS_CONTRACT_DRIFT}`），**⛔ 不影響 glyph**。**會出現在畫面上的是 6 態：#1／#3／#4／#5／#6／#7**（`UI_PAGE_HOLD ②`；#2 未落地、#8／#9 待接線、**#10 本頁不畫且⛔ 不得併進 #8**）。⚠️ **兩套 `D-*` 同名不同義、⛔ 不得補零統一**：`S2-UI_SPEC §9.3` 的 `D-1`~`D-6`（＝**揭露落點**）⛔ **不是** `INDICATOR_SPEC §5`／`DECISION_RULES §8` 的 `D-01`~`D-35`（＝**應然差異**）。
- **TTL／fallback 兩欄由程式碼端補**：`UI_PAGE_HOLD.md` 與 `docs/v2/wireframe/wf_page_hold.js` 的 `TTL`／`fallback`／`備援`／`頻率` **全部 0 命中**〔GB 實測；`S2-ENG_SPEC §3.3` 逐字「**「備援鏈跑到第幾層」這個資訊在 repo 裡不存在**」〕，**唯一有規格 TTL 的來源是 `portfolios` → `TTL_15MIN`**（`S1-1 §1.3`）⇒ 其餘一律寫 **GA AST 掃 `@st.cache_data` 裝飾器**得到的 `shared/ttls.py` 常數名、**⛔ 不寫秒數**；兩端都追不到者才寫「⚠️ 待確認」（跨兩頁共用的 fallback 缺口＝`GB ⓑ`）。⚠️ **VIX 是 TTL 例外**：走 `src/data/macro/macro_core.py::_YF_CLOSE_TTL`（module-level memo），**⛔ 非 `shared/ttls.py` 常數**。**三種狀態⛔ 不可混為一談**〔GA，三法交集：字面 grep＝0 **且** AST 動態 key 無涵蓋 **且** `section_header` 全清單（10 處）無對應段〕：**✅ 有實作 14 塊**／**🟡 已實作但 `wired=False` 灰卡 3 塊**／**🔴 真正未接線、畫面完全不存在 6 塊**（14＋3＋6＝23，與 `HOLD_LAYOUT` 筆數對帳相符）。⚠️ **「6 塊」是 GA 單組窮舉的全稱句。**

### 1. 二十三個 block（順序照 `HOLD_LAYOUT`）

| block key | 畫面顯示什麼 | 資料來源 | 計算 | 缺值處理 |
|---|---|---|---|---|
| `hold.run_scope` `[A:無此頁]` | 🟡 **有實作，但位置與規格不同** —— 「這一輪要跑到哪裡」`st.radio` 兩選項 ＋ 主 CTA「🚀 跑存股戰情室」。〔GB 線框 live 逐字〕「◉ 我的持股（連 Google 讀你的持股清單）　○ 只看市場（不連 Google）」＋「🔒 **這一頁只讀不寫** —— 不會新增／修改／刪除你 Sheet 裡的任何一列，**畫面上也不會出現你的 Sheet 位址**」 | `page_hold.py::_render_holdings_form` → **共用層 `src/ui/views/_ui_kit.py::single_submit_form`**（本頁**直接用共用層**，⛔ 未像頁2／頁3 就地實作第四份 —— 本頁表單輸入剛好只有一組 radio；逐字「**能用共用層就不要多開一把尺**」）〔GA〕。主 CTA 名稱 SSOT ＝ `page_hold.py::ACTION_RUN_WARROOM_LABEL`＝**🚀 跑存股戰情室**（`S2-UI_SPEC §11.3` C-02 定案：主 CTA 名稱一律取自 production code 常數）〔GB〕。widget key `SS_SCOPE_WIDGET`；**已套用值 `SS_APPLIED_HOLD` ＝ 全檔唯一寫入點**。選項：`SCOPE_MARKET`「只讀市場端（不連 Google，也不讀你的持股）」／`SCOPE_WITH_BINDING`「加讀 Google Sheet：綁定狀態 ＋ 你的持股清單（唯讀，不寫入）」。**表單零取數** ⇒ **TTL 無快取／fallback 不適用** | **無計算**（widget 當下值 → 已套用值的搬運） | `empty`／`missing`／`na`／`partial`／`degraded`／`unwired`＝**null**〔GB〕。⚠️ **位置差異（兩組同測、判定留白）**：`HOLD_LAYOUT` 放 **n0、`leaf: None`（葉外頁級常駐）**，實作放在 **葉2 `::_render_setup_form_block`** 內（`with _leaf2:`）⇒ Streamlit 預設開**葉1** ⇒「**主 CTA 在預設葉可見」現況不成立**（〔GB G1〕線框標 `★-09 待客戶拍板`；〔`GA ⓘ`〕只能證明兩者不一致，**判哪一邊該改屬規格端，⛔ 不代為裁決**）。〔GB G1 另測〕`single_submit_form` **無 `index=`** ⇒ 預設 index 0 ＝ `SCOPE_MARKET`「只讀市場端」，**與線框相反**。⭐ **執行順序 ≠ 顯示順序，而且是必要的**：`::render_page_hold` 先進 `_leaf2` 跑表單（submit handler 寫 gate）再畫 `_leaf1` —— 否則 submit 那一次 rerun 葉1 先跑會讀不到 key，使用者按了鈕戰情室仍一片「尚未執行」、**且⛔ 不會再自動 rerun 救回來**（守衛 `tests/test_p04_hold_view.py::TestFormRunsBeforeItsConsumers`）。⭐ **這個 radio ⛔ 不是裝飾**：選「只讀市場端」時本頁**真的不發那一次網路呼叫**。〔GB G7〕**`loading` 停用態做不到** —— `single_submit_form` **無 `disabled` 參數** ⇒ `UI_COMPONENTS §3` 停用態在補上該參數前**是規格、⛔ 不是現況**。⚠️ 版面位置異動 ＋ 改預設值落在 `CLAUDE.md §-1.5.D §03-2 ①`：**須客戶拍板**。可重試：✅。**編號 ⚠️ 待確認**（`GB F-2` 對不上） |
| `hold.scope_note` `[A:無此頁]` | 🟡 **有實作（部分）** —— 表單下方**兩段常駐 `st.caption`**：接線揭露 ＋ 唯讀宣告。〔GB 線框〕`live`＝「✅ 已讀取你的持股　12 檔　09/13 08:41　唯讀」（⚠️ **示意值、⛔ 非實測**）；`missing`＝「⬜ **這一輪沒有讀你的持股** —— 你選的是「只看市場」，所以這一頁**真的沒有去連 Google**（不是連了失敗，也不是拿上一次的舊資料頂替）…⚠️ **這不代表你沒有持股。**」＋〔改成讀我的持股，重跑一次〕**一鍵修好** | `page_hold.py::WIRING_DISCLOSURE` ＋ `::READONLY_DISCLOSURE`，由 `::_render_setup_form_block` 渲染；另有 `::GOOGLE_NOT_ASKED_NOW／WHY／WHERE`，但那是**各張卡 `idle` 時的 `Note`**、⛔ 不是獨立的頁級 note。**本塊零取數**〔GA〕。〔GB〕其所揭露的持股來源＝Google Sheets worksheet **`portfolios`**（`src/data/portfolio/gsheet_portfolio.py::_HEADERS`＝`name／ticker／lots／avg_price／updated_at` **五欄**），讀取走 OAuth／Service Account 雙路（`src/data/portfolio/oauth_state.py`，`CLAUDE.md §8.2.A.1 EX-OAUTH-1`）；**頻率**＝事件驅動（user 編輯）；**TTL `TTL_15MIN`**（`S1-1 §1.3` 逐字「讀取快取」）；**fallback ⚠️ 待確認**。**本塊自身 TTL 無快取／fallback 不適用** | **無計算**（靜態字串）〔GA〕。〔GB〕其所報的檔數＝計數；「N／M 檔」＝有報價檔數 ÷ 總檔數。⚠️ **讀取後正規化** `gsheet_portfolio.py::parse_portfolio_records` → `{ticker, lots, avg_price}`，且 **`lots<=0` 或 `avg_price<=0` 直接丟棄** ⇒ 髒列會**靜默消失**（`S1-5 §4.1 M6`） | **無缺值態**（常駐，不隨狀態消失）〔GA〕。〔GB 十格〕`na`＝**null**；`partial`→**#9 ⚠️ 規格要求；實作畫 `▨`**；`empty`＝「▨ 已讀完，你的帳本裡沒有任何持股列（**這是有效結果**，不是錯誤）」→**#7 ⚠️ 規格要求**；`missing`→**#3 ⬜**（**非缺漏**）＋一鍵修好。⚠️ **fail-safe：N／M 分子分母拿不到 → 一律降 #7，⛔ 不得退回 #1**（`UI_COMPONENTS §2`；實作端是否落地 ＝ `GA ⓖ`）。⚠️ `error` 文案 ⛔ **不得含模組路徑**（`S1-3B §3.4.6`），寫「讀『Google Sheet 綁定狀態』時失敗：〈原始訊息〉」，程式路徑寫 log；**原始錯誤訊息本身要留**（`CLAUDE.md §1`）。⚠️ **位置差異同 `hold.run_scope`**（`GA ⓘ`）。⭐ **揭露內容逐字點名仍未接線的三項**：⑥ 再平衡（**帳本沒有「目標比例」欄**）／⑥ 葡萄串領息（實作在 L5）／葉2 Sheet 選擇與觀察清單管理（**那兩項是寫入，本頁唯讀**），並逐字寫「**配息現金流只算到扣二代健保為止，不含綜所稅**」；`::UNWIRED_WHERE_PREFIX` 要求每張未接線卡的 `where` **必須各接自己那一段**（共用一句會讓「補哪一層才會好」這個資訊消失）。可重試：✅。**編號 ⚠️ 待確認** |
| `hold.scale_disclosure` `[A:無此頁]` | ✅ **有實作** —— ② 「同一個名詞，兩套刻度」卡（**實作 key `hold.scales`**）＋ 其下**兩張 `st.dataframe` 逐盞門檻對照**（這一盞／門檻／出處／判別力）。live 值＝`::SCALE_LIVE_VALUE` 逐字「**兩套刻度並存 —— 不可互比**」；〔GB 線框逐字〕「本頁的『健檢』：**四燈體檢（現金／負債／毛利／盈餘品質）四分法**」vs「查一檔的『健康度』：**六因子加權 0–100 分**」，其餘九格＝「**常駐**」 | `page_hold.py::build_scale_card` ＋ `::_render_scale_tables` ← `::build_scale_disclosure` → **L0 `shared/station_specs.py::STATION_SPECS`／`::SPECS_BY_KEY`**（**純常數、零 I/O、零網路**）〔GA〕。兩套 key：本頁 `HOLD_SCALE_SPEC_KEYS`＝`KEY_HEALTH_A/B/C/D` ＋ `KEY_STOCK_SWAP`；查一檔 `INSPECT_SCALE_SPEC_KEYS`＝`KEY_STOCK_HEALTH`；另讀 `HOLD_SCALE_INPUT_SPEC_KEYS`＝`KEY_STOCK_HEALTH／TREND／KD`（**輸入**）。⭐ **本塊是 📖 憑什麼頁 `why.edu.scales` 指過來的來源端**（`DATA_MAP_WHY` 記「本頁這一段必須比頁4 完整」）〔GB〕。**TTL 無快取（L0 常數）／fallback 不適用** | **無取數計算**；只讀 `StationSpec` 的 `label`／`threshold_text`／`source`／`discriminative`。〔GB〕`degraded ＝ ∃r: r.discriminative=False`（**算出來、⛔ 非寫死**；旗標翻回 `True` 會自己回 live） | ⭐ **`requested=True` 傳的是字面 `True`，⛔ 不是恆真式** —— 檔頭逐字「揭露常駐，不隨載入狀態消失」，輸入是 L0 `frozen` 常數表、**沒有任何取數**；「恆真式的問題⛔ 不在於它恆真，而在於它**假裝自己在判斷**」。⭐ **本頁三處 `UI_DEGRADED` 之一，而且是真的**：L0 實測 `KEY_STOCK_TREND`（財報趨勢）標 `discriminative=False`、`degraded_reason` 逐字「這格只比較**最近兩季**，看不出趨勢」，而那盞燈正是本頁個股「健檢」欄（`swap_level`）的**輸入之一** ⇒ **#4 `🟠`**；**可否重試：❌**（門檻性質，重按無效）。`empty` 傳 `reason=MISS_NO_INPUT`→**#7 ⚠️ 規格要求；實作畫 `▨`**。⚠️ `::_render_scale_tables` **⛔ 不判態、⛔ 不取數**（多判一次就有兩個可能矛盾的說法）。〔GB〕**十格 0 null（本頁唯一）**；核准線框自陳「**v1 漏畫**。它是防『同名不同義』誤讀的唯一揭露 —— 拿掉之後就會有人拿兩套刻度互比，**而畫面不會攔他**」。**編號＝`S2-UI_SPEC §9.3` 的 `D-6`**（⚠️ ⛔ **不是** `INDICATOR_SPEC §5` 的 `D-06`），該表逐字「頁4 葉1 ②／**第二層，常駐揭露 · 非摺疊**／✅ 常駐」 |
| `hold.cold_start_toc` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**（n1「今日大白話結論」，規格逐字「**只有**這一塊」）。〔GB 線框〕`idle`＝一份**靜態目錄**，逐條預告按鈕按下去之後 ①~⑦ 會出現什麼 ＋「── 另一葉 ── 📁 **組合設定**…（**後兩項未接線**，本頁不寫入）」 | 🔴 **0 命中**〔GA，`page_hold.py` grep `cold_start`／`目錄`／`導讀`／`TOC` ＝ **0**；`section_header` 全清單（10 處）無對應段；AST 動態 key 盤點無涵蓋它的容器〕。⚠️ **「冷啟動」這個詞在實作端只出現在各張卡的 `idle` 文案分流**（`IDLE_NOW／WHY／WHERE` vs `GOOGLE_NOT_ASKED_*`），**⛔ 沒有任何一塊獨立卡承載 n1**；`::render_page_hold` 在 `st.tabs` 之前**只有** `st.markdown(page_label)` ＋ `st.caption("我已經持有的，該加、該換、該減。")` ⇒ **n0／n1 整層在畫面上不存在**（洞 `GA-I-6`）。〔GB〕線框 `src` 亦只指靜態目錄、**不取數**；📌 **實證（`S1-3B §3.4.1 B-07`）**：現況是 `::_idle_note(scope_idle=True)` **同一段文字**被掛到 **14 張卡**上各自 `render_note()` 一次（靜態掃出 12 支 `build_*` ＋ 綁定 2 張卡 ＝ 14，與紅隊實跑的 14 次**兩邊獨立得到同一個數字**）⇒「**14 張空卡沒有提供任何一格資訊，只提供 14 次同一句話**」。**TTL／fallback 不適用** | 無計算（無實作） | **無態可判**（畫面上不存在）。⚠️ ⛔ **不可與各卡的 `idle`（#3）混為一談**：那是**卡層級**的態，n1 要的是**頁層級**的一塊。⭐ 兩種 `idle` 文案**刻意分家**（`page_hold.py::_idle_note(scope_idle)`）：**冷啟動**要你去按鈕 vs **選了不讀 Google**要你去改選項 —— 逐字「共用一句話會對其中一半的人指錯路」。〔GB 十格〕`empty`／`na`／`partial`／`degraded`＝**null**；⭐ **摺成目錄時，下面四件事一項都不准跟著消失**（`CLAUDE.md §1`）：⬜ 尚未載入**可以**摺；⛔ **未接線不摺**；🟠 門檻已失準不摺；🔴 錯誤不摺、各自一張紅卡 ＋ **原始錯誤訊息**。線框 flags `['★待拍板（改變設計）','偏離核准線框','未驗證']`（性質為**改變設計** → `★-09` 併案）。**編號 ⚠️ 待確認** |
| `hold.conclusion_cards` `[A:無此頁]` | ✅ **有實作（容器 block，3 張子卡）** —— ① 結論三張卡。🔴 **卡名兩組不同，⛔ 並陳不裁決**：〔GA 實作端〕現行三個 label ＝ **「該做什麼」／「訊號可信度」／「需要處理」**（live 值：可信度＝`{judged}/{total_lights} 盞給得出判定`；需要處理＝`加碼 {add_n} · 汰弱 {cut_n} · 未判 {unjudged_rows}`）；〔GB 規格端〕**客戶 2026-09-16 裁示逐字**「結論三卡：用**線框改名版**（**這次讀到的持股／訊號可信度／燈號分布**）。**合規理由成立**，改回舊名」⇒ **第 1、3 張 label 成為待修（GB 逐字：⛔ 不得寫成「實作已符合」）**，理由是「該做什麼」讀起來像指示動作、落在全頁禁止買賣建議的判準射程內 | 🔴 **字面 grep `hold.conclusion_cards` ＝ 0；實作用三個常數 key**（方法論 ①）：`CONCLUSION_ACTION_KEY`＝`hold.conclusion.action`／`CONCLUSION_CONFIDENCE_KEY`＝`hold.conclusion.confidence`／`CONCLUSION_TODO_KEY`＝`hold.conclusion.todo`（`page_hold.py::build_conclusion_cards` → `::build_action_card`／`::build_confidence_card`／`::build_todo_card`）〔GA〕。鏈：`::load_station` → **L3 `src/services/dividend_station_service.py::get_station_rows`**（內含 `::fetch_vix` 一次 ＋ 逐檔 `::fetch_metrics`）＋ **`::build_station_digest`** ＋ **`::compute_portfolio_totals`**；N／M 可信度走 **L4 `src/ui/render/station_cards.py::aggregate_judged`／`::tally_states`／`::is_fully_judged`／`::cruise_or_gap`**（**與既有戰情室同一把尺**）。**TTL**：持股 `TTL_15MIN`；日線／配息 **`TTL_1HOUR`**（`src/data/etf/etf_fetch.py::fetch_etf_price`→`::_fetch_etf_price_max`／`::fetch_etf_dividends`）／NAV **`TTL_2HOUR`**／折溢價 **`TTL_15MIN`**／財報 **`TTL_1HOUR`**（`src/data/core/financial_statements_fetcher.py::fetch_financial_statements`）；VIX ⚠️ **⛔ 非 `shared/ttls.py`**（見 `hold.war_table`）。**fallback**：`.TW` 抓空 → **`.TWO`（上櫃）**；其餘 ⚠️ 待確認 | **多層**：**L1** 日線／配息／NAV／財報 → **L2 `src/compute/etf/dividend_station.py`** 判 235 燈／3-3-3／健檢四盞 → **L3** 組表 ＋ `::build_station_digest` 彙整**已算好**的欄位（逐字「⛔ 不重抓、⛔ 不推估」）→ **L4** 算 N／M → **L5 只組卡、⛔ 不重判**。〔GB〕**逐格獨立判態**（核准線框 `errCells` 只有第三格紅、前兩格維持 ⬜） | ⭐ **「未判」與「沒事」⛔ 不可合成一個數字**（檔內逐字：「前者是**還不知道**，後者是**沒事**」）⇒ facts 單獨列「未判 N」。`idle`→**#3**（兩種文案分流，見 `hold.cold_start_toc`）／`empty`＝「▨ **持股 0 檔**（**這是有效結果**，不是錯誤）」→**#7 ⚠️ 規格要求；實作畫 `▨`**／`failed`→**#6**（「**另外兩格維持原樣**」）／`na`＝**null**／`partial`→**#9 ⚠️ 規格要求**（值帶 `*` ＋ 就地寫 `N / M 計入` ＋ **下方列出缺的是哪幾檔**，不會只寫「部分資料」）／`degraded`→**#4**（「該格**門檻已失準**，**觀測照出、判決留白**；原因照出，**不自己編**」）／`unwired`→**#5**。⚠️ **fail-safe：N／M 拿不到 → 降 #7，⛔ 不得退回 #1**。⭐ **`requested=` 一律由 `SS_APPLIED_HOLD` 的「存在性」帶下來，⛔ 禁止 `bool(rows)` 反推** —— 逐字理由：`bool(rows)` **分不出「還沒按」與「按了但你沒有持股」**，而後者對新使用者是**極常見且完全正常**的狀態。可重試：✅。**編號＝`S2-UI_SPEC §9.3` 的 `D-5`**（未計入清單；⛔ 不是 `D-05`）；`DECISION_RULES §6` 衝突解決矩陣**對映 ⚠️ 待確認**。⚠️ **斷點**：3 欄只在 >1280 與 881–1280 成立；641–880 降 2＋1；≤640 單欄 |
| `hold.alloc_deviation` `[A:無此頁]` | ✅ **有實作（容器 block，3 張子卡）** —— ⑤ **「80/20 配置偏離（該減）」**（key `hold.alloc_split`）／**「衛星停利」**（`hold.take_profit`）／**「建議持股水位（市場端）」**（`hold.position_cap`）。〔GB 線框 live〕「核心 76% / 衛星 24%（**偏離 −4pp**）· **對照基準：核心 80% ／衛星 20%** —— 這是**系統內建的預設值，不是你設定的**」＋「衛星部位中 **2 檔**的未實現報酬率 **≥ +15%**…**這一層只講「幾檔」與「門檻是多少」，不指名是哪幾檔**」 | 🔴 **字面 grep `hold.alloc_deviation` ＝ 0；三張子卡各自字面 key**（方法論 ①）。`page_hold.py::build_allocation_split_card`／`::build_take_profit_card` ← `::load_station` → **L3 `dividend_station_service.py::build_station_digest`**（內含 **`::compute_allocation_split`** ＋ **`::flag_take_profit`**）；`::build_position_cap_card` ← `::load_allocation` → **L3 `src/services/allocation_service.py::get_allocation`**（另讀 `src/services/macro_state_locker.py::get_macro_state`）〔GA〕。**門檻 L0 `shared/dividend_station_thresholds.py`**：`CORE_TARGET_PCT=80.0`／`SATELLITE_TARGET_PCT=20.0`／`SATELLITE_TAKE_PROFIT_PCT=15.0`。持股輸入 **TTL `TTL_15MIN`**；其餘 TTL 同 `hold.conclusion_cards`（共用同一輪 `load_station`）；**fallback ⚠️ 待確認** | **多層，逐層標明**：**L1** 日線 → **L3 `::compute_portfolio_totals`** 算 `市值`（**＝張數×現價，乘法點在 L3，本頁⛔ 一個乘法都沒有**）→ **L3 `::compute_allocation_split`（純函式）**：`core_pct ＝ 核心市值 ÷ (核心+衛星) × 100`、`sat_pct ＝ 100 − core_pct`、**`core_dev ＝ core_pct − CORE_TARGET_PCT`** → **L5 只格式化**。**停利**：**L3 `::flag_take_profit`** 對 `held` 且 `種類=='個股'` 且 `損益%` 非 None 者，`損益% ≥ 15.0` ⇒ 列入（「超出門檻」欄＝報酬率 − 門檻 pp）。**核心／衛星以「ETF ↔ 個股」近似**（⚠️ **主題型 ETF 會被算成核心**，卡面 facts 已註記此限制）。〔GB〕**容忍帶＝查無** —— 卡片僅 **inline `abs(dev)>=1`（1 百分點）** 決定短標籤（**寫死** inline）⇒ 應然差異 **`D-31`**；`R4-1` 判定順序：`n_classified==0`→`is_computable=False`（⛔ 不得以 `hhi=0`／`0%` 頂替）／`n_classified<n_total`→只算已分類且**強制同時顯示 `coverage_pct`**／**帳本無張數→`basis='equal_weight'` 且 UI 強制揭露等權假設**（⛔ 不得把等權結果講成實際權重）。⚠️ **未實現報酬率本身的算式規格端查無**（`HOLD-G-07`／`GB ⓗ`） | 🔴 **未決衝突（原樣標註，⛔ 本檔不裁決哪一邊對）**：〔GA 程式碼端實測〕對照基準走 **L0 系統常數 `CORE_TARGET_PCT=80.0`／`SATELLITE_TARGET_PCT=20.0`**；〔GB 規格端〕`UI_PAGE_HOLD ③` **三條硬禁令第 2 條逐字「目標比例只能由使用者自己填，系統⛔ 不得預填、⛔ 不得建議、⛔ 不得給「參考配置」」**，且該三條「**凌駕本頁其餘版面規定**」⇒ **兩者是否牴觸＝已登記、⚠️ 未裁決**（線框現行改寫為「這是**系統內建的預設值，不是你設定的**」＋ flags `['★待拍板（對照基準要不要保留）']`）。🔴 **另一條已登記未決項**：本卡為「**全站最像 `S2-UI_SPEC §9.1` 實質違規的卡**」—— 卡面 label 逐字含**動作語**「（該減）」、區段註解仍寫「衛星**停利**」（〔GA〕另測 `::build_switch_card`＝「（該換）」、`::build_action_card`＝「該做什麼」⇒ **動作語是 label 本身，⛔ 不是缺值文案**）；**兩組皆⛔ 不重判**。缺值：`split is None`→**`empty` #7 ⚠️ 規格要求；實作畫 `▨`**，⭐ 文案分兩種、⛔ 不共用：**沒有可拆的持股** vs **有持股但一列都沒有市值**（後者逐字「本站**⛔ 不用檔數當比例頂替** —— 三檔各一張與三檔各一百張，配置完全不同」）；`missing`＝**缺成本就不判**（「沒有均價就沒有未實現報酬率，**硬判等於替你編一個報酬率**」，「**判不了」不會被寫成「沒達標」**）；`na`＝**只有 1 檔時** `N/A`（單一持股沒有「配置」的概念，**不加 `⚠︎`**）→**#8 ⚠️ 規格要求**；`error`→**#6**（**兩半各自判斷**，比例算不出來⛔ 不把達門檻那半一起染紅）。⭐ **`partial` 揭露**：L3 回 `partial=True`（`valued_n < held_n`）時 facts 加「**⚠️ 這個比例只涵蓋一部分**：N 檔持有列裡只算了 M 檔 —— **僅供參考**」（＝**#9 的語意，但 `◧` 全 repo 0 命中 ⇒ 用 facts 文字代替、⛔ 沒有徽章**）。⭐ **缺張數／均價的持有列⛔ 不進分子也不進分母**。⭐ **「沒有一檔達停利門檻」是 `empty`（灰），而且是好消息、⛔ 不是故障**。⚠️ 合規逐字：⛔ **「你設定的停利門檻」是事實錯誤**，一律寫「**系統內建的，不是你設定的**」。可重試：✅（補齊張數／均價後重按）。**編號**：`DECISION_RULES §5`(a)／`D-25`（比例 0–1 vs %，差 100×）／`D-31`（容忍帶 inline `1`）；`INDICATOR_SPEC R-2／R-3` |
| `hold.war_table` `[A:無此頁]` | ✅ **有實作（容器 block，2 張卡 ＋ L4 燈牆）** —— ③ **燈牆卡**（key `hold.lightwall`，live 值＝`N 檔 · J/T 盞有判定`）＋ **VIX 卡**（key `hold.vix`）＋ **逐檔燈條 HTML**（L4）。〔GB 線框 live〕「12 檔 × 235 加碼燈 / 3-3-3 判定 —— **點任一檔（整列可點）就地展開該檔的週線與布林 z**…**卡片本身仍不可點**。⚠️ **一個邊界**：窄螢幕會把一檔渲染成一張卡 —— 變成卡之後就回到「卡片不可點」」 | 🔴 **字面 grep `hold.war_table` ＝ 0**（方法論 ①）。`page_hold.py::build_lightwall_card`／`::build_vix_card`／`::_render_light_wall`；**燈牆渲染走 L4 `src/ui/render/station_cards.py::render_light_wall`／`::render_legend`／`::CSS`**（本頁⛔ 不畫第二種燈格）〔GA〕。**VIX 鏈（🔴 兩頁共用）**：`::load_vix` → **L3 `dividend_station_service.py::fetch_vix`** → **L1 `src/data/macro/macro_core.py::fetch_yf_close("^VIX", range_="6mo")`**（`range_` 逐字「對齊 `risk_radar` 同源快取」）。**⚠️ TTL 例外**：該支**⛔ 不用 `@st.cache_data`**，走 **module-level memo ＋ `macro_core.py::_YF_CLOSE_TTL`**（**⛔ 非 `shared/ttls.py` 常數**；`CLAUDE.md §2.4` 表列 3600 s，⚠️ 那是憲法表、**⛔ 非本頁規格** ＝ `GB ⓘ`）。**fallback**：`CLAUDE.md §2.1` 登記 VIX ＝ Yahoo `^VIX` 主 → CBOE CDN 備，但〔GA〕**未在 `::fetch_vix` 內看到 CBOE 分支**（失敗直接回 `None`）⇒ **⚠️ 待確認（`GA ⓓ`）、⛔ 不寫成「有 fallback」**；〔GB〕**本頁規格未重述** | **多層**：**L1** 日線 → **L2 `src/compute/etf/dividend_station.py::light_235`／`::screen_333`** 逐盞判燈（235 加碼燈／3-3-3 ①②③／健檢 A~D／個股 `swap_level`）→ **L3** 組 rows ＋ `_lights` → **L4** 算 N／M 並畫格 → **L5 只組卡**。〔GB〕**235 燈**（`INDICATOR_SPEC §3`）：三軸（VIX／週線／`z=(週收−MA20w)÷std20w`）**取最嚴重**；SSOT `shared/dividend_station_thresholds.py::VIX_LIGHT1/2/3 = 20/25/30`／`::Z_LIGHT1~3`／`::DEPLOY_*_PCT`／`::MIN_WEEKS_FOR_BOLL`；**逐軸判可用性，三軸全缺 → 燈本身＝缺漏**（`D-19`：現行回 `⚪ 巡航` 是設計變更待修），`_ok(v)` 須同時擋 `None` 與 `NaN`。**3-3-3**：三子項布林 AND；同類 ETF < `PEER_MIN_GROUP_SIZE` → **不適用（非缺漏）**；`passed` 應三態（`D-18`）。**VIX 門檻本頁只印成 facts、⛔ 不參與判定**（`UI_PAGE_HOLD ⑥A`） | ⭐ **燈牆與 VIX 各自判態 —— 一邊沒有⛔ 不把另一邊染色**（`section_header` 副標逐字）。⭐ **沒有 `_lights` 就整段⛔ 不畫**（逐字「**畫一排空格子假裝有燈，比不畫糟糕得多**」）＋ 留一行 `print` log。⭐ **燈牆 caption 逐字揭露雙通道**：「**填色＝這盞燈自己的判定**、**外框／紋理＝這盞燈可不可信**（四態）—— 一盞『亮著綠燈但其實沒有資料』的燈，填色會是灰的而且帶斜紋」＋「分母比格子數少是正常的（還沒有判燈規則的燈⛔ 不進分母）」。⚠️ `::_render_light_wall` **⛔ 不判態、⛔ 不取數**（狀態在卡上）。`idle`→**#3**／`empty`＝「該檔該燈無資料（歷史太短 → **等時間累積**，**⛔ 不會叫你重跑**）」→**#7 ⚠️ 規格要求；實作畫 `▨`**／`missing`＝「該檔該燈這一輪沒拿到輸入 → **可以再抓一次**」／`na`＝`N/A` 該盞（例：ETF 不適用某盞個股燈，**不加 `⚠︎`**）→**#8 ⚠️ 規格要求**／`partial`→**#9 ⚠️ 規格要求**（「N／M 檔計入」＋未計入那幾檔**單獨列出**）／`degraded`→**#4**（**觀測照出、判決留白**）／`unwired`→**#5**（**不會被摺起來**）／`error`→**#6**（「**無法讀取持股清單**；① 的燈號分布卡同步紅；**VIX 那一格照常顯示**」）。⚠️ **`R-2` 單調型彙總例外**（`INDICATOR_SPEC §2`）：worst-of／布林 AND **已見足以定論的輸入（已見 🔴／已見 False）→ 回該結論（＝有值），⛔ 不得被缺漏改寫成未評估**。⚠️ `S1-4 §3.2`：**`⚪` 在 235 這把尺上是「巡航＝正常」**，與別處的「不適用／未評估」**語意相反**；`❌`（3-3-3 不符）**刻意不給紅色**、3-3-3 **從不參與 `_worst_level`**。⚠️ **VIX 兩軌⛔ 不得同時引用**（`DECISION_RULES §9 R9-6`：`>30` ⚫ 極端恐慌／`>22` 🟡 波動加劇／其餘 🟢 市場平靜 —— 同一天 VIX=23 會同時出現 🟡 與 🟢）。⭐ **「點一列就地展開」刻意未做**（逐字：那是**新增互動元件**，落在 `CLAUDE.md §-1.5` A-8 UI 草稿先行）⇒ ⚠️ **已登記的設計 gate，⛔ 不是漏做**。可重試：✅。**編號**：`D-18`／`D-19`／`R9-6` |
| `hold.swap_compare` `[A:無此頁]` | ✅ **有實作（容器 block，2 張卡）** —— ④ **「換股建議（該換）」**（key `hold.switch`，live 值＝`換出 N 檔 → 換入 M 檔`）＋ **「總經位階（換股的攻守閘門）」**（key `hold.macro_stage`）。🔴 **合規改名已裁定（`S1-3B` 附錄 B-03，全檔統一）**：核准線框原用舊名「換股⛔建+議」，**已裁定改為「④ 換股對照」＋ 就地加「客觀對照，非買賣建議」**；處置範圍逐字「⛔ **但區塊本身⛔ 不動、位置⛔ 不動、內容⛔ 不動 —— 改的只有那兩個字**」，性質為 `CLAUDE.md §-1.5.A-8` 判準的「**修正錯誤**」〔GB〕 | 🔴 **字面 grep `hold.swap_compare` ＝ 0**（方法論 ①）。`page_hold.py::build_switch_card`／`::build_macro_stage_card` ← `::load_switch(station, macro, holdings)` → **L3 `dividend_station_service.py::get_switch_in_candidates`** ＋ **`::build_switch_advice`**；位階 ← `::load_macro` → **L3 `dividend_station_service.py::get_station_macro`**〔GA〕。**換入候選 fallback 鏈（逐字）**：`::get_switch_in_candidates` → **L3 `src/services/fundamental_screener_service.py::get_ranked_picks`**（**＝選股網畫面同源排名**）；失敗 → **回 `[]`，⛔ 不捏造標的**。**位階 fallback 鏈（逐字）**：`allocation_service.py::get_macro_regime`（session 內，全站唯一 regime 橋樑）→ 失敗退 **`macro_state_locker.py::get_macro_state`**（只讀 `macro_state.json` 快照）→ 再失敗回 `{}`；**位階⛔ 不打 API**。〔GB〕另一份輸入＝**觀察清單** worksheet **`stock_watchlist`**（`name／ticker／updated_at` **3 欄**，**物理隔離、無任何價格欄**）；**TTL `TTL_15MIN`**（持股與觀察清單）；**總經契約 TTL／fallback ⚠️ 待確認** | **多層**：**L3 `::build_switch_advice`** —— 換出＝持有中判 🔴 汰弱者；換入＝選股池 top（`top_n=5`，內部取 `max(top_n*4, 40)` 再篩）；**總經位階當攻守閘門**。⭐ **exclude 去後綴比對走 L0 `dividend_station_thresholds.py::normalize_ticker`**（逐字理由：否則 `2330.TW` 擋不掉 bare `2330` ⇒ **已持有卻被推「換入」**）。姿態走 **L0 `shared/position_throttle.py::compute_position_throttle(health, regime, defense)`** → `posture_label`／`posture_range`。⚠️ **〔GB〕規格端未給「換出體質轉弱」的判定式與「套用總經位階後怎麼重新排序」的算式 ⇒ ⚠️ 待確認（`HOLD-G-05`／`GB ⓕ`），⛔ 未猜** | ⭐ **選股池不可用／空 → 回 `[]`，caller 顯示「選股池暫無候選」，⛔ 不捏造標的**（`CLAUDE.md §1`）。⭐ **位階讀不到 → 標「總經未評估」，⛔ 不瞎給攻守**（L3 docstring 逐字）；`loaded=False` 時 `posture_label='總經未評估'`、`defense=None`（**⛔ 不填 False**）。`idle`→**#3**（「需要燈牆結果 ＋ 觀察清單兩份輸入」）／`empty`＝「▨ **觀察清單是空的**」→**#7 ⚠️ 規格要求；實作畫 `▨`**／`failed`→**#6**／`degraded`→**#4**（「已產生換股對照，但**未套用總經位階**…**不會拿「中性」代替，也不猜多空**」）／`unwired`→**#5**／`missing`／`na`／`partial`＝**null**〔GB〕。⭐ **`_switch` 抽成變數供 ⑦ 共用**（逐字：在 ⑦ 再 `load_switch()` 一次會多打一次選股池取數，**而且畫面那張卡與推播文字有機會講出不一樣的換股建議**＝`CLAUDE.md §2.1` 同一頁⛔ 不出現兩份）。可重試：✅。**編號 ⚠️ 待確認**（`GB F-2` 對不上；合規面可引 `S2-UI_SPEC §9.1` 禁用語表「相對位置」列 → **客觀對照**）；`DECISION_RULES §6` 衝突解決矩陣**對映 ⚠️ 待確認** |
| `hold.rebalance_deviation` `[A:無此頁]` | 🟡 **已實作，但內容是 `wired=False` 灰卡** —— ⑥「再平衡」未接線卡（**實作 key `hold.deep.rebalance`**），facts 四列逐項說明卡在哪一層（卡住的層／持股清單 ✅ 已接線／L3 wrapper ✅ 已補／為什麼不拿現況當目標）。〔GB 線框〕三態各一張長相完全不同的表：`live`（**狀態 B**）逐檔 `目前% / 目標% / 偏離(pp)`（`2330.TW 25.00 / 25.00 / **0.00** 🟢 在容忍內`）／`missing`（**狀態 A**）輸入欄一律 `〔＿＿＿＿〕`＋偏離欄 `▨ 未設定`／`degraded`（**狀態 C**）偏離欄一律 `▨ 不計算`＋合計格 `⚠︎ 差 8.00` | 🔴 **字面 grep `hold.rebalance_deviation` ＝ 0；實作 key ＝ `hold.deep.rebalance`**（`page_hold.py::DEEP_SPECS` 的 `UnwiredSpec`）→ `::build_unwired_card`。**`wired=False` ⇒ 恆 `UI_UNWIRED`、⛔ 零取數**〔GA〕。⚠️〔GB G2〕實作把它掛在 **⑥ 區、⛔ 不是 ⑤ 區**；狀態 A/B/C 判定實作在 **L2 `src/compute/etf/portfolio_gates.py::evaluate_rebalance_gate`**，而 **`portfolio_gates` 未被 import**（AST 確認，2 處字串命中皆為 docstring／文案）。🔴 **技術原因（與 `hold.evi_target_pct` 同源，兩組同測）**：`gsheet_portfolio.py::_HEADERS` **5 欄**、`target_pct` 在 `page_hold.py` grep **0 命中**；`::save_portfolio` 的 `new_rows.append([name, tk, lots, avg, ts])` **位置式寫死 5 寬**、存檔為 `ws.clear()` → 重寫 ⇒ **第 6 欄每存一次被擦一次，且無任何錯誤訊息**。**TTL `TTL_15MIN`（持股）／fallback ⚠️ 待確認**；**本卡自身 TTL 無快取** | **無計算**（靜態，卡面恆 unwired）〔GA〕。〔GB 應然〕✅ **容忍值已決（客戶 2026-09-16 裁示逐字）**：「容忍值：**三個並陳**，接線時用 **SSOT 常數 1.0pp**。判法照 SSOT：`abs(...) > tol`，**不用 `math.isclose`**。寫進規格，**不准自己挑一個**。」⇒ 三數並陳保留（線框 B 態 **±5.0pp**／C 態 **±0.1pp**／SSOT `shared/signal_thresholds.py::PORTFOLIO_TARGET_SUM_TOLERANCE_PP = 1.0`），判法 `abs(_sum - 100.0) > tol`（`_sum` 先 `round(...,4)`）。⚠️ **這是具名例外、⛔ 不是全稱句**：`CLAUDE.md §4.3`／§6「浮點比較一律用容差」**通則一條未減** —— 例外成立是因 **SSOT 判法本身已自帶 `tol`**，外面再包一層會變成兩層容差。**`DECISION_RULES §5`(c)**：目標留白／部分留白／總和偏離 > tol → `'unknown'` ⚪「無法判定」，**⛔ 不得拿現況當目標、⛔ 不得補 0、⛔ 不得判綠燈**；全區間在帶內→`'pass'`、全在帶外→`'fail'`、其餘→`'unknown'`；顯示「目標% / 實際% / 偏離度%」三欄，**未設定一律印 `—` ＋ caption「系統不會拿現況當目標」** | 🔴 **⛔ 缺的不是部位大小，也不是 L3 wrapper —— 缺的是「目標比例」欄**（`::DEEP_SPECS` 逐字：「本批補的 `services.portfolio_deep_service` **就在那裡**，隔壁的壓力測試與 VaR 走的就是它。缺的是**目標權重**…而你的持股帳本那張表只有 `name / ticker / lots / avg_price / updated_at` **五欄**」）。⭐ **⛔ 不拿現況當目標**：逐字「偏離度必然是 0.0% —— 那**⛔ 不是「已平衡」而是「沒算」**」（`portfolio_gates.py::evaluate_rebalance_gate` 對這種輸入的判定就是「⚪ 無法判定」；舊分頁 `src/ui/etf/etf_tab_portfolio.py` 註解「原碼:`target_pct = actual_pct` → `deviation` 恆 0 → 再平衡永遠印…」就是這個病）。⭐ **⛔ 也不改用 `src/compute/strategy/portfolio_manager.py::CoreSatelliteManager` 頂替**：它的核心比例是**另一套**（依 regime 0.60~0.85），與本頁 ⑤／⑥ 用的 L0 80/20 **不同** ⇒ 同一頁兩個矛盾的核心比例（`§2.1` SSOT）。⭐ `::MISSING_L3_WRAPPER_WHY` **已刪除且註明「⛔ 不要再加回來」**（留著會叫下一個人去補一支已經在那裡的東西）—— 守衛 `tests/test_p04_hold_view.py::TestUnwiredStaysUnwired`。**狀態 #5 `⛔`；可否重試：❌**。**三個「零」見 §0 第四條，⛔ A/B/C 不得共用寫法。** 〔GB〕`empty`＝「▨ 無資料 ——「你的帳本裡沒有任何持股，沒有比例可以算」」；`na`＝**只有 1 檔時** `N/A`（「單一持股沒有再平衡的概念」，**不加 `⚠︎`**）→**#8 ⚠️ 規格要求**；`partial`→**#9 ⚠️ 規格要求**（「4 檔中 1 檔缺報價，目前比例以**其餘 3 檔重新歸一**計算」＋**缺報價那檔單獨列出**）；`unwired`＝「本區塊目前**不會出現這一態**」（⚠️ **與 GA 實測的「恆 unwired」相反，⛔ 並陳不裁決**）。**要接上＝(i) 新增「目標比例%」輸入元件（＝新增視覺元件 → `CLAUDE.md §-1.5.D §03-2 ①` 草稿先行 gate）→ (ii) import L2 gate → (iii) 八處 D-3 到位；⛔ 在 (i) 之前不得先接 (ii)**（沒有輸入端的 gate 只會回「無法判定」，**而那會被誤讀成「已平衡」**）。**編號**：`D-3`（八處落點）／`D-5`（partial 未計入清單）；`DECISION_RULES §5`(c) |
| `hold.scenario_calc` `[A:無此頁]` | 🔴 **兩組指到兩個不同的東西，⛔ 並陳不裁決**：〔GA 程式碼端〕本 block ＝ ✅ **有實作（容器，2 張卡）** —— ⑥ **「壓力測試」**（key `hold.deep.stress`）＋ **「VaR（風險值）」**（key `hold.deep.var`）；〔GB 規格端〕本 block ＝ **⑥ 再平衡的「情境試算」展開區**（`☑ 顯示情境試算` → `金額變化(TWD)`／`換算（參考）` 兩欄表 ＋ 五條試算限制），並實測 **G3：`st.checkbox` AST 0 處**（唯一 `checkbox` 字面在 docstring）⇒ **gate 不存在、整個情境試算區塊亦不存在** | 〔GA〕🔴 字面 grep `hold.scenario_calc` ＝ 0（方法論 ①）。`page_hold.py::build_stress_card`／`::build_var_card` ← `::load_deep(station)` → **L3 `src/services/portfolio_deep_service.py::get_portfolio_stress`／`::get_portfolio_var`** → **L2 `src/compute/etf/etf_calc.py::calc_portfolio_stress_test`／`::align_portfolio_returns`**；價格走 **L1 `src/data/etf/etf_fetch.py::fetch_etf_price`（`TTL_1HOUR`，經 `::_fetch_etf_price_max`）**。**fallback**：`portfolio_deep_service.py::_fetch_price_with_otc_fallback` —— **`.TW` → `.TWO`（上櫃）**；抓不到者進 `no_price` 名單（**排除、⛔ 不補值**）。〔GB〕持股／報價 **TTL `TTL_15MIN`**；**fallback ⚠️ 待確認**；🔴 **正在取代的現行違規（實測逐字引用 production）**：`src/ui/etf/etf_tab_portfolio.py` 的 `st.markdown('#### ⚖️ 再平衡交易指令')`、欄位 `'動作': action, … '建議股數': shares`、逐檔印出「{動作} {ETF} 約 {建議股數:,} 股」 | 〔GA〕**多層**：**L3 `portfolio_deep_service.py::_lot_to_shares`** ＝ **該檔唯一乘法點**（`張 → 股`，常數 **L0 `shared/sector_flow_thresholds.py::SHARES_PER_LOT`**）→ **L2** 算壓測跌幅金額／VaR → **L5 ⛔ 一個乘法都沒有**（`CLAUDE.md §4.1`：漏乘 ＝ **1000 倍低估**）。守衛 `tests/test_p04_hold_view.py::TestPortfolioDeepServiceIsReadOnly::test_there_is_exactly_one_multiplication_site`／`::TestTheLotToShareClaimIsMeasurable`。⚠️ **「該檔唯一乘法點」⛔ 不等於「全站唯一」**（檔頭自陳 `dividend_station_service.py` 與 `src/compute/sector_flow.py` 各自也乘每張股數）。〔GB〕**情境試算的算式規格端查無**（線框只給欄名與示意數字 `−176,200.00`／`約 0.89 張`）⇒ ⚠️ 待確認（`HOLD-G-06`／`GB ⓖ`），⛔ 未猜；⭐ `S2-ENG_SPEC §3.1` 硬規則「**再平衡試算、涵蓋率計算屬 L2 純函式 —— ⛔ 不得寫在 `page_hold.py` 裡（那會重演 `V-APP-1` 的病）**」；換算用「張」：`S1-1 §1.3` 逐字「`lots` 語意為『**張**』（台股 1 張 = 1000 股），**非股數**」 | 〔GA〕⭐ **本頁三處 `UI_DEGRADED` 之二**（`page_hold.py::_degraded_bits`）—— **兩個失準因子，都由 L3 欄位帶下來、本頁⛔ 不自己判**：(a) **`reconciled=False`**（本格總市值與 ① 的 `::compute_portfolio_totals` **對不起來**，兩邊納入的持股列不是同一批）；(b) **`no_price` 非空**（有持股抓不到價格序列，被排除在樣本外）⇒ **#4 `🟠`**。⭐ **降級後數字⛔ 不藏起來**：`Card` 規定非 live ⛔ 不得帶結論文字 ⇒ **現值改掛 facts 的「現值」列**，只是不再當結論、**也不再出燈號**。⭐ **⛔ 不降成 `UI_FAILED`（紅）** —— 逐字「紅是『系統真出錯』；把打折畫成故障就是**製造假警報**，而滿版假紅字會讓**真的**故障沒人看得見」（`CLAUDE.md §1.A-4`）。⚠️ VaR facts 逐字「**日報酬是原幣別報酬；外幣計價的持股未含匯率變動**」（與 `hold.fx_pnl` 交會）。`idle`→**#3**／`empty`→**#7 ⚠️ 規格要求；實作畫 `▨`**／`failed`→**#6**。可重試：✅（補價格／對齊納入口徑後）。〔GB 十格〕`idle`＝「☐ 顯示情境試算（**勾選後才計算，預設不跑**）」；`missing`＝「上游目標比例未設定（狀態 A）→ **本區塊不可用**」；`degraded`＝「上游目標比例總和 ≠ 100%（狀態 C）→ **本區塊不可用**」；`na`＝只有 1 檔時 `N/A`（**不加 `⚠︎`**）；`partial`→**#9 ⚠️ 規格要求**；`unwired`＝**null**。✅ **Checkbox Gate 屬內部自決**（`CLAUDE.md §-1.5.F 判定 7`：**畫面結構不變的效能 gate**，⛔ 不是版面異動）；**但它承載的表格本身屬 G2 的新元件、仍受 G2 拘束**。**編號**：`DECISION_RULES §4`（組合風險）／`D-31`（inline `0.7`）；**＋`S2-UI_SPEC §9.3` 的 `D-4`（試算五條限制：未計手續費／未考慮最小交易單位／以某日收盤價計／不是下單清單／無下單功能／不考慮稅務）＋ `D-3` 第 #6 處落點**（短版 ＋「本試算用的是你這一輪填的值」） |
| `hold.fx_pnl` `[A:無此頁]` | 🔴 **真正未接線 —— 畫面完全不存在**（匯兌損益）〔GA〕。〔GB 線框〕**十格全部＝`N/A`（本頁唯一）**：`na` 逐字「**匯兌損益：算不出來**／為什麼：你的持股帳本**沒有存買入匯率**…**當初那個數字這本帳裡從來沒有**／系統**不估、不猜、也不拿今天的匯率頂替 —— 估了就是捏造**／去哪補：**目前無解。**…**這不是你操作錯誤，也不是系統壞了**」 | 🔴 **0 命中**〔GA，`page_hold.py` grep `hold.fx_pnl`／`匯兌`／`fx_` ＝ **0**；`section_header` 全清單（10 處）無對應段；動態 key 盤點無涵蓋它的容器〕。⚠️ **唯一沾邊的是兩句「已知限制」facts（屬於別的 block）**：`::build_var_card` 的「日報酬是**原幣別**報酬；外幣計價的持股未含匯率變動」、`::build_dividend_cash_card` 的「**外幣／海外標的（已排除，僅標記）**」⇒ **⛔ 沒有任何一塊卡承載匯兌損益本身**。〔GB〕【結構性缺欄位，實測】`gsheet_portfolio.py::_HEADERS` ＝ **五欄，沒有幣別、沒有買入匯率**；【L2 已逐字寫下同一件事】`src/compute/etf/portfolio_fx.py` 檔頭「已知限制(誠實揭露,§1)」逐字「成本與現值**都用同一個今日即期匯率**換算…**本頁沒有那筆資料，故不估（估了就是捏造）。caller 須把這句話顯示給使用者。**」—— ⭐ **本頁就是那個 caller**；匯率來源 `TWD=X`（Yahoo，`S1-1 §1.4`），**由 L5 caller 抓後傳入**，換匯時點＝rows 建構完成後**只呼叫一次** `::convert_rows_to_twd`，`FX_SCALED_FIELDS` 七欄（**ratio 類刻意不換**），sanity `USDTWD_SANITY_MIN=25.0 ~ MAX=40.0`，**拿不到匯率／超出 sanity → 絕不預設 1.0**、該檔標 `needs_fx=True` 移入 `excluded` 且**不計入任何總計**。**TTL**：`S1-1 §4` 表列基金站 `_FX_CACHE_TTL=300 s`（⚠️ **台股站本欄 TTL 規格未給** → ⚠️ 待確認）；**fallback 不適用** | ⭐ **本欄結構上無算式可用。** `S1-5 §4.4 G1` 逐字「**買入匯率 $f$：5 欄裡從來沒存過。要回推需要「買入日期」，而 G2 說明那也沒有**」⇒ `fx_twd_per_native = NULL`、`cost_twd_is_partial=True`、**台幣成本與匯兌損益一律 NaN**。🔒 **明文禁止的方案（總管已裁定）**：「用今天的 USD/TWD 回推歷史成本」—— 同時違反 `CLAUDE.md §1`（自行估一個合理值）、`§2.3`（lookahead）、`§2.2`（provenance），**而且它不會炸**，算得出一個看起來很合理的數字，**這正是它最危險的地方**。`S1-5 §2.1` 的正解 `F ≡ B ÷ (q·C)` 在 v1 帳本**無 B 也無 C** ⇒ 用不了。⚠️ `S1-5` 自陳「**設計文件。未建表、未寫 migration**」＋ §7 列 U-1~U-8「沒能驗到的事」⇒ **引用時照此打折** | 🔴 **三方並陳，⛔ 不裁決**：〔GA 程式碼端〕**畫面上 0 實作**，概念只以「已知限制」facts 出現在 `hold.deep.var`／`hold.deep.dividend_cash` **兩張別的卡**上；產生器 `HOLD_BADGES_ON_PAGE` 上方註解逐字「規格 ⑤-G4 的總管判定認為匯兌損益欄**應該**是 **#8**（結構上不適用），但 ② 的實作現況是『`N/A` 全頁 0 命中』⇒ **應然是 #8、現況是不畫**」（缺口 `H6`／洞 `GA-I-4`，GA 實測與之一致）。〔GB 規格端〕🔴 **兩份規格對同一格給不同符號（洞 `HOLD-G-01`）**：`UI_PAGE_HOLD ⑤ G4` 裁定 **`N/A`（#8）** 並連帶修掉現況把兩種缺值混成 `—` 的做法；`S1-5 §4.5` 逐字寫 **`—（需要買入匯率）`**、⛔ 禁「估一個」、並稱「缺買入匯率屬『資料就是沒有』，是**灰色 ⓘ，不是紅色錯誤**」。⚠️ **`N/A` ⛔ 一律不加 `⚠︎`**、⛔ **不得放進大字數值區**（`S2-UI_SPEC §3.3 ★-03`），走 `facts` 中繼列。⛔ **不得標成「未接線」**（`unwired` 代表「接上就會有」，本欄**接上也不會有 —— 缺的是資料本身**）。**可否重試：❌**（「目前無解」）。**編號**：**無 `D-*`**，但有一條更強的規格依據 ＝ `portfolio_fx.py` 檔頭逐字「**caller 須把這句話顯示給使用者**」，本頁就是那個 caller |
| `hold.deep_analysis` `[A:無此頁]` | ✅ **有實作（容器 block，6 張子卡：4 接線 ＋ 2 未接線）** —— ⑥ 組合深度分析，**順序照線框**：再平衡／核心衛星／壓力測試／VaR／配息現金流／葡萄串。〔GB 線框〕「六項並列（**各自 gate**）」；`empty`／`error`＝「該項無資料／取得失敗（**其餘五項不受影響**）」 | 🔴 **字面 grep `hold.deep_analysis` ＝ 0；6 張子卡各自 key `hold.deep.*`**（`page_hold.py::build_deep_cards`）：`hold.deep.rebalance`(⛔ unwired)／`hold.deep.core_satellite`(✅)／`hold.deep.stress`(✅)／`hold.deep.var`(✅)／`hold.deep.dividend_cash`(✅)／`hold.deep.grape`(⛔ unwired)〔GA〕。**核心／衛星走 `::build_core_satellite_card(station)` ＝ 與 ⑤ 共用同一份數字、⛔ 不重算**；另三張走 `::load_deep(station)` ＝ **另外三次取數**（**拆成兩個參數是刻意的**，逐字「混成一個容器會讓『這一格是不是重算了第二遍』變得看不出來」）。**配息現金流**：L3 `portfolio_deep_service.py::get_dividend_cash_flow` → L3 `src/services/dividend_tax_service.py`；配息序列 L1 `etf_fetch.py::fetch_etf_dividends`（**`TTL_1HOUR`**）。持股 **`TTL_15MIN`**；**fallback ⚠️ 待確認**。🔴 **〔GB G5〕`::DEEP_CAPTION` 自相矛盾尚未修**：同一段先寫「改**並列 ＋ 各自 gate**（3 欄 × 2 排，不是一排六欄）」、下一句又寫「**它們共用送出鈕那一個 gate**」（洞 `HOLD-G-09`；⛔ **不得只改 code 留著錯的說明**，且**規格端沒有給那段 caption 的正確文字**） | **多層**（各子卡見 `hold.scenario_calc`／`hold.alloc_deviation` 列）。**配息現金流**：L3 算 `gross_twd` → 扣**二代健保補充保費** → `net`；**⛔ 不含綜所稅**（`marginal_rate=None`）〔GA〕。〔GB〕**組合風險門檻（`DECISION_RULES §4`，強制欄「誰決定的」）**：單一持股權重 `0.10`（**比例 0–1、非 %**；顯示端自乘 100 ＝ `D-25`）＝寫死 `src/config/config.py::MAX_POSITION_PER_STOCK`；持股檔數 `10`＝寫死 `config.py::MAX_POSITIONS`；**產業集中度（個股組合）＝刻意無門檻**（`src/compute/risk/concentration.py` 明文「本模組不提供任何燈號 / 門檻」，**user 2026-08-14 裁示、2026-09-16 拍板維持，⛔ 不得自行推翻**）；ETF 產業上限 `30%`（inline L4 `src/ui/render/etf_render.py::_SECTOR_CONCENTRATION_MAX_PCT`）；相關性（個股組合）＝**查無實作**；ETF 矩陣 `0.85`（`shared/signal_thresholds.py::ETF_CORR_HIGH_THRESHOLD`）；ETF 分散度 `0.7`／`0.7`（inline `src/compute/etf/etf_smart_analysis.py::PRICE_CORR_HIGH_WARN`／`::DOWNSIDE_CORR_HIGH_WARN`）；風險佔比−市值佔比 `10.0`pp（`shared/risk_contribution_thresholds.py::RC_CONCENTRATION_GAP_PCT`）。**`R4-1`**：有門檻 → 顯示「狀態 ＋ 門檻數值 ＋ 誰決定的」三件；無門檻 → **只印量測值，⛔ 不得補門檻、⛔ 不得把量測值講成「過高／應降低」** | ⭐ **⑥ 從 expander 提升為並列區塊**（`::DEEP_CAPTION` 逐字：「**expander 收合只是視覺收合、body 每次 rerun 照跑** —— 埋起來沒省效能」）。⭐ **兩張未接線卡的 `where` ⛔ 不共用**（一個是帳本缺欄位、一個是 L5 widget key 撞名）。**葡萄串**（`hold.deep.grape`）：實作住 L5 `src/ui/tabs/grape_ladder.py`、**自帶寫死 widget key**，本頁再掛一次會撞 `DuplicateWidgetID` ⇒ 逐字「那⛔ 不是『畫得醜』，是**整頁當場拋例外**」⇒ **#5 `⛔`，可否重試：❌**（⚠️ 它是容器第 6 張子卡，**⛔ 不在 23 塊清單內**）。⭐ **本頁三處 `UI_DEGRADED` 之三**（`::_cash_degraded_bits`）：失準因子**只有一個** ＝ L3 `full_coverage=False`（有張數的持股沒有全部進 `gross_twd`）⇒ **#4 `🟠`**。⭐ **`no_payout_tickers` 刻意⛔ 不算失準因子**：逐字「那幾檔**有**進總額、貢獻 0 元，而 L3 明說上游把『真的沒除息』與『抓不到配息』回成**同一個空序列**、本層分不出來。拿一個分不出來的東西翻成橘燈，會讓『手上有一檔本來就不配息的股票』這種正常組合**永遠掛著失準標記 ＝ 假警報**」⇒ **改照實揭露在 facts**。⭐ **降級時燈號頻道留白**：「不含綜所稅」**⛔ 不是 band 觀測，是一句完整性宣告**，覆蓋率不足時它就變成假的。⚠️ **「不含綜所稅」必須在卡面講**（否則「稅後」兩字就是說謊）—— 稅率是**使用者輸入**，新增輸入元件要先過 A-8 草稿 gate。〔GB〕`partial`→**#9 ⚠️ 規格要求**；`degraded`→**#4**；`unwired`→**#5**；`na`＝`N/A` 該項（**不加 `⚠︎`**）。⚠️ **凡本區塊任一項印出台幣損益欄，匯兌損益欄一律照 `hold.fx_pnl` 規格寫 `N/A`**。⛔ **3 欄 × 2 排的版面已在核准線框內、故不另計偏離**；改成別的欄數 → **版面異動 → 草稿先行**。⚠️ **`S1-6 §3.1` 的 AI 免責三件事⛔ 不適用本塊**（六項皆為計算輸出、本塊不是 AI 出口）。✅ **斷點已決改三段制**（`≤640／641–880／≥881`；客戶 2026-09-16「四段是舊版殘留，改掉」）。**編號**：`DECISION_RULES §4`／`D-25`／`D-31`；`INDICATOR_SPEC R-2／R-3` |
| `hold.ai_summary` `[A:無此頁]` | ✅ **有實作** —— ⑦ AI 戰情總結（**字面 key `hold.ai_summary`**，live 值＝「已生成一段推播文字（見下方）」）＋ **單顆 `st.button`「⚡ 生成 AI 總結」**（AST 全檔 `st.button` **恰 1 處**）。〔GB 線框逐字〕「按一次生成一次。**會用到付費的 AI 服務，所以不會自動跑；結果不會存起來，要留請自己複製。**　〔 📋 複製 〕」 | `page_hold.py::build_ai_summary_card` ← `::load_ai_summary(pressed, station, switch)` → **L3 `dividend_station_service.py::build_station_digest` → `::build_summary_prompt` → `::build_ai_summary`**，**AI transport 由 L3 `src/services/app_ai_service.py::gemini_call` 注入**〔GA〕。按鈕 widget key `AI_BUTTON_KEY`＝`p04v_ai_summary_btn`。**TTL：⚠️ 無快取**（本頁⛔ 不把結果寫 session）／**fallback ⚠️ 待確認**（`GA ⓕ`：未讀 `app_ai_service.py` ⇒ 金鑰池／退避／`CLAUDE.md §1.A-3`「只快取成功結果」三項**皆未驗**）。⚠️〔GB〕核准線框 live 原文寫「一段可直接推播的文字總結（**含當日建議與理由**）」—— 線框**刪掉「含當日建議」那半句**，依客戶拍板第 3 條「全站不產生直接買賣建議」；**AI 產出同樣受 `S2-UI_SPEC §9.1` 禁用語拘束**（該表明文涵蓋「AI 產出」）。**這是有意識的刪改、⛔ 不是漏抄**；線框自陳「**本組未驗證** AI 總結的 prompt 端是否已過濾禁用語」 | **多層**：**L3 `::build_station_digest`** 先產**規則式事實**（純函式、離線可測、`CLAUDE.md §1` ⛔ 不生數字，逐字「彙整**已算好**的欄位，⛔ 不重抓、⛔ 不推估」）→ **L3 `::build_summary_prompt`** 組 prompt → **L3 `::build_ai_summary`** 打 AI 潤稿。**⇒ 數字由規則式層產生，AI 只潤稿**（〔GB〕適用版本＝**版本 A｜潤稿型**，無數值計算） | 🔴 **gate 是 `st.button()` 的「當次回傳值」，⛔ 不是 `HoldRequest.submitted`** —— 逐字理由：「每次 rerun 都打一次**付費** AI，比少一顆鈕嚴重得多」。守衛 `tests/test_p04_hold_view.py::TestTheAiCostsNothingUntilYouPress`。⭐ **「按一次生成一次，本頁⛔ 不記住」，而且畫面上必須講出來**（`::AI_KEEPS_NOTHING` 那一列 facts ＋ 區塊說明）—— 逐字「**假裝它會記住，使用者會以為自己弄丟了東西**」；⛔ **不得為了「讓它留著」而開 session key**（會同時打破「gate 只有一個寫入點」與「本頁不自建狀態」），正確做法是**推播出去**。`idle`→**#3**（「**AI 總結以上方六段的結論為輸入，避免無中生有**」）／`failed`→**#6**（`::_ai_failed_note`，附錯誤型別）／`empty`→**#7 ⚠️ 規格要求；實作畫 `▨`**／`unwired`＝「**未設定 API 金鑰** → **就地給設定入口**（⛔ 不會叫你去改設定檔）」→**#5**／`missing`／`na`／`partial`／`degraded`＝**null**〔GB〕。🔴 **`S1-6 §3.1` 全站免責強制樣板的來源就是本塊**（樣板來源逐字＝`page_hold.py::AI_DISCLOSURE`，「**它是全站唯一同時交代三件事的一份，故抬為樣板**」；`§3.0` 盤點表把 **A14 列為全 repo 唯一「✅ 完整」**）。**實作紀律三條**：(1) **一處常數、兩處引用**（⛔ 禁止在各頁手抄免責）；(2) ⭐ **每一態都印** —— 空狀態／錯誤態／載入中都要印，**否則使用者只在成功時看到免責，而失敗時的半截輸出才是最容易被誤讀的**；(3) **免責⛔ 不得取代改字**。⚠️ **抬成樣板時要改的一處**：現行原文含函式名 `build_summary_prompt()`，落在「⛔ 不得混入使用者可見文案的開發者備忘」形狀 ⇒ **換成白話**。🔴 **洞 `HOLD-G-02`：線框十格無一格出現免責句**（實作有、線框沒收）。⚠️ **FE-31 事實更正已登記**：這顆鈕**客戶早就拍板過**（線框逐字畫著），當初誤判為「還沒拍板」—— **A-8 規則⛔ 沒有被放寬，變的是「這顆鈕有沒有線框」這個事實**。可重試：✅（**但每次重按＝再打一次付費 API**）。**編號 ⚠️ 待確認** |
| `hold.setup` `[A:無此頁]` | ✅ **有實作** —— 葉2「組合設定」的**區塊標題 ＋ 副標**（容器 block）。副標逐字「**就地完成，不必去別頁** —— 但本頁對你的持股帳本**一律唯讀**，需要寫入才做得到的部分會標「未接線」並說明原因」；葉名 `::LEAF_SETUP_TITLE`＝「組合設定（＝組合管理＋Sheet 綁定）」。〔GB〕其餘九格＝「**常駐**」（`error` 另加「**真故障時更需要看得到「這一頁本來就不寫入」**，才不會有人以為是寫入失敗」） | 🔴 **字面 grep `hold.setup` ＝ 4，但全部是 `hold.setup.preview`／`hold.setup.pick_sheet`／`hold.setup.watchlist` 的前綴命中，⛔ 沒有一張 key 恰為 `hold.setup` 的卡**〔GA，AST 字面 key 清單實測〕。實作對應 `page_hold.py::_render_setup_form_block` 的 `section_header(f"葉2 · {LEAF_SETUP_TITLE}", …)` ＋ `::READONLY_DISCLOSURE`。**零取數** ⇒ **TTL 無快取／fallback 不適用**。⚠️〔GB〕**本葉在本次修訂前⛔ 完全沒有線框**（紅隊 C 實測：`S1-3B` 全稿對葉2 的著墨只有矩陣最後一列一行 ＋ 冷啟動分頁列示意 `┌ 戰情室 ┬ 組合設定 ┐`，而**葉2 承載四件事（Google 登入／選 Sheet／持股預覽／觀察清單），四件事沒有一件有文案**）⇒ **本次依實地盤點拆成 6 個區塊** | **無計算**（靜態揭露） | **無缺值態**（區塊標題恆顯示）；〔GB〕**十格 0 null**（全「常駐」）、**無徽章**。⚠️ **它是容器，⛔ 不是卡** —— 其下三段分別由 `hold.binding`／`hold.portfolio_count`／`hold.setup.*` 三塊承載。⚠️ `unwired` 那格特別註：「**這一態尤其要看得到** —— 下面兩張未接線卡的理由就靠這段唯讀宣告才讀得懂」。線框 flags `['偏離核准線框','未驗證']`。**編號 ⚠️ 待確認** |
| `hold.binding` `[A:無此頁]` | ✅ **有實作** —— 「Google Sheet 綁定」卡（**字面 key `hold.binding`**，label「Google Sheet 綁定」，live 值＝「已綁定一本」）。〔GB 線框 live 逐字〕「🟢 **已綁定一本**／· 本頁對這本 Sheet 的權限：**唯讀**／· 畫面**不顯示的東西：Sheet 識別碼與任何憑證**（**結構上就沒有帶進本層**）／· Google 登入：已登入」 | `page_hold.py::build_binding_card` ← `::load_binding(req)` → **L3 `src/services/portfolio_binding_service.py::get_binding_state`** → **L1 `src/data/portfolio/gsheet_portfolio.py::list_portfolios`（`TTL_15MIN`）**〔GA〕。**fallback**：讀取失敗 → **回中性 `bound`**（L3 docstring 逐字）。**gate**：`SS_APPLIED_HOLD` **並且**使用者選了 `SCOPE_WITH_BINDING` —— 沒選就**真的不發那一次網路呼叫**。OAuth 端 `src/data/portfolio/oauth_state.py`（`CLAUDE.md §8.2.A.1 EX-OAUTH-1`）〔GB；GB 端 TTL／fallback ⚠️ 待確認〕 | **無計算**（只讀布林） | 🔴 **⛔ 一個字元都不印 Sheet 識別碼** —— `BindingState.sheet_id` 本頁**只印「有沒有綁」這個布林**（唯讀鐵律第 3 道；〔GB〕對照 `S1-7 §2` 第 6 列「**Sheet ID 會進 OAuth `state` 參數 → URL → 瀏覽器歷史／中介 log**」）。⭐ **三種「沒有」⛔ 不可混**（`::build_binding_card` docstring 逐字）：「沒按／這一輪選了不讀 Google → `idle`；讀了、沒登入或沒綁 → `empty`（**有效結果，不是故障**）；讀了、Google 那端掛了 → `failed`」⇒ `idle` 那一格因此**寫了兩種來源、⛔ 不得合併成一句**（`::binding_scope_idle()` 就是那個分流點）⇒ **#3**。⭐ **「沒有綁 Sheet ≠ 失敗」**（`holdings_service.py` 逐字）→ `bound=False` ＋ 空清單 → **`empty`（灰）#7 ⚠️ 規格要求；實作畫 `▨`**，⛔ **不是紅** —— 逐字「把『還沒做』畫成紅色錯誤是**捏造故障**」（`CLAUDE.md §1.A` 第 4 點）；〔GB〕另逐字「**本站不會拿範例持股頂替**…**新使用者在這裡看到灰色是正常的**」。Google 那端掛了→**#6**（**唯一准用紅色的狀態**）＋**原始錯誤訊息**。`missing`／`na`／`partial`／`degraded`／`unwired`＝**null**。⚠️ **凡指向舊七頁籤的指路句，⛔ 一律必須寫出「先把左邊側欄切回『不使用（用下方原功能）』」這一步** —— 新五頁與舊七頁互斥（`app.py` 的 `st.stop()`），**少寫這一步使用者永遠找不到**。可重試：✅。**編號 ⚠️ 待確認**；`S1-7_PERMISSIONS_PII §2` 對映⚠️ 待確認 |
| `hold.portfolio_count` `[A:無此頁]` | ✅ **有實作** —— 「這本 Sheet 裡的組合」卡（**字面 key `hold.portfolio_count`**，live 值＝`N 本`）。〔GB 線框 live 逐字〕「🟢 **N 本**／· 讀法：**只數「這本 Sheet 裡有幾本組合」，不讀任何一列持股內容**」 | `page_hold.py::build_portfolio_count_card` ← **同一份 `::load_binding(req)`**（**⛔ 不另發一次呼叫**）。欄位 `BindingState.portfolio_count`。**TTL `TTL_15MIN`**（同 `hold.binding`）／**fallback** 同上〔GA；GB 端 TTL／fallback ⚠️ 待確認〕 | **無計算**（只讀計數；**只數組合本數、⛔ 不讀持股內容**） | 🔴 **「還沒綁」與「綁了但空」拆成兩張卡 —— 這是結構性解法、⛔ 不是文案技巧**（檔頭逐字：「前者要你去綁，後者要你去填 —— 指路句完全不同，混在一起等於**對其中一半的人指錯路**」）⇒ **⛔ 沒有任何一格需要同時代表兩件事**，故本卡與 `hold.binding` **⛔ 不得合併**。**沒綁時本卡是 `idle`（⛔ 不是 empty）** —— 逐字「沒有人問過『那本裡有幾本』，因為根本還沒有那一本」，且與 L3 契約對齊：`BindingState.portfolio_count=None` 註解逐字「未知（未綁 or 讀取失敗）；**⛔ 不腦補 0**」；〔GB〕`idle` 逐字有**三種來源、三句話**（①冷啟動還沒按／②按了但選「只看市場」／③讀了但根本還沒綁），「共用一句會寫出『上一張卡顯示你還沒有綁定』這種**不存在的事**」。**空組合 → `empty`（灰）#7 ⚠️ 規格要求；實作畫 `▨`**（傳 `reason=binding.count_missing_reason`），文案逐字「**這是一個有效的結果**（已經讀完，⛔ 不是還沒讀、也⛔ 不是故障）—— 空的組合就是空的，本站⛔ 不把它畫成紅色錯誤」（＝ v3 §02「假性錯誤滿版」）。〔GB〕`missing`＝「▨ **讀不到本數** ＋ `⚠︎` ＋ 就地寫明原因。**⛔ 不會顯示 `0`** ——「讀不到」與「真的是 0 本」是**兩件事**」→**#7**（`INDICATOR_SPEC §1` 禁令 1：缺值⛔ 不得用 `0` 表達）；`error`→**#6** ＋ 原始錯誤訊息，⭐ **本頁把被上游吞掉的失敗還原成紅色**（上游為了不擋住全域狀態列把它降級成中性，**本頁不沿用那個降級**：真故障要看得見）；`na`／`partial`／`degraded`／`unwired`＝**null**。可重試：✅（去 📁 組合管理填完再按）。**編號 ⚠️ 待確認**；`INDICATOR_SPEC R-1／R-3` |
| `hold.setup.preview` `[A:無此頁]` | ✅ **有實作** —— 「持股列預覽」卡（**字面 key `hold.setup.preview`**，live 值＝`N 列（持有 X · 觀察 Y）`）＋ **`st.dataframe` 六欄表**：代號／名稱／種類／**張數**／**均價**／來源（兩組逐字一致）。⭐ 線框逐字「**觀察清單那幾列的張數／均價顯示「—」，不是 0** —— 那份分頁只有「清單名／代號／更新時間」**三欄，本來就沒有**張數與均價。**寫 0 會讀成「持有 0 張、成本 0 元」，那是一個結論，不是缺值**」 | 🔴 **本列直接推翻「HOLD 頁拿不到部位大小」那個舊前提**〔GA 更正〕：`page_hold.py::build_holdings_preview_card` ＋ `::_render_holdings_preview` ← `::load_holdings` → **L3 `src/services/holdings_service.py::get_holdings`** → **L1 `gsheet_portfolio.py::list_portfolios`／`::load_portfolio`／`::list_stock_watchlists`／`::load_stock_watchlist`（四支全 `TTL_15MIN`）**。**兩張表，⛔ 不是一張**：`::load_portfolio`（worksheet `portfolios`，`_HEADERS`＝`name／ticker／lots／avg_price／updated_at` **5 欄**）→ `held=True` **帶 `lots`＋`avg_price`**；`::load_stock_watchlist`（worksheet `stock_watchlist`，`_STOCK_WATCHLIST_HEADERS`＝`name／ticker／updated_at` **3 欄**）→ `held=False`、`lots`／`avg_price` **誠實留 `None`（⛔ 不填 0）**。⭐ **每一次呼叫都顯式帶 `sheet_id=`**（L3 檔頭逐字踩過的坑：`sheet_id` 是**快取鍵的一部分**，傳 `None` 會讓**換一本 Sheet 之後 15 分鐘內拿到上一本的資料**＝張冠李戴）。中文名 best-effort ← L3 `dividend_station_service.py::resolve_holding_names`（失敗留空、⛔ 不捏造）。**fallback ⚠️ 待確認** | **無計算**（原樣搬運）＋ 計數 `N 列（持有 X · 觀察 Y）`。⭐ `gsheet_portfolio.py::parse_portfolio_records` 已保證 `lots>0` / `avg_price>0` 才進來；L3 逐字「原樣搬運，**⛔ 不補值、⛔ 不四捨五入**」（`CLAUDE.md §1` ⛔ 不加工使用者的成本）。**去重**：同一檔同時在 Portfolio 與 Watchlist ⇒ **只留 `held` 那一列**（比對走 L0 `dividend_station_thresholds.py::normalize_ticker`，否則 `2330` 與 `2330.TW` 會被當兩檔）—— 逐字理由：否則換股建議會**叫你買你已經有的東西**。⭐ **「—」不是 0 這一條是 code 端已經做對的事**（`::_fmt_lots` 逐字「`None` → `'—'`（**⛔ 不是 0**）；**零股⛔ 不被四捨五入掉**」），線框**追認並寫死它、⛔ 不得在改版時退回 0**〔GB〕 | 🔴 **張數／均價缺時顯示 `—`，⛔ 不顯示 0**（見左欄）。⭐ **`CLAUDE.md §1` 兩半不同處置（L3 `holdings_service.py` 檔頭逐字）**：**投資組合讀取失敗 → `raise`**（⛔ 不 best-effort、⛔ 不回半份 —— 80/20、未實現損益、停利的**分母都是整份清單**，少一半算出來的百分比**看起來完全正常、實際上是錯的**）；**觀察清單讀取失敗 → ⛔ 不 raise**，記在 `watchlist_error`，**但⛔ 不可被呼叫端吞掉**。⭐ **`more_portfolios` / `more_watchlists` 必須講出來**（否則使用者會以為畫面上就是他的全部部位）。⭐ **表格上方 `st.caption` 逐字「唯讀 —— 要新增／修改／刪除請到既有的 📁 組合管理分頁」**。⚠️ `::_render_holdings_preview` **⛔ 不判態**（狀態在卡上）。〔GB 十格〕`empty`＝**兩種空表、兩句話**（①還沒綁／②綁好了但裡面還沒有任何一列持股 ——「⭐ 這與上一種**不是同一件事**：你已經綁好了，**缺的是內容**」）→**#7 ⚠️ 規格要求；實作畫 `▨`**；`partial`＝「⚠️ **觀察清單那半讀不到**（持股本身不受影響）—— **⛔ 不會靜默略過**：就地寫明它會讓 ④ 換股對照的「換入」**退回選股池全自動排名**」→**#9 ⚠️ 規格要求**；`error`→**#6** ＋ 原始錯誤訊息，⭐ **本站⛔ 不顯示半份清單**；`missing`／`na`／`degraded`／`unwired`＝**null**。**`D-3` 第 #7 處落點**（若該表顯示目標比例欄：完整版 ＋「**這一欄不會寫回你的 Sheet**」）。可重試：✅。線框 flags `['★待拍板（目標比例欄）','合規補正','單組結論']`；`S1-5_HOLDINGS_MODEL` 逐字對映 ⚠️ 待確認 |
| `hold.setup.pick_sheet` `[A:無此頁]` | 🟡 **已實作，但內容是 `wired=False` 灰卡** —— 「Sheet 選擇」未接線卡（**字面 key `hold.setup.pick_sheet`**，label「Sheet 選擇」）。〔GB 十格逐字〕`live`＝「**本區塊沒有「正常顯示」這一態**」／`idle`＝「**恆為未接線**（⛔ 不會因為「還沒按」就改寫成「尚未執行」—— **按一百次也一樣**）」／`loading`＝「**⛔ 不畫骨架** —— 骨架會讓人以為「等一下就會有」」 | `page_hold.py::SETUP_UNWIRED_SPECS` 的 `UnwiredSpec` → `::build_unwired_card`。**`wired=False` ⇒ 恆 `UI_UNWIRED`、⛔ 零取數**〔GA〕。facts 兩列：「為什麼」＝「選 Sheet 會**改寫你的綁定設定** —— 那是寫入」／「現行入口」＝既有 📁 組合管理分頁。⭐ **十格都是「恆為未接線」⛔ 不是偷懶，是 code 端的實際行為**：`::build_unwired_card` docstring 逐字「`wired=False` → `classify_ui_state` 第 1 條規則**直接判 `unwired`，與請求與否無關**：未接線的東西**不會因為多按一次而改變**，這正是它與『查不到』必須分成兩態的原因」（`S1-4 §1.2.2`：**`unwired` 是閘門、⛔ 不是並列項**，「先問這一格接線了嗎，再問這一輪叫過了嗎。**順序不可顛倒**」）。**TTL 無快取／fallback 不適用** | **無計算**（靜態） | 🔴 **⛔ 不是缺 L3，是缺授權**（`::READONLY_WHY` 逐字：「這一項本質上是**寫入**…而本頁對你的持股帳本**一律唯讀** —— 本批⛔ 不實作任何寫入路徑，也⛔ 不在畫面上放一顆按了會改資料的鈕」）。⭐ `::build_setup_unwired_cards` docstring 逐字：「寫成『補一支 L3 就會有』是**假的**：就算補了，本頁也不會做那件事」。**十格 0 null、全 #5 `⛔`；可否重試：❌**（`::READONLY_WHERE` 逐字「本頁**刻意沒有這個出口**」）。⭐ **本格逐字給出 #5 vs #8 的分界（可直接引為規格依據）**：`na` 那格逐字「**⛔ 不會改標 `N/A`** —— `N/A` 是「**結構上不適用**」，本項是「**該有、但本頁刻意不做**」」，與 `UI_COMPONENTS §2` #5／#8 一致；`error` 那格＝「**恆為未接線**（**別處出錯⛔ 不會把這張卡一起染紅** —— 它本來就沒有在跑）」。⚠️ **要在本頁做，必須先由客戶拍板「這一頁可以寫入」**，再補一支寫入型 L3 並補上二次確認與失敗回報。⚠️ 指路句必須含「**先把左邊側欄切回『不使用（用下方原功能）』**」這一步。線框 flags `['★待拍板（這一頁可不可以寫入）','合規補正']`。**編號 ⚠️ 待確認** |
| `hold.setup.watchlist` `[A:無此頁]` | 🟡 **已實作，但內容是 `wired=False` 灰卡** —— 「觀察清單管理」未接線卡（**字面 key `hold.setup.watchlist`**，label「觀察清單管理」）。十格同 `hold.setup.pick_sheet`（**恆為未接線**） | 同 `page_hold.py::SETUP_UNWIRED_SPECS` → `::build_unwired_card`〔GA〕。facts 兩列：「為什麼」＝「新增或移除觀察清單會**寫回你的 Sheet** —— 那是寫入」／「現行入口」＝既有 📁 組合管理分頁 ／ 選股網的「加入觀察清單」。目標 worksheet＝`stock_watchlist`（**3 欄**）〔GB〕。**TTL 無快取／fallback 不適用** | **無計算**（靜態） | 同 `hold.setup.pick_sheet`：**⛔ 不是缺 L3，是缺授權**；**十格 0 null、全 #5 `⛔`；可否重試：❌**。⚠️ **唯讀由測試釘死**：`tests/test_p04_hold_view.py::TestReadOnly` 以 **AST 黑名單**禁止整個 gsheet 寫入面（`save_portfolio`／`delete_portfolio`／`save_stock_watchlist`／`add_to_stock_watchlist`／`create_new_sheet`／`rename_sheet`…）一個都不准出現 ⇒ **⛔ 不是自律，是 CI 紅燈**。⭐ **本卡是全葉唯一有「新五頁內可達」入口的未接線卡**〔GB〕：現行入口有**兩個** —— (1) **先把左邊側欄切回「不使用（用下方原功能）」**，再到 📁 組合管理；(2) 留在新五頁的話，到「🔍 找標的 › 選股網」用「加入觀察清單」，**第 2 個不必切側欄**。⚠️ `S1-4 §1.5` 規則 2 要求 **unwired 的 `where` 應「明說沒有出口」**，本卡**給得出替代出口** ⇒ **文案刻意與 #5 通則不同**（同型先例見 `DATA_MAP_WHY` 的 `why.edu.legacy`）；⚠️「**不可重跑**」與「**有替代出口**」是兩件事、⛔ 不可混寫。線框 flags `['★待拍板（這一頁可不可以寫入）','合規補正','未驗證']`。**編號 ⚠️ 待確認** |
| `hold.evi_cost` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 展開佐證區塊**（成本口徑）。〔GB 線框〕`live`＝「展開＝成本口徑說明：**加權移動平均** vs **FIFO** 的差別、本頁用的是哪一種、以及兩者在同一筆持股上會差多少」；其餘九格＝「展開器**常駐可點**（靜態說明，不依賴本輪資料）」 | 🔴 **0 命中**〔GA，`page_hold.py` grep `hold.evi_cost`／`成本口徑` ＝ **0**；`section_header` 全清單（10 處）無 n4 佐證段〕。⚠️ **產生器 `HOLD_LAYOUT` 的 `src` 逐字自陳**：「`UI_PAGE_HOLD ①` 五層結構表「第四層」列 ＋ ⑦ 成本口徑（**客戶 2026-09-16 裁示先不放**）」⇒ **規格端本來就寫著「先不放」** ⇒ **GA 程式碼端實測（0 實作）與規格端一致，⛔ 不是漏做**。〔GB〕**`D-2` 落點**＝`S2-UI_SPEC §9.3`「**頁4 葉1（③ 下方）＋ 頁4 葉2 ＋ 再平衡區塊**／第四層展開佐證內／展開後常駐」，✅ **三個落點全在本頁內**（**⛔ 不像查一檔頁是落點偏離**）；**(b) 實測**：`FIFO`／`加權移動平均` 在 `page_hold.py` grep **各 0 命中**，`avg_price` 是**使用者自己填在 Sheet 的欄位** ⇒ **系統不計算成本**。**TTL／fallback 不適用** | 無計算（無實作）。⚠️ **算式規格在 `S1-5 §2`（設計文件，狀態逐字「未建表、未寫 migration、未改動任何 repo」）**：核心式 **`F ≡ B ÷ (q·C)`**（平均買入匯率為**衍生值**，`B`＝台幣成本基底為累加的一等狀態）⇒ `q·C·F ≡ B` 恆等、無誤差；等價於**用原幣成本金額加權**。**FIFO 差異文案草稿在 `S1-5 §5`**（91 字版 ＋ 56 字備用版），該節末逐字「⚠️ **這兩段是 UI 文案 → 落在 `CLAUDE.md §-1.5` A-8 / v3 §03-2 ①（新增視覺元件），須隨線框一起送客戶拍板，本階段⛔ 不得直接寫進畫面**」 | **無態可判**（無實作）；〔GB〕`na`＝**null**。🔴 **⛔ 硬規則「L1 未補前不得放」（客戶 2026-09-16 裁示逐字）**：「D-2 逐字文案：**先不放**。畫面不能寫『本系統採加權移動平均法』，因為**實作沒有成本計算**。逐字文案**列為獨立任務**，等 L1/L3 補完才放。寫進規格，標明『**L1 未補前不得放**』。」⚠️ **落點判定⛔ 不受本裁示影響**：「**本頁是 `D-2` 的正確落點**」**仍然成立、⛔ 不得刪除** —— **落點對**與**時機未到**是兩件事；L1／L3 補完後落點仍是本頁。🔴 **合規硬規則**：⛔ **在「實作不存在」成立的前提下，畫面上不得寫「本系統採加權移動平均法」**（那會是宣稱一件系統沒做的事，違 `CLAUDE.md §1`）；⛔ **使用者看到的字面不得出現「FIFO」「加權移動平均」「D-2」「L1」這類內部詞**。**編號＝`S2-UI_SPEC §9.3` 的 `D-2`**（⚠️ ⛔ **不是** `INDICATOR_SPEC §5` 的 `D-02`）。線框 flags `['未驗證']` |
| `hold.evi_take_profit` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 區塊**（停利達門檻的佐證）。〔GB 線框 live〕「**門檻：未實現報酬率 ≥ +15%**／· 門檻來源：**系統內建的策略常數，不是你設定的，畫面上也沒有地方可以改它**／· **只判衛星（個股）**：核心（ETF）不套這條規則／· **缺均價的那幾檔不判**…**單獨列出，⛔ 不會靜默剔除**…⚠️ **這是一張客觀對照表，不是行動清單**」 | 🔴 **0 命中**〔GA，grep `hold.evi_take_profit` ＝ 0〕。⚠️ **佐證內容在實作端是併進 `hold.take_profit` 卡的 facts／`Note`**（門檻 **L0 `shared/dividend_station_thresholds.py::SATELLITE_TAKE_PROFIT_PCT = 15.0`**），**⛔ 沒有獨立 n4 塊**；產生器 `src` 逐字「＋ ⑧ 末段達門檻那一條」。清單由 **L3 `dividend_station_service.py::flag_take_profit`**（`held` ＋ 種類=="個股" ＋ 損益% ≥ 15.0）；持股／均價來源同 `hold.scope_note`（**`TTL_15MIN`**；**fallback ⚠️ 待確認**）。⭐〔GB〕**本區塊是為了接住 ⑤-a 被降級下來的個股明細而新開的**（紅隊 C「F-3」第 2 步：「第一層**只給計數與門檻**，個股代號降到〔展開佐證 ▸〕」）。**本塊自身 TTL／fallback 不適用** | 無計算（無獨立實作）。〔GB 應然〕未實現報酬率 ≥ `SATELLITE_TAKE_PROFIT_PCT(15.0)`；「超出門檻」欄＝報酬率 − 門檻（pp）。⚠️ **未實現報酬率本身的算式規格端未給** ⇒ ⚠️ 待確認（`HOLD-G-07`／`GB ⓗ`）；且 `S1-1 §1.4` 已指出現行 `capital_gain` 是**純價格報酬 × 今日匯率**、**⛔ 不含匯兌損益** | **無態可判**（無獨立實作）。⚠️ ⛔ **不可寫成「已實作」** —— facts 列是 `hold.alloc_deviation` 容器底下那張卡的一部分，`HOLD_LAYOUT` 要的是 n4 **一個獨立的 1/1/1 塊**。〔GB 十格應然〕`empty`＝「常駐可點 —— 展開後寫「**這一輪逐檔判過了，沒有任何一檔衛星達門檻**」。⭐ **這是有效結果，不是錯誤，也⛔ 不加 `⚠︎`**」；`missing`＝逐檔列出**因為缺均價而判不了**的是哪幾檔（**⛔ 不會併進「沒達標」**）；`na`＝「這一輪沒有衛星部位可判」（**不加 `⚠︎`**）→**#8 ⚠️ 規格要求**；`degraded`→**#4**（⭐ **觀測照出、判決留白**的標準寫法：門檻已失準時**照樣把代號與報酬率列出**，但**移除「超出門檻」那一欄**）；`partial`→**#9 ⚠️ 規格要求**；`unwired`＝**null**。⭐ **為什麼「降到第四層」不是換個地方違規**：差別在**誰按的** —— 第一層是**系統主動推到你眼前**的結論，第四層是**你自己點開**的佐證（`S1-3B §2.4`：展開佐證本來就是「就地展開、預設收合、**使用者主動觸發**」）—— 「一份使用者主動索取的門檻命中明細，與一份系統主動遞上的名單，**言語行為不同**」。⚠️ **但這個豁免有邊界、⛔ 不得被擴大引用**：成立前提是**展開區內⛔ 不得出現任何行動語**。⚠️ 合規逐字：⛔ **「你設定的停利門檻」是事實錯誤**，一律寫「**系統內建的**」。**編號 ⚠️ 待確認** |
| `hold.evi_target_pct` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 區塊**（「為什麼目標比例不會被保存」）。〔GB 線框 live 逐字〕「你的持股帳本（Google Sheet 那本）目前是 **5 欄**…**沒有「目標比例」這一欄。**⚠️ 而且即使加上第 6 欄，現行的存檔流程仍會**把它清空** —— 存檔時是「**整表清空、再照 5 欄重寫一次**」…**每存一次就被擦掉一次，而且不會有任何錯誤訊息**⇒ …這一欄**只存在於你這一次的瀏覽器分頁裡**。**關掉就沒了。這不是你操作錯誤。**」 | 🔴 **0 命中**〔GA，grep `hold.evi_target_pct` ＝ 0；`目標比例` 命中 **7 處但全部在 `hold.deep.rebalance` 的 `UnwiredSpec` 文案與檔頭註解內**，⛔ 沒有獨立卡〕。產生器 `src` 逐字「＋ ④ D-3 落點 #8（技術原因完整版）」；`HOLD_VERBATIM_ALLOW_WEISHENME` 逐字收錄線框 `name`「▸ **為什麼目標比例不會被保存**」（23 個 name 裡**唯一**命中「為什麼」三字者）。🔴 **技術原因兩組同測、逐項命中**：`gsheet_portfolio.py::_HEADERS` 確為 **5 元素**；`::save_portfolio` 的 `new_rows.append([name, tk, lots, avg, ts])` 確為**位置式寫死 5 寬**；存檔確為 `ws.clear()` → `append_row(_HEADERS)` → `append_rows(keep_rows)` → `append_rows(new_rows)`；另 `page_hold.py` 的 `st.session_state[...] =` 指派 **AST 0 處**，唯一寫入在 `_ui_kit.py` 且只寫 `{"mode": ...}` ⇒ **重新整理即消失**。⭐ **對照組（同檔內已有正確寫法）**：前進式驗證寫入走 `[[r.get(h,'') for h in _FT_HEADERS] for r in rows]` ——**依欄名取值** ⇒ **修法在同一個檔案裡已經有現成範本**。**TTL／fallback 不適用**（靜態揭露） | 無計算（無獨立實作） | **無態可判**（無獨立實作）。⚠️ **技術原因在實作端已充分說明，只是住在別的 block**：`::DEEP_SPECS` 的 `hold.deep.rebalance` facts 四列（「卡住的層」「持股清單 ✅ 已接線」「L3 wrapper ✅ 已補」「為什麼不拿現況當目標」）＝ 本塊本來要承載的內容 ⇒ **內容在、獨立塊不在**。〔GB〕`missing`＝「常駐可點（**狀態 A 時尤其要看得到** —— 它解釋了為什麼輸入欄是空的）」；`degraded`＝「常駐可點（**狀態 C 時尤其要看得到**）」；`na`／`unwired`＝**null**。⚠️ **殘留風險（`S1-5 §4.2` 逐字，無法用程式消除）**：舊版本 app（另一台裝置、Streamlit Cloud 尚未 redeploy）仍跑舊碼 ⇒ 存檔時一樣會清空新欄，「**這是部署期間的真實視窗，設計上無解**」。⚠️ `S2-ENG_SPEC §1.3` 另立**強制前置（同一個 commit 內）**：「修 M2…⛔ **不修就加欄 ＝ 每存一次檔，使用者剛填的目標比例就被靜默清空一次，而且沒有任何錯誤訊息**」。**編號＝`S2-UI_SPEC §9.3` 的 `D-3`，本格＝八處落點的第 #8 處（技術原因完整版）**（⛔ 不是 `INDICATOR_SPEC` 的 `D-03`）；`DECISION_RULES §5`。線框 flags `['★待拍板（要不要加第 6 欄）']` |
| `hold.evi_notready` `[A:無此頁]` | 🔴 **真正未接線 —— 沒有獨立的 n4 區塊**（N 計數器／未判說明）。〔GB 線框〕`idle`＝「▸ **有 N 項本來就還沒做好**（點開看是哪些）〔摺疊，但**標題本身永遠在**〕」；`live`＝「（正常時本區塊**不出現** —— 沒有未接線／失準／錯誤的項目可列）」 | 🔴 **0 命中**〔GA，grep `hold.evi_notready`／`notready` ＝ **0**〕。⚠️ **N 計數在實作端存在，但住在 `hold.conclusion_cards` 與 `hold.war_table`**：`::build_confidence_card` live 值＝`{judged}/{total_lights} 盞給得出判定`、`::build_todo_card` facts「未判 N」＝逐字「**有幾列判不全** —— ⛔ 不是「沒事」，是還不知道」、燈牆 caption「每一列右邊的 N/M 與上面 ① 的可信度是**同一把尺**」⇒ **⛔ 沒有獨立 n4 塊**；產生器 `src` 逐字「＋ ⑤-G6（N 計數器）」。🔴 〔GB G6〕**沒有計數器**：未接線固定 **4 張**（`DEEP_SPECS` 2 ＋ `SETUP_UNWIRED_SPECS` 2），**畫面上⛔ 不顯示 N**；**(c)** 需要一個**跨區塊彙總**（把本輪所有 `unwired`／`degraded`／`error` 的項目數加總成 N）。**TTL／fallback 不適用**（彙總自本頁各 block 狀態） | 無計算（無獨立實作）。〔GB 應然〕**N ＝ 本輪 `unwired` ∪ `degraded` ∪ `error` 的項目數**；⚠️ **規格只給語意、未給精確集合定義**（未定義「項目」的計數單位 —— **block？卡？盞燈？**）⇒ ⚠️ 待確認（`HOLD-G-08`／`GB ⓚ`） | **無態可判**（無獨立實作）。🔴 **⚠️ 這裡有一個真的缺口**：`UI_COMPONENTS §2` 的 **#9「資料不完整 `◧` N／M 計入」** 正是這塊要用的徽章，而 **`◧` 全 repo 0 命中**（`page_hold.py`／`page_inspect.py`／`shared/ui_state.py` 皆 0）⇒ **N/M 只以文字呈現、⛔ 沒有徽章**。⚠️ **`UI_COMPONENTS §2` fail-safe：「N／M」分子分母若拿不到 → 一律降級顯示為 #7，⛔ 不得退回 #1** —— 〔GA〕**未在實作端找到對應分支**（L4 `station_cards.py::is_fully_judged` 空列回 `False`、`::cruise_or_gap` 不准說「沒事」屬**語意上的 fail-safe**，但**⛔ 沒有任何「降成 #7」的徽章分支**）⇒ 無法判定是「已用別的方式滿足」還是「未落地」＝ `GA ⓖ`。⚠️ **N ⛔ 不得寫死**（`S1-3B §3.4.9` 第 5 項逐字：「前輪稽核說 **4 項**，**本組未複驗**；畫面上⛔ 不得寫死一個數字，應由 `wired=False` 的**實際盞數即時算出**」；`UI_PAGE_HOLD` 沿用該限制，**同樣未複驗那個 4**）。⚠️ **依 fail-safe，N 算不出來時 ⛔ 不得退回「沒有未完成項」** —— 那是把缺口掩蓋成完整（違 `CLAUDE.md §1`）。⚠️ **與 B-07 的邊界**：冷啟動可以把「⬜ 尚未載入」摺成目錄，但**未接線／失準／錯誤三類 ⛔ 不摺**；`error` 逐字「**絕不摺起來，各自一張紅卡** ＋ **原始錯誤訊息**」。⭐ 本塊即 `S2-UI_SPEC §2.2 ★-02` 的**逐元件例外**載體。`empty`／`missing`／`na`／`partial`＝**null**。**編號 ⚠️ 待確認** |

### 2. ⚠️ 待確認與規格洞（客戶本輪**未授權**動 `DATA_MAP_GAPS.md` ⇒ **本節一行帶過，⛔ 不進 GAPS、⛔ 不另立第二份**；**編號沿用原組原樣，⛔ 未重編、⛔ 未補零統一**）

- **規格洞**：〔GA 程式碼端〕`GA-I-4` 匯兌損益**應然 #8、現況不畫**（＝缺口 `H6`）／`GA-I-5` 帳本缺「目標比例」欄（`DECISION_RULES §5`：屬**設計變更**，須先過 `CLAUDE.md §-1.5` A-8 UI 草稿先行 gate）／`GA-I-6` **n0／n1／n4 整層在實作端缺席**（n1 全缺、n4 全缺、n0 三塊中兩塊被搬進葉2）；另 `GA-I-1`（`#7`／`#8`／`#9` 未落地）與 `GA-I-7`（chrome 兩頁 layout 不一致）**兩頁通用**。〔GB 規格端〕`HOLD-G-01` 匯兌損益符號**兩份規格相反**（`UI_PAGE_HOLD G4` 的 `N/A` vs `S1-5 §4.5` 的 `—（需要買入匯率）`）／`HOLD-G-02` `hold.ai_summary` **線框十格無免責句、但實作有**（線框漏收自己的樣板來源）／`HOLD-G-03` 容忍值裁示**涵蓋不到 B 態**（B＝逐檔偏離容忍 `5.0pp` vs C＝總和容差 `1.0pp`，兩個不同的量；⚠️ **GB 自標為本輪新提判讀、⛔ 非既有登記，請優先覆核**）／`HOLD-G-04` ⑤-a **容忍帶查無**（只有 inline `abs(dev)>=1` ＝ `D-31`，規格端未給應然值）／`HOLD-G-05` 換股**判定式與排序式查無**／`HOLD-G-06` 情境試算**算式查無**／`HOLD-G-07` **未實現報酬率算式查無**／`HOLD-G-08` `hold.evi_notready` 的 **N 無精確集合定義**／`HOLD-G-09` `DEEP_CAPTION` **自相矛盾尚未修**（且規格端沒給正確文字）。
- **⚠️ 待確認（9 條，⛔ 未猜、⛔ 未發明資料源／算式／中文標籤）**：`GA ⓓ` VIX 的 **CBOE CDN 備援**是否存在於現行 code path（`CLAUDE.md §2.1` 有記、`::fetch_vix` 只見一條路且失敗回 `None`）／`GA ⓕ` **AI 失敗退避與金鑰池**策略（未讀 `src/services/app_ai_service.py`，TTL／退避／只快取成功結果三項皆未驗）／`GA ⓘ` `hold.run_scope`／`hold.scope_note` 的**位置差異**是規格洞還是實作偏離；`GB ⓕ` 換出判定式與換入排序式／`GB ⓖ` 情境試算算式／`GB ⓗ` 未實現報酬率算式／`GB ⓘ` **本頁** VIX 的 TTL 與 fallback 規格值／`GB ⓚ` `evi_notready` 的 N 計數單位／`GB ⓛ` 本頁線框有無 `D-1` 頁尾 block。**另跨兩頁共用**：`GB ⓑ`（全部 block 的 fallback 順序，`S2-ENG_SPEC §3.3` 逐字「repo 裡不存在」）。**上表另有 13 個 block 寫「編號 ⚠️ 待確認」**（＝`GB F-2` 對不上任何 `D-*` 者）。
- ⚠️ **誠實揭露（`CLAUDE.md §-2` 規則 6）**：本檔素材為 **GA（程式碼端）／GB（規格端）兩組單組產出，⛔ 未互相複驗，本撰寫組亦⛔ 未回 code 複驗**；GA **⛔ 未實跑任何一頁、⛔ 未跑 pytest**（上表「畫面顯示什麼」全是**讀 code 推得的，⛔ 不是看畫面看到的**），GB **⛔ 未讀任何 `.py`**（凡 GB 標「實測」者皆為規格或線框自己記載的實測），且 GB **⛔ 未讀 `S1-3B_FULL_DRAFT_SPEC`／`S1-3_IA_WIREFRAME` 正文**（凡標「`S1-3B §x.y` 逐字」者實為二手轉錄）⇒ **若該正文另有 `HOLD-G-05`／`-06`／`-07` 說「查無」的算式，那幾條洞會被推翻**。所有「0 命中／只有 N 處／全部」皆為**單組窮舉的全稱句**（`§8.2.A.0` 規則 2 禁窮舉宣稱）⇒ **只能當待驗事項，⛔ 不得作為後續動作的前提、⛔ 不得寫進 commit message／PR 描述當成已完成的事實**。⚠️ **`UI_PAGE_HOLD` 自身的複驗分級＝總管實查 4 項 ＋ 客戶裁示 6 項，其餘一律單組**〔GB 原級轉錄、⛔ 未升級〕。⚠️ **線框 `live`／`partial` 的數字一律是規格示意值、⛔ 非實測**（例：`hold.conclusion_cards` 的「12 檔 · 3 加碼 · 1 汰換」、`hold.rebalance_deviation` 全表數字、`hold.scope_note` 的 `09/13 08:41`、`hold.scenario_calc` 的 `−176,200.00`）。**量測值一律帶日期**：GA AST 量測日 **2026-09-22**・HEAD `415c184`，GB 量測日 **2026-09-22**，本檔彙整日 **2026-09-23**。
