# STRATEGY_INTAKE — 外部策略提示語納入前盤點

**這是什麼**：客戶有一份外部策略提示語，內容為三套方法論（總經配置／財報五大數字選股／週期循環定位）。
**本檔是納入前的盤點，不是採用決定** —— 採不採用、怎麼收斂，由客戶在 §4 裁示。

⚠️ **誠實邊界（全檔遵守，讀本檔前必看）**：**該外部提示語的逐字原文並未落檔於本 repo。**
§1「核心主張」層級只能來自客戶親自交代的兩件事：(a) 三套方法論的名稱與領域；(b) 客戶給的六條合規改寫規則
（由此可反推原文含「加碼／買進／停損／出場／具體標的／具體比例」這類語句）。⛔ 故本檔**不宣稱**該提示語說過
任何具體數字、門檻、標的或比例；§1 一律寫成**方法論層級的通用主張**（例：「以景氣位階決定股票曝險高低」），**不是**「它說要配置 60%」。

**免責聲明（全檔只講一次）**：本檔與本專案所有輸出均為**資料揭露與客觀對照**，
不構成任何投資建議、買賣要約或獲利保證；所有數值僅供使用者自行判斷參考。

---

## §1 三套方法論核心主張盤點

> 「缺值時顯示」只准用既有語彙（SSOT：`docs/v2/spec/UI_COMPONENTS.md` §2 徽章 #7／#8）：
> **`⚠︎ —`＝缺漏・可重跑**（上游這輪失敗）／**`N/A`＝不適用・重跑無效**（本機根本無此資料源）。
> 圖示與文字皆為必填，⛔ 不得只靠顏色、⛔ 不得填 0、⛔ 不得發明第三種。

### §1.1 總經配置方法論（7 條）

| 主張 | 判定 | 適用範圍 | 需要的資料欄位 | 現有規格對應 | 缺值時顯示 |
|---|---|---|---|---|---|
| 以多項總經燈號綜合判讀市場體制 | 可納入｜已有 16 盞燈 | 兩者 | `shared/macro_buckets.py` DangerSpec 16 盞（另 2 條 `REFERENCE_BUCKET` 參考走勢） | `STRATEGY_MANUAL.md` §2 市場體制引擎 L65-140 | `⚠︎ —` |
| 通膨走向影響資產相對吸引力 | 可納入｜CPI 已接線 | 兩者 | `us_core_cpi`（美國核心 CPI YoY） | `shared/macro_buckets.py` | `⚠︎ —` |
| 資金動能鬆緊作為領先訊號 | 可納入｜已接線 | 兩者 | `m1b_m2_gap`（M1B-M2 資金動能） | `shared/macro_buckets.py` | `⚠︎ —` |
| 以景氣位階決定股票曝險高低 | 需改寫｜曝險屬處方值 | 兩者 | `src/services/macro_state_locker.py::get_macro_state` 的 `regime`（5 值） | `docs/v2/spec/DECISION_RULES.md` §5 配置偏離三套目標 | `⚠︎ —` |
| 市場波動度上升時降低風險暴露 | 需改寫｜降曝險＝處方 | 兩者 | `vix`；⚠️ 門檻雙軌 | `macro_buckets` 走 `MACRO_THRESHOLDS['VIX']`（黃 22）vs `src/compute/risk/risk_radar.py::VIX_WARN_LEVEL=25.0`／`VIX_PANIC_LEVEL=30.0` | `⚠︎ —` |
| 以利率水準判定估值壓力 | 需改寫｜僅有絕對值 | 兩者 | `us10y`（10Y 公債殖利率）；⚠️ 亦為雙軌 | `shared/macro_buckets.py` | `⚠︎ —` |
| 依總經位階給出具體配置比例（股債幾成） | 不可納入｜處方值違母法 | 兩者 | 不適用 | `S1-6` 母法「嚴禁預設最佳配置推薦」 | `N/A` |

### §1.2 財報五大數字選股法（8 條）

| 主張 | 判定 | 適用範圍 | 需要的資料欄位 | 現有規格對應 | 缺值時顯示 |
|---|---|---|---|---|---|
| 以 EPS 高低作為獲利能力排名 | 可納入｜已在 composite | 個股 | `eps_high`（composite 5 因子之一） | `src/services/fundamental_screener_service.py`；`S1-2_METRIC_SSOT.md` 有 EPS | `⚠︎ —` |
| 以本益比高低作為估值排名 | 可納入｜已在 composite | 個股 | `pe_low`；`src/data/stock/yield_pe_fetcher.py::fetch_pe_name_maps` | 同上；`S1-2_METRIC_SSOT.md` 有本益比 | `⚠︎ —` ⚠️ 見下註 D-08 |
| 毛利率作為競爭力篩選 | 需改寫｜僅門檻不進分數 | 個股 | `src/data/stock/mops_bulk_fetcher.py::fetch_mops_income_bulk` | `src/compute/screener/fundamental_prescreen.py::run_fundamental_prescreen`（prescreen 門檻） | `⚠︎ —` |
| 營益率／淨利率作為經營效率篩選 | 需改寫｜同上僅門檻 | 個股 | 同上 | 同上 | `⚠︎ —` |
| 負債比作為財務穩健篩選 | 需改寫｜同上僅門檻 | 個股 | `mops_bulk_fetcher.py::fetch_mops_balance_bulk` | 同上 | `⚠︎ —` |
| ROE 作為股東報酬率排名 | 需改寫｜無專屬 fetcher | 個股 | `src/data/core/financial_statements_fetcher.py::fetch_financial_statements`（僅原始值） | **不在 composite**；`S1-2_METRIC_SSOT.md` 無 ROE | `⚠︎ —` |
| 月營收 YoY 動能作為成長性排名 | 需改寫｜無資料回中性分 | 個股 | `src/data/stock/monthly_revenue_fetcher.py::fetch_monthly_revenue` | `src/compute/scoring/scoring_engine.py::calc_revenue_yoy_score` 無資料回 `50.0` | `⚠︎ —` |
| 以五大數字合成單一買進名單 | 不可納入｜名單＝處方 | 個股 | 不適用 | `S1-6` 母法禁用語「建議買進」 | `N/A` |

> **本節兩項技術前提（實測）**：① composite 因子**固定只有 5 個** `{pe_low, eps_high, shortage, rs_leader, trend}`
> ⇒ 五大數字裡**只有 EPS 與本益比真正進排名**，毛利率／營益率／淨利率／負債比**只做 prescreen 門檻**，
> **ROE／PB／殖利率／自由現金流／存貨週轉完全不在 composite 內**。② **D-08**：`fetch_pe_name_maps` 對 NaN
> 或 ≤0 一律 `continue`、key 不入 map ⇒ **虧損股（PE 為負）與「這輪沒抓到」在 map 裡同為「沒有 key」**，無法分辨。

### §1.3 週期循環定位法（7 條）

| 主張 | 判定 | 適用範圍 | 需要的資料欄位 | 現有規格對應 | 缺值時顯示 |
|---|---|---|---|---|---|
| 以景氣同時指標定位當前循環位階 | 可納入｜NDC 燈號已接線 | 兩者 | `ndc_signal`（NDC 景氣對策燈號） | `shared/macro_buckets.py` | `⚠︎ —` |
| 以 PMI 判定製造業擴張或收縮 | 可納入｜已接線 | 兩者 | `ism_pmi`（台灣 PMI） | `shared/macro_buckets.py` | `⚠︎ —` |
| 以出口訂單 YoY 作為外需領先訊號 | 可納入｜已接線 | 兩者 | `tw_export`（台灣出口訂單 YoY） | `shared/macro_buckets.py` | `⚠︎ —` |
| 以殖利率曲線利差判定循環轉折 | 不可納入｜本機無資料源 | 兩者 | `本機無資料源`（10Y-2Y／10Y-3M 全 repo 僅 `src/ui/tabs/tab_edu.py` 教學字串，無任何 fetch） | 無 | `N/A` |
| 以央行資產負債表擴縮判定流動性循環 | 不可納入｜本機無資料源 | 兩者 | `本機無資料源`（WALCL 同上，僅教學字串） | 無 | `N/A` |
| 以景氣×通膨二維框架決定類股輪動 | 不可納入｜計算實作已刪 | 兩者 | `本機無資料源`（`merrill_clock.py` v18.359 整檔刪除，無實作承接） | 僅 `src/ui/tabs/tab_edu.py` 教學文字 | `N/A` |
| 以循環位階推導進出場時點 | 不可納入｜時點＝處方 | 兩者 | 不適用 | `DECISION_RULES.md` 為狀態語氣非動作語氣 | `N/A` |

> **GDP 同屬「常數存在但無資料餵入」**，與上表後三條同一類，故不另立一行。⚠️ **別誤認**：
> `shared/sector_flow_thresholds.py` 的「四象限」是**板塊資金**象限，**不是**總經循環象限。

---

## §2 三套方法論的衝突點（**並陳，⛔ 不代客戶決定**）

| # | 衝突 | 一邊主張 | 另一邊主張 | 現有規格是否已規定 |
|---|---|---|---|---|
| C1 | 同一時點三套給出相反位階判讀，誰優先 | 總經燈號轉弱 | 財報排名仍居前／循環位階仍在擴張 | **否**。`DECISION_RULES.md` §6 衝突解決矩陣只規定「總經 vs 個股技術面 → 總經贏」，**未規定三套方法論之間** |
| C2 | 時間尺度不一致 | 財報選股為**落後**指標、**季頻**（公告日約季後 45 天） | 週期定位為**領先**指標、**月頻** | **否**。無跨頻率合成規則 |
| C3 | 輸出型別不同、無法直接相加 | 總經配置要的是「整體曝險百分比」 | 財報選股給的是「個股百分位排名」 | **否**。兩者單位與值域皆不同，相加無定義 |
| C4 | 循環法核心輸入本機無資料源 | 方法論要殖利率利差／央行資產負債表 | 本機只有 `ndc_signal`／`ism_pmi`／`tw_export` | **否**。若以替代指標頂替，**等於換了一套方法論**，不再是原方法 |
| C5 | 同一指標雙軌門檻 | `macro_buckets` 走 `MACRO_THRESHOLDS`（VIX 黃線 22） | `risk_radar.py` 走 `VIX_WARN_LEVEL=25.0`／`VIX_PANIC_LEVEL=30.0`；US10Y 亦雙軌 | **部分**。兩者不同源不同值；三套若各引一軌，同一天可給相反燈號 |
| C6 | 缺值語意在 repo 內不一致 | 正確側：`fundamental_prescreen.py::_safe_ratio`（分母≤0→NaN）、`yield_pe_fetcher.py::get_pb_ratio`（雙源皆敗回 None）、`v5_modules.py::calc_dividend_yield_357`（缺→`est_yield=None`＋未評估） | 違反側：`tw_stock_data_fetcher.py::calc_financial_metrics` 六個比率缺分母一律 `else 0.0`；`financial_statements_fetcher.py::_v()` 查無永遠回 `0.0`；`fuzzy_get_from_df(default=0.0)`；`scoring_engine.py::calc_revenue_yoy_score` 無資料回 `50.0` | **是，但未統一落實**。三套合流會把「沒資料」放大成「算得出來的位階」 |

---

## §3 合規改寫規則

### §3.1 客戶六條規則 vs 既有 S1-6 比對

| 客戶規則（語彙類別） | S1-6 現行做法 | 張力在哪 |
|---|---|---|
| 「買進」→ 改寫為「位階偏低」 | 母法禁用語含「建議買進」；`S1-6` §1.3 裸標籤（如「🟢甜甜價」「便宜價」）⛔ 違反母法 | **有**。「位階偏低」若單獨出現即是**裸標籤**（形容詞不帶數字），遮蔽檢查不過 |
| 「出場」→ 改寫為「觸發觀察條件」 | S1-6 既有用語為「出場條件」 | **有**。兩版用語分歧，客戶版更嚴（不含動作動詞） |
| 「具體標的」→ 移除 | 母法禁「推薦」 | **有**。需先界定「使用者自己持倉的代號」算不算具體標的 |
| 「具體比例」→ 移除 | 母法「嚴禁預設最佳配置推薦」；再平衡一律「偏離提示、客觀對照、情境試算」 | **有**。需先界定「目前偏離 +3.5pp」這類**觀測**百分比算不算具體比例 |
| 「加碼」→ 改寫為「偏離提示」 | 母法逐字已規定再平衡一律「**偏離提示、客觀對照、情境試算**」；母法禁用語另含「應該加碼」 | **部分**。客戶的替代詞「偏離提示」**正是母法原文用語**，在**再平衡脈絡**下完全一致；但 S1-6 §2.2／§2.4 在**非再平衡脈絡**（個股評分、殖利率位階）對「加碼」用的是更具體的門檻缺口（例：「距 7% 門檻尚有 {gap}pp」）而非「偏離提示」⇒ 非再平衡脈絡該用哪一種，未定 |
| 「停損」→ 改寫為「跌破關鍵位階」 | §2.4 已將「🛑停損:跌到這裡要認賠出場」改寫為「🛑**型態失效價**:**跌破此價位**視為型態不成立」 | **邏輯一致、用語分歧**。兩版同樣用「跌破 ＋ 位階／價位」且都不含動作動詞；但 S1-6 叫「型態失效價」、客戶叫「跌破關鍵位階」⇒ 需登記對映，否則日後分叉 |

### §3.2 總管的拆解

⚠️ **以下四點是 AI 總管的判讀，非客戶原文，客戶一句話即可推翻。**

- **觀測值 vs 處方值**：`S1-6` §2.0 逐字「**改寫幾乎一律會讓資訊量「變多」而不是變少**…
  **如果你的改寫讓句子變得更模糊，那是改錯方向了。**」⇒ S1-6 要求保留的數字是**觀測值**
  （殖利率 4.12%、距門檻 2pp、目前偏離 +3.5pp）；客戶要移除的是**處方值**（該配置 60%、建議買 1,234 股）。
  **兩者不衝突：觀測值留、處方值刪。**
- **標的同理**：使用者自己持倉裡已存在的代號屬**觀測**，保留；外部策略推薦的代號屬**處方**，移除。
- **「位階偏低」不得單獨出現**（否則即 `S1-6` §1.3 禁的裸標籤），
  **必須與產生它的數字同一個渲染單位**（標籤本身就帶門檻，遮蔽檢查才會過）。
- **「觸發觀察條件」比 S1-6 的「出場條件」更嚴**，新內容採客戶版；
  **登記兩版用語分歧**（§4-6），避免日後分叉成兩套詞彙表。

---

## §4 待客戶裁示（依 `CLAUDE.md` §-1.5.D §03-2：每項均附總管推薦方案）

| # | 待裁示事項 | 總管推薦方案 |
|---|---|---|
| 1 | 要不要納入這三套方法論 | **推薦分階段納入**：先納入 §1 判定「可納入」的 8 條（皆已有接線資料源），「需改寫」8 條隨後，「不可納入」6 條不納入 |
| 2 | C1 三套位階判讀相反時的優先序 | **推薦沿用 `DECISION_RULES.md` §6「上層贏、禁止平均」精神**：總經 > 循環 > 財報，但**只作顯示排序、不作加權合成** |
| 3 | C2 時間尺度不一致 | **推薦各自標示資料頻率與 `as_of` 並列呈現，不做跨頻率合成**（合成會製造一個沒有來源的新數字） |
| 4 | C3 輸出型別不同 | **推薦兩者並列、不相加**（曝險%與百分位排名值域不同，相加無定義） |
| 5 | C4 循環法核心輸入本機無資料源 | **推薦不以替代指標頂替**，該三條維持 `N/A`（不適用・重跑無效）；若客戶要求補源，屬新資料源導入，另案評估 |
| 6 | C5／§3 用語分歧（「出場條件」vs「觸發觀察條件」；「型態失效價」vs「跌破關鍵位階」） | **推薦新內容一律採客戶版（「觸發觀察條件」／「跌破關鍵位階」）**，並在合規詞彙表登記兩版對映，舊文案不追溯改寫 |
| 7 | 「加碼」→「偏離提示」在非再平衡脈絡是否適用 | **推薦「偏離提示」僅限再平衡脈絡**（那是母法原文的射程）；個股評分／殖利率位階等非再平衡脈絡，**沿用 S1-6 既有做法：直接印門檻缺口數字**（例「距 7% 門檻尚有 {gap}pp」），⛔ 不套用「偏離提示」這個詞，避免同一個詞在兩種脈絡指不同東西 |

⚠️ **另登記（本檔只登記事實，⛔ 未動該檔一個字）**：`STRATEGY_MANUAL.md`（909 行）本身有合規負債 ——
實測 **37 行**含買賣語彙（停損／出場／賣出／買進／目標價），例如 L797、L817。是否清理屬另案。
⚠️ **IA 邊界**：UI 既有 IA 為**已核准封閉清單**（`docs/v2/spec/S2-PRD.md`：五頁、十一葉、二十九區塊）。
本方法論若需新增分頁或葉，屬「分頁動線異動」，**必須先出線框草稿送客戶拍板**，不得逕行新增。

---

## §5 複驗分級（依 `CLAUDE.md` §-2 規則 6）

**A. 實測事實（本組於本 repo 實地查證，附檔:符號）** —— composite 固定 5 因子（`fundamental_screener_service.py`）／
prescreen 僅門檻（`fundamental_prescreen.py::run_fundamental_prescreen`）／缺值行為不一致六處（§2 C6 所列檔:符號）／
`calc_revenue_yoy_score` 回 `50.0`／`calc_financial_metrics` 六個比率 `else 0.0`／DangerSpec 16 盞＋2 條 `REFERENCE_BUCKET`
（`shared/macro_buckets.py`）／`get_macro_state` 的 `regime` 5 值含 `unknown`／10Y-2Y、10Y-3M、WALCL 全 repo 僅 `tab_edu.py` 教學字串命中／
`merrill_clock.py` 已刪／VIX 與 US10Y 雙軌門檻／S1-6 母法與 §2.0、§1.3 逐字／`UI_COMPONENTS.md` §2 徽章 #7／#8 語彙／`DECISION_RULES.md` §6 L137-140 與 L112 逐字／`STRATEGY_MANUAL.md` 37 行買賣語彙／`S2-PRD.md` 五頁十一葉二十九區塊。

**B. 總管判讀（非客戶原文，非實測；客戶一句話即可推翻）** —— §1 全部「判定」欄與理由／§1「缺值時顯示」的分派規則（本機無資料源→`N/A`、上游這輪失敗→`⚠︎ —`）／
§2 六項衝突的歸類與是否「現有規格已規定」／§3.2 四點拆解／§4 七項推薦方案。

**C. 未經第二組複驗（⛔ 不得作為後續動作的前提）** —— 本檔全部內容由 WS 撰寫組單組產出，**未經第二組獨立稽核**。三項承重判定請優先覆核：
(1) §1 的 22 條主張是否漏列關鍵主張（**外部提示語原文未落檔，本組無法窮舉比對**）；(2) §2 是否漏列衝突（單組歸納，非窮舉宣稱）；
(3) §3.2「觀測值 vs 處方值」這條切法是**本組提出的政策新增**，非客戶原文推導，需客戶明示確認。
