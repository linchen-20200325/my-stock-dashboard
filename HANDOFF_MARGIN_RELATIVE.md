# 融資餘額燈「相對分位」方案 — 調查與預覽結果（2026-09-24 存檔）

> **本檔為交接存檔**，不是規格書、不是動工授權。
> 依 §-1（沒實際 bug／沒具體需求 → 不要動）：本輪**只調查與預覽，未改任何 `.py`、未改測試、未動 `data_cache/`**。

---

## 1. 客戶要解的問題

客戶要的是「**趨勢**」不是「**水位**」。

實測 `shared/macro_buckets.py` 全檔 `pct_change|rolling|quantile|.diff(|.shift(|ewm` **命中數 = 0**
⇒ **28 盞燈全部只判水位。**

---

## 2. 已存在但未接線的解法

`shared/relative_thresholds.py`（**448 行**）已寫好、`tests/test_relative_thresholds.py` **33 測試綠**、**0 production caller**。

**簽章逐字**：

```
classify_by_pct_rank(series, *, window=756, yellow=0.75, red=0.90, high_bad=True, min_periods=None) -> RelativeVerdict
```

`macro_buckets` 自陳逐字：

> 「絕對門檻已被市值成長淹沒，實測 5,148 億早已穿透兩線 → 燈號恆紅、鑑別力歸零。相對化方案見 shared/relative_thresholds（尚未接線，屬行為變更需另案）。」

---

## 3. 🔴 預覽結論：相對分位**救不了**（這是本輪最重要的發現）

2026 年 122 個有結論日，**A 組（絕對門檻）與 B 組（相對分位）都是 122/122 恆紅、0 轉態**。

**原因**：融資餘額 2026 單邊創新高（**3,509.5 → 5,878.7 億**），**單調上升序列的滾動分位恆等於 1.0**
（實測 2026 pct_rank 中位數 **0.9954**、**40/122 天 = 1.0**、**122/122 天 ≥ 0.90 紅線**）。

⇒ `relative_thresholds.py` docstring 宣稱的「改成相對分位才有鑑別力」，**在本 repo 實測資料上不成立**。

**唯一真實增益**：B 組比 A 組**早約一個月轉紅**（**2025-11-28** vs **2025-12-30**）
＝ **靈敏度差異，不是鑑別力**。

**可行方向（未驗證，僅登記）**：
- 換分母（`margin_leverage_ratio(margin_yi, market_cap_yi)`，但**本地無總市值資料源**，TWII 是指數點位不能當分母）
- 或改判**變化率／動能**

---

## 4. 🔴 順帶查出的**現存資料 bug**（登記，本輪未修）

`data_cache/finmind_margin.parquet` **單位混用**：4,957 筆中

| 筆數 | 量級 | 口徑 | sanity `[500,10000]` 億 |
|---|---|---|---|
| **1,982 筆** | 1e11~1e12 | 元（`MarginPurchaseMoney`） | 通過 |
| **2,975 筆** | 1e5~1e7 | **張數**（`MarginPurchaseVolume`） | — |

**逐日交錯混在整段歷史。**

`source` 欄：**4,930 筆**舊 buggy 路徑、**27 筆**修好的 `:MarginPurchaseMoney:TodayBalance`。

**根因**已記於 `shared/margin_schema.py` 檔頭（B3 事故：cron `scripts/update_macro_history.py` 用個股版寬格式欄位偵測 + `drop_duplicates(keep="last")`），**至今仍在 parquet 裡沒修**。

⚠️ **任何以這份 parquet 為輸入的既有／未來分析，結論都不可信。**
⚠️ 依 §-1（沒指派、沒 bug 觸發不動工）本輪**僅登記，未動它**。

---

## 5. 28 盞燈的歷史可得性分級（⚠️ 單組窮舉，未經第二組驗）

### 總經 16 盞

| 級別 | 盞數 | 清單 |
|---|---|---|
| **A（序列已在手）** | **8** | `bias_240`／`margin`／`vix`／`dxy`／`adl`／`fut_net`／`jingqi`／`us10y`(走 `cl_data.intl` 次源) |
| **A−（本地 parquet 有月頻序列，缺一行 loader）** | **2** | `m1b_m2_gap`(240 月)／`ism_pmi`(170 月) |
| **B（上游有歷史但只取最新值）** | **4** | `health`／`ndc_signal`／`us_core_cpi`／`tw_export` |
| **C** | **2** | `foreign_net`（卡單位未確認）／`news_systemic`（真的沒歷史） |

⚠️ 其中**真能做三年分位的只有 2 盞**（`margin`／`bias_240`，唯二 20 年序列）；
其餘 A 級窗只有 **14~60 個交易日**，只做得了「近 N 日方向」。

### 持股 12 盞

**A 8**／**B 3**／**N/A 1**（`screen_inception` 成立年數，變化率無意義）／**C 0**。

---

## 6. 待客戶裁示（下次接手從這裡開始）

預覽已證明「**(a) 接線 `relative_thresholds`**」**達不到客戶要的效果**。三個後續選項：

| 選項 | 內容 | 成本 |
|---|---|---|
| **(a′) 換分母** | `margin_leverage_ratio` + 找總市值資料源 | 新資料源，成本高 |
| **(b) 改判變化率／動能** | 而非水位分位 | 計算層，但 `relative_thresholds` 現成函式不直接支援 |
| **(c) 先修 §4 的 parquet 單位污染** | 因為無論選哪案，輸入不乾淨結論都不可信 | — |

⚠️ **總管推薦 (c) 優先** —— 理由：前兩案都吃同一份被污染的 parquet，不先修就是拿髒資料做決定。
⚠️ 此推薦**未經第二組驗**。

---

## 7. 本輪的誠實揭露（§-2 規則 6）

- 第 **3**、**4** 節的數字**總管已親自實測複驗**（量級分布、sanity 計數、2026 分位分布），**非僅轉述**。
- 第 **5** 節分級、以及「**口徑判準＝通過 sanity 就是 Money 列**」**為單組結論，未經第二組獨立驗**，**不得當既定前提**。
- 預覽用的 CLEAN 子集是**不規則取樣**（~100 筆/年），故 `window=756`「**列**」≈ **5.5 個日曆年**而非 3 年；
  已跑敏感度 `window ∈ {252, 378, 411, 504, 756, 1008}`，**結論不變**（window ≥ 504 全是 100% 紅、0 轉態）。
- 預覽腳本與圖在 sandbox scratchpad，**未進 repo**。
