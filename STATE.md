# STATE.md — 台股 AI 戰情室 (Stock Dashboard)

> 極簡狀態檔。歷史 PR 紀錄見 git log；技術細節見 `ARCHITECTURE.md` / `DATASTATION.md` / `STRATEGY_MANUAL.md`。

## 專案定位
- **產品**：台股 / ETF 多 Tab 投資儀表板（市場 / 個股 / 組合 / 總經 / ETF）
- **技術棧**：Streamlit + pandas + Plotly + altair（<5）+ FinMind + yfinance + Gemini AI
- **基建**：NAS Squid Proxy + FastAPI 中繼站（個股新聞）
- **目前版本**：v18.228_EtfPriceCentralCache（PR #235 squash merge @ bf25279。**user 訴求延續 v18.227**：「基金跟股票資料下載的很慢」P1-S1 後續。**Explore Agent 盤點**：`fetch_etf_price` 10 個呼叫點散在 portfolio (3) / single (2) / backtest (2) / grp_compare (1) / etf_calc (2)；原 `@st.cache_data(ttl=3600, max_entries=10)` cache key 含 `period`，同檔 ETF 因 1y/5y/10y 不同 period 重抓 2~3 次。**修法**（1 檔 +33/-4）：(1) 新增 `_fetch_etf_price_max(ticker)` 共用底層，`@st.cache_data(ttl=3600, max_entries=20)`，一次抓 `period='max'`；(2) `fetch_etf_price` 改為 thin slicer，呼叫底層 + 用 `_PERIOD_TO_DAYS` 對照表（5d/1mo/3mo/6mo/1y/2y/3y/5y/10y/ytd/max → days）做記憶體切片；(3) 公開簽章 0 改動、10 個呼叫點全免動。**驗證**：py_compile + AST 確認 `_fetch_etf_price_max` 帶正確 decorator + 切片 smoke test（5d/1y/5y/10y/max 順序正確、1y ≈ 262 trading days、unknown period fallback to 5y）全通過。**[邏輯]** cache key 從 `(ticker, period)` 降維至 `(ticker,)`；slicer `df.loc[df.index >= cutoff]` 純 pandas O(n) 操作，n=4000 < 1ms；unknown period fallback 5y default。**[邊界]** yfinance period='max' 首次 call 較慢（1 次性，後續 1hr cache hit）；auto_adjust=True 在 max range 的標準化基準與 5y 差 < 0.5%；新 ETF 上市 < 5y 時 `max` 自然回較短，slicer 直接回全段不爆；empty DataFrame 早期 return。**[效能]** portfolio + single + grp_compare 同檔 ETF 從 2~3 次 yfinance call → 1 次；預估載入 -1~3s；max_entries 20 容納雙位數獨立 ticker。**[Debug]** 補完後 portfolio 1y / single 5y / backtest 10y 同檔 ETF 共用底層 cache；max range 持有 ~17 年 data 切片瞬間。**架構意義**：與 v18.227 stdlib TTL cache 形成「P0 fetcher cache + P1 跨 tab 集中」雙層 IO 優化；ETF tab 端首次達成「一次抓 max、所有 period 切片」設計範式。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / P1-S2 fetch_yf_close VIX 共用快取 / P1-F1 Fund FRED 集中入口 / 等 user 新方向。）

- **前一版**：v18.227_FetcherTtlCache（PR #234 squash merge @ 29dfcce。**user 訴求**：「基金跟股票資料下載的很慢，是不是還有沒有執行 SSOT 的指標？」**雙 Explore 盤點**：Stock 找出 6 慢因（fetch_etf_info 同行雙呼 / fetch_etf_price 跨 tab period 不同 / fetch_etf_dividends 多模組獨立呼叫 / fetch_yf_close VIX 多地無 cache / fetch_finmind_foreign_investor 多地無 cache / fetch_cbc_m1b_m2 + fetch_twse_breadth 無 cache）；核心慢因不是「SSOT 違規重複抓」而是「5 個高頻 fetcher 沒套 cache」。**重大設計約束**：`tw_macro.py:22-24` 明示「不依賴 streamlit」（雙 repo 共用設計），不能加 `@st.cache_data`。**修法**（1 檔 +39/-1，版本 1.0.0→1.1.0）：tw_macro.py 內建 30 行純 stdlib TTL+LRU cache decorator（`_ttl_cache(ttl_sec, maxsize)` 純 functools/time，wrapper 暴露 cache_clear），5 fetcher 全套 `@_ttl_cache(ttl_sec=600, maxsize=4~8)`：fetch_twse_breadth / fetch_finmind_foreign_investor / fetch_cbc_m1b_m2 / fetch_ndc_signal_history / fetch_foreign_consecutive_days。**SSOT 維持**：fetcher 簽章 0 改動、呼叫端 0 改動、模組純度保持（不引入 streamlit）。py_compile + AST（5/5 decorator 落位）+ TTL 行為動態測試（hit/miss/clear/unhashable bypass）三段驗證通過。**[邏輯]** stdlib-only TTL cache，hit 命中跳網路 IO；cache key=(args, sorted kwargs)，unhashable bypass 走原 fn；LRU maxsize 滿時砍最舊。**[邊界]** 5 fetcher 全部入參 hashable（int/str）安全；fetcher 內 fetch_url 失敗回 dict with error → 失敗結果也會被 cache 10 min（避免 retry storm）；ttl 過期自動重抓；模組重 import 不清 cache（cross-rerun 共享）。**[效能]** 總經 tab rerun 5 次網路 IO → 1 次（10 min TTL 內）；預估首屏 -2~4 秒；cache hit O(1) dict lookup ns 級；maxsize 4~8 記憶體 negligible。**[Debug]** 補完後總經 tab 重複 rerun 不再重抓 TWSE/FinMind/CBC；wrapper.cache_clear() 可在未來「全域刷新」按鈕統一清；版本 1.1.0 標記 cache 引入點。**架構意義**：與 Fund #297 v19.64（4 fetcher 補 cache）同步交付「P0 IO 慢因清掃」；P1（fetch_etf_price 跨 tab period 集中、fetch_yf_close VIX 共用）待 P0 驗證後評估。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證載入時間下降 / 等 user 新方向（P1 集中還是新題目）。）

- **前一版**：v18.226_DiagForeignStreak（PR #233 squash merge @ e20bad2。**user 訴求**：「請幫我檢查是否所有指標 總經 個股 ETF 基金 從網路上抓下來的，都有在資料診斷中被監控，如果有缺少，請幫忙補上，記得維持 SSOT」。**Explore Agent 雙路盤點**：`health_inspector.py` 已監控 13 項（VIX / 美國核心 CPI / 台灣 PMI / NDC / 台灣出口 / M1B-M2 / 國際指數 loop / 三大法人 / 融資 / ADL / 先行指標 / 個股 K線+營收+財報 / ETF K線）。**漏監控孤兒項**：`fetch_foreign_consecutive_days`（`tw_macro.py:669`）已抓並存 `st.session_state['_fi_streak_cache']` 但 health_inspector 從未讀取 → SSOT 對齊缺一行。**修法**（1 檔 +6/-0）：`health_inspector.py` 既有「台灣 M1B / M2」`_g_add()` 之後新增 1 行 `_g_add('外資連續日數', 'FinMind TaiwanStockTotalInstitutionalInvestors', 'daily', date_str=...)` 從 `_fi_streak_cache.date_latest` 讀取，consec_days 為 None 時 date_str 傳 None → `_g_add` 標 🔴「未取得」走既有降級流程。AST + py_compile ✓。**[邏輯]** 純新增監控行 0 行為改動既有 13 項；沿用既有 `_g_add()` 簽章 + `_light()` 新鮮度判斷邏輯；session_state 讀取容錯 `or {}`。**[邊界]** `_fi_streak_cache` 缺 → 顯 🔴 未取得 不爆；`consec_days` None → date_str None；fetcher 端 0 改動（fetch_foreign_consecutive_days 簽章不變）。**[效能]** 零新 IO（session_state in-memory ns 級）；不調 FRED API（非 FRED-backed 指標，下次 Release 欄顯「—」沿用 v18.225 邏輯）。**[Debug]** 用戶「全域資料健康總表」第 14 行可見「外資連續日數」daily 新鮮度，連配 `_fi_streak_cache` 是否更新可即時診斷；補完 SSOT「fetcher 抓 = registry 監控」雙端對齊。**架構意義**：與 Fund #296 v19.63（5 + 2 監控項補齊）同步收尾兩 repo「資料診斷孤兒項」清單；雙 repo 監控覆蓋率從 ~85% → 100%。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / 等 user 新方向。）

- **前一版**：v18.225_PmiStaleCacheFredNextRelease（PR #232 squash merge @ a1eb730。**user 訴求兩件**：(1) 改善「資料異常清單」抓不到資料的問題（screenshot 顯示「tw 台灣製造業 PMI」未取得、「台灣出口 YoY」108 天前）；(2) 台股資料診斷補上與基金一樣的「下次 Release 時間」欄。）**T1 PMI stale-cache 救援**：`macro_core.py` 新增通用 snapshot helpers — `_macro_cache_save(key, payload)` / `_macro_cache_load(key)`（90 天 TTL、`cache/macro_snapshot/<key>.json`、任何 IO 錯誤靜默吞不波及 happy path）；`fetch_tw_pmi()` 命中時呼叫 `_macro_cache_save('tw_pmi', result)` snapshot 持久化，10 段全失敗時讀 stale cache 返回 `is_stale=True` + `source='stale-cache(<orig>)'` + `stale_err=<errs>` 標記讓 UI 可區分「真失敗 🔴」vs「stale 可參考 🟡」。**T2 FRED 下次 Release 顯示**：鏡像 Fund `repositories/macro_repository.py` L89-169 的 `fred_get_next_release_date(series_id, api_key)` 函式到 Stock `macro_core.py`（30 天 disk cache @ `cache/fred_release/<series_id>.json` + `/fred/series/release` 取 release_id + `/fred/release/dates` 查未來最近一筆 release date）；`health_inspector.py` 全域資料健康總表 grid `2fr 1.6fr 0.7fr 1fr 1.4fr 0.7fr` → `2fr 1.6fr 0.7fr 1fr 1.4fr 1fr 0.7fr` 新增「下次 Release」第 6 欄；`_g_add()` 加 `fred_series_id=''` 參數，FRED-backed rows 透過 `@st.cache_data(ttl=86400, show_spinner=False)` 包覆的 `_next_release_cached(series_id, api_key_present)` lazy 查（disk 30d + Streamlit 1d 雙層 cache 防 rerun 重打 API）；FRED_API_KEY 從 `os.environ` / `st.secrets` 取（既有 pattern），無 key 時直接回 `''` UI 顯示「—」；首個 FRED-backed item 為「美國核心 CPI YoY」(CPILFESL)。改動 2 檔 +201/-5（macro_core +167 / health_inspector +39/-5）。**py_compile ✓ / ruff new code 0 errors**（15 pre-existing E701/E702 全在 legacy code）/ smoke 4 case 全綠（macro_core import + fred_get_next_release_date(empty) → None + _macro_cache_save/load round-trip + TTL 90 天到期回 None + stale fallback shape）。**[邏輯]** stale cache 純 module-level helpers 零依賴；fred_get_next_release_date 100% 鏡像 Fund 版本（含 30 天 TTL + 兩段查詢 + 失敗回 None）；UI 端 Streamlit @st.cache_data 多包 1 日 cache 形成兩層保護，rerun 不重打 FRED API。**[邊界]** PMI 90 天 TTL 覆蓋月度公布 + 假日緩衝；過期或解析失敗回 None；FRED_API_KEY 缺 → `_next_release_cached('', False)` 直接回 ''；fred_get_next_release_date 任一步驟失敗（HTTP/JSON parse/release_id 缺/未來無排程）皆回 None；UI 顯示「—」不報錯；非 FRED 指標（VIX/台股相關等）不傳 fred_series_id → row 仍顯示「—」。**[效能]** PMI cache：純 JSON IO 微秒級；FRED release：首次冷打 ~2-3s，30 天 disk cache 命中後零 IO；Streamlit 1d cache 命中再 0 ms；總 API 呼叫頻率 max 每月 1 次/series。**[Debug]** 「資料異常清單」中 PMI 從「🔴 抓不到/過舊」改為「🟡 stale-cache(原 source)」可繼續參考；「全域資料健康總表」第 6 欄「下次 Release」首批支援美國核心 CPI YoY (CPILFESL)，未來其他 FRED-backed 指標（如 M2/Fed Rate）依需新增 `fred_series_id=...` 即可。**架構意義**：完成「stock 端資料診斷視覺 = 基金端」對齊的下一步 — 過去 fund 端 release schedule 來自 `repositories/macro_repository.py`，stock 端首次擁有同款 FRED API 整合；stale-cache fallback pattern 通用化（`_macro_cache_save/load` 任何 macro indicator 都可呼叫），未來可推廣至 NDC 景氣燈號 / 台灣出口 YoY 等多 SSOT 來源的 fetcher。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / 等 user 新方向。

- **前一版**：v18.224_EtfNormTickerPlus4SSOT（PR #231 squash merge @ 40b8f22。**user 3 訴求中 Stock 端 2 件**：(1)「多檔的 ETF 尾巴不需要加 .TW，請比照辦理，單一基金與組合 ETF 也可以去掉 .TW 找到」；(2)「多檔的 ETF 總表請幫我補上與單一基金分析中的 ETF 折溢價、7% 存股估值買賣點、配息健康度 + 年化報酬、品質評等」。）**T1 .TW 規範化 SSOT**：`etf_helpers.py` 新增 `normalize_etf_ticker(raw)` 純函式 — 純 4-6 碼台股（含主動式 ETF `00980A`）自動補 `.TW`，其餘原樣大寫去空白；空 / None → ''；`_TW_PURE_RE = ^\d{4,6}[A-Z]?$`。3 caller 收斂：`etf_tab_single` L52-56（單檔輸入 + 對照基準雙欄改 `normalize_etf_ticker(text_input(...))`、預設值從 `0050.TW` → `0050`、placeholder 改「台股純數字自動補 .TW」）/ `etf_tab_portfolio` L94-99 + L1138-1143（編輯表迴圈 + 儲存 records 2 處，from etf_helpers import + 用 SSOT 替代 `str(...).strip().upper()`） / `etf_tab_grp_compare.parse_etf_codes` 共用 SSOT（`_TW_RE` regex 刪除直接用 normalize_etf_ticker，原 inline 邏輯消除）。**T3 多檔總表補 4 SSOT 欄**：`etf_tab_grp_compare._fetch_one_etf` 加 4 個 SSOT 補欄 — (a) 折溢價：`calc_premium_discount(_info, _df, ticker)` → `premium_pct` + `stale_nav` 旗標（主動式 ETF NAV stale 顯示 ⚠️）；(b) 7% 存股估值：`calc_avg_yield(_df, _divs, years=5)` + 新 helper `_yield_valuation_zone(cur_yield, avg_yield)` 鏡像 `etf_tab_single` L262-298 孫慶龍策略（≥7%🟢強烈買進 / 5%~7%⚪中性 / 3%~5%🟡適度減碼 / ≤3%🔴獲利了結，avg_yield 缺顯示 "—"）；(c) 配息健康度：新 helper `_dividend_health_label(cur_yield, total_ret_1y, cagr_3y)` 鏡像 `etf_tab_single` L231-246 MK 框架 #1+#2（含息≥殖利率=✅雙贏 +X.Xpp、含息<殖利率=🔴吃本金 −X.Xpp；無配息看 3Y CAGR ≥7%✅達標 / 否則🟡未達標）；(d) 品質星等：已含於綜合分（`compute_etf_quality`）。`render_etf_grp_compare` df 從 13 → 17 欄；`column_config` 加 4 個 Column/TextColumn 配 tooltip 解釋公式與閾值；caption 補「4 SSOT 補欄」說明條。改動 4 檔 +119/-15。py_compile ✓ / ruff All checks passed / smoke **20 case 全綠**（normalize_etf_ticker 8 + _yield_valuation_zone 6 + _dividend_health_label 6）。**[邏輯]** `normalize_etf_ticker` 純函式零依賴；4 SSOT 補欄 100% 複用 `calc_premium_discount`/`calc_avg_yield`/`compute_etf_quality` 既有函式；2 新 helper 100% 鏡像 `etf_tab_single` inline 邏輯零重複實作；3 caller 收斂後 `_TW_RE` 重複定義消滅、改 normalize_etf_ticker SSOT 一處全 4 處同步。**[邊界]** 純數字無效格式 → 原樣回傳大寫；avg_yield_5y `<=0` 或 None → 估值顯示 "—"；cur_yield None → 配息健康「⬜ 資料不足」；total_ret_1y None → 「⬜ 1Y 報酬缺」；premium_pct stale → 表格顯示 "⚠️ NAV stale"；單欄計算失敗 try/except 不波及整檔。**[效能]** 純函式無 IO；`calc_premium_discount` 內含 `fetch_etf_nav_history` ttl_cache（10 日）；`compute_etf_quality` 既有 `@st.cache_data ttl=86400`；ThreadPool 並行 5 不變、首次 ~20s、cache 秒回。**[Debug]** UX 三件升級終局：(1) 單檔 textarea 預設 `0050.TW` → `0050` 提示 user 純數字即可；(2) ETF 組合表單代號欄純數字自動補 .TW；(3) 多檔總表從 13 欄 → 17 欄含折溢價/7%估值/配息健康/星等齊備（user 在 v18.223 既有的 5 星評等基礎上再覆加單檔深度的 4 維 SSOT 視圖）。**架構意義**：v18.223 完成「7 維度加權星等」基礎評分、v18.224 補完「單檔深度 4 SSOT」橫向 PK 視圖 — multi-檔 ETF 評分表至此**覆蓋 11 個 SSOT 指標**（綜合分 + 7 維度 + 折溢價 + 估值 + 健康度），UX 與「單檔深度診斷」場景一致性首次達成。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / 等 user 新方向。

- **前一版**：v18.223_T3UnifyEtfCompare（PR #230 squash merge @ 453970b。**user 訴求兩件**：(1) 個股組合 tab「批次分析」按下就應自動執行 MJ 趨勢分數 + 三階段濾網「結果」要重新設計變整潔；(2) ETF tab 應支援多檔查詢 + 評分表比較。)**T1 個股組合一鍵化**：`tab_stock_grp.py:347` 批次跑完同時鎖 `st.session_state['t3_batch_codes'] = tuple(stock_list_t3)`；L1222 後 MJ + picker 兩段改吃 `_batch_codes` 而非直接吃 `stock_list_t3`，沒按批次按鈕 → 整段不顯示（避免空白佔頁面 + textarea 改動不誤觸發重跑）；`_render_mj_trend_section` 加 `auto_run=False` 參數，True 時跳過「📊 跑 MJ 趨勢分數」按鈕直接跑，加 `_mj_trend_rows_<hash>_<weight>_<yyyymm>` session cache 防 rerun 重跑；`_render_stage_picker_section` 加 `auto_run` 透傳至 `render_tab_stock_picker`。`tab_stock_picker.py:42` `render_tab_stock_picker` 簽名加 `*, auto_run=False`：L101 button check 改 `if not auto_run and not st.button(...)`；L133 加 `_pick_cache_key = f'{key_prefix}_results_{hash(tuple(_tickers))}'` 防 rerun 重打 yfinance + FinMind 並行 5 worker；L209 AI 按鈕改 `if auto_run` 自動跑 + cache（`_ai_cache_key` 防 rerun 重打 Gemini），否則保留既有「按按鈕」path 加同款 cache。**「高息網」場景 `source_label='高息網'` + `auto_run=False`（預設）= 零行為改動**；個股組合場景 `auto_run=True` 完成「按一次批次分析自動串跑 MJ + 三階段 + AI」流程。UX 從「批次按鈕 → MJ slider → MJ 按鈕 → 三階段按鈕 → AI 按鈕」5 步收斂為「批次按鈕」1 步。**T4 ETF 多檔評分（新功能）**：新建 `etf_scoring_helpers.py` 95 行 — 7 維度（`total_ret_1y` 25% / `cagr_3y` 20% / `sharpe` 15% / `mdd` 15% / `expense_ratio` 12% / `aum` 8% / `div_yield_cv` 5%）線性 rescale 標準化（缺項 rescale 有效權重）+ 5 星映射（鏡像 `etf_quality.compute_etf_quality` 0.80/0.65/0.50/0.35 階）。新建 `etf_tab_grp_compare.py` 196 行 — `parse_etf_codes` 解析（純 4-6 碼數字自動補 .TW，美股原樣保留）；`_fetch_one_etf` 線程安全 worker 複用 `etf_fetch.fetch_etf_price/fetch_etf_dividends/fetch_etf_info` + `etf_calc.calc_total_return_1y/calc_current_yield/calc_sharpe/calc_mdd/calc_cagr` + `etf_quality.compute_etf_quality`（零新算法，純既有層 orchestration）；UI：textarea 預設 5 檔（`0050 0056 00878 00919 00929`）+ 「🎯 開始批次評分」按鈕 + `ThreadPoolExecutor(max_workers=5)` 並行抓 5y + `as_completed` 進度條 + 維持輸入順序排序 + 統計 5 metric（🌟5★/⭐4★/✨3★/💧≤2★/❌失敗）+ 評分表（13 欄：星等/綜合分/市價/1Y累積%/3Y CAGR%/夏普/MDD%/費用率%/AUM億/殖利率%/備註）按綜合分排序。`app.py:1538` ETF tab 加子分頁 `st.tabs(['🔍 單檔深度診斷', '📊 多檔評分比較'])` 第一子分頁原樣保留 `render_etf_single`、第二子分頁 lazy import 新模組。改動 5 檔 +401/-74。py_compile ✓ / ruff All checks passed。**[邏輯]** 全部複用既有 etf_calc/etf_quality 函式零重算；compute_etf_quality 自帶 `@st.cache_data(ttl=86400)` 線程內呼叫安全；session_state cache key 含 tickers/權重/yyyymm 多維 hash 避免誤命中。**[邊界]** auto_run=False 既有高息網場景 0 行為改動；t3_batch_codes 缺 → 顯示 info 提示；MJ cache key 含權重變動會 invalidate；ETF parse 解析錯誤 token 跳過；ThreadPool 失敗單檔回 error dict 不阻斷批次；空 _qualified 不跑 AI 不爆；AI cache key 含 _qualified tickers tuple；fetch_etf_info 缺 expense_ratio/aum/beta 走 None → 7 維度 valid_w rescale 不爆。**[效能]** 個股組合一鍵化：零新 API、少 3 個 widget render + 3 次 Gemini token 浪費（cache 防 rerun 重打）；ETF 批次：ThreadPool max_workers=5 × 10 檔 ~ 15-20s 首次（5y 抓取）後 cache 秒回；fetch_etf_price/dividends/info/expense_ratio_safe/compute_etf_quality 全有 `@st.cache_data` ttl 900-86400s 自動命中。**[Debug]** UX 三步收斂為一步（user 直接訴求）；批次按下後 textarea 改動不會誤觸發重跑（鎖 batch_codes）；改 textarea 再按批次自動 invalidate cache 重跑；ETF 7 維度評分公開透明（caption 列權重/門檻），user 可預測哪檔該幾星。**架構意義**：完成「個股組合 + 三階段濾網 + AI 一鍵化」UX 收斂終局；ETF tab 從「單檔診斷」升級「單檔深度 + 多檔 PK」雙模式（子分頁切換 UX 一致性）；evt_scoring_helpers 證明「複用既有計算層 + 純加權合成」可快速建立多檔 PK 評等系統，未來個股組合也可套用同款（7 維度加權合成）的設計範式。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / 等 user 新方向。

- **前一版**：v18.222_PickerT3AutoSel（PR #229 squash merge @ 73003f0。**user 訴求**：「個股組合的股票清單抓最上方的，下方 MJ 趨勢分數 + 三階段濾網的『結果』」。原 v18.221 整合三階段濾網到個股組合 tab 後，picker 內 `multiselect` / `extra_codes` / 按鈕三段 UI 在「個股組合輸入」場景下仍要求 user 二次勾選候選 + 加額外代碼，與「上方已輸入 10 檔」直觀流程衝突。）**修法**：`tab_stock_picker.py:66-95` 條件分流 `source_label` — `'個股組合輸入'` 場景跳過 multiselect/extra_codes 兩段 UI 改顯示「✅ 已自動帶入上方輸入的 N 檔」chip（前 10 檔預覽），`_sel = list(_codes)` 等價手動全選、`_extra_raw = ''`；保留「🎯 開始三階段篩選」按鈕避免一打開 tab 就吃 yfinance IO 風險（user 仍須一鍵觸發）。「高息網」tab（`key_prefix='picker'`）零行為改動 — 完整保留 multiselect/extra_codes/按鈕三段 UI。改動 1 檔 +22/-10。py_compile ✓ / ruff All checks passed。**[邏輯]** 純 UI 條件分流；上游 `stock_list_t3` 既是 user 輸入 10 檔 parse 的純代碼 list，直接 `_sel = list(_codes)` 等價手動全選；高息網場景條件分支隔離零風險。**[邊界]** `'高息網'` 既有 `key_prefix='picker'` 場景行為 0 改動；`_codes` 空時早期 return 邏輯不變；按鈕未按仍 return 不爆 IO；前 10 檔預覽截 chip + `...` 標示溢出避免 UI 過長。**[效能]** 零新 IO；少跑 2 個 Streamlit widget render；spinner 後 Stage 1+2 並行 `ThreadPoolExecutor(max_workers=5)` 不變。**[Debug]** UX 從「二次勾選 + 加碼 + 按按鈕」三步收斂為「按按鈕看結果」一步；MJ 趨勢分數同樣保留按鈕一鍵觸發避免 IO 風暴（風險對稱）；個股組合 tab 三段疊放保留 v18.221 設計：① T3 AI 投資組合總結 → ② MJ 趨勢分數 → ③ 三階段濾網（按鈕觸發路徑統一）。**架構意義**：v18.221 完成「整合」、v18.222 收尾「UX 簡化」— 場景分流 + key_prefix 隔離成為「同 picker 多場景共用」的成熟範式，未來新場景（如 ETF 候選 → picker）可直接接此 pattern。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / 等 user 新方向。

- **前一版**：v18.221_StockGrpStagePicker（PR #228 squash merge @ 43d28ef。**user 訴求**：「個股組合直接結合 MJ 趨勢分數（v18.189）分析 + 智慧選股 三階段濾網；如果有相同公式或來源記得共用程式與來源，勿將程式碼膨脹」）。**重構策略**：`tab_stock_picker.render_tab_stock_picker(...)` 新增 `source_label='高息網'` + `key_prefix='picker'` 兩參數抽參（向後相容，app.py 既有呼叫零行為改動）— `source_label` 動態切換 multiselect 標籤與 prompt 文案、`key_prefix` 隔離所有 `st.*` widget key 避同頁多處渲染碰撞。`tab_stock_grp.py` 末段 `_render_mj_trend_section` 之後新增 `_render_stage_picker_section(stock_list_t3)` 函式 — 把純代碼 list 轉成 picker 需要的最小 DataFrame（`pd.DataFrame({'代碼': stock_list})`），呼叫 `render_tab_stock_picker(gemini_fn=gemini_call, candidates=_df, source_label='個股組合輸入', key_prefix='picker_t3')`。**共用資源**：Stage 1 基本面 9 項與 MJ 趨勢分數共用 `data_loader.fetch_financial_statements` + `financial_health_engine.analyze_financial_health`（同源，零 fetcher 重複）；Stage 3 共用 `gemini_call`（同 T3 AI 投資組合報告同源）；Stage 2 籌碼技術獨立模組（yfinance OHLCV + FinMind 法人 + 集保大戶）。改動 2 檔 +63/-15。py_compile ✓ / ruff All checks passed。**[邏輯]** Stage 1 fetcher + Stage 3 gemini 100% 共用；零行為複製。**[邊界]** picker 內 L50 expander「💡 三階段濾網在篩什麼？」在個股組合 tab 無外層 expander → 非巢狀；`key_prefix='picker_t3'` 隔離 → 高息網 + 個股組合同時打開不衝突；`stock_list_t3` 為 `[]` 時整段 skip 無 None 爆。**[效能]** Stage 1+2 並行 `ThreadPoolExecutor(max_workers=5)`，10 檔約 30s 內完成；零新 API。**[Debug]** 個股組合 tab 三段疊放：① T3 AI 投資組合總結 → ② MJ 趨勢分數（月/季雙頻率）→ ③ 三階段濾網（基本面 9 + 籌碼技術 6 + AI 三型），覆蓋「中長期動能 → 當下進場可行性 → AI 配置建議」完整決策鏈。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證 / 等 user 新方向。

- **前一版**：v18.220_Py314Lxml（PR #227 squash merge @ 1c966ba。🚨 P0 hotfix — Streamlit Cloud 升 Python 3.14.6，原 `lxml>=4.9.0` 被 pip resolver backtrack 至 `lxml==4.9.4`（cp314 無 wheel），嘗試 source build 但 base image 無 `libxml2-dev`/`libxslt1-dev` → installer non-zero exit、app 無法啟動 "Error installing requirements"）。**根因**：Streamlit Cloud build image 一夜升 Python 3.14.6，舊版 lxml 4.x 無 cp314 wheel；某 transitive constraint（疑為 FinMind / dbnomics）強制 lxml<5 → pip 雖一度下了 `lxml-6.1.1-cp314.whl` metadata 但被踢回 4.9.4 source tarball；base image 無 C dev libs → build 失敗。**雙保險修法**：(1) `requirements.txt:11` `lxml>=4.9.0` → `lxml>=5.3.0` 阻止 backtrack 回 4.x（lxml 5.3+ 有 cp314 wheel）；(2) 新增 `packages.txt` 含 `libxml2-dev` + `libxslt1-dev` 做 fallback 雙保險（萬一其他 C ext source build 也需要）。改動 2 檔 +3/-1（新增 packages.txt 2 行 + lxml pin 修 1 行）。**[邏輯]** 純 build env 修復，零行為改動；lxml 5.3+ API 完全相容 4.x，BeautifulSoup / FinMind / pandas.read_html 全等價。**[邊界]** Fund 端 log 確認 `lxml==6.1.1` cp314 wheel 直接抓成功（Fund requirements.txt 無 lxml pin → 自動拿最新），故 Fund 不需修；若未來 PyPI 抽掉 lxml 5.x wheel，packages.txt 仍可走 source build 自救。**[效能]** 預編譯 wheel 安裝零 build time；packages.txt 觸發 apt install 約 5-10s 額外冷啟動但僅 build 階段一次。**[Debug]** `[14:38:07] 🚀 Starting up` → `[14:38:32] ❗️ installer returned a non-zero exit code` 約 25s pip resolve + lxml source build 失敗。**架構意義**：建立「Streamlit Cloud Python 版本飛升」災難回應 SOP — 雙保險（pin lower bound + packages.txt）；後續 K 系列若遇類似 cp314 wheel 缺，可同款式快修。Fund 端 `lxml==6.1.1` 自動抓 cp314 wheel 成功，無需動作。下一步候選：等 Streamlit Cloud Stock app rebuild 驗證通過 / 等 user 新方向。

- **前一版**：v18.219_K4b4bSSOT（PR #226 squash merge @ 46a95c4。K4b-4b 跨 repo `shared/colors.py` SSOT 接收 + `shared/macro_card.py` 9 hex 收斂）。**配對 Fund #290 @ ce3199a**：Fund-side 從零建 `shared/colors.py` 7 hex SSOT（4 TRAFFIC + 3 MATERIAL）後透過 `sync_to_stock.sh` 同步至 Stock；Stock 端 `shared/colors.py` + `shared/__init__.py` 接收 Fund-side mirror（加 DO NOT EDIT 警告）；`shared/macro_card.py` 9 處 Material hex 替換（`#f44336/#ff9800/#00c853` → `MATERIAL_RED/ORANGE/GREEN`）+ 加 `from shared.colors import MATERIAL_GREEN, MATERIAL_ORANGE, MATERIAL_RED` import + header 警告改寫（colors.py 仍 sync 自 Fund、macro_card.py 兩端正式脫鉤各自演化）。改動 3 檔 +31/-28。py_compile ✓ / smoke `_z_color(3, True) == "#f44336"` 行為等價 / Material hex regex re-scan `shared/macro_card.py` = 0（連同 v18.217 K4b-4a 2 檔 5 hex + 本棒 1 檔 9 hex = K4b-4 全 3 檔 14 hex 收斂結案）。**[邏輯]** 純常數參考替換；caller 行為等價（MATERIAL_RED == "#f44336" / MATERIAL_GREEN == "#00c853" / MATERIAL_ORANGE == "#ff9800" 編譯期等價）。**[邊界]** brand 藍 `#64b5f6` / yellow marker `#ffeb3b`/`#000` / dark theme chrome `#11161e/#1f2933/#e6edf3` 保留 inline（非 Material 範疇，避免 scope creep）；`color == "#64b5f6"` default 比較邏輯字面保留。**[效能]** SSOT 常數 module-level 載入零 runtime overhead；無新增 IO / cache。**[Debug]** 改 palette 改 Fund canonical 後跑 `sync_to_stock.sh` 自動推 Stock 端；雙 repo SSOT pipeline 重建完成。**架構意義**：解決 v18.217 STATE.md 標記的「跨 repo sync 腳本路徑漂移」根因 — Fund 端從零建 `shared/` SSOT、修 sync 腳本同步清單（`macro_card.py` → `colors.py`）、雙端 9 hex 收斂、`macro_card.py` 兩端脫鉤各自演化；**K4 系列累計 6 + 339 + 44 + 70 + 188 + 5 + 9 = 661 hex 全收斂**；K4b-4 結案。下一步候選：等 user 新方向（K4b 全棒收官 / K8 LOW Verdict closed / 無已知 outstanding hex）。

- **前一版**：v18.218_K8ThreadAudit（K8 評估報告 — Skip-PR Whitelist 文件改動直推 main，零 .py 邏輯變動。**結論：Verdict = LOW，不需遷移**。Explore Agent 唯讀盤點 `tab_stock_picker.py:122` ThreadPoolExecutor(max_workers=5) 提交 `_check_one_stock()` worker：(1) yfinance 呼叫 2 處 — `L237-238` `yf.Ticker(f'{ticker}{_sfx}').history(period='1y')` + `L385` `yf_ticker.dividends`，皆位於 worker 函式內部；(2) worker 內**零 `st.*` 呼叫** — Streamlit context 完全隔離；(3) **無共享 mutable state** — 無 module-level dict / list / cache 寫入；`yf.Ticker` per-stock 獨立 instantiation 無 singleton 風險；無 `@st.cache_data` 包裝；(4) yfinance 版本 `requirements.txt:6` 無上界 pin（≥0.2.36 — 文檔聲明 thread-safe，內部 requests session per-Ticker 隔離）；(5) 全 repo 共 10 處 ThreadPoolExecutor 一致使用模式（`tab_stock.py:357` mw=7 / `tab_stock_grp.py:181,659` mw=3 / `daily_checklist.py:509` mw≤8 / `leading_indicators.py:940` mw=4 / `macro_core.py:529` mw=N_sources / `data_loader.py:1623` mw=3 / `etf_tab_portfolio.py:165` mw≤6 / `tab_macro.py:749,807` 動態），max_workers 3-7 區間合理。**[邏輯]** 設計符合並行隔離模式（獨立 IO / 無共享狀態 / context 隔離）；caller 行為等價 — 本任務零 .py 改動。**[邊界]** `_check_one_stock` 嚴格符合 thread-safe 候選；其他 9 處 executor 若未來懷疑 regression 另起子任務逐一驗證（本評估僅 K8 指定點）。**[效能]** max_workers=5 對 yfinance proxy 端壓力合理；無迫切遷移需求；維持現狀效能最佳。**[Debug]** 若未來 yfinance 升級 ≥0.3 出現 thread-safety regression，回到本評估作為基準對照基線（基線：requirements.txt yfinance 無上界 / `_check_one_stock` 零 `st.*` 零 global write）。**K8 closed — no code change required**。下一步候選：K4b-4b（跨 repo `shared/macro_card.py` 8 hex SSOT 重建，需先建 Fund-side `shared/colors.py` + 修 sync 腳本路徑漂移）/ 等 user 新方向。

- **前一版**：v18.217_ColorsMigMatlA（PR #225 squash merge @ 17cea34。K4b 第六棒 — K4b-4 拆兩棒之 a 棒：`tab_edu.py` + `health_inspector.py` 共 2 檔 5 處 Material legacy hex 收斂至 `shared.colors.MATERIAL_{RED,GREEN,ORANGE}` 常數參考）。**audit 修正**：STATE.md 原預估「K4b-4 4 檔 16 處」實際 grep 為 **3 檔 13 處**（檔數 -1 / 處數 -3）。**範式調整**：K4b-4 因 `shared/macro_card.py`（8 hex）標 AUTO-SYNCED 自 Fund repo 但 sync 腳本（`my-Fund-dashboard/scripts/sync_to_stock.sh`）指向 `fund/shared/` 路徑已 refactor 至 `ui/components/` 不存在，且 Fund repo 目前無 `shared/colors.py`，故拆兩棒：(1) **K4b-4a（本棒）** Stock-only 2 檔 5 hex；(2) **K4b-4b（延後）** `shared/macro_card.py` 8 hex 需先建 Fund-side SSOT + 修 sync 腳本 + 雙 repo 同步處理。改動 2 檔 +8/-6：(1) `tab_edu.py:184-187`（3 hex）— Z-score 三色 ternary（±2σ red/green、±1.5σ orange、其餘藍 `#79c0ff` 非 Material 範疇保留）；加 `from shared.colors import MATERIAL_GREEN, MATERIAL_ORANGE, MATERIAL_RED` import；(2) `health_inspector.py:460,1306`（2 hex）— 頻率徽章 dict 月頻欄；既有 `from shared.colors import TRAFFIC_GREEN, TRAFFIC_RED, TRAFFIC_YELLOW` import 補 `MATERIAL_ORANGE`（py_lint 字母順序：`MATERIAL_ORANGE` 在前 `TRAFFIC_*` 在後）。py_compile **2/2 ✓** / ruff baseline **3 → 3 零新增**（3 個 E701 為 health_inspector.py L117/L118/L415 pre-existing 與本 PR 無關）/ pytest **1396 passed in 102.20s 零回歸** / 全 repo Material hex regex re-scan：唯一殘留 `shared/macro_card.py` 8 hex（預期 — 延後 K4b-4b 處理）。**[邏輯]** 純常數參考替換；caller 行為等價（`MATERIAL_RED == "#f44336"` / `MATERIAL_GREEN == "#00c853"` / `MATERIAL_ORANGE == "#ff9800"` 編譯期等價）。**[邊界]** tab_edu Z-score ternary 四級分支（±2σ 紅/綠 / ±1.5σ 橙 / 其餘藍）邊界保留原 hex `#79c0ff` 為 GitHub-style 輔助色非 Material 範疇刻意不納；health_inspector 兩個 `_FREQ_COLOR` dict 其他三色 `#42a5f5`/`#ef5350`/`#9e9e9e` 同非 Material 保留。**[效能]** SSOT 常數 module-level 載入零 runtime overhead；無新增 IO / cache。**[Debug]** 改 palette 仍只改 `shared/colors.py` 4 行；**K4 系列累計 6 + 339 + 44 + 70 + 188 + 5 = 652 hex 全收斂**（剩 K4b-4b 8 hex 跨 repo 待規劃）。**架構意義**：發現「跨 repo sync 腳本路徑漂移」問題 — Fund repo 已從 `shared/` refactor 至 `ui/components/` 但 sync 腳本與 Stock 端 AUTO-SYNCED 警告未同步更新；K4b-4b 視為「重建 cross-repo SSOT pipeline」的起點，需 audit 是否 Fund 還有其他 hex 散落（目前 Stock 已有 colors SSOT，Fund 端零基礎）。User 上線驗證：reboot → 教學 Tab 即時值 Z-score chip 三色（紅/綠/橙）+ Raw Data Health Inspector 頻率徽章月頻橘 視覺與 PR 前一致（運行時等值）。下一步候選：K4b-4b（建 Fund-side colors SSOT + 修 sync 腳本 + 雙 repo 同步 `shared/macro_card.py` 8 hex）/ K8（Stock thread-safe yfinance 評估）/ 等 user 新方向。

- **前一版**：v18.216_ColorsMigSmall（PR #224 squash merge @ 54d083a。K4b 第五棒「分散小檔批量遷移」 — 25 檔 188 處 hex 收斂至 `shared.colors.TRAFFIC_{GREEN,YELLOW,RED}` SSOT 常數）。**三層遷移策略**：(1) **Phase 1 bulk script**（156 hex / 22 檔）— standalone quoted (`'#xxx'` / `"#xxx"`) → `TRAFFIC_X` 常數參考；(2) **app.py CSS pretransform**（3 hex）— plain `<style>` block `.health-A/B/C` border bare hex → f-string + 雙重 escape (`{{ }}`) + `{TRAFFIC_X}` 注入；(3) **Phase 2 targeted edits**（29 hex / 7 檔）— CSS-embedded hex 分 4 類：(a) 既有 f-string 內 bare hex → substring replace `{TRAFFIC_X}`（unified_decision / etf_tab_portfolio / data_registry，共 6 hex）；(b) 單行 plain quoted CSS → 加 `f` prefix + `{TRAFFIC_X}` 注入（daily_checklist / etf_tab_single，共 2 hex）；(c) 多行 implicit concat → 首行 `f` prefix（yield_screener，共 3 hex）；(d) leading_indicators 1 comment hex 改文字描述（去 `#f85149`/`#3fb950` 保 `(TRAFFIC_RED)`/`(TRAFFIC_GREEN)`）+ 16 return statement 轉 f-string（共 18 hex，5 distinct pattern × N occurrences + 期望次數 assert）。改動 25 檔 +177/-150：每檔加 `from shared.colors import TRAFFIC_{需要色}, ...` import（按該檔實際用色挑 subset 動態組）。**E402 修正**：`daily_checklist.py:7-10` 將 `urllib3.disable_warnings()` 從同行 `;` 分隔改為 import block 之後單行，避免 shared.colors import 被歸為「非 top」。**Audit 誤差校正**：v18.215 STATE.md 原預估「K4b-3 26 檔 152 處」實際 grep + script 處理為 **25 檔 188 處**（檔數 -1 / 處數 +36 — 多出的 36 處主要為 CSS-embedded 在 audit 時被歸 plain quoted 類）。**Multi-line import anchor 修正**：`etf_quality.py` / `tab_etf_margin_simulator.py` 兩檔 `from X import (` continuation block 被 anchor 插入後手動 Edit 移正（將 shared.colors import 上移至 multi-line import 之上）。py_compile **25/25 ✓** / ruff baseline **279 → 279 zero new** / pytest **1396 passed in 94.96s 零回歸** / 全 repo hex regex re-scan = 0（排除 `shared/colors.py` 定義與 `tests/`）。**[邏輯]** 三層策略 + 期望次數 assert 保護每類；caller 行為等價（SSOT 常數 module-level 一次決議、runtime 純引用與原硬碼編譯期等價、f-string 編譯期決定零 overhead）。**[邊界]** Phase 1 bulk script 6 pattern（3 色 × `'/"`）assert by-color count；Phase 2 7 個 handler 各自 anchor uniqueness assert；leading_indicators 16 returns 5 distinct pattern × expected count 雙重 assert；multi-line `from X import (` continuation 偵測失敗 fallback 由 py_compile 攔截後 Edit 修正。**[效能]** SSOT 常數 module-level 載入時一次決議，runtime 純引用零成本；f-string 編譯期決定與 plain string concat 成本相當；無新增 IO / cache。**[Debug]** 改 palette 仍只改 `shared/colors.py` 4 行；**K4 系列累計 6 + 339 + 44 + 70 + 188 = 647 hex 全收斂**；K4b-3 為 K4b 系列收官棒（剩 K4b-4 4 檔 16 處 Material legacy 與 K8 thread-safe yfinance）。**架構意義**：K4b-3 證明「bulk script + per-pattern targeted edits」雙階段範式可處理「audit 誤判但結構多元」的散落遷移，建立 standalone / CSS-embedded / f-string-bare / plain-CSS-multi-line 四類遷移處理 SOP，未來新 SSOT 遷移可直接套用範式。User 上線驗證：reboot → 個股／組合／ETF／總經各 Tab 三色 chip / health 卡 / 殖利率防禦網 / leading indicators 表格 / NAS Proxy badge 視覺與 PR 前一致（運行時等值）。下一步候選：K4b-4（Stock 4 檔 16 處 Material legacy `#00c853/#f44336/#ff9800`，鏡像 K4b-3 範式）/ K8（Stock thread-safe yfinance 評估）/ 等 user 新方向。

- **前一版**：v18.215_ColorsMigGrp（PR #223 squash merge @ a2e292a。K4b 第四棒 — `tab_stock_grp.py` 1 檔 70 hex traffic-light SSOT 收斂）。**audit 校正**：STATE.md 原預估「K4b-2 中小檔 13 檔 173 處」實際 grep 為 **1 檔 70 處**；剩餘 26 檔 152 處實屬 K4b-3（重定義為「分散小檔批量遷移」）。**70 hex 三類遷移**：(1) 12 處 alpha-suffix quoted（`"#3fb95018"` / `"#3fb95055"` 各 3 / `"#d2992218"` / `"#d2992255"` 各 1 / `"#f8514918"` / `"#f8514955"` 各 2）→ `f"{TRAFFIC_X}<suffix>"`；(2) 57 處 plain quoted literal（ternary / dict / kwarg / tuple，24 GREEN + 11 YELLOW + 22 RED）→ `TRAFFIC_X` 常數；(3) 1 處 bare hex in f-string CSS（L1100 `border-left:3px solid #3fb950;`）→ `{TRAFFIC_X}` 插值。改動 1 檔 +38/-37：1 import 加 `from shared.colors import TRAFFIC_GREEN, TRAFFIC_RED, TRAFFIC_YELLOW`；70 hex 全清。**順序敏感**：alpha-suffix（step 1）必先做避免 step 2 plain quoted 把 `#3fb950` 部分替換留下 `TRAFFIC_GREEN18` 字面 bug；step 0 import → step 1 alpha → step 2 plain → step 3 bare CSS 四步順序 + 期望次數 assert 全通過。py_compile ✓ / ruff baseline **All checks passed == K4b-2 All checks passed**（零新增警告）/ pytest **1396 passed in 110.82s**（**zero regression**）/ remaining hex regex re-scan 為 0（全清）。**[邏輯]** Python 腳本 + step 順序 assert + 期望次數 assert 保護每類；caller 行為等價（SSOT 常數 module-level 載入時一次決議、runtime 純引用零成本與原硬碼字面值編譯期等價）。**[邊界]** 12 alpha-suffix 三常數（GREEN / YELLOW / RED）各兩種 alpha 變體（18 / 55）全分支覆蓋；1 處 bare CSS L1100 anchor 字串唯一性 assert；非 traffic-light hex（如 `#484f58` / `#2ea043` / `#f97316` / `#8b949e` / `#161b22` / `#c9d1d9` 等）刻意保留非 SSOT 範疇（這些是 GitHub-style 灰階輔助色，未來若擴 SSOT palette 再納入）。**[效能]** SSOT 常數 module-level 載入零 runtime overhead；無新增 IO / cache。**[Debug]** 後續調 palette 仍只改 `shared/colors.py` 4 行；K4 + K4b-1 + K4b-1b + K4b-2 共四棒累計 **6 + 339 + 44 + 70 = 459 處遷移**收斂；剩 K4b-3（26 檔 152 處小檔）+ K4b-4（4 檔 16 處 Material）。**架構意義**：tab_stock_grp.py 是 K4b 系列「最後一塊中型檔」；剩餘 K4b-3 為分散 26 個小檔 152 處（多在 `etf_*` / `macro_*` / `ui_widgets` / `health_*` / `daily_checklist` / `leading_indicators` / `merrill_clock` / `unified_decision` 等），K4b-3 預估走「script 批量 + 多檔 git stash 對比」pattern（與 K6b Fund 52 sites + K4b-2 70 hex 同款 Python 腳本 + 唯一性 assert 範式）。User 上線驗證：reboot → 個股組合比較 Tab 的健康度三色 chip / DNA banner / 系統 stress 預警表 / 快照新鮮度 chip 視覺與 PR 前一致（運行時等值）。下一步候選：K4b-3（Stock 26 檔 152 處小檔批量）/ K4b-4（Stock 4 檔 16 處 Material legacy）/ K8（Stock thread-safe yfinance 評估）/ 等 user 新方向。

- **前一版**：v18.214_HealthThresholdsSSOT（PR #222 squash merge @ a262e10。K7 — `health_grade` 閾值 SSOT 化，鏡像 v18.210 K4 colors SSOT 模式）。STATE.md 列為 K7 待辦項「health_grade 閾值對齊（80/50 vs 60/40 inconsistency）」；Explore 勘查發現實際是「不同維度評分系統各有獨立閾值」非真不一致 — 個股健康度 80/50（scoring_helpers）/ tab_macro 廣度評分 60/40 / app 市場曝險 60/40/20 / macro_state_locker 風險級別 70/40 / etf_quality 星評 0.80/0.65/0.50，前者個股域、後 4 者市場/組合/ETF 域，刻意保持獨立。改動 3 檔 +28/-4：(1) 新增 `shared/health_thresholds.py` — `HEALTH_GRADE_A_MIN=80` / `HEALTH_GRADE_B_MIN=50` 兩常數（由 `test_scoring_helpers.py` 11 項測試背書 80/50 為 spec 標準）；(2) `scoring_helpers.py:243-252` `health_grade()` import 常數替換硬碼；(3) `tab_helpers.py:88-94` 投組評分函數同步 import 常數替換。**保守策略**：`tab_stock.py:1069` 的 80/60/else 為「進場教學文字三段顯示」（>= 80 強勢/>= 60 中性偏多/else 偏弱）非 grade 分界，保留現狀避免改 UX 文字；其他散落 60/40 為獨立評分系統不在 health_grade 範疇，保留。py_compile ✓ / ruff **All checks passed** / pytest tests/test_scoring_helpers.py 52/52 ✓ / pytest tests/ 全套 **1396 passed 零回歸**（102.98s）/ import smoke：常數值 + health_grade 三級行為等價（85→優質優良 / 60→震盪盤整 / 30→弱勢危險）。**[邏輯]** 純常數抽取 refactor，零行為改動；caller import 後 inline 替換等價；scoring_helpers 同時走 colors SSOT + thresholds SSOT 雙常數模組。**[邊界]** `HEALTH_GRADE_B_MIN >= score >= HEALTH_GRADE_A_MIN` 邊界值由既存 11 項測試覆蓋（80/50/49.99/79.99 四個臨界值）；tab_helpers 內 `health` 缺欄走 row.get('_health', 0) → 0 落到 C 級不爆。**[效能]** module-level 常數一次決議，runtime 純引用零成本，與原硬碼字面值編譯期等價；無新增 IO/cache。**[Debug]** 改閾值改一處生效；後續若 spec 調 A 級下界從 80 → 75，僅改 `shared/health_thresholds.py:21` 一處兩 callers 同步。K 系列剩餘：K4b-2 中小檔 13 檔 173 處 / K4b-3 小檔 20 檔 109 處 / K4b-4 Material legacy 4 檔 16 處 / K6 Fund `fmt_twd()` / K8 tab_stock_picker thread-safe yfinance。下一步候選：K6（Fund 同步 SSOT 27+ 處鏡像）/ K4b-2（Stock 中小檔批量遷移）/ K8（thread-safe 評估）/ 等 user 新方向。

- **前一版**：v18.213_GrpHealthTable（PR #221 squash merge @ 18fe696。L2 — 組合健診多檔表格化鏡像 Fund L1）。User 訴求延伸「基金的組合健檢，多檔請用表格的方式呈現」之 Stock 端對應；Fund L1 已於 v19.77 @ e22f5b6 把 expander → 合併表，本次 Stock 端對等補完。改動 1 檔 +61：`tab_stock_grp.py:702` 摘要比較表後、既有「個股詳細展開卡片」前，新增 2 張多檔模組比較表 — 經營能力 7 欄（代碼/DSO 應收/DIO 存貨/DPO 應付/翻桌率/完整循環/現金缺口）+ 獲利能力 6 欄（代碼/毛利率/營業利益率/安全邊際/淨利率/ROE）；沿用 L690 摘要表 column_config + TextColumn pattern；空 dict / `_fd.get('error')` 跳過該檔；無資料則不顯示該表頭。既有 expander 卡片完全保留 — 漸進式 UX 升級、零迴歸。py_compile ✓ / ruff **All checks passed** / pytest **1396 passed 零回歸** / import smoke OK。**[邏輯]** 純 UI 局部插入；不動 `_process_one_stock` 或健診引擎；既有摘要表 + expander 完全保留。**[邊界]** `operating_module` / `profitability_module` 空時跳過該檔；`_op_rows` / `_pf_rows` 全空則不顯示該表頭；error stock 過濾；profitability nested dict `.get('Value', 'N/A')` 沿用既存 pattern。**[效能]** 零新增 IO；DataFrame 構造 O(N) N=檔數；視覺化負荷 < 既有 N expander 點開逐一看。**[Debug]** 鏡像 Fund L1「expander → 合併表」pattern；user 多檔比較認知負荷顯著收斂；Fund L 系列兩 repo 對稱完成（Fund L1 v19.77 + Stock L2 v18.213）。K 系列剩餘：K4b-2 中小檔 13 檔 173 處 / K4b-3 小檔 20 檔 109 處 / K4b-4 Material legacy 4 檔 16 處 / K6 Fund `fmt_twd()` / K7 health_grade 閾值對齊 / K8 tab_stock_picker thread-safe yfinance。下一步候選：K6（Fund 同步 SSOT 27+ 處）/ K7（Stock 閾值對齊低風險）/ K4b-2（Stock 中小檔批量遷移）/ 等 user 新方向。

- **前一版**：v18.212_ColorsMigCss（K4b 第三棒 — CSS 內嵌 hex 補完：tab_macro.py + tab_stock.py 兩巨檔剩餘 44 處 CSS 內嵌 hex 改 f-string `{CONST}` 注入，包含 13 處 alpha-suffix 變體 `#3fb95018` → `{TRAFFIC_GREEN}18` 透過子字串自然延伸）。**grep 證據**：tab_macro 15 處（#f85149 4 / #3fb950 1 / #d29922 3 / #6e7681 4 / + 3 plain→f 轉換）+ tab_stock 29 處（純字面 16 + alpha 13；4 plain→f 轉換）。改動 2 檔 +38/-38 淨增 0：(1) `tab_macro.py:31` import 補 `TRAFFIC_NEUTRAL`；3 plain 字串補 `f` 前綴（L411 `f'''` triple-quote / L691 `f'<div...'` implicit-concat 首段 / L3536 `f'<span...'` 條件式）；replace_all `#xxx` → `{CONST}` 涵蓋全部 15 處。(2) `tab_stock.py:24` 同 `TRAFFIC_NEUTRAL` import；4 plain 字串補 `f`（L751 `f'<div...'` 單行 / L1632 `f"""..."""` triple-quote / L2160 `f'<div...'` implicit-concat 首段 / L3242 `f"""..."""` triple-quote）；replace_all 涵蓋 29 處（含 alpha-suffix 自然子字串替換）。py_compile ✓ / AST ✓ / 全檔 import OK / ruff **All checks passed**（零新增警告）/ pytest tests/ test_*.py **1396 passed 零回歸**（v18.211 期 4 個 pre-existing fails 全部隨日期/source 變動自然修復）。**[邏輯]** plain quoted-literal CSS 字串補 `f` 前綴變 f-string；hex 透過 `{TRAFFIC_RED}` 等 SSOT 常數注入 runtime 等值；alpha-suffix 變體 `#3fb95018` 因子字串匹配 `#3fb950` 自然展開為 `{TRAFFIC_GREEN}18` runtime 仍為 `#3fb95018`。**[邊界]** 5 處 multi-line implicit-concat 僅首段補 `f`（其餘段無目標 hex 維持 plain 省 runtime 成本）；triple-quote `'''` / `"""` block 整段補 `f` 前綴；CSS 文字含 `>15%` `<0%` 非 f-string `{` 字面無需 escape；alpha-suffix 不需額外處理。**[效能]** SSOT 常數 module-level 載入零 runtime overhead；f-string 編譯期決定，與原 plain 字串拼接成本相當；無新增 IO/cache。**[Debug]** 改 palette 仍是改 `shared/colors.py` 4 行；K4b-1 + K4b-1b 兩棒收官「tab_macro + tab_stock」兩巨檔完整遷移共 339 + 44 = **383 處**；K4b-2/-3/-4 待續其餘 37 檔。K 系列剩餘：K4b-2 中小檔 13 檔 173 處 / K4b-3 小檔 20 檔 109 處 / K4b-4 Material legacy 4 檔 16 處 / K6 Fund `fmt_twd()` / K7 health_grade 閾值對齊 / K8 tab_stock_picker thread-safe yfinance / K9 Fund pytest pre-existing fix / K10 Stock pytest 4 pre-existing 已自然修復收回清單。User 驗證：reboot → 個股健康度 / 大盤 regime 三色 banner / 月虧損警示 / 利空背離警告 視覺與 PR 前一致（運行時等值）。

- **前一版**：v18.211_ColorsMigBig（K4b 第二棒 — 大檔批量遷移：tab_macro.py + tab_stock.py 兩巨檔 339 處純字面 hex → `shared.colors.TRAFFIC_*` SSOT 常數）。**grep 證據**：tab_macro 165 處（#f85149 65 / #3fb950 56 / #d29922 44）+ tab_stock 174 處（#f85149 53+8 / #3fb950 57+9 / #d29922 45+2）共 **339 處純字面** 全遷；剩 **44 處 CSS 內嵌**（`color:#xxx;` 在 HTML 字串內）留 K4b-1b 後續批單獨處理（需 f-string 轉換非單純 replace_all）。改動 2 檔 +237/-234 淨增 3：(1) `tab_macro.py:30` 加 `from shared.colors import TRAFFIC_GREEN, TRAFFIC_RED, TRAFFIC_YELLOW` + 165 處純字面 hex 替換；(2) `tab_stock.py:25` 同 import + 174 處純字面 hex 替換（含單引號 + 雙引號兩種字面樣式）。`TRAFFIC_NEUTRAL`（#6e7681）本批未涉及純字面（5 處皆 CSS 內嵌歸 K4b-1b）。py_compile ✓ / AST ✓ / ruff **All checks passed**（兩檔零新增警告）/ pytest tests/ test_*.py **1464 passed + 1 deselected** 零回歸（4 pre-existing fails 全在 main 重現：test_adl_fallback_uses_tw_macro_breadth / test_app_uses_tw_macro_m1b_m2 / test_trend_arrow_recent_rebound / test_get_market_assessment_fallback_via_macro_core，非 K4b 引入）。**[邏輯]** `'#f85149'` / `"#f85149"` 純字面字串 → `TRAFFIC_RED` 常數參考；ternary / dict / kwarg 三大主用法等價（運行時 SSOT 常數展開值與原 hex 一致）。**[邊界]** 僅替換 quoted-literal 兩種樣式（`'...'` + `"..."`），CSS 內嵌（如 `f'<div style="color:#f85149;">'`）刻意保留避免 f-string 改造風險；TRAFFIC_NEUTRAL 不涉純字面跳過。**[效能]** SSOT 常數 module-level 載入時一次決議，runtime 純引用零成本；無新增 IO/cache。**[Debug]** 改 palette 從原 339 處掃改變成 `shared/colors.py` 改 4 行；保留的 44 處 CSS 內嵌列入 K4b-1b 後續批清單（需用 `f"color:{TRAFFIC_RED};"` 改造）。K 系列剩餘排毒：K4b-1b CSS 內嵌（44 處 2 檔）/ K4b-2 中小檔批量遷移（13 檔 173 處）/ K4b-3 小檔 + test_ui_widgets（20 檔 109 處）/ K4b-4 Material legacy（4 檔 16 處）/ K6 Fund `fmt_twd()` / K7 health_grade 閾值對齊 / K8 tab_stock_picker thread-safe yfinance。User 驗證：reboot → 個股健康度三色 / 總經 regime 紅綠燈視覺與 PR 前一致（運行時等值）。

- **前一版**：v18.210_ColorsSSOT（PR #218 squash merge @ 5fe5220。K4 第一棒 — Phase 1 audit S3：traffic-light 顏色 SSOT 模組建立 + 3 caller 遷移示範）。**grep 證據**：`#3fb950`/`#d29922`/`#f85149` 散落 **15 檔 110 處**；material colors（`#00c853`/`#f44336`/`#ff9800`）**4 檔 16 處**。改動 4 檔 +53/-6：(1) 新增 `shared/colors.py` SSOT — `TRAFFIC_GREEN/YELLOW/RED/NEUTRAL` 四常數 + `MATERIAL_*` 別名（v18 早期向後相容）+ `TRAFFIC_EMOJI` tuple + `emoji_to_hex(emoji) -> str` 反查。(2)(3)(4) 3 個小/中央 caller 遷移：`macro_stock_link.py:30-31` bull/neutral/bear 三色 dict（2 hex）、`portfolio_linkage.py:98` ✅ 命中 banner border（1 hex）、`scoring_helpers.py:244-248` health_grade 三級 return（3 hex）。py_compile ✓ / pytest **1396 passed 零回歸** / smoke **4 case**（常數值 + emoji→hex 反查 + Material 別名 + health_grade 三級行為等價）全綠。**[邏輯]** 純常數模組零 import 依賴；caller import 後 inline 替換等價。**[邊界]** 未知 emoji → TRAFFIC_NEUTRAL；MATERIAL 別名單純常數無行為差異。**[效能]** 純常數查找；零新增 IO。**[Debug]** 集中後改 palette 改一處生效；K4b 後續可批量遷移大檔（tab_stock_grp 37 / app.py 12 / macro_helpers 12 / 其他 9 檔 / material 16 處）。**保守策略**：第一棒只動 3 個小檔 6 hex 確保 PR 小且 zero 風險；建立 SSOT pattern 後 K4b 可分批清掃，避免「一次改 15 檔」大範圍風險。K3~K8 剩餘：K3 `_auto_fetch_moneydj` 抽 helper（Fund）/ K4b 大檔批量遷移（Stock）/ K6 `fmt_twd()` helper（Fund）/ K7 health_grade 閾值對齊（Stock）/ K8 tab_stock_picker:237 thread-safe 遷移。User 驗證：reboot → 個股健康度 / ETF 持倉聯動 / 大盤 regime 三色 banner 視覺與 PR 前一致。

- **前一版**：v18.209_YfProxy（PR #217 squash merge @ 4ebe5bc。K5 — Phase 1 audit S1 修復：yfinance 直呼從 audit 報「8+ 處」收斂到 1 處 defer，Cloud IP 403 風險根除）。**deep check 修正**：原 audit 過度估計 — etf_fetch.py 4 處 yf.Ticker 全在 @cache_data parent 內、yield_screener:95 在 fetch_dividend_history @cache 內、stock_names:160 有 pkl cache；真未 cache 僅：app.py:363 fetch_dividend_data + daily_checklist:487 fetch_single + tab_stock_picker:237（後者在 ThreadPoolExecutor「零 st.* 線程安全」defer 評估）。改動 3 檔 +107/-41 淨增 66：(1) 新增 root-level `yf_proxy.py` — `_proxy_env()` contextmanager 統一 NAS Squid env var setup（try/finally exception-safe，reuse `tw_stock_data_fetcher._load_proxy_config`）；`cached_history(ticker, period)` + `cached_dividends(ticker)` 雙 wrapper @st.cache_data(ttl=3600, max_entries=200)；抓不到回空 DF/Series 不爆。(2) `app.py:fetch_dividend_data` yfinance fallback 改 `cached_dividends`，省 22 行 boilerplate。(3) `daily_checklist.py:fetch_single` 改 `cached_history` 保留 pkl 30min cross-restart cache 形成兩層保護（pkl 跨 process + st 1h process-internal），省 10 行 boilerplate。py_compile ✓ / pytest **1396 passed 零回歸** / smoke **5 case**（history 成功/失敗→空 DF / dividends 成功/空 / proxy_env 無 proxy 不爆）全綠。**[邏輯]** contextmanager exception-safe；雙層 cache 互補；抓不到回空容器。**[邊界]** proxy_load 失敗→不設 env 直走；yf 例外→print log + 空容器；ticker 空→cache key 仍唯一。**[效能]** 命中 cache 1h 內秒讀；daily_checklist 兩層保護使 process 重啟仍有 30min cache。**[Debug]** print log 含 ticker + 例外型別；reboot 後不再因海外 IP 403 全死。**架構意義**：K5 示範「Phase 1 audit deep check 修正過度估計」 — 真未 cache 僅 3 處，遷移聚焦避免大範圍改動風險。K3~K8 排毒清單：K3 `_auto_fetch_moneydj` 抽 helper（Fund）/ K4 顏色 emoji 常數（Stock）/ K6 `fmt_twd()` helper（Fund）/ K7 health_grade 閾值對齊（Stock）/ K8 tab_stock_picker:237 thread-safe 評估遷移。User 驗證：reboot → 個股查詢 fetch_dividend_data fallback 仍可抓配息（等價 + cache 加速）。

- **前一版**：v18.208_EtfMacroLink（PR #216 squash merge @ 75f7b70。I6 — ETF 投組 ↔ 總經 regime 跨 Tab 聯動 banner，鏡像 I4 個股端 + Fund v19.64 I1 macro→組合模式。改動 1 檔 +9：`etf_tab_portfolio.py:209` `etf_portfolio_rows` session 共享後、資產總覽卡之前插入 `render_macro_stock_backdrop(st.session_state)` 呼叫（直接 reuse 既有 helper — 純讀 `mkt_info` 不分個股/ETF，零新增模組、零新 IO）。三色 banner：🐂 bull → 友善「順勢」/ 🟠 neutral → 無方向 / 🐻 bear → 警示「ETF 投組宜減碼」/ 總經未載入 → 灰提示「載入總經 Tab 後顯示 regime」。py_compile ✓ / pytest **1396 passed 零回歸**。**[邏輯]** reuse `macro_stock_link.render_macro_stock_backdrop`（純讀 mkt_info，與個股版完全等價）；插入位置在 etf_portfolio_rows 已寫入後、資產總覽前，與 I4 同 pattern「freshness/portfolio 寫入 → 大盤背景 → 主畫面」。**[邊界]** 總經未載入→helper 自帶灰提示；try/except + print log 不擋主畫面；mkt_info 缺欄→helper 內部 fallback。**[效能]** 純讀 session_state + HTML markdown ~ms 級，零新增 fetch/cache，零新增模組（reuse > 鏡像 > 新建）。**[Debug]** 與 I4 個股 banner 視覺一致讓 user 跨 Tab 認知統一；ETF 投組 bear 警示與既有「ETF 戰情室規則」呼應。**架構意義**：I6 示範「先 reuse 既有 helper，再考慮鏡像，最後才新建模組」的克制路徑 — 「render_macro_stock_backdrop」名稱含「stock」但純讀 mkt_info 通用，直接 reuse 比為 ETF 另寫一份更 clean。I 系列兩 repo 對稱表更新：Fund I1（macro→組合）/ I2（單檔↔組合）/ I3（穿透集中度）；Stock I4（個股↔總經）/ I5（個股↔投組+組合）/ I6（ETF 投組↔總經）。User 上線驗證：reboot → 載入總經 Tab → 切 ETF 投組 → 資產總覽卡上方應顯示 regime banner。下一步候選：等 user 新方向 / I7 Fund sector 集中度。

- **前一版**：v18.207_PortfolioLinkage（PR #215 squash merge @ 64201d2。I5 — 鏡像 Fund v19.65 I2，補 Stock 端「個股 ↔ 持倉」跨 Tab 訊號聯動。研究單檔個股時讀其他 Tab session_state，顯示是否已在 ETF 投組 / 組合比較中（張數/權重/角色/損益 或 健康度/評級）。改動 2 檔 +111：(1) 新增 root-level `portfolio_linkage.py`（鏡像 `macro_stock_link.py` 配置）`render_stock_portfolio_membership(session_state, sid, name)` — 讀 `etf_portfolio_rows`（list of {ticker, lots, actual_pct, role, capital_gain_pct, ...}）+ `t3_data["results"]`（list of {stock_id, 代碼, 名稱, 健康度, 評級}），by-ticker upper/strip 正規化比對，3 顯示模式：命中綠框（含 🎯核心/🛰️衛星角色 + 損益±%）/ 未命中但 Tab 已載入→灰提示「尚未出現於 ETF 投組/組合比較」/ 兩 Tab 皆未載入→靜默不打擾。(2) `tab_stock.py:604` macro_stock_link try/except 後插入新呼叫（兩聯動 banner 並列：總經背景 + 持倉聯動）。py_compile ✓ / pytest **1396 passed 零回歸** / functional smoke **5 case**（empty silent / ETF hit + 核心 / T3 hit + 健康度 / 兩 Tab 載入無此股→灰框 / case-insensitive + 衛星 -3.2%）全綠。**[邏輯]** 純讀其他 Tab 已寫入的 session_state；by-ticker 正規化（upper+strip）容錯使用者大小寫；3 模式互斥（靜默/灰提示/綠框）。**[邊界]** session_state 兩 key 皆缺 → 靜默；list 內非 dict 過濾；ticker None/空 → 跳過；try/except 外層 + print log 不擋主畫面。**[效能]** 純 in-memory 線性掃描 + HTML markdown ~ms 級，零新增 fetch/cache。**[Debug]** ETF 投組顯張數+權重+角色+損益讓 user 一眼看「重押還是衛星」；組合比較顯健康度+評級讓 user 看「上次群組分析的成績」；兩個訊號互補。I 系列兩 repo 對稱進度：Fund I1（macro→組合）/ I2（單檔↔組合）/ I3（穿透集中度）/ I4（Stock 個股↔總經 backdrop）/ I5（Stock 個股↔投組+組合）。User 上線驗證：reboot → 載入 ETF 投組 5 檔 → 切個股查其中一檔 → freshness 條下方應顯示綠框「✅ 已在 ETF 投組」。下一步候選：I6 ETF 投組↔總經 regime / 等 user 新方向。

- **前一版**：v18.206_EtfPortfolioParallel（PR #214 squash merge @ f014e29。H2 — ETF 投組批次抓價/配息序列 for-loop → ThreadPoolExecutor 並行化（鏡像 Fund v19.68 H + Stock v18.195 個股 6-IO 模式）。問題：`etf_tab_portfolio.py:129` 原 `for r in rows: fetch_etf_price(...) + fetch_etf_dividends(...)` 每檔 2 個 yfinance 呼叫序列累加，5 檔 ETF 約 10s wallclock。改動 1 檔 +33/-13：抽 `_fetch_one(_tk, _shares)` worker（無 `st.*` 呼叫 → thread-safe，回 `(price, div_amt, price_end_ts)`）+ ThreadPoolExecutor(min(N, 6))（6 worker 對 yfinance/proxy 友善）；`as_completed` 主執行緒收集到既有 `_cur_prices`/`_div_received` dict（by-key），下游邏輯零異動；`_pf_price_end` 取所有 future max。py_compile ✓ / pytest **1396 passed 零回歸**。**[邏輯]** worker 純抓 + 純算（_cutoff 預在 main 算好傳入）；by-key 收集自然解決並行順序問題。**[邊界]** N 檔皆失敗 → dict 補 0.0 不爆；`_p_end` 解析失敗 → None；任一 future 例外 → 賦零三件組。**[效能]** N 檔 wallclock：Σ(每檔 2s) → max(每檔) × ceil(N/6)；5 檔約 10s → 2s（5×）。**[Debug]** spinner 仍顯「抓取現價與配息資料...」；現價/已領配息/損益數值與序列版完全等價。效能系列收斂：v18.195 個股 6-IO + v18.206 ETF 投組 N-IO 兩處關鍵 cold-start 並行化齊備。User 上線驗證：reboot → ETF 投組 Tab 載入 5+ 檔 → spinner 時間明顯縮短，數值與序列版一致。下一步候選：I5 跨 Tab 聯動延伸。

- **前一版**：v18.205_SidebarAI（PR #213 squash merge @ 5aa1a78。G2 — 鏡像 Fund v19.67 G，在 sidebar 資料健康 🔴/🟠 時顯示「🤖 AI 解讀資料異常」按鈕，按需呼叫 Gemini 解釋偏舊原因與建議動作。改動 1 檔 +72：`sidebar_health.py` 新增 `_call_gemini_brief(prompt)` helper（鏡像 ai_engine.py 多模型嘗試：gemini-2.5-flash / 2.0-flash-exp / 1.5-flash-latest / 1.5-flash，max_tokens=500，讀 `st.secrets["GEMINI_API_KEY"]` 或 env var）+ `_render_data_health_ai(lines)` 渲染按鈕 + AI 回覆卡；`render_sidebar_data_health` headline 🔴/🟠 時 caption 後追加 `_render_data_health_ai(_lines)` 呼叫。零自動 API 消耗 — 只有 user 主動點按鈕才打 Gemini；結果快取 `session_state["_data_health_ai_resp"]` 避免重複呼叫。py_compile ✓ / AST ✓ / pytest **1396 passed 零回歸**。**[邏輯]** 多模型嘗試順序 2.5-flash→2.0-exp→1.5-flash-latest→1.5-flash；404 跳下一個、30s timeout；最終仍失敗回 ⚠️ 提示。**[邊界]** 無 API key → 友善提示；所有 model 失敗 → 不爆只顯提示；按鈕點 N 次只打 1 次（結果已快取）。**[效能]** 零自動 API 消耗；按鈕 on-demand；max_tokens=500 控成本。**[Debug]** prompt 含完整資料源狀態字串，AI 回覆直接顯示於 sidebar 按鈕下方。兩 repo G 系列對稱完成（Fund G v19.67 + Stock G2 v18.205）。下一步候選：H2 Stock cold-start 效能極致化。

- **前一版**：v18.204_StockMacroLink（PR #212 squash merge @ 98ec4f0。I4 — 個股↔總經 regime 聯動 banner，鏡像 Fund v19.64 I1，兩 repo I 系列首棒對稱完成。問題：`tab_stock.py` 已讀 `mkt_info`（L709/738/761/1561/1582/2469）但僅用於內部加權評分，未展示給 user — bear regime 時即使基本面強的股也難抗系統性風險，需在個股視圖明示。改動 2 檔 +66：(1) 新增 `macro_stock_link.py`（root，Stock 扁平結構）`render_macro_stock_backdrop(session_state)` — 讀 `mkt_info`（market_strategy.get_market_assessment 結果：regime 'bull'/'neutral'/'bear'、label、exposure_pct、index_below_ma5）+ `jingqi_info`（市場廣度，optional），渲染大盤背景 banner：bull 綠 / neutral 橙 / bear 紅 三色邊框 + label + 建議持股 % + MA5 對照 + 市場廣度副欄；bear → 警示 caption「個股操作宜保守 / 減碼」；bull → 友善 caption「順勢操作環境」；neutral → 無方向性 caption。(2) `tab_stock.py:L597` stale 警示後插入 banner（`_fin_degraded` caption 之後、即時價格儀表板之前）。py_compile ✓ / ruff baseline **673 → 673 零新增**（新檔 All checks passed）/ functional smoke **6 case**（無 mkt_info→提示載入總經 / 空 dict→容錯 / bull regime+友善 caption / bear regime+警示 caption / neutral 無方向性 / jingqi_info 廣度副欄）全綠 / pytest **1396 passed 零回歸**。**[邏輯]** 純讀總經 Tab 已寫入 session_state 的 `mkt_info`；bear/neutral/bull 三色邊框 + 方向性 caption；`jingqi_info` 為可選副欄。**[邊界]** 總經未載入 / mkt_info 空 → 容錯提示；`index_below_ma5` 為 None → 跳過；`jingqi_info` 無 label → 跳過副欄；try/except + print log（非靜默）。**[效能]** 純讀 session_state + HTML markdown ~ms 級，零新增 fetch/cache。**[Debug]** banner 標「來自總經 Tab」明示資料源；bear 時主動警示 + 行動建議（減碼）。設計對齊 Fund I1：Fund I1 聚合 Tab1 macro→Tab3 組合（推送 regime + alloc），Stock I4 聚合總經→個股（推送 regime + exposure），同款「總經訊號跨 Tab 推送」模式，零新 IO 純既有 session_state reuse。User 上線驗證：reboot → 先載入總經 Tab → 切個股 Tab → 新鮮度條下方應出現「🧭 大盤總經背景」banner。下一步候選：G Gemini AI / H cold-start 效能。

- **前一版**：v18.203_SidebarDataHealth（PR #211 squash merge @ 78dc213。F2 — 鏡像 Fund 端 v19.63 F，把散落各 Tab 的資料新鮮度（個股六大資料源 + 總經羅盤）聚合到 sidebar，讓 user 一眼看出哪些資料源已過期 / 走備援源，該按強制刷新；兩 repo sidebar 全局健康總覽對稱完成。改動 2 檔 +171：(1) 新增 `sidebar_health.py`（root，Stock 扁平結構）— `_src_emoji(src_key, val)` 各源 primary 集合分類（🟢 主源 / 🟠 降級 / 🔴 missing / ⬜ unknown，`_SRC_PRIMARY` 對齊 B1 L462-474 + E2 LABEL 慣例：price_src→yahoo_adj、inst/margin_src→finmind_sdk、rev_src→finmind、qtr_src→finmind_rest、qtr_extra_src→finmind）；`_collect_src_counts(t2d)` 聚合個股六源（price/inst/margin from `df.attrs` + rev/qtr/qtr_extra 各自 df attrs）紅綠燈統計，None/empty 財報 df → 🔴；`_age_emoji_min`/`_fmt_age` 抓取 age traffic-light（🟢<1h/🟠<4h/🔴≥4h，對齊 tab_stock 既有閾值）；`render_sidebar_data_health(session_state)` 聚合 `t2_data`（個股 fetch age + K線截止日 + 六源統計）+ `_macro_compass_cache`（總經羅盤 `_ts` age），headline=各域最差燈，全空→「尚未載入」，偏舊→提示按強制刷新。(2) `app.py:L1095` sidebar「🔄 強制刷新數據」後插入「📊 全局資料健康」panel（try/except + print log 非靜默）。**關鍵**：`t2_data['fetched_at']`（pd.Timestamp.now）與 `_macro_compass_cache['_ts']`（datetime.now）皆 tz-naive local，age 用 naive `now()` 計算（混 tz-aware 會 TypeError）。py_compile ✓ / ruff baseline **673 → 673 零新增**（新檔 All checks passed）/ functional smoke **7 case**（_src_emoji 6 / _age_emoji_min / _collect_src_counts 3🟢1🟠2🔴 / 空 session / 完整雙域聚合+提示 / t2_data 空 dict）全綠 / pytest **1396 passed 零回歸**。**[邏輯]** sidebar 讀 session_state 各 Tab 已填的新鮮度 metadata；個股六源由 `df.attrs`（B1/D2/E2 寫入）+ 財報三 df attrs 聚合；headline 取各域最差燈。**[邊界]** sidebar 先於 tab render → 首次全 key 未填 → 「尚未載入」；t2_data 無 sid（未載入）→ 跳過；財報 df None/empty → 🔴；naive datetime age 計算例外容錯；整 panel try/except + print log（非靜默）。**[效能]** 純讀 session_state + in-memory 聚合 + HTML markdown ~ms 級，零新增 fetch/cache。**[Debug]** 一眼掃個股六源 + 總經健康，偏舊時提示按強制刷新，與 sidebar 既有「🔄 強制刷新數據」按鈕上下相伴形成閉環。設計對齊 Fund F：Fund F 聚合 FRED/組合 NAV/單檔，Stock F2 聚合個股六源/總經羅盤 — 反映兩 repo 資料源差異但 UI 模式統一。User 上線驗證：Stock Streamlit Cloud reboot → sidebar「🔄 強制刷新數據」下方應出現「📊 全局資料健康」panel；載入個股 / 總經後重整 sidebar 顯示六源紅綠燈統計 + age + 偏舊提示。下一步候選：G — Gemini AI 摘要整合（資料源異常時主動 AI 解釋）/ H — cold-start 效能極致化 / 兩 repo F panel 互動增強（點擊跳對應 Tab）。

- **前一版**：v18.202_FinReportSrcChip（PR #210 squash merge @ 2c280f6。E2 — 鏡像 v18.200 B1（K線/籌碼/融資 src chip）+ v18.201 D2（hover tooltip），把「資料源實際命中標籤化」擴展到**財報三段**：月營收 / 季財報 / 季財報-extra。讓 user 看「這季 EPS 怎麼沒變」時能區分「走備援源 / 缺失」vs「真的沒變」。問題：月營收（FinMind→MOPS）、季財報（FinMind REST→SDK→yfinance）、季財報-extra（FinMind BS/CF→MOPS 補合約負債）三段 fallback 鏈，主源失敗靜默走備援或缺失，面板無提示，user 無法判斷數值可信度。改動 2 檔 +91：(1) `data_loader.py` 新增 `_stamp_finreport_attrs(df, src_key, src_val)` helper 寫 `df.attrs['{key}_src']` + `['{key}_fetched_at']` — **關鍵發現**：財報 fetcher 回傳 `(df, err)` tuple 過 `@st.cache_data`，但 pandas DataFrame `.attrs` 直接 pickle 可保留（app.py wrapper 直接回傳 loader tuple 未做 `.tail()/.reset_index()` 轉換，與 fetch_price_data B1 走轉換掉 attrs 需 `result.attrs.update` 不同），故 **data_loader 寫入即可、app.py 免動**；`get_monthly_revenue` 用 `_rev_src` local var 追蹤 finmind/mops（5 分支標記 + return 前 stamp）、`get_quarterly_data` 用 `_qtr_src` 追蹤 finmind_rest/finmind_sdk/yfinance、`get_quarterly_bs_cf` 用 `_qtr_extra_src` 追蹤 finmind/finmind_mops。(2) `tab_stock.py:457` 資料新鮮度條既有 K線/籌碼/融資 chip 下方加第二行 `📈 月營收 / 📊 季財報 / 📑 季財報extra` 三 chip（hover tooltip 顯示源 + 抓取時間）；`_fin_attrs()` None/empty → missing(🔴) / 無 attrs → unknown(⬜)；新增 `_fin_degraded` 警示 caption（rev mops/missing、qtr finmind_sdk/yfinance/missing、qtr_extra finmind_mops/missing 任一觸發）。py_compile ✓ / ruff baseline **673 → 673 零新增** / functional smoke：`_stamp_finreport_attrs` + pickle round-trip / 三段 LABEL 映射 / `_fin_attrs` 四態（None/empty/valid/no-attrs）全綠 / pytest **1396 passed 零回歸**。**[邏輯]** 各 fetcher 用 local var 追蹤實際命中分支（guard `if df is None` 確保只有一分支執行，最後命中的 src 勝出），return 前寫 `df.attrs`，靠 pandas pickle 過 `@st.cache_data` 保留；tab 讀 `rev2`/`qtr2`/`qtr_extra2` 的 attrs 渲染 chip。**[邊界]** df None/empty → missing(🔴)；無 attrs → unknown(⬜)；stale-cached 值（從 session_state 回填）帶舊 attrs 顯示其來源不誤導；`_stamp` try/except 不擋 return。**[效能]** 純 local var + dict 查表 + HTML markdown ~ms 級，零新增 IO/cache。**[Debug]** hover tooltip 顯示「源 + 抓取於」，user 排查 EPS 異常時可一眼看出走哪條 fallback；`_fin_degraded` caption 主動提示備援/缺失。設計差異 vs B1：B1 在 `fetch_price_data` 因 `.tail().reset_index()` 掉 attrs 需 app.py `result.attrs.update(df.attrs)` 補救；E2 財報段 wrapper 不轉換 df，attrs 直接過 pickle，更簡潔。User 上線驗證：Stock Streamlit Cloud reboot → Tab Stock 查股票 → 資料新鮮度條第二行應出現「📈 月營收 / 📊 季財報 / 📑 季財報extra」三 chip，hover 顯示源 + 抓取時間；走備援源時 chip 轉 🟠 + 出現降級 caption。下一步候選：**E3** — Fund Tab2/Tab3 資料新鮮度 chip 補完（鏡像 Stock v18.197 個股新鮮度條，延伸 v19.61 E1 從 Tab5 到 Tab2 單一基金 + Tab3 組合基金）。

- **前一版**：v18.201_FinMindLastUpdate（PR #209 squash merge @ dd90341。鏡像 Fund 端 v19.60 D1（FRED `realtime_start`）同款設計理念，補上 FinMind dataset 後台 update 時間。問題：v18.200 B1 已做了 K線/籌碼/融資 3 個資料源命中標籤化（chip 顯示 🟢主源/🟠降級/🔴缺失），但 user 看到「📡 K線：🟢 Yahoo還原」時無從得知「資料是何時被 ingest 的」— FinMind 後台 update 時間（response top-level `last_update` 欄位）可幫助判斷資料新鮮度。改動 2 檔 +51/-3：(1) `data_loader.py` 新增 module-level dict `_FINMIND_META` + helper `_capture_finmind_meta(src_key, j_response)` — raw HTTP fetcher (`_fetch_finmind_inst_raw` / `_fetch_finmind_price_raw`) 從 response top-level 取 `last_update`，SDK 三段路徑 (`_price_src` / `_inst_src` / `_margin_src` = `finmind_sdk`) 同步呼叫但傳空 dict（SDK 無 response，`last_update` 留空、只記 fetched_at）；`StockDataLoader.get_stock_data` attrs assign block 統一寫 `df.attrs['{k}_last_update']` + `df.attrs['{k}_fetched_at']` 給 chip 用。(2) `tab_stock.py:457` 3 個既有 chip（K線/籌碼/融資）wrap 進 `<span title="...">` HTML hover tooltip，新增 `_fm_tooltip(key, label)` helper 拼出「{SRC.upper()} ｜ 後台 update YYYY-MM-DD HH:MM ｜ 抓取於 YYYY-MM-DD HH:MM:SS」三層資訊，chip 文字無變化、零視覺破壞。py_compile ✓ / ruff baseline **673 → 673 零新增** / functional smoke 4 case：raw HTTP 含 last_update / SDK 空 dict / None response / 缺欄補空 — 全綠 / pytest **1396 passed 零回歸**。**[邏輯]** FinMind raw HTTP response top-level 帶 `last_update` 時直接取（FinMind 後台 ingest 時間 ≈ dataset update），SDK 路徑無 response 故 `last_update` 永遠空，僅 `fetched_at` (wallclock) 作為估計。**[邊界]** `None` response / 缺欄 / SDK path — 全 graceful，hover 缺 `last_update` 時只顯 src + fetched_at；attrs 缺對應 key 時 `_fm_tooltip` 回空字串，HTML 自動不渲染 `title` attr。**[效能]** 零新增 HTTP call、零新增 cache；module dict 純 in-memory，每次 fetch 跑完才覆寫；hover 純前端 HTML title attr 零 JS。**[Debug]** tooltip 三層資訊（src ｜ 後台 update ｜ 抓取於）讓 user 排查資料延遲時清楚區分「FinMind 後台 ingest 卡」vs「客戶端 cache 命中」vs「資料源完全沒回應」。對齊 Fund D1 設計差異：D1 用 DataFrame 多一欄存 realtime_start（per-observation），D2 用 module dict 存 dataset-level last_update（per-fetcher-call）— 反映 FRED 是 series-of-observations 而 FinMind 是 dataset-snapshot。下一步：用戶上線 Reboot Streamlit Cloud → Tab Stock 查一檔股票 → hover「📡 K線」/「🏦 籌碼」/「💰 融資」chip → 應顯示 tooltip「{SRC} ｜ 後台 update ｜ 抓取於」三層；SDK path 只顯抓取時間（後台 update 留空）。A/B/C/D 系列全部收官，下一步候選需 user 點題新需求。

- **前一版**：v18.200_KlineFallbackSrcChip（PR #208 squash merge @ 5feeff1。延續「資料新鮮度可視化」進入 **B 系列「fallback 警示擴展」**首棒（尚未完成候選清單 B1）。個股 Tab 「📊 資料新鮮度條」原本「📡 來源：FinMind / Yahoo」是寫死字串，無論 K線（Yahoo還原→FinMind SDK→FinMind HTTP→Yahoo備援 4 級鏈）/ 籌碼（FinMind SDK→FinMind HTTP→TWSE→TPEX 4 級鏈）/ 融資（FinMind SDK 唯一）主源失敗有沒有走降級，面板永遠長一樣 — 正是 user 在意「之前有看過資料是舊的」靜默 fallback 場景。改動 3 檔：(1) `data_loader.py:get_combined_data` 三段資料源實際命中追蹤 — 區域變數 `_price_src`/`_inst_src`/`_margin_src` 鋪進三段分支（股價 5 態 `yahoo_adj`/`finmind_sdk`/`finmind_raw`/`yahoo_fallback`/`unknown`；籌碼 6 態 `finmind_sdk`/`finmind_raw`/`twse`/`tpex`/`missing`/`unknown`；融資 3 態 `finmind_sdk`/`missing`/`unknown`），`return` 前一次寫進 `df.attrs['price_src'/'inst_src'/'margin_src']`，**零改回傳 arity** 既有 2 caller（`app.py` / `tab_stock_grp.py`）完全不動；(2) `app.py:fetch_price_data:298` 在 `result = df.tail(days).reset_index(drop=True)` 後加 `result.attrs.update(df.attrs)`，保 attrs 過 `@st.cache_data` pickle 不丟；(3) `tab_stock.py:457-494` 新鮮度條 — 寫死「📡 來源：FinMind / Yahoo」拆三段獨立 chip：📡 K線 / 🏦 籌碼 / 💰 融資 — 主源🟢 / 降級🟠 / 缺失🔴 / 未知⬜；任一段非主源 → 條下方追加 🟠 caption「主資料來源失敗已降級，技術指標 / 籌碼 / 融資數值可能與正常情況不同；建議按右側 🔄 強制重抓 重試主源」。AST ✓ / py_compile ✓ / ruff baseline **71 errors → 71 零新增** / pytest tests/ **1396 passed 零回歸**。**[邏輯]** sentinel 在三段分支末端逐一覆寫，零改既有 if/else 結構；attrs 隨 pickle 過 cache 不丟；K 線 `finmind_sdk` 視為降級因主源實為 Yahoo 還原。**[邊界]** attrs 缺（舊 session pickle）→ `'unknown'` fallback 不爆；`df=None` 短路 return 早於 attrs 寫；try/except 包覆 attrs 寫入。**[效能]** 純局部變數 + dict 查表 + HTML markdown ~ms 級，零新增 IO/cache。**[Debug]** 三色 chip 一眼看出哪段降級，user 不再「不知道在看主源還是備援」；降級 caption 明示「技術指標數值可能不同」對症 user 疑慮。下一步候選：B2 FRED 個別指標 fallback / C1 Per-Tab 精準 .clear() / C2 Sidebar 全域刷新總開關 / D1 FRED realtime_start / D2 FinMind dataset last_update。

- **前一版**：v18.199_MjTrendFreshnessBar（PR #207 squash merge @ 063e185。延續「資料新鮮度可視化」系列，補上**個股組合 → 📊 MJ 趨勢分數**區塊（尚未完成候選清單 A4；註：A4 原標的「📊 MJ 體檢變化 Tab」已於 v18.189 archive，MJ 快照現役消費點即此區塊）。該區對每檔跑 `list_snapshots(sid)` 取近 3 季 MJ 體檢快照算「MJ 季財報分」，本季快照缺時自動補抓，但**補抓失敗（FinMind quota/error）會靜默沿用舊季快照算分** — 正是 user 在意的「看到舊資料」場景。改動 `tab_stock_grp.py` 純回傳欄位 + UI 擴充（`compute_trend_score` 合議邏輯零變動）：(1) `_compute_one_stock_trend` 回傳值加 `snap_ym`（實採最新快照季 `yms[0]`）+ `snap_stale`（`yms[0] != yyyymm_curr`；三態 False/True/None = 最新/落後/無快照），**零新增 IO**（`list_snapshots` 本就在跑）；(2) `_render_mj_trend_table` 加「季別」欄（每檔 MJ 分來自哪季：🟢 最新 / 🟡 舊 / ⬜ 無）+ 表格上方插「📊 快照新鮮度條」（📅 應有最新季 `_fmt_quarter(yyyymm_curr)`：`202503`→`2025Q1` + traffic-light 🟢 全最新 / 🟡 部分落後 / 🔴 全缺或舊 + 🟢/🟡/⬜ 檔數統計）；(3) 新 `_fmt_quarter` 純 helper（季底月份→季別字串，格式不符回原字串）。AST ✓ / py_compile ✓ / ruff **All checks passed** / `_fmt_quarter` smoke 8/8 / pytest tests/ **1396 passed 零回歸**。**[邏輯]** snap 資訊由既有 `list_snapshots` 結果直接讀（`yms[0]` vs `yyyymm_curr`），合議邏輯零變動。**[邊界]** 三態 False/True/None 分離（最新/落後/無快照）；`_fmt_quarter` 格式不符回原字串不爆；空 rows early-return。**[效能]** 純 in-memory 計數 + HTML markdown ~ms 級，無新增 fetch/cache。**[Debug]** 季別欄逐檔暴露 MJ 分來源季，補抓失敗不再靜默沿用舊季。下一步候選：A5 危機回測 / B1 K 線 fallback / B2 FRED 指標 fallback / C1-C2 強制重抓粒度 / D1-D2 上游時間戳。

- **前一版**：v18.198_EtfPortfolioFreshnessBadge（PR #206 squash merge @ c9a57d4。延續 v18.197「資料新鮮度可視化」，比照同款設計補上 **ETF 投組 Tab**（尚未完成候選清單 A1）。User 反映「之前有看過資料是舊的」— ETF 投組現價快取 1h（`fetch_etf_price` ttl=3600）、成份股快取 1 天（`fetch_etf_holdings` ttl=86400），但 user 看不到「幾點抓的、收盤截止哪天」。改動 `etf_tab_portfolio.py` 純 UI 新增 3 處：(1) 批次抓價迴圈（:122）記錄價格截止日 `_pf_price_end`（取各檔 `_df_p.index[-1]` 最大）+ 抓取完成時戳 `_pf_fetched_at`；(2) 資產總覽卡（:202）下方插入「📊 投組資料新鮮度條」— 📅 價格截止日 + 🕐 抓取時間 + ⏱️ age（綠<1h/黃<4h/紅>4h）+ 📡 來源 yfinance；(3) 🔄 強制重抓鈕：`st.cache_data.clear()` + `st.rerun()`，因 `etf_p_active` 保留 → 自動以最新資料重算免重填表格。AST ✓ / py_compile ✓ / ruff All checks passed / pytest tests/ **1396 passed 零回歸**。**[邏輯]** age 用 `pd.Timestamp.now()` 與 `_pf_fetched_at` 算分鐘差；價格截止日從各檔 5d K 線 index 取最大；強制重抓清 cache 後 rerun，`etf_p_active` 保留自動重算。**[邊界]** `_pf_price_end=None`（全檔抓取失敗）→ 顯「—」；`.clear()` 包 try/except；單檔日期解析失敗 graceful pass。**[效能]** banner 純 HTML markdown ~ms 級；強制重抓清整個 app cache_data（user 主動點按鈕場景副作用可接受）。**[Debug]** age traffic-light 顏色一目了然，user 不再「不知道在看哪天的現價」。下一步候選見尚未完成清單 A2-A6。

- **前一版**：v18.197_StockFreshnessBadge（PR #205 merge @ d511ec4。User 反映「之前有看過資料是舊的」明確指明需求轉軸：「可以存放（不砍快取保速度）但要保證面板資料是新的」。Explore 確認 5 種「看到舊資料」根因：① FinMind quota 滿 fallback 舊值無提示；② `@st.cache_data(ttl=86400)` 1 天內回舊；③ FRED 月頻指標本身就有 lag（PMI/M2/CPI 月底才更新）；④ NAS proxy 失敗沿用 session 殘留；⑤ `/tmp/*.pkl` + `data_cache/` 跨 session 殘留。Plan 不砍快取，改加 3 套可視化機制：(1) `tab_stock.py:387` t2_data dict 加 `fetched_at: pd.Timestamp.now()`；(2) `tab_stock.py:437` 之後個股 dashboard 頂部插入「📊 資料新鮮度條」— K 線截止日 + 抓取時間 + age（< 1h 綠 / 1-4h 黃 / >4h 紅）+ 來源；(3) `_rev2_cached` / `_qtr2_cached` 觸發時強制顯橘條「⚠️ 月營收/季財報抓取失敗，目前顯示上次成功舊值」；(4) 加「🔄 強制重抓」按鈕：`st.cache_data.clear()` + 清 4 個 session_state key（t2_data / _exit_news_titles / _last_rev / _last_qtr）+ `st.rerun()`。AST ✓ / py_compile ✓ / ruff All checks passed / pytest tests/ 1396 passed 零回歸。**[邏輯]** age 顯示用 pd.Timestamp.now() 與 fetched_at 計算分鐘差；K 線截止日從 df2.index[-1] 或 df2['date'].iloc[-1] try 兩源；強制重抓清完 cache 後 rerun → 再按「載入完整分析」就 cache miss 真重抓。**[邊界]** fetched_at 為 None 時 banner 不顯示；df2 空時 K 線截止日顯「—」；st.cache_data.clear() 包 try/except 容錯；session_state.pop 用 default=None 不 KeyError。**[效能]** banner 純 HTML markdown ~ms 級無感；強制重抓會清整個 app cache_data → 其他 Tab 下次進去要重抓（user 主動點按鈕的場景，副作用可接受）。**[Debug]** age 顏色 traffic light 一目了然，user 不再「不知道在看哪天的資料」；fallback 警示主動顯示而非 silently 沿用舊值。下一步候選：ETF Tab / 月營收 / 台股熱錢 / MJ 體檢 4 個面板若 user 反映也有舊資料疑慮，比照同套機制補上。

- **前一版**：v18.196_StockParallelLoadV2（PR #204 merge @ e5cb8fc。User 第三輪反饋：「面板仍慢 + 出現順序奇怪」。Explore 確認剩餘瓶頸與順序錯亂熱點 3 處：(1) `tab_stock.py:662` `_fetch_stock_news` RSS 同步抓取 3-5s 卡住出場點區塊；(2) `tab_stock.py:1236` G區籌碼 `with st.spinner` 包裝 + `analyze_20d_chips(sid2)` fallback 第二次 FinMind API call；(3) 「順序奇怪」根因 = 上方出場點 freeze 等 RSS 但下方 K 線圖率先 render → 視覺跳序。改動 3 處：(a) t2_run ThreadPoolExecutor 6→7 worker，把 `_fetch_stock_news` 加入並行池，預抓 RSS 標題寫進 `session_state['_exit_news_titles_{sid}']`，下游 line 662 cache hit 跳過 3-5s 阻塞；(b) G區直算 — 移除 `st.spinner` 包裝 + 移除 `analyze_20d_chips(sid2)` fallback（df2 已含三大法人欄，直算 ms 級），省第二次 API call 並消除 spinner 視覺跳動；(c) §2 大掃除 — 移除已不再使用的 `analyze_20d_chips` import。AST ✓ / py_compile ✓ / ruff All checks passed / pytest 22 passed + 1 pre-existing `test_trend_arrow_recent_rebound` baseline 持平。**[邏輯]** RSS 預抓寫入 session_state cache key 與下游 line 662 的 cache key 對齊；預抓失敗 silent → 下游回退原行為（沒變慢）；G區直算保留 error caption 處理 df2 無籌碼欄 edge case。**[邊界]** RSS 用 sid2 作 name 參數 fallback（fetch_price_data 尚未完成不知道 name2）；預抓 exception 不阻斷其他 6 IO；analyze_20d_chips_from_df 返回 error 時 caption 顯示「⚫ 籌碼集中度取得失敗」。**[效能]** 預估 cold-start wallclock 再省 3-5s（RSS 並行）；G區從序列計算 + spinner 顯示閃爍變成 instant render 視覺穩定。**[Debug]** 出場點區塊不再 freeze 等 RSS，與 K 線圖渲染順序自然按 code 順序 top-down 不跳序。下一步：user 上線確認個股載入體感 < 25s + 出場點區塊不再卡。

- **前一版**：v18.195_StockParallelLoad（PR #203 merge @ 207f756。User 反饋：「兩個面板的產生還是很慢」，明確拒絕用 `@st.cache_data` 暫存方案。Explore 確認 Stock 整頁 critical path 最大瓶頸：`tab_stock.py:350-358` 個股「載入完整分析」按鈕觸發後 6 個 IO 函數 `fetch_price_data` / `fetch_dividend_data` / `fetch_financials` / `fetch_revenue` / `fetch_quarterly` / `fetch_quarterly_extra` 全是序列呼叫，即便每個都有 `@st.cache_data(ttl=3600)`，cache miss 時序列總和 wallclock ~100s+。改動：6 個 cache_data 函數改用 `ThreadPoolExecutor(max_workers=6)` 並行 submit/result，其他下游邏輯（rsi/ibs/vr/k/d/bb/vcp/health 計算 + session_state 寫入）完全不變。效益：cold-start wallclock 從序列總和 ~100s+ 改為 `max(各函數)` ~30s（受最慢 `fetch_financials` ~40-45s 主導），實測省 30-50s；cache hit 時並行 overhead 可忽略。AST ✓ / py_compile ✓ / ruff All checks passed / pytest test_macro_core 42 passed 零回歸。**[邏輯]** 6 submit/result 完整等齊，介面回傳結構不變；**[邊界]** 任一函數 exception 透過 `future.result()` 拋出不靜默；**[效能]** worst case wallclock = max(6 函數) 而非 sum；**[Debug]** Streamlit `@st.cache_data` 並行 invoke 已實證可運作（warning 但不 fail）。下一步：user 上線確認個股載入體感 < 30s；若仍慢可進一步砍 fetch_financials per-API timeout 或 dedup BS/CF 跨函數重複呼叫。

- **前一版**：v18.194_MacroFailTrace（user 反饋總經 6 chip 全 ⬜「待取得」但不知為何 — 必須登 Streamlit Cloud build log 才能看根因。3 個並存根因：① `_fetch_ndc` 雙源失敗 silent return None 沒 `_err_ndc` 標記；② `_job_macro` outer 80s timeout → `_macro_res = None` → 6 chip 全 ⬜；③ 既有 `_err_vix/_cpi/_fed_funds/_pmi/_export` 標記只 print log，UI 完全不讀。改動 2 處：(1) `tab_macro.py:1463-1464` `_fetch_ndc` 最後 `return {'_err_ndc': '...'}` 取代 None；(2) `tab_macro.py:3970+` 6 chip 下方新增 fail trace expander 三狀態：全失敗（`_macro_info` 無 6 keys 但有 `_loaded_at`）→ 紅框 expander expanded=True 列 3 個可能根因 + 各 `_err_*`；部分失敗（有 `_err_*`）→ 摺疊 expander expanded=False 列各錯誤碼；全綠 → 不顯示。仿 Fund v19.43 RadarFailTrace 同款 pattern。User 無需 log 即可看「FRED HTTP 429」/「stat.gov.tw timeout 12s」等具體錯誤碼對症排查。AST ✓ / py_compile ✓ / ruff All checks passed / pytest 216 passed（pre-existing `test_trend_arrow_recent_rebound` 失敗無關本 PR）；PR #202 merge @ 913bfa1。零既有功能變動（純新增 UI 元素）。下一步：user 上線後若 6 chip 仍 ⬜，截圖紅框 expander → 根據錯誤碼對症補 FRED key / 拉高 timeout / 補 fallback 源。

- **前一版**：v18.193_MacroParallelTickers（user 反饋「總經面板載入慢」→ 第二輪 Explore 確認 14 個 yfinance ticker 雖在外層 3 個 job 平行，但每個 job 內部仍序列呼叫 `fetch_single`，per-ticker 5-8s 累加。改動：tab_macro.py:741-758 新增 `_parallel_fetch(_mp, **_kw)` helper（根據 map 大小開 ThreadPoolExecutor），`_job_intl/_job_tw/_job_tech` 改為 1-line wrapper 呼叫 helper；保留 `fetch_single` /tmp pickle 30 分鐘 cache + DX-Y.NYB→DX=F→UUP 美元指數備援 + 外層 `do_refresh` gating。預估冷啟動 ~80s → ~55-65s（按按鈕後等待時間）。AST ✓ / py_compile ✓ / ruff All checks passed / pytest test_macro_helpers + test_dual_verdict_ui + test_risk_radar + test_macro_state_locker 217 passed；PR #201 merge @ b351c93。跳過項目（Explore 二次澄清後撤回）：① 熱啟動條件跳過 — `tab_macro.py:731 if do_refresh:` 已包覆整段抓取，rerun 自動 skip；② yf.download 批量 API — 選擇內層並行而非批量 API，保留 fetch_single 既有 /tmp pickle cache + 美元備援邏輯，風險更低。下一步候選：per-API timeout 砍小（stat.gov.tw 12s→6s / FinMind 10s→5s）省 3-8s、calc_traffic_light 加 cache 省 0.3-0.5s、Section 十一 expanded=False UX 改進。

- **前一版**：v18.192_RestoreAiVerdict（user 反饋「最下方的 AI 總經不見了」→ 還原 v18.190 PR #198 移除的 section 十一 🤖 AI 總裁決 整塊。**恢復 6 個 imports**（json / MacroStateLocker / calculate_system_state / load_macro_state / _fetch_macro_news / gemini_call）+ **SECTION 十一 整塊 +289 行**（LLM 報告 expander）：🔒 執行 AI 裁決 button（呼叫 Gemini API ~15-30s）/ 🗑️ 清除報告 / 量化數據快照 `_macro_numbers`（VIX/M1B/PMI/CPI/FedFunds/BIAS240/PCR/FuturesNetShort/IndexBelowMA5/SahmRule）→ `MacroStateLocker.lock_system_state_only` / `build_structured_summary_prompt` 4 段（系統判斷 + 量化原始 + 熱錢動向三角交叉 + 拐點訊號六大面向）+ news_text → `gemini_call(max_tokens=2400)` / 唯讀渲染 macro_state.json（市場體制 + 系統風險 + 曝險上限 % + 現金/防禦 %）+ 時效檢查（鎖定快照 vs 即時紅綠燈不一致警告）/ Markdown AI 戰情報告 session_state `_macro_ai_report`。**保留 v18.190 同 PR 移除的雙視角 UI（不還原）— user 未要求恢復**。改動方式：tab_macro.py imports 加 6 個 + 檔末 append section 十一 整塊 verbatim 還原 v18.190^ 版本。tab_macro.py 4434 → 4729 行 (+295)；面板數 16 → 17（section 十一 復活）。AST ✓ / py_compile ✓ / ruff All checks passed / pytest test_macro_helpers + test_risk_radar + test_dual_verdict_ui 164 passed；PR #200 merge @ cff887a。下一步：user 上線確認 AI 總裁決按鈕可觸發 Gemini → 出報告即為成功。

- **前一版**：v18.191_ArchiveValidationSection（user 反饋總經面板過於複雜，v18.190 後仍有 21 個 → 進一步封存 C. 歷史驗證區 5 個 expander：🎯 TWII Crisis 事件清單 + 🚦 Phase 3 訊號預測力驗證（命中率+逐事件+📐 精確率分析）+ 📡 跨資料源比對視角矩陣（Phase E）+ 🎯 MT5-style 自動校準（walk-forward + 3 重 anti-overfit gate）+ 🔬 多因子權重最佳化（高原區 + walk-forward OOS）。改動方式：tab_macro.py L4415-4423 try/except call site 整段註解 + 6 行 v18.191 ARCHIVED 標題塊明示封存原因 + 復活步驟（1 分鐘工）；tab_macro_validation.py / macro_validation_tw / macro_signal_lookback_tw / multi_factor_optimization 全留磁碟。面板數 21 → 16 (-24%)。零模組刪除、零測試破壞。AST ✓ / py_compile ✓ / ruff All checks passed / pytest test_macro_helpers + test_risk_radar + test_dual_verdict_ui 164 passed；PR #199 merge @ e2c6ba3。下一步候選：剩 16 個面板（頂部 6 + sections 8 個編號 + section 九 AI 投資決策）等 user 進一步決策 keep/archive。

- **前一版**：v18.190_MacroDedupPanels（user 反饋「股票的總經面板目前也太過複雜」→ 盤點 23 個面板/expander，砍 2 個重複塊。**A. 雙視角面板 -101 行**：移除頂部「🔭 長短期雙視角總經判讀」UI（並排 2 卡 + 🔰 教學 expander）+ `_st_r = _cls_st(...)` 計算 + `classify_short_term_regime` import；**保留 `_lt = _cls_lt(...)` 長期 regime 計算**（仍餵下方雷達雙速合議 `_slow_v`）；理由：與「拐點 6 面向 + MK 黃金訊號」功能重疊，雙視角為純加權打分未經 backtest。**B. AI 總裁決 section 十一 -290 行**：移除 L4431-4720 整塊（hr + section_header + LLM 報告 expander）+ 6 個 unused imports（json / MacroStateLocker / calculate_system_state / load_macro_state / _fetch_macro_news / gemini_call）；保留 section 九「AI 投資決策」5 卡（規則式即時生成，無需 GEMINI_API_KEY、無需等 15-30 秒）；理由：兩者均為 AI verdict 但 section 九已涵蓋核心 5 面向，十一 ROI 低。tab_macro.py 4803 → 4419 行 (-384 / -8%)，面板 23 → 21。AST ✓ / py_compile ✓ / ruff All checks passed / pytest 164 passed (test_macro_helpers + test_dual_verdict_ui + test_risk_radar)；PR #198 merge @ e306b00。下一步候選：剩 21 個面板走 ROI 評分（用 section 十 Phase 3 命中率引擎當判官，<2/5 crisis 命中率的砍掉）；或繼續砍 VIX/HY 卡片級重複（頂部雷達 vs section 八）

- **前一版**：v18.189_MjTrendScoreInStockGroup（user pivot：v18.188 剛重寫的 batch Tab 也 archive，改在「🏆 個股組合」批次體檢區塊下方加新區塊「📊 MJ 趨勢分數」整合月+季雙頻率合議。**月權重 65% / 季權重 35%** — 月營收 10 日公布（先行指標）權重高搶時效、季財報 45 天遞延（落後但見獲利品質）權重低保品質，互補不對立。**新檔 mj_trend_score.py ~180 行純函式 zero-IO**：① `compute_monthly_revenue_subscore(monthly_3m)` → 範圍 [-2.5, +2.5]，公式 = YoY 階梯分數 [-2, +2]（≥+10%→+2 / >0→+1 / <0→-1 / ≤-10%→-2）+ 末月 MoM [-0.5, +0.5]；② `compute_mj_trend_subscore(mj_snapshots_3q)` → 範圍 [-2, +2]，對近 3 季快照逐期 diff 兩次（Q-2→Q-1、Q-1→Q），同向（兩次同 sign）累加 s1+s2、分歧衰減 0.5×（防假訊號）；n<2 時 graceful 回 0；③ `compute_trend_score(monthly, snapshots, w_monthly=0.65)` → 5 段判定（≥+1.5🚀 強進步 / +0.5~+1.5📈 進步 / -0.5~+0.5➖ 中性 / -1.5~-0.5📉 退步 / ≤-1.5🔻 強退步）；w_monthly 越界（>1 / <0 / 非數字）自動 fallback 0.65。**mj_snapshot_io.py 加 `current_finmind_yyyymm(today=None)` helper**：依今日推算最近完成季 yyyymm（1-3月→去年Q3、4-5月→去年Q4、6-8月→Q1、9-11月→Q2、12月→Q3，純函式無 IO）。**tab_stock_grp.py 新區塊**：在 render_stock_grp() 函式尾部 4-space 加 `if stock_list_t3: _render_mj_trend_section(stock_list_t3)`，新區塊內：月權重 slider (0.4-0.9 step 0.05 default 0.65) + 「📊 跑 MJ 趨勢分數」按鈕 + progress bar；單檔流程 `_compute_one_stock_trend(sid, yyyymm_curr, token, w_monthly)`：① 月營收 — fetch_monthly_revenue(months=15) → compute_yoy_mom 取 yoy_last3 + mom_last 拼 list[dict]；② MJ 快照 — list_snapshots(sid) 缺本季 yyyymm 自動 fetch_financial_statements + analyze_financial_health(news_context='', 零 LLM) + save_snapshot 補抓，最後取前 3 季 reverse 成時序；③ compute_trend_score 合議；單檔例外永遠 graceful（month/MJ/合議 3 段獨立 try）。**結果表渲染** `_render_mj_trend_table`：5 段 KPI metric 卡（強退步在前）+ DataFrame（退步在前排序）+ 逐檔細節 expander 顯示 monthly_detail / mj_detail JSON。**app.py archive 📊 MJ 體檢變化 Tab**：tabs tuple 11→10 拔 tab_mj_diff、labels 11→10 拔 '📊 MJ 體檢變化'、with-block 註解；模組 tab_mj_health_diff.py + mj_trend_score.py 全留磁碟（v18.188 batch 版內容保留方便未來啟用）。**tests/test_mj_trend_score.py 48 case**：TestSafeNum 7（None/int/float/str/garbage/NaN/inf）+ TestYoyStepScore 5（強進/緩進/0/緩退/強退邊界）+ TestSquash 5（0/1/3/飽和正負）+ TestLabelFromScore 6（5 段邊界）+ TestMonthlyRevenueSubscore 7（空/壞輸入/全強進 MoM 正/全強退 MoM 負/緩進 MoM=0/缺 yoy/缺 MoM/非 dict 跳）+ TestMjTrendSubscore 8（壞輸入/空/僅 1 季/2 季改善/2 季惡化/3 季同向改善/3 季分歧/>3 季吃最新 3）+ TestComputeTrendScore 9（皆空/雙強進/雙強退/月主導 MJ 缺/MJ 滿但月缺達不到 strong/自訂權重/越界 fallback/output shape/65 權重 mon 主導）；48/48 passed 0.11s + tests/ 1396 passed（v18.187 1348 → +48 新）零回歸；ruff All checks passed；py_compile 全綠；PR #196 squash @ b106460。**邏輯**：月+季加權合分，同向加權保留趨勢確認、分歧衰減防假訊號、 _walk_statuses 用大寫 Survival_Module 等掃 6 模組（fixture 對齊）；**邊界**：w_monthly 越界 fallback、n<2 快照 graceful 0、monthly_3m 空 list / 非 list / 非 dict items 全 graceful；自動補抓只補本季（fetch_financial_statements 只回最新季），歷史季靠 user 累積；**效能**：純函式 O(n)、UI 1 按鈕 1 progress bar 不自動觸發避免拖累批次分析；單檔 fetch 月營收 ~2-3s + MJ analyze ~1s（deterministic）+ 落 disk ~5ms，10 檔 ~30-40s；**Debug**：rows 每個 row 帶 monthly_detail / mj_detail dict + note 字串記錄哪段失敗，逐檔 expander 暴露子分數推導鏈讓 user 看到「月營收 +2.5 為什麼」+「MJ +1.0 為什麼」。下一輪可選：（a）UI 加「強退步雷股自動列入觀察清單 session_state」按鈕；（b）月權重從 slider 變 preset（短線/平衡/長線 3 鍵）

- **前一版**：v18.187_ArchiveMonthlyRevenueTab（user 截圖回報「🔴 全市場月營收抓取失敗」紅 banner — v18.180 `monthly_revenue_screener.py` 使用 FinMind `TaiwanStockMonthRevenue` 不帶 `data_id` 一次抓全市場（batch endpoint），但該端點已不再支援免費/sponsor tier。**v18.186 新增** 📊 MJ 體檢變化 Tab 已上線（基於 MJ 林明樟「4 力 1 棒子 + 現金流矩陣」跨期 status 比對，零 LLM 成本）→ 用此覆蓋個股財務體系變好/變差偵測需求，故封存 📈 月營收進退 Tab。**採 v18.182 封存 backtest Tab 同款套路**：UI 移除、模組保留磁碟。**app.py 3 處改動全在 UI 層（零模組刪除、零測試破壞）**：(1) **L1176-1181 tabs 註解+ tuple+labels**：上方加 3 行 `v18.187 ARCHIVED` 註解明示復活步驟，tuple 12 → 11 移除 `tab_rev_screener`、labels 12 → 11 移除 `'📈 月營收進退'`；(2) **L1587-1592 with tab_rev_screener body**：整 block 註解，上方加 `v18.187 ARCHIVED` 標題塊註解明示模組保留位置（`monthly_revenue_screener.py` 全留磁碟）；**復活步驟**：取消 3 段註解即可重生功能（1 分鐘工）。驗證：py_compile OK / ruff app.py All checks passed / pytest tests/ → **1348 passed 零回歸**（v18.186 1348 → 1348 完全持平，因模組未動測試全綠）；無新資料源 / 無 API 變動 / 無 session_state schema 變動。**邏輯**：UI 層完全移除月營收進退入口，user 看不到也按不到該壞掉 batch UI；**邊界**：模組保留磁碟確保未來 FinMind tier 恢復或改 per-stock parallel fallback 後啟用零學習成本，註解明示步驟；**效能**：少 1 個 lazy import + 1 個 tab 渲染 → 啟動微正向；**Debug**：3 段 `v18.187 ARCHIVED:` 註解可 grep 一次定位全部復活點；PR #194 squash @ 2680443。下一輪若 user 要恢復 month-revenue 功能，建議走 per-stock parallel fallback（ThreadPoolExecutor max_workers=20 跑 1700 檔單股端點 ~30-90s）取代 batch endpoint，免費 tier 跑得起來）
- **前一版**：v18.186_MjSnapshotIoUi（user 拍板 3 條後續路徑中選「IO + UI 包一個 PR」，建立 v18.185 mj_health_diff 的 end-to-end 使用流程，避免每次 session 重打 LLM 燒錢。**新檔 mj_snapshot_io.py ~140 行 stdlib zero-dep**：`save_snapshot(sid, yyyymm, mj_dict)` atomic write（.tmp → os.replace）防中途斷電壞檔 + `load_snapshot` + `list_snapshots(sid)` 降序 + `load_latest_two(sid)` 一次回 (prev, curr, p_ym, c_ym) 給 diff_mj_health 直用 + `list_all_stocks_with_snapshots()` UI 下拉清單用；`_sanitize_sid` 只留 [A-Za-z0-9.-] 防路徑注入（無 / 或 \ 就無法逃出 base_dir、BRK.B / 00929-DR 正常代碼保留）+ `_sanitize_yyyymm` 強制 6 碼數字拒絕 2025-03 / 25Q1 等格式；落地路徑 `data_cache/mj_snapshots/{sid}_{yyyymm}.json`。**新檔 tab_mj_health_diff.py ~120 行 Streamlit Tab zero LLM cost**：list_all_stocks_with_snapshots → selectbox 選股 → load_latest_two → diff_mj_health → verdict banner (improving/deteriorating/mixed/stable) + is_turnaround/is_breakdown 鏡像 banner + 變好/變差/不變 3 區塊表 + Raw verdict JSON debug expander；雜訊緩衝門檻 number_input (預設 1) 傳遞 min_net_delta；純 read disk 零 LLM 呼叫（新增快照走未來 MJ 體檢 Tab 補的存檔鈕，本 PR 先不處理）。**app.py 接入**：st.tabs() tuple 11→12，labels 加 '📊 MJ 體檢變化' 在 '📈 月營收進退' 之後（同屬個股財務體系兄弟）；with tab_mj_diff: render_mj_health_diff_tab() 區塊插在 tab_rev_screener 之後 tab_diag 之前。**tests/test_mj_snapshot_io.py 25 case**：TestSanitize 5（alnum/dot/dash 保留、路徑注入剝除、yyyymm 6 碼強制）+ TestSaveSnapshot 6（atomic write 無 .tmp 殘留、覆寫、4 種防呆）+ TestLoadSnapshot 4（缺檔/壞 JSON/非 dict JSON/壞輸入）+ TestListSnapshots 4（空/降序/sid filter/忽略無關檔）+ TestLoadLatestTwo 4（空/僅 1 季/2 季/取 top 2）+ TestListAllStocks 2（dedupe sort/empty）；25/25 passed 0.08s + tests/ 1348 passed（v18.185 1323 → +25 新）零回歸；ruff 全綠；py_compile 4 檔通過（含 app.py）；PR #192 squash @ 26fef2b。**邏輯**：IO 純讀寫不碰 LLM、UI 純讀 disk 不觸發任何遠端 IO；diff 仍由 v18.185 純函式做（職責分離）；**邊界**：_sanitize_sid 防路徑注入（無 / \ 就安全）/ _sanitize_yyyymm 強制 6 碼 / 缺檔/壞 JSON/非 dict 全 None graceful / 僅 1 季快照顯示 info banner 不爆；**效能**：atomic write 1 次 os.replace syscall、list_snapshots 只 stat 不 read、UI 完全 read-only 無 cache 需求；季更頻率資料一檔 ~3KB JSON、累計 100 檔×4 季=1.2MB 可忽略；**Debug**：所有失敗 print `[mj_snapshot_io] ... 失敗: {type}: {msg}`、Raw verdict JSON expander 讓 user 直接看 diff 細節。下一步：v18.187 可在 financial_health_engine 跑完後加「💾 存檔本季結果」按鈕閉環資料來源）
- **前一版**：v18.185_MjHealthDiff（user 給 spec「MJ 體檢表有沒有變好與有沒有變差的篩選」：對 `financial_health_engine.analyze_financial_health` 兩期結果做 status 等級比對偵測「變好/變差/不變」，純函式 zero-IO、無 LLM 呼叫，建立在 MJ 引擎吐 JSON 之後。**新檔 mj_health_diff.py ~270 行**：`STATUS_SCORES` 對照表把所有 MJ 標籤 normalize 到 0/1/2 分（Pass=2/Acceptable=1/Fail=0 + 🟢🟡🔴 + Good/Hard Work + Top Tier/Good/Weak + Strong/Weak + Excellent/Moderate/Fail + Thin Profit + Yes/No + None(leverage)=2 + High Debt Ratio→0 容錯前綴匹配）+ `MetricDiff` frozen dataclass（module/metric/prev_status/curr_status/delta+1/0/-1/direction）+ `HealthDiffVerdict` dataclass（improvements/deteriorations/unchanged list + verdict 4 段 + is_turnaround/is_breakdown 鏡像旗標 + `net_delta` property + `to_dict`）+ `_walk_statuses(mj_result)` 深掃 6 個 *_Module + 3 個 top-level 燈號（cash_ratio_status/ocf_status/debt_ratio_status）；特例抓 `Operating_Margin.Core_Business_Profitable` 與 `ROE.Leverage_Warning`，純字串 verdict/insight 略過不算 status。**核心 `diff_mj_health(prev, curr, stock_id, min_net_delta=1)`**：取共同鍵交集逐項比對（避免新增/移除指標誤判），Status 等級相同 → unchanged 不算雜訊（50.1% vs 49.9% 不算），未知 status → skip。**verdict 規則**：`improve − deteriorate ≥ min_net_delta` → improving；反之 → deteriorating；分歧 → mixed；全 unchanged → stable。**鏡像旗標**：Core_Business_Profitable No→Yes 標 `is_turnaround=True`（本業虧轉盈）/Yes→No 標 `is_breakdown=True`（本業盈轉虧）。**漏斗 `screen_health_changes(snapshots, mode, min_net_delta)`**：mode 4 種 improving/deteriorating/both(過濾 stable)/all；個別股 try/except 隔離單股失敗不影響整批；非 list 或 snap 非 dict / id 空字串直接 skip。**tests/test_mj_health_diff.py 43 case**：TestStatusScores 4（3 段/emoji/5 label 分類/binary）+ TestScore 8（直接命中/前綴匹配/High Debt 特例/None/非字串/空字串/未知）+ TestWalkStatuses 7（top-level 燈/6 模組掃描/CBP/leverage/純 verdict 略過/非 dict）+ TestDiffMjHealth 13（全變好/全變差/穩定/mixed/min_delta buffer/turnaround/breakdown/缺資料/共同鍵交集/未知 status 跳）+ TestScreenHealthChanges 8（mode 4 種/壞輸入/min_delta 傳遞）+ TestHelpers 3（net_delta/to_json_rows/to_dict shape）；43/43 passed 0.16s + tests/ 1323 passed（v18.184 1280 → +43 新）零回歸；ruff `mj_health_diff.py + tests/` All checks passed；py_compile 兩檔通過；零 app.py 改動（純資料層，下一輪可 wire-up 為 Streamlit Tab 渲染變好/變差紅綠矩陣）；PR #190 squash @ dc0a807。**邏輯**：ordinal score 比對 + 共同鍵交集 + 4 段 verdict，turnaround/breakdown 走 Core_Business_Profitable 獨立旗標路徑；**邊界**：Status 對應不到 _score→skip 不算/缺 prev 或 curr→empty verdict/min_net_delta 緩衝避免 1 項異動誤觸/共同鍵交集避免新增指標誤判；**效能**：純函式 O(n×m) n=股票 m≤20 指標線性掃，無 IO 無 LLM 呼叫 — 跑在 MJ JSON 已備好之後；**Debug**：每個 MetricDiff 帶 module/metric/prev_status/curr_status/delta，to_dict/to_json_rows 便於 UI 渲染紅綠雙色矩陣，is_turnaround/is_breakdown flag 獨立暴露給下游 UI 加註「轉機股/雷股」icon）
- **前一版**：v18.184_FundamentalScreenerV2（user 給 spec「財報轉強/基本面變好」進階篩選器：在已建立的台股財報模組之上新增 3 維度組合篩選，純函式設計易與 v18.180 monthly_revenue 與未來技術面/籌碼面條件層層疊加。**新檔 fundamental_screener.py ~220 行**：`@dataclass(frozen=True) ConditionResult`（code/passed/reason/is_turnaround flag）+ `@dataclass ScreenResult`（to_dict / is_turnaround property）+ 1 個 `_safe_num(obj, *keys)` 深層 None/缺欄/NaN/非數字一律回 None 守門 + 3 個條件判斷純函式 + 1 個 `screen_stocks` orchestrator + `filter_passed/to_json_rows` 便捷工具。**3 維度條件**：① **A 營收雙增** = 月營收 MoM>0 且 YoY>0（短期+長期動能皆向上） ② **B 三率三升** = 毛利率/營益率/淨利率 三項皆 QoQ>0 且 YoY>0 ③ **C 獲利成長與轉機** = EPS>0 且（YoY>0 或 淨利率虧轉盈）。**虧轉盈獨立路徑**：上年同期淨利率<0 且本季>0 → 直接 True 不走 YoY 增長率公式（避免基期負值/趨近零導致 % 失真），標記 `is_turnaround=True` 供下游 UI 加註「轉機股」icon。**架構**：`screen_stocks(stocks, enable_a, enable_b, enable_c, extra_checks)` 3 維度獨立 toggle + `extra_checks: list[Callable[[dict], ConditionResult]]` 擴充點預留疊加技術面/籌碼面（個別 callable 異常被吃掉不拖垮原 A/B/C）；passed = bool(conds) and all(passed) AND 邏輯，全 disable 視為 False。**防呆**：所有欄位 None/undefined/非數字/NaN → 該條件預設 False 絕不拋 Exception；個別股 try/except 隔離單股失敗不影響整批；輸入非 list 或 stock 非 dict / id 空字串直接 skip。**tests/test_fundamental_screener.py 50 case**：TestSafeNum 10（None/非 dict/NaN/缺鍵/字串強轉）+ TestRevenueDualGrowth 8（邊界 mom=0 strict>0/缺 yoy/壞型）+ TestTripleMarginUp 7（QoQ=0 strict/部分缺值/非 dict）+ TestEpsGrowthOrTurnaround 10（EPS≤0/淨利率虧轉盈/`prev_year_q=0` 不算虧轉盈）+ TestScreenStocks 11（enable flags/壞輸入/extra_check 拋例外被吃掉）+ TestHelpers 4（filter_passed/to_json_rows/`is_turnaround` property）；50/50 passed 0.13s + tests/ 1280 passed（v18.183 1230 → +50 新）零回歸；ruff `fundamental_screener.py + tests/test_fundamental_screener.py` All checks passed；py_compile 兩檔通過；零 app.py 改動（純模組，下一輪可由 user 拍板 UI wire-up 位置或先掛在「📈 月營收進退」Tab 加開關 toggle 切換規則）；PR #189 squash @ f8fe7ca。**邏輯**：3 條件純函式 + 1 orchestrator + 虧轉盈獨立路徑（不走失真公式）順著 spec 三段對應；**邊界**：strict>比較（0 不算過）/`prev_year_q=0` 不算虧轉盈（spec 明確「由負轉正」）/缺欄→False；**效能**：純函式 O(n) 線性掃，無 IO 無 cache，extra_checks 個股獨立 O(1) 不會 N²；**Debug**：每個 `ConditionResult.reason` 帶實際數字（如 `MoM=5.20% / YoY=30.10%`、`淨利率虧轉盈 -1.20% → 18.00%`）UI 直接渲染，`is_turnaround` flag 獨立暴露給下游 UI 加註轉機股 icon）
- **前一版**：v18.183_MultiSourceFallback_PCRScale（user 雙反饋：①「VIX/3M、Put/Call 仍顯示無資料但其他雷達燈正常」② 截圖顯示「PCR 選擇權比值 126.80 突破警戒上限（>1.5）→ 高風險」明顯為髒值。Root cause：①  v18.181 CBOE fallback 對 user NAS Squid 環境仍可能掛點（白名單問題）；② `leading_indicators.py` L993 寫入 `pcr_dict[dk] = putV/callV*100` 為百分比 scale（50-200），但 `config.py` L120-125 PCR threshold `red_above=1.5/yellow_above=1.2` 採標準 PCR ratio (0.5-2.0)，scale mismatch → 任何正常 PCR ×100 後都 > 1.5 觸發紅警示假陽性。修法 2 處：**(1) `risk_radar.py` 新增 `_fetch_stooq_csv(symbol)` helper + 擴充 chain**：打 stooq.com 公開 `https://stooq.com/q/d/l/?s={symbol}&i=d`，Date+Close parser + 守門 + console log；`_resolve_vix3m` chain 從 3→5 層（+stooq `^vix3m`/`^vxv`），`_resolve_put_call` chain 從 4→6 層（+stooq `^cpc`/`^cpce`）；label 動態反映實際源便於 UI caption 判讀。**(2) `macro_alert.py::fetch_macro_snapshot` PCR scale normalize**：L283-293 在 `snap['pcr'] = float(_pcr_raw)` 加 sanity guard，`_pcr_val > 10 → /100` 把百分比 scale 回除對齊 config threshold；註解明示 v18.182 PCR=126.80 假陽性 bug 修法。**保留行為**：`tab_macro.py` L3175-3182 既有 `<80`/`>150` 判斷仍走百分比 scale（讀原 `選PCR` column 不經 macro_alert.py）→ 兩條路徑互不衝突；`leading_indicators.py` 寫入邏輯零改動避免影響其他下游 consumer。驗證：py_compile OK / ruff `risk_radar.py` 0 errors（`macro_alert.py` 1 個 pre-existing F541 baseline 持平、非本次新增）/ pytest `tests/test_risk_radar.py + test_macro_alert.py` → **149 passed** ✅ / pytest tests/ 全 repo → **1230 passed 零回歸**（v18.182 1230 → 1230 完全持平）；無新 dependency / 無 API 變動 / 無 session_state schema 變動。**邏輯**：兩 fix 互相獨立 — fallback chain 處理「沒資料」、scale normalize 處理「髒值假警示」；**邊界**：stooq 不一定有所有 CBOE ticker → 全源失敗 graceful ⬜；PCR 正常區間 [0.5, 2.0] 完全不受 `>10 /100` guard 影響（向後相容）；異常值 PCR>10 必走 /100 path 即使原本是真實值也壓回 ratio 區間（trade-off：誤判 PCR=15 真實情境，但歷史上 PCR 超過 5 已是極罕見、超過 10 必為髒值）；**效能**：fallback chain Yahoo primary short-circuit 零變化；scale normalize O(1)；**Debug**：console log `[risk_radar/stooq] {symbol} HTTP {code}` + UI caption 動態 label 雙路徑可診斷 fallback chain；PCR 修法用註解明示 bug context，未來看 git blame 可秒理解）
- **前一版**：v18.182_BacktestArchived（user 反饋「請封存基金、股票的回測功能，等未來有需要再使用」→ stock app.py 純 UI 區塊移除，模組檔完整保留磁碟以利未來一行恢復。**3 處改動全在 app.py（零模組刪除、零測試破壞）**：(1) **L64-68 etf_dashboard import block**：`render_etf_backtest,` 該行加 `# v18.182 ARCHIVED:` 註解前綴；(2) **L1175-1178 st.tabs() tuple + labels**：tuple 12 → 11（移除 `tab_backtest`）、labels list 12 → 11（移除 `'🧪 回測找參數'`）、上方加 3 行重新啟用步驟註解；(3) **L1539-1553 ETF 組合 sub-section ②**：`render_etf_backtest(gemini_fn=gemini_call)` + 下方 divider markdown 兩行整段註解掉；(4) **L1562-1567 with tab_backtest body**：整 block 註解，上方加 ARCHIVED 標題塊註解明示模組保留位置（`tab_backtest_optimization.py` + `backtest_engine.py` + `tw_backtest.py` 全留磁碟）。**保留模組清單**：`tab_backtest_optimization.py` / `etf_tab_backtest.py` / `backtest_engine.py` / `tw_backtest.py` / `tests/test_tw_backtest.py` 全原封不動磁碟保留方便未來 1-2 分鐘恢復；**復活步驟**：取消 4 段註解即可重生功能。驗證：py_compile OK / ruff app.py All checks passed（baseline clean 維持）/ pytest tests/ → **1230 passed 零回歸**（v18.181 1230 → 1230 完全持平，因模組未動測試全綠）；無新資料源 / 無 API 變動 / 無 session_state schema 變動。**邏輯**：UI 層完全移除回測入口，user 看不到也按不到回測；**邊界**：模組保留磁碟確保未來啟用零學習成本，註解明示步驟；**效能**：少 2 個 lazy import + 1 個 tab 渲染 → 啟動微正向（~50ms）；**Debug**：4 段 `v18.182 ARCHIVED:` 註解可 grep 一次定位全部復活點。下一輪 user 若反饋仍要看到回測，取消註解 push 即可

- **前一版**：v18.181_RadarCboeFallback（風險雷達 2 燈無資料修復 — user 截圖回報「VIX/3M 期限結構」+「Put/Call」顯示無資料，根因 Yahoo 已停供 `^VIX3M` 與 `^CPC` 兩個 CBOE 衍生 ticker。修法：multi-source fallback chain：① **新增 `_fetch_cboe_csv(short_name)` helper**：打 CBOE 官方 CDN `https://cdn.cboe.com/api/global/us_indices/daily_prices/{short}_History.csv`（VIX3M/CPC/CPCE 等），DATE+CLOSE 兩欄 parser + 守門 200 + 空欄 + 異常 graceful，回 6mo tail Series ② **新增 `_resolve_vix3m()`**：Yahoo `^VIX3M` → Yahoo `^VXV` (legacy) → CBOE `VIX3M_History.csv` 三層 chain，回 (series, label_used) ③ **新增 `_resolve_put_call()`**：Yahoo `^CPC` → `^CPCE` → CBOE `CPC_History.csv` / `CPCE_History.csv` 四層 chain ④ `_signal_vix_term_struct` + `_signal_put_call_ratio` 改用 resolver，label 動態反映實際源（user UI caption 看到「Yahoo ^VIX / CBOE VIX3M_History.csv」即知走 fallback） ⑤ 既有 7 個既測（Yahoo 第二參數非空即 break）無破壞性變動 ⑥ tests/test_risk_radar.py +14 case：TestCboeCsvHelper 4（成功 parse / HTTP None / 500 / 缺 CLOSE）+ TestResolveVix3m 4（Yahoo primary / VXV fallthrough / CBOE fallthrough / 全失敗 empty）+ TestResolvePutCall 4（CPC primary / CPCE / CBOE / 全失敗）+ TestVixTermStructCboeFallback 1（CBOE label 注入驗證）+ TestPutCallCboeFallback 1（CBOE label 注入驗證）；77/77 risk_radar passed（63 + 14 新）+ tests/ 1230 passed（v18.180 1216 → +14 新）零回歸；ruff + py_compile 全綠。**邏輯**：每燈內部 graceful chain，每源獨立 try/except 不互相拖累；**邊界**：4 種 fallback 全失敗仍回 ⬜ 無資料但 note 明確標示「Yahoo + CBOE 全源失敗」助 debug；CBOE CSV 欄位變動（找不到 DATE/CLOSE）→ 印 log + 空 Series；**效能**：Yahoo 成功時直接 short-circuit 不打 CBOE，舊行為延續零成本；CBOE CSV 一次抓全歷史 .tail(180) 對齊 6mo 視窗；**Debug**：label 動態反映 Yahoo 還是 CBOE 路徑、便於 user 從 UI 一眼看出資料源；fund repo `services/risk_radar.py` 同樣需鏡像（下一 PR 或 user 觸發））
- **前一版**：v18.180_MonthlyRevenueScreener（user 需求「篩選近 3 期財報有進步或退步的股票」→ 改月報版（FinMind `TaiwanStockMonthRevenue` 每月 10 日公告，比季報快且免費 tier 即可）。新增「📈 月營收進退」Tab 插在「💎 高息網」之後（11 tab → 12 tab）。① **新增 monthly_revenue_screener.py ~350 行**（4 純函式 + UI）：`fetch_monthly_revenue(stock_id, months=18)` 單股 / `fetch_batch_monthly_revenue(months=18)`（不帶 data_id 一次抓全市場，避開 1700 檔逐股 ~15min API 風暴 → 單次 ~15-60s）/ `compute_yoy_mom(df_stock)` 純函式算近 3 月 YoY + 末月 MoM / `classify_trend(stats, yoy_threshold=15.0)` 純函式分類 / `screen_from_batch(df_batch, ..., name_map)` 分組 + 億單位轉換 / `filter_by_mode(df_result, mode)` 模式過濾 / `render_monthly_revenue_screener()` UI ② **判斷規則（user 拍板 YoY + MoM 雙條件最嚴格）**：🚀 強進步 = 近 3 月 YoY 全 ≥ +threshold% 且末月 MoM ≥ 0 / 📈 進步 = 全 > 0% 且末月 MoM ≥ 0 / 🔻 強退步 = 全 ≤ -threshold% 且末月 MoM ≤ 0 / 📉 退步 = 全 < 0% 且末月 MoM ≤ 0 / ➖ 中性 = 其餘 / ⚪ 資料不足 = 不足 15 月歷史 ③ **UI 流程**：FINMIND_TOKEN 守門 → 規則細節 expander → 3 column 篩選（YoY 門檻 slider 5-50% 預設 15 / 模式 radio 5 種：強進步/進步含強/退步含強/強退步/全部 / 顯示上限 number_input 10-500 預設 100）→ 「📡 抓取全市場月營收 + 計算」按鈕觸發 batch fetch + TWSE BWIBBU_d 名稱對照 → session_state cache → 5-column summary metrics（各 trend 檔數）→ 結果表（代碼/名稱/末月日期/末月營收(億)/YoY-2/YoY-1/YoY/MoM/趨勢，依 YoY 絕對值降冪 + topN）→ CSV utf-8-sig 下載 ④ **`@st.cache_data(ttl=21600)` 6 小時快取**對齊月營收每月公告節奏 ⑤ **app.py 改 2 處**：tab tuple 11 → 12（'📈 月營收進退' 插在 '💎 高息網' 之後）+ tab_rev_screener block 6 行 import + render ⑥ **tests/test_monthly_revenue_screener.py 29 case**：§1 compute_yoy_mom 6 case（15 月正確 / MoM 正確 / 短歷史 / 空 DF / 零分母不爆 / last_revenue 回傳） §2 classify_trend 12 case（4 基本 + 2 中性 + 3 不足 + 邊界 YoY 恰=門檻 + MoM 恰=0 + 自訂門檻 7%） §3 screen_from_batch 4 case（多股分組 / name_map / 億單位轉換 / 空 batch） §4 filter_by_mode 5 case（strong_up / any_up / any_down / all / 空） §5 TREND_LABELS 完整性 §6 整合 pipeline 強進步 end-to-end；29/29 passed 2.0s + tests/ 1216 passed（v18.179 1187 → +29 新）零回歸；ruff + py_compile 全綠。**邏輯**：YoY+MoM 雙條件命中數少但訊號最強，user 可調 yoy_threshold 寬鬆化；**邊界**：基期=0 不爆（None）/ 上市未滿 15 月歸資料不足非真中性 / 缺 token 友善 error / batch fetch 失敗紅 banner 指引；**效能**：batch 單次 FinMind call 省 95% 時間 vs 逐股迴圈；**Debug**：session_state cache 中介結果支援切換 mode/threshold 不重抓；TREND_LABELS dict 可單獨擴充 emoji；上市未滿 15 月新股自動歸「資料不足」UI 加 caption 提示）
- **前一版**：v18.179_TripleVerdictExtension（鏡像 fund v19.22+ 設計，將 v18.173 `synthesize_dual_verdict` 升級為「雙速 + 第三維度補充」— 新增 2 個 Optional kwargs `valuation_level`（"便宜"/"合理"/"偏貴"/"極貴"）與 `event_calendar_level`（"順風"/"中性"/"逆風"/"重大事件"）。**最小入侵 backward-compat 設計**：兩參數皆 None 時行為與 v18.173 完全一致（既有 9 個 test case 0 修改全綠）；至少一個提供時透過新 helper `_apply_third_axis_overlay(base, valuation, event_cal)` post-process 在 dual base verdict 上**只 append 註解到 action 與新增的 `third_axis_notes` list**，**不改 mode/icon/color/level**（dual 4-mode 決策表完全保留，下游 UI 零變動）。**疊加規則**：估值極貴 + 非 override_defense → 「估值頂部分位，建議減倉至中性」/ 估值便宜 + 非 adopt_slow → 「估值底部分位，可逐步擇機加碼」/ 重大事件 + adopt_slow → 「重大事件臨近，暫緩單筆加碼」/ 逆風 + adopt_slow → 「事件曆逆風，留意波動放大」/ 順風 + downgrade mode → 「事件曆順風，可酌量擇機」；override_defense（雷達極端警報）永不被任何第三維度註解（雷達壓倒一切）。**重構 5 個 return → 5 個 `_base = ...` + 單一 return overlay**（同時把第 1/3 branch 的隱式 fallthrough 改 elif/else 結構更清晰）。**tests/test_dual_verdict_ui.py +9 case TestThirdAxisOverlay**：backward compat（皆 None → notes=[] 不改）/ 估值極貴 in adopt_slow / 估值便宜 in downgrade_2 / **估值極貴 in override_defense 不疊加（壓倒測試）** / 重大事件 in adopt_slow / 順風 in downgrade_2 / 逆風 in adopt_slow / 兩維度同時觸發 2 條 notes / 中性值 noop。驗證：18/18 (9 既有 + 9 新) ✅；全 repo 1187 passed 零回歸 ✅；ruff/py_compile 全綠。**邏輯**：第三維度為「補充警語」非「決策推翻」，最低風險可漸進採用；**邊界**：override_defense 永不被第三維度升級或註解（雷達極端警報壓倒一切）/ 估值合理 + 事件曆中性 → noop（無冗餘輸出）；**效能**：純函式 zero-IO，常數時間 overlay；**Debug**：`third_axis_notes` 獨立暴露給 UI 想單獨 render 或統計用；fund v19.28 實證結論顯示 dual_verdict 真正價值在 real-time 防守而非 pre-warning，第三維度只在 user 明確提供 valuation/event 訊號時才生效，預設不影響 90% 用例
- **前一版**：v18.178_PhaseECrossSourceMatrix（Phase E 跨資料源比對矩陣 — 鏡像 fund Phase E。在 Phase 3 命中表（訊號視角）之後加新 expander「📡 跨資料源比對視角矩陣」：縱軸訊號 / 橫軸 TWII crisis 事件 / 單元格 ✅ Xd 提前 / ⚪ 未命中 / — 無資料；末列「📊 多源共識」= 該事件被多少訊號預警（≥3 高可信 / <2 低可信誤判率高）。串入 PR-A PMI Parquet 作第 5 訊號：① fetch_pmi_below_50_series 讀 tw_pmi.parquet threshold=50 below ② TW_SIGNAL_FETCHERS / DEFAULT_TW_SIGNALS 4→5 ③ _render_phase4_cross_source_matrix 複用 evaluate_signal_at_event 引擎不重發明 ④ +9 case (PMI fetcher 3 / 註冊 2 / matrix UI 源碼層 4) + 更新既有 2 case 從 4 改 5；tests/ 1185 → 1194 passed 零回歸；ruff/py_compile 全綠。**Phase D/E 收官**；下一步請手動觸發 update_macro_history.yml workflow_dispatch + bootstrap=true / years=25 → tw_pmi.parquet 首次填入後 Phase E 矩陣即可看資料源命中對比）
- **前一版**：v18.177_TwMacroNdcMigration（tw_macro NDC 抓取脫離 FinMind 付費牆 → data.gov.tw 國發會 OpenData 直連。tw_macro.fetch_ndc_signal_history / fetch_ndc_leading_index 走 FinMind TaiwanMacroEconomics dataset 被改 Sponsor 付費 tier（NT$500+/月），live tab 雖 try/except 包覆但實際無資料。新增 3 個 helper：① _dgtw_search_dataset_ids(keyword) — data.gov.tw search API 找 dataset ID（多 endpoint + 多 result shape parser） ② _dgtw_fetch_dataset_csv(ds_id, value_keywords) — metadata API 找 CSV → 全表解析 [date, value] 含 sanity (-1e6, 1e6) + 月度去重 ③ _dgtw_ndc_indicator_series(keywords, value_keywords, candidate_ids) — 二路徑彙整 search 找 ID + probe 鄰近 6097-6109 + 6053-6056（國發會景氣指標系列鄰近 PMI=6100 錨點）。兩 fetcher chain 改：dgtw PRIMARY → FinMind FALLBACK → graceful error；source 欄正確標示 'data.gov.tw' / 'FinMind' / None；信號分數 sanity ∈ [9, 45] 過濾髒值。+17 case (dgtw helper 7 / signal chain 4 / leading chain 3 / constants 3) + 順手清 1 個 pre-existing ruff E702；tests/ + test_tw_macro.py 1168 → 1185 passed 零回歸）
- **前二版**：v18.176_TwPmiParquetPhaseD（鏡像 fund Phase D 把台灣 PMI 月頻 history 補進 data_cache/。fetch_tw_pmi_history(start, end) 直打 data.gov.tw dataset/6100（國發會 NDC 提供，免 token、月頻穩定），3 個 metadata URL 變體 fallback 找 CSV → 全 CSV 解析（不只末筆）→ Parquet schema [date YYYY-MM-01, pmi float]；_parse_pmi_csv_full 含 sanity (20≤PMI≤80) + 月度 dedupe；DATASETS 4→5 加 tw_pmi、FETCHERS 註冊（needs_token=False）；PMI 月頻 ~300 列 / 25 年量極小每次 bootstrap 全 CSV 抓取 _merge_dedupe 自動去重無增量必要。+8 case (DATASETS/FETCHERS 註冊 2 / CSV parser 4 / full chain mock 2)；tests/ 1144 → 1152 passed 零回歸；ruff/py_compile 全綠）
- **前三版**：v18.175_PbRatioAuthoritativeAndIndustryBands（個股 P/B 升級 — 接續 v18.174 user 二度拍板：(a) **資料源走 TWSE 官方 PBratio 直取** PRIMARY、FinMind BS fallback，(b) **閾值依產業別動態調整**。① **三段資料源 chain**：PRIMARY = TWSE OpenAPI BWIBBU_d 個股 PBratio（伺服器端官方權威值，重用 yield_screener.fetch_twse_yield_pe() 1 日全市場 cache，過濾出 sid + sanity 守門 0.01<pb<100），BPS 反推 = 當前股價 / PBratio；SECONDARY = FinMind TaiwanStockBalanceSheet 季度 BPS = 股東權益 / (股本/10)；FALLBACK = yfinance bookValue ② **產業別閾值表**：金融保險業 / 銀行 / 證券 / 保險 → (0.5, 0.9, 1.2)（資產驅動 PB<1 屬正常）；半導體 / 電子工業 / 光電 / 通信網路 / 電腦周邊 / 其他電子 / 電子零組件業 → (1.5, 2.5, 4.0)（高 ROE / 智財權溢價）；製造業 default → (0.8, 1.5, 2.5)（慣例值，保持 v18.174 行為）③ **新增 4 個 helper**：_fetch_pbratio_from_twse(sid) / _fetch_industry_category(sid) 從 FinMind TaiwanStockInfo industry_category 抓 / _get_pb_bands(industry) 對照表 / _pb_bands_label(industry) caption 字串 ④ **UI 變更**：圖標題加產業 label（如 `📐 2330 台積電 股價淨值比河流圖（BPS 138.5元 × PB 1.5/2.5/4.0 · 成長科技（半導體業））`）+ 資料源 caption 改 3 段 chain 明示 + 閾值依據（產業別）+ 當前 P/B 顯示優先用 TWSE 官方值（無則自算）⑤ **tests/test_pb_industry_bands_and_twse.py 15 case**：§1 TWSE PBratio 抓取 + sanity 6 case / §2 產業閾值映射 8 case / §3 產業 label 4 case / §4 產業 fetcher 防呆 4 case / §5 TWSE→BPS 反推整合 1 case；15/15 passed + tests/ 1144 passed 零回歸（v18.174 1121 → +15 新）；ruff 兩檔全綠；py_compile 通過。PR #182 squash @ db667bc）
- **前四版**：v18.174_PbRatioDataSourceFix（個股股價淨值比 P/B 資料源修正 — User 回報「個股股價淨值比錯誤」。診斷：公式 ✅ 標準正確（tab_stock.py:1629 `P/B = 股價 / BPS`），問題在資料源（tab_stock.py:1595 `_fetch_bps()` 單靠 yfinance.Ticker().info['bookValue']，台股季報切換時段常落後 1-3 個月，且部分中小型股 / TPEx 直接無 bookValue 欄位）：① **新增 `_fetch_bps_from_finmind(sid)`** — 抓 FinMind TaiwanStockBalanceSheet 最新季度，取「股東權益總額」+「普通股股本」算 BPS；公式 = 股東權益總額 / (普通股股本 / 10 元面額)（台股股票面額固定 10 元，∴ 流通股數 = 股本 / 10）② **`_fetch_bps()` 改 chain**：FinMind PRIMARY → yfinance FALLBACK 兩段（FinMind 命中跳 yfinance、空退 yfinance、全失敗回 0.0）③ **Sanity 守門**：BPS ∈ (0.1, 5000) 元，範圍外回 0.0 避免單位錯亂污染河流圖橫帶 ④ **父子科目辨識**：避開「特別股股本」被誤抓為普通股股本（type 黑名單 + origin_name `特別股` 排除）；股東權益匹配 `Equity` / `TotalEquity` / `StockholdersEquity` 或 origin_name 含 `股東權益總額`/`權益總額`/`股東權益合計`/`權益合計` ⑤ 河流圖 caption 更新明示資料源與公式 ⑥ **tests/test_bps_data_source.py 13 case**：§1 公式正確性（TSMC 模擬 3.5T / 2593B → BPS≈135、金融股 1T / 80B → BPS=125）/ §2 PRIMARY/FALLBACK chain 3 case / §3 Sanity 守門 2 case / §4 多季只取最新 date / §5 父子科目辨識 2 case / §6 防呆 HTTP 500 + 缺欄位 3 case；13/13 passed 0.60s + tests/ 1121 passed 零回歸（v18.173 1108 → +13 新）；ruff 兩檔全綠；test_app_step4 2 failed pre-existing 無關。PR #181 squash @ 5a10e11）
- **前二版**：v18.173_DualVerdictSynth（A+B 雙向鏡像計畫 Step 4/4 收官 — 鏡像 fund v19.21 `synthesize_dual_verdict` 到 stock UI，把 v18.171 長期 regime 與 v18.172 風險雷達 level 合議成單一行動建議：① **tab_macro.py 3 處改動**：(a) hoist `_lt = None` 到 v18.171 dual-view try 外，供下方合議讀取；(b) `_render_global_risk_radar` 加 `slow_verdict: dict | None` 參數，10 燈渲染完之後 call `risk_radar.synthesize_dual_verdict(slow_level/score/color/icon/action, radar_level=_rs["level"])` 渲染「🤝 雙速合議」banner（icon + level 大字 + action 行動建議 + 規則 caption）；(c) call site 把 `_lt` 映射成 slow_verdict dict，`_lt["score"] × 5` 校準到 fund synth 期望的 ~[-10,+10] 範圍（_cls_lt 原 score 範圍 ~[-2,+2]，乘 5 保留閾值判斷語意一致性）② **決策表**：雷達 None/平靜 → adopt_slow（採用慢總經）/ 雷達警戒 + 慢樂觀 → downgrade_1 警戒觀察 / 雷達警戒 + 慢中性/悲觀 → downgrade_1 中性觀察 / 雷達警報 + 慢樂觀 (score≥+5) → downgrade_2 雙速分歧降槓桿（50-60% 倉位）/ 雷達警報 + 慢中性 → downgrade_2 偏空 25-30% 現金 / 雷達警報 + 慢悲觀 → downgrade_2 全面防守 35%+ / 雷達極端警報 → override_defense 立即減倉防守（慢總經暫不採信）③ synth 例外 graceful：print 不爆 + 不渲染 banner ④ **tests/test_dual_verdict_ui.py 9 case**：slow_verdict None / 雷達平靜 + bull adopt_slow / 雷達警報 + bull downgrade_2 / 雷達極端 override_defense / 雷達警戒 + bull 警戒觀察 / 雷達警戒 + bear 中性觀察 / 缺欄位 fallback / synth 例外 graceful / AppTest 保護門也覆蓋 slow_verdict 路徑；72/72 passed (63 risk_radar + 9 dual_verdict_ui) 0.88s + tests/ 1108 passed 零回歸（v18.172 1099 → +9 新）；ruff 三檔全綠 + py_compile 通過；test_app_step4 2 failed pre-existing 與本 PR 無關。**🎉 A+B 雙向鏡像計畫收官** — Fund v19.23/24/25 + Stock v18.172/173 共 4 PR 完成；PR #180 squash @ 7c9a409）
- **前二版**：v18.172_GlobalRiskRadar（A+B 雙向鏡像計畫 Step 3/4 — 鏡像 fund v19.20 services/risk_radar.py 到 stock 端，補拐點偵測中心 5 大訊號全是「月～季級慢速 macro inflection」對 1-day 急殺事件結構性盲眼：① 新增 **risk_radar.py 532 行**（detect_risk_radar / summarize_radar / synthesize_dual_verdict 三公開函式）；10 燈清單：VIX 級距（≥30 或 +20% → 🔴）/ VIX/VIX3M 期限結構（≥1.10 → 🔴）/ HY OAS 1-day Δ（≥+30bp → 🔴）/ 10Y 1-day Δ（≥+10bp → 🔴）/ MOVE 級距（≥130 → 🔴）/ SPX vs 50/200 DMA / SOX 單日跌幅（≤-3% → 🔴）/ 防禦 (XLP+XLU+XLV) vs 攻擊 (XLK+XLY+XLF) 30D 動能差（≥+4pp → 🔴）/ CBOE Put/Call（≥1.20 → 🔴）/ Nikkei+HSI 亞洲夜盤（≤-2.5% → 🔴）；每燈獨立 try/except 單一資料源掛點不拖垮其餘 9 燈；summarize_radar 規則：red≥4 極端警報 / red≥2 警報 / red+yellow≥4 警戒 / 其餘平靜 ② **tab_macro.py +99 行**：`_render_global_risk_radar(fred_api_key)` 模組層 helper（10 燈 5×2 grid + 整體 level banner + 細節 expander）+ call site 插入紅綠燈卡與 v18.171 長短期雙視角面板之後、blue box「今日市場總覽」之前 ③ **AppTest 保護門**：fred_api_key < 30 字元 → 跳過渲染（測試環境 key 通常 14 字元，真實 32 字元，避開 4×~15s 序列 HTTP 撞 240s budget）④ 完全 verbatim 鏡像 fund v19.20，僅換 import `repositories.macro_repository` → `macro_core`（fetch_fred/fetch_yf_close 簽名相容）；透過既有 NAS proxy 抓 Yahoo + FRED（BAMLH0A0HYM2/DGS10）⑤ synthesize_dual_verdict 預先 port（Step 4/4 會接 UI）⑥ **tests/test_risk_radar.py 63 case**：10 燈每燈 calm/yellow/red + summarize 7 case + dual_verdict 10 case + 整合 detect_risk_radar 5 case；63/63 passed 0.26s + tests/ 1099 passed 零回歸（v18.171 1036 → +63 新）；ruff 三檔全綠；test_app_step4 2 failed 確認 pre-existing 與本 PR 無關。PR #179 squash @ 06292a3）
- **前二版**：v18.171_DualViewMacroTopOfPanel（純 UI 位置調整 — 將「🔭 長短期雙視角總經判讀」面板從 tab_macro.py 拐點 expander 之後（原 L2362, 8-space indent）上移到紅綠燈卡填完後、「今日市場總覽」blue box 之前（L223, 4-space indent），緊接「綜合健康度燈號」之下，並於 expander 結尾外掛 `st.divider()` 與下方 blue box 視覺隔開。功能 / 演算法 / 介面行為零變動：純函式 `classify_long_term_regime / classify_short_term_regime` 不動；`session_state.get('macro_info')` 讀取時機向前移但 Streamlit rerun 後仍能讀到正確值；`_lt / _st_r / _mi_d` 均為 try-block 區域變數無耦合；try/except + print 包覆確保渲染失敗不阻斷後續 tab。`python -m py_compile tab_macro.py` ✅；`pytest tests/test_macro_helpers.py` → 69 passed ✅）
- **前二版**：v18.170_LongShortMacroDualView（總經 tab 加「長期 vs 短期」雙視角總經面板 — 鏡像 fund services/macro_service.py 的 `identify_regime()` 長期景氣象限 + risk_radar 短期動量雙軌設計，但對齊台股財報季（Q1/Q2/Q3/Q4 5/8/11/3 月）改用「1Q 視角」短期分類：① `macro_helpers.classify_long_term_regime(cpi_yoy, fed_rate, fed_prev_rate, ndc_score, pmi, mk_signal)` 純函式 — 12M 視角加權打分（CPI 25% / Fed 方向 20% / NDC 景氣燈號 20% / 台 PMI 20% / MK 拐點 15%），每項 [-2, +2] 加權合分，>=+1 🟢 成長期 / >=0 🔵 復甦期 / >=-1 🟡 過熱期 / <-1 🔴 衰退期，含 suggest_pct 持股建議 ② `macro_helpers.classify_short_term_regime(export_yoy, pmi, vix_current, fi_streak_days, cpi_yoy, cpi_prev_yoy)` 純函式 — 1Q 視角加權打分（出口 YoY 25% / PMI 25% / VIX 15% / 外資連續 20% / CPI 月降 15%），>=+0.8 ⚡ 偏多 / >=-0.3 ⚖️ 中性 / <-0.3 ⚠️ 偏空，含 action 短線建議 ③ MK 訊號邏輯保護：只當至少一個主指標存在時才計入 MK，避免 MK 獨自驅動 regime；資料全空 → ⚪ 資料不足 ④ tab_macro.py L2360 拐點 expander 結束後新增「🔭 長短期雙視角總經判讀」並排 2 卡（🏔️ 長期 12M ｜ ⚡ 短期 1Q），每卡 regime 大字 + 綜合分數 + suggest/action + components 細項 caption 拆解每指標分數，總共 ~100 行 UI ⑤ 教學保護 expander「🔰 為什麼要分長期 / 短期？」含雙視角定義 + 4×3 決策矩陣 markdown 表（長期🟢🔵🟡🔴 × 短期⚡⚖️⚠️ = 12 組行動建議）+ 兩者矛盾時處理原則（長期為主、短期為輔）⑥ 純複用既有 fetcher（CPI/Fed/NDC/PMI/VIX/Export 6 個 + session_state 外資連續日數），零新 fetcher、零動 pool、零動註冊表 ⑦ tests/test_macro_helpers.py +22 case（長期 11 個：4 regime 各一情境 + 資料不足 + MK 獨自不算 + 部分資料 + components 結構 + 字串 graceful + 分數範圍 + 強 vs 弱 MK 加成；短期 11 個：⚡⚖️⚠️ 3 regime + 資料不足 + 出口單項 + 外資正反 + VIX 邊界 + components 五項 + 字串 graceful + CPI 月降影響 + action 訊息）；tests/ 1036 passed 零回歸；ruff 三檔全綠；無新增檔、無新增 fetcher，純加 UI 面板 + 2 個 helper + 22 tests）
- **前二版**：v18.169_MkGoldenInflection（總經 tab 加 MK 黃金拐點 + 面板簡化 — 鏡像 fund services/macro_service.py::_detect_inflection 核心訊號「⭐ MK 黃金拐點 = CPI YoY × Fed Funds Rate 雙頂回落」：① 新增 `_fetch_fed_funds()`（FRED FEDFUNDS 月均利率，2 層備援 fredgraph.csv → FRED-API 帶 key 加速；prev/current 月度比較）併入既有 5 fetcher 並行池（max_workers 5→6）② `_fetch_cpi` 三路徑全部補 `prev_yoy` 欄位（fredgraph / FRED-API / BLS）③ `macro_helpers.detect_mk_golden_inflection(cpi_yoy, cpi_prev_yoy, fed_rate, fed_prev_rate)` 純函式，雙噪聲門檻 ±0.05ppt 防雜訊：CPI 月降 ≥0.2 + Fed 持平/降 → ⭐ 強訊號（多頭最佳買點）；CPI 月降 [0.05,0.2) + Fed 持平/降 → ✅ 弱訊號觀察中；任一上升 → None ④ 拐點面板 expander 標題「五大面向」→「六大面向 + MK 黃金拐點」L2026-2350 新增第 7 try/except block 呼叫 helper、註冊表（L1715 + L1984）+ AI context（L4284, L4318）同步補 fed_funds / prev_yoy 4 個量化欄位給 Gemini ⑤ **面板去重簡化**：Section 七 KPI 3 卡（M1B-M2 / 年線 240 / 月線 20）→ 2 卡（M1B-M2 / 年線+月線統合卡，月線併入副標「月線20MA: +X% (過熱/機會/正常)」）+ 加 caption「📖 完整乖離訊號 → 詳見頂部拐點面板第 2 面向」⑥ Section 八 Row2 layout [1,2] → [1,1,2]：CPI 旁加 Fed Funds Rate KPI（紅黃綠色碼門檻 5/3/0%，副標 trendline ↓→↑ + 上月對照），原 CPI caption 教學「Fed 目標值 = 2%」原樣保留 + Fed KPI 加新 caption「💡 與 CPI 配對：兩者同步月降 → ⭐ MK 黃金拐點」⑦ 教學保護清單全保留（L234 紅綠燈教學 / L2237 拐點 expander / L2313 熱錢三角 / L3799 三環攻擊 / L4120 AI 總裁決）⑧ tests/test_macro_helpers.py +12 新 case（強訊號 CPI+Fed 雙降 / 強訊號 CPI 降 Fed 持平 / 弱訊號 / CPI 升無訊號 / Fed 升無訊號 / CPI 持平噪聲區 / 缺資料 / 字串 graceful / detail 含數值 / 0.20 ppt 邊界強/弱 / 0.06 ppt 剛過噪聲 / Fed 微升 0.03 在噪聲區仍強訊號）；tests/ 1014 passed 零回歸；ruff 三檔全綠；後續可選 Wave 2 加 HY/Sahm/CFNAI 三信號補齊 fund _detect_inflection 全套）
- **前一版**：v18.168_StockFactorPool7（stock 多因子池 4 → 7 — 補強「量價結構 + 融資加速度 + 已實現波動率」三個 parquet 衍生因子，零新基礎設施／零新 cron／全用既有 `data_cache/`：① TWSE_VOL_RATIO（volume / SMA60 - 1 → z-score，above；異常爆量警戒）② MARGIN_GROWTH_5D（margin_balance.diff(5) 億元，above；散戶 5 日加碼加速）③ TWII_REALIZED_VOL_20D（returns.std × √252 × 100，above；本地 VIX 替代品）；3 個新 fetcher + TW_SIGNAL_FETCHERS registry + FactorSpec 全 source="local"；fund v18.296 已先擴成 13 因子，stock 跟進；69 unit cases passed（新 9 個）+ 1002 全測試 passed 零回歸；ruff 全綠；SHORT_BALANCE / USD_TWD / MOVE / NFCI / 銅金比 / PCR / 三大法人 / ADL 留 Wave 2-3）
- **前一版**：v18.167_BacktestOptimizationTab（新增 top-level Tab「🧪 回測找參數」排在「💰 ETF質借模擬」之後 — 完整版多因子權重最佳化 workflow：📖 操作說明 expander（含核心數學公式 + 6 slider 解釋）+ 1️⃣ TWII 回撤門檻 + 抓資料按鈕 → 2️⃣ 因子 multiselect + Plateau 目標 radio (F1/Sharpe) + 6 顆 slider（grid 步長 / 鄰域半徑 / λ / train / test / 警戒線）+ 3️⃣ 高原最佳權重 metric 卡 + CSV 匯出 + 4️⃣ plateau 2D heatmap / 3D surface toggle + 5️⃣ walk-forward 各折表 + OOS F1/Sharpe + CSV；引擎複用 v18.165 multi_factor_optimization.py 零重寫；macro validation 內 expander 並存「快速版」）
- **前一版**：v18.165_MultiFactorPlateauWalkForward（Phase 3 加「🔬 多因子權重最佳化」expander — 綜合分數 S_t = Σ w_i × normalize(I_{i,t−1})（lag=1 防未來引用），simplex 權重 grid sweep 算 F1+Sharpe，**plateau 評分 = 鄰域 mean − λ × std**（不取單一最高 F1）；walk-forward 滾動 train/test 串 OOS 權益曲線；plotly 2D heatmap + 3D surface toggle；4 因子池（FOREIGN_SELL_5D / MARGIN_BALANCE / M1B_M2_DIFF / TWII_DROP_20D 全 source="local"）；FactorSpec source literal 擴 yahoo|fred|local 兼容台股本地訊號；鏡像 fund v18.285；28 case 驗收 + ruff 全綠）
- **前一版**：v18.164_MT5StyleAutoCalibration（Phase 3 加「🎯 MT5-style 自動校準」expander — walk-forward 4 折 grid sweep × 3 重 anti-overfit gate（折間票選 + drift>30% 回退 + cap 守門）；session-only override 機制；對齊 fund v18.283；963 全測試 passed）
- **前一版**：v18.163_SignalPrecisionAnalysis（Phase 3 加「📐 訊號精確率分析」forward-looking 區塊 — 解召回率單面向：遍歷歷史 crossings 算 TP/FP/精確率/誤報率/avg lead time；對齊 fund v18.282；953 全測試 passed）
- **前一版**：v18.162_EtfMarginSimulator（ETF 質借倒金字塔加碼模擬器：4 風格 preset × 3 階梯觸發 + HWM 回撤 + 擔保維持率 140/130% 爆倉檢測；新 Tab「💰 ETF質借模擬」+ 43 case 全綠 + 全測試 948 passed）
- **前一版**：v18.161_EdgeDetectionUiSurfacing（Phase 3 標題加「v2 轉折偵測」+ 提前 → 轉折提前）
- **前二版**：v18.160_EdgeDetectionFix（永遠在警戒 = 假預警修復 + bootstrap 5 → 20 年）
- **前三版**：v18.159_MacroSignalLookbackTW（台股本地 4 訊號 × TWII crisis 命中率 — 對齊 fund Phase 3）
  - **User 需求**：截圖回報「基金有這測試總經的預測力（VIX / HY / T10Y2Y / UNRATE × 5 場 SPX crisis 命中率），台股沒有看到」+ 選「台股本地 4 訊號」方案
  - **4 個本地訊號**（讀 `data_cache/` 既有 4 表，**無需額外抓數據**）：
    | 訊號 | 資料源 | 閾值 | 解讀 |
    |---|---|---|---|
    | 外資 5 日累積買賣超 | finmind_inst.foreign_buy | sum5 ≤ -500 億 | 連 5 日合計大賣 |
    | 融資餘額 | finmind_margin.margin_balance | ≥ 3400 億 | 散戶槓桿過熱 |
    | M1B/M2 缺口惡化 | finmind_m1m2.m1b_m2_gap.diff | ≤ -2 pts/月 | 資金流出股市 |
    | TWII 20 日跌幅 | twii_ohlcv.close.pct_change | ≤ -5% | 加速下跌確認 |
  - **macro_signal_lookback_tw.py**（新檔 ~310 行純函式，**公式鏡像 fund services/macro_signal_lookback.py**）：
    - `TwSignalSpec` frozen dataclass + `TwSignalLookback` mutable dataclass
    - 4 個 series fetcher：`fetch_foreign_sell_5d_series` / `fetch_margin_balance_series` / `fetch_m1b_m2_diff_series` / `fetch_twii_drop_20d_series`（各自 `_load_parquet_safe` 缺檔 graceful）
    - `TW_SIGNAL_FETCHERS` registry dict + `fetch_all_tw_signal_series` 批次
    - `evaluate_signal_at_event` — 點觀測（peak - offset 日 ≤ target 最近一筆）+ 峰前最早警戒搜尋（max_lookback_days 內）
    - `lookback_all_signals_tw` 批次
    - `compute_signal_hit_rate` — 命中率 = n_hit / n_covered、平均提前天數
  - **tab_macro_validation.py**（149 → 252 行）：
    - 加 `_PHASE1_CACHE_KEY` / `_PHASE3_CACHE_KEY` session_state cache，**修 v18.157 既有 button rerun 吞 events bug**（鏡像 fund v18.261 PR #117 fix）
    - 上方「跑歷史驗證」按鈕觸發 Phase 1 events 計算 → 存 cache；門檻變動失效 Phase 3 cache
    - 新 `_render_phase3_signal_section(events, cache_dir)`：2 sliders（offset 30-180 / 上限 90-540 日）+ 跑按鈕 + 命中率總覽表（訊號 / 閾值 / 涵蓋事件 / 命中事件 / 命中率 / 平均提前天數 / 解讀）+ 逐事件 × 逐訊號明細表（✅ 提前 Xd / ❌ (值) / —）
  - **tests/test_macro_signal_lookback_tw.py**（新檔 21 case 全綠）：DEFAULT_TW_SIGNALS 完整性 + Registry 對齊 / _is_warning above/below 邊界（閾值包含） / 4 fetcher 缺檔 graceful + 正常 schema 計算（rolling sum / 億元轉換 / monthly diff / pct_change(20)） / fetch_all 批次缺檔 / evaluate value_at_lookback / triggered / first_warning earliest / below direction / lookback_all_signals_tw dict 結構 / hit_rate empty / mixed coverage 公式
  - **回歸**：tests/ **901 passed**（v18.157 880 + 21 新）零回歸
  - **下一步**：使用者進 Tab macro 看 section 十 — 上方按 「跑歷史驗證」→ 下方看 Phase 3「總經訊號預測力驗證」表
- **前一版**：v18.158_LeadingTableRedGreen（先行指標表配色換台股慣例 — 紅漲綠跌 + ▲▼ icon）
  - **User 需求**：截圖回報「台股這顏色我分辨不出來」— `render_leading_table` 原配色「正藍 #58a6ff / 負紅 #f85149」在 OLED 螢幕藍色偏紫，與紅幾乎同色相。User 選「台股紅綠（紅漲綠跌）+ 三角 icon」方案
  - **leading_indicators.py:render_leading_table** 改 fmt() + sty() 兩 fn（line 1262-1304）：
    - **fmt** 加 ▲/▼ icon prefix（BRACKET / SPOT / 韭菜指數 三類數值）。BRACKET 負數保留會計慣例 `(43,596)` 加 `▼ ` 前綴；正數 `▲ ` 前綴
    - **sty 色彩語意**：BRACKET / SPOT / 韭菜指數 / 未平倉口數 — 正紅 `#f85149` 負綠 `#3fb950`（台股紅漲綠跌）
    - **PCR 反向語意**：< 80 看多紅、> 120 看空綠（原本相反；台股紅漲綠跌邏輯）
    - **融資餘額不變**（紅 = 過熱警示 / 綠 = 寬鬆），這是水位指標非漲跌
    - **韭菜指數維持「直接視角」**（散戶淨多正數 = 紅；反向解讀由 user 自行）；line 793 `render_table` 是 legacy（已用紅綠 + 反向韭菜邏輯）dead code 未動
  - **tests/test_leading_indicators.py**：5 個 PCR case 更新 — `test_pcr_below_80_blue` 改 `test_pcr_below_80_red`、`test_pcr_above_120_red` 改 `test_pcr_above_120_green`，斷言色從 `#58a6ff` 改 `#f85149`、`#f85149` 改 `#3fb950`
  - **回歸**：tests/test_leading_indicators.py **47 passed** 零回歸
  - **下一步**：refresh Tab1 macro 看新配色，正負分辨度應大幅提升
- **前一版**：data_cache 4/4 全綠 ✅ + 標題但無值清單三件
  - **M1B/M2 終於進去**：經 9 輪 iteration 確認 CBC PXWeb API 真實結構 `sdmx["data"]["dataSets"]`（list of `[YYYYMmm, value, ...]`），寫順鏈 parser；bootstrap 抓 5 年得 58 個月度資料 + YoY gap 欄位
  - **CBC API breaking change 記事**：舊 `ms1.json` 全 404 / 回 HTML、SDMX EF15M01 合表 API 改回 metadata + dataSets；新 parser 用 EF19M01（M1B）+ EF21M01（M2）分表抓再 merge on period_raw
  - **BFI82U 教學卡**：tab_edu 加 handler 抽外資 net 顯示為億；BWIBBU_d 仍標散點需個股 Tab
  - **ETF N/A 強化**：內扣費用率 / Beta / AUM / 折溢價率 / 追蹤誤差 5 個 metric 加 help= hover
- **前一版**：v18.157_PureTwiiDrawdown（C 案降級 — 砍 NDC + 領先指標，Section 十 改純 TWII drawdown 事件表）
  - **背景**：v18.151-156 連 6 輪 PR 嘗試解 NDC 卡關（FinMind enum 改名 → 付費牆 → NDC SPA HTML → data.gov.tw search 無對應 dataset → NAS FastAPI 8765 端 user 未架設）。User 拍板「股票跟基金一樣只走 Squid Proxy 3128」— fund 從來沒用 NAS_BASE_URL FastAPI，只用 PROXY_URL Squid，stock 應該對等
  - **update_macro_history.py**（819 → 419 行，砍 395 行）：移除 `_NDC_*_URL_CANDIDATES` / `_NDC_VALUE_KEYS` / `_NDC_DATE_KEYS` / `_fetch_via_nas_relay` / `_try_ndc_via_relay` / `_DGTW_*` constants / `_fetch_dgtw_search_dataset_ids` / `_fetch_dgtw_dataset_csv_full` / `_fetch_dgtw_indicator` / `fetch_ndc_signal` / `fetch_ndc_leading_index`；DATASETS 從 6 個剩 4（twii / inst / margin / m1m2）；FETCHERS dict 同步
  - **macro_validation_tw.py**（344 → 157 行，砍 187 行）：移除 `load_ndc_signal_from_parquet` / `load_leading_index_from_parquet` / `NdcVerifyResult` / `verify_ndc_signal_vs_crises` / `LeadingIndexVerifyResult` / `compute_smoothed_change_pct` / `verify_leading_index_vs_crises` / `compute_hit_rate`；只保留 `load_twii_close_from_parquet` + `TwiiCrisisEvent` + `detect_twii_crisis_events`
  - **tab_macro_validation.py**（258 → 131 行，砍 127 行）：rewrite UI — 純 TWII drawdown 事件表（1 slider 回撤門檻 + TWII + crisis 紅區走勢圖 + 事件清單表 + CSV 下載）；移除 NDC/領先指標命中表 + 命中率卡 + 3 slider 變 1 slider
  - **tests/test_update_macro_history.py**（479 → 112 行）：砍 _FakeResp + dgtw 11 case + ndc 6 case 共 17 個 NDC/dgtw/relay 測試
  - **tests/test_macro_validation_tw.py**（346 → 154 行）：砍 NDC/leading/hit_rate/smoothed_change 共 17 個測試；test_load_handles_corrupt_parquet 改用 twii 檔
  - **回歸**：tests/ **880 passed**（v18.156 918 - 38 NDC/dgtw/relay 測試 = 880）零功能回歸
  - **未來還原路徑**：若 user 將來 SSH 進 NAS 跑 FastAPI 8765 + 設好 `NAS_BASE_URL` secret → revert PR 即恢復；目前選擇與 fund 對等的最簡架構
- **前一版**：v18.156_MacroSectionSwap（macro tab section 十 ↔ 十一 交換 — 歷史驗證上、AI 總裁決下）
  - **User 需求**：「十一、總經訊號歷史驗證 往上移到十 交換」— UI 排序調整
  - **tab_macro_validation.py**：內部 markdown header「十一、」改「十、」（兩處：docstring + render markdown）
  - **tab_macro.py**：插入新 section 十（render_history_validation_section call）在原 section 十之前；原 AI 總裁決的 section_header 從 '十' 改 '十一'；刪除文件末尾的重複 validation block（避免渲染兩次）
  - **tests/test_macro_validation_tw.py**：`test_ui_section_after_section_ten` rename + 邏輯改為「歷史驗證（十）必須在 AI 總裁決（十一）之前」
  - **回歸**：tests/ **918 passed** 零回歸
- **前一版**：v18.155_NdcRelayWithAjaxHeader（user 提醒 NAS 中繼站 → 強制走 relay 帶 AJAX header 觸發 SPA 回 JSON）
  - **背景**：v18.152/153 NDC URL 全敗的根因錯怪：不是 NDC URL pattern 錯，而是**透過 Squid Proxy 抓沒帶 `X-Requested-With: XMLHttpRequest`**。`nas_server.py:35` 的 `_HDR` 已配 AJAX header，AngularJS SPA 偵測到 XHR 應該會切回 JSON 模式（典型雙模式 SPA 行為）— 但 `fetch_url` 內 Squid 是 transparent forwarder 不會自動加這 header
  - **解法**：新增 `_fetch_via_nas_relay(url, label)` 直接呼叫 `proxy_helper.nas_relay_fetch()` 跳過 Squid 優先序，header 由 NAS 中繼站 FastAPI 自動填（X-Requested-With + Accept: application/json）
  - **update_macro_history.py**：
    - 重啟 `_NDC_SIGNAL_URL_CANDIDATES` / `_NDC_LEADING_URL_CANDIDATES`（v18.154 砍掉的）+ `_NDC_VALUE_KEYS` / `_NDC_DATE_KEYS`
    - 新增 `_fetch_via_nas_relay(url, label)` — 強制走 NAS 中繼站；JSON 解析 + verbose log（CT + body 前 300 char）
    - 新增 `_try_ndc_via_relay(candidates, label)` — 逐一試 candidate；第一成功就 break
    - `fetch_ndc_signal` / `fetch_ndc_leading_index` 三路徑彙整：① relay+AJAX header ② data.gov.tw search ③ data.gov.tw ID probe（保留 v18.154 dgtw 作兜底）
  - **tests/test_update_macro_history.py**：新增 7 case：candidate URL constants 完整性 / `_fetch_via_nas_relay` 成功 / NAS 未設定 graceful / HTML 回應 graceful / `_try_ndc_via_relay` 短路 / `fetch_ndc_signal` relay 成功不走 dgtw / relay 失敗 fallthrough 到 dgtw；既有「全失敗 graceful」測試補 monkeypatch relay
  - **回歸**：tests/ **918 passed**（911 + 7 net）零回歸
  - **下一步**：merge 後 bootstrap 一次 → log 會印 `[ndc-relay/...]` 系列訊息，若 SPA 確實對 AJAX header 回 JSON → ✅；若 relay 也回 HTML → 走 dgtw 兜底；若 dgtw 也敗 → §5 警鈴啟動 C 案
- **前一版**：v18.154_DgtwSearchFallback（最後一輪 — 棄 NDC SPA → data.gov.tw search API + dataset CSV probe）
  - **背景**：v18.153 診斷確認 `index.ndc.gov.tw/app/data/indicator/*` **全 5 個 candidate URL 回相同 AngularJS SPA index HTML**（`<html ng-app="NDCapp">` + `csrf-token` meta）— SPA client-side routing fallback 結構性 dead，繼續加 candidate slug 沒用
  - **解法**：完全砍 NDC OpenAPI，改用 **data.gov.tw 政府資料開放平臺**（鏡像 `macro_core._pmi_src_dgtw` 已驗證 dataset/6100 PMI 在生產 work 的模式）
  - **update_macro_history.py**：
    - 移除 dead code：`_NDC_SIGNAL_URL_CANDIDATES` / `_NDC_LEADING_URL_CANDIDATES` / `_NDC_VALUE_KEYS` / `_NDC_DATE_KEYS` / `_fetch_ndc_indicator_full`（v18.152/153 殘骸）
    - 新增 `_DGTW_*` constants：3 個 signal keywords + 3 個 leading keywords + 16 個 candidate IDs（6097-6109 + 6052-6056；國發會系列 ID 鄰近 6100 PMI）+ 6 個 signal value 欄關鍵字 + 3 個 leading value 欄關鍵字 + 9 個 date 欄關鍵字
    - 新增 `_fetch_dgtw_search_dataset_ids(keyword, label)` — 打 3 個 search API 變體（v2/v1/front）+ 多 result shape parser；印 candidate IDs + titles
    - 新增 `_fetch_dgtw_dataset_csv_full(ds_id, value_keywords, label)` — metadata API 取 resources → 找 CSV → 下載 → 全 row 解析 [date, value]（不限末筆，bootstrap 用），完全鏡像 `_pmi_src_dgtw` 但回完整 DataFrame
    - 新增 `_fetch_dgtw_indicator(keywords, value_keywords, candidate_ids, label)` — 二路徑彙整：① search API 找 ID ② 直接 probe 鄰近 ID 範圍
    - `fetch_ndc_signal` / `fetch_ndc_leading_index` 內部改呼叫 `_fetch_dgtw_indicator`
  - **tests/test_update_macro_history.py**：替換 7 個 NDC OpenAPI 測試 → 11 個 dgtw 測試（含擴強 `_FakeResp` 支援 CSV bytes + Content-Type）：constants 完整性 / search 解析 results / search 0 results / search HTML fallback / dataset CSV parse 完整 / dataset 無 CSV resource / CSV 無 value 欄 / search→dataset 直通成功 / search fail fallback probe / fetch_ndc_signal Int64+filter / fetch_ndc_leading_index float / 全失敗 graceful
  - **回歸**：tests/ **911 passed**（907 + 4 net）零功能回歸
  - **下一步**：merge 後再按一次 bootstrap → log 會印 search API 真實回應 + 各 candidate ID metadata 結果 → 命中或徹底失敗都能診斷
  - **§5 警鈴**：若本輪仍失敗 → 直接走 C 案（Section 十一 純 TWII drawdown，捨 NDC 驗證），不再嘗試第 5 路徑
- **前一版**：v18.153_NdcOpenApiDiag（NDC URL 多 candidate 全 JSON 失敗 — 加強診斷 log 找根因）
  - **背景**：v18.152 bootstrap 10 個 NDC OpenAPI candidate URL 全回「JSON 解析失敗 line 1 column 1 (char 0)」— proxy 報「成功抓取」表示 HTTP 200 + body 有內容（非空），但首字元非 JSON。推測：NDC SPA 對所有 `/app/data/indicator/*` 路徑回 HTML 首頁（client-side routing fallback）
  - **update_macro_history.py**：`_fetch_ndc_indicator_full` JSON 解析失敗分支新增 `Content-Type` header 與 `body[:300]` dump（從原本只印 exception type → 補出 HTML/JSONP/空 body 的關鍵診斷）
  - **回歸**：tests/ 907 passed 零回歸（log 字串變動，不影響邏輯路徑測試）
  - **下一步**：merge 後 bootstrap 一次 → log 會印出 NDC 實際回什麼 → 我下一輪 PR 依結果選正確路徑（HTML 抓 → 改 data.gov.tw search；JSONP → 加 unwrap；空 → 改 query string 帶參數）
- **前一版**：v18.152_NdcOpenApiMigration（脫離 FinMind 付費牆 → 國發會 NDC OpenAPI 免費直連 + multi-URL probe）
  - **背景**：v18.151 把 dataset 改 `TaiwanBusinessIndicator` 後，bootstrap 撞 HTTP 400 `"Your level is register"` — FinMind 已把 NDC 信號/領先指標歸到 Sponsor 付費 tier（NT$500+/月）。Streamlit live tab `tw_macro.py` 用同 dataset 也壞掉（user 未察覺因為 tab 有 graceful 包覆）
  - **解法**：脫離 FinMind 改打**國發會 NDC OpenAPI**（`https://index.ndc.gov.tw/app/data/indicator/{slug}`）— PMI 已驗證有效（`macro_core._pmi_src_ndc`）+ proxy 白名單已涵蓋（`nas_server.py`），免 token、免錢
  - **update_macro_history.py**：
    - 移除 dead code `_finmind_macro_table` / `_pick_macro_column` / `_MACRO_FULL_TABLE_CACHE`（FinMind 路完全切斷，wide-format helper 不再需要）
    - 新增 `_NDC_SIGNAL_URL_CANDIDATES`（5 個 slug：monitoring / composite / signal / cyclical / SignalScore）+ `_NDC_LEADING_URL_CANDIDATES`（5 個：leading / Leading / lead / LeadingIndex / leadingComposite）+ `_NDC_VALUE_KEYS`（8 個 fallback）+ `_NDC_DATE_KEYS`（7 個）
    - 新增 `_fetch_ndc_indicator_full(candidates, label)` — 逐一 try URL via `proxy_helper.fetch_url`，多 JSON shape parser（list / {data:[]} / {items:[]} / {result.records:[]}）+ verbose log（HTTP code + body 前 200 char）供下一輪鎖死
    - `fetch_ndc_signal(start, end)` + `fetch_ndc_leading_index(start, end)` 取代舊 `fetch_finmind_*`（簽名拿掉 `token` 參數）
    - FETCHERS dict 把兩 dataset 的 `needs_token` 從 True 改 False（v18.152 起 NDC 表完全免 token）
    - Parquet 檔名保留 `finmind_ndc_signal.parquet` / `finmind_leading_index.parquet`（向後相容 macro_validation_tw.py + Section 十一 UI）
  - **tests/test_update_macro_history.py**：替換 11 個 FinMind 測試 → 11 個 NDC 測試（含 _FakeResp helper）：candidate URL constants 完整性 / 第一 URL 成功不打第二 / 第一 fail fallback 第二 / 全 URL fail 回空 / JSON shape={'data':[]} unwrap / yearMonth date key / round + Int64 cast / 範圍 filter / 全 fail graceful
  - **tw_macro.py 暫不動**：本 PR 純 bootstrap fetcher 修復；Streamlit live tab 同樣是 FinMind 來源，留下次 PR 一起遷移到 NDC OpenAPI（避免 hotfix scope 爆量）
  - **回歸**：tests/ **907 passed** 零回歸
  - **下一步**：merge 後請手動觸發 `update_macro_history.yml` workflow_dispatch + bootstrap=true / years=25 → bootstrap log 會印每個 candidate URL 的 HTTP 狀態 → 第一個 work 的 URL 自動寫進 Parquet；若全失敗，貼 log 給我 → 下一輪 PR 加新 candidate / 改用 data.gov.tw dataset ID fallback
- **前一版**：v18.151_FinMindBusinessIndicatorFix（FinMind v4 dataset 改名 hotfix — bootstrap 失敗復原）
  - **背景**：v18.149 用 dataset `TaiwanMacroEconomics` 抓 NDC + 領先指標，bootstrap 時 FinMind 回 HTTP 422 enum invalid（dataset 已被廢除/改名）→ NDC/領先 Parquet 0 rows → Section 十一驗證無資料可看
  - **根因**：FinMind v4 把 NDC monitoring + 領先 leading 合併打包成 wide-format dataset `TaiwanBusinessIndicator`（欄位：date / leading / leading_notrend / coincident / coincident_notrend / lagging / lagging_notrend / monitoring）
  - **update_macro_history.py**：dataset 改 `TaiwanBusinessIndicator`；移除 dead code `_filter_macro_indicator` + `NDC_SIGNAL_KEYS` + `LEADING_INDEX_KEYS`（wide-format 不需 long-format indicator filter）；新增 `_pick_macro_column(df, col)` wide-format 抽欄輔助；`fetch_finmind_ndc_signal` 改抓 `monitoring`、`fetch_finmind_leading_index` 改抓 `leading`；`_finmind_macro_table` cache 機制不變（NDC + leading 仍共用一次 API call）
  - **tests/test_update_macro_history.py**：移除 5 個 `_filter_macro_indicator` 測試 + 改寫 4 個 fetcher / cache 測試以 wide-format 為 fixture；新增 3 個 `_pick_macro_column` 測試（基本抽欄 + NaN drop / 缺欄 graceful / 空輸入）+ 1 個 dataset 名稱驗證測試（防 FinMind 再次改名時無感）
  - **回歸**：tests/ **907 passed**（v18.150 909 - 5 obsolete + 3 new = 907）零功能回歸
  - **下一步**：請手動觸發 `update_macro_history.yml` workflow_dispatch + bootstrap=true / years=25 → 應抓到 ~300 月 NDC + 領先資料（1984 起算）→ Section 十一可看 7 場 crisis 命中表 → 接 Phase E 跨來源比對
- **前二版**：v18.150_MacroBacktestUI（macro tab section 十一 — 歷史驗證 UI，鏡像 fund Phase 6a）
  - **User 需求**：「兩邊都可以做回測來驗證台股的總經 tab 與基金（全球的）總經 tab」。Sister repo my-Fund-dashboard 已於 v18.276 Phase B.2 把 Parquet 接到 Phase 6a 驗證 UI；本 PR 為台股對等版本，讀 PR #149 v18.149 鋪好的 NDC + 領先指標 Parquet + 既有 twii_ohlcv → 雙指標 × TWII crisis 命中表
  - **macro_validation_tw.py**（~270 行 pure data）：3 個 Parquet loader（NDC / 領先 / TWII close）+ `detect_twii_crisis_events`（high-water-mark walk-forward，與 fund crisis_backtest 同範式，預設 20% 回撤）+ `verify_ndc_signal_vs_crises`（峰前 N 月 → 峰時 NDC 下降 ≥ drop_pts 分；預設 4 分 ≈ 1 燈號跨度）+ `verify_leading_index_vs_crises`（peak 月 6M smoothed change < 0 即命中）+ `compute_smoothed_change_pct`（與 tw_macro.fetch_ndc_leading_index 一致 rolling 6 月 MA → pct_change × 100）+ `compute_hit_rate` 統計卡 helper
  - **tab_macro_validation.py**（~240 行 Streamlit UI）：`render_history_validation_section(cache_dir)` — Section 十一 渲染；缺檔友善 banner 引導 user 等 cron；3 sliders（TWII 回撤門檻 10-40% / lead_months 3-12 / NDC drop_pts 2-10）+ 跑按鈕 + 2 張命中率卡 + plotly 雙軸圖（TWII 主軸 + NDC 副軸 + crisis 紅區）+ NDC/領先指標雙命中表 + 2 個 CSV 下載按鈕（utf-8-sig BOM 解 Excel）
  - **tab_macro.py**：插 11 行（section 十之後，結尾 `<hr>` 之前）— `from tab_macro_validation import render_history_validation_section` + 呼叫，try/except 包覆避免 Parquet 缺檔影響整 tab 渲染
  - **tests/test_macro_validation_tw.py**（29 case 全綠）：Parquet 讀取（缺檔 / 正常解析 / 壞檔 graceful）/ detect_twii_crisis_events（v 型 / 門檻下無事件 / 空 / 過短 / open-ended 無 recovery / 多事件）/ verify_ndc（命中 exact 4 pts / 明確 8 pts / 無事件 / 空 series）/ compute_smoothed_change（常數 → 0% / 線性升全正 / 空 input）/ verify_li（peak 時 smooth<0 命中 / 單調升不命中 / 空 input）/ compute_hit_rate（mixed / empty / all None drop_pts）/ UI source-level（tab_macro.py import 與呼叫 + 模組 export render fn + section 順序在 10 之後）
  - **回歸**：tests/ **909 passed** 零回歸（880 + 29 新）
  - Roadmap：Phase D（PMI 補抓進 Parquet）→ Phase E（fund + stock 同框跨來源比對工具）
- **前三版**：v18.149_NdcLeadingHistoryFetchers（macro_history 擴 2 新表：景氣對策信號 + 領先指標）
  - **User 需求**：「兩邊都可以做回測來驗證台股的總經 tab 與基金（全球的）總經 tab」+ 「直接抓取資料放在資料庫，之後每周定期更新」→ 沿用 PR #144 已建的 Parquet 快取模式（非 SQLite），補上**驗證總經 Tab 的關鍵 2 指標**：NDC 景氣對策信號（拐點偵測 9-45 分）+ 領先指標綜合指數（6M smoothed change 領先期）
  - **update_macro_history.py**：新增 `fetch_finmind_ndc_signal` + `fetch_finmind_leading_index`（都打 FinMind TaiwanMacroEconomics dataset，模組級 `_MACRO_FULL_TABLE_CACHE` 字典快取讓兩 fetcher 共用一次 API call）；新增 `_finmind_macro_table()` 全表抓取（含 cache）+ `_filter_macro_indicator()` 指標篩選工具（支援 indicator/name/metric × value/data 欄位變體 + exact→contains 兩層 fallback）；DATASETS 從 4 表擴 6 表，FETCHERS dict 同步註冊（皆需 FINMIND_TOKEN）
  - **新增 Parquet**：`data_cache/finmind_ndc_signal.parquet`（columns: date, ndc_signal Int64）+ `data_cache/finmind_leading_index.parquet`（columns: date, leading_index float）；領先指標**只存原始值**，6M smoothed change 由分析端 on-the-fly 算（rolling 6 月需 lookback context，fetcher 增量時無法正確算）
  - **tests/test_update_macro_history.py**（+12 case = 19 全綠）：DATASETS 註冊驗證 / `_filter_macro_indicator` exact match / contains fallback / no match / empty input / missing columns / 替代欄位名（metric+data）/ macro_table_cache 同 key 不重打 / fetch_ndc_signal shape（含 Int64 + round）/ fetch_leading_index shape / fetch 找不到時回空 / NDC + LI 共用同一次 API call cache
  - **回歸**：tests/ **880 passed** 零回歸
  - Roadmap：Phase 2（macro tab 加歷史驗證 UI 子區塊 — 讀 Parquet 算月度 score、對齊 TWII crisis hit/miss）→ Phase 3（fund-dashboard FRED 9 指標 Parquet cache）
- **前三版**：v18.148 代碼淨化收尾（#148 — v5_modules / v4_strategy_engine / nas_server unused vars/imports 移除，0 邏輯改動）
- **前四版**：v18.147 macro-history 守護鏈強化（#144-#147）
  - **#144**：總經歷史資料快取層 + 每日增量 Actions（解 TWII-only 結構性偏弱）。data_cache/ Parquet 表 git 追蹤——twii_ohlcv / finmind_inst / finmind_margin / finmind_m1m2 + metadata.json。`update_macro_history.py` 無 streamlit 相依爬蟲，增量 append+dedupe；workflow 每日 UTC 09:00 + bootstrap mode；`calibrate_macro_traffic.py` 加 load_from_cache + _enrich_with_finmind 注入 FinMind 欄位
  - **#145-#147**：production hardening——FinMind 改直連印 HTTP status、三大法人英文 name 篩 'Foreign'、CBC M1B/M2 三 URL 備援 + SDMX EF15M01 fallback
- **前四版**：紅綠燈門檻校準走 Walk-Forward + 季度排程（反過擬合）
  - **calc_traffic_light**：加 `health_defense_threshold` / `bull_min_score` optional kwargs（不影響 production，純供校準注入）
  - **macro_helpers.py**：module load 時優先讀 `macro_thresholds.json`，缺檔/越界 silently fall back module 常數；越界守門 H∈[20,60]、S∈[1,6]
  - **calibrate_macro_traffic.py**：新增 `walk_forward_validate(df, n_folds=4)`——滾動切折、每折 train 找最佳門檻 grid (H∈[25,45] step 2 × S∈[2,5])、test 報告 OOS、目標函數含「偏離現行常數」正則項；新增 `evaluate_thresholds` / `grid_search_thresholds` / `emit_thresholds_json` / `build_proposal_report`；CLI 加 `--optimize --emit-json --emit-proposal --n-folds`
  - **過擬合三重保護**：(a) walk-forward 永不在 train 評分 (b) 票選 + drift>30% 過半即回退預設 (c) 季度排程開 PR 給人類審閱、不自動 merge
  - **`.github/workflows/recalibrate_macro.yml`**：季度排程（1/4/7/10 月 1 號）+ workflow_dispatch（input range/n_folds），需 Secrets `PROXY_URL` + `FINMIND_TOKEN`，跑完開 PR with proposal as body
  - **calibration_ui.py**：UI 加 `_show_threshold_status()` 顯示現行門檻 + 最後校準時間戳 + 方法
  - **tests/test_calibrate_walkforward.py**：8 個 smoke（合成資料 walk-forward 不 raise、過擬合保護回退、threshold 覆寫單調性、JSON 讀寫一致、正則項懲罰偏離）全綠
- **前五版**：資料異常清單 2 筆續修——PMI / 出口 YoY 新源（CIER 英文月度頁 + stat.gov.tw），全走 NAS 中繼站
  - **PMI**：新增 `_pmi_src_cier_en_monthly`（macro_core.py）為最高優先源，直接打 CIER 英文月度 slug `/en/eco/taiwan-manufacturing-pmi-{月}-{年}/`，每次嘗試 current/-1/-2 月共 3 個 slug；CIER 是官方發布單位、slug 結構自 2024 起穩定、HTML 乾淨命中率 >95%。9 源 → 10 源並行，全走 `fetch_url`（NAS Squid → 直連 → NAS 中繼站 fallback）
  - **出口 YoY**：在 `tab_macro.py:_fetch_export()` 最前面插方案 0 stat.gov.tw（DGBAS 出口年增率頁 `Point.aspx?sid=t.8&n=3587&sms=11480`），HTML 抓「YYYY年M月 出口年增率 XX.X%」格式；與 #141 的 FRED series 校正 + data.gov.tw 6053 互補形成 6 源備援鏈
  - 根因：海外 IP 雖透過 `fetch_url` 三層 fallback 過 NAS，但**舊源 URL 結構失效**（CIER `/news/list?cid=21` 列表改、MOF 月度 CSV 路徑變動）→ 全鏈路滑落到靜態備援（出口 92 天前）或全失敗（PMI 未取得）
- **前六版**：v18.142 資料抓取器 series id / endpoint 校正（#141）
  - **美國核心 CPI**：誤用 `CPIAUCSL`（總體）→ 改 `CPILFESL`（核心）；新增方案 0 `fredgraph.csv` 無需 API key；BLS fallback 改 `CUSR0000SA0L1E`
  - **台灣出口 YoY**：FRED `VALEXPTWM052N`（IFS 延遲 13 月）→ 改 `XTEXVA01TWM664S`（OECD MEI 延遲 2-3 月）；新增 data.gov.tw dataset 6053 海關進出口貿易統計方案（走 NAS proxy）
  - tab_edu.py + data_registry.py 同步 identifier；新增 10 個 source-level regression tests；tests/ 850 passed
- **前七版**：紅綠燈校準誠實化 + 門檻收斂（#137-#140）
  - **#140**：校準報告加 TWII-only 警語 banner、建議讀現行常數（`HEALTH_DEFENSE_THRESHOLD=35` / `BULL_MIN_SCORE=4`）
  - **#139**：5Y TWII 校準顯示防禦 precision 14.8%、多頭 30.7% → `calc_traffic_light` 防禦 `_health < 40` 改 `<35`、bull 加 `_score >= 4` 條件
  - **#137-#138**：校準 Streamlit UI Tab + 比較×排行卡片載入修
- **前八版**：ETF 經理人換手偵測持久化（#131-#132）
  - `update_etf_managers.py`（獨立爬蟲走 proxy_helper）+ workflow + watchlist/managers JSON；`track_etf_manager_change` 改以 repo 持久檔為主、`/tmp` 降次要，解決雲端重啟即清空→紅框不跳
  - 沿革：#119 PMI/NAV/費用率改走 fetch_url → #122 主動式 ETF 中文名 + NAV 改 Basic0003 → #124 fetch_etf_meta_moneydj（Basic0004 一次取齊）→ #126 經理人異動偵測 → #127 折溢價直讀 MoneyDJ Basic0001 + 費用率破「經理費(%)」陷阱 → #129 折溢價假日對齊最後交易日 → **#131** 經理人換手持久化 → **#132** ETF watchlist 擴充至 28 檔
  - ⚠️ **前提**：MoneyDJ 擋海外 IP，Streamlit App secrets 須設 `PROXY_URL`（家用 NAS 台灣 IP）這些 ETF 欄位才有值，否則 N/A
- **Secrets**：`FINMIND_TOKEN` · `GEMINI_API_KEY[_2..6]` · `PROXY_URL` · `FRED_API_KEY` ·（選配 `NAS_BASE_URL` / `NAS_API_KEY`）

## 模組分層（PR #58–#73 大重構後）
```
UI 層         app.py（1378 行入口） · tab_macro / tab_stock / tab_stock_grp / tab_edu
             etf_dashboard（49 行 shim）→ etf_tab_single / _portfolio / _backtest / _ai
             ui_widgets.py（9 個純 HTML 函式）
共用層        tab_helpers.py · macro_helpers.py · etf_helpers.py · tech_indicators.py
資料抓取      data_loader · macro_core · tw_macro · daily_checklist · tw_stock_data_fetcher
ETF 三層      etf_fetch（I/O） · etf_calc（計算） · etf_render（UI）
引擎          scoring_engine / financial_health_engine / market_strategy / risk_control
             backtest_engine / unified_decision / v4_strategy_engine / v5_modules
             yield_screener / flow_engine / exit_signals
AI / 警示     ai_engine · macro_alert · macro_state_locker · persona
雲端 / 基建   gsheet_portfolio · oauth_state · infra/oauth · proxy_helper · nas_server
熱錢監測      hot_money.py（三角交叉：外資 × 匯率 × 背離）
```

## 測試
- `pytest tests/` + `pytest test_*.py`
- 核心 smoke：`tests/test_hot_money.py`、`test_app_step4.py`、`test_market_strategy.py`

## 配置
- `requirements.txt` — runtime；`.streamlit/config.toml` — UI 設定
- 分支：開發於 `claude/etf-portfolio-download-CKR5h`，主幹 `main`
